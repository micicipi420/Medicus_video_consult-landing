---
phase: quick
plan: 260410-014
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/liquid-glass.css
  - js/main.js
  - partials/svg-defs.html
  - index.html
  - online-consultations.html
  - treatment-abroad.html
  - checkup.html
  - contacts.html
  - 404.html
  - styleguide.html
  - css/styles.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Liquid glass cards show a specular rim-light along top edge"
    - "Liquid cards display an animated glint border that sweeps continuously"
    - "Mouse movement over liquid-card elements creates a specular highlight that follows the cursor"
    - "SVG refraction filter uses updated turbulence values for stronger displacement"
    - "Dark mode reduces rim-light and glint opacity"
    - "prefers-reduced-motion disables glint animation"
    - "Header backdrop class available for extended blur technique"
  artifacts:
    - path: "src/styles/liquid-glass.css"
      provides: "Rim-lights, glint border, header-backdrop, mouse-reactive ::after, @keyframes glint"
      contains: "@keyframes glint"
    - path: "js/main.js"
      provides: "Mouse-tracking specular on .liquid-card elements"
      contains: "initMouseSpecular"
    - path: "partials/svg-defs.html"
      provides: "Updated SVG refraction filter with feGaussianBlur"
      contains: "feGaussianBlur"
    - path: "css/styles.css"
      provides: "Rebuilt Tailwind output with all new CSS"
  key_links:
    - from: "src/styles/liquid-glass.css"
      to: "css/styles.css"
      via: "Tailwind build pipeline"
      pattern: "@keyframes glint"
    - from: "js/main.js"
      to: "src/styles/liquid-glass.css"
      via: "--mouse-x/--mouse-y custom properties"
      pattern: "--mouse-x.*--mouse-y"
    - from: "partials/svg-defs.html"
      to: "*.html"
      via: "build-pages.sh splicer"
      pattern: "liquid-refract"
---

<objective>
Maximize Liquid Glass fidelity by implementing specular rim-lights, animated glint borders, mouse-tracking specular highlights, extended header backdrop, and an upgraded SVG refraction filter.

Purpose: Push glass material to maximum visual impact with all remaining CSS/JS effects.
Output: Updated liquid-glass.css with new effects, js/main.js with mouse tracking, rebuilt Tailwind CSS, and updated SVG filter propagated to all HTML pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/styles/liquid-glass.css
@src/styles/squircles.css
@js/main.js
@partials/svg-defs.html
@src/styles/tailwind.css

<interfaces>
<!-- Existing liquid glass classes and structure in liquid-glass.css -->

.liquid-regular — base glass material (isolation, backdrop-filter, box-shadow)
.liquid-card — card glass with position: relative already set, ::after specular pseudo-element
.liquid-btn-secondary — secondary button glass
.stats-glass — grouped stats backdrop
.liquid-card-wrap — deprecated no-op wrapper

Dark mode uses `.dark` class selector (defined via @custom-variant in theme.css).
Section 7: html[data-refract="true"] selectors for SVG filter progressive enhancement.
Section 10: prefers-reduced-motion media query.
Section 11: prefers-reduced-transparency media query.

<!-- JS module pattern in main.js -->
All functions defined inside IIFE, called from initAll().
window.MU.reinitPageContent() re-inits page-content modules after SPA navigation.
initRefractionProbe() already exists at line 533 — uses CSS.supports.

<!-- Build system -->
partials/svg-defs.html gets spliced into all HTML via scripts/build-pages.sh.
Tailwind rebuild: npx @tailwindcss/cli -i src/styles/tailwind.css -o css/styles.css --minify
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specular rim-lights, animated glint border, and header backdrop class</name>
  <files>src/styles/liquid-glass.css</files>
  <action>
Add the following to src/styles/liquid-glass.css. Insert new sections BEFORE Section 7 (refraction), renumbering subsequent sections.

**Section 7 — Specular rim-lights:**

Add `position: relative` to .liquid-regular, .liquid-btn-secondary, and .stats-glass (note: .liquid-card already has it at line 77).

Add a `::before` pseudo-element to .liquid-regular, .liquid-btn-secondary, and .stats-glass for a top-edge rim-light:
```css
.liquid-regular::before,
.liquid-btn-secondary::before,
.stats-glass::before {
  content: '';
  position: absolute;
  top: 0;
  left: 10%;
  right: 10%;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.6), transparent);
  pointer-events: none;
  z-index: 1;
}
```

**Section 8 — Animated glint border (liquid-card only):**

Add a `::before` pseudo-element to .liquid-card for the glint border animation. The card does NOT get a rim-light (the glint covers the rim visually):
```css
.liquid-card::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  padding: 2px;
  background: linear-gradient(45deg, rgba(255,255,255,0) 30%, rgba(255,255,255,0.8) 50%, rgba(255,255,255,0) 70%);
  background-size: 200% 100%;
  animation: glint 6s linear infinite;
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
  pointer-events: none;
  z-index: 1;
}

@keyframes glint {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

**Section 9 — Header backdrop (Josh Comeau extended technique):**

```css
.liquid-header-backdrop {
  position: absolute;
  inset: 0;
  height: 200%;
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  mask-image: linear-gradient(to bottom, black 0% 50%, transparent 50% 100%);
  -webkit-mask-image: linear-gradient(to bottom, black 0% 50%, transparent 50% 100%);
  pointer-events: none;
  z-index: -1;
}
```

**Dark mode additions** — add to the existing `.dark` section (near current line 312-314):
```css
.dark .liquid-regular::before,
.dark .liquid-btn-secondary::before,
.dark .stats-glass::before {
  opacity: 0.3;
}

.dark .liquid-card::before {
  opacity: 0.5;
}
```

**prefers-reduced-motion addition** — add inside the existing Section 10 @media block:
```css
.liquid-card::before {
  display: none;
}
```

**prefers-reduced-transparency addition** — add inside the existing Section 11 @media block:
```css
.liquid-regular::before,
.liquid-btn-secondary::before,
.stats-glass::before,
.liquid-card::before {
  display: none;
}
```

Renumber all subsequent sections (old Section 7 Refraction becomes Section 10, etc.) and update their comment headers accordingly.
  </action>
  <verify>
    <automated>grep -c "@keyframes glint" src/styles/liquid-glass.css | grep -q "1" && grep -c "liquid-header-backdrop" src/styles/liquid-glass.css | grep -q "1" && grep -c "mask-composite: exclude" src/styles/liquid-glass.css | grep -q "1" && echo "PASS: All new CSS sections present"</automated>
  </verify>
  <done>
    - .liquid-regular, .liquid-btn-secondary, .stats-glass have position: relative and ::before rim-light
    - .liquid-card has ::before glint border with @keyframes glint animation
    - .liquid-header-backdrop class exists with extended backdrop technique
    - Dark mode reduces rim-light to 0.3 opacity, glint to 0.5
    - prefers-reduced-motion hides glint ::before
    - prefers-reduced-transparency hides all ::before pseudo-elements
    - All sections renumbered cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Mouse-tracking specular highlight and update ::after gradient</name>
  <files>src/styles/liquid-glass.css, js/main.js</files>
  <action>
**CSS change — update .liquid-card::after** (currently at line 87-99 in src/styles/liquid-glass.css):

Replace the existing static radial-gradient in .liquid-card::after with a mouse-reactive version using CSS custom properties:
```css
.liquid-card::after {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  background: radial-gradient(
    ellipse at var(--mouse-x, 30%) var(--mouse-y, 0%),
    rgba(255, 255, 255, 0.15) 0%,
    transparent 60%
  );
  pointer-events: none;
  z-index: 1;
}
```

The default values (30%, 0%) provide a reasonable static fallback when JS hasn't set the properties yet.

**JS change — add initMouseSpecular() to js/main.js:**

Add a new function BEFORE initAll() (around line 542):
```js
/**
 * Mouse-tracking Specular Highlight
 * Sets --mouse-x/--mouse-y CSS custom properties on .liquid-card elements
 * so the ::after radial-gradient follows the cursor position.
 * Desktop-only: uses mousemove. Touch devices use CSS default (30%, 0%).
 */
function initMouseSpecular() {
  // Skip on touch-primary devices — no mousemove events worth tracking
  if (window.matchMedia && window.matchMedia('(pointer: coarse)').matches) return;
  // Respect reduced motion
  if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

  document.addEventListener('mousemove', function(e) {
    var cards = document.querySelectorAll('.liquid-card');
    for (var i = 0; i < cards.length; i++) {
      var rect = cards[i].getBoundingClientRect();
      var x = ((e.clientX - rect.left) / rect.width * 100);
      var y = ((e.clientY - rect.top) / rect.height * 100);
      cards[i].style.setProperty('--mouse-x', x + '%');
      cards[i].style.setProperty('--mouse-y', y + '%');
    }
  });
}
```

Add `initMouseSpecular();` call inside initAll() — place it right after `initRefractionProbe();` (line 544).

Do NOT add to reinitPageContent() — mousemove listener is document-level, only needs one registration.

Expose on window.MU: add `window.MU.initMouseSpecular = initMouseSpecular;` alongside the other exports (line 573).
  </action>
  <verify>
    <automated>grep -c "initMouseSpecular" js/main.js | grep -q "[2-9]" && grep -c "\-\-mouse-x" src/styles/liquid-glass.css | grep -q "1" && echo "PASS: Mouse specular wired in JS and CSS"</automated>
  </verify>
  <done>
    - .liquid-card::after uses var(--mouse-x, 30%) var(--mouse-y, 0%) for gradient position
    - initMouseSpecular() in js/main.js registers a mousemove listener on document
    - Listener updates --mouse-x/--mouse-y on all .liquid-card elements
    - Skips on touch-primary devices (pointer: coarse)
    - Skips when prefers-reduced-motion is active
    - Function called from initAll(), exposed on window.MU
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SVG refraction filter and rebuild</name>
  <files>partials/svg-defs.html, css/styles.css</files>
  <action>
**Update partials/svg-defs.html** — replace the entire content with the upgraded SVG refraction filter that includes feGaussianBlur for smoother displacement:
```html
  <svg width="0" height="0" style="position:absolute" aria-hidden="true">
    <defs>
      <filter id="liquid-refract" color-interpolation-filters="sRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.008" numOctaves="2" seed="92" result="noise"/>
        <feGaussianBlur in="noise" stdDeviation="2" result="blurred"/>
        <feDisplacementMap in="SourceGraphic" in2="blurred" scale="30" xChannelSelector="R" yChannelSelector="G"/>
      </filter>
    </defs>
  </svg>
```

Key changes from old filter:
- baseFrequency: 0.02 -> 0.008 (larger, smoother noise pattern)
- Added `color-interpolation-filters="sRGB"` for correct color handling
- Added `result="noise"` attribute to feTurbulence for named piping
- Added feGaussianBlur (stdDeviation="2") to smooth the noise before displacement
- scale: 8 -> 30 (much stronger displacement for visible refraction effect)
- seed: 4 -> 92 (different noise pattern)
- Added `in2="blurred"` to feDisplacementMap (uses blurred noise instead of raw)

**Propagate to all HTML pages:**
```bash
sh scripts/build-pages.sh
```
This splices the updated partial into all 7 HTML pages via BUILD:svg-defs markers.

**Rebuild Tailwind CSS** to compile all new liquid-glass.css additions:
```bash
npx @tailwindcss/cli -i src/styles/tailwind.css -o css/styles.css --minify
```
  </action>
  <verify>
    <automated>grep -c "feGaussianBlur" partials/svg-defs.html | grep -q "1" && grep -c "feGaussianBlur" index.html | grep -q "1" && grep -c "@keyframes glint" css/styles.css | grep -q "1" && echo "PASS: SVG filter updated, pages rebuilt, Tailwind compiled"</automated>
  </verify>
  <done>
    - partials/svg-defs.html contains upgraded filter with feGaussianBlur and scale="30"
    - All 7 HTML pages updated via build-pages.sh with new SVG filter
    - css/styles.css rebuilt with all new liquid-glass.css additions (rim-lights, glint, header-backdrop, mouse-reactive ::after)
    - No build errors
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `grep -c "@keyframes glint" css/styles.css` returns 1 (glint compiled into output)
2. `grep -c "liquid-header-backdrop" css/styles.css` returns 1 (header backdrop compiled)
3. `grep -c "feGaussianBlur" index.html` returns 1 (SVG filter propagated)
4. `grep -c "initMouseSpecular" js/main.js` returns 3+ (function def, call in initAll, MU export)
5. `grep -c "mask-composite" css/styles.css` returns 1+ (glint border mask compiled)
6. Open index.html in browser: cards show animated glint border sweeping across edges, mouse movement shifts specular highlight on cards
</verification>

<success_criteria>
- All 5 new effects implemented: rim-lights, glint border, header backdrop class, mouse-tracking specular, upgraded SVG refraction
- Dark mode reduces effect intensities
- Reduced motion disables glint animation
- Reduced transparency hides all pseudo-element effects
- Touch devices skip mouse-tracking (no wasted event listeners)
- All HTML pages have the updated SVG filter
- Tailwind CSS rebuilt successfully with all new rules
</success_criteria>

<output>
After completion, create `.planning/quick/260410-014-maximize-liquid-glass-fidelity-with-all-/260410-014-SUMMARY.md`
</output>
