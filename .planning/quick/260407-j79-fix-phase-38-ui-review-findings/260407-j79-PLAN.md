---
phase: 260407-j79-fix-phase-38-ui-review-findings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contacts.html
  - 404.html
  - treatment-abroad.html
  - index.html
autonomous: true
requirements:
  - UIREV38-01
  - UIREV38-02
  - UIREV38-03
must_haves:
  truths:
    - "contacts.html <main> applies lg:pt-section-pt-lg on desktop (10rem top padding, matching content pages)"
    - "404.html <main> applies lg:pt-section-pt-lg on desktop (10rem top padding, matching content pages)"
    - "treatment-abroad.html hero inner container no longer has mb-16 (hero bottom gap = 4rem, not 8rem)"
    - "index.html hero H1 caps at xl:text-7xl (text-8xl no longer appears on this H1)"
    - "Site-wide text-8xl usage reduced from 2 → 1 (only 404.html '404' glyph retains it)"
  artifacts:
    - path: "contacts.html"
      provides: "compact-tier main with desktop rhythm boost"
      contains: "lg:pt-section-pt-lg"
    - path: "404.html"
      provides: "compact-tier main with desktop rhythm boost"
      contains: "lg:pt-section-pt-lg"
    - path: "treatment-abroad.html"
      provides: "hero section with correct single-source bottom padding"
      contains: 'class="container mx-auto px-4 lg:px-6"'
    - path: "index.html"
      provides: "hero H1 capped at text-7xl at xl breakpoint"
      contains: "lg:text-7xl xl:text-7xl"
  key_links:
    - from: "contacts.html:106"
      to: "src/styles/theme.css (--spacing-section-pt-lg token)"
      via: "lg:pt-section-pt-lg utility"
      pattern: "lg:pt-section-pt-lg"
    - from: "404.html:103"
      to: "src/styles/theme.css (--spacing-section-pt-lg token)"
      via: "lg:pt-section-pt-lg utility"
      pattern: "lg:pt-section-pt-lg"
---

<objective>
Fix three surgical findings from the Phase 38 UI review (20/24 audit). All three are line-level edits using existing Tailwind v4 utilities already compiled in css/styles.css — no token changes, no theme changes, no new CSS.

Purpose: Close the Phase 38 UI review gaps so v3.1 milestone audit shows a clean post-fix score. Restores cross-page desktop rhythm on contacts/404, removes double-padding on treatment-abroad hero, and tightens the font-size scale for TA 45+ by dropping the single stray xl:text-8xl on index hero H1.

Output: 4 HTML files modified, 3 findings closed, grep verification proves each edit landed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/38-vertical-rhythm-section-sizing/38-UI-REVIEW.md

<interfaces>
<!-- All utilities below exist in the compiled css/styles.css from Phase 38. -->
<!-- No new CSS is generated by this plan. -->

From src/styles/theme.css (Phase 38 rhythm tokens — already compiled):
- pt-section-pt      → padding-top: 8rem
- pt-section-pt-lg   → padding-top: 10rem  (applied via lg:pt-section-pt-lg at ≥1024px)
- pb-section-pb      → padding-bottom: 4rem

Existing class strings on target lines (exact, verified by Read tool):

contacts.html:106 (current):
  <main id="page-content" class="relative z-10 min-h-section-hero-compact pt-section-pt pb-[calc(7rem+env(safe-area-inset-bottom))] lg:pb-section-pb">

404.html:103 (current):
  <main id="page-content" class="relative z-10 pt-section-pt pb-section-pb min-h-section-hero-compact flex items-center justify-center">

treatment-abroad.html:121 (current):
  <div class="container mx-auto px-4 lg:px-6 mb-16">

index.html:209 (current):
  <h1 class="text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-extrabold mb-6 leading-[1.1] tracking-tight">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lg:pt-section-pt-lg to compact-tier mains (contacts.html + 404.html)</name>
  <files>contacts.html, 404.html</files>
  <action>
Restore cross-page desktop rhythm on the two compact-tier pages. Both currently use only pt-section-pt (8rem) at all breakpoints; all four content-page heroes apply lg:pt-section-pt-lg (10rem) on desktop. Add the utility to both.

Use Edit tool with these exact replacements:

**contacts.html — line 106:**
  old_string:
    `<main id="page-content" class="relative z-10 min-h-section-hero-compact pt-section-pt pb-[calc(7rem+env(safe-area-inset-bottom))] lg:pb-section-pb">`
  new_string:
    `<main id="page-content" class="relative z-10 min-h-section-hero-compact pt-section-pt lg:pt-section-pt-lg pb-[calc(7rem+env(safe-area-inset-bottom))] lg:pb-section-pb">`

  Rationale for insertion point: place lg:pt-section-pt-lg immediately after pt-section-pt so the mobile→desktop top-padding progression reads as a pair, matching the four content-page heroes (e.g., index.html:198 has `pt-section-pt pb-section-pb lg:pt-section-pt-lg` — we use the adjacent-pair ordering here because contacts.html's pb utilities are already compound).

**404.html — line 103:**
  old_string:
    `<main id="page-content" class="relative z-10 pt-section-pt pb-section-pb min-h-section-hero-compact flex items-center justify-center">`
  new_string:
    `<main id="page-content" class="relative z-10 pt-section-pt lg:pt-section-pt-lg pb-section-pb min-h-section-hero-compact flex items-center justify-center">`

Do NOT touch any other classes, do NOT reorder unrelated utilities, do NOT run a reformatter over the file. This is a two-string replacement only.

Closes UIREV38-01 (priority finding #1 from 38-UI-REVIEW.md).
  </action>
  <verify>
<automated>grep -c 'lg:pt-section-pt-lg' contacts.html 404.html | grep -E ':(1|[2-9])$' &amp;&amp; echo "PASS: both files contain lg:pt-section-pt-lg"</automated>
Additional manual grep checks:
- `grep -n 'pt-section-pt lg:pt-section-pt-lg' contacts.html` → must return line 106
- `grep -n 'pt-section-pt lg:pt-section-pt-lg' 404.html` → must return line 103
- `grep -c 'lg:pt-section-pt-lg' contacts.html` → 1
- `grep -c 'lg:pt-section-pt-lg' 404.html` → 1
  </verify>
  <done>
- contacts.html line 106 &lt;main&gt; element contains `pt-section-pt lg:pt-section-pt-lg` (adjacent pair)
- 404.html line 103 &lt;main&gt; element contains `pt-section-pt lg:pt-section-pt-lg` (adjacent pair)
- No other classes on either line are modified
- grep returns exactly 1 match per file for `lg:pt-section-pt-lg`
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove stray mb-16 from treatment-abroad hero inner container</name>
  <files>treatment-abroad.html</files>
  <action>
The hero section at line 120 already applies `pb-section-pb` (4rem = 64px). The inner container at line 121 adds another `mb-16` (4rem), producing 128px effective bottom gap before the next section — double the intended rhythm. No other page has this inner mb-16 on its hero container.

IMPORTANT: Only the HERO'S inner container on line 121 should have mb-16 removed. All subsequent `<section>` bodies in treatment-abroad.html (the process, step cards, pricing, etc.) use `mb-16` on their own inner containers as an inherited body-section spacing pattern — do NOT touch those.

Use Edit tool with this exact replacement (the `replace_all` flag must be FALSE — we need exactly one replacement):

  old_string:
    `<section class="min-h-section-hero-medium flex items-center justify-center pt-section-pt pb-section-pb lg:pt-section-pt-lg" id="hero-abroad">
      <div class="container mx-auto px-4 lg:px-6 mb-16">`
  new_string:
    `<section class="min-h-section-hero-medium flex items-center justify-center pt-section-pt pb-section-pb lg:pt-section-pt-lg" id="hero-abroad">
      <div class="container mx-auto px-4 lg:px-6">`

Using a two-line old_string that anchors on the hero `<section id="hero-abroad">` guarantees uniqueness — only the hero's inner container matches this sequence, so other mb-16 containers in the file are untouched.

Closes UIREV38-02 (priority finding #2 from 38-UI-REVIEW.md).
  </action>
  <verify>
<automated>grep -n 'id="hero-abroad"' treatment-abroad.html &amp;&amp; sed -n '120,122p' treatment-abroad.html | grep -c 'mb-16' | grep -q '^0$' &amp;&amp; echo "PASS: hero inner div has no mb-16"</automated>
Additional checks:
- `sed -n '121p' treatment-abroad.html` → must show `<div class="container mx-auto px-4 lg:px-6">` (no mb-16)
- `grep -c 'mb-16' treatment-abroad.html` → count should decrease by exactly 1 from the pre-edit count (confirms we only removed one instance, not all)
- `grep -n 'hero-abroad' treatment-abroad.html` → still exists at line 120 (section intact)
  </verify>
  <done>
- treatment-abroad.html line 121 reads `<div class="container mx-auto px-4 lg:px-6">` (mb-16 removed)
- The hero `<section id="hero-abroad">` on line 120 is unchanged
- mb-16 instances on non-hero containers later in the file are unchanged (verify via grep count delta of exactly -1)
  </done>
</task>

<task type="auto">
  <name>Task 3: Drop xl:text-8xl → xl:text-7xl on index.html hero H1</name>
  <files>index.html</files>
  <action>
The site-wide font-size scale has 12 distinct steps; text-8xl appears exactly once as a type step (on index.html:209 hero H1 at the xl breakpoint). For a TA 45+ audience, dropping xl:text-8xl to xl:text-7xl consolidates the scale without hurting hero impact — the lg:text-7xl breakpoint already establishes the largest size, and the xl step simply maintains rather than escalates it (effectively making the H1 cap at text-7xl on large viewports).

Note: the `text-8xl` usage on 404.html:105 (the decorative "404" glyph) is INTENTIONAL — it's a display treatment for the error code, not a type-scale step. Leave it alone. This task only touches index.html:209.

Use Edit tool with this exact replacement:

  old_string:
    `<h1 class="text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-extrabold mb-6 leading-[1.1] tracking-tight">`
  new_string:
    `<h1 class="text-5xl md:text-6xl lg:text-7xl xl:text-7xl font-extrabold mb-6 leading-[1.1] tracking-tight">`

Why keep `xl:text-7xl` explicitly instead of just deleting it: retaining the breakpoint classifier documents intent ("we considered the xl breakpoint and chose to hold at 7xl") and keeps the class string parallel with other breakpoints. If a future phase wants to re-introduce an xl step, the anchor is preserved.

Closes UIREV38-03 (priority finding #3 from 38-UI-REVIEW.md, partial — xs consolidation deferred to follow-up per user scope note).
  </action>
  <verify>
<automated>grep -c 'xl:text-8xl' index.html | grep -q '^0$' &amp;&amp; grep -q 'lg:text-7xl xl:text-7xl' index.html &amp;&amp; echo "PASS: index hero H1 capped at xl:text-7xl"</automated>
Additional checks:
- `grep -n 'xl:text-7xl' index.html` → must show line 209
- `grep -c 'text-8xl' index.html` → 0 (no remaining text-8xl anywhere in index.html)
- `grep -c 'text-8xl' 404.html` → 1 (unchanged; the decorative "404" glyph stays)
  </verify>
  <done>
- index.html line 209 H1 class string is exactly: `text-5xl md:text-6xl lg:text-7xl xl:text-7xl font-extrabold mb-6 leading-[1.1] tracking-tight`
- Zero occurrences of `text-8xl` remain in index.html
- 404.html still has exactly 1 occurrence of `text-8xl` (decorative glyph, intentionally untouched)
  </done>
</task>

</tasks>

<verification>
Run all three task verify commands in sequence. Expected final state:

```
# Task 1
grep -c 'lg:pt-section-pt-lg' contacts.html   # → 1
grep -c 'lg:pt-section-pt-lg' 404.html        # → 1

# Task 2
sed -n '121p' treatment-abroad.html           # → <div class="container mx-auto px-4 lg:px-6">

# Task 3
grep -c 'text-8xl' index.html                 # → 0
grep -c 'text-8xl' 404.html                   # → 1 (decorative, unchanged)
grep 'xl:text-7xl' index.html                 # → line 209 match
```

Cross-page rhythm check (informational, not gating): after edits, all 6 pages should have the following top-padding progression on their primary hero/main element:

| Page | Base | Desktop |
|------|------|---------|
| index.html | pt-section-pt | lg:pt-section-pt-lg |
| online-consultations.html | pt-section-pt | lg:pt-section-pt-lg |
| treatment-abroad.html | pt-section-pt | lg:pt-section-pt-lg |
| checkup.html | pt-section-pt | lg:pt-section-pt-lg |
| **contacts.html** | pt-section-pt | **lg:pt-section-pt-lg (NEW)** |
| **404.html** | pt-section-pt | **lg:pt-section-pt-lg (NEW)** |

No CSS rebuild required — all utilities are already present in css/styles.css (generated during Phase 38). If the executor wants belt-and-braces: `ls -la css/styles.css` (timestamp) and grep the compiled file for `pt-section-pt-lg` to confirm the utility exists pre-edit.
</verification>

<success_criteria>
- All 3 findings from 38-UI-REVIEW.md "Top 3 Priority Fixes" section are closed
- contacts.html and 404.html each have `lg:pt-section-pt-lg` on their `<main id="page-content">` element
- treatment-abroad.html line 121 has no `mb-16` on the hero's inner container div
- index.html line 209 hero H1 does not contain `text-8xl` (uses `xl:text-7xl` instead)
- 404.html line 105 decorative "404" glyph still uses `text-8xl` (intentionally unchanged)
- Zero unrelated changes (no reformatting, no class reordering beyond the targeted inserts)
- All 3 task verify commands pass
- No CSS rebuild needed (utilities already compiled by Phase 38)
</success_criteria>

<output>
After completion, create `.planning/quick/260407-j79-fix-phase-38-ui-review-findings/260407-j79-SUMMARY.md` documenting:
- Which 3 findings closed with their review IDs
- Exact line-by-line diffs applied (old → new for each file)
- grep verification output proving each edit landed
- Confirmation that text-8xl on 404.html:105 (decorative glyph) was intentionally preserved
- Note whether the text-xs consolidation (minor recommendation from pillar 4) remains as follow-up scope
</output>
