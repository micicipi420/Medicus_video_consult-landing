---
milestone: v1.2
status: passed
date: 2026-03-23
---

# Milestone v1.2 Audit: Brand Visual Alignment

## Requirements Coverage

All 9 requirements verified with direct code evidence in `css/styles.css`.

| Requirement | Description | Phase | CSS Evidence | Status |
|-------------|-------------|-------|--------------|--------|
| TOKEN-01 | CSS-переменные обновлены (green primary CTA, warm bg) | 15 | `:root` lines 68-70: `--color-cta: #35B678; --color-cta-hover: #25A467; --color-text-on-cta: #FFFFFF` | SATISFIED |
| BTN-01 | Все кнопки pill-shape (border-radius: 100px) | 15 | `.button { border-radius: 100px; }` line 272 | SATISFIED |
| BTN-02 | Primary CTA зелёный (#35B678) | 15 | `.button--primary { background-color: var(--color-cta); }` line 281; `--color-cta: #35B678` line 68 | SATISFIED |
| BTN-03 | Hover primary = #25A467 | 15 | `.button--primary:hover { background-color: var(--color-cta-hover); }` line 286; `--color-cta-hover: #25A467` line 69 | SATISFIED |
| SPACE-02 | Hero-фон тёплый кремовый (#fffbf4) | 15 | `.hero { background: #fffbf4; }` line 422; no `.hero::before` rule present | SATISFIED |
| CARD-01 | border-radius: 20px на карточках | 16 | `--radius-lg: 1.25rem; /* 20px */` line 116; `.card { border-radius: var(--radius-lg); }` line 291; `.pricing__card { border-radius: var(--radius-lg); }` line 841 | SATISFIED |
| CARD-02 | Hover-эффект translateY(-2px) вместо scale(1.02) | 16 | `.card:hover { transform: translateY(-2px); }` line 300; `scale(1.02)` absent from entire file (grep: no matches) | SATISFIED |
| CARD-03 | Тени карточек облегчены | 16 | `--shadow-md: 0 1px 2px rgba(16, 24, 40, 0.05)` line 110 (rest); `--shadow-lg: 0 4px 6px -1px rgba(16, 24, 40, 0.1)` line 111 (hover); applied via lines 292, 301 | SATISFIED |
| SPACE-01 | Секционные отступы 100px на desktop | 16 | `--section-padding-desktop: 6.25rem; /* 100px */` line 102; `.section { padding-block: var(--section-padding-desktop); }` line 1445 inside desktop media query | SATISFIED |

**Coverage: 9/9 requirements satisfied.**

---

## Cross-Phase Integration

Phase 15 establishes design tokens; Phase 16 consumes them for card and spacing implementation.

### Token-to-Consumer Wiring

| Token (Phase 15) | Consuming Rule (Phase 16) | Connection | Status |
|------------------|--------------------------|------------|--------|
| `--radius-lg: 1.25rem` (line 116) | `.card { border-radius: var(--radius-lg); }` (line 291) | Direct CSS custom property reference | WIRED |
| `--radius-lg: 1.25rem` (line 116) | `.pricing__card { border-radius: var(--radius-lg); }` (line 841) | Direct CSS custom property reference | WIRED |
| `--shadow-md` (line 110) | `.card { box-shadow: var(--shadow-md); }` (line 292) | Direct CSS custom property reference | WIRED |
| `--shadow-lg` (line 111) | `.card:hover { box-shadow: var(--shadow-lg); }` (line 301) | Direct CSS custom property reference | WIRED |
| `--section-padding-desktop: 6.25rem` (line 102) | `.section { padding-block: var(--section-padding-desktop); }` (line 1445) | Direct CSS custom property reference | WIRED |
| `--color-cta: #35B678` (line 68) | `.button--primary { background-color: var(--color-cta); }` (line 281) | Direct CSS custom property reference | WIRED |
| `--color-cta-hover: #25A467` (line 69) | `.button--primary:hover { background-color: var(--color-cta-hover); }` (line 286) | Direct CSS custom property reference | WIRED |

### HTML → CSS Connection

All button instances in `index.html` use `.button.button--primary` or `.button.button--outline`, inheriting `border-radius: 100px` from the `.button` base rule. Verified instances:
- Line 59: hero primary CTA
- Line 60: hero outline CTA
- Line 351: doctors section outline
- Line 536: pricing CTA (`.pricing__cta`)
- Line 616: form submit button
- Line 718-719: final CTA section
- Line 756: sticky bar CTA

All 8 button instances are connected to the Phase 15 pill-shape rule.

The `.section` class applied to all major page sections (hero, problem, benefits, process, doctors, advantages, scenarios, pricing) connects to the Phase 16 desktop spacing rule via `.section { padding-block: var(--section-padding-desktop); }`.

**Cross-phase integration: fully wired. No orphaned tokens or disconnected consumers.**

---

## Regression Check

### v1.0 Functionality (Phases 1–10)

| Area | Check | Status |
|------|-------|--------|
| Form submission | `js/main.js` retains `fetch(API_URL, ...)` at line 430; `API_URL` points to Directus endpoint | INTACT |
| Directus API wiring | `API_URL = 'https://api.medicusunion.kz/items/consultation_requests'` in `js/main.js` | INTACT |
| FAQ accordion | `initAccordion()` function present in `js/main.js`; `.faq__question` elements intact in `index.html` | INTACT |
| Fonts | `@font-face` for Inter Variable and Manrope Variable declared in `css/styles.css` lines 1–47 | INTACT |
| SEO meta tags | Open Graph, Twitter Card, canonical, and description meta tags present in `index.html` | INTACT |

### v1.1 Functionality (Phases 11–14)

| Area | Check | Status |
|------|-------|--------|
| Sticky navigation | `.site-header { position: sticky; top: 0; }` at line 343; `.is-scrolled` class with box-shadow at line 352 | INTACT |
| Mobile-first responsive layout | `--section-padding-mobile: var(--space-6)` (48px) unchanged at line 101; desktop override only applies at media query boundary | INTACT |
| Reduced-motion override | `.card:hover { transform: none; }` inside `@media (prefers-reduced-motion: reduce)` confirmed in Phase 16 verification (line 1536) | INTACT |
| Scroll entrance animations | `.animate-on-scroll` rule present at line 1455 | INTACT |
| Button hover translateY(-2px) | `.button:hover { transform: translateY(-2px); }` at line 1478 — button hover not regressed by card hover changes | INTACT |

### No Regressions Found

The Phase 15 changes were additive (new tokens `--color-cta`, `--color-cta-hover`, `--color-text-on-cta`; overriding hero `background`; changing `.button` `border-radius` from prior value to 100px) and do not affect form logic, navigation, or layout structure.

The Phase 16 changes modified token values (`--radius-lg`, `--shadow-md`, `--shadow-lg`, `--section-padding-desktop`) and updated `.card:hover` transform. Mobile padding token `--section-padding-mobile` is explicitly unchanged.

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| TOKEN-01 | Phase 15: `:root` token definitions → consumed by `.button--primary`, `.hero`, and Phase 16 card/section rules | WIRED | — |
| BTN-01 | Phase 15: `.button { border-radius: 100px }` → all 8 button instances in `index.html` inherit via class | WIRED | — |
| BTN-02 | Phase 15: `--color-cta` token → `.button--primary { background-color: var(--color-cta) }` → 6 primary button instances in HTML | WIRED | — |
| BTN-03 | Phase 15: `--color-cta-hover` token → `.button--primary:hover { background-color: var(--color-cta-hover) }` | WIRED | — |
| SPACE-02 | Phase 15: `.hero { background: #fffbf4 }` → `index.html` `<section class="section hero">` | WIRED | — |
| CARD-01 | Phase 15 token `--radius-lg` → Phase 16 `.card` and `.pricing__card` rules → card elements in `index.html` | WIRED | — |
| CARD-02 | Phase 16: `.card:hover { transform: translateY(-2px) }` → card elements in `index.html`; `scale(1.02)` fully removed | WIRED | — |
| CARD-03 | Phase 15 tokens `--shadow-md`/`--shadow-lg` → Phase 16 `.card` rest/hover box-shadow rules | WIRED | — |
| SPACE-01 | Phase 15 token `--section-padding-desktop: 6.25rem` → Phase 16 `.section` desktop media query rule → all `class="section"` elements in `index.html` | WIRED | — |

**Requirements with no cross-phase wiring:** None. All 9 requirements touch at least Phase 15 → Phase 16 token consumption or Phase 15 → HTML structural connection.

---

## Verdict

**Status: PASSED**

All 9 requirements (TOKEN-01, BTN-01, BTN-02, BTN-03, CARD-01, CARD-02, CARD-03, SPACE-01, SPACE-02) are implemented in `css/styles.css` with direct code evidence. Phase 15 design tokens are correctly consumed by Phase 16 card and spacing rules — no orphaned tokens, no disconnected consumers. No regressions found in v1.0 form/backend integration or v1.1 navigation/animation functionality.

The only items not verifiable programmatically are visual/perceptual: pill-shape rendering in browser, hero cream background appearance, card hover lift feel, and shadow subtlety. These are listed as human verification items in the phase verification reports and do not constitute gaps.

---

*Audit performed: 2026-03-23*
*Auditor: Claude (integration-checker)*
