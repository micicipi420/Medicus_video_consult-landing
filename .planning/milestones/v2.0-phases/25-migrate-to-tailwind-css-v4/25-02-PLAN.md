---
phase: 25-migrate-to-tailwind-css-v4
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - index.html
autonomous: true
requirements:
  - TW-INDEX-TOP
must_haves:
  truths:
    - "index.html top-half elements (mesh bg, header, hero, stats, services, guide) carry Tailwind utility classes matching the Redesign TSX source"
    - "All existing BEM class names used by JS (header, header--scrolled, header__menu-btn, mobile-menu-overlay, hero__badge, hero__title, etc.) are PRESERVED alongside Tailwind classes"
    - "Tailwind build compiles without errors after class additions"
  artifacts:
    - path: "index.html"
      provides: "Top-half sections with Tailwind utility classes"
      contains: "bg-mu-text-50"
  key_links:
    - from: "index.html"
      to: "js/main.js"
      via: "BEM class selectors preserved"
      pattern: "class=\".*header.*\""
    - from: "index.html"
      to: "js/animations.js"
      via: "animation class selectors preserved"
      pattern: "class=\".*animate-fade-up.*\""
---

<objective>
Migrate the top half of index.html to Tailwind CSS v4 utility classes. For each HTML section (mesh background, header, mobile menu, hero, stats, services, guide), read the corresponding TSX component from Redesign/ and copy the Tailwind className strings into the HTML element class attributes. Preserve all existing BEM classes used by JS selectors.

Purpose: Replace hand-written CSS styling with Tailwind utilities for the first half of the main page, matching the Redesign prototype pixel-perfect.
Output: index.html top-half sections carry Tailwind utility classes from Redesign TSX source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-migrate-to-tailwind-css-v4/25-CONTEXT.md
@.planning/phases/25-migrate-to-tailwind-css-v4/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate mesh background, header, mobile menu, and hero sections to Tailwind classes</name>
  <files>index.html</files>
  <read_first>
    - Redesign/src/app/components/Layout.tsx (mesh background classes — 45 lines)
    - Redesign/src/app/components/Header.tsx (header + mobile menu classes — 175 lines)
    - Redesign/src/app/components/Hero.tsx (hero section classes — 166 lines)
    - index.html (current HTML — lines 1-140, top through hero section end)
    - js/main.js (lines 85-140 — header scroll + mobile menu selectors to preserve)
    - js/animations.js (lines 55-100 — hero animation selectors to preserve)
  </read_first>
  <action>
CRITICAL RULE: Every existing BEM class used by JS must be KEPT. Add Tailwind classes ALONGSIDE them.
Format: `class="header fixed z-50 transition-all duration-500 ..."` (BEM class first, then Tailwind utilities).

**JS-targeted classes that MUST be preserved (from main.js and animations.js):**
- `.header` — scroll detection
- `.header--scrolled` — added/removed by JS
- `.header__menu-btn` — mobile menu toggle
- `.mobile-menu-overlay` — mobile menu container
- `.is-open` — mobile menu state
- `.mobile-menu__link`, `.mobile-menu__cta` — mobile menu links
- `.icon-menu`, `.icon-close` — menu icons
- `.hero__badge`, `.hero__title`, `.hero__subtitle`, `.hero__buttons`, `.hero__trust`, `.hero__photos` — hero animation targets
- `.hero__floating-badge` — floating badge animation
- `.animate-fade-up`, `.animate-stagger`, `.animate-scale-in`, `.animate-fade-left`, `.animate-fade-right` — animation trigger classes
- `.header__inner`, `.header__logo`, `.header__nav`, `.header__nav-link`, `.header__phone`, `.header__cta`, `.header__actions` — header structure used by CSS or JS

**Section 1: Root body wrapper**
From Layout.tsx line 17: The root wrapper has `relative min-h-screen bg-mu-text-50 selection:bg-mu-blue/30 selection:text-mu-text-900 overflow-x-hidden`.
Add these to the `<body>` tag.

**Section 2: Mesh Background** (Layout.tsx lines 20-37)
Current HTML has div.mesh-bg with 3 blob children and an overlay.
Map from Layout.tsx:
- Outer container: `class="mesh-bg fixed inset-0 z-0 overflow-hidden pointer-events-none"` (keep `mesh-bg` for any CSS/JS refs)
- Blob 1 (mesh-bg__blob--1): `class="mesh-bg__blob mesh-bg__blob--1 absolute top-[-10%] left-[-10%] w-[60vw] h-[60vw] rounded-full bg-mu-blue/30 mix-blend-multiply blur-[120px]"`
- Blob 2 (mesh-bg__blob--2): `class="mesh-bg__blob mesh-bg__blob--2 absolute top-[20%] right-[-10%] w-[50vw] h-[50vw] rounded-full bg-mu-green-300/20 mix-blend-multiply blur-[120px]"`
- Blob 3 (mesh-bg__blob--3): `class="mesh-bg__blob mesh-bg__blob--3 absolute bottom-[-10%] left-[20%] w-[70vw] h-[70vw] rounded-full bg-mu-accent-blue/15 mix-blend-multiply blur-[120px]"`
- Overlay (mesh-bg__overlay): `class="mesh-bg__overlay absolute inset-0 bg-white/40 backdrop-blur-[40px] backdrop-saturate-[180%]"`

**Section 3: Header** (Header.tsx lines 43-113)
Current HTML has header.header > div.header__inner > logo + nav + actions + menu-btn.
Map from Header.tsx:
- `<header>`: `class="header fixed z-50 transition-all duration-500 top-4 left-4 right-4 mx-auto max-w-7xl rounded-[2.5rem] px-4 md:px-8 border-[0.5px] border-white/50 shadow-glass-header bg-white/30 backdrop-blur-[40px] backdrop-saturate-[150%] py-5"` 
  NOTE: The scrolled state (`bg-white/50 backdrop-blur-[60px] backdrop-saturate-[180%] py-3`) is toggled by JS via .header--scrolled. We need to handle this. Options: (a) keep the scrolled styling in a small `<style>` block or (b) JS directly manipulates classes. Best approach: add a minimal `<style>` block in `<head>` for `.header--scrolled` overrides:
  ```css
  .header--scrolled { --tw-bg-opacity: 0.5; backdrop-filter: blur(60px); -webkit-backdrop-filter: blur(60px); backdrop-saturate: 180%; padding-top: 0.75rem; padding-bottom: 0.75rem; }
  ```
  Actually simpler: modify js/main.js to toggle Tailwind classes. But the plan says JS should NOT be modified unless selectors change. So use a `<style>` tag approach for the `.header--scrolled` override. Place it in `<head>` after the stylesheet link.

- `<div class="header__inner flex items-center justify-between">`
- Logo `<a>`: `class="header__logo text-2xl font-bold tracking-tight bg-gradient-to-r from-mu-blue to-mu-accent-blue bg-clip-text text-transparent"`
- Desktop nav: `class="header__nav hidden lg:flex items-center gap-8"`
- Nav links: `class="header__nav-link text-mu-text-700 hover:text-mu-blue transition-colors font-medium tracking-tight"`
- Desktop actions wrapper: `class="header__actions hidden lg:flex items-center gap-4"`
- Phone link: `class="header__phone flex items-center gap-2 text-mu-text-700 hover:text-mu-blue transition-colors font-medium tracking-tight"`
- CTA button: `class="header__cta bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-6 py-2.5 rounded-full font-semibold shadow-lg shadow-mu-blue/25 hover:shadow-xl hover:shadow-mu-blue/30 transition-shadow inline-block tracking-tight"`
- Mobile menu button: `class="header__menu-btn lg:hidden p-2 text-mu-text-700 bg-white/50 rounded-full backdrop-blur-xl backdrop-saturate-[180%] border border-white/50"`

**Section 4: Mobile Menu** (Header.tsx lines 117-173)
- Overlay: `class="mobile-menu-overlay fixed inset-0 z-40 lg:hidden"` (keep existing structure)
- Backdrop (if exists as separate div): `class="absolute inset-0 bg-black/50"`
- Menu panel: `class="mobile-menu absolute top-24 left-4 right-4 bg-white/60 backdrop-blur-[80px] backdrop-saturate-[200%] shadow-glass-lg rounded-3xl overflow-hidden border-[0.5px] border-white/50"`
- Nav container: `class="mobile-menu__nav flex flex-col p-6 gap-2"`
- Menu links: `class="mobile-menu__link text-mu-text-900 hover:bg-white/40 rounded-2xl px-4 py-3 transition-colors font-medium tracking-tight"`
- Divider: `class="mobile-menu__divider h-[0.5px] bg-white/40 my-2"`
- Phone: `class="mobile-menu__phone flex items-center gap-3 text-mu-text-900 hover:bg-white/40 rounded-2xl px-4 py-3 transition-colors font-medium tracking-tight"`
- CTA: `class="mobile-menu__cta bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-6 py-4 rounded-2xl font-semibold tracking-tight shadow-lg mt-4 w-full text-center block"`

**Section 5: Hero** (Hero.tsx lines 17-165)
- Section: `class="hero relative min-h-screen flex items-center justify-center pt-32 pb-16 lg:pt-40"`
- Container: `class="container mx-auto px-4 lg:px-6 relative z-10"`
- Grid: `class="hero__grid grid lg:grid-cols-2 gap-12 lg:gap-8 items-center"`
- Left content: `class="hero__content max-w-2xl"`
- Badge: `class="hero__badge glass-badge inline-flex items-center gap-2 bg-white/40 backdrop-blur-[20px] border border-glass-border px-5 py-2.5 rounded-full shadow-glass-sm mb-6"`
- Badge text span: `class="text-sm font-semibold text-mu-text-900"`
- H1: `class="hero__title text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-extrabold mb-6 leading-[1.1] tracking-tight"`
- H1 first span: `class="text-mu-text-900 drop-shadow-sm"`
- H1 gradient span: `class="text-gradient bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent"`
- Subtitle p: `class="hero__subtitle text-xl text-mu-text-700 mb-10 max-w-xl leading-relaxed font-medium"`
- Buttons wrapper: `class="hero__buttons flex flex-col sm:flex-row items-center gap-4 mb-12"`
- Primary CTA: `class="btn-primary w-full sm:w-auto bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-8 py-4 rounded-3xl font-semibold shadow-lg shadow-mu-blue/30 shadow-glass-inner hover:shadow-xl hover:shadow-mu-blue/40 transition-all flex items-center justify-center gap-2 group text-lg"`
- Secondary CTA: `class="btn-secondary w-full sm:w-auto bg-white/50 backdrop-blur-[20px] text-mu-text-900 px-8 py-4 rounded-3xl font-semibold shadow-glass hover:bg-white/60 transition-all border border-glass-border text-lg"`
- Trust wrapper: `class="hero__trust flex items-center gap-8 text-base text-mu-text-700 font-semibold"`
- Trust items: `class="hero__trust-item flex items-center gap-2"`
- Photos container: `class="hero__photos animate-scale-in relative lg:h-[600px] w-full mt-10 lg:mt-0"`
- Main photo div: `class="hero__photo-main absolute right-0 top-0 w-[85%] h-[85%] rounded-[3rem] overflow-hidden shadow-glass-lg border-[8px] border-white/40 backdrop-blur-3xl z-10 bg-white/20"`
- Main photo img: `class="w-full h-full object-cover object-center"`
- Secondary photo div: `class="hero__photo-secondary absolute left-0 bottom-4 w-3/5 h-[45%] rounded-[2.5rem] overflow-hidden shadow-glass-lg border-[6px] border-white/50 backdrop-blur-2xl z-20 bg-white/30"`
- Floating badge (doctors): `class="hero__floating-badge hero__floating-badge--doctors absolute -right-6 top-1/4 bg-white/70 backdrop-blur-[40px] p-4 rounded-[2rem] shadow-glass border border-glass-border-strong z-30 flex items-center gap-4"`
- Badge icon wrapper: `class="hero__badge-icon w-14 h-14 bg-gradient-to-br from-mu-green-500 to-mu-green-600 rounded-2xl flex items-center justify-center text-white shadow-inner"`
- Badge number: `class="hero__badge-number text-2xl font-bold text-mu-text-900 tracking-tight"`
- Badge label: `class="text-sm text-mu-text-700 font-semibold"`
- Floating rating: `class="hero__floating-badge hero__floating-badge--rating absolute left-8 top-12 bg-white/70 backdrop-blur-[40px] px-6 py-4 rounded-[2rem] shadow-glass border border-glass-border-strong z-30 flex items-center gap-3"`
- Rating stars wrapper: `class="flex gap-1"`
- Rating number: `class="font-extrabold text-mu-text-900 text-lg"`

Also add a `<style>` block in `<head>` for the `.header--scrolled` override and any other JS-toggled states that cannot be purely Tailwind (since JS adds/removes `.header--scrolled`, not individual utility classes):
```html
<style>
  .header--scrolled {
    background: rgba(255,255,255,0.5);
    backdrop-filter: blur(60px);
    -webkit-backdrop-filter: blur(60px);
    -webkit-backdrop-filter: saturate(180%);
    backdrop-filter: blur(60px) saturate(180%);
    -webkit-backdrop-filter: blur(60px) saturate(180%);
    padding-top: 0.75rem;
    padding-bottom: 0.75rem;
  }
  .mobile-menu-overlay { display: none; }
  .mobile-menu-overlay.is-open { display: block; }
  .is-invalid { border-color: var(--mu-accent-red) !important; }
</style>
```
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && ./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css 2>&1 && grep -c 'bg-mu-text-50\|backdrop-blur\|rounded-\[2\.5rem\]' css/tw-output.css | xargs test 0 -lt && echo "CLASSES COMPILED" || echo "MISSING CLASSES"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'bg-mu-text-50' index.html` finds the body tag
    - `grep 'class="header fixed' index.html` finds header with both BEM and Tailwind classes
    - `grep 'class="hero relative min-h-screen' index.html` finds hero section
    - `grep 'header__menu-btn' index.html` confirms JS selector class preserved
    - `grep 'mobile-menu-overlay' index.html` confirms mobile menu JS class preserved
    - `grep 'hero__badge' index.html` confirms hero animation class preserved
    - `grep 'animate-scale-in' index.html` confirms animation trigger class preserved
    - Tailwind build succeeds with the new classes
  </acceptance_criteria>
  <done>Mesh background, header, mobile menu, and hero sections in index.html carry Tailwind utility classes from Layout.tsx, Header.tsx, and Hero.tsx while preserving all JS-targeted BEM classes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate stats, services, and guide sections to Tailwind classes</name>
  <files>index.html</files>
  <read_first>
    - Redesign/src/app/components/StatsSection.tsx (stats grid — 78 lines)
    - Redesign/src/app/components/ServicesSection.tsx (service cards — 188 lines)
    - Redesign/src/app/components/GuideSection.tsx (guide cards — 139 lines)
    - index.html (lines 140-500 approx — stats through guide section)
  </read_first>
  <action>
Continue editing index.html. For each section below, find the corresponding HTML elements and ADD Tailwind classes from the TSX source. Keep all existing BEM/animation classes.

**JS-targeted classes to preserve:**
- `.stat-card__number[data-target]` — counter animation in main.js
- `.animate-fade-up`, `.animate-stagger` — animation triggers in animations.js

**Section 6: Stats Section** (StatsSection.tsx lines 66-78)
- Section: `class="stats relative py-12 z-20"` (keep any existing BEM like `stats`)
- Container: `class="container mx-auto px-4 lg:px-6"`
- Grid: `class="grid grid-cols-2 lg:grid-cols-4 gap-6"` (keep `animate-stagger` if present)
- Each stat card: `class="stat-card relative group flex flex-col items-center justify-center p-8 bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-glass-border shadow-glass hover:shadow-glass-lg hover:bg-white/70 hover:border-glass-border-strong transition-all duration-500 overflow-hidden"`
- Stat number (from StatCard line 55): `class="stat-card__number text-5xl md:text-6xl font-extrabold mb-3 drop-shadow-sm relative z-10"` + the color class per card:
  - "43 клиники": add `text-mu-accent-blue`
  - "11 стран": add `text-mu-accent-teal`
  - "500+ врачей": add `text-mu-accent-orange`
  - "15+ лет опыта": add `text-mu-green-600`
- Stat label: `class="text-mu-text-700 font-bold text-lg text-center uppercase tracking-wider relative z-10"`
- Background hover glow (div inside card, from line 52): `class="absolute -inset-10 opacity-0 group-hover:opacity-20 transition-opacity duration-700 blur-2xl rounded-full"` + bg color per card (`bg-mu-blue`, `bg-mu-accent-teal`, `bg-mu-accent-orange`, `bg-mu-green-500`). If these glow divs don't exist in the current HTML, ADD them as the first child inside each stat card.

**Section 7: Services Section** (ServicesSection.tsx lines 99-187)
- Section: `class="services py-16 relative z-10"` + keep `id="services"`
- Container: `class="container mx-auto px-4 lg:px-6"`
- Section title wrapper: `class="text-center mb-16 max-w-3xl mx-auto"` (keep any animate classes)
- Title badge: `class="inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-glass-border px-5 py-2.5 rounded-full shadow-sm shadow-glass-inner mb-6"`
- Badge span: `class="text-sm font-bold text-mu-accent-blue uppercase tracking-wider"`
- H2: `class="text-5xl md:text-6xl font-extrabold mb-6"`
- H2 gradient span: `class="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent"`
- Description p: `class="text-mu-text-700 text-lg leading-relaxed font-medium"`
- Services grid: `class="grid md:grid-cols-2 lg:grid-cols-3 gap-8"` (keep `animate-stagger` if present)
- Each service card outer: `class="relative group h-full flex flex-col"` (keep `animate-fade-up`)
- Card inner: `class="bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:shadow-glass-lg transition-all duration-500 hover:-translate-y-2 h-full flex flex-col overflow-hidden"`
- Image section: `class="relative h-56 w-full overflow-hidden p-3"`
- Image inner: `class="relative w-full h-full rounded-[2rem] overflow-hidden border border-white/40 shadow-inner"`
- Image: `class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"`
- Image overlay: `class="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent"`
- Floating icon: `class="absolute top-4 right-4 w-12 h-12 backdrop-blur-xl rounded-2xl flex items-center justify-center shadow-glass-sm border border-glass-border transition-transform duration-500 group-hover:scale-110 group-hover:rotate-3"` + per-card iconBg/iconColor:
  - Online consultations: `bg-mu-blue/10 text-mu-accent-blue`
  - Treatment abroad: `bg-mu-accent-teal-bg text-mu-accent-teal`
  - Checkups: `bg-mu-green-50 text-mu-green-600`
- Content section: `class="p-8 pt-4 flex-grow flex flex-col"`
- Badge: `class="inline-flex items-center gap-2 bg-white/50 backdrop-blur-md border border-glass-border px-4 py-1.5 rounded-full shadow-sm w-fit mb-5"`
- Badge text: `class="text-sm font-bold"` + color (`text-mu-accent-blue`, `text-mu-accent-teal`, `text-mu-green-700`)
- Card title h3: `class="text-2xl font-bold text-mu-text-900 mb-4"`
- Card description p: `class="text-mu-text-700 font-medium leading-relaxed mb-6"`
- Features ul: `class="space-y-4 mb-8 flex-grow"`
- Feature li: `class="flex items-start gap-3"`
- Check icon wrapper: `class="w-6 h-6 bg-white/60 backdrop-blur-md border border-glass-border rounded-full flex items-center justify-center flex-shrink-0 mt-0.5 shadow-glass-inner-strong"`
- Feature text span: `class="text-mu-text-900 font-medium"`
- CTA button: `class="w-full bg-white/50 backdrop-blur-xl border border-glass-border text-mu-text-900 py-4 rounded-2xl font-bold shadow-glass-sm hover:bg-white/70 hover:shadow-glass transition-all flex items-center justify-center gap-2 mt-auto"`

**Section 8: Guide Section** (GuideSection.tsx lines 77-139)
- Section: `class="guide py-16 relative z-10"`
- Container: `class="container mx-auto px-4 lg:px-6 relative z-10"`
- Title wrapper: `class="text-center mb-16"` (keep any animate class)
- H2: `class="text-4xl md:text-5xl font-bold text-mu-text-900 mb-4"`
- Subtitle p: `class="text-mu-text-700 text-lg max-w-2xl mx-auto"`
- Guide grid: `class="grid md:grid-cols-2 lg:grid-cols-3 gap-10"` (keep `animate-stagger`)
- Each guide card outer: `class="group cursor-pointer flex flex-col h-full"` (keep `animate-fade-up`)
- Card inner: `class="bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:shadow-glass-lg transition-all duration-500 h-full flex flex-col overflow-hidden p-3"`
- Image area: `class="relative h-48 w-full shrink-0"`
- Image inner: `class="absolute inset-0 overflow-hidden rounded-[2rem] border border-white/40 shadow-inner"`
- Image: `class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"`
- Image overlay: `class="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent"`
- Floating icon: `class="absolute bottom-0 left-8 translate-y-1/2 w-14 h-14 backdrop-blur-xl rounded-2xl flex items-center justify-center shadow-glass-sm border border-glass-border z-10 group-hover:-translate-y-2 group-hover:scale-110 group-hover:rotate-6 transition-all duration-500"` + per-card iconBg/iconColor
- Content area: `class="p-8 pt-12 flex flex-col flex-grow relative"`
- Card title h3: `class="text-2xl font-bold text-mu-text-900 mb-3 transition-colors duration-500"` + per-card hover color
- Description p: `class="text-mu-text-700 font-medium leading-relaxed mb-6 flex-grow"`
- Link/CTA div: `class="flex items-center gap-2 font-bold transition-all duration-300 mt-auto bg-white/50 border border-glass-border shadow-glass-inner p-4 rounded-2xl justify-center"` + per-card iconColor
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && ./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css 2>&1 && grep -q 'grid-cols-2 lg:grid-cols-4' index.html && grep -q 'rounded-\[3rem\]' index.html && echo "SECTIONS OK" || echo "SECTIONS MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'grid grid-cols-2 lg:grid-cols-4' index.html` finds stats grid
    - `grep 'grid md:grid-cols-2 lg:grid-cols-3' index.html` finds services and guide grids
    - `grep 'stat-card__number' index.html` confirms JS selector preserved
    - `grep 'animate-fade-up' index.html` confirms animation classes preserved
    - `grep 'backdrop-blur-2xl rounded-\[3rem\]' index.html` confirms glass card styling from TSX
    - Tailwind build succeeds
  </acceptance_criteria>
  <done>Stats, services, and guide sections in index.html carry Tailwind utility classes from StatsSection.tsx, ServicesSection.tsx, and GuideSection.tsx, with all JS-targeted classes preserved.</done>
</task>

</tasks>

<verification>
1. `./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css` succeeds
2. All 6 sections (mesh bg, header, mobile menu, hero, stats, services, guide) have Tailwind classes
3. JS-targeted BEM classes preserved: header, header__menu-btn, mobile-menu-overlay, hero__badge, hero__title, stat-card__number, animate-fade-up, animate-stagger
4. `grep -c 'class="' index.html` returns similar count (classes enriched, not removed)
</verification>

<success_criteria>
Top half of index.html (through guide section) carries Tailwind utility classes copied verbatim from the corresponding Redesign TSX components. All JS-targeted classes are preserved. Tailwind build compiles all new utility classes without error.
</success_criteria>

<output>
After completion, create `.planning/phases/25-migrate-to-tailwind-css-v4/25-02-SUMMARY.md`
</output>
