---
phase: 25-migrate-to-tailwind-css-v4
plan: 04
type: execute
wave: 3
depends_on: ["25-01"]
files_modified:
  - online-consultations.html
  - treatment-abroad.html
  - checkups.html
  - contacts.html
autonomous: true
requirements:
  - TW-SERVICE-PAGES
  - TW-CONTACTS-PAGE
must_haves:
  truths:
    - "All 4 service/contact pages carry Tailwind utility classes matching their Redesign TSX page source"
    - "Shared elements (header, footer, mobile menu) use identical Tailwind classes as index.html"
    - "All JS-targeted classes (contact-form, form__submit, faq__question, header, etc.) are preserved across all pages"
    - "Tailwind build compiles all classes from all 5 HTML pages"
  artifacts:
    - path: "online-consultations.html"
      provides: "Online consultations service page with Tailwind classes"
      contains: "backdrop-blur"
    - path: "treatment-abroad.html"
      provides: "Treatment abroad service page with Tailwind classes"
      contains: "backdrop-blur"
    - path: "checkups.html"
      provides: "Checkups service page with Tailwind classes"
      contains: "backdrop-blur"
    - path: "contacts.html"
      provides: "Contacts page with Tailwind classes"
      contains: "backdrop-blur"
  key_links:
    - from: "online-consultations.html"
      to: "js/main.js"
      via: "Form and header selectors preserved"
      pattern: "contact-form|header__menu-btn"
---

<objective>
Migrate all 4 service/contact pages (online-consultations.html, treatment-abroad.html, checkups.html, contacts.html) to Tailwind CSS v4 utility classes. For each page, read the corresponding TSX page component from Redesign/src/app/pages/ and copy the Tailwind className strings into HTML. Shared elements (header, footer, mobile menu) should use the same classes as index.html.

Purpose: Complete the Tailwind migration for all non-index pages.
Output: All 4 pages carry Tailwind utility classes matching Redesign source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-migrate-to-tailwind-css-v4/25-CONTEXT.md
@.planning/phases/25-migrate-to-tailwind-css-v4/25-01-SUMMARY.md
@.planning/phases/25-migrate-to-tailwind-css-v4/25-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate online-consultations.html and treatment-abroad.html to Tailwind classes</name>
  <files>online-consultations.html, treatment-abroad.html</files>
  <read_first>
    - Redesign/src/app/pages/OnlineConsultationsPage.tsx (198 lines)
    - Redesign/src/app/pages/TreatmentAbroadPage.tsx (212 lines)
    - Redesign/src/app/components/Header.tsx (for shared header classes reference)
    - Redesign/src/app/components/Footer.tsx (for shared footer classes reference)
    - online-consultations.html (354 lines — full file)
    - treatment-abroad.html (394 lines — full file)
    - index.html (lines 1-75 — to copy header/mobile-menu class pattern exactly)
  </read_first>
  <action>
For each of the 2 pages, apply Tailwind classes in 3 areas:

**A. SHARED ELEMENTS (identical across all pages — copy from index.html Plan 02):**
Apply the SAME Tailwind classes as index.html for:
- `<body>` tag: `class="relative min-h-screen bg-mu-text-50 selection:bg-mu-blue/30 selection:text-mu-text-900 overflow-x-hidden"`
- Mesh background (all blobs + overlay): same classes as index.html
- Header: same classes as index.html
- Mobile menu: same classes as index.html
- Footer: same classes as index.html (from Plan 03)
- Sticky bar: same classes as index.html (if present)
- `<head>` `<style>` block: copy the same `.header--scrolled`, `.mobile-menu-overlay`, `.is-invalid`, FAQ styles from index.html

**B. PAGE-SPECIFIC CONTENT (from TSX page components):**

**online-consultations.html** from OnlineConsultationsPage.tsx:
- Page wrapper: `class="pt-32 pb-16"`
- Hero section container: `class="container mx-auto px-4 lg:px-6 mb-16"`
- Hero grid: `class="grid lg:grid-cols-2 gap-12 items-center"`
- Badge pill: `class="inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-white/60 px-5 py-2.5 rounded-full shadow-glass-inner mb-6"`
- Badge text: `class="text-sm font-bold text-mu-text-900 uppercase tracking-wider"`
- H1: `class="text-5xl md:text-6xl lg:text-7xl font-extrabold mb-6 leading-[1.1]"`
- H1 gradient span: `class="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent"`
- Description p: `class="text-xl text-mu-text-700 font-medium mb-10 leading-relaxed max-w-xl"`
- CTA buttons wrapper: `class="flex flex-col sm:flex-row gap-4"`
- Primary CTA: `class="bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-8 py-4 rounded-3xl font-bold shadow-lg shadow-mu-blue/30 hover:shadow-xl transition-all flex items-center gap-2 text-lg"`
- Hero image: `class="rounded-[3rem] overflow-hidden shadow-glass-lg border-[6px] border-white/40 bg-white/20"`
- Image: `class="w-full h-full object-cover"`

Read the FULL TSX file to map all remaining sections (steps, features grid, specializations, pricing, CTA). Each section follows the same pattern:
- Section wrappers: consistent `py-16 relative z-10`
- Cards: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-glass-border shadow-glass`
- Grids: `grid md:grid-cols-2 lg:grid-cols-3 gap-8` (or gap-6)
- Text classes: `text-mu-text-900 font-extrabold` for titles, `text-mu-text-700 font-medium` for descriptions

Map EVERY className from the TSX to the corresponding HTML element. The HTML structure should already match the TSX structure from Phase 01 build.

**treatment-abroad.html** from TreatmentAbroadPage.tsx:
Same approach — read the full TSX, map every className to HTML.
Key sections: hero, countries grid, process steps, features, CTA.
Same patterns as online-consultations but with different content and some color variations (teal accents instead of blue for treatment abroad).

**C. CSS LINK:**
In each page's `<head>`, the existing `<link rel="stylesheet" href="css/styles.css">` stays (it will point to Tailwind compiled output after Plan 05 renames it). Also add the `<style>` block for JS-toggled states (same as index.html).
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && ./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css 2>&1 && grep -q 'backdrop-blur' online-consultations.html && grep -q 'backdrop-blur' treatment-abroad.html && grep -q 'header fixed z-50' online-consultations.html && echo "2 PAGES OK" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'pt-32 pb-16' online-consultations.html` confirms page wrapper from TSX
    - `grep 'header fixed z-50' online-consultations.html` confirms shared header Tailwind classes
    - `grep 'backdrop-blur-3xl rounded-\[3rem\]' online-consultations.html` confirms glass card styling
    - `grep 'header__menu-btn' online-consultations.html` confirms JS class preserved
    - `grep 'contact-form' online-consultations.html` confirms form JS class preserved (if form present)
    - Same checks pass for treatment-abroad.html
    - Tailwind build succeeds with classes from both pages
  </acceptance_criteria>
  <done>online-consultations.html and treatment-abroad.html carry Tailwind utility classes from their respective TSX page sources. Shared header/footer/mobile-menu classes match index.html. All JS-targeted classes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate checkups.html and contacts.html to Tailwind classes</name>
  <files>checkups.html, contacts.html</files>
  <read_first>
    - Redesign/src/app/pages/CheckupsPage.tsx (206 lines)
    - Redesign/src/app/pages/ContactsPage.tsx (208 lines)
    - checkups.html (450 lines — full file)
    - contacts.html (353 lines — full file)
    - index.html (lines 1-75 — header/mobile-menu class pattern reference)
  </read_first>
  <action>
Same approach as Task 1 for the remaining 2 pages.

**A. SHARED ELEMENTS:** Identical header, footer, mobile menu, mesh bg, `<style>` block as index.html.

**B. PAGE-SPECIFIC CONTENT:**

**checkups.html** from CheckupsPage.tsx:
Read full TSX. Map every className. Key sections:
- Hero with pricing badge, gradient H1
- Programs grid (checkup packages) — cards with glass styling
- Process steps
- Features grid
- CTA section
Pattern: same glass card/grid/text utilities as other pages.

**contacts.html** from ContactsPage.tsx:
Read full TSX. Map every className. Key sections:
- Hero with gradient title
- Contact form (likely similar to ContactSection.tsx from index.html)
- Coordinator card
- Map/address section
- Office info
The form on this page is the MAIN contact form. Ensure all form field classes match ContactSection.tsx patterns:
- Labels: `class="block text-sm font-bold text-mu-text-900 mb-2"`
- Inputs: `class="w-full px-5 py-4 rounded-2xl border border-white/40 bg-white/50 backdrop-blur-md focus:bg-white/70 focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-all placeholder:text-mu-text-500 font-medium text-mu-text-900 shadow-glass-inner"`
- Select: same pattern with `appearance-none`
- Textarea: same pattern with `resize-none`
- Submit: `class="form__submit w-full bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white py-4 rounded-2xl font-bold shadow-lg shadow-mu-blue/30 hover:shadow-xl hover:shadow-mu-blue/40 transition-all flex items-center justify-center gap-2 text-lg"`

CRITICAL: Preserve all JS-targeted classes:
- `.contact-form`, `.form__submit`, `.form__success`, `.form__error`, `.form__field-error`
- `.faq__question`, `.is-open` (if FAQ present)
- `.header`, `.header__menu-btn`, `.mobile-menu-overlay`
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && ./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css 2>&1 && grep -q 'backdrop-blur' checkups.html && grep -q 'backdrop-blur' contacts.html && grep -q 'contact-form' contacts.html && echo "2 PAGES OK" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'pt-32 pb-16' checkups.html` confirms page wrapper
    - `grep 'header fixed z-50' checkups.html` confirms shared header classes
    - `grep 'contact-form' contacts.html` confirms form JS class preserved
    - `grep 'form__submit' contacts.html` confirms submit button JS class preserved
    - `grep 'backdrop-blur-3xl' contacts.html` confirms glass styling
    - Tailwind build succeeds with classes from all 5 HTML pages
  </acceptance_criteria>
  <done>checkups.html and contacts.html carry Tailwind utility classes from their respective TSX page sources. All 5 HTML pages now have Tailwind classes. All JS-targeted classes preserved.</done>
</task>

</tasks>

<verification>
1. `./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css` succeeds
2. All 4 pages have Tailwind classes in their page-specific sections
3. Shared header/footer/mobile-menu classes identical across all 5 pages
4. JS-targeted classes preserved across all pages
</verification>

<success_criteria>
All 4 non-index pages carry Tailwind utility classes from their Redesign TSX sources. Combined with Plans 02-03 (index.html), all 5 HTML pages are migrated. Tailwind build compiles all utility classes without error.
</success_criteria>

<output>
After completion, create `.planning/phases/25-migrate-to-tailwind-css-v4/25-04-SUMMARY.md`
</output>
