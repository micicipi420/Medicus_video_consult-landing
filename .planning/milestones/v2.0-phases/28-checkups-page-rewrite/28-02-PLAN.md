---
phase: 28-checkups-page-rewrite
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified: [checkup.html]
autonomous: false
requirements: [CHECK-06, CHECK-07, CHECK-08, CHECK-09, CHECK-10]

must_haves:
  truths:
    - "Turkey section shows 4 programs with exact prices from copywriting"
    - "Process section shows 5 steps with verbatim text"
    - "B2B section has 2 scenario cards and 5 trust items"
    - "FAQ section has 7 accordion items with verbatim Q&A pairs"
    - "Form section has working lead form with name, phone, direction dropdown, description"
    - "Closing CTA shows exact H2 and text from copywriting"
    - "Form uses id=lead-form for js/main.js binding"
    - "Page has no old design patterns (glass, orbs, dividers, dark mode)"
  artifacts:
    - path: "checkup.html"
      provides: "Complete checkup page with all 10 sections in new design"
      contains: "Частые вопросы"
  key_links:
    - from: "checkup.html form"
      to: "js/main.js"
      via: "id=lead-form"
      pattern: "id=\"lead-form\""
    - from: "checkup.html FAQ buttons"
      to: "js/main.js"
      via: "faq__question class"
      pattern: "class=\"faq__question\""
    - from: "checkup.html sticky bar CTA"
      to: "#form-checkup"
      via: "anchor link"
      pattern: "href=\"#form-checkup\""
---

<objective>
Complete the checkup.html rewrite by adding sections 6-10 (Turkey Programs, Process, B2B, FAQ, Form/CTA) at the Plan 02 insert point.

Purpose: Plan 01 created the page shell + sections 1-5. This plan adds the remaining 5 sections to complete the full 10-section checkup page, all using the new clean design system with verbatim copywriting text.

Output: Fully complete checkup.html with all 10 content sections, working form, FAQ accordion, and cross-sell CTA — ready for visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-checkups-page-rewrite/28-01-SUMMARY.md
</context>

<read_first>
/Users/mikhail/Downloads/MedicusUnion_Checkup_Page_Copy.md — COPYWRITING SOURCE (single source of truth for ALL text)
checkup.html — file modified by Plan 01 (contains sections 1-5 + insert point marker)
index.html — REFERENCE for new design patterns (form, footer, final-cta structure)
online-consultations.html — REFERENCE for FAQ accordion pattern, form pattern, pricing section
css/styles.css — check existing CSS classes (b2b, programs--turkey sections have CSS)
js/main.js — check form binding (getElementById lead-form) and FAQ binding (faq__question)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Add Sections 6-8 (Turkey Programs, Process Steps, B2B Block)</name>
  <files>checkup.html</files>
  <action>
    Find the `<!-- PLAN-02-INSERT-POINT -->` comment in checkup.html and replace it with sections 6-8. All text VERBATIM from copywriting doc.

    SECTION 6 — TURKEY PROGRAMS (CHECK-06):
    - Section class: `section programs programs--turkey` id `programs-turkey`
    - H2 VERBATIM: "Чек-ап в Турции"
    - Subheading VERBATIM: "Liv Hospital · Medicana — ведущие клиники Стамбула с аккредитацией JCI"
    - Intro text VERBATIM from copywriting
    - "Что включено во все программы" block (h3):
      List items with checkmark SVGs (use same SVG as b2b trust items in current file):
      - "Обследование у профессоров и ведущих врачей"
      - "Расширенная программа анализов"
      - "Всё за 1–2 дня, без очередей"
      - "Трансфер из аэропорта и между отелем и клиникой"
      - "Личный сопровождающий на всех этапах"
      - "24/7 поддержка и переводчик"
      - "Забор анализов прямо в отеле"
    - 4 program cards with `card programs__card animate-on-scroll` (NO glass):
      1. Лабораторный чек-ап — от ~$350 — text VERBATIM
      2. Silver — от ~$1,100 (муж.) / ~$1,200 (жен.) — text VERBATIM
      3. Gold — от ~$2,100 (муж.) / ~$2,200 (жен.) — text VERBATIM
      4. Platinum — от ~$3,100 (муж.) / ~$3,200 (жен.) — text VERBATIM (use programs__badge--accent)
    - CTA: "Подобрать программу" button linking to #form-checkup

    SECTION 7 — PROCESS / 5 STEPS (CHECK-07):
    - Section class: `section process` id `how-it-works`
    - H2 VERBATIM: "От заявки до результатов — 5 шагов"
    - 5 steps using process__step pattern with numbered 01-05:
      01. "Оставьте заявку" — text VERBATIM
      02. "Согласование и подготовка" — text VERBATIM
      03. "Прилёт и трансфер" — text VERBATIM
      04. "Обследование" — text VERBATIM
      05. "Результаты и дальнейшие шаги" — text VERBATIM
    - Use `checkup-steps` class on the steps container for the 5-column layout

    SECTION 8 — B2B BLOCK (CHECK-08):
    - Section class: `section b2b` id `b2b`
    - H2 VERBATIM: "Чек-апы для бизнеса"
    - Subheading VERBATIM: "Корпоративные программы обследования для топ-менеджмента и клиентов вашей компании"
    - Intro text VERBATIM from copywriting
    - 2 scenario cards with `card b2b__card animate-on-scroll` (NO glass):
      1. "Для ваших сотрудников" — text VERBATIM
      2. "Для ваших клиентов" — text VERBATIM
    - "Почему компании выбирают MedicusUnion" block:
      5 trust items with checkmark SVGs:
      - "Юридическое лицо в Австрии (GmbH) — договор, акты, прозрачная отчётность"
      - "Офис в Казахстане — резидент Astana Hub"
      - "ISO 27001 — медицинские данные сотрудников защищены по международным стандартам"
      - "Одно контактное лицо — координация группы любого размера"
      - "Личный кабинет для каждого участника — компания не видит результатов, сотрудник/клиент видит свои"
    - CTA: "Обсудить корпоративную программу" button linking to #form-checkup

    HTML entity usage: &nbsp; for non-breaking spaces, &mdash; for em-dashes, &ndash; for en-dashes, &middot; for middle dots.
    NO section-divider blocks between sections.
    NO card--glass or animate-on-scroll--glass classes.
  </action>
  <verify>
    <automated>grep -c "Чек-ап в.*Турции" checkup.html && grep -c "От.*заявки до.*результатов" checkup.html && grep -c "Чек-апы для бизнеса" checkup.html && grep -c "Для ваших сотрудников" checkup.html && grep -c "Platinum" checkup.html</automated>
  </verify>
  <done>
    Sections 6-8 present in checkup.html with all verbatim copywriting text. Turkey has 4 program cards with prices. Process has 5 numbered steps. B2B has 2 scenario cards and 5 trust items. No old design patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Sections 9-10 (FAQ, Form/CTA) and finalize page</name>
  <files>checkup.html</files>
  <action>
    Add sections 9-10 after the B2B section, before the closing main tag and footer. All text VERBATIM from copywriting doc.

    SECTION 9 — FAQ (CHECK-09):
    - Section class: `section faq` id `faq-checkup`
    - H2 VERBATIM: "Частые вопросы"
    - 7 FAQ items using faq__item / faq__question / faq__answer pattern (MUST match js/main.js selectors):
      Each item structure:
      ```
      div.faq__item > button.faq__question[type=button][aria-expanded=false] > span.faq__question-text + span.faq__icon
      div.faq__item > div.faq__answer[hidden] > p
      ```
      Q&A pairs ALL VERBATIM from copywriting:
      1. "Нужна ли виза в Южную Корею?" — answer VERBATIM
      2. "Нужна ли виза в Турцию?" — answer VERBATIM
      3. "Сколько дней нужно закладывать на поездку?" — answer VERBATIM
      4. "Что если чек-ап выявит проблему?" — answer VERBATIM
      5. "Будет ли переводчик?" — answer VERBATIM
      6. "Как я получу результаты?" — answer VERBATIM
      7. "Можно ли добавить отдельные обследования к программе?" — answer VERBATIM

    SECTION 10 — FORM + CLOSING CTA (CHECK-10):
    - Section class: `section lead-form-section` id `form-checkup`
    - Two-column layout using lead-form__grid (match index.html form pattern)
    - Left column (lead-form__info):
      H2 VERBATIM: "Узнайте всё о своём здоровье за 1–2 дня"
      Subtext VERBATIM: "Оставьте заявку — мы свяжемся с вами, поможем выбрать программу и направление, и организуем чек-ап полностью. Бесплатная консультация, без обязательств."
      Trust list (3 items with checkmark SVGs):
      - "Бесплатный подбор программы"
      - "Организация под ключ"
      - "Ваши данные защищены (ISO 27001)"
    - Right column (form):
      Form with id="lead-form" (CRITICAL for js/main.js binding)
      Fields:
      1. Name: label "Ваше имя", placeholder "Например, Айгуль", required
      2. Phone: label "Телефон", placeholder "+7 (___) ___-__-__", required, inputmode="numeric"
      3. Direction: select, label "Направление", required, options: Южная Корея, Турция, Не определился
      4. Description: textarea, label "Комментарий (необязательно)", placeholder "Возраст, интересующая программа, вопросы"
      Honeypot field (visually-hidden)
      Submit button: "Подобрать программу"
      Privacy text: "Мы свяжемся с вами в течение 24 часов. Ваши данные защищены."
      Disclaimer: "Цены указаны ориентировочно в USD и могут меняться в зависимости от курса на дату обследования."
      Success overlay (hidden by default, shown after submission)
    - Use `lead-form__wrapper` (NO card--glass) on the form wrapper div

    CROSS-SELL CTA (after form, before footer):
    - Section class: `section final-cta` id `final-cta-checkup`
    - H2: "Нужна консультация врача, а не чек-ап?"
    - Text: "Получите второе мнение европейского специалиста по видео — без перелёта, от 450 EUR, за 5 дней."
    - Two buttons: "Онлайн-консультации" (link to consultations.html) + "Лечение за рубежом" (link to treatment-abroad.html)

    FINAL VERIFICATION: After adding all sections, do a quick scan to ensure:
    - Zero occurrences of: card--glass, hero__orb, section-divider, theme-toggle, data-theme, animate-on-scroll--glass, scroll-progress
    - The PLAN-02-INSERT-POINT comment is removed
    - Footer is present with correct links
    - Sticky bar points to #form-checkup
    - Script tag for js/main.js is present at bottom
  </action>
  <verify>
    <automated>grep -c "Частые вопросы" checkup.html && grep -c "id=\"lead-form\"" checkup.html && grep -c "id=\"faq-checkup\"" checkup.html && grep -c "Нужна консультация врача" checkup.html && grep -c "card--glass" checkup.html | grep "^0$" && grep -c "PLAN-02-INSERT" checkup.html | grep "^0$"</automated>
  </verify>
  <done>
    checkup.html is complete with all 10 content sections + cross-sell CTA. FAQ has 7 items matching js/main.js selectors. Form has id="lead-form" for JS binding. Disclaimer text present. No old design patterns remain. No insert-point markers remain. Page ready for visual verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete checkup page</name>
  <files>checkup.html</files>
  <action>
    Human verifies the complete checkup.html page in browser.
  </action>
  <verify>
    <automated>grep -c "faq__question" checkup.html && grep -c "lead-form" checkup.html</automated>
  </verify>
  <done>User confirms page looks correct and all interactive elements work.</done>
  <what-built>Complete checkup.html page rewrite from old glassmorphism design to new clean medtech design. All 10 content sections with verbatim copywriting text, working form, FAQ accordion, cross-sell CTA.</what-built>
  <how-to-verify>
    1. Open checkup.html in browser
    2. Verify page loads cleanly — no dark mode toggle, no glass effects, no blob decorations, no wave dividers
    3. Scroll through all sections — verify text matches copywriting doc:
       - Hero: "Проверьте здоровье в Samsung Medical Center и Severance Hospital..."
       - Problem: 3 objection cards
       - Why Abroad: 4 advantage cards
       - Why MedicusUnion: 5 numbered items
       - Korea Programs: 7 comprehensive + teen + 6 specialized (14 total cards with prices)
       - Turkey Programs: 4 program cards with prices + included items list
       - Process: 5 steps
       - B2B: 2 scenario cards + trust list
       - FAQ: 7 accordion items (click each to verify opening/closing)
       - Form: 4 fields, submit button works (validation triggers)
    4. Test sticky bar at bottom — "Подобрать программу" scrolls to form
    5. Test header nav — Korea/Turkey/FAQ links scroll to correct sections
    6. Verify mobile responsiveness (resize to 375px width)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "card--glass" checkup.html` returns 0
2. `grep -c "hero__orb" checkup.html` returns 0
3. `grep -c "section-divider" checkup.html` returns 0
4. `grep -c "theme-toggle" checkup.html` returns 0
5. `grep -c "data-theme" checkup.html` returns 0
6. `grep -c "animate-on-scroll--glass" checkup.html` returns 0
7. `grep -c "scroll-progress" checkup.html` returns 0
8. All 10 section headings present: "Проверьте здоровье", "Почему не стоит ждать", "Что даёт чек-ап", "Не просто посредник", "Чек-ап в Южной Корее", "Чек-ап в Турции", "От заявки до результатов", "Чек-апы для бизнеса", "Частые вопросы", "Узнайте всё о своём здоровье"
9. Form has id="lead-form" for JS binding
10. FAQ items use faq__question class for JS binding
11. Page opens in browser without JS errors
</verification>

<success_criteria>
- checkup.html is a complete, loadable page
- All 10 content sections present with verbatim copywriting text
- New clean design: no glass, no orbs, no dividers, no dark mode
- Form submits via js/main.js (id="lead-form")
- FAQ accordion works via js/main.js (faq__question buttons)
- Sticky bar and header nav links work correctly
- Mobile responsive at 375px
- All prices match copywriting source exactly
</success_criteria>

<output>
After completion, create `.planning/phases/28-checkups-page-rewrite/28-02-SUMMARY.md`
</output>
