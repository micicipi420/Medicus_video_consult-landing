---
phase: 40-ux-cosmetic-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 404.html
autonomous: false
requirements:
  - COSMETIC-01
user_setup: []

must_haves:
  truths:
    - "404.html H1 'Страница не найдена' fits within a 320px viewport without relying on html { overflow-x: clip }"
    - "documentElement.scrollWidth === clientWidth on 404.html at 320px"
    - "Subject+verb nbsp binding (Страница&nbsp;не&nbsp;найдена) is preserved"
    - "make build exits 0 (byte-identity gate from Phase 39-03 still passes)"
  artifacts:
    - path: "404.html"
      provides: "404 page with mobile-fitting H1"
      contains: 'class="text-3xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4">Страница&nbsp;не&nbsp;найдена</h1>'
  key_links:
    - from: "404.html line 110 H1"
      to: "Tailwind v4 step-function breakpoint scale (text-3xl mobile floor)"
      via: "utility class string"
      pattern: "text-3xl sm:text-5xl md:text-6xl lg:text-7xl"
---

<objective>
Close COSMETIC-01: change the 404.html H1 mobile size from text-4xl (36px) to text-3xl (30px) so the phrase "Страница не найдена" fits within a 320px viewport without relying on the `html { overflow-x: clip }` safety net introduced in Phase 38.1.

Purpose: Prove the mobile overflow safety net is no longer compensating for an underlying sizing bug. The fix is a single-character class swap that preserves the existing Tailwind step-function breakpoint pattern and the mandatory subject+verb nbsp binding.

Output: 404.html with corrected H1 class string. Pre-commit hook (`make build` byte-identity gate) passes. Playwright MCP measurement at 320px confirms no horizontal overflow and no clipping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-ux-cosmetic-cleanup/40-CONTEXT.md
@404.html

# User memory rules — MANDATORY for any H1 text edit on this project
@/Users/mikhail/.claude/projects/-Users-mikhail-Projects-Medicus-video-consult-landing/memory/feedback_nbsp-subject-verb.md
@/Users/mikhail/.claude/projects/-Users-mikhail-Projects-Medicus-video-consult-landing/memory/feedback_nbsp-orphan-prevention.md

<interfaces>
<!-- Current state of 404.html line 110 (verified 2026-04-08): -->

```html
<h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4">Страница&nbsp;не&nbsp;найдена</h1>
```

<!-- Surrounding context (lines 104-119) — DO NOT EDIT anything outside line 110: -->

```html
  <!-- 404 Content (NotFoundPage.tsx) -->
  <main id="page-content" class="relative z-10 pt-section-pt lg:pt-section-pt-lg pb-section-pb min-h-section-hero-compact flex items-center justify-center">
    <div class="text-center max-w-lg mx-auto px-4">
      <div class="text-8xl font-extrabold bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent mb-6">
        404
      </div>
      <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4">Страница&nbsp;не&nbsp;найдена</h1>
      <p class="text-mu-text-700 font-medium mb-8">
        ...
```

<!-- Tailwind step-function reference (no clamp, breakpoint scale): -->
<!-- text-3xl = 30px, text-4xl = 36px, text-5xl = 48px, text-6xl = 60px, text-7xl = 72px -->
<!-- Mobile-first: text-3xl applies < 640px, sm:text-5xl from 640px+, md:text-6xl from 768px+, lg:text-7xl from 1024px+ -->

<!-- Phase 39 byte-identity gate (verify edit is OUTSIDE chrome partials): -->
<!-- 404.html chrome regions are bracketed by <!-- BUILD:header --> ... <!-- /BUILD:header --> -->
<!-- and similar for footer/sticky-bar/mobile-menu. Line 110 sits inside <main id="page-content"> -->
<!-- which is page-specific body content — NOT a chrome partial. The splicer is a no-op on this edit. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Step 404 H1 mobile class from text-4xl to text-3xl</name>
  <files>404.html</files>
  <read_first>
    - 404.html (read line 110 to confirm current state matches expected fragment below)
    - .planning/phases/40-ux-cosmetic-cleanup/40-CONTEXT.md (D-01 — locked decision text)
    - /Users/mikhail/.claude/projects/-Users-mikhail-Projects-Medicus-video-consult-landing/memory/feedback_nbsp-subject-verb.md (mandatory: subject+verb nbsp binding rule)
    - /Users/mikhail/.claude/projects/-Users-mikhail-Projects-Medicus-video-consult-landing/memory/feedback_nbsp-orphan-prevention.md (mandatory: orphan prevention rule)
  </read_first>
  <action>
Per locked decision D-01 in .planning/phases/40-ux-cosmetic-cleanup/40-CONTEXT.md, perform a single-line class string substitution in 404.html.

EXACT operation — substitute the OLD line for the NEW line:

OLD (currently at 404.html line 110):
```html
      <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4">Страница&nbsp;не&nbsp;найдена</h1>
```

NEW (replacement):
```html
      <h1 class="text-3xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4">Страница&nbsp;не&nbsp;найдена</h1>
```

Diff: `text-4xl` → `text-3xl`. Nothing else changes.

CRITICAL preservation requirements (per user memory rules):
1. The string `Страница&nbsp;не&nbsp;найдена` MUST appear byte-for-byte in the new line. Both `&nbsp;` entities are subject+verb bindings — DO NOT remove, replace with literal spaces, or replace with the U+00A0 character.
2. The H1 surrounding tags `<h1 ...>` and `</h1>` must be unchanged.
3. No other classes are added, removed, or reordered.
4. Indentation (6 leading spaces) is preserved.

DO NOT touch anything outside line 110. Do not edit `<head>`, do not edit the `404` number block on lines 107-109, do not edit the `<p>` on lines 111-113, do not edit anything inside the `<!-- BUILD:* -->` chrome partial regions.

Why text-3xl and not a clamp() or text-2xl: D-01 specifies a single mobile step-down to match the existing Tailwind step-function pattern across the codebase. text-3xl (30px) gives the phrase "Страница не найдена" (~14 character glyph run with two nbsp bindings) enough room at 320px viewport (320 - 32px container padding = 288px content width) without dropping to a font size that looks visually weak relative to the 8xl "404" number above it.
  </action>
  <verify>
    <automated>grep -q '^      &lt;h1 class="text-3xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4"&gt;Страница&amp;nbsp;не&amp;nbsp;найдена&lt;/h1&gt;$' 404.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'text-3xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4">Страница&nbsp;не&nbsp;найдена</h1>' 404.html` returns `1`
    - `grep -c 'text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4"' 404.html` returns `0` (old class string fully removed)
    - `grep -c 'Страница&nbsp;не&nbsp;найдена' 404.html` returns `1` (subject+verb nbsp binding preserved verbatim)
    - `grep -c '<h1 ' 404.html` returns `1` (still exactly one H1 on the page)
    - `git diff --stat 404.html` shows exactly 1 line changed (1 insertion, 1 deletion)
    - `make build` exits 0 (Phase 39-03 byte-identity gate passes — the splicer is a no-op because the edit is outside chrome partial regions)
    - `git diff --quiet -- '*.html'` after running `make build` (no chrome drift introduced)
  </acceptance_criteria>
  <done>404.html line 110 H1 uses `text-3xl` as the mobile class with all higher breakpoints unchanged. Subject+verb nbsp binding `Страница&nbsp;не&nbsp;найдена` is preserved byte-for-byte. `make build` passes the byte-identity gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Playwright MCP verification — 404 H1 fits at 320px (D-06)</name>
  <files>.planning/phases/40-ux-cosmetic-cleanup/40-01-SUMMARY.md (verification results section only)</files>
  <read_first>
    - 404.html (confirm Task 1 was applied)
    - .planning/phases/40-ux-cosmetic-cleanup/40-CONTEXT.md (D-06 — verification strategy)
  </read_first>
  <action>
    Execute the verification protocol described in <how-to-verify> below. This is a human-gated checkpoint task: Claude runs all the automated browser/curl/grep measurements via Playwright MCP and Bash tools, records the results in the plan SUMMARY, then pauses for the user to review and type the resume signal.
  </action>
  <verify>
    <automated>See <acceptance_criteria> below — every assertion is run via mcp__playwright__browser_evaluate, mcp__playwright__browser_console_messages, or curl/grep, with results captured into the plan SUMMARY before the resume signal is requested</automated>
  </verify>
  <done>All criteria in <acceptance_criteria> below pass and the user has typed the <resume-signal></done>
  <what-built>
404.html H1 mobile class stepped down from text-4xl (36px) to text-3xl (30px), preserving the subject+verb nbsp binding and the existing Tailwind step-function breakpoint pattern. Edit is in body content outside chrome partial regions; pre-commit hook byte-identity gate passes.
  </what-built>
  <how-to-verify>
Use Playwright MCP browser tools to measure the fix at 4 viewport widths. Serve the site locally first:

1. Start a local static server in the repo root:
   ```sh
   python3 -m http.server 8000 &
   ```

2. Use `mcp__playwright__browser_navigate` to load `http://localhost:8000/404.html`.

3. For each viewport width in [320, 375, 768, 1024], run `mcp__playwright__browser_resize` with the width and height 800, then `mcp__playwright__browser_evaluate` to measure:
   ```js
   () => {
     const html = document.documentElement;
     const h1 = document.querySelector('h1');
     const r = h1.getBoundingClientRect();
     return {
       viewport: window.innerWidth,
       scrollWidth: html.scrollWidth,
       clientWidth: html.clientWidth,
       overflow_px: html.scrollWidth - html.clientWidth,
       h1_left: r.left,
       h1_right: r.right,
       h1_width: r.width,
       h1_text: h1.textContent.trim(),
       h1_visible_within_viewport: r.left >= 0 && r.right <= window.innerWidth
     };
   }
   ```

4. Assert at every viewport:
   - `overflow_px === 0` (no horizontal overflow regardless of `overflow-x: clip` safety net)
   - `h1_visible_within_viewport === true` (no clipping at any edge)
   - `h1_text === 'Страница не найдена'` (text content unchanged after entity decode)

5. Run `mcp__playwright__browser_console_messages` and assert no entries match `404` for the 404.html page itself (the favicon 404 console error is a separate concern fixed in Plan 02).

6. Stop the server: `kill %1` or `pkill -f "python3 -m http.server 8000"`.

7. Append a "Verification Results" section to 40-01-SUMMARY.md with the measurement table for all 4 viewports.

If any viewport shows `overflow_px > 0` or `h1_visible_within_viewport === false`, the fix is insufficient and you must escalate (e.g., consider text-2xl, but flag this as a deviation from D-01 and request user approval before applying).
  </how-to-verify>
  <acceptance_criteria>
    - At viewport 320px: `overflow_px === 0` AND `h1_visible_within_viewport === true`
    - At viewport 375px: same
    - At viewport 768px: same
    - At viewport 1024px: same
    - `h1.textContent.trim() === 'Страница не найдена'` at every viewport
    - 40-01-SUMMARY.md contains a "Verification Results" section with the 4-row measurement table
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 4 viewports pass, or describe the failing viewport and measurement values</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan modifies a single HTML class string in static markup. No user input, no network I/O, no auth, no data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | N/A | 404.html static markup | accept | Cosmetic HTML class change — no user input, no data flow, no auth surface. Threat: none identified. Mitigation: N/A. |
</threat_model>

<verification>
1. `grep -c 'text-3xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4">Страница&nbsp;не&nbsp;найдена</h1>' 404.html` returns `1`
2. `grep -c 'text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4"' 404.html` returns `0`
3. `make build` exits 0 with no chrome drift
4. Playwright MCP measurement table in 40-01-SUMMARY.md confirms `overflow_px === 0` at 320, 375, 768, 1024
</verification>

<success_criteria>
1. 404.html line 110 H1 uses the new class string `text-3xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-mu-text-900 mb-4`
2. `Страница&nbsp;не&nbsp;найдена` subject+verb nbsp binding preserved byte-for-byte
3. `make build` passes the Phase 39-03 byte-identity gate
4. Playwright MCP measurement at 320, 375, 768, 1024 shows zero horizontal overflow on 404.html and the H1 fully visible within the viewport (the safety net `overflow-x: clip` is no longer compensating)
</success_criteria>

<output>
After completion, create `.planning/phases/40-ux-cosmetic-cleanup/40-01-SUMMARY.md` containing:
- Goal restatement (COSMETIC-01)
- Diff applied (1-line class swap on 404.html:110)
- Verification Results section with the 4-viewport measurement table from the Playwright MCP run
- Confirmation that `make build` exits 0 and `git diff --quiet -- '*.html'` after build
- Note: subject+verb nbsp binding preserved (grep count confirms 1)
</output>
