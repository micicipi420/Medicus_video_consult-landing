---
phase: 94-polish-and-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/components/sections/contacts/ContactsHero.tsx
  - next/src/components/sections/contacts/ContactMethodGrid.tsx
  - next/src/components/sections/contacts/CoordinatorCard.tsx
  - next/src/components/sections/contacts/TrustBadges.tsx
autonomous: true
requirements: [POL-03]
must_haves:
  truths:
    - "The 4 dead-code TSX files no longer exist on disk"
    - "`grep -r` across `next/src/` returns 0 references to any of the deleted symbols"
    - "`pnpm build` and `pnpm lint` exit 0 after deletion"
    - "`/contacts` route still renders identically (no visual or behavioral regression)"
  artifacts:
    - path: ".planning/phases/94-polish-and-hygiene/94-02-DELETION-AUDIT.md"
      provides: "Pre-deletion grep audit proving 0 imports of the 4 symbols"
      contains: "ContactsHero"
  key_links:
    - from: "next/src/app/contacts/page.tsx"
      to: "next/src/components/sections/service/LeadFormSection.tsx"
      via: "ESM import"
      pattern: "from '@/components/sections/service'"
---

<objective>
Delete 4 unimported TSX files in `next/src/components/sections/contacts/`. They have been unrendered since v8.0 — Phase 93 Plan 05 (`93-05-CONTACTS-AUDIT.md`) verified `next/src/app/contacts/page.tsx` imports only from `next/src/components/sections/service/`, and the cleanup todo (`contacts-route-dead-code-cleanup.md`) records user approval to delete OUTSIDE the Phase 93 scope. Closes POL-03.

Purpose: Removes 4 unused components from the canonical Next source tree. Reduces audit surface for Phase 95 (axe-core, Lighthouse) and removes a path discrepancy noted in 93-SWEEP-AUDIT.md Outstanding Item #1.

Output: Delete 4 files; produce a deletion audit document; verify build/lint green; verify /contacts still renders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/contacts-route-dead-code-cleanup.md
@.planning/phases/93-per-page-propagation-sub-routes/93-SWEEP-AUDIT.md
@next/src/app/contacts/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion reachability audit + write 94-02-DELETION-AUDIT.md</name>
  <files>.planning/phases/94-polish-and-hygiene/94-02-DELETION-AUDIT.md</files>
  <read_first>.planning/todos/pending/contacts-route-dead-code-cleanup.md, next/src/app/contacts/page.tsx</read_first>
  <action>Before deleting anything, prove the 4 files are unreferenced.

The actual on-disk path is `next/src/components/sections/contacts/` — the cleanup todo says `next/src/app/contacts/*` but Phase 93 Plan 05 confirmed this is wrong. Use the canonical path.

Run these greps and write the verbatim output into `.planning/phases/94-polish-and-hygiene/94-02-DELETION-AUDIT.md`:

```
# 1. Symbol grep across all of next/src
grep -rnE '\b(ContactsHero|ContactMethodGrid|CoordinatorCard|TrustBadges)\b' next/src/

# 2. Path-import grep
grep -rnE "from ['\\\"](@/components/sections/contacts|\\.\\./components/sections/contacts)" next/src/

# 3. List of files about to be deleted
ls -la next/src/components/sections/contacts/
```

The audit document MUST include:
- Verbatim grep output (each command + output block)
- A 4-row table listing each file (`ContactsHero.tsx`, `ContactMethodGrid.tsx`, `CoordinatorCard.tsx`, `TrustBadges.tsx`) with disposition `DELETE` and column "References found: 0"
- A "Build status (pre-deletion)" footer with output of `cd next && pnpm build 2>&1 | tail -20` (so any pre-existing build errors are recorded as baseline, not blamed on deletion)

If grep #1 returns ANY hit OUTSIDE the 4 doomed files themselves, ABORT — record the hit in the audit and create a follow-up task; do NOT proceed to Task 2. Per the cleanup todo, the precondition is "grep across `next/src/` returns 0 references."

Symbols match exactly the 4 component names — these are PascalCase TSX exports unique enough that string-match is safe. (No need to escape anything inside the regex other than the word boundary.)</action>
  <verify>
    <automated>test -f .planning/phases/94-polish-and-hygiene/94-02-DELETION-AUDIT.md &amp;&amp; grep -c "DELETE" .planning/phases/94-polish-and-hygiene/94-02-DELETION-AUDIT.md | awk '{exit !($1 &gt;= 4)}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/94-polish-and-hygiene/94-02-DELETION-AUDIT.md` exists
    - File contains all 4 component names: `ContactsHero`, `ContactMethodGrid`, `CoordinatorCard`, `TrustBadges`
    - File contains the verbatim output of all 3 grep commands
    - File contains the string `References found: 0` at least 4 times (one per doomed file) OR the audit aborts with a clear "ABORT" header naming the unexpected import
  </acceptance_criteria>
  <done>Audit document written; either ABORT recorded with offending import OR 0 references confirmed for all 4 files.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 4 files + verify build / lint / /contacts render</name>
  <files>next/src/components/sections/contacts/ContactsHero.tsx, next/src/components/sections/contacts/ContactMethodGrid.tsx, next/src/components/sections/contacts/CoordinatorCard.tsx, next/src/components/sections/contacts/TrustBadges.tsx</files>
  <read_first>.planning/phases/94-polish-and-hygiene/94-02-DELETION-AUDIT.md</read_first>
  <action>If 94-02-DELETION-AUDIT.md does NOT contain `ABORT`, proceed:

1. Delete the 4 files via `rm`:
   - `next/src/components/sections/contacts/ContactsHero.tsx`
   - `next/src/components/sections/contacts/ContactMethodGrid.tsx`
   - `next/src/components/sections/contacts/CoordinatorCard.tsx`
   - `next/src/components/sections/contacts/TrustBadges.tsx`

2. If `next/src/components/sections/contacts/` is now empty, also remove the empty directory: `rmdir next/src/components/sections/contacts/`. (If it has any remaining files, leave the directory and just delete the 4 doomed ones.)

3. Re-run `cd next && pnpm install --offline 2>/dev/null || true` (no-op if no lock changes; defensive only — deletion does not change package.json).

4. Verify the build still passes: `cd next && pnpm build`. Capture the tail (last 30 lines) for the SUMMARY.

5. Verify lint still passes: `cd next && pnpm lint`. Capture output.

6. Verify /contacts still renders against a dev server: `cd next && PLAYWRIGHT_PORT=3100 pnpm exec playwright test tests/visual/baseline.spec.ts -g 'contacts' --project=desktop --reporter=list`. The visual diff baseline IS the post-Phase-93 snapshot, so this passes if the route still renders identically (which it will, because the deleted files were unrendered).

If ANY of build/lint/playwright fails, restore the files via `git checkout` and ABORT — record the failure in the SUMMARY.

Do NOT delete `next/src/app/contacts/page.tsx` (that IS the rendered route). Do NOT delete or modify `LeadFormSection.tsx` or anything in `next/src/components/sections/service/`.</action>
  <verify>
    <automated>cd next &amp;&amp; test ! -e src/components/sections/contacts/ContactsHero.tsx &amp;&amp; test ! -e src/components/sections/contacts/ContactMethodGrid.tsx &amp;&amp; test ! -e src/components/sections/contacts/CoordinatorCard.tsx &amp;&amp; test ! -e src/components/sections/contacts/TrustBadges.tsx &amp;&amp; pnpm build 2&gt;&amp;1 | tail -5 &amp;&amp; pnpm lint 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files do NOT exist after this task: `next/src/components/sections/contacts/ContactsHero.tsx`, `ContactMethodGrid.tsx`, `CoordinatorCard.tsx`, `TrustBadges.tsx`
    - `cd next && pnpm build` exits 0
    - `cd next && pnpm lint` exits 0
    - `grep -rE '\\b(ContactsHero|ContactMethodGrid|CoordinatorCard|TrustBadges)\\b' next/src/` returns 0 lines
    - Playwright `baseline.spec.ts` test for `/contacts` passes (no visual regression)
  </acceptance_criteria>
  <done>4 files removed; build + lint + /contacts visual diff all green; SUMMARY captures evidence.</done>
</task>

</tasks>

<verification>
- 4 named TSX files do not exist on disk after this plan executes.
- 0 references to any of the 4 component names anywhere in `next/src/`.
- `next` package builds and lints cleanly.
- `/contacts` route still renders identically (post-Phase-93 baseline preserved).
</verification>

<success_criteria>
- POL-03 success criterion 3 satisfied (REQUIREMENTS.md): 4 dead-code files removed; `grep -r` across canonical `src/` returns 0 references; `pnpm build` and `pnpm lint` exit 0.
- 93-SWEEP-AUDIT.md Outstanding Item #1 (path discrepancy) resolved by using the actual on-disk path.
</success_criteria>

<output>
After completion, create `.planning/phases/94-polish-and-hygiene/94-02-SUMMARY.md` recording:
- The deletion-audit grep findings
- 4 files deleted (with `git diff --stat` line counts removed)
- Build / lint / Playwright /contacts results (tail of each)
- Commit hash
</output>
