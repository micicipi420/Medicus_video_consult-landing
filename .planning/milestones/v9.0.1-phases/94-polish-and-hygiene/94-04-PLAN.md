---
phase: 94-polish-and-hygiene
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
  - CLAUDE.md
  - next/src/components/sections/service/ServiceHero.tsx
  - next/src/components/layout/StickyBar.tsx
  - .planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md
autonomous: true
requirements: [POL-02]
must_haves:
  truths:
    - "Mobile `/treatment-abroad` hero viewport satisfies the `≤2 glass per viewport` contract — either by code change or by documented contract clarification"
    - "DESIGN.md's `≤2 glass per viewport` rule states unambiguously whether sticky chrome (header, sticky bottom CTA / FAB) counts toward the budget"
    - "CLAUDE.md mirrors the DESIGN.md rule — no contract drift between the two"
    - "Resolution method is captured in the commit message"
  artifacts:
    - path: ".planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md"
      provides: "Live measurement of glass surfaces visible at /treatment-abroad mobile hero, plus the decision rationale"
      contains: "ServiceHero"
    - path: "DESIGN.md"
      provides: "Updated glass-budget rule (clarified scope: chrome vs content)"
      contains: "Glass layer count"
  key_links:
    - from: "DESIGN.md"
      to: "CLAUDE.md"
      via: "design-contract mirror"
      pattern: "≤2 glass"
---

<objective>
Resolve the mobile `≤2 glass per viewport` contract violation flagged by Phase 93 UAT (F2) on `/treatment-abroad` hero (375×667). Three glass surfaces are simultaneously visible: sticky `<header>`, eyebrow pill `МЕДИЦИНСКИЙ ТУРИЗМ` (ServiceHero `eyebrow` prop), and sticky bottom CTA wrapper (StickyBar). Two of those (header + StickyBar) pre-date Phase 93 — only the eyebrow is "content" by any reasonable reading. Closes POL-02.

Per the cleanup todo's Suggested Fix and CLAUDE.md's autonomous-decisions delegation, default resolution is option (3): clarify in DESIGN.md and CLAUDE.md that sticky chrome surfaces (sticky `<header>`, sticky bottom FAB) do NOT count toward the per-viewport glass budget, since the rule was authored against content-glass nesting risk (Phase 82 lesson) not chrome co-presence. This is the cheapest fix, preserves desktop and mobile look exactly, and unblocks Phase 95 audits.

If the live measurement in Task 1 surfaces a 4th content-glass surface (i.e. header + StickyBar + eyebrow + ANY other content glass) that would still push the count above 2 even after carving chrome out of the budget, fall back to a UI fix (drop eyebrow pill on mobile to non-glass) per option (1) from the todo. This branching is encoded in Task 2.

Purpose: Clears the only outstanding design-contract violation surfaced by Phase 93 UAT. Establishes a clean contract reading for Phase 95 axe-core / Lighthouse / brand-review work.

Output: An investigation document with live measurement; a clarified DESIGN.md rule; mirrored CLAUDE.md text; optionally an eyebrow-pill mobile fallback if measurement shows it's needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/mobile-hero-glass-layer-count.md
@.planning/phases/93-per-page-propagation-sub-routes/93-UAT.md
@DESIGN.md
@CLAUDE.md
@next/src/components/sections/service/ServiceHero.tsx
@next/src/components/layout/Header.tsx
@next/src/components/layout/StickyBar.tsx
@next/src/app/treatment-abroad/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-measure glass surfaces at /treatment-abroad mobile hero (375×667)</name>
  <files>.planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md</files>
  <read_first>next/src/components/sections/service/ServiceHero.tsx, next/src/components/layout/Header.tsx, next/src/components/layout/StickyBar.tsx, .planning/todos/pending/mobile-hero-glass-layer-count.md</read_first>
  <action>Use Playwright MCP (or `cd next && PLAYWRIGHT_PORT=3100 pnpm exec playwright test ...` against a running dev server) to measure exactly which DOM elements have non-zero `backdrop-filter: blur(...)` AND a non-zero alpha `background-color` at the top-of-page viewport on `/treatment-abroad` mobile (375×667).

Steps:

1. Start `cd next && PORT=3100 pnpm dev` in the background (or use the existing dev server on port 3100 if already running).

2. Use Playwright to:
   - Set viewport to 375×667
   - Navigate to `/treatment-abroad`
   - Wait for `networkidle`
   - Scroll to top
   - Run JS in page context: enumerate every element where `getComputedStyle(el).backdropFilter` matches `/blur\([1-9]/` AND `getComputedStyle(el).backgroundColor` is NOT `rgba(0,0,0,0)` / `transparent`. For each match, capture:
     - Tag name + relevant class list (truncate to first 80 chars)
     - Computed `backdrop-filter` value
     - Computed `background-color` value
     - Bounding rect (x, y, width, height)
     - Whether it intersects the visible viewport (0..667 y range)

3. Filter to elements whose bounding rect intersects 0..667 — this is the "in-viewport" set.

4. Categorize each in-viewport glass surface as:
   - **CHROME** if it's `<header>` or descendant, OR if it's the StickyBar (`fixed bottom-...`)
   - **CONTENT** otherwise (e.g. eyebrow pill, hero card, etc.)

5. Write `.planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md` containing:
   - The Playwright script used (verbatim)
   - The list of in-viewport glass surfaces (tag + class + computed style + rect)
   - A categorization table (CHROME vs CONTENT) with counts
   - A "Conclusion" section stating: "If we exclude chrome from the budget, the content-glass count is N (target: ≤2)."
   - A "Decision" section selecting one of two paths:
     - **Path A** if `content_count <= 2`: Clarify DESIGN.md + CLAUDE.md that chrome does not count. NO source changes needed.
     - **Path B** if `content_count > 2`: Apply Path A clarification AND drop eyebrow pill to non-glass on mobile in `ServiceHero.tsx` (e.g. wrap the eyebrow JSX in `md:` prefix on the glass classes, or use `md:bg-[var(--glass-section-fill)]` and `md:backdrop-blur-[var(--glass-section-blur)]` so mobile has solid pale fill instead of glass).

Per autonomous-decisions delegation in MEMORY.md, Task 2 will execute the chosen path without prompting the user. Path A is overwhelmingly likely from the prior UAT data (3 surfaces total, 2 of them chrome → content_count = 1 = within budget).

If the dev server is unreachable (port 3000 + 3100 both occupied), record the obstruction in the investigation doc and HALT — do NOT proceed to Task 2.</action>
  <verify>
    <automated>test -f .planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md &amp;&amp; grep -c "Path A\|Path B" .planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md | awk '{exit !($1 &gt;= 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md` exists
    - File contains a categorization table with CHROME vs CONTENT counts
    - File contains the literal string `Path A` or `Path B` selecting the resolution path
    - File contains the Playwright enumeration script (verbatim, copy-pasteable)
  </acceptance_criteria>
  <done>Investigation document captures the live measurement and selects Path A or Path B based on data.</done>
</task>

<task type="auto">
  <name>Task 2: Apply chosen path — clarify DESIGN.md/CLAUDE.md (always); drop eyebrow pill on mobile (only if Path B)</name>
  <files>DESIGN.md, CLAUDE.md, next/src/components/sections/service/ServiceHero.tsx</files>
  <read_first>.planning/phases/94-polish-and-hygiene/94-04-INVESTIGATION.md, DESIGN.md</read_first>
  <action>Read the Decision section of `94-04-INVESTIGATION.md`. Apply Path A unconditionally, plus Path B if and only if the investigation selected it.

**Path A (always — DESIGN.md + CLAUDE.md clarification):**

1. In `DESIGN.md` § "Project-specific hard constraints (stricter than HIG floor)" (around line 633), update the existing bullet:

   Existing: `- **Glass layer count:** ≤ 2 glass elements per viewport. Stacked glass-on-glass is forbidden — kills GPU on budget Android devices that dominate the KZ market.`

   Replace with:
   ```
   - **Glass layer count (content):** ≤ 2 glass content surfaces per viewport. Stacked glass-on-glass is forbidden — kills GPU on budget Android devices that dominate the KZ market. **Sticky chrome surfaces** (`<header>`, sticky bottom FAB / `StickyBar`, modal backdrops) are excluded from this budget — they're at most 2 chrome layers in any viewport, are positioned outside the content flow, and were measured stable on real-device UAT (v9.0 Phase 93). The constraint targets content-glass nesting (the Phase 82 services-grid/sticky-bar overlap lesson), not chrome co-presence.
   ```

2. Update DESIGN.md `## Anti-Patterns` row #13 (around line 316 in the YAML and ~line 739 in the markdown body) accordingly:
   - YAML entry name: `>2 glass content layers per viewport` (was `>2 glass layers per viewport`)
   - Markdown body anti-pattern #13: rephrase to "Why: `DESIGN.md` hard constraint; mobile-first ЦА 45+ readability + GPU budget. Where it manifests: nested CONTENT-glass cards inside a CONTENT-glass section inside a CONTENT-glass container. Chrome (sticky header, sticky FAB) is excluded — see `Project-specific hard constraints` in the Liquid Glass section. Do instead: respect responsive-glass-nesting (mobile = 1 wrapper, desktop = N cards). Phase 82 `StatsBar` is the reference implementation."

3. In `CLAUDE.md` § Constraints / Design line, update:

   Existing: `- **Design**: Mobile-first, ЦА 45+ — крупный шрифт, понятная навигация, высокий контраст. Mobile blur ≤12px, ≤2 glass elements per viewport.`

   Replace with:
   ```
   - **Design**: Mobile-first, ЦА 45+ — крупный шрифт, понятная навигация, высокий контраст. Mobile blur ≤12px, ≤2 glass CONTENT elements per viewport (sticky chrome — header, FAB — excluded; see DESIGN.md § "Project-specific hard constraints").
   ```

**Path B (only if investigation selected it — drop eyebrow pill on mobile):**

If and only if `94-04-INVESTIGATION.md` Decision section says `Path B`, also edit `next/src/components/sections/service/ServiceHero.tsx` to drop the eyebrow pill from glass on mobile:

- Line 35 currently:
  ```tsx
  <div className="mb-5 inline-flex max-w-full items-center gap-2 rounded-full border border-white/60 bg-[var(--glass-section-fill)] px-4 py-2.5 shadow-glass-inner backdrop-blur-[var(--glass-section-blur)] sm:px-5">
  ```

- Replace with mobile-non-glass / desktop-glass split (uses Tailwind `md:` prefix to apply glass only at ≥768px; mobile gets a solid pale fill `bg-mu-blue-50` or similar pre-existing token):
  ```tsx
  <div className="mb-5 inline-flex max-w-full items-center gap-2 rounded-full border border-white/60 bg-mu-blue-50 px-4 py-2.5 shadow-glass-inner md:bg-[var(--glass-section-fill)] md:backdrop-blur-[var(--glass-section-blur)] sm:px-5">
  ```

  (Verify `bg-mu-blue-50` exists as a Tailwind token; if not, substitute the closest pale `--mu-*` token from `next/src/app/globals.css`. Do NOT invent a new color — DESIGN.md "Brand color parity" rule.)

Either way (Path A or Path B), at end of task: run `cd next && pnpm build` and `pnpm lint`. Both must exit 0.

Commit message must explicitly state which path was taken. Example: `docs(94): POL-02 — clarify ≤2 glass-per-viewport excludes chrome (Path A based on investigation)`.</action>
  <verify>
    <automated>grep -c 'Glass layer count (content)' DESIGN.md | awk '{exit !($1 &gt;= 1)}' &amp;&amp; grep -c 'glass CONTENT elements per viewport' CLAUDE.md | awk '{exit !($1 &gt;= 1)}' &amp;&amp; cd next &amp;&amp; pnpm build 2&gt;&amp;1 | tail -3 &amp;&amp; pnpm lint 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `DESIGN.md` contains the new phrase `Glass layer count (content)` (Path A applied)
    - `DESIGN.md` contains the phrase `Sticky chrome surfaces` in the Liquid Glass constraints section
    - `CLAUDE.md` contains the phrase `glass CONTENT elements per viewport`
    - DESIGN.md anti-pattern #13 still exists but the wording reflects content-only scope
    - If `94-04-INVESTIGATION.md` says Path B → `ServiceHero.tsx` line 35 contains both `md:bg-[var(--glass-section-fill)]` AND a non-glass mobile fallback fill
    - `cd next && pnpm build` exits 0
    - `cd next && pnpm lint` exits 0
  </acceptance_criteria>
  <done>DESIGN.md + CLAUDE.md clarified; build/lint green; if Path B, ServiceHero.tsx has mobile-non-glass eyebrow.</done>
</task>

</tasks>

<verification>
- `/treatment-abroad` mobile hero satisfies the (clarified) glass budget — verifiable by re-running the Task 1 measurement and confirming content-glass count ≤ 2.
- DESIGN.md and CLAUDE.md state the same rule (no drift).
- DESIGN.md anti-pattern #13 still exists and reflects the clarified scope.
- Build + lint clean.
</verification>

<success_criteria>
- POL-02 success criterion 2 (REQUIREMENTS.md) satisfied: contract resolved either by UI fix OR by DESIGN.md clarification; resolution method documented in commit message.
- 93-UAT.md F2 finding closed.
- No regression on other routes (the eyebrow pill rule applies wherever ServiceHero is used; if Path B is taken, all 3 service pages get the same mobile fallback — verify visually with `pnpm exec playwright test tests/visual/baseline.spec.ts --project=mobile-375`).
</success_criteria>

<output>
After completion, create `.planning/phases/94-polish-and-hygiene/94-04-SUMMARY.md` recording:
- Path A vs Path B selection rationale (with reference to investigation doc)
- The DESIGN.md / CLAUDE.md diff hunks
- If Path B, the ServiceHero.tsx diff
- Visual baseline regen needed? (yes if Path B, no if Path A)
- Commit hash
</output>
