---
phase: 97-admin-submissions-view
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - next/src/app/admin/page.tsx
  - next/src/app/admin/submissions-table.tsx
  - next/src/app/admin/submission-mappings.ts
  - next/src/app/admin/pagination.tsx
  - .planning/phases/97-admin-submissions-view/97-02-SUMMARY.md
autonomous: true
requirements: [ADM-02]
must_haves:
  truths:
    - "View renders submissions sorted by date_created DESC by default"
    - "Each page shows at most 50 submissions; pagination controls advance/retreat"
    - "All 6 columns render: дата, имя, телефон, направление, описание (truncated 80ch), статус"
    - "Specialization renders as Russian label; raw English keys are mapped if present"
    - "Total submission count and current page range is visible (e.g., '51–100 из 247')"
    - "Description column truncates at 80 characters with ellipsis; full text accessible via title attr"
    - "Loading state visible during SSR transition between pages"
  artifacts:
    - path: "next/src/app/admin/page.tsx"
      provides: "RSC that queries Drizzle with limit/offset and passes rows to client table"
      contains: "limit(50)"
    - path: "next/src/app/admin/submissions-table.tsx"
      provides: "Client component renders 6 columns + Russian labels + truncation"
      contains: "Описание"
    - path: "next/src/app/admin/submission-mappings.ts"
      provides: "specialization label map (handles BOTH stored Russian strings AND legacy English keys)"
      exports: ["SPECIALIZATION_LABELS", "STATUS_LABELS", "labelForSpecialization", "truncate"]
    - path: "next/src/app/admin/pagination.tsx"
      provides: "Client pagination component reading/writing ?page= URL param"
  key_links:
    - from: "next/src/app/admin/page.tsx"
      to: "drizzle submissions table"
      via: "db.select().from().orderBy(desc(date_created)).limit(50).offset(...)"
      pattern: "limit\\(50\\).*offset"
    - from: "next/src/app/admin/page.tsx"
      to: "next/src/app/admin/pagination.tsx"
      via: "props.totalCount + props.currentPage"
      pattern: "<Pagination"
    - from: "next/src/app/admin/submissions-table.tsx"
      to: "submission-mappings.ts"
      via: "import labelForSpecialization, STATUS_LABELS"
      pattern: "labelForSpecialization"
---

<objective>
Upgrade the existing /admin/submissions view (currently next/src/app/admin/page.tsx +
submissions-table.tsx, shipped pre-Phase 97) so that it satisfies ADM-02:

- 6 columns including the previously missing "Описание" (truncated 80ch) and proper
  "Дата заявки" header text
- Specialization rendered via Russian-label mapping that is RESILIENT to both legacy
  English keys (consultation/treatment/checkup/not-sure) AND stored-Russian strings
  ('Онлайн-консультация', 'Лечение за рубежом', 'Чек-ап', 'Пока не определился')
  because actions.ts already maps to Russian at WRITE time
- Server-side pagination at 50 rows/page via LIMIT/OFFSET, ?page= URL param
- Total count + page range indicator
- Loading state on page-to-page navigation

Purpose: operator can scan inbound leads at a glance without scrolling through 200+ rows
or losing the description preview that contains the case detail. ADM-02 is the heart of
the view; ADM-03 will layer filters on top in plan 03.

Output: 6-column paginated table with stable Russian labels and 50/page bound.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@./CLAUDE.md
@./DESIGN.md
@.planning/phases/97-admin-submissions-view/97-01-SUMMARY.md
@next/src/app/admin/page.tsx
@next/src/app/admin/submissions-table.tsx
@next/src/lib/db/schema.ts
@next/src/lib/db/actions.ts
@next/src/lib/db/index.ts

<interfaces>
<!-- Drizzle schema (existing, frozen for this plan): -->
```typescript
// next/src/lib/db/schema.ts
export const submissions = pgTable('submissions', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: varchar('name', { length: 255 }).notNull(),
  phone: varchar('phone', { length: 30 }).notNull(),
  specialization: varchar('specialization', { length: 100 }).notNull(),
  description: text('description'),
  status: varchar('status', { length: 50 }).notNull().default('new'),
  dateCreated: timestamp('date_created', { withTimezone: true }).notNull().defaultNow(),
});
```

<!-- CRITICAL discovery from actions.ts: specialization is stored as RUSSIAN STRINGS, not -->
<!-- raw English keys, because submitContactForm maps before insert: -->
```typescript
// next/src/lib/db/actions.ts (existing)
const specMap: Record<string, string> = {
  consultation: 'Онлайн-консультация',
  treatment:    'Лечение за рубежом',
  checkup:      'Чек-ап',
  'not-sure':   'Пока не определился',
};
await db.insert(submissions).values({
  // ...
  specialization: specMap[result.data.specialization] ?? result.data.specialization,
});
```

<!-- Phase context advice ("Консультация" / "Лечение" / "Чек-ап" / "Не уверен") does NOT match -->
<!-- what is in the DB. The mapping in submission-mappings.ts MUST handle the stored Russian -->
<!-- form. We also keep the English-key fallback in case any legacy or test row was inserted -->
<!-- without the mapping. -->

<!-- Existing client component (will be EXTENDED, not replaced): -->
```typescript
// next/src/app/admin/submissions-table.tsx — current shape
export type SubmissionRow = { /* defined in page.tsx */ };
interface SubmissionsTableProps { submissions: SubmissionRow[]; }
// 5 columns: Дата | Имя | Телефон | Направление | Статус
// Client-side filter via useState — will be REPLACED by URL-driven filter in plan 03.
// For plan 02: keep client-side filter intact, ADD description column + pagination.
```

<!-- Drizzle count helper: -->
```typescript
import { sql, desc } from 'drizzle-orm';
const [{ count }] = await db
  .select({ count: sql<number>`count(*)::int` })
  .from(submissions);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Submission mappings + truncate utility</name>
  <files>
    next/src/app/admin/submission-mappings.ts
    next/src/app/admin/submission-mappings.test.ts
  </files>
  <behavior>
    - labelForSpecialization('consultation') === 'Онлайн-консультация'
    - labelForSpecialization('treatment') === 'Лечение за рубежом'
    - labelForSpecialization('checkup') === 'Чек-ап'
    - labelForSpecialization('not-sure') === 'Пока не определился'
    - labelForSpecialization('Онлайн-консультация') === 'Онлайн-консультация'  // already-mapped passthrough
    - labelForSpecialization('Лечение за рубежом') === 'Лечение за рубежом'   // already-mapped passthrough
    - labelForSpecialization('Чек-ап') === 'Чек-ап'                            // already-mapped passthrough
    - labelForSpecialization('Пока не определился') === 'Пока не определился' // already-mapped passthrough
    - labelForSpecialization('garbage-unknown') === 'garbage-unknown'          // unknown passthrough (visible to operator, not silent fallback)
    - labelForSpecialization('') === '—'                                       // empty becomes em-dash placeholder
    - labelForSpecialization(null) === '—'
    - STATUS_LABELS['new'] === 'Новая'
    - STATUS_LABELS['contacted'] === 'Связались'
    - STATUS_LABELS['completed'] === 'Завершено'
    - truncate('exactly eighty chars...', 80) returns string with length === 80 and trailing '…' if input was longer
    - truncate('short', 80) returns 'short' unchanged
    - truncate(null, 80) returns ''
    - truncate(undefined, 80) returns ''
  </behavior>
  <action>
    Create `next/src/app/admin/submission-mappings.ts`:

    ```typescript
    /**
     * Specialization storage history:
     * - Pre-v3.1: raw English keys (consultation/treatment/checkup/not-sure)
     * - Current (per actions.ts): Russian strings, mapped at WRITE time before db.insert
     *   ('Онлайн-консультация' / 'Лечение за рубежом' / 'Чек-ап' / 'Пока не определился')
     *
     * The display layer must accept BOTH forms because some legacy rows may exist as
     * English keys, and any new write produces the Russian form. This mapping is
     * idempotent — the Russian form maps to itself.
     */
    export const SPECIALIZATION_LABELS: Record<string, string> = {
      consultation: 'Онлайн-консультация',
      treatment:    'Лечение за рубежом',
      checkup:      'Чек-ап',
      'not-sure':   'Пока не определился',
      // idempotent passthrough for already-mapped Russian strings:
      'Онлайн-консультация': 'Онлайн-консультация',
      'Лечение за рубежом':  'Лечение за рубежом',
      'Чек-ап':              'Чек-ап',
      'Пока не определился': 'Пока не определился',
    };

    export function labelForSpecialization(raw: string | null | undefined): string {
      if (!raw) return '—';
      return SPECIALIZATION_LABELS[raw] ?? raw;
    }

    export const STATUS_LABELS: Record<string, string> = {
      new: 'Новая',
      contacted: 'Связались',
      completed: 'Завершено',
    };

    export function truncate(text: string | null | undefined, max: number): string {
      if (!text) return '';
      if (text.length <= max) return text;
      // Reserve 1 char for ellipsis so total length === max
      return text.slice(0, max - 1) + '…';
    }
    ```

    Co-author behavior tests in `submission-mappings.test.ts` (Vitest if configured,
    else node:test — match the choice from plan 01 task 1).

    NOTE: actions.ts uses the longer Russian strings 'Онлайн-консультация' (NOT 'Консультация')
    and 'Лечение за рубежом' (NOT 'Лечение') and 'Пока не определился' (NOT 'Не уверен'). The
    phase context's shorter labels are wrong — use the actual stored strings to avoid
    creating a SECOND independent label set that diverges from what users see in the form.
  </action>
  <verify>
    <automated>cd next && (grep -q '"vitest"' package.json && pnpm vitest run src/app/admin/submission-mappings.test.ts) || node --test src/app/admin/submission-mappings.test.ts</automated>
  </verify>
  <done>
    All behavior cases pass. Mapping handles both English keys AND already-stored Russian
    strings idempotently. Truncate produces exact-length output with ellipsis.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pagination component</name>
  <files>
    next/src/app/admin/pagination.tsx
    next/src/app/admin/pagination.test.tsx
  </files>
  <behavior>
    - Renders "Назад" button disabled when currentPage === 1
    - Renders "Вперёд" button disabled when currentPage === totalPages
    - Renders text "Страница X из Y" between buttons
    - Renders text showing current row range "<start>–<end> из <total>" (e.g., "51–100 из 247")
    - "Вперёд" navigates to ?page=N+1 preserving any other query params (filter params from plan 03)
    - "Назад" navigates to ?page=N-1 preserving any other query params
    - When totalPages === 1, both buttons disabled and no "Страница 1 из 1" displayed (or display
      is allowed — pick consistent behavior and assert it in test)
    - When totalCount === 0, displays "Нет заявок" and no buttons
  </behavior>
  <action>
    Create `next/src/app/admin/pagination.tsx` as a Client Component:

    ```typescript
    'use client';
    import { useRouter, useSearchParams, usePathname } from 'next/navigation';

    interface PaginationProps {
      currentPage: number;   // 1-indexed
      pageSize: number;      // 50
      totalCount: number;
    }

    export function Pagination({ currentPage, pageSize, totalCount }: PaginationProps) {
      const router = useRouter();
      const pathname = usePathname();
      const params = useSearchParams();

      const totalPages = Math.max(1, Math.ceil(totalCount / pageSize));
      const start = totalCount === 0 ? 0 : (currentPage - 1) * pageSize + 1;
      const end = Math.min(currentPage * pageSize, totalCount);

      function navigate(nextPage: number) {
        const next = new URLSearchParams(params.toString());
        if (nextPage <= 1) next.delete('page'); else next.set('page', String(nextPage));
        router.push(`${pathname}?${next.toString()}`);
      }

      // ... render Назад / range / Страница X из Y / Вперёд
    }
    ```

    Use shadcn Button primitive (`@/components/ui/button`) for visual consistency with
    submissions-table. NO glass tokens — opaque utility per DESIGN.md / Phase 93 audit.

    Co-author tests using @testing-library/react if configured; otherwise the test can be a
    snapshot/render test via `node:test` + `experimental react-server-dom-webpack` is too heavy
    — fall back to a unit test of the pure pagination math (`computePageRange(currentPage,
    pageSize, totalCount)` extracted as exported helper) to keep tests trivial.

    SIMPLIFICATION (recommended): export `computePageRange` as a pure function from
    pagination.tsx, test it via node:test, and skip JSX rendering tests. Visual behavior is
    covered by the Task 4 manual smoke test.
  </action>
  <verify>
    <automated>cd next && (grep -q '"vitest"' package.json && pnpm vitest run src/app/admin/pagination.test.tsx) || node --test src/app/admin/pagination.test.tsx</automated>
  </verify>
  <done>
    All behavior cases pass. computePageRange handles totalCount=0, totalCount=1,
    totalCount=247 with pageSize=50 (5 pages, last page 47 rows), and out-of-range
    currentPage gracefully (clamps to [1, totalPages]).
  </done>
</task>

<task type="auto">
  <name>Task 3: Server-side pagination + 6-column table extension</name>
  <files>
    next/src/app/admin/page.tsx
    next/src/app/admin/submissions-table.tsx
  </files>
  <action>
    **page.tsx changes:**

    1. Accept searchParams: `{ searchParams: Promise<{ page?: string }> }`. In Next 15
       searchParams is async — use `const sp = await searchParams; const page = parsePage(sp.page);`
    2. Extract `parsePage(raw: string | undefined): number` — defaults to 1, clamps to
       positive integer, NaN-safe.
    3. Compute offset = (page - 1) * 50.
    4. Replace existing query with TWO queries (run in parallel via Promise.all):
       ```typescript
       const [rows, totals] = await Promise.all([
         db.select().from(submissions).orderBy(desc(submissions.dateCreated)).limit(50).offset(offset),
         db.select({ count: sql<number>`count(*)::int` }).from(submissions),
       ]);
       const totalCount = totals[0]?.count ?? 0;
       ```
    5. Pass `currentPage`, `pageSize: 50`, `totalCount` to a new `<Pagination />` rendered
       above and below the table inside `<main>`.
    6. Update the count badge text from "Всего: N" to "Страница X из Y · Всего: N".
    7. Preserve `export const dynamic = 'force-dynamic'` and `metadata.robots.index = false`.
    8. Add a "loading.tsx" sibling: `next/src/app/admin/loading.tsx` rendering
       a simple skeleton ("Загрузка заявок…") so the SSR transition has a loading state.

    **submissions-table.tsx changes:**

    1. Add 6th column "Описание" between "Направление" and "Статус":
       ```tsx
       <TableHead>Описание</TableHead>
       ```
    2. Update header text "Дата" → "Дата заявки" per phase-context spec; keep "Имя" /
       "Телефон" / "Направление" / "Статус" but add "Описание" between them.
    3. Render description cell using `truncate(row.description, 80)`. Add `title={row.description ?? ''}`
       attribute to the `<TableCell>` so operator can hover to read full text.
    4. Replace specialization cell content with `labelForSpecialization(row.specialization)`.
    5. Update colSpan in empty-state row from 5 to 6.
    6. Import: `import { labelForSpecialization, STATUS_LABELS, truncate } from './submission-mappings';`
       — REMOVE the local STATUS_LABELS constant (now in submission-mappings.ts).
    7. PRESERVE the existing client-side filter UI for now — plan 03 will replace it with
       URL-driven filters. This plan is additive only.

    Threat T-97-08 mitigation: do NOT log row contents anywhere. The catch block in page.tsx
    already returns a generic error message — keep it that way. Per CLAUDE.md / project
    instructions, PII (phone numbers) must never appear in server logs.
  </action>
  <verify>
    <automated>cd next && pnpm build && pnpm lint</automated>
  </verify>
  <done>
    Build + lint green. Description column visible. Specialization renders via mapping.
    Pagination component appears above and below table. URL ?page=2 changes offset.
    Loading skeleton renders during navigation.
  </done>
</task>

<task type="auto">
  <name>Task 4: Manual data smoke + curl verification</name>
  <files>
    .planning/phases/97-admin-submissions-view/97-02-MANUAL-VERIFY.md
  </files>
  <action>
    Verify against a running database. Steps:

    1. Ensure local Postgres has at least 51 submissions (so pagination is exercised).
       If not, generate via:
       ```bash
       for i in {1..55}; do
         curl -s -X POST http://localhost:3000/api/contact \
           -H 'content-type: application/json' \
           -d "{\"name\":\"Test $i\",\"phone\":\"+7 (701) 532 24 78\",\"specialization\":\"checkup\",\"description\":\"Lorem ipsum description for row $i — long enough to test the 80-character truncation rule applied in the admin view rendering layer\",\"honeypot\":\"\",\"loadTime\":$(($(date +%s%N)/1000000 - 5000))}";
       done
       ```
       (The contact form endpoint may not exist as a REST route — adapt to the actual server
       action invocation method, or insert directly via `psql` for the test.)

    2. With ADMIN_TOKEN set in .env.local, visit
       `http://localhost:3000/admin/submissions?token=<token>` — verify:
       - 6 columns present in this order: Дата заявки | Имя | Телефон | Направление | Описание | Статус
       - Direction (Направление) shows Russian labels (e.g., "Чек-ап") not raw keys
       - Description column shows truncated text with "…" if original > 80 chars
       - Hover over description cell shows full text via title attribute
       - Pagination text reads "1–50 из 55" (or similar)
       - "Вперёд" button is enabled, "Назад" is disabled
    3. Visit `?token=<token>&page=2` — verify rows 51–55 visible, "Назад" enabled, "Вперёд" disabled.
    4. Verify `pnpm build && pnpm lint` exit 0.
    5. Verify Drizzle query produces correct SQL via `db:studio` or postgres logs:
       expect `SELECT * FROM submissions ORDER BY date_created DESC LIMIT 50 OFFSET 50`.

    Record outputs in `97-02-MANUAL-VERIFY.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/97-admin-submissions-view/97-02-MANUAL-VERIFY.md && grep -q 'page=2' .planning/phases/97-admin-submissions-view/97-02-MANUAL-VERIFY.md</automated>
  </verify>
  <done>
    Manual verify document exists with screenshots or curl outputs confirming all 5 checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → admin RSC | Authenticated by plan 01 gate |
| admin RSC → Postgres | Trusted; uses Drizzle parameterized queries (no raw user input) |
| RSC server → operator browser | Sends PII in HTML; relies on TLS at edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-08 | Information Disclosure | server log of submission rows | mitigate | Catch block returns generic message; never log row contents. Existing actions.ts already errors on insert generically — preserve that posture in the read path. |
| T-97-09 | Tampering | ?page= injection | mitigate | parsePage() clamps to positive integer; non-numeric → 1; offset always >= 0. Drizzle limit/offset are parameter-bound. |
| T-97-10 | Information Disclosure | description full text in DOM | accept | Operator-only route (gate active per plan 01). Hover-title attribute is intentional UX. Out of public reach. |
| T-97-11 | Denial of Service | unbounded query without LIMIT | mitigate | LIMIT 50 enforced server-side; count(*) query is fast on a varchar table without joins. |
</threat_model>

<verification>
- pnpm build exit 0
- pnpm lint exit 0
- /admin/submissions?token=... renders 6 columns
- Specialization renders Russian labels for both English-key and Russian-stored rows
- Description truncated to 80 chars with ellipsis; full text on hover
- Pagination shows "X–Y из Z" range; navigation preserves token query param
- Loading skeleton renders during page change
</verification>

<success_criteria>
1. ADM-02 columns: Дата заявки, Имя, Телефон, Направление (Russian), Описание (truncated 80), Статус
2. Default sort: date_created DESC (verified via DB query log)
3. Pagination: 50 rows/page via LIMIT/OFFSET; ?page= URL param drives it
4. Loading state visible during SSR page transition (loading.tsx skeleton)
5. specialization mapping is idempotent — accepts BOTH English keys and Russian stored strings
6. submission-mappings.ts is the SINGLE source of truth for status + specialization labels
   (STATUS_LABELS removed from submissions-table.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/97-admin-submissions-view/97-02-SUMMARY.md`
documenting:
- Drizzle query shape used (limit/offset, count(*)::int via sql template)
- Two parallel queries (rows + count) — performance note: empirical query time on local DB
- Why mapping handles both English keys AND Russian strings (actions.ts WRITE-time mapping
  history) — critical context for future maintainers
- Decision: client-side filter UI from existing submissions-table.tsx is preserved AS-IS in
  this plan; plan 03 replaces it with URL-driven server filters
- loading.tsx skeleton design (utility opaque, no glass tokens per DESIGN.md)
- Performance numbers: query time at 50/page on local Postgres for N=55, N=247 (if synthetic
  data was generated to that scale)
</output>
