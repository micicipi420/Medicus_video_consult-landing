---
phase: 02-hero-problem-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
autonomous: true
requirements: [STRUC-02, UX-06]

must_haves:
  truths:
    - "Hero section displays headline 'Мнение немецкого врача — за 5 дней, без перелёта'"
    - "Subheadline explains the service in one sentence"
    - "Primary CTA reads 'Получить консультацию — от 450 EUR' and links to #form"
    - "Secondary CTA reads 'Узнать, подходит ли мой случай' and links to #form"
    - "Hero is text-left on desktop (1024px+), stacked full-width on mobile"
    - "Tone is calm and confident — no countdown timers, no urgency tricks"
  artifacts:
    - path: "index.html"
      provides: "Hero section HTML replacing demo content"
      contains: "hero"
    - path: "css/styles.css"
      provides: "Hero section styles"
      contains: ".hero"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes .hero, .hero__title, .hero__subtitle, .hero__actions"
      pattern: "hero__"
---

<objective>
Build the Hero section — the above-the-fold hook that communicates "European doctor second opinion from home" within 3 seconds. Replace the Phase 1 demo content in index.html with the real Hero section.

Purpose: First thing visitors see. Must immediately convey value proposition and present a clear call to action.
Output: Working Hero section in index.html with responsive layout and styled CTAs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-problem-sections/02-CONTEXT.md
@index.html
@css/styles.css

<interfaces>
<!-- Existing design system classes from css/styles.css -->

Layout:
```css
.section { padding-block: var(--section-padding-mobile); }
.section--dark { background-color: var(--color-dark); color: var(--color-text-on-dark); }
.container { max-width: var(--container-max); margin-inline: auto; padding-inline: var(--space-2); }
```

Buttons:
```css
.button { display: inline-flex; min-height: 48px; min-width: 48px; ... }
.button--primary { background-color: var(--color-primary); color: var(--color-text-on-primary); }
.button--secondary { background-color: var(--color-secondary); color: var(--color-text-on-secondary); }
```

Tokens:
```css
--font-size-h1: 2.25rem;  /* 36px */
--font-size-lg: 1.25rem;  /* 20px */
--space-2: 1rem; --space-3: 1.5rem; --space-4: 2rem; --space-6: 3rem;
--container-max: 1200px;
```

Breakpoints: min-width: 768px (tablet), min-width: 1024px (desktop)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace demo content with Hero section HTML and add Hero CSS</name>
  <read_first>index.html, css/styles.css</read_first>
  <files>index.html, css/styles.css</files>
  <action>
**index.html — Replace ALL content between `<body>` and `</body>` with:**

```html
  <main>
    <!-- Hero Section -->
    <section class="section hero" id="hero">
      <div class="container hero__container">
        <div class="hero__content">
          <h1 class="hero__title">Мнение немецкого врача&nbsp;&mdash; за&nbsp;5&nbsp;дней, без&nbsp;перелёта</h1>
          <p class="hero__subtitle">Видеоконсультация с&nbsp;европейским специалистом&nbsp;&mdash; на&nbsp;вашем языке, с&nbsp;переводом. Загрузите документы в&nbsp;приложение&nbsp;&mdash; мы&nbsp;переведём их, передадим врачу и&nbsp;организуем встречу.</p>
          <div class="hero__actions">
            <a href="#form" class="button button--primary">Получить консультацию&nbsp;&mdash; от&nbsp;450&nbsp;&euro;</a>
            <a href="#form" class="button button--outline">Узнать, подходит&nbsp;ли мой случай</a>
          </div>
        </div>
      </div>
    </section>
  </main>
```

Keep the existing `<head>` fully intact (font preloads, meta tags, stylesheet link). Remove the old `<header>`, `<main>`, and `<footer>` demo content entirely. Do NOT add `<header>` or `<footer>` elements — those come in later phases.

Use `&nbsp;` and `&mdash;` as shown above for proper Russian typographic spacing (prevents orphaned prepositions wrapping to new lines).

**css/styles.css — Add to Section 7 (after the comment "Section-specific styles added in phases 2-5"):**

```css
/* -- Hero -- */

.hero {
  padding-block: var(--space-8) var(--space-6);
  background-color: var(--color-light);
}

.hero__content {
  max-width: 720px;
}

.hero__title {
  margin-bottom: var(--space-3);
}

.hero__subtitle {
  font-size: var(--font-size-lg);
  color: var(--color-text-primary);
  margin-bottom: var(--space-5);
  max-width: 640px;
}

.hero__actions {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2);
}

/* Outline button variant for secondary CTA */
.button--outline {
  background-color: transparent;
  color: var(--color-text-primary);
  border: 2px solid var(--color-dark);
}

.button--outline:hover {
  background-color: var(--color-dark);
  color: var(--color-text-on-dark);
}

/* Hero responsive: desktop left-align is default (mobile-first, text is left-aligned).
   On desktop, add more generous top padding. */
@media (min-width: 1024px) {
  .hero {
    padding-block: var(--space-10) var(--space-8);
  }
}
```

Place these styles in the existing Section 7 area of styles.css. Do NOT modify any existing CSS — only add new rules.

Per UX-06: No animations, no countdown timers, no urgency language. The tone is calm, confident, medical.
  </action>
  <verify>
    <automated>grep -q 'hero__title' index.html && grep -q 'Мнение немецкого врача' index.html && grep -q 'button--outline' css/styles.css && grep -q '.hero__subtitle' css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- index.html contains `<section class="section hero" id="hero">`
- index.html contains `<h1 class="hero__title">Мнение немецкого врача`
- index.html contains `href="#form"` on both CTA buttons
- index.html contains `button--primary` on primary CTA
- index.html contains `button--outline` on secondary CTA
- index.html contains text "Получить консультацию" and "от 450"
- index.html contains text "Узнать, подходит ли мой случай"
- index.html does NOT contain "Дизайн-система MedicusUnion" (demo removed)
- css/styles.css contains `.hero` block styles
- css/styles.css contains `.hero__title`, `.hero__subtitle`, `.hero__actions`
- css/styles.css contains `.button--outline` with border and transparent background
  </acceptance_criteria>
  <done>Hero section visible at top of page with headline, subtitle, two CTA buttons. Demo content fully replaced. All text matches ТЗ exactly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Hero section visually</name>
  <what-built>Hero section with headline, subtitle, primary CTA ("Получить консультацию — от 450 EUR"), and secondary outline CTA ("Узнать, подходит ли мой случай"). Text-left layout, calm medical tone, no animations.</what-built>
  <how-to-verify>
    1. Open index.html in browser
    2. Verify headline reads: "Мнение немецкого врача — за 5 дней, без перелёта"
    3. Verify subtitle text is readable (20px, dark color)
    4. Verify primary CTA button is blue (#38C6F4) with dark text
    5. Verify secondary CTA button has dark border outline, transparent background
    6. Resize to 375px width — both buttons should stack, text full-width
    7. Resize to 1024px+ — text left-aligned, generous padding
    8. Confirm calm, professional feel — no aggressive marketing elements
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Hero section renders on page load with correct Russian text
- Both CTA buttons point to #form anchor
- Layout is responsive: stacked on mobile, left-aligned on desktop
- No demo content remains in index.html
- Tone is calm per UX-06 — no urgency markers
</verification>

<success_criteria>
- Headline "Мнение немецкого врача — за 5 дней, без перелёта" visible above the fold
- CTA button reads "Получить консультацию — от 450 EUR" and scrolls to #form
- Page renders correctly at 375px, 768px, and 1200px widths
- No JavaScript required — pure HTML+CSS
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-problem-sections/02-01-SUMMARY.md`
</output>
