---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
  - css/styles.css
autonomous: false
requirements:
  - UX-02
  - UX-03
  - UX-07
must_haves:
  truths:
    - "Body text renders at minimum 18px on all viewports"
    - "Headings render at 28-36px range on all viewports"
    - "All interactive elements have minimum 48x48px touch targets on mobile"
    - "Text-to-background contrast passes WCAG AA on all color combinations used"
    - "Fonts render as Inter (body) and Manrope (headings) visually on the page"
  artifacts:
    - path: "index.html"
      provides: "Demo content showing all design system elements for visual verification"
      contains: "section"
    - path: "css/styles.css"
      provides: "Responsive typography adjustments and any missing interactive element styles"
      contains: "min-width: 48px"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes applied to demo elements"
      pattern: "class=\"(section|container|button|card)"
---

<objective>
Add a design system demo/test page to index.html showing all typography, colors, buttons, cards, and layout components, then verify all UX requirements (font sizes, touch targets, WCAG contrast) are met across viewports.

Purpose: Proves the design system works visually before building real sections. Catches font loading issues, contrast failures, and sizing problems early.
Output: Updated index.html with demo content for visual verification; any CSS adjustments discovered during testing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 output -- executor needs these to build demo content -->

Expected CSS classes available:
- `.container` -- max-width 1200px centered wrapper
- `.section` -- vertical padding, block-level section
- `.section--dark` -- dark background variant
- `.button` -- base button with 48px min touch target
- `.button--primary` -- blue brand button
- `.button--secondary` -- green brand button
- `.card` -- white background card with shadow
- `.card__title` -- card heading
- `.card__text` -- card body text
- `.visually-hidden` -- screen reader text
- `.text-center` -- center alignment

Expected CSS custom properties:
- `--font-size-base: 1.125rem` (18px)
- `--font-size-h1: 2.25rem` (36px)
- `--font-size-h2: 2rem` (32px)
- `--font-size-h3: 1.75rem` (28px)
- `--color-primary: #38C6F4`
- `--color-secondary: #35B678`
- `--color-dark: #18212C`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add design system demo content to index.html and fix any CSS gaps</name>
  <files>index.html, css/styles.css</files>
  <read_first>
    index.html (current state from Plan 01)
    css/styles.css (current state from Plan 01 -- all available classes and tokens)
    .planning/phases/01-foundation-design-system/01-RESEARCH.md (WCAG contrast table, anti-patterns)
  </read_first>
  <action>
    Update `index.html` body to include demo content that exercises every design system element. This demo content will be replaced by real sections starting in Phase 2, but proves the foundation works.

    Add this inside `<body>`:

    ```html
    <!-- DESIGN SYSTEM DEMO -- will be replaced by real sections in Phase 2+ -->

    <header class="section section--dark">
      <div class="container">
        <h1>Дизайн-система MedicusUnion</h1>
        <p>Демо-страница для проверки шрифтов, цветов, отступов и адаптивности</p>
      </div>
    </header>

    <main>
      <!-- Typography Demo -->
      <section class="section">
        <div class="container">
          <h2>Типографика</h2>
          <h1>Заголовок H1 -- Manrope 36px (2.25rem)</h1>
          <h2>Заголовок H2 -- Manrope 32px (2rem)</h2>
          <h3>Заголовок H3 -- Manrope 28px (1.75rem)</h3>
          <p>Основной текст -- Inter 18px (1.125rem). Мнение европейского врача не выходя из дома. Онлайн-консультации с врачами из Германии, Израиля, Швейцарии и других стран.</p>
          <p><a href="#">Пример ссылки в тексте</a> -- должна быть контрастной на белом фоне.</p>
        </div>
      </section>

      <!-- Color Swatches -->
      <section class="section" style="background-color: var(--color-light);">
        <div class="container">
          <h2>Цветовая палитра</h2>
          <div style="display: flex; flex-wrap: wrap; gap: var(--space-2); margin-top: var(--space-3);">
            <div style="width: 120px; height: 80px; background: var(--color-primary); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; color: var(--color-text-on-primary); font-weight: 600;">#38C6F4</div>
            <div style="width: 120px; height: 80px; background: var(--color-secondary); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; color: var(--color-text-on-secondary); font-weight: 600;">#35B678</div>
            <div style="width: 120px; height: 80px; background: var(--color-dark); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; color: var(--color-text-on-dark); font-weight: 600;">#18212C</div>
            <div style="width: 120px; height: 80px; background: var(--color-light); border: 1px solid var(--color-dark); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; color: var(--color-text-primary); font-weight: 600;">#F8FAFB</div>
          </div>
        </div>
      </section>

      <!-- Buttons Demo -->
      <section class="section">
        <div class="container">
          <h2>Кнопки (touch target 48x48px min)</h2>
          <div style="display: flex; flex-wrap: wrap; gap: var(--space-2); margin-top: var(--space-3);">
            <a href="#" class="button button--primary">Получить консультацию</a>
            <a href="#" class="button button--secondary">Узнать подробнее</a>
          </div>
        </div>
      </section>

      <!-- Cards Demo -->
      <section class="section" style="background-color: var(--color-light);">
        <div class="container">
          <h2>Карточки</h2>
          <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: var(--space-3); margin-top: var(--space-3);">
            <div class="card">
              <h3 class="card__title">Второе мнение</h3>
              <p class="card__text">Независимая оценка диагноза и плана лечения от европейского специалиста.</p>
            </div>
            <div class="card">
              <h3 class="card__title">План действий</h3>
              <p class="card__text">Конкретные рекомендации по дальнейшим шагам лечения.</p>
            </div>
            <div class="card">
              <h3 class="card__title">Видеоконсультация</h3>
              <p class="card__text">Личная беседа с врачом через защищённый видеозвонок.</p>
            </div>
          </div>
        </div>
      </section>

      <!-- Dark Section Demo -->
      <section class="section section--dark">
        <div class="container">
          <h2>Тёмная секция</h2>
          <p>Белый текст на тёмном фоне (#18212C). Контраст 16.24:1 -- превышает WCAG AA.</p>
          <div style="margin-top: var(--space-3);">
            <a href="#" class="button button--primary">CTA на тёмном фоне</a>
          </div>
        </div>
      </section>

      <!-- Spacing Demo -->
      <section class="section">
        <div class="container">
          <h2>Сетка отступов (8px шаг)</h2>
          <div style="display: flex; flex-direction: column; gap: var(--space-1); margin-top: var(--space-3);">
            <div style="height: var(--space-1); background: var(--color-primary); border-radius: 2px; width: 8%;"></div>
            <div style="height: var(--space-1); background: var(--color-primary); border-radius: 2px; width: 16%;"></div>
            <div style="height: var(--space-1); background: var(--color-primary); border-radius: 2px; width: 24%;"></div>
            <div style="height: var(--space-1); background: var(--color-primary); border-radius: 2px; width: 32%;"></div>
            <div style="height: var(--space-1); background: var(--color-primary); border-radius: 2px; width: 40%;"></div>
          </div>
        </div>
      </section>
    </main>

    <footer class="section section--dark">
      <div class="container text-center">
        <p>Демо дизайн-системы MedicusUnion KZ. Будет заменено реальным контентом.</p>
      </div>
    </footer>
    ```

    After adding demo content, verify in css/styles.css:
    - The `.section--dark h1, .section--dark h2, .section--dark h3` rules exist to make headings white on dark backgrounds. If missing, add:
      ```css
      .section--dark h1,
      .section--dark h2,
      .section--dark h3 {
        color: var(--color-text-on-dark);
      }
      ```
    - Links (`a` tags) use `--color-primary-dark: #0E7490` for WCAG AA compliance on white (5.36:1 ratio). NOT `--color-primary` which fails at 1.99:1.
    - Card component has `overflow: hidden` for future image support.

    Fix any gaps found when reading the existing styles.css.
  </action>
  <verify>
    <automated>grep -c "section" index.html | xargs test 5 -le && grep -c "button--primary" index.html && grep -c "card__title" index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains `<h1>` through `<h3>` demo headings (grep: `<h1>`, `<h2>`, `<h3>`)
    - `index.html` contains `class="button button--primary"` (grep: exact string)
    - `index.html` contains `class="button button--secondary"` (grep: exact string)
    - `index.html` contains `class="card"` (grep: exact string)
    - `index.html` contains `class="card__title"` (grep: exact string)
    - `index.html` contains `class="section section--dark"` (grep: exact string)
    - `index.html` contains `class="container"` (grep: exact string)
    - `css/styles.css` contains rule for headings in dark sections (grep: `.section--dark`)
    - `css/styles.css` link color uses `--color-primary-dark` NOT `--color-primary` (grep for `color: var(--color-primary-dark)` in link rule)
  </acceptance_criteria>
  <done>index.html contains comprehensive demo content exercising all design system elements: typography (h1-h3, body, links), colors (all 4 brand swatches with correct text contrast), buttons (primary + secondary with 48px targets), cards (BEM structure), dark sections, spacing grid. CSS gaps fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of design system across viewports</name>
  <what-built>
    Complete CSS design system with self-hosted Inter and Manrope fonts, brand color tokens, responsive typography (18px+ body, 28-36px headings), 48px touch target buttons, WCAG AA contrast compliance, and demo page showing all components.
  </what-built>
  <how-to-verify>
    1. Open `index.html` in a browser (just open the file directly, no server needed)
    2. Check fonts: Body text should be in Inter, headings in Manrope (visually distinct -- Manrope is more geometric)
    3. Check font sizes:
       - Right-click body text > Inspect > Computed tab > font-size should show 18px
       - H1 should show 36px, H2 32px, H3 28px
    4. Check colors: Color swatches section should show blue (#38C6F4), green (#35B678), dark (#18212C), light (#F8FAFB)
    5. Check buttons: "Получить консультацию" (blue) and "Узнать подробнее" (green) should have dark text -- easily readable
    6. Check contrast: Text link should be dark teal (#0E7490), NOT bright blue
    7. Check dark sections: White text on dark background (#18212C) -- should be very readable
    8. Resize to mobile (375px): All sections should stack vertically, text stays readable, buttons stay tappable
    9. Resize to tablet (768px): Container gets more padding
    10. Resize to desktop (1200px+): Container maxes out at 1200px, centered
    11. DevTools Network tab: Font files should load from /assets/fonts/ (4 woff2 files), no external requests
  </how-to-verify>
  <resume-signal>Type "approved" if design system looks correct, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `index.html` opens in browser without errors
2. Inter font renders for body text (DevTools: computed font-family)
3. Manrope font renders for headings (DevTools: computed font-family)
4. Body text computed size >= 18px on all viewports (UX-02)
5. H1 = 36px, H2 = 32px, H3 = 28px (UX-02)
6. Buttons computed min-height >= 48px (UX-03)
7. No bright blue (#38C6F4) or green (#35B678) used as text color on white backgrounds (UX-07)
8. Dark text on colored button backgrounds is clearly readable (UX-07)
9. Network tab shows 4 WOFF2 files from local assets, no Google Fonts requests (UX-05)
10. Page renders correctly at 375px, 768px, 1024px, 1440px widths
</verification>

<success_criteria>
- All 5 phase requirements visually verified:
  - UX-02: Body 18px+, headings 28-36px (DevTools computed styles)
  - UX-03: Buttons 48px+ height (DevTools computed styles)
  - UX-04: Brand colors #38C6F4, #35B678, #18212C present in design tokens
  - UX-05: Inter + Manrope self-hosted WOFF2, no external font requests
  - UX-07: All text-on-background combos pass WCAG AA (no bright brand colors as text on white)
- Demo page displays all components correctly across 3 viewport sizes
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
