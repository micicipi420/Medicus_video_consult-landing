---
phase: 06-navigation-mobile-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
  - js/main.js
autonomous: true
requirements:
  - NAV-01
  - NAV-03

must_haves:
  truths:
    - "Header displays phone +7 701 532 24 78 as click-to-call tel: link"
    - "Every CTA button with href=#form triggers smooth scroll toward the form anchor"
    - "Phone number in header is visible on all viewports (mobile, tablet, desktop)"
  artifacts:
    - path: "index.html"
      provides: "Header element with phone link before <main>"
      contains: "site-header"
    - path: "css/styles.css"
      provides: "Header styling with responsive layout"
      contains: "site-header"
    - path: "js/main.js"
      provides: "Smooth scroll handler for CTA links"
      contains: "initSmoothScroll"
  key_links:
    - from: "js/main.js"
      to: "index.html"
      via: "querySelectorAll href=#form links"
      pattern: "querySelectorAll.*href.*form"
---

<objective>
Add a minimal site header with click-to-call phone number and implement smooth-scroll behavior for all CTA buttons targeting #form.

Purpose: NAV-03 requires the phone number in header/sticky/footer. NAV-01 requires CTA buttons to smooth-scroll to the form. This plan handles the header and scroll JS.
Output: Header element in index.html, header CSS, smooth scroll JS function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
@css/styles.css
@js/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add site header with click-to-call phone and header CSS</name>
  <read_first>index.html, css/styles.css</read_first>
  <files>index.html, css/styles.css</files>
  <action>
1. In index.html, add a header element BEFORE the opening `<main>` tag (line 17). Insert this exact HTML:

```html
<header class="site-header" id="header">
  <div class="container site-header__container">
    <span class="site-header__brand">MedicusUnion</span>
    <a href="tel:+77015322478" class="site-header__phone">+7&nbsp;701&nbsp;532&nbsp;24&nbsp;78</a>
  </div>
</header>
```

2. In css/styles.css, add header styles in section 7 (SECTIONS), BEFORE the Hero section comment. Add this block:

```css
/* -- Site Header -- */

.site-header {
  background-color: var(--color-white);
  border-bottom: 1px solid rgba(24, 33, 44, 0.08);
  padding-block: var(--space-2);
}

.site-header__container {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.site-header__brand {
  font-family: var(--font-heading);
  font-size: var(--font-size-lg);
  font-weight: 700;
  color: var(--color-text-primary);
}

.site-header__phone {
  font-family: var(--font-heading);
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-primary-dark);
  text-decoration: none;
  min-height: 48px;
  display: inline-flex;
  align-items: center;
}

.site-header__phone:hover {
  color: var(--color-dark);
  text-decoration: underline;
}
```

Per NAV-03: phone must be click-to-call with tel: link. Per UX-03: 48px min touch target.
  </action>
  <acceptance_criteria>
    - grep -q "site-header" index.html
    - grep -q 'href="tel:+77015322478"' index.html
    - grep -q "site-header__phone" css/styles.css
    - grep -q "site-header__brand" css/styles.css
  </acceptance_criteria>
  <verify>
    <automated>grep -c "site-header" index.html && grep -c 'tel:+77015322478' index.html && grep -c "site-header__phone" css/styles.css</automated>
  </verify>
  <done>Header visible at top of page with MedicusUnion brand text and clickable phone number +7 701 532 24 78. Phone link uses tel: protocol for mobile click-to-call.</done>
</task>

<task type="auto">
  <name>Task 2: Add smooth scroll JS for CTA buttons targeting #form</name>
  <read_first>js/main.js, index.html</read_first>
  <files>js/main.js</files>
  <action>
In js/main.js, add an initSmoothScroll function AFTER the initAccordion function definition (after line 48, before the initialization block). Add:

```javascript
/**
 * Smooth Scroll for CTA buttons
 * - All links with href="#form" scroll smoothly to form section
 * - Falls back to native behavior if target not found (form not yet built)
 * - Uses scrollIntoView for cross-browser smooth scroll
 */
function initSmoothScroll() {
  var links = document.querySelectorAll('a[href^="#"]');
  if (!links.length) return;

  links.forEach(function (link) {
    link.addEventListener('click', function (e) {
      var targetId = this.getAttribute('href');
      if (!targetId || targetId === '#') return;

      var target = document.querySelector(targetId);
      if (!target) return;

      e.preventDefault();
      target.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  });
}
```

Then in the initialization block (inside the `if/else` for DOMContentLoaded), add `initSmoothScroll` call right after `initAccordion`. The init block should become:

```javascript
function initAll() {
  initAccordion();
  initSmoothScroll();
}

// Initialize when DOM is ready
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', initAll);
} else {
  initAll();
}
```

Replace the existing initialization block (lines 51-55) with this new initAll pattern. Keep ES5-compatible syntax per Phase 05 decision. The forEach on NodeList works because we target modern browsers but use ES5 function syntax.
  </action>
  <acceptance_criteria>
    - grep -q "initSmoothScroll" js/main.js
    - grep -q "scrollIntoView" js/main.js
    - grep -q 'querySelectorAll.*href' js/main.js
    - grep -q "initAll" js/main.js
  </acceptance_criteria>
  <verify>
    <automated>grep -c "initSmoothScroll" js/main.js && grep -c "scrollIntoView" js/main.js && grep -c "initAll" js/main.js</automated>
  </verify>
  <done>All anchor links (including CTA buttons with href="#form") smooth-scroll to their target. When form section is added in Phase 7, CTAs will automatically scroll to it.</done>
</task>

</tasks>

<verification>
- Open index.html in browser: header visible with brand name and phone number
- Phone number is a clickable tel: link
- On mobile viewport (375px): header layout doesn't break, phone visible
- Click any CTA button: smooth scroll behavior fires (currently no #form target, but no error either)
</verification>

<success_criteria>
- Header element exists before main with tel: link to +7 701 532 24 78
- All anchor links have smooth scroll JS handler
- Header is responsive across all viewports
- ES5-compatible JS syntax maintained
</success_criteria>

<output>
After completion, create `.planning/phases/06-navigation-mobile-interaction/06-01-SUMMARY.md`
</output>
