---
phase: 05-pricing-faq-final-cta-footer
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [index.html, css/styles.css, js/main.js]
autonomous: true
requirements: [STRUC-10, NAV-04]

must_haves:
  truths:
    - "FAQ section displays 6 questions that expand/collapse on click"
    - "Only one FAQ answer is open at a time"
    - "FAQ works without JavaScript -- all answers visible as fallback"
    - "Question headers have 48px+ touch targets"
  artifacts:
    - path: "index.html"
      provides: "FAQ section HTML after pricing section"
      contains: "id=\"faq\""
    - path: "css/styles.css"
      provides: "FAQ and accordion styles"
      contains: ".faq"
    - path: "js/main.js"
      provides: "Accordion toggle logic"
      contains: "addEventListener"
  key_links:
    - from: "js/main.js"
      to: "index.html"
      via: "DOM query for accordion buttons"
      pattern: "querySelectorAll.*faq"
    - from: "index.html"
      to: "js/main.js"
      via: "script tag"
      pattern: "src=\"js/main.js\""
---

<objective>
Add the FAQ accordion section with 6 questions and vanilla JS toggle behavior.

Purpose: Handles remaining objections (technical, language, commitment) so visitors feel confident before reaching the final CTA. This is the first JavaScript on the page.
Output: FAQ section with 6 expandable/collapsible Q&A items, only one open at a time, accessible without JS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pricing-faq-final-cta-footer/05-CONTEXT.md
@index.html
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FAQ section HTML and accordion CSS</name>
  <files>index.html, css/styles.css</files>
  <read_first>index.html, css/styles.css</read_first>
  <action>
In index.html, insert the following section AFTER the pricing section (id="pricing") and BEFORE `</main>`:

```html
<!-- FAQ Section -->
<section class="section faq" id="faq">
  <div class="container">
    <h2 class="faq__heading">Частые вопросы</h2>
    <div class="faq__list">
      <div class="faq__item">
        <button class="faq__question" type="button" aria-expanded="false">
          <span class="faq__question-text">Как проходит консультация технически?</span>
          <span class="faq__icon" aria-hidden="true"></span>
        </button>
        <div class="faq__answer" hidden>
          <p>Видеозвонок через приложение MedicusUnion. Работает на&nbsp;телефоне, планшете и&nbsp;компьютере. Нужен только интернет.</p>
        </div>
      </div>
      <div class="faq__item">
        <button class="faq__question" type="button" aria-expanded="false">
          <span class="faq__question-text">Сколько длится консультация?</span>
          <span class="faq__icon" aria-hidden="true"></span>
        </button>
        <div class="faq__answer" hidden>
          <p>Обычно 30&ndash;60 минут. Врач не&nbsp;торопится&nbsp;&mdash; это не&nbsp;поликлиника с&nbsp;10-минутным приёмом.</p>
        </div>
      </div>
      <div class="faq__item">
        <button class="faq__question" type="button" aria-expanded="false">
          <span class="faq__question-text">А&nbsp;если я&nbsp;не&nbsp;говорю по-английски?</span>
          <span class="faq__icon" aria-hidden="true"></span>
        </button>
        <div class="faq__answer" hidden>
          <p>Не&nbsp;нужно. Мы&nbsp;обеспечиваем перевод во&nbsp;время консультации. Документы тоже переводим сами.</p>
        </div>
      </div>
      <div class="faq__item">
        <button class="faq__question" type="button" aria-expanded="false">
          <span class="faq__question-text">Можно показать заключение своему врачу?</span>
          <span class="faq__icon" aria-hidden="true"></span>
        </button>
        <div class="faq__answer" hidden>
          <p>Конечно. Вы&nbsp;получите письменное заключение&nbsp;&mdash; его можно скачать, распечатать, показать любому врачу.</p>
        </div>
      </div>
      <div class="faq__item">
        <button class="faq__question" type="button" aria-expanded="false">
          <span class="faq__question-text">Консультация обязывает к&nbsp;лечению?</span>
          <span class="faq__icon" aria-hidden="true"></span>
        </button>
        <div class="faq__answer" hidden>
          <p>Нет. Это ваше решение. Консультация даёт информацию для принятия решения, а&nbsp;не&nbsp;обязательство.</p>
        </div>
      </div>
      <div class="faq__item">
        <button class="faq__question" type="button" aria-expanded="false">
          <span class="faq__question-text">Можно выбрать конкретного врача?</span>
          <span class="faq__icon" aria-hidden="true"></span>
        </button>
        <div class="faq__answer" hidden>
          <p>Да. Вы&nbsp;можете выбрать врача на&nbsp;платформе сами, или мы&nbsp;подберём подходящего специалиста под ваш случай.</p>
        </div>
      </div>
    </div>
  </div>
</section>
```

Also add `<script src="js/main.js" defer></script>` BEFORE the closing `</body>` tag in index.html.

In css/styles.css, add the following AFTER the pricing styles and BEFORE the UTILITIES section comment:

```css
/* -- FAQ (Accordion) -- */

.faq {
  background-color: var(--color-light);
}

.faq__heading {
  margin-bottom: var(--space-5);
}

.faq__list {
  max-width: 720px;
  display: flex;
  flex-direction: column;
  gap: 0;
}

.faq__item {
  border-bottom: 1px solid rgba(24, 33, 44, 0.1);
}

.faq__item:first-child {
  border-top: 1px solid rgba(24, 33, 44, 0.1);
}

.faq__question {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  min-height: 48px;
  padding: var(--space-3) 0;
  background: none;
  border: none;
  cursor: pointer;
  text-align: left;
  font-family: var(--font-heading);
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--color-text-primary);
  gap: var(--space-2);
}

.faq__question:hover {
  color: var(--color-primary-dark);
}

.faq__question:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}

.faq__icon {
  flex-shrink: 0;
  width: 24px;
  height: 24px;
  position: relative;
  transition: transform var(--transition-normal);
}

.faq__icon::before,
.faq__icon::after {
  content: '';
  position: absolute;
  background-color: var(--color-text-primary);
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  transition: transform var(--transition-normal);
}

.faq__icon::before {
  width: 16px;
  height: 2px;
}

.faq__icon::after {
  width: 2px;
  height: 16px;
}

.faq__question[aria-expanded="true"] .faq__icon::after {
  transform: translate(-50%, -50%) rotate(90deg);
  opacity: 0;
}

.faq__answer {
  overflow: hidden;
  transition: max-height var(--transition-normal), padding var(--transition-normal);
}

.faq__answer[hidden] {
  display: none;
}

.faq__answer p {
  padding-bottom: var(--space-3);
  font-size: var(--font-size-base);
  line-height: var(--line-height-body);
  color: var(--color-text-primary);
  max-width: 640px;
}

/* No-JS fallback: show all answers when JS hasn't loaded */
.no-js .faq__answer[hidden] {
  display: block;
}
```

IMPORTANT: Add class="no-js" to the `<html>` tag as a no-JS fallback marker. The JS will remove it on load.
  </action>
  <verify>
    <automated>grep -c 'id="faq"' /Users/mikhail/Projects/Medicus_video_consult-landing/index.html && grep -c 'faq__question' /Users/mikhail/Projects/Medicus_video_consult-landing/index.html && grep -c 'js/main.js' /Users/mikhail/Projects/Medicus_video_consult-landing/index.html && grep -c '.faq__icon' /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css</automated>
  </verify>
  <acceptance_criteria>
- grep 'id="faq"' index.html returns match
- grep 'faq__question' index.html returns 6+ matches (6 buttons)
- grep 'Частые вопросы' index.html returns match
- grep 'js/main.js' index.html returns match
- grep '.faq__icon' css/styles.css returns match
- grep 'aria-expanded' index.html returns 6 matches
  </acceptance_criteria>
  <done>FAQ section with 6 questions exists in index.html. Each question is a button with aria-expanded. CSS provides plus/minus icon animation and answer reveal styling. Script tag links to js/main.js.</done>
</task>

<task type="auto">
  <name>Task 2: Create accordion JavaScript</name>
  <files>js/main.js</files>
  <read_first>index.html</read_first>
  <action>
Create the file js/main.js with the following content:

```javascript
/**
 * MedicusUnion KZ Landing - Main JS
 * Accordion and interactive behaviors
 */

(function () {
  'use strict';

  // Remove no-js class — JS is available
  document.documentElement.classList.remove('no-js');

  /**
   * FAQ Accordion
   * - Click toggles open/close
   * - Only one item open at a time
   * - Uses aria-expanded and hidden attribute
   */
  function initAccordion() {
    var buttons = document.querySelectorAll('.faq__question');
    if (!buttons.length) return;

    buttons.forEach(function (button) {
      button.addEventListener('click', function () {
        var isOpen = this.getAttribute('aria-expanded') === 'true';
        var answer = this.nextElementSibling;

        // Close all other items
        buttons.forEach(function (otherButton) {
          if (otherButton !== button) {
            otherButton.setAttribute('aria-expanded', 'false');
            var otherAnswer = otherButton.nextElementSibling;
            if (otherAnswer) {
              otherAnswer.hidden = true;
            }
          }
        });

        // Toggle current item
        if (isOpen) {
          this.setAttribute('aria-expanded', 'false');
          answer.hidden = true;
        } else {
          this.setAttribute('aria-expanded', 'true');
          answer.hidden = false;
        }
      });
    });
  }

  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initAccordion);
  } else {
    initAccordion();
  }
})();
```

IMPORTANT: Use ES5-compatible syntax (var, function, forEach) because the target audience is 45+ and may use older browsers. The `defer` attribute on the script tag already handles load timing, but the DOMContentLoaded check is a safety net.
  </action>
  <verify>
    <automated>test -f /Users/mikhail/Projects/Medicus_video_consult-landing/js/main.js && grep -c 'initAccordion' /Users/mikhail/Projects/Medicus_video_consult-landing/js/main.js && grep -c 'aria-expanded' /Users/mikhail/Projects/Medicus_video_consult-landing/js/main.js && grep -c 'addEventListener' /Users/mikhail/Projects/Medicus_video_consult-landing/js/main.js</automated>
  </verify>
  <acceptance_criteria>
- js/main.js file exists
- grep 'initAccordion' js/main.js returns match
- grep 'aria-expanded' js/main.js returns match
- grep 'querySelectorAll' js/main.js returns match
- grep 'no-js' js/main.js returns match
  </acceptance_criteria>
  <done>js/main.js exists with accordion logic. Clicking a question toggles its answer. Only one answer is open at a time. Uses aria-expanded for accessibility. Removes no-js class from html element on load.</done>
</task>

</tasks>

<verification>
- Open index.html in browser, scroll to FAQ section
- Verify 6 questions visible with plus icons
- Click first question -- answer expands, icon changes to minus
- Click second question -- first closes, second opens
- Click open question again -- it closes
- Resize to 375px -- questions and answers readable, touch targets adequate
- Disable JS in browser -- all answers should be visible (no-js fallback)
</verification>

<success_criteria>
FAQ section with 6 questions works as an accordion (one open at a time). Accessible via keyboard (button focus + Enter/Space). Graceful no-JS fallback. Touch targets 48px+.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pricing-faq-final-cta-footer/05-02-SUMMARY.md`
</output>
