---
phase: 10-visual-design-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-17
  - D-22
must_haves:
  truths:
    - "All emoji icons (except country flags) are replaced with inline SVG"
    - "SVG icons have duotone style: brand blue stroke with light fill"
    - "Country flags remain as emoji"
    - "Cards have left-border accent and subtle scale on hover"
    - "Header has a thin gradient line beneath it"
  artifacts:
    - path: "index.html"
      provides: "Inline SVG icons replacing emoji in benefits, advantages, scenarios, pricing, form-success"
      contains: "<svg"
    - path: "css/styles.css"
      provides: "Icon size tokens, card hover effects, header gradient line"
      contains: "--icon-size-sm"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "SVG icon class sizing and card hover"
      pattern: "icon-size"
---

<objective>
Replace all emoji icons with inline SVG in duotone style, add icon CSS size tokens, enhance card hover states, and add header gradient line.

Purpose: Emoji render inconsistently across platforms (Android/iOS/Windows). Inline SVG icons give precise control over style, color, and size. Card hover and header line add polish.

Output: Consistent duotone SVG icons throughout, enhanced card interactions, branded header accent.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-design-enhancement/10-CONTEXT.md
@index.html
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add icon size tokens, card hover effects, and header gradient line CSS</name>
  <files>css/styles.css</files>
  <read_first>css/styles.css, .planning/phases/10-visual-design-enhancement/10-CONTEXT.md</read_first>
  <action>
In css/styles.css, make these additions:

1. **Icon size custom properties (per D-08):** Add to `:root` block (after the existing border-radius tokens, around line 111):
   ```css
   /* Icon sizes */
   --icon-size-sm: 32px;
   --icon-size-md: 48px;
   --icon-size-lg: 64px;
   ```

2. **SVG icon base styles:** Add after the `.card__text` rule (around line 304):
   ```css
   .icon {
     display: inline-flex;
     align-items: center;
     justify-content: center;
     flex-shrink: 0;
   }
   .icon svg {
     width: var(--icon-size-md);
     height: var(--icon-size-md);
   }
   .icon--sm svg {
     width: var(--icon-size-sm);
     height: var(--icon-size-sm);
   }
   .icon--lg svg {
     width: var(--icon-size-lg);
     height: var(--icon-size-lg);
   }
   ```

3. **Card hover effects (per D-17):** Add after the existing `.card` rule (around line 294):
   ```css
   .card {
     transition: transform var(--transition-normal), box-shadow var(--transition-normal), border-left-color var(--transition-normal);
     border-left: 3px solid transparent;
   }
   .card:hover {
     transform: scale(1.02);
     box-shadow: var(--shadow-lg);
     border-left-color: var(--color-primary);
   }
   ```
   NOTE: Merge the `border-left` and `transition` into the existing `.card` rule rather than creating a duplicate. The existing `.card` has `background-color`, `border-radius`, `box-shadow`, `padding`, `overflow`. Add the new properties to that same rule block.

4. **Header gradient line (per D-22):** Add after the `.site-header` rules (around line 345):
   ```css
   .site-header::after {
     content: '';
     display: block;
     height: 3px;
     background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
   }
   ```
   Also remove the existing `border-bottom: 1px solid rgba(24, 33, 44, 0.08)` from `.site-header` since the gradient line replaces it.

5. **Update icon containers:** Replace the font-size-based sizing in `.benefits__icon`, `.advantages__icon`, `.scenarios__check`, `.pricing__check` with the new `.icon` class approach. The existing rules that set `font-size: 2.5rem` on icon containers should be updated:
   ```css
   .benefits__icon,
   .advantages__icon {
     line-height: 1;
   }
   ```
   Remove `font-size: 2.5rem` from `.benefits__icon` and `.advantages__icon` since SVG sizing is now handled by `.icon` class.

6. **Scenarios check icon sizing:**
   ```css
   .scenarios__check {
     line-height: 1;
     flex-shrink: 0;
     margin-top: 0.1em;
   }
   .scenarios__check svg {
     width: var(--icon-size-sm);
     height: var(--icon-size-sm);
   }
   ```
   Remove `font-size: 1.5rem` from the existing `.scenarios__check` rule.

7. **Pricing check icon:**
   ```css
   .pricing__check svg {
     width: 20px;
     height: 20px;
   }
   ```
   Remove `font-size: 1.25rem` and `font-weight: 700` from existing `.pricing__check` since SVG replaces the text checkmark.
  </action>
  <verify>
    <automated>grep -q "\-\-icon-size-sm" css/styles.css && grep -q "\-\-icon-size-md" css/styles.css && grep -q "scale(1.02)" css/styles.css && grep -q "site-header::after" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - css/styles.css `:root` contains `--icon-size-sm: 32px`, `--icon-size-md: 48px`, `--icon-size-lg: 64px`
    - css/styles.css contains `.icon svg` with `width: var(--icon-size-md)`
    - css/styles.css `.card` rule includes `transition` with `transform`, `box-shadow`
    - css/styles.css `.card:hover` contains `transform: scale(1.02)`, `box-shadow: var(--shadow-lg)`, `border-left-color: var(--color-primary)`
    - css/styles.css `.site-header::after` contains `linear-gradient(90deg, var(--color-primary), var(--color-secondary))` and `height: 3px`
    - css/styles.css `.site-header` no longer has `border-bottom: 1px solid`
  </acceptance_criteria>
  <done>Icon tokens, card hover effects, and header gradient line are in CSS</done>
</task>

<task type="auto">
  <name>Task 2: Replace emoji with inline SVG icons in HTML</name>
  <files>index.html</files>
  <read_first>index.html, css/styles.css</read_first>
  <action>
In index.html, replace all emoji HTML entities with inline SVG icons in duotone style (per D-05, D-06, D-09). Each SVG uses `stroke="#38C6F4"` (brand blue) with `fill="rgba(56,198,244,0.1)"` (10% opacity fill). Per D-07, country flags in doctors section STAY as emoji.

**Benefits section icons** (lines 78-95 area) — replace the `&#xNNNN;` content inside each `.benefits__icon` div, and add class `icon` to each container:

1. Magnifier icon (second opinion) — replace `&#x1F50D;`:
   ```html
   <div class="benefits__icon icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
       <circle cx="20" cy="20" r="14" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
       <line x1="30" y1="30" x2="40" y2="40" stroke="#38C6F4" stroke-width="2.5" stroke-linecap="round"/>
     </svg>
   </div>
   ```

2. Clipboard icon (action plan) — replace `&#x1F4CB;`:
   ```html
   <div class="benefits__icon icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
       <rect x="10" y="8" width="28" height="34" rx="3" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
       <rect x="17" y="4" width="14" height="8" rx="2" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
       <line x1="16" y1="22" x2="32" y2="22" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
       <line x1="16" y1="28" x2="28" y2="28" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
       <line x1="16" y1="34" x2="24" y2="34" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
     </svg>
   </div>
   ```

3. Document icon (written conclusion) — replace `&#x1F4C4;`:
   ```html
   <div class="benefits__icon icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
       <path d="M12 6h18l10 10v26a2 2 0 01-2 2H12a2 2 0 01-2-2V8a2 2 0 012-2z" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
       <path d="M30 6v10h10" stroke="#38C6F4" stroke-width="2.5" fill="none"/>
       <line x1="16" y1="24" x2="32" y2="24" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
       <line x1="16" y1="30" x2="28" y2="30" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
     </svg>
   </div>
   ```

4. Question mark icon (answers) — replace `&#x2753;`:
   ```html
   <div class="benefits__icon icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
       <circle cx="24" cy="24" r="18" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
       <path d="M18 18a6 6 0 0112 0c0 4-6 4-6 8" stroke="#38C6F4" stroke-width="2.5" fill="none" stroke-linecap="round"/>
       <circle cx="24" cy="34" r="1.5" fill="#38C6F4"/>
     </svg>
   </div>
   ```

**Advantages section icons** (lines 183-201 area) — same pattern, add `icon` class:

5. Document/translation icon — replace `&#x1F4C4;`:
   Same as benefits document icon (#3 above).

6. Speech bubble icon (live translation) — replace `&#x1F5E3;`:
   ```html
   <div class="advantages__icon icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
       <path d="M8 12a4 4 0 014-4h24a4 4 0 014 4v16a4 4 0 01-4 4H20l-8 6v-6H12a4 4 0 01-4-4V12z" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
       <line x1="16" y1="18" x2="32" y2="18" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
       <line x1="16" y1="24" x2="26" y2="24" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
     </svg>
   </div>
   ```

7. Mobile/app icon — replace `&#x1F4F1;`:
   ```html
   <div class="advantages__icon icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
       <rect x="12" y="4" width="24" height="40" rx="4" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
       <line x1="12" y1="12" x2="36" y2="12" stroke="#38C6F4" stroke-width="2"/>
       <line x1="12" y1="36" x2="36" y2="36" stroke="#38C6F4" stroke-width="2"/>
       <circle cx="24" cy="40" r="2" fill="#38C6F4"/>
     </svg>
   </div>
   ```

8. Plane/travel icon — replace `&#x2708;`:
   ```html
   <div class="advantages__icon icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
       <path d="M24 4L8 20h10v16h12V20h10L24 4z" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)" stroke-linejoin="round"/>
       <rect x="20" y="36" width="8" height="8" rx="1" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
     </svg>
   </div>
   ```
   NOTE: This is an "organization/building" concept (arrow pointing up + base), representing "we organize everything". The original plane emoji meant travel organization.

**Scenarios section** (lines 213-232 area) — replace all 5 `&#x2705;` checkmark emoji in `.scenarios__check` spans:
   ```html
   <span class="scenarios__check" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="none">
       <circle cx="16" cy="16" r="12" stroke="#38C6F4" stroke-width="2" fill="rgba(56,198,244,0.1)"/>
       <path d="M10 16l4 4 8-8" stroke="#35B678" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
     </svg>
   </span>
   ```
   Apply this to all 5 scenario items.

**Pricing section** (lines 249-267 area) — replace all 5 `&#x2713;` checkmarks in `.pricing__check` spans:
   ```html
   <span class="pricing__check" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="none">
       <path d="M4 10l4 4 8-8" stroke="#047857" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
     </svg>
   </span>
   ```
   Apply to all 5 pricing items. Uses `--color-secondary-dark` (#047857) for the green check.

**Form success** (line 327) — replace `&#x2705;` in `.lead-form__success-icon`:
   ```html
   <span class="lead-form__success-icon" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" fill="none" width="64" height="64">
       <circle cx="32" cy="32" r="26" stroke="#35B678" stroke-width="3" fill="rgba(53,182,120,0.1)"/>
       <path d="M20 32l8 8 16-16" stroke="#35B678" stroke-width="3.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
     </svg>
   </span>
   ```

**DO NOT touch** the country flag emoji in the `.doctors__flag` divs (per D-07).
  </action>
  <verify>
    <automated>grep -c "<svg" index.html | xargs -I{} test {} -ge 15 && grep -c "doctors__flag.*&#x" index.html | xargs -I{} test {} -ge 7 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains at least 15 inline `<svg` elements (4 benefits + 4 advantages + 5 scenarios + 5 pricing + 1 form-success = 19 minimum, plus hero illustration from Plan 01)
    - All 7 `.doctors__flag` divs still contain emoji HTML entities (&#x1F1E9; etc.), NOT SVG
    - No remaining emoji HTML entities (&#x1F50D;, &#x1F4CB;, &#x1F4C4;, &#x2753;, &#x1F5E3;, &#x1F4F1;, &#x2708;, &#x2705;, &#x2713;) outside of `.doctors__flag` divs
    - Each benefits/advantages icon div has class `icon` alongside its existing class
    - SVG icons use `stroke="#38C6F4"` for primary elements and `fill="rgba(56,198,244,0.1)"` for duotone fill
    - Scenario check SVGs use `stroke="#35B678"` (green) for the checkmark path
    - Pricing check SVGs use `stroke="#047857"` for green checkmark
  </acceptance_criteria>
  <done>All emoji icons replaced with consistent duotone inline SVG, country flags preserved as emoji, cards have hover states, header has gradient accent line</done>
</task>

</tasks>

<verification>
1. Open index.html in Chrome, Firefox, and Safari — SVG icons render identically across all browsers
2. Country flags in doctors section still show correctly as emoji
3. Hover over any `.card` element — left border turns blue, card scales slightly (1.02), shadow deepens
4. Header shows thin blue-to-green gradient line below the border
5. Icon sizes are consistent (48px default for benefits/advantages, 32px for scenario checks, 20px for pricing checks)
</verification>

<success_criteria>
- Zero emoji icons remain (except 7 country flags)
- All SVG icons are duotone: blue stroke + 10% blue fill
- Cards have hover transition (border-left, scale, shadow)
- Header displays gradient accent line (blue to green, 3px)
- Icon sizes controlled via CSS custom properties
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-design-enhancement/10-02-SUMMARY.md`
</output>
