---
phase: 07-lead-capture-form
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - js/main.js
autonomous: true
requirements:
  - FORM-04
  - FORM-05
must_haves:
  truths:
    - "Required fields show Russian error messages when submitted empty"
    - "Phone validates Kazakhstan format (11 digits starting with 7)"
    - "Invalid fields get red border, error text appears below"
    - "Successful submission hides form and shows thank-you message"
  artifacts:
    - path: "js/main.js"
      provides: "Form validation and submission logic"
      contains: "initFormValidation"
  key_links:
    - from: "js/main.js"
      to: "index.html"
      via: "getElementById lead-form, form-success"
      pattern: "getElementById.*lead-form"
---

<objective>
Add client-side validation with Russian error messages and form success state logic.

Purpose: Users get immediate feedback on invalid fields; successful submission shows confirmation.
Output: Validation logic for all required fields, error display, success state toggle.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-lead-capture-form/07-CONTEXT.md
@.planning/phases/07-lead-capture-form/07-01-SUMMARY.md
@index.html
@js/main.js
</context>

<interfaces>
<!-- From 07-01 plan: HTML IDs the executor needs to know -->
Form element: id="lead-form"
Name input: id="name", id="name-error"
Phone input: id="phone", id="phone-error"
Specialty select: id="specialty", id="specialty-error"
Description textarea: id="description" (optional, no validation)
Submit button: class="lead-form__submit"
Success container: id="form-success"
Error class on inputs: .is-invalid
Error span visibility: hidden attribute toggle
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Form validation and submission handler</name>
  <files>js/main.js</files>
  <read_first>js/main.js (full file -- understand IIFE structure, initAll, initPhoneMask from plan 01)</read_first>
  <action>
Add an `initFormValidation` function to js/main.js inside the IIFE, before `initAll`. Register it in `initAll()`. Use ES5-compatible syntax (var, function, no arrow functions).

Add this function before `initAll`:

```javascript
/**
 * Form Validation and Submission
 * - Validates required fields on submit
 * - Shows Russian error messages (FORM-04)
 * - On success: hides form, shows success message (FORM-05)
 * - Submission target URL configured as data attribute or constant (wired in Phase 8)
 */
function initFormValidation() {
  var form = document.getElementById('lead-form');
  if (!form) return;

  var successEl = document.getElementById('form-success');

  // Validation rules
  var rules = {
    name: {
      required: true,
      message: 'Укажите ваше имя',
      validate: function (value) {
        return value.trim().length >= 2;
      }
    },
    phone: {
      required: true,
      message: 'Укажите номер телефона',
      validate: function (value) {
        var digits = value.replace(/\D/g, '');
        return digits.length === 11 && digits.charAt(0) === '7';
      }
    },
    specialty: {
      required: true,
      message: 'Выберите специализацию',
      validate: function (value) {
        return value !== '';
      }
    }
  };

  function showError(fieldId, message) {
    var input = document.getElementById(fieldId);
    var errorEl = document.getElementById(fieldId + '-error');
    if (input) {
      input.classList.add('is-invalid');
    }
    if (errorEl) {
      errorEl.textContent = message;
      errorEl.hidden = false;
    }
  }

  function clearError(fieldId) {
    var input = document.getElementById(fieldId);
    var errorEl = document.getElementById(fieldId + '-error');
    if (input) {
      input.classList.remove('is-invalid');
    }
    if (errorEl) {
      errorEl.textContent = '';
      errorEl.hidden = true;
    }
  }

  function clearAllErrors() {
    var fields = ['name', 'phone', 'specialty'];
    fields.forEach(function (fieldId) {
      clearError(fieldId);
    });
  }

  function validateForm() {
    var isValid = true;
    clearAllErrors();

    Object.keys(rules).forEach(function (fieldId) {
      var rule = rules[fieldId];
      var input = document.getElementById(fieldId);
      if (!input) return;

      var value = input.value;
      if (!rule.validate(value)) {
        showError(fieldId, rule.message);
        isValid = false;
      }
    });

    return isValid;
  }

  // Clear error on input change
  ['name', 'phone', 'specialty'].forEach(function (fieldId) {
    var input = document.getElementById(fieldId);
    if (!input) return;
    var eventType = fieldId === 'specialty' ? 'change' : 'input';
    input.addEventListener(eventType, function () {
      clearError(fieldId);
    });
  });

  form.addEventListener('submit', function (e) {
    e.preventDefault();

    if (!validateForm()) {
      // Focus first invalid field
      var firstInvalid = form.querySelector('.is-invalid');
      if (firstInvalid) {
        firstInvalid.focus();
      }
      return;
    }

    // Collect form data
    var formData = {
      name: document.getElementById('name').value.trim(),
      phone: document.getElementById('phone').value,
      specialty: document.getElementById('specialty').value,
      description: document.getElementById('description').value.trim()
    };

    // TODO Phase 8: Submit to Directus API
    // For now, simulate successful submission
    console.log('Form data:', formData);

    // Show success state (FORM-05)
    form.hidden = true;
    if (successEl) {
      successEl.hidden = false;
    }
    // Hide subtext and privacy note
    var subtext = form.parentElement.querySelector('.lead-form__subtext');
    var privacy = form.parentElement.querySelector('.lead-form__privacy');
    if (subtext) subtext.hidden = true;
    if (privacy) privacy.hidden = true;
  });
}
```

Then update `initAll` to include the new function:
```javascript
function initAll() {
  initAccordion();
  initSmoothScroll();
  initStickyBar();
  initPhoneMask();
  initFormValidation();
}
```
  </action>
  <acceptance_criteria>
    - grep "initFormValidation" js/main.js returns at least 2 matches (definition + initAll call)
    - grep "Укажите ваше имя" js/main.js returns match
    - grep "Укажите номер телефона" js/main.js returns match
    - grep "Выберите специализацию" js/main.js returns match
    - grep "is-invalid" js/main.js returns match
    - grep "form-success" js/main.js returns match
    - grep "form.hidden" js/main.js returns match
    - No arrow functions: grep "=>" js/main.js returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c "initFormValidation" js/main.js && grep -c "Укажите" js/main.js && grep -c "=>" js/main.js | grep "^0$"</automated>
  </verify>
  <done>Form validates name (min 2 chars), phone (11 digits starting with 7), specialty (non-empty) on submit. Russian error messages appear below invalid fields with red border. On valid submit, form hides and success message with green checkmark appears. Errors clear on field input/change. First invalid field receives focus.</done>
</task>

</tasks>

<verification>
- Open index.html, click "Отправить заявку" without filling fields: 3 error messages in Russian appear
- Fill name with 1 char, submit: name error shows
- Fill phone with incomplete number, submit: phone error shows
- Leave specialty as placeholder, submit: specialty error shows
- Fill all required fields correctly, submit: form hides, success message visible
- Type into a field that shows an error: error clears immediately
</verification>

<success_criteria>
All 3 required fields validate on submit with Russian error messages. Invalid fields get red border. Errors clear on user input. Valid submission hides form and shows "Спасибо! Мы свяжемся с вами в течение 24 часов." confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-lead-capture-form/07-02-SUMMARY.md`
</output>
