---
phase: 03-value-process-sections
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [index.html, css/styles.css]
autonomous: true
requirements: [STRUC-05]

must_haves:
  truths:
    - "Visitor sees heading 'Три шага до мнения европейского врача'"
    - "Visitor sees 3 numbered steps with titles and descriptions"
    - "Steps display horizontally on desktop and stack vertically on mobile"
  artifacts:
    - path: "index.html"
      provides: "Process section HTML with 3 steps"
      contains: "id=\"process\""
    - path: "css/styles.css"
      provides: "Process section styles with responsive layout"
      contains: ".process"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes .process, .process__step"
      pattern: "process__step"
---

<objective>
Add the "Три шага до мнения европейского врача" section with 3 numbered process steps after the Benefits section.

Purpose: Show the visitor how simple the consultation process is -- upload docs, doctor reviews, video call. Reduce anxiety about complexity.
Output: Process section in index.html with numbered steps styled in styles.css.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-value-process-sections/03-CONTEXT.md
@.planning/phases/03-value-process-sections/03-01-SUMMARY.md

<interfaces>
<!-- Existing CSS classes (from css/styles.css) -->

.section          — padding-block: var(--section-padding-mobile)
.container        — max-width: 1200px, margin-inline: auto

<!-- Design tokens -->
--color-white: #FFFFFF
--color-primary: #38C6F4
--color-primary-dark: #0E7490
--color-dark: #18212C
--color-text-primary: #18212C
--space-2: 1rem (16px)
--space-3: 1.5rem (24px)
--space-4: 2rem (32px)
--space-5: 2.5rem (40px)
--font-size-h2: 2rem (32px)
--font-size-lg: 1.25rem (20px)
--font-heading: 'Manrope Variable', ...
--radius-full: 9999px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add process steps section HTML and CSS</name>
  <read_first>index.html, css/styles.css</read_first>
  <files>index.html, css/styles.css</files>
  <action>
**In index.html**, insert the following section AFTER the closing `</section>` of the Benefits section (id="benefits") and BEFORE the closing `</main>` tag:

```html
<!-- Process Section: How It Works -->
<section class="section process" id="process">
  <div class="container">
    <h2 class="process__heading">Три шага до&nbsp;мнения европейского врача</h2>
    <div class="process__steps">
      <div class="process__step">
        <div class="process__number" aria-hidden="true">01</div>
        <h3 class="process__title">Загрузите документы</h3>
        <p class="process__text">Снимки, анализы, заключения&nbsp;&mdash; в&nbsp;любом формате, на&nbsp;любом языке. Мы&nbsp;переведём всё сами и&nbsp;подготовим для врача.</p>
      </div>
      <div class="process__step">
        <div class="process__number" aria-hidden="true">02</div>
        <h3 class="process__title">Врач изучает ваш случай</h3>
        <p class="process__text">Специалист готовится к&nbsp;встрече: изучает документы, снимки, историю болезни. На&nbsp;консультации он&nbsp;уже в&nbsp;курсе вашего случая.</p>
      </div>
      <div class="process__step">
        <div class="process__number" aria-hidden="true">03</div>
        <h3 class="process__title">Видеоконсультация</h3>
        <p class="process__text">Встреча по&nbsp;видео с&nbsp;переводчиком. Врач даёт оценку, отвечает на&nbsp;вопросы. После&nbsp;&mdash; письменное заключение в&nbsp;личном кабинете.</p>
      </div>
    </div>
  </div>
</section>
```

**In css/styles.css**, add after the Benefits CSS block (after the `.benefits` media query), still within section 7:

```css
/* -- Process (How It Works) -- */

.process {
  background-color: var(--color-white);
}

.process__heading {
  margin-bottom: var(--space-5);
}

.process__steps {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-4);
}

.process__step {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}

.process__number {
  font-family: var(--font-heading);
  font-size: 3rem;
  font-weight: 800;
  line-height: 1;
  color: var(--color-primary);
  margin-bottom: var(--space-1);
}

.process__title {
  font-size: var(--font-size-lg);
}

.process__text {
  color: var(--color-text-primary);
  max-width: 360px;
}

@media (min-width: 768px) {
  .process__steps {
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-5);
  }
}
```

This gives: single column stacked steps on mobile, 3-column horizontal layout on tablet/desktop. Step numbers are large bold numerals in brand blue for visual prominence.
  </action>
  <verify>
    <automated>grep -c "process__step" index.html | grep -q "3" && grep -q "id=\"process\"" index.html && grep -q ".process__steps" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- grep "id=\"process\"" index.html returns a match
- grep -c "process__step" index.html returns 3 (the 3 step divs)
- grep "Три шага" index.html returns a match
- grep "Загрузите документы" index.html returns a match
- grep "Врач изучает ваш случай" index.html returns a match
- grep "Видеоконсультация" index.html returns a match (process step title)
- grep ".process__steps" css/styles.css returns a match
- grep "grid-template-columns: repeat(3, 1fr)" css/styles.css returns a match
- grep ".process__number" css/styles.css returns a match
  </acceptance_criteria>
  <done>Process section visible in index.html with 3 numbered steps containing exact Russian text from ТЗ, styled with responsive 3-column grid on desktop and vertical stack on mobile</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify process section visually</name>
  <what-built>Process steps section showing 3 steps to consultation</what-built>
  <how-to-verify>
    1. Open index.html in a browser
    2. Scroll past the Benefits section
    3. Verify heading "Три шага до мнения европейского врача" is visible
    4. Verify 3 steps are displayed with large blue numbers (01, 02, 03), titles, and descriptions
    5. Resize to mobile width (375px) -- steps should stack vertically
    6. Resize to desktop width (1024px+) -- steps should display in 3 columns
    7. Verify step numbers are visually prominent (large, colored)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- index.html contains section#process with 3 .process__step elements
- css/styles.css contains .process, .process__steps, .process__number rules
- Layout is responsive: 1 column mobile, 3 columns tablet+
- Step numbers 01, 02, 03 are large and styled in brand blue
</verification>

<success_criteria>
- 3 process steps visible with exact Russian copy from ТЗ
- Step numbers (01, 02, 03) visually prominent in brand blue
- Responsive layout: vertical stack on mobile, 3 horizontal columns on 768px+
</success_criteria>

<output>
After completion, create `.planning/phases/03-value-process-sections/03-02-SUMMARY.md`
</output>
