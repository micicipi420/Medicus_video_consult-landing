---
phase: 03-value-process-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, css/styles.css]
autonomous: true
requirements: [STRUC-04]

must_haves:
  truths:
    - "Visitor sees heading 'Что вы получите за одну консультацию'"
    - "Visitor sees 4 benefit cards with titles and descriptions"
    - "Cards display in 2x2 grid on desktop and single column on mobile"
  artifacts:
    - path: "index.html"
      provides: "Benefits section HTML with 4 cards"
      contains: "id=\"benefits\""
    - path: "css/styles.css"
      provides: "Benefits section styles with responsive grid"
      contains: ".benefits"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes .benefits, .benefits__card"
      pattern: "benefits__card"
---

<objective>
Add the "Что вы получите за одну консультацию" section with 4 benefit cards after the Problem section.

Purpose: Show the visitor concrete value they receive from one consultation -- second opinion, action plan, written conclusion, Q&A answers.
Output: Benefits section in index.html with responsive card grid styled in styles.css.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-value-process-sections/03-CONTEXT.md

<interfaces>
<!-- Existing CSS classes to reuse (from css/styles.css) -->

.section          — padding-block: var(--section-padding-mobile)
.section--dark     — dark bg variant (NOT used here -- benefits on light bg)
.container        — max-width: 1200px, margin-inline: auto, padding-inline: var(--space-2)
.card             — bg white, border-radius: var(--radius-lg), box-shadow: var(--shadow-md), padding: var(--space-4)
.card__title      — font-heading, font-size-lg (20px), weight 600
.card__text       — color: var(--color-text-primary)

<!-- Existing design tokens -->
--color-light: #F8FAFB
--space-3: 1.5rem (24px)
--space-4: 2rem (32px)
--space-5: 2.5rem (40px)
--font-size-h2: 2rem (32px)
--shadow-md: 0 4px 12px rgba(24,33,44,0.08)
--radius-lg: 0.75rem (12px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benefits section HTML and CSS</name>
  <read_first>index.html, css/styles.css</read_first>
  <files>index.html, css/styles.css</files>
  <action>
**In index.html**, insert the following section AFTER the closing `</section>` of the Problem section (id="problem") and BEFORE the closing `</main>` tag:

```html
<!-- Benefits Section: What You Get -->
<section class="section benefits" id="benefits">
  <div class="container">
    <h2 class="benefits__heading">Что вы получите за&nbsp;одну консультацию</h2>
    <div class="benefits__grid">
      <div class="card benefits__card">
        <div class="benefits__icon" aria-hidden="true">&#x1F50D;</div>
        <h3 class="card__title">Второе мнение по&nbsp;вашему диагнозу</h3>
        <p class="card__text">Врач изучит ваши документы до&nbsp;встречи и&nbsp;даст оценку вашего случая. Не&nbsp;общие слова, а&nbsp;конкретное заключение по&nbsp;вашим анализам и&nbsp;снимкам.</p>
      </div>
      <div class="card benefits__card">
        <div class="benefits__icon" aria-hidden="true">&#x1F4CB;</div>
        <h3 class="card__title">Понятный план действий</h3>
        <p class="card__text">Что делать дальше: какие обследования пройти, какое лечение рекомендуется, какие есть варианты. Вы&nbsp;уходите с&nbsp;консультации с&nbsp;ясностью, а&nbsp;не&nbsp;с&nbsp;новыми вопросами.</p>
      </div>
      <div class="card benefits__card">
        <div class="benefits__icon" aria-hidden="true">&#x1F4C4;</div>
        <h3 class="card__title">Письменное заключение</h3>
        <p class="card__text">После консультации вы&nbsp;получите документ с&nbsp;рекомендациями врача. Его можно показать своему лечащему врачу или использовать для принятия решения.</p>
      </div>
      <div class="card benefits__card">
        <div class="benefits__icon" aria-hidden="true">&#x2753;</div>
        <h3 class="card__title">Ответы на&nbsp;ваши вопросы</h3>
        <p class="card__text">Консультация&nbsp;&mdash; это не&nbsp;монолог врача. Вы&nbsp;задаёте вопросы, врач отвечает. Переводчик обеспечивает полное понимание.</p>
      </div>
    </div>
  </div>
</section>
```

Icons used (Unicode emoji, no external deps):
- Card 1: magnifying glass (U+1F50D) for "second opinion / review"
- Card 2: clipboard (U+1F4CB) for "action plan"
- Card 3: page (U+1F4C4) for "written conclusion"
- Card 4: question mark (U+2753) for "Q&A answers"

**In css/styles.css**, add after the `/* -- Problem -- */` block (after `.problem__text:last-child` rule), within section 7:

```css
/* -- Benefits (What You Get) -- */

.benefits {
  background-color: var(--color-light);
}

.benefits__heading {
  margin-bottom: var(--space-5);
}

.benefits__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-3);
}

.benefits__card {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.benefits__icon {
  font-size: 2.5rem;
  line-height: 1;
}

@media (min-width: 768px) {
  .benefits__grid {
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
  }
}
```

This gives: single column on mobile, 2x2 grid on tablet/desktop. Cards reuse existing `.card`, `.card__title`, `.card__text` classes from Phase 1.
  </action>
  <verify>
    <automated>grep -c "benefits__card" index.html | grep -q "4" && grep -q "id=\"benefits\"" index.html && grep -q ".benefits__grid" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- grep "id=\"benefits\"" index.html returns a match
- grep -c "benefits__card" index.html returns 4
- grep "Что вы получите" index.html returns a match
- grep "Второе мнение" index.html returns a match
- grep "Понятный план действий" index.html returns a match
- grep "Письменное заключение" index.html returns a match
- grep "Ответы на" index.html returns a match
- grep ".benefits__grid" css/styles.css returns a match
- grep "grid-template-columns: repeat(2, 1fr)" css/styles.css returns a match
  </acceptance_criteria>
  <done>Benefits section visible in index.html with 4 cards containing exact Russian text from ТЗ, styled with responsive 2x2 grid on desktop and single column on mobile</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify benefits section visually</name>
  <what-built>Benefits section with 4 cards showing consultation value</what-built>
  <how-to-verify>
    1. Open index.html in a browser
    2. Scroll past the Problem section
    3. Verify heading "Что вы получите за одну консультацию" is visible
    4. Verify 4 cards are displayed with icons, titles, and descriptions
    5. Resize to mobile width (375px) -- cards should stack vertically
    6. Resize to desktop width (1024px+) -- cards should show in 2x2 grid
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- index.html contains section#benefits with 4 .benefits__card elements
- css/styles.css contains .benefits, .benefits__grid, .benefits__card rules
- Cards reuse existing .card BEM classes from Phase 1
- Layout is responsive: 1 column mobile, 2 columns tablet+
</verification>

<success_criteria>
- 4 benefit cards visible with exact Russian copy from ТЗ
- Responsive grid: single column on mobile, 2x2 on 768px+
- Cards use existing .card component styles (shadow, border-radius, padding)
</success_criteria>

<output>
After completion, create `.planning/phases/03-value-process-sections/03-01-SUMMARY.md`
</output>
