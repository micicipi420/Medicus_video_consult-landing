---
phase: 29-404-page-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - online-consultations.html
  - checkup.html
  - treatment-abroad.html
  - contacts.html
  - index.html
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "All 6 pages share identical header class attributes, footer class attributes, mobile menu, and sticky bar markup"
    - "Every form across all pages includes honeypot spam protection"
    - "All FAQ accordions on every page open and close with smooth animation"
    - "All CTA buttons link to the correct destinations with no placeholder href='#' links"
    - "checkup.html loads Motion CDN and animations.js like all other pages"
  artifacts:
    - path: "online-consultations.html"
      provides: "Service page with consistent header/footer"
      contains: 'class="header fixed'
    - path: "checkup.html"
      provides: "Checkup page with all scripts"
      contains: "motion@12"
    - path: "index.html"
      provides: "Main page with consistent footer class"
      contains: 'class="footer relative'
  key_links:
    - from: "checkup.html"
      to: "motion CDN"
      via: "script tag"
      pattern: "cdn.jsdelivr.net/npm/motion@12"
    - from: "checkup.html"
      to: "js/animations.js"
      via: "script tag"
      pattern: 'src="js/animations.js"'
    - from: "online-consultations.html header"
      to: "CSS header class"
      via: "class attribute"
      pattern: 'class="header fixed'
---

<objective>
Audit and fix UI consistency across all pages: normalize header/footer class attributes, add missing scripts to checkup.html, verify honeypot protection, verify FAQ accordion functionality, and fix all placeholder CTA links.

Purpose: Every page on the site must have identical shared-element markup and all interactive elements must work correctly.
Output: 5 HTML files updated with consistent shared elements and working links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html (canonical reference for shared elements)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix header/footer/script inconsistencies across all pages</name>
  <files>online-consultations.html, checkup.html, index.html</files>
  <read_first>
    - index.html line 55 (header class reference)
    - index.html line 980 (footer class reference)
    - index.html lines 1070-1072 (scripts reference)
    - online-consultations.html line 59 (header -- missing "header" class)
    - online-consultations.html line 765 (footer class)
    - checkup.html lines 831-833 (scripts -- missing Motion CDN + animations.js)
    - checkup.html line 761 (footer class)
    - treatment-abroad.html line 879 (footer class)
    - contacts.html line 257 (footer class)
  </read_first>
  <action>
Fix 3 specific inconsistencies found during audit:

**Fix 1: online-consultations.html header class (per UI-01)**
Line 59 currently: `<header class="fixed z-50 transition-all duration-500..."`
Change to: `<header class="header fixed z-50 transition-all duration-500..."` (add `header` class to match index.html, checkup.html, contacts.html, treatment-abroad.html).

**Fix 2: Normalize footer class across all pages (per UI-01)**
The canonical pattern is `class="footer relative overflow-hidden z-10 py-16"` (used by checkup, contacts, treatment-abroad). Two pages differ:
- index.html line 980: `class="relative overflow-hidden z-10 py-16"` -- add `footer` class
- online-consultations.html line 765: `class="relative overflow-hidden z-10 py-16"` -- add `footer` class

**Fix 3: checkup.html missing scripts (per UI-01)**
Line 831 currently only has `<script src="js/main.js"></script>`. Add Motion CDN and animations.js BEFORE main.js:
```html
<script src="https://cdn.jsdelivr.net/npm/motion@12.23.24/dist/motion.js"></script>
<script src="js/main.js"></script>
<script src="js/animations.js"></script>
```
Note: Motion CDN must load before main.js and animations.js (animations.js depends on window.Motion).

No other changes needed. All 5 pages already have: identical header content, identical mobile menu, identical sticky bar, identical mesh background.
  </action>
  <verify>
    <automated>grep -c 'class="header fixed' online-consultations.html | grep -q "1" && grep -c 'class="footer relative' index.html | grep -q "1" && grep -c 'class="footer relative' online-consultations.html | grep -q "1" && grep -c 'motion@12' checkup.html | grep -q "1" && grep -c 'animations.js' checkup.html | grep -q "1" && echo "PASS"</automated>
  </verify>
  <done>
    - online-consultations.html header has the `header` class matching all other pages
    - index.html and online-consultations.html footer has `footer` class matching other pages
    - checkup.html loads Motion CDN, main.js, and animations.js (3 script tags)
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix all CTA links, verify honeypot and FAQ across all pages</name>
  <files>index.html, online-consultations.html, treatment-abroad.html, checkup.html, contacts.html</files>
  <read_first>
    - Run: grep -n 'href="#"' *.html (find all placeholder links)
    - Run: grep -n 'visually-hidden' *.html (verify honeypot on all forms)
    - Run: grep -n 'faq__item' *.html (verify FAQ items exist)
    - Run: grep -n 'faq__answer' *.html (verify FAQ answer containers)
    - Run: grep -c 'contact-form' *.html (count forms per page)
  </read_first>
  <action>
**Part A: Fix placeholder href="#" links (per UI-04)**

Search all 5 HTML files for `href="#"`. For each occurrence:
- If it is a footer "Политика конфиденциальности" link: leave as `href="#"` (no privacy policy page exists yet -- this is expected)
- If it is an App Store / Google Play link in the footer: leave as `href="#"` (per STATE.md: "App Store / Google Play ссылки -- ждём от клиента")
- If it is any other CTA/nav link with `href="#"`: fix to point to the correct destination

Common pattern: sticky bar CTA should link to the page's own contact form anchor (e.g., `#contact` on pages with forms, or `index.html#contact` on pages without forms).

Audit every anchor tag across all 5 pages. Verify:
- Service page nav links (Консультации, Лечение, Чек-ап, Контакты) point to correct .html files
- "Обсудить случай" / form CTA buttons link to the page's own `#contact` section
- Footer service links match the actual file names (checkup.html not checkups.html, etc.)
- Phone links are all `tel:+77015322478`
- Email links are all `mailto:kz@medicusunion.com`

**Part B: Verify honeypot spam protection (per UI-02)**

All 5 pages already have honeypot fields (confirmed in audit). Verify each form's honeypot has:
- A `div.visually-hidden` wrapper with `aria-hidden="true"`
- A text input with `tabindex="-1"` and `autocomplete="off"`
- A `name` attribute (used by js/main.js to detect bots)

If any form is missing these attributes, add them.

**Part C: Verify FAQ accordion markup (per UI-03)**

Pages with FAQ: index.html, online-consultations.html, treatment-abroad.html, checkup.html.
Pages without FAQ: contacts.html (correct -- contacts page has no FAQ).

For each FAQ page, verify:
- Each FAQ item has `class="faq__item"` on the container
- Each answer has `class="faq__answer"` on the collapsible div
- The inline style block includes `.faq__answer { max-height: 0; overflow: hidden; transition: max-height 0.3s ease; }` and `.faq__item.is-open .faq__answer { max-height: 500px; }`
- The button inside each faq__item has proper click target for js/main.js accordion handler

Fix any discrepancies found. If online-consultations.html uses different max-height patterns (e.g., separate `.faq__answer--open` class), normalize to the index.html pattern.
  </action>
  <verify>
    <automated>for f in index.html online-consultations.html treatment-abroad.html checkup.html contacts.html; do echo "=== $f ===" && grep -c 'visually-hidden' "$f" && grep -c 'contact-form' "$f"; done && echo "--- href=# audit ---" && grep -c 'href="#"' index.html online-consultations.html treatment-abroad.html checkup.html contacts.html && echo "PASS"</automated>
  </verify>
  <done>
    - All CTA/nav links point to correct destinations (no broken href="#" except privacy policy and app store placeholders)
    - Every form on every page has honeypot with visually-hidden div, tabindex="-1", autocomplete="off"
    - All FAQ sections use consistent faq__item/faq__answer markup with max-height animation
    - Footer service links match actual filenames across all pages
  </done>
</task>

</tasks>

<verification>
- All 6 pages (including 404.html from plan 01) share identical header class, footer class, mobile menu, sticky bar, and scripts
- Every form has honeypot protection
- All FAQ accordions use consistent markup
- No broken CTA links (only expected placeholders for privacy policy and app store)
</verification>

<success_criteria>
Opening any page on the site shows identical header/footer/navigation behavior. All forms validate and include spam protection. All FAQ accordions animate smoothly. All CTA buttons navigate to their intended destinations.
</success_criteria>

<output>
After completion, create `.planning/phases/29-404-page-ui-polish/29-02-SUMMARY.md`
</output>
