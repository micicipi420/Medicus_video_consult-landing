---
phase: 84-cta-form-blue-gradient
plan: 01
type: execute
wave: 1
depends_on: ["79", "83"]
files_modified:
  - next/src/components/sections/ContactSection.tsx
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
must_haves:
  truths:
    - "ContactSection has bg-gradient-to-br with from-mu-blue/via-mu-accent-blue/to-mu-blue"
    - "Section contains 3 trust signals: Конфиденциально, Ответ в течение 24 часов, Врачи Европы"
    - "Each trust signal has icon + title + body"
    - "ContactForm is imported and rendered unmodified — no edits to ContactForm.tsx"
    - "Form sits in a white card (bg-white border-white/40) on top of the gradient"
    - "Submission flows through submitContactForm server action (unchanged)"
    - "Section aria-label='Заявка на консультацию'"
    - "No transition-all in ContactSection.tsx"
  artifacts:
    - path: "next/src/components/sections/ContactSection.tsx"
      provides: "Blue-gradient CTA section with explicit trust signals and white-card form wrapper"
  key_links:
    - from: "ContactSection background"
      to: "v8.0 brand gradient"
      via: "bg-gradient-to-br from-mu-blue via-mu-accent-blue to-mu-blue"
      pattern: "from-mu-blue via-mu-accent-blue"
    - from: "ContactSection form wrapper"
      to: "submitContactForm server action"
      via: "<ContactForm /> render preserved"
      pattern: "<ContactForm\\s*/>"
---

<objective>
Reskin only — preserve all submission behavior.
</objective>

<verification>
```bash
grep "bg-gradient-to-br from-mu-blue via-mu-accent-blue" next/src/components/sections/ContactSection.tsx  # 1
grep -E "Конфиденциально|Ответ.*24|Врачи Европы" next/src/components/sections/ContactSection.tsx  # 3
grep "<ContactForm /\>" next/src/components/sections/ContactSection.tsx  # 1
grep "transition-all" next/src/components/sections/ContactSection.tsx  # 0
git diff --stat next/src/components/sections/ContactForm.tsx  # empty (form unchanged)
```
</verification>
