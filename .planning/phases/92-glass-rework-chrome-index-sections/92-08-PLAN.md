---
phase: 92-glass-rework-chrome-index-sections
plan: 08
type: execute
wave: 4
depends_on:
  - 01
  - 03
  - 04
  - 05
  - 06
  - 07
files_modified:
  - next/src/components/sections/FinalCTA.tsx
autonomous: false
requirements:
  - GLASS-08
must_haves:
  truths:
    - "FinalCTA.tsx line 14 mix-blend-multiply decorative blob retired (anti-pattern #8 violation cleared)"
    - "Phase 92 final phase-gate sweep: every IN-SCOPE file + utility class shows zero hardcoded `bg-white/{N}` opacity classes (except sanctioned exceptions: HeroHub over-photo controls, ContactForm success overlay, ContactSection trust signals over blue gradient, FinalCTA image overlay)"
    - "Every CTA call-site verified opaque (CTA invariant negative-grep across 5 files)"
    - "Header.tsx (legacy) handled per 92-02-AUDIT.md Section 3 conclusion (sweep applied OR documented as dead-code skip)"
    - "All 10 GLASS-NN requirements code-complete (cross-checked against this plan + prior plans 92-01..92-07)"
    - "pnpm build clean"
  artifacts:
    - path: "next/src/components/sections/FinalCTA.tsx"
      provides: "Anti-pattern #8 violation cleared; decorative mix-blend-multiply blob retired"
    - path: ".planning/phases/92-glass-rework-chrome-index-sections/92-08-SWEEP-AUDIT.md"
      provides: "Phase-gate audit report — per-file grep results + CTA invariant negative-grep + GLASS-NN coverage matrix"
  key_links:
    - from: "92-08-SWEEP-AUDIT.md"
      to: "/gsd-verify-work + Phase 93 planner (consumes finalized invariants)"
      via: "sign-off documentation"
      pattern: "PHASE 92 SWEEP AUDIT COMPLETE"
---

<objective>
Close out Phase 92 with three deliverables:

1. **Retire the FinalCTA.tsx:14 `mix-blend-multiply` decorative blob** (anti-pattern #8 violation flagged in 92-02-AUDIT.md). Decision point gate: REMOVE entire decorative `<div>` (recommended per UI-SPEC) OR sanction with Key Decision (alternative path).
2. **Phase-gate sweep audit** — produce `92-08-SWEEP-AUDIT.md` with per-file grep results across all Phase 92 IN-SCOPE files, CTA invariant negative-grep, and GLASS-NN coverage matrix.
3. **Header.tsx (legacy) handling** — per 92-02-AUDIT.md Section 3: if RENDERED, apply Archetype A sweep here as a small extension; if NOT RENDERED, document the dead-code skip + flag follow-up quick task.

Purpose: Phase 92 must ship without leaving the FinalCTA anti-pattern #8 violation in production AND without leaving any IN-SCOPE file half-swept. This plan is the unambiguous owner of both. Wave 4 (after every other plan) so audit reflects final state.
Output: 1 modified TSX file (FinalCTA), conditionally 1 more (Header.tsx), 1 new audit markdown; pnpm build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md
@DESIGN.md
@CLAUDE.md
@next/src/components/sections/FinalCTA.tsx
@next/src/components/layout/Header.tsx

<interfaces>
<!-- FinalCTA.tsx line 14 (current state per 92-02-AUDIT.md): -->
<!--   `<div className="absolute top-0 left-0 w-96 h-96 bg-mu-blue/30 rounded-full blur-[100px] -z-10 mix-blend-multiply" />` -->
<!-- This violates anti-pattern #8 (mix-blend-mode on decorative element overlapping glass surfaces). -->

<!-- Phase 92 IN-SCOPE files (cumulative across plans 92-01..92-07): -->
- next/src/styles/liquid-glass.css (92-01)
- next/src/components/layout/HeaderClient.tsx, MobileMenu.tsx, StickyBar.tsx, Footer.tsx (92-03)
- next/src/components/sections/HeroHub.tsx, StatsBar.tsx, ServicesGrid.tsx (92-04)
- next/src/components/sections/ProcessSection.tsx, ProblemSection.tsx, WhyUsSection.tsx, ClinicsSection.tsx, PlatformSection.tsx, ReviewsSection.tsx (92-05)
- next/src/components/sections/FAQSection.tsx, FinalCTA.tsx (92-06; FinalCTA finalized in 92-08)
- next/src/components/sections/ContactSection.tsx, ContactForm.tsx (92-07)
- conditionally: next/src/components/layout/Header.tsx (92-08, depends on 92-02-AUDIT.md Section 3)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide FinalCTA.tsx:14 mix-blend-multiply retirement path</name>
  <decision>
    The decorative `<div>` at FinalCTA.tsx:14 — `bg-mu-blue/30 ... blur-[100px] -z-10 mix-blend-multiply` — violates anti-pattern #8 (mix-blend-mode on decorative element overlapping glass surfaces).

    Choose retirement path.
  </decision>
  <context>
    UI-SPEC recommendation: REMOVE the entire decorative `<div>` (let heat-leak handle ambient glow). Anti-pattern #8 cleanly cleared.
    PATTERNS.md alternative: drop only `mix-blend-multiply`, keep the rest (still anti-pattern but #8 specifically retired). Requires Key Decision sanctioning the remaining anti-pattern.
  </context>
  <options>
    <option id="option-a">
      <name>Remove entire decorative `<div>` (UI-SPEC recommended)</name>
      <pros>Cleanly retires anti-pattern #8 with no residual violation. Heat-leak gradient on `.liquid-card` / `.liquid-regular` (commit 9c93b9f) provides ambient glow elsewhere; FinalCTA's blue tone comes from the section frame. Simplest path; no Key Decision needed.</pros>
      <cons>Marginal visual change at FinalCTA — loses the soft blue glow accent in the top-left corner. Acceptable per UI-SPEC.</cons>
    </option>
    <option id="option-b">
      <name>Drop only `mix-blend-multiply`, keep the decorative blur</name>
      <pros>Preserves visual decoration. Retires anti-pattern #8 specifically.</pros>
      <cons>The remaining `bg-mu-blue/30 blur-[100px] -z-10` decoration is still suspicious (large blurred decorative element overlapping glass surfaces — borderline anti-pattern). Requires Key Decision sanctioning the residual decoration with explicit rationale.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (recommended), option-b (with Key Decision rationale), or escalate-to-user (if neither fits)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply chosen retirement path to FinalCTA.tsx:14</name>
  <files>next/src/components/sections/FinalCTA.tsx</files>
  <read_first>
    - next/src/components/sections/FinalCTA.tsx (lines 10–18 — locate the decorative `<div>` precisely)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (baseline className verbatim)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (Archetype I + §Plan 92-08 → FinalCTA.tsx line 14 row)
  </read_first>
  <action>
    Based on Task 1 resolution:

    **IF option-a (remove entire `<div>`):**
    - Locate the line containing `mix-blend-multiply` via `grep -n 'mix-blend-multiply' next/src/components/sections/FinalCTA.tsx`.
    - Remove the entire `<div className="absolute top-0 left-0 w-96 h-96 bg-mu-blue/30 rounded-full blur-[100px] -z-10 mix-blend-multiply" />` element (or whichever exact form it takes — match what's in the file at execution time).
    - Confirm the JSX still compiles (sibling element commas / closing tags intact).

    **IF option-b (drop mix-blend-multiply only):**
    - Edit the className: BEFORE `... blur-[100px] -z-10 mix-blend-multiply` → AFTER `... blur-[100px] -z-10`.
    - Append a Key Decision row in PROJECT.md: `KD-v9-003` — sanction residual `bg-mu-blue/30 blur-[100px] -z-10` decoration on FinalCTA as cosmetic blue accent; status approved (Phase 92 owner sign-off); rationale: post-#8-retirement decoration is a static blur, not a mix-blend operation, and the visual cost is acceptable per planner judgment.

    **IF escalate-to-user:** STOP. Surface back to user; do not modify the file.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'mix-blend-multiply' next/src/components/sections/FinalCTA.tsx` returns 0 (anti-pattern #8 violation cleared regardless of path chosen)
    - **IF option-a:** `grep -c 'bg-mu-blue/30.*blur-\[100px\]' next/src/components/sections/FinalCTA.tsx` returns 0 (entire `<div>` removed)
    - **IF option-b:** `grep -c 'bg-mu-blue/30.*blur-\[100px\]' next/src/components/sections/FinalCTA.tsx` returns ≥1 AND `grep -c 'KD-v9-003' .planning/PROJECT.md` returns ≥1
    - FinalCTA frame Tier 0 from Plan 92-06 still intact: `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/FinalCTA.tsx` returns ≥1
    - FinalCTA primary CTA opaque: `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/FinalCTA.tsx` returns ≥1; `grep 'from-mu-blue to-mu-accent-blue' next/src/components/sections/FinalCTA.tsx | grep -c 'backdrop'` returns 0
  </acceptance_criteria>
  <done>
    Anti-pattern #8 violation retired by chosen path; FinalCTA otherwise unchanged from Plan 92-06 state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Header.tsx (legacy) handling per 92-02-AUDIT.md Section 3 conclusion</name>
  <files>next/src/components/layout/Header.tsx (conditional)</files>
  <read_first>
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (Section 3 conclusion: RENDERED or NOT RENDERED)
    - next/src/components/layout/Header.tsx (only if AUDIT says RENDERED)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§No Analog Found / Special Cases — Header.tsx legacy row; Archetype A template if sweep applies)
    - next/src/components/layout/HeaderClient.tsx (post-92-03 reference state — same Archetype A template)
  </read_first>
  <action>
    Read 92-02-AUDIT.md Section 3 conclusion:

    **IF RENDERED:**
    - Apply Archetype A sweep to `Header.tsx`. Likely surfaces:
      - Wordmark gradient (preserve — Archetype J equivalent for brand identity)
      - Phone CTA gradient (preserve — Archetype J)
      - Any glass surface (`bg-white/{N}` + `backdrop-blur-*`): swap to `bg-[var(--glass-section-fill)]` + `backdrop-blur-[var(--glass-section-blur)]`
    - Verify `pnpm build` clean post-edit.

    **IF NOT RENDERED (dead code):**
    - DO NOT MODIFY `Header.tsx`. Document in `92-08-SWEEP-AUDIT.md`:
      > Header.tsx (legacy) NOT rendered per 92-02-AUDIT.md Section 3. No sweep applied. Recommend follow-up Quick Task `gsd:quick "remove dead Header.tsx"` to delete the file and any orphan imports — out of scope for Phase 92.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - **IF RENDERED:** `grep -c 'bg-white/' next/src/components/layout/Header.tsx` returns 0 (or returns only sanctioned exceptions documented inline)
    - **IF NOT RENDERED:** `git status --porcelain next/src/components/layout/Header.tsx` returns empty (untouched); `92-08-SWEEP-AUDIT.md` contains "Header.tsx legacy NOT rendered" entry
  </acceptance_criteria>
  <done>
    Header.tsx handled per AUDIT conclusion; rendered → swept; not rendered → documented skip with follow-up flag.
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase-gate sweep audit — produce 92-08-SWEEP-AUDIT.md with per-file grep + CTA invariant + GLASS-NN coverage matrix</name>
  <files>.planning/phases/92-glass-rework-chrome-index-sections/92-08-SWEEP-AUDIT.md</files>
  <read_first>
    - All 17 Phase 92 IN-SCOPE files (per `<interfaces>` block above)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (CTA invariant baseline)
    - .planning/REQUIREMENTS.md (GLASS-01..GLASS-10 definitions)
    - All prior summaries (.planning/phases/92-glass-rework-chrome-index-sections/92-{01..07}-SUMMARY.md if available)
  </read_first>
  <action>
    Produce `.planning/phases/92-glass-rework-chrome-index-sections/92-08-SWEEP-AUDIT.md` with these sections:

    ### Section 1: Per-file `bg-white/{N}` residue grep

    For each Phase 92 IN-SCOPE TSX file (16 files), run and embed:
    ```bash
    grep -n 'bg-white/[0-9]' <file>
    ```

    Each result must be either ZERO matches OR explicitly documented as sanctioned exception:
    - HeroHub over-photo controls (`bg-mu-text-900/55` — not `bg-white/N`, but flag if any white residue): preserve
    - ContactForm success overlay (`bg-white/82` line 111): UI-SPEC explicit preserve
    - ContactSection trust signal cards (`bg-white/15`, `bg-white/10`, `bg-white/20`) over blue gradient: planner-judgment preserve per Plan 92-07 SUMMARY
    - FinalCTA image-overlay gradient (`bg-gradient-to-r from-white/60 to-transparent`): not glass — image overlay
    - Cosmetic borders `border-white/{N}`: stay (decorative)

    Any unsanctioned residue → BLOCK and surface back to planner for additional sweep.

    ### Section 2: Hardcoded `backdrop-blur-*` residue grep

    ```bash
    grep -rn 'backdrop-blur-\(xl\|2xl\|3xl\|sm\|md\|lg\|\[[0-9]\+px\]\)' \
      next/src/components/sections/ next/src/components/layout/
    ```

    Expected residues (sanctioned):
    - MobileMenu.tsx:47 overlay backdrop (`backdrop-blur-sm` on dark dimmer — not glass-tier)
    - ContactForm.tsx:111 success overlay (`backdrop-blur-3xl` on opaque overlay — UI-SPEC explicit preserve)
    - ContactSection.tsx:60/63/83 trust signals (`backdrop-blur-md` over blue gradient — planner judgment per 92-07)

    Any other hardcoded backdrop-blur → BLOCK.

    ### Section 3: CTA invariant negative-grep across 5 IN-SCOPE files

    Run the cross-cutting verification grep from PATTERNS.md "Shared Patterns":
    ```bash
    grep -rn 'backdrop-filter\|backdrop-blur' \
      next/src/components/sections/HeroHub.tsx \
      next/src/components/layout/MobileMenu.tsx \
      next/src/components/layout/StickyBar.tsx \
      next/src/components/sections/ContactForm.tsx \
      next/src/components/sections/FinalCTA.tsx \
      | grep -E 'gradient-to-r|from-mu-blue|from-mu-cta'
    ```

    Expected: **zero matches**. Embed actual output. Non-zero → BLOCK.

    ### Section 4: GLASS-NN coverage matrix

    | Req ID | Plan(s) | File(s) modified | Grep gate result | Status |
    |--------|---------|------------------|------------------|--------|
    | GLASS-01 | 92-03 | HeaderClient, MobileMenu, StickyBar | per Section 1/2 above | ✅ / ❌ |
    | GLASS-02 | 92-04 | HeroHub | per Section 1/2 above + CTA invariant | ✅ / ❌ |
    | GLASS-03 | 92-04 | StatsBar | per Section 1/2 above | ✅ / ❌ |
    | GLASS-04 | 92-04 | ServicesGrid | per Section 1/2 above + nested badge collapsed | ✅ / ❌ |
    | GLASS-05 | 92-05 | ProcessSection, ProblemSection, WhyUsSection, ClinicsSection, PlatformSection, ReviewsSection | per Section 1/2 above | ✅ / ❌ |
    | GLASS-06 | 92-06 | FAQSection | per Section 1/2 above + accordion preserved | ✅ / ❌ |
    | GLASS-07 | 92-07 | ContactSection, ContactForm | WCAG measurement recorded; KD-v9-002 status | ✅ / ❌ |
    | GLASS-08 | 92-06 + 92-08 | FinalCTA | per Section 1/2 above + mix-blend-multiply retired (Task 2) | ✅ / ❌ |
    | GLASS-09 | 92-03 | Footer | per Section 1/2 above | ✅ / ❌ |
    | GLASS-10 | 92-01 | liquid-glass.css | utility re-pointing + heat-leak preserved | ✅ / ❌ |

    Every row must be ✅ before sign-off. Any ❌ → BLOCK and document remediation.

    ### Section 5: Anti-pattern enforcement gate (Decision H)

    For each anti-pattern listed in DESIGN.md `## v9.0 Anti-Patterns`, document Phase 92 compliance status:
    - #4 (fills > 0.16): all token consumption (PASS) — KD-v9-002 sanctioned escalation if applicable
    - #5 (statically painted green tint): heat-leak gradient blob-driven; brand-color icon chips inside cards (PASS)
    - #6 (animated backdrop-filter): HeaderClient transition list trimmed (PASS, 92-03 Task 1)
    - #8 (mix-blend-mode on glass): FinalCTA:14 retired (PASS, 92-08 Task 2)
    - #11 (backdrop-filter on .living-blob-field): no-op — Phase 92 doesn't touch blob-field (PASS)
    - #12 (mobile blur >12px): token clamp (PASS)
    - #13 (≤2 glass per viewport): ServicesGrid nested badge collapsed; sanctioned exceptions documented (PASS)
    - #14 (new glass class without @a11y-layer-coverage): no new classes added (PASS)
    - #15 (cheat-passing a11y): every a11y row marked "pending live-toggle (Phase 94)" — no claim of "verified" (PASS)

    ### Section 6: Sign-off

    ```
    PHASE 92 SWEEP AUDIT COMPLETE: <date>
    All 10 GLASS-NN requirements code-complete.
    All 5 IN-SCOPE CTAs verified opaque.
    FinalCTA mix-blend-multiply retired via option-<a|b>.
    Header.tsx (legacy) handled: <swept | dead-code-skip>.
    KD-v9-002: <triggered with rationale | not triggered, default 0.14 passed WCAG>.
    Phase 92 ready for /gsd-verify-work and Phase 93 hand-off.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/92-glass-rework-chrome-index-sections/92-08-SWEEP-AUDIT.md && grep -c 'PHASE 92 SWEEP AUDIT COMPLETE' .planning/phases/92-glass-rework-chrome-index-sections/92-08-SWEEP-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/92-glass-rework-chrome-index-sections/92-08-SWEEP-AUDIT.md` exists
    - Section 1: per-file `bg-white/{N}` grep results documented; all unsanctioned residue ZERO or explicitly listed in sanctioned exceptions
    - Section 2: hardcoded backdrop-blur grep documented; all residue sanctioned
    - Section 3: CTA invariant negative-grep returns ZERO matches (embedded output verbatim)
    - Section 4: GLASS-NN coverage matrix all ✅
    - Section 5: anti-pattern enforcement table all PASS
    - Section 6: "PHASE 92 SWEEP AUDIT COMPLETE" sign-off line present
    - `pnpm --dir next build` exits 0
    - Russian copy / `&nbsp;` preservation (baseline counts must hold post-sweep — Russian subject+verb binding + orphan prevention per project memory):
      - `grep -c '&nbsp;' next/src/components/sections/FinalCTA.tsx` returns 0 (unchanged from pre-sweep baseline)
  </acceptance_criteria>
  <done>
    Phase 92 closed out — anti-pattern #8 retired, Header.tsx handled, all GLASS-NN code-complete, audit committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FinalCTA decorative blob ↔ glass frame underneath | mix-blend-multiply causes paint compositing interaction with glass — anti-pattern #8 |
| Phase 92 sign-off ↔ Phase 93 hand-off | SWEEP-AUDIT is the contract Phase 93 (sub-route propagation) consumes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-08-01 | Tampering | Anti-pattern #8 violation persists into v9.0 ship (FinalCTA decorative mix-blend artifact on every page render) | mitigate | Task 1 + Task 2 retire it via option-a or option-b with Key Decision |
| T-92-08-02 | Repudiation | Phase 92 ships with un-swept files (silent gaps masquerading as complete) | mitigate | Task 4 SWEEP-AUDIT performs deterministic grep across every IN-SCOPE file with sanctioned-exception list |
| T-92-08-03 | Tampering | CTA invariant regressed by some intermediate plan during Wave 2/3 | mitigate | Task 4 Section 3 cross-cutting CTA invariant negative-grep |
| T-92-08-04 | Information disclosure | GLASS-NN coverage matrix marks something ✅ that isn't actually code-complete (cheat-pass) | mitigate | Each ✅ requires evidence (grep gate result OR file:line in modified files); Section 6 sign-off line is the audit trail |
| T-92-08-05 | Denial of service | Header.tsx (legacy) is rendered AND un-swept — chrome inconsistency on some routes (mobile blur >12px regression) | mitigate | Task 3 handles per 92-02-AUDIT.md conclusion; if RENDERED, sweep applied; if NOT RENDERED, documented with follow-up flag |
</threat_model>

<verification>
1. `pnpm --dir next build` exits 0.
2. All 4 tasks' acceptance criteria pass.
3. `92-08-SWEEP-AUDIT.md` Section 6 sign-off line present.
4. Manual: open `/`, scroll to FinalCTA — no top-left blue mix-blend artifact visible.
5. Manual: full-page visual sweep at 375px and 1440px — no glass-tier surfaces show legacy `bg-white/{N}` opacity (all consume v9.0 tier tokens).
</verification>

<success_criteria>
- FinalCTA mix-blend-multiply retired (anti-pattern #8 cleared)
- Header.tsx (legacy) handled per AUDIT
- 92-08-SWEEP-AUDIT.md written with all 6 sections
- All 10 GLASS-NN requirements ✅ in coverage matrix
- All 5 in-scope CTAs verified opaque
- Anti-pattern enforcement gate all PASS
- `pnpm build` clean
- Phase 92 ready for /gsd-verify-work and Phase 93 hand-off
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-08-SUMMARY.md`. Reference SWEEP-AUDIT.md as primary deliverable; SUMMARY notes the Task 1 decision outcome (option-a or option-b), Task 3 Header.tsx handling, and overall Phase 92 sign-off.
</output>
