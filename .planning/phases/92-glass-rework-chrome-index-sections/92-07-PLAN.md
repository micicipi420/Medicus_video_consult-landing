---
phase: 92-glass-rework-chrome-index-sections
plan: 07
type: execute
wave: 4
depends_on:
  - 03
  - 05
  - 06
files_modified:
  - next/src/components/sections/ContactSection.tsx
  - next/src/components/sections/ContactForm.tsx
  - .planning/PROJECT.md
  - next/src/app/globals.css (conditional — Task 3 KD-v9-002 escalation only)
autonomous: false
requirements:
  - GLASS-07
must_haves:
  truths:
    - "ContactSection form panel migrated from `bg-white p-6` (opaque) to `bg-[var(--glass-form-fill)] backdrop-blur-[var(--glass-form-blur)]`"
    - "ContactSection blue-gradient outer (line 26) Path A preserved (gradient stays; localized blob dimming documented as architecturally moot)"
    - "ContactForm inputs migrated from `bg-white/50 backdrop-blur-md focus:bg-white/72` to `bg-white` (opaque, no blur, no animated transparency)"
    - "ContactForm input transition list drops `background-color` (no longer animates)"
    - "ContactForm labels remain `text-mu-text-900 font-bold` (already promoted; verify-only)"
    - "ContactForm submit gradient at line 247 (`from-mu-blue to-mu-accent-blue`) untouched (Archetype J)"
    - "ContactForm success overlay at line 111 (`bg-white/82 backdrop-blur-3xl`) untouched (UI-SPEC explicit preserve)"
    - "WCAG measurement performed on body copy at heat=0 AND heat=1; if either <4.5:1, KD-v9-002 escalation triggered"
    - "If escalated: --glass-form-fill desktop bumped from 0.14 to 0.30; KD-v9-002 row added to PROJECT.md Key Decisions"
    - "Form submission still reaches Directus endpoint (FORM-03 preserved)"
  artifacts:
    - path: "next/src/components/sections/ContactSection.tsx"
      provides: "Tier 2 form panel; Path A blue-gradient backdrop preserved"
    - path: "next/src/components/sections/ContactForm.tsx"
      provides: "Opaque input fields; promoted labels; preserved submit + success overlay"
    - path: ".planning/PROJECT.md"
      provides: "KD-v9-002 row IF escalation triggered (form-fill 0.14 → 0.30)"
      contains: "KD-v9-002"
  key_links:
    - from: "ContactSection.tsx form panel"
      to: "globals.css --glass-form-fill / --glass-form-blur"
      via: "Tailwind arbitrary-value classes"
      pattern: "bg-\\[var\\(--glass-form-fill\\)\\]"
    - from: "ContactForm submit button"
      to: "Directus /items/submissions endpoint"
      via: "fetch() in onSubmit handler"
      pattern: "fetch.*submissions"
---

<objective>
Apply form-safety treatment to ContactForm + ContactSection per Decision B (GLASS-07): migrate the form panel from opaque-white to Tier 2 form-fill; flatten inputs to opaque white (drop blur, drop animated transparency on focus); promote labels (verify-only — already promoted in v8.0); preserve the success overlay opaque-leaning style; preserve the submit-gradient CTA. Then perform WCAG AA contrast measurement on body copy at heat=0 AND heat=1 worst-case blob positions. If either reading is <4.5:1, escalate `--glass-form-fill` desktop value 0.14 → 0.30, log KD-v9-002 in PROJECT.md, re-measure.

Path A locked per RESEARCH §Pitfall 2 + **KD-v9-003** (CONTEXT.md): keep ContactSection's blue-gradient outer (line 26) — the form panel sits over a vivid blue rectangle, not the page blob. "Localized blob dimming" (Decision B step 4 / REQUIREMENTS.md GLASS-07 sub-clause) is therefore architecturally moot for the current layout and is **deferred under KD-v9-003**; no dimming overlay is added.

Purpose: GLASS-07 is the highest-risk requirement in Phase 92 — a 6× drop in form-panel opacity (1.00 → 0.14) at the page's primary conversion point. This plan owns the contrast measurement gate AND the KD-v9-002 escalation flow. Wave 4 because it depends on Wave 2/3 chrome and section sweeps having shipped (so the visual context the form sits in is final, making the contrast measurement meaningful).
Output: 2 modified TSX files; conditionally PROJECT.md updated with KD-v9-002; pnpm build clean; documented WCAG measurement evidence in SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md
@DESIGN.md
@CLAUDE.md
@next/src/app/globals.css
@next/src/components/sections/ContactSection.tsx
@next/src/components/sections/ContactForm.tsx

<interfaces>
<!-- Archetype F (PATTERNS.md §Archetype F) — Form panel + inputs FORM-SAFETY -->

Tier 2 form-fill consumption:
- `bg-[var(--glass-form-fill)] backdrop-blur-[var(--glass-form-blur)]` → desktop 0.14 / mobile 0.18 (clamp ≤12px blur on mobile)

Decision B form-safety contract:
1. Panel fill: start at 0.14 (token default). Escalate to 0.30 (KD-v9-002) only if WCAG fails.
2. Label color: `text-mu-text-900 font-bold` (verify-only — already promoted in v8.0).
3. Inputs: `bg-white` opaque, NOT glass.
4. Localized blob dimming: SKIP (Path A — form sits over blue gradient, not page blob; deferred under KD-v9-003).
5. Body copy contrast ≥4.5:1, large text ≥3:1, measured at heat=0 AND heat=1 worst-case.

Decision G — KD-v9-002 escalation:
- Auto-decided per delegation; no further user gate.
- If body copy <4.5:1: bump --glass-form-fill desktop to 0.30; re-measure; log KD-v9-002 row in PROJECT.md with rationale + before/after contrast values + screenshot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactSection.tsx + ContactForm.tsx — form panel Tier 2 migration; inputs flattened to opaque; CTA + success overlay preserved</name>
  <files>next/src/components/sections/ContactSection.tsx, next/src/components/sections/ContactForm.tsx</files>
  <read_first>
    - next/src/components/sections/ContactSection.tsx (FULL FILE — outer gradient at line 26, decorative blurs at 35/36, trust signal cards at 60/63/83, form panel at line 120)
    - next/src/components/sections/ContactForm.tsx (FULL FILE — success overlay at line 111, success icon disc at line 112, input template at line 128, labels at lines 137/159/182/207, submit at line 247)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Archetype F template + §Plan 92-07 table)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md (Decision B + Decision G)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (ContactForm.tsx:247 CTA invariant)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md (§Pitfall 2 — form sits over blue gradient, not page blob; Path A locked)
  </read_first>
  <action>
    **ContactSection.tsx:**

    1. **Line 26 (outer section gradient — Path A locked)**:
       - `relative z-10 overflow-hidden bg-gradient-to-br from-mu-blue via-mu-accent-blue to-mu-blue py-16 sm:py-20 lg:py-24` — **NO CHANGE — Path A preserves blue-gradient backdrop** (RESEARCH §Pitfall 2).
       - Document in SUMMARY.md: "Localized blob dimming (Decision B step 4 / GLASS-07 sub-clause) deferred under KD-v9-003 — form panel sits over opaque blue rectangle, not page blob field. No dimming overlay added."

    2. **Lines 35, 36 (decorative blurs inside section)**:
       - `absolute -left-40 -top-40 h-[480px] w-[480px] rounded-full bg-white/15 blur-[120px]` — **NO CHANGE** (decorative blur, not glass-tier).
       - `absolute -bottom-40 -right-32 h-[420px] w-[420px] rounded-full bg-mu-green-500/25 blur-[120px]` — **NO CHANGE**.

    3. **Lines 60, 63, 83 (trust signal cards over the blue gradient)**:
       - `flex flex-col gap-2 rounded-2xl border border-white/15 bg-white/10 p-4 backdrop-blur-md` — **NO CHANGE** by default. RATIONALE: these sit over the vivid blue gradient where 0.10 white reads differently than over the page blob. Migrating to `bg-[var(--glass-button-fill)]` (0.12/0.16) would over-saturate vs the contract ("white-on-blue trust signals"). PATTERNS.md notes "planner judgment — recommendation: leave as-is".
       - Document choice in SUMMARY.md.

    4. **Line 63 (icon bg over gradient)**:
       - `bg-white/20` — **NO CHANGE** (decorative).

    5. **Line 120 (form panel wrapper — Archetype F)**:
       - BEFORE: `rounded-[2rem] border border-white/40 bg-white p-6 shadow-glass-lg sm:rounded-[2.5rem] sm:p-8`
       - AFTER:  `rounded-[2rem] border border-white/40 bg-[var(--glass-form-fill)] backdrop-blur-[var(--glass-form-blur)] p-6 shadow-glass-lg sm:rounded-[2.5rem] sm:p-8`
       - This is a 7× drop in opacity (1.00 → 0.14 desktop). Task 2 measures contrast and may escalate.

    **ContactForm.tsx:**

    6. **Line 111 (success overlay — UI-SPEC PRESERVE OPAQUE)**:
       - `absolute inset-0 z-20 flex flex-col items-center justify-center rounded-[inherit] bg-white/82 p-8 text-center shadow-glass-lg backdrop-blur-3xl` — **NO CHANGE — preserve opaque-leaning success overlay** (UI-SPEC explicit per PATTERNS.md Archetype F note).

    7. **Line 112 (success icon disc)**:
       - `mb-6 flex h-24 w-24 ... rounded-full border border-white/70 bg-white/80 shadow-glass-sm` — **NO CHANGE** (decorative inner; opaque-leaning matches success-overlay context).

    8. **Line 128 (input template — `inputBase`) — Archetype F template**:
       - BEFORE: `'w-full min-h-14 px-5 py-4 rounded-2xl border bg-white/50 backdrop-blur-md focus:bg-white/72 focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-[background-color,border-color,box-shadow,transform] duration-200 placeholder:text-mu-text-500 font-medium text-mu-text-900 shadow-glass-inner'`
       - AFTER:  `'w-full min-h-14 px-5 py-4 rounded-2xl border bg-white focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-[border-color,box-shadow,transform] duration-200 placeholder:text-mu-text-500 font-medium text-mu-text-900 shadow-glass-inner'`
       - Diff (4 changes):
         - Drop `bg-white/50` → `bg-white`
         - Drop `backdrop-blur-md`
         - Drop `focus:bg-white/72` (no animated-transparency on focus)
         - Drop `background-color` from `transition-[...]` list (no longer animates)
       - Keep: `focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20` (focus ring outside input).

    9. **Lines 137, 159, 182, 207 (labels — verify-only)**:
       - `block text-sm font-bold text-mu-text-900 mb-2` — **NO CHANGE — already promoted** (Decision B step 2 verify-only).

    10. **Line 247 (submit gradient — Archetype J)**:
        - `... rounded-2xl bg-gradient-to-r from-mu-blue to-mu-accent-blue ...` — **NO CHANGE — opaque-forever**.
        - VERIFY: className contains zero `backdrop-*`.

    Preserve the form's `onSubmit` handler (FORM-03 — Directus endpoint integration must not regress).
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - ContactSection: `grep -c 'bg-white p-6\|bg-white p-8' next/src/components/sections/ContactSection.tsx` returns 0 (form panel migrated)
    - ContactSection: `grep -c 'bg-white\([^/]\|$\)' next/src/components/sections/ContactSection.tsx` drops by ≥1 vs pre-sweep baseline (robust to JSX formatting variations — counts standalone `bg-white` not followed by `/`)
    - ContactSection: `grep -c 'bg-\[var(--glass-form-fill)\]' next/src/components/sections/ContactSection.tsx` returns ≥1
    - ContactSection: `grep -c 'backdrop-blur-\[var(--glass-form-blur)\]' next/src/components/sections/ContactSection.tsx` returns ≥1
    - ContactSection: `grep -c 'from-mu-blue via-mu-accent-blue to-mu-blue' next/src/components/sections/ContactSection.tsx` returns ≥1 (Path A: blue gradient preserved)
    - ContactForm: `grep -c 'bg-white/50\|focus:bg-white/72' next/src/components/sections/ContactForm.tsx` returns 0
    - ContactForm: `grep -c 'bg-white' next/src/components/sections/ContactForm.tsx` returns ≥1 (input fields use opaque white)
    - ContactForm: `grep -c 'backdrop-blur-md' next/src/components/sections/ContactForm.tsx` returns 0 (input blur dropped)
    - ContactForm: `grep -c 'transition-\[background-color' next/src/components/sections/ContactForm.tsx` returns 0 (animated-transparency on input dropped)
    - ContactForm: `grep -c 'bg-white/82' next/src/components/sections/ContactForm.tsx` returns ≥1 (success overlay PRESERVED)
    - ContactForm: `grep -c 'text-mu-text-900 font-bold\|font-bold text-mu-text-900' next/src/components/sections/ContactForm.tsx` returns ≥4 (labels promoted — verify-only)
    - ContactForm: `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/ContactForm.tsx` returns ≥1 (submit CTA preserved)
    - CTA invariant: `grep 'from-mu-blue to-mu-accent-blue' next/src/components/sections/ContactForm.tsx | grep -c 'backdrop'` returns 0
    - Directus endpoint integration intact: `grep -c 'fetch\|action.*submissions\|/items/submissions' next/src/components/sections/ContactForm.tsx` returns ≥1 (FORM-03 invariant)
  </acceptance_criteria>
  <done>
    Form panel at Tier 2; inputs opaque with focus ring intact; success overlay preserved; submit CTA preserved; FORM-03 endpoint integration intact.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: WCAG AA measurement at heat=0 AND heat=1; conditional KD-v9-002 escalation</name>
  <what-built>
    ContactForm panel migrated to Tier 2 form-fill (`--glass-form-fill` desktop 0.14 / mobile 0.18). Inputs flattened to opaque `bg-white`. Submit + success overlay preserved.
  </what-built>
  <how-to-verify>
    Use VALIDATION.md Recipe 3 (ContactForm WCAG measurement):

    1. Run `pnpm --dir next dev` and open `/`.
    2. Scroll to ContactSection (form is over the blue gradient).
    3. Open Chrome DevTools → Elements → select a body copy `<p>` element inside the form panel (e.g., the form intro text or the privacy notice).
    4. Computed → click on the foreground `color` swatch → DevTools shows the contrast ratio against the **composite** background.
    5. **Force heat=0** via console: `window.__blobDebug?.setHeat?.(0)` (Phase 91 debug helper). Note the contrast ratio reading.
    6. **Force heat=1** via console: `window.__blobDebug?.setHeat?.(1)` AND park the blob centroid behind the form: `window.__blobDebug?.setMode?.('static')` then move cursor to form area to lock position. Note the contrast ratio reading.
    7. Repeat steps 5–6 for label `<label>` elements (must be ≥4.5:1) and large/heading text (must be ≥3:1).

    **DECISION GATE:**
    - If ALL readings ≥4.5:1 (body) and ≥3:1 (large): respond `passed`. No escalation needed. Plan complete.
    - If ANY reading <4.5:1 body / <3:1 large: respond `escalate <ratio> on <element>`. Plan executor will perform Task 3 (escalation flow).
    - If you cannot run the live measurement (e.g., dev server unavailable, Phase 91 debug helper missing because Phase 91 not yet shipped): respond `defer <reason>`. The escalation will be deferred to a follow-up after Phase 91 lands; document deferral in SUMMARY.md.

    Provide screenshot evidence (saved to `.planning/phases/92-glass-rework-chrome-index-sections/wcag-evidence/heat-0.png` and `heat-1.png`) — these become the artifact for KD-v9-002 if escalation triggers AND for FORM-03 sign-off if it doesn't.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `passed <body-contrast> / <label-contrast>` — no escalation
    - `escalate <body-contrast> on <element>` — proceed to Task 3 escalation
    - `defer <reason>` — Phase 91 dependency unmet; document and continue
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3 (CONDITIONAL — only runs if Task 2 returned `escalate`): KD-v9-002 escalation — bump --glass-form-fill 0.14 → 0.30; re-measure; log Key Decision in PROJECT.md</name>
  <files>next/src/app/globals.css, .planning/PROJECT.md</files>
  <read_first>
    - next/src/app/globals.css (FROZEN-mostly per Decision I — but `--glass-form-fill` IS the escalation knob; Decision G explicitly authorizes this single bump under KD-v9-002)
    - .planning/PROJECT.md (Key Decisions section — locate KD-v9-001 row; KD-v9-002 row appended below)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md (Decision G escalation flow)
  </read_first>
  <action>
    **ONLY EXECUTE if Task 2 returned `escalate <ratio>`. Otherwise skip — mark Task 3 N/A in SUMMARY.md.**

    Per Decision G escalation flow:

    1. **Edit globals.css** (single-line bump under explicit KD-v9-002 authorization — this is the only Decision-I freeze exception):
       - Locate the `--glass-form-fill` declarations in `:root` and the mobile media query block.
       - Desktop: `--glass-form-fill: rgba(255, 255, 255, 0.14);` → `rgba(255, 255, 255, 0.30);`
       - Mobile (if present in `@media (max-width: 768px)` block): `0.18` → `0.30` (or proportional intermediate value if planner determines tier ratio matters; default to 0.30 for parity).
       - DO NOT TOUCH any other `--glass-*` token. KD-v9-002 escalates ONLY the form-fill token.

    2. **Re-run Task 2 measurement procedure** (Recipe 3) and confirm body copy ≥4.5:1, labels ≥4.5:1, large text ≥3:1 at both heat=0 and heat=1.

    3. **Append KD-v9-002 row to PROJECT.md** Key Decisions table with these fields:
       - **ID:** KD-v9-002
       - **Date:** 2026-04-30 (or current execution date)
       - **Decision:** Escalate `--glass-form-fill` from 0.14/0.18 → 0.30/0.30 (desktop/mobile) per Decision G to satisfy WCAG AA on ContactForm body copy at worst-case blob position.
       - **Status:** approved (auto-decided per CONTEXT.md Decision G delegation; no further user gate)
       - **Rationale:** Form panel at Tier 2 default 0.14 yielded body-copy contrast <4.5:1 at heat=1 with blob centroid parked behind form (recorded reading: <pre-bump-ratio>). Escalation to 0.30 raises body-copy contrast to <post-bump-ratio> ≥4.5:1.
       - **Evidence:** `.planning/phases/92-glass-rework-chrome-index-sections/wcag-evidence/heat-0.png` + `heat-1.png` (pre-bump) and `.planning/phases/92-glass-rework-chrome-index-sections/wcag-evidence/heat-0-post.png` + `heat-1-post.png` (post-bump).
       - **Owner:** Phase 92 Plan 92-07 executor.

    4. Commit globals.css edit + PROJECT.md edit together: `feat(92-07): escalate --glass-form-fill to 0.30 per KD-v9-002`.

    Anti-pattern enforcement (Decision H): #4 (fills > 0.16) — escalated 0.30 is the form-safety exception family explicitly sanctioned by Decision G. PATTERNS.md cross-cutting rule: "Tier 2 form panel ≤ 0.18 mobile (form-safety exception family) escalatable to 0.30 only via KD-v9-002". Compliant.
  </action>
  <verify>
    <automated>pnpm --dir next build && grep -c 'KD-v9-002' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - **IF executed:**
      - `pnpm --dir next build` exits 0
      - `grep -c 'KD-v9-002' .planning/PROJECT.md` returns ≥1
      - `grep 'glass-form-fill' next/src/app/globals.css | grep -c '0.30'` returns ≥1 (either desktop or mobile entry)
      - Re-measured WCAG body-copy contrast ≥4.5:1 at heat=1 worst-case (recorded in SUMMARY.md)
      - WCAG evidence screenshots saved (pre + post) at `.planning/phases/92-glass-rework-chrome-index-sections/wcag-evidence/`
    - **IF SKIPPED (Task 2 returned `passed`):**
      - SUMMARY.md notes "KD-v9-002 escalation NOT triggered; body-copy contrast at default 0.14 = <ratio> ≥4.5:1"
      - globals.css unchanged
      - PROJECT.md unchanged
    - Russian copy / `&nbsp;` preservation (baseline counts must hold post-sweep — Russian subject+verb binding + orphan prevention per project memory):
      - `grep -c '&nbsp;' next/src/components/sections/ContactSection.tsx` returns 5 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/sections/ContactForm.tsx` returns 0 (unchanged from pre-sweep baseline)
  </acceptance_criteria>
  <done>
    EITHER (a) escalation executed cleanly with KD-v9-002 logged + re-measurement passing, OR (b) escalation skipped because default 0.14 already passed WCAG AA — both paths satisfy GLASS-07.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContactForm body copy ↔ composite background (form panel + blob heat) | WCAG AA contrast must hold at heat=0 AND heat=1 worst-case |
| ContactForm submit handler ↔ Directus endpoint | FORM-03 must not regress |
| Form panel opacity ↔ KZ medical content legal/regulatory exposure | Sub-AA contrast on medical-content forms = compliance risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-07-01 | Information disclosure | WCAG AA failure on body copy / labels (medical content legal risk for KZ audience) | mitigate | Task 2 manual measurement + Task 3 conditional KD-v9-002 escalation to 0.30 |
| T-92-07-02 | Tampering | ContactForm submit gradient paired with `backdrop-*` (CTA disappear-into-blob — PITFALL #3.1) | mitigate | Acceptance: CTA invariant negative-grep |
| T-92-07-03 | Tampering | Directus endpoint integration regressed during sweep (FORM-03 broken — submissions stop reaching admin UI) | mitigate | Acceptance: `fetch` / `submissions` grep ≥1; manual smoke test in Task 2 (submit a test entry) |
| T-92-07-04 | Denial of service | Animated transparency on input focus (`focus:bg-white/72` → `transition-[background-color,...]`) hits anti-pattern: per-frame compositing on focus | mitigate | Task 1 drops `focus:bg-white/72` AND removes `background-color` from transition list |
| T-92-07-05 | Information disclosure | Mobile blur on form panel >12px (45+ audience battery + form jank) | mitigate | `--glass-form-blur` clamps to 12px on mobile (Phase 90 frozen) |
| T-92-07-06 | Repudiation | Escalation performed without KD-v9-002 audit trail (decision log gap) | mitigate | Task 3 mandatory PROJECT.md row + screenshot evidence |
</threat_model>

<verification>
1. `pnpm --dir next build` exits 0.
2. Task 1 acceptance criteria pass.
3. Task 2 manual WCAG measurement performed; result recorded in SUMMARY.md.
4. Task 3 EITHER executed (escalation logged, re-measurement passes) OR explicitly skipped with body-copy contrast at default 0.14 documented.
5. Functional smoke: submit a test entry from the form; verify it reaches Directus admin UI (FORM-03 unchanged).
</verification>

<success_criteria>
- Form panel at Tier 2 (default 0.14 OR escalated 0.30 with KD-v9-002 logged)
- Inputs opaque white with no animated-transparency on focus
- Labels verified promoted (already done in v8.0)
- Submit gradient preserved
- Success overlay preserved
- WCAG AA body copy ≥4.5:1 at heat=1 worst-case (verified)
- FORM-03 Directus integration unchanged
- `pnpm build` clean
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-07-SUMMARY.md`. Document:
1. Path A choice (blue gradient backdrop preserved; localized blob dimming deferred under KD-v9-003 — see 92-CONTEXT.md).
2. WCAG measurement readings at heat=0 and heat=1 for body + labels + large text.
3. KD-v9-002 status: triggered (with pre/post readings) OR skipped (with default reading).
4. Trust signal cards over blue gradient: kept as-is (planner judgment per PATTERNS.md).
5. Manual smoke test result for FORM-03 Directus integration.
</output>
