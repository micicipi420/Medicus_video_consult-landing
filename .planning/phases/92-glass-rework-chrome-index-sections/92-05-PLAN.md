---
phase: 92-glass-rework-chrome-index-sections
plan: 05
type: execute
wave: 3
depends_on:
  - 03
  - 04
files_modified:
  - next/src/components/sections/ProcessSection.tsx
  - next/src/components/sections/ProblemSection.tsx
  - next/src/components/sections/WhyUsSection.tsx
  - next/src/components/sections/ClinicsSection.tsx
  - next/src/components/sections/PlatformSection.tsx
  - next/src/components/sections/ReviewsSection.tsx
autonomous: true
requirements:
  - GLASS-05
must_haves:
  truths:
    - "ProcessSection step cards consume --glass-card-fill / --glass-card-blur (Tier 1); inner icon chips Tier 3 via --glass-button-{fill,blur}"
    - "ProblemSection cards consume --glass-card-fill / --glass-card-blur; inner icon chips keep brand-color tint, blur migrated to --glass-button-blur"
    - "WhyUsSection section pill at Tier 0; advantage icon chips brand-color preserved with --glass-button-blur; image frames at Tier 0; stat card at Tier 1"
    - "ClinicsSection country cards Tier 1 → Tier 2 hover"
    - "PlatformSection single panel at Tier 0"
    - "ReviewsSection cards Tier 1 → Tier 2 hover"
    - "All hardcoded `backdrop-blur-{xl,2xl,3xl}` removed from these 6 files"
    - "pnpm build clean"
  artifacts:
    - path: "next/src/components/sections/ProcessSection.tsx"
      provides: "Tier 1 step cards with Tier 3 inner icon chips"
    - path: "next/src/components/sections/ProblemSection.tsx"
      provides: "Tier 1 problem cards; brand-color inner icon chips with token-based blur"
    - path: "next/src/components/sections/WhyUsSection.tsx"
      provides: "Tier 0 section pill, image frames; Tier 1 stat card; brand-color advantage chips"
    - path: "next/src/components/sections/ClinicsSection.tsx"
      provides: "Tier 1 country cards"
    - path: "next/src/components/sections/PlatformSection.tsx"
      provides: "Tier 0 single panel"
    - path: "next/src/components/sections/ReviewsSection.tsx"
      provides: "Tier 1 review cards"
  key_links:
    - from: "6 mid-section files"
      to: "globals.css --glass-{section,card,button}-{fill,blur}"
      via: "Tailwind arbitrary-value classes"
      pattern: "bg-\\[var\\(--glass-(section|card|button|form)-fill\\)\\]"
---

<objective>
Sweep 6 mid-page section components from direct Tailwind opacity classes (`bg-white/{40,60,65}`) and hardcoded blurs (`backdrop-blur-{xl,2xl,3xl}`) to v9.0 tier-token arbitrary-value classes per PATTERNS.md per-line tables. Each card gets Tier 1 default; cards with hover state ramp to Tier 2 hover via `--glass-form-fill` (Archetype C); section frames go to Tier 0 (Archetype B); decorative inner icon chips with brand-color tints keep their fill but migrate `backdrop-blur-*` to `--glass-button-blur` for token consistency.

Purpose: This is the largest mechanical sweep in Phase 92 — 6 files share the same archetypes (B + C + decorative inner). Treating them as one plan keeps context lean (each file < 150 lines, 2–4 swap sites per file). Wave 3 because chrome (Wave 2) needed to land first to prevent visual conflicts with mid-section glass nesting.
Output: 6 modified TSX files; pnpm build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@next/src/app/globals.css
@next/src/components/sections/ProcessSection.tsx
@next/src/components/sections/ProblemSection.tsx
@next/src/components/sections/WhyUsSection.tsx
@next/src/components/sections/ClinicsSection.tsx
@next/src/components/sections/PlatformSection.tsx
@next/src/components/sections/ReviewsSection.tsx

<interfaces>
<!-- Archetypes: B (Tier 0 frame), C (Tier 1 hover-card → Tier 2 via --glass-form-fill) -->
<!-- Brand-color decorative chips (advantage icons, problem icons): KEEP brand-color fill (`${color}` data-driven), MIGRATE blur to var(--glass-button-blur) for token consistency. NOT anti-pattern #5 — these are brand-color icon backgrounds inside cards, not green-tint-on-card surfaces. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessSection.tsx + ProblemSection.tsx — Tier 1 cards + decorative inner chips</name>
  <files>next/src/components/sections/ProcessSection.tsx, next/src/components/sections/ProblemSection.tsx</files>
  <read_first>
    - next/src/components/sections/ProcessSection.tsx (FULL FILE — step card at line 107, inner icon chip at line 117)
    - next/src/components/sections/ProblemSection.tsx (FULL FILE — problem card at line 102, inner icon chip at line 105)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-04 → ProcessSection / ProblemSection tables)
  </read_first>
  <action>
    **ProcessSection.tsx:**

    1. **Line 107 (step card — Archetype C)**:
       - BEFORE: `group relative flex h-full flex-col rounded-[2rem] border border-glass-border bg-white/65 p-6 shadow-glass backdrop-blur-2xl transition-[...] duration-300 hover:border-glass-border-strong hover:shadow-glass-lg sm:p-7`
       - AFTER:  `group relative flex h-full flex-col rounded-[2rem] border border-glass-border bg-[var(--glass-card-fill)] p-6 shadow-glass backdrop-blur-[var(--glass-card-blur)] transition-[...] duration-300 hover:border-glass-border-strong hover:bg-[var(--glass-form-fill)] hover:shadow-glass-lg sm:p-7`
       - **ADD `hover:bg-[var(--glass-form-fill)]`** to ramp Tier 1 → Tier 2 on hover (parity with ServicesGrid pattern).

    2. **Line 117 (inner icon chip)**:
       - BEFORE: `flex h-11 w-11 items-center justify-center rounded-xl border border-glass-border bg-white/55 text-mu-text-700 backdrop-blur-md`
       - AFTER:  `flex h-11 w-11 items-center justify-center rounded-xl border border-glass-border bg-[var(--glass-button-fill)] text-mu-text-700 backdrop-blur-[var(--glass-button-blur)]`

    **ProblemSection.tsx:**

    3. **Line 102 (problem card — Archetype C)**:
       - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:shadow-glass-lg transition-all duration-500 h-full flex flex-col overflow-hidden p-3`
       - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:bg-[var(--glass-form-fill)] hover:shadow-glass-lg transition-all duration-500 h-full flex flex-col overflow-hidden p-3`
       - **ADD `hover:bg-[var(--glass-form-fill)]`** for Tier 2 hover ramp.

    4. **Line 105 (inner icon chip — brand-color preserved)**:
       - BEFORE: `w-14 h-14 ${card.iconBg} backdrop-blur-xl rounded-2xl ...` (where `${card.iconBg}` is brand-color tint like `bg-mu-blue/10`)
       - AFTER:  `w-14 h-14 ${card.iconBg} backdrop-blur-[var(--glass-button-blur)] rounded-2xl ...`
       - **PRESERVE `${card.iconBg}`** — brand-color tint from data, NOT an anti-pattern #5 violation. Only migrate the hardcoded `backdrop-blur-xl` to token form.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - ProcessSection: `grep -c 'bg-white/65\|bg-white/55' next/src/components/sections/ProcessSection.tsx` returns 0
    - ProcessSection: `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/ProcessSection.tsx` returns ≥1
    - ProcessSection: `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/ProcessSection.tsx` returns ≥1 (inner chip)
    - ProcessSection: `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/ProcessSection.tsx` returns ≥1
    - ProcessSection: `grep -c 'backdrop-blur-2xl\|backdrop-blur-md' next/src/components/sections/ProcessSection.tsx` returns 0
    - ProblemSection: `grep -c 'bg-white/60' next/src/components/sections/ProblemSection.tsx` returns 0
    - ProblemSection: `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/ProblemSection.tsx` returns ≥1
    - ProblemSection: `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/ProblemSection.tsx` returns ≥1
    - ProblemSection: `grep -c 'backdrop-blur-2xl\|backdrop-blur-xl' next/src/components/sections/ProblemSection.tsx` returns 0
    - ProblemSection: `grep -c '\${card.iconBg}' next/src/components/sections/ProblemSection.tsx` returns ≥1 (brand-color preserved)
  </acceptance_criteria>
  <done>
    ProcessSection step cards Tier 1 → Tier 2 hover with Tier 3 inner chip; ProblemSection cards Tier 1 → Tier 2 hover with brand-color inner chip preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: WhyUsSection.tsx — section pill (Tier 0), advantage icon chips (brand-color preserved, blur tokenized), image frames (Tier 0), stat card (Tier 1)</name>
  <files>next/src/components/sections/WhyUsSection.tsx</files>
  <read_first>
    - next/src/components/sections/WhyUsSection.tsx (FULL FILE — section pill at line 13, advantage chips at lines 28/45/62/79, image frames at lines 99/102/107, stat card at line 110)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-05 → WhyUsSection.tsx table)
  </read_first>
  <action>
    Per PATTERNS.md §Plan 92-05 → WhyUsSection.tsx table:

    1. **Line 13 (section pill — Archetype B)**:
       - BEFORE: `inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-glass-border px-5 py-2.5 rounded-full shadow-sm shadow-glass-inner mb-6`
       - AFTER:  `inline-flex items-center gap-2 bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] border border-glass-border px-5 py-2.5 rounded-full shadow-sm shadow-glass-inner mb-6`

    2. **Lines 28, 45, 62, 79 (advantage icon chips — decorative, brand-color preserved)**:
       - Pattern: `w-16 h-16 ${color} backdrop-blur-xl rounded-[1.5rem] flex ... shadow-glass-sm border border-glass-border ...` where `${color}` is one of `bg-mu-blue/10`, `bg-mu-accent-teal-bg`, `bg-mu-accent-orange-bg`, `bg-mu-green-50`.
       - AFTER: `w-16 h-16 ${color} backdrop-blur-[var(--glass-button-blur)] rounded-[1.5rem] flex ... shadow-glass-sm border border-glass-border ...`
       - PRESERVE all 4 brand-color `${color}` interpolations — anti-pattern #5 NOT triggered (brand-color icon chips inside section, not green-tint-on-card surfaces).

    3. **Lines 99, 102, 107 (image frames — Archetype B)**:
       - BEFORE: `h-{64,80} rounded-[3rem] overflow-hidden shadow-glass-lg border-[6px] border-white/50 backdrop-blur-2xl bg-white/20`
       - AFTER:  `h-{64,80} rounded-[3rem] overflow-hidden shadow-glass-lg border-[6px] border-white/50 backdrop-blur-[var(--glass-section-blur)] bg-[var(--glass-section-fill)]`

    4. **Line 110 (stat card — Tier 1, no hover)**:
       - BEFORE: `h-64 rounded-[3rem] overflow-hidden shadow-glass-lg border border-white/60 bg-white/40 backdrop-blur-2xl p-8 flex flex-col justify-center shadow-glass-inner`
       - AFTER:  `h-64 rounded-[3rem] overflow-hidden shadow-glass-lg border border-white/60 bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] p-8 flex flex-col justify-center shadow-glass-inner`
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/40\|bg-white/20' next/src/components/sections/WhyUsSection.tsx` returns 0
    - `grep -c 'backdrop-blur-xl\|backdrop-blur-2xl' next/src/components/sections/WhyUsSection.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/WhyUsSection.tsx` returns ≥4 (pill + 3 image frames)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/WhyUsSection.tsx` returns ≥1 (stat card)
    - `grep -c 'backdrop-blur-\[var(--glass-section-blur)\]' next/src/components/sections/WhyUsSection.tsx` returns ≥4
    - `grep -c 'backdrop-blur-\[var(--glass-button-blur)\]' next/src/components/sections/WhyUsSection.tsx` returns ≥4 (4 advantage chips)
    - Brand-color tints preserved: `grep -c 'bg-mu-blue/10\|bg-mu-accent-teal-bg\|bg-mu-accent-orange-bg\|bg-mu-green-50' next/src/components/sections/WhyUsSection.tsx` returns ≥4
  </acceptance_criteria>
  <done>
    WhyUsSection swept across 4 archetypes (B + decorative + B + Tier 1) with brand-color preservation.
  </done>
</task>

<task type="auto">
  <name>Task 3a: ClinicsSection.tsx + PlatformSection.tsx — Tier 1 cards / Tier 0 single panel</name>
  <files>next/src/components/sections/ClinicsSection.tsx, next/src/components/sections/PlatformSection.tsx</files>
  <read_first>
    - next/src/components/sections/ClinicsSection.tsx (country card at line 130)
    - next/src/components/sections/PlatformSection.tsx (single panel at line 15)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-05 → ClinicsSection / PlatformSection tables)
  </read_first>
  <action>
    **ClinicsSection.tsx:**

    1. **Line 130 (country card — Archetype C)**:
       - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] shadow-glass border border-glass-border p-8 hover:shadow-glass-lg hover:border-glass-border-strong transition-all duration-300`
       - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] shadow-glass border border-glass-border p-8 hover:bg-[var(--glass-form-fill)] hover:shadow-glass-lg hover:border-glass-border-strong transition-all duration-300`
       - **ADD `hover:bg-[var(--glass-form-fill)]`** for Tier 2 hover ramp (visual parity with ServicesGrid).

    **PlatformSection.tsx:**

    2. **Line 15 (single panel — Archetype B)**:
       - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[3rem] p-8 md:p-12 border border-glass-border shadow-glass max-w-3xl mx-auto`
       - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[3rem] p-8 md:p-12 border border-glass-border shadow-glass max-w-3xl mx-auto`

    Anti-pattern enforcement: both files consume tier tokens — anti-pattern #4 / #12 compliant.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - ClinicsSection: `grep -c 'bg-white/60' next/src/components/sections/ClinicsSection.tsx` returns 0
    - ClinicsSection: `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/ClinicsSection.tsx` returns ≥1
    - ClinicsSection: `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/ClinicsSection.tsx` returns ≥1
    - ClinicsSection: `grep -c 'backdrop-blur-2xl' next/src/components/sections/ClinicsSection.tsx` returns 0
    - PlatformSection: `grep -c 'bg-white/60' next/src/components/sections/PlatformSection.tsx` returns 0
    - PlatformSection: `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/PlatformSection.tsx` returns ≥1
    - PlatformSection: `grep -c 'backdrop-blur-2xl' next/src/components/sections/PlatformSection.tsx` returns 0
  </acceptance_criteria>
  <done>
    ClinicsSection cards at Tier 1 → Tier 2 hover; PlatformSection single panel at Tier 0.
  </done>
</task>

<task type="auto">
  <name>Task 3b: ReviewsSection.tsx — Tier 1 review cards (Tier 2 hover)</name>
  <files>next/src/components/sections/ReviewsSection.tsx</files>
  <read_first>
    - next/src/components/sections/ReviewsSection.tsx (review card at line 77)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-05 → ReviewsSection table)
  </read_first>
  <action>
    **ReviewsSection.tsx:**

    1. **Line 77 (review card — Archetype C)**:
       - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border p-8 hover:shadow-glass-lg hover:border-glass-border-strong transition-all duration-300`
       - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[3rem] shadow-glass border border-glass-border p-8 hover:bg-[var(--glass-form-fill)] hover:shadow-glass-lg hover:border-glass-border-strong transition-all duration-300`
       - **ADD `hover:bg-[var(--glass-form-fill)]`** for Tier 2 hover ramp.

    Anti-pattern enforcement: all token consumption clamped; #4 / #12 compliant.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - ReviewsSection: `grep -c 'bg-white/60' next/src/components/sections/ReviewsSection.tsx` returns 0
    - ReviewsSection: `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/ReviewsSection.tsx` returns ≥1
    - ReviewsSection: `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/ReviewsSection.tsx` returns ≥1
    - ReviewsSection: `grep -c 'backdrop-blur-2xl' next/src/components/sections/ReviewsSection.tsx` returns 0
    - Russian copy / `&nbsp;` preservation (baseline counts must hold post-sweep — Russian subject+verb binding + orphan prevention per project memory):
      - `grep -c '&nbsp;' next/src/components/sections/ProcessSection.tsx` returns 13 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/sections/ProblemSection.tsx` returns 0 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/sections/WhyUsSection.tsx` returns 0 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/sections/ClinicsSection.tsx` returns 0 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/sections/PlatformSection.tsx` returns 0 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/sections/ReviewsSection.tsx` returns 0 (unchanged from pre-sweep baseline)
  </acceptance_criteria>
  <done>
    ReviewsSection cards at Tier 1 → Tier 2 hover.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mid-section glass surfaces ↔ live blob behind them | Glass must remain WCAG-readable at every blob heat level |
| Brand-color decorative chips ↔ anti-pattern #5 (no green-tint-on-card) | Brand-color tints in data are sanctioned; flat green tints on card backgrounds are not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05-01 | Denial of service | Mobile blur >12px on 6 mid-sections (compositing cost on 45+ audience devices) | mitigate | All sweeps consume `--glass-{tier}-blur` tokens; clamp ≤12px on mobile (Phase 90 frozen) |
| T-92-05-02 | Tampering | WhyUsSection brand-color advantage chips accidentally swept to neutral white-glass (loses brand identity) | mitigate | Acceptance: brand-color grep ≥4 |
| T-92-05-03 | Tampering | Anti-pattern #5 (statically painted green tint on card surface) regression introduced | accept | No-op: heat-leak gradient is blob-driven (gated by `var(--blob-heat)`); brand-color icon chips are inside cards, not card surfaces themselves. Sanctioned per PATTERNS.md cross-cutting rule. |
| T-92-05-04 | Denial of service | ≤2 glass per viewport violated when 6 mid-sections stack on scroll | mitigate | Each section is a single Tier-0 frame OR cards of Tier 1; viewports never show >2 glass siblings simultaneously (sanctioned exception per RESEARCH §Anti-pattern #13) |
</threat_model>

<verification>
1. `pnpm --dir next build` exits 0.
2. All 3 tasks' acceptance criteria pass.
3. Manual: scroll through `/`, observe each mid-section card — Tier 1 default, Tier 2 hover (where applicable).
4. Manual DevTools Recipe 4: 375px viewport — every mid-section card resolves to `blur(12px)`.
5. Manual DevTools Layers panel: at any scroll position, count glass-tier siblings ≤2 (cards count as siblings of each other per Phase 82 sanctioned exception).
</verification>

<success_criteria>
- 6 mid-section files swept to v9.0 tiers
- Brand-color decorative chips preserved (no anti-pattern #5 regression)
- Hover ramps added where missing for visual parity
- All hardcoded `backdrop-blur-{xl,2xl,3xl}` removed
- `pnpm build` clean
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-05-SUMMARY.md`.
</output>
