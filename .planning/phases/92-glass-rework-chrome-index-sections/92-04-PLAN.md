---
phase: 92-glass-rework-chrome-index-sections
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - next/src/components/sections/HeroHub.tsx
  - next/src/components/sections/StatsBar.tsx
  - next/src/components/sections/ServicesGrid.tsx
autonomous: true
requirements:
  - GLASS-02
  - GLASS-03
  - GLASS-04
must_haves:
  truths:
    - "HeroHub pill badge + secondary CTA frame consume --glass-section-fill / --glass-section-blur"
    - "HeroHub credibility badge consumes --glass-card-fill / --glass-card-blur (Tier 1 per Decision A)"
    - "HeroHub secondary CTA hover ramps to Tier 1 (--glass-card-fill)"
    - "HeroHub primary CTA at line 48 (`from-mu-blue to-mu-accent-blue`) untouched (Archetype J)"
    - "HeroHub over-photo controls at lines 94/103/115 (`bg-mu-text-900/55`) untouched (Archetype H — NOT GLASS)"
    - "StatsBar mobile-1-wrapper / desktop-4-cards Phase 82 responsive nesting preserved"
    - "StatsBar mobile wrapper at Tier 0; desktop cards at Tier 1 → Tier 2 hover (via --glass-form-fill)"
    - "ServicesGrid section pill at Tier 1 (decorative); cards Tier 1 → Tier 2 hover"
    - "ServicesGrid nested price badge — backdrop-blur-md DROPPED (≤2 glass per viewport rule, anti-pattern #13)"
    - "pnpm build clean"
  artifacts:
    - path: "next/src/components/sections/HeroHub.tsx"
      provides: "Tier 0 hero frame + Tier 1 credibility badge; opaque CTA + over-photo controls untouched"
    - path: "next/src/components/sections/StatsBar.tsx"
      provides: "Responsive Tier 0 mobile wrapper / Tier 1 desktop cards"
    - path: "next/src/components/sections/ServicesGrid.tsx"
      provides: "Tier 1 cards with Tier 2 hover; ≤2 glass per viewport via dropped nested-badge blur"
  key_links:
    - from: "HeroHub / StatsBar / ServicesGrid"
      to: "globals.css :root --glass-{section,card}-{fill,blur}"
      via: "Tailwind arbitrary-value classes"
      pattern: "bg-\\[var\\(--glass-(section|card|form)-fill\\)\\]"
---

<objective>
Sweep `HeroHub`, `StatsBar`, `ServicesGrid` from direct Tailwind opacity classes to v9.0 tier-token arbitrary-value classes per PATTERNS.md per-line tables. Preserve:
- HeroHub primary CTA (Archetype J)
- HeroHub over-photo controls (Archetype H — NOT GLASS, dark surface over photo)
- StatsBar Phase 82 responsive-glass-nesting (mobile 1 wrapper / desktop 4 cards switch via `sm:` Tailwind variant)
- ServicesGrid card link semantics + Phase 82 sanctioned ≤2 glass exception (4 cards as siblings of each other, not of an enclosing glass)

Address ServicesGrid nested price-badge nesting (anti-pattern #13 risk): drop the inner `backdrop-blur-md` to collapse 2-layer nesting to 1.
Output: 3 modified TSX files; pnpm build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md
@DESIGN.md
@CLAUDE.md
@next/src/app/globals.css
@next/src/components/sections/HeroHub.tsx
@next/src/components/sections/StatsBar.tsx
@next/src/components/sections/ServicesGrid.tsx

<interfaces>
<!-- Tier-token arbitrary-value class forms (Phase 90 frozen) -->
- Tier 0 frame: `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)]`
- Tier 1 card:  `bg-[var(--glass-card-fill)]    backdrop-blur-[var(--glass-card-blur)]`
- Tier 2 hover: `hover:bg-[var(--glass-form-fill)]` (consumes form-fill token = 0.14/0.18 — used as the "Tier 2 hover" surface per PATTERNS.md Archetype C)
- Tier 3 button:`bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)]`

<!-- Archetypes used in this plan: B (Tier 0 frame), C (Tier 1 hover-card), E (responsive-glass-nesting), H (preserve over-photo controls), J (preserve CTA) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeroHub.tsx — pill (Tier 0), secondary CTA frame (Tier 0 → Tier 1 hover), credibility badge (Tier 1); preserve primary CTA + over-photo controls</name>
  <files>next/src/components/sections/HeroHub.tsx</files>
  <read_first>
    - next/src/components/sections/HeroHub.tsx (FULL FILE — pill at line 15, primary CTA at line 48, secondary CTA at line 56, over-photo controls at lines 94/103/115, credibility badge at line 139)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-03 → HeroHub.tsx table; cross-cutting "CTA opaque-forever invariant" verification grep; Archetype H "Over-photo controls" — preserve as-is)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md (Decision A row 2: HeroHub pill = Tier 0; credibility badge = Tier 0/1 — UI-SPEC says Tier 1)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (HeroHub.tsx:48 CTA invariant)
  </read_first>
  <action>
    Per PATTERNS.md §Plan 92-03 → HeroHub.tsx table:

    1. **Line 15 (pill badge — Archetype B)**:
       - BEFORE: `mb-5 inline-flex max-w-full items-center gap-2 rounded-full border border-glass-border bg-white/40 px-4 py-2.5 shadow-glass-sm backdrop-blur-[20px] sm:px-5`
       - AFTER:  `mb-5 inline-flex max-w-full items-center gap-2 rounded-full border border-glass-border bg-[var(--glass-section-fill)] px-4 py-2.5 shadow-glass-sm backdrop-blur-[var(--glass-section-blur)] sm:px-5`

    2. **Line 48 (primary CTA — Archetype J)**:
       - `bg-gradient-to-r from-mu-blue to-mu-accent-blue ...` — **NO CHANGE — opaque-forever**.
       - VERIFY: the className string contains zero `backdrop-*` after this plan ships.

    3. **Line 56 (secondary CTA glass — Archetype B with hover ramp)**:
       - BEFORE: `... border border-glass-border bg-white/50 ... shadow-glass backdrop-blur-[20px] transition-[...] duration-200 hover:bg-white/60 sm:w-auto`
       - AFTER:  `... border border-glass-border bg-[var(--glass-section-fill)] ... shadow-glass backdrop-blur-[var(--glass-section-blur)] transition-[...] duration-200 hover:bg-[var(--glass-card-fill)] sm:w-auto`
       - Hover ramps Tier 0 → Tier 1 per UI-SPEC.

    4. **Lines 94, 103, 115 (over-photo controls — Archetype H, NOT GLASS)**:
       - `... bg-mu-text-900/55 px-... backdrop-blur-md ...` — **NO CHANGE — dark surface over photo, not a glass-tier surface** (Decision A row 3, PATTERNS.md Archetype H).

    5. **Line 139 (credibility badge — Decision A: Tier 1)**:
       - BEFORE: `... border border-glass-border-strong bg-white/75 p-3 shadow-glass backdrop-blur-[40px] ...`
       - AFTER:  `... border border-glass-border-strong bg-[var(--glass-card-fill)] p-3 shadow-glass backdrop-blur-[var(--glass-card-blur)] ...`

    Preserve all other classes verbatim. Do NOT add `backdrop-*` to any `<svg>`, icon, or text element that doesn't already carry it.

    Anti-pattern enforcement: verify HeroHub primary CTA className string has no `backdrop-*` (`grep` in acceptance).
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/40\|bg-white/50\|bg-white/75' next/src/components/sections/HeroHub.tsx` returns 0
    - `grep -c 'backdrop-blur-\[20px\]\|backdrop-blur-\[40px\]' next/src/components/sections/HeroHub.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/HeroHub.tsx` returns ≥2 (pill + secondary CTA frame)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/HeroHub.tsx` returns ≥1 (credibility badge)
    - `grep -c 'hover:bg-\[var(--glass-card-fill)\]' next/src/components/sections/HeroHub.tsx` returns ≥1 (secondary CTA hover)
    - `grep -c 'bg-mu-text-900/55' next/src/components/sections/HeroHub.tsx` returns ≥3 (over-photo controls preserved at lines 94/103/115)
    - `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/HeroHub.tsx` returns ≥1 (primary CTA preserved)
    - CTA invariant: `grep 'from-mu-blue to-mu-accent-blue' next/src/components/sections/HeroHub.tsx | grep -c 'backdrop'` returns 0
  </acceptance_criteria>
  <done>
    HeroHub frame at Tier 0 + Tier 1 credibility badge; primary CTA opaque; over-photo controls preserved; hover ramps verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: StatsBar.tsx — Phase 82 responsive nesting preserved (mobile Tier 0 wrapper / desktop Tier 1 cards → Tier 2 hover)</name>
  <files>next/src/components/sections/StatsBar.tsx</files>
  <read_first>
    - next/src/components/sections/StatsBar.tsx (FULL FILE — mobile wrapper at line 49, desktop card per stat at line 56, icon chip at line 59)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-03 → StatsBar.tsx table; Archetype E "Responsive-glass-nesting" — full template)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md (Decision A rows 4–5: mobile wrapper Tier 0 / desktop cards Tier 1 → Tier 2 hover; Phase 82 nesting preserved)
  </read_first>
  <action>
    Per PATTERNS.md Archetype E full templates + §Plan 92-03 → StatsBar.tsx table:

    1. **Line 49 (mobile wrapper — Archetype E)**:
       - BEFORE: `rounded-[2rem] border border-glass-border bg-white/60 p-4 shadow-glass backdrop-blur-2xl sm:rounded-none sm:border-0 sm:bg-transparent sm:p-0 sm:shadow-none sm:backdrop-blur-none`
       - AFTER:  `rounded-[2rem] border border-glass-border bg-[var(--glass-section-fill)] p-4 shadow-glass backdrop-blur-[var(--glass-section-blur)] sm:rounded-none sm:border-0 sm:bg-transparent sm:p-0 sm:shadow-none sm:backdrop-blur-none`
       - **CRITICAL: preserve `sm:bg-transparent sm:backdrop-blur-none`** — this is the Phase 82 responsive switch that turns the wrapper transparent on desktop. NO CHANGE to the `sm:` half.

    2. **Line 56 (desktop card per stat — Archetype E + C)**:
       - BEFORE: `group relative ... sm:rounded-[2.5rem] sm:border sm:border-glass-border sm:bg-white/60 sm:p-7 sm:shadow-glass sm:backdrop-blur-2xl sm:transition-[...] sm:hover:bg-white/70 sm:hover:border-glass-border-strong sm:hover:shadow-glass-lg`
       - AFTER:  `group relative ... sm:rounded-[2.5rem] sm:border sm:border-glass-border sm:bg-[var(--glass-card-fill)] sm:p-7 sm:shadow-glass sm:backdrop-blur-[var(--glass-card-blur)] sm:transition-[...] sm:hover:bg-[var(--glass-form-fill)] sm:hover:border-glass-border-strong sm:hover:shadow-glass-lg`
       - Tier 1 default → Tier 2 hover (via `--glass-form-fill` token = 0.14 desktop / 0.18 mobile, sanctioned per PATTERNS.md Archetype C).

    3. **Line 59 (icon chip)**:
       - `${stat.iconBg} ${stat.iconText}` — **NO CHANGE** (color-tinted decorative chips already use brand-color tokens like `bg-mu-accent-blue/12`; not glass-tier).
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/60' next/src/components/sections/StatsBar.tsx` returns 0
    - `grep -c 'sm:bg-white/60\|sm:hover:bg-white/70' next/src/components/sections/StatsBar.tsx` returns 0
    - `grep -c 'backdrop-blur-2xl' next/src/components/sections/StatsBar.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/StatsBar.tsx` returns ≥1 (mobile wrapper)
    - `grep -c 'sm:bg-\[var(--glass-card-fill)\]' next/src/components/sections/StatsBar.tsx` returns ≥1 (desktop card)
    - `grep -c 'sm:hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/StatsBar.tsx` returns ≥1 (desktop card hover)
    - **Phase 82 responsive switch preserved**: `grep -c 'sm:bg-transparent' next/src/components/sections/StatsBar.tsx` returns ≥1
    - **Phase 82 responsive switch preserved**: `grep -c 'sm:backdrop-blur-none' next/src/components/sections/StatsBar.tsx` returns ≥1
  </acceptance_criteria>
  <done>
    StatsBar mobile wrapper Tier 0 / desktop cards Tier 1 → Tier 2 hover; Phase 82 responsive nesting preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 3: ServicesGrid.tsx — Tier 1 cards (Tier 2 hover); section pill Tier 1 decorative; nested price badge — drop blur (≤2 glass per viewport)</name>
  <files>next/src/components/sections/ServicesGrid.tsx</files>
  <read_first>
    - next/src/components/sections/ServicesGrid.tsx (FULL FILE — section pill at line 80, card at line 104, nested price badge at line 115)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-04 → ServicesGrid.tsx table; cross-cutting "≤2 glass siblings per viewport rule")
    - .planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md (Decision A row: ServicesGrid cards Tier 1 → Tier 2 hover)
  </read_first>
  <action>
    Per PATTERNS.md §Plan 92-04 → ServicesGrid.tsx table:

    1. **Line 80 (section pill — Archetype B)**:
       - BEFORE: `... border border-glass-border bg-white/40 px-5 py-2.5 shadow-glass-inner backdrop-blur-xl`
       - AFTER:  `... border border-glass-border bg-[var(--glass-section-fill)] px-5 py-2.5 shadow-glass-inner backdrop-blur-[var(--glass-section-blur)]`

    2. **Line 104 (card — Archetype C)**:
       - BEFORE: `group flex h-full flex-col rounded-[2rem] border border-glass-border bg-white/60 p-6 shadow-glass backdrop-blur-2xl transition-[...] duration-300 hover:-translate-y-0.5 hover:border-glass-border-strong hover:bg-white/70 hover:shadow-glass-lg sm:p-7`
       - AFTER:  `group flex h-full flex-col rounded-[2rem] border border-glass-border bg-[var(--glass-card-fill)] p-6 shadow-glass backdrop-blur-[var(--glass-card-blur)] transition-[...] duration-300 hover:-translate-y-0.5 hover:border-glass-border-strong hover:bg-[var(--glass-form-fill)] hover:shadow-glass-lg sm:p-7`
       - Tier 1 default → Tier 2 hover via `--glass-form-fill` token (consistent with PATTERNS.md Archetype C).

    3. **Line 115 (nested price badge — anti-pattern #13 ≤2 glass per viewport)**:
       - BEFORE: `... border border-glass-border bg-white/50 px-3 py-1 shadow-sm backdrop-blur-md`
       - AFTER (Option B per PATTERNS.md recommendation): `... border border-glass-border bg-[var(--glass-button-fill)] px-3 py-1 shadow-sm`
       - **DROP `backdrop-blur-md` entirely**. Decorative tag pill inside a Tier-1 card does NOT need its own blur — collapses nesting from 2 glass layers (card backdrop + badge backdrop) to 1, satisfying anti-pattern #13.
       - Keep `bg-[var(--glass-button-fill)]` as cosmetic fill so the badge still reads as a chip; the parent card's `backdrop-filter` provides the optical glass effect.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/40\|bg-white/50\|bg-white/60\|bg-white/70' next/src/components/sections/ServicesGrid.tsx` returns 0
    - `grep -c 'backdrop-blur-xl\|backdrop-blur-2xl\|backdrop-blur-md' next/src/components/sections/ServicesGrid.tsx` returns 0 (all hardcoded blurs removed; nested badge `backdrop-blur-md` DROPPED)
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/ServicesGrid.tsx` returns ≥1 (section pill)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/ServicesGrid.tsx` returns ≥1 (card default)
    - `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/ServicesGrid.tsx` returns ≥1 (card hover)
    - `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/ServicesGrid.tsx` returns ≥1 (nested price badge)
    - `grep -c 'backdrop-blur-\[var(--glass-section-blur)\]\|backdrop-blur-\[var(--glass-card-blur)\]' next/src/components/sections/ServicesGrid.tsx` returns ≥2 (pill + card)
    - Card link semantics intact: `grep -c '<Link' next/src/components/sections/ServicesGrid.tsx` returns ≥4 (one per service card; SVC-03 invariant)
  </acceptance_criteria>
  <done>
    ServicesGrid section pill + 4 cards consume v9.0 tier tokens; hover ramps verified; nested badge nesting collapsed via dropped `backdrop-blur-md` (anti-pattern #13 mitigated).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hero CTA ↔ live blob behind it | CTA must remain visually opaque at every blob position |
| StatsBar responsive switch ↔ Tailwind `sm:` variant | Phase 82 mobile-1-wrapper / desktop-4-cards contract preserved |
| ServicesGrid card ↔ nested price badge | ≤2 glass per viewport (anti-pattern #13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04-01 | Tampering | HeroHub primary CTA disappears into blob (PITFALL #3.1) | mitigate | CTA invariant grep gate in Task 1 acceptance |
| T-92-04-02 | Tampering | StatsBar Phase 82 responsive switch broken — mobile shows transparent wrapper or desktop shows opaque wrapper | mitigate | Acceptance: `sm:bg-transparent` and `sm:backdrop-blur-none` ≥1 grep gates |
| T-92-04-03 | Denial of service | ≤2 glass per viewport violated on ServicesGrid (compositing cost) — 4 cards + nested badges = 8+ blurs simultaneously visible | mitigate | Task 3 drops nested-badge `backdrop-blur-md`; collapses to 4 card-level blurs (sanctioned exception per RESEARCH §Anti-pattern #13) |
| T-92-04-04 | Denial of service | Mobile blur >12px (45+ audience battery) | mitigate | Token consumption clamp ≤12px on mobile |
| T-92-04-05 | Tampering | HeroHub over-photo controls (lines 94/103/115) accidentally swept to glass-tier (Archetype H violation — would over-saturate the photo) | mitigate | Acceptance: `bg-mu-text-900/55` grep ≥3 |
</threat_model>

<verification>
1. `pnpm --dir next build` exits 0.
2. All 3 tasks' acceptance criteria pass.
3. Manual: open `/`, scroll-test on 375px and 1440px viewports — Phase 82 StatsBar responsive nesting visually intact.
4. Manual DevTools Recipe 4 (mobile blur cap): each surface resolves to `blur(12px)` on 375px viewport.
5. Manual DevTools Layers panel (anti-pattern #13): scroll over ServicesGrid; card area shows ≤2 transparent layers stacked simultaneously.
</verification>

<success_criteria>
- HeroHub, StatsBar, ServicesGrid swept to v9.0 tiers
- HeroHub primary CTA + over-photo controls preserved
- StatsBar Phase 82 responsive switch preserved
- ServicesGrid card link semantics + 4-card sanctioned exception preserved
- Anti-pattern #13 nested-badge violation retired (blur dropped)
- `pnpm build` clean
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-04-SUMMARY.md`.
</output>
