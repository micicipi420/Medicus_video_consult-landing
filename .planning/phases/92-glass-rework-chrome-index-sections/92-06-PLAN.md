---
phase: 92-glass-rework-chrome-index-sections
plan: 06
type: execute
wave: 3
depends_on:
  - 03
  - 04
files_modified:
  - next/src/components/sections/FAQSection.tsx
  - next/src/components/sections/FinalCTA.tsx
autonomous: true
requirements:
  - GLASS-06
  - GLASS-08
must_haves:
  truths:
    - "FAQSection closed item wrapper at Tier 1 (--glass-card-fill / --glass-card-blur)"
    - "FAQSection button hover ramps to Tier 2 (--glass-form-fill)"
    - "FAQSection accordion `transition-[max-height]` preserved verbatim"
    - "FAQSection open-state Tier 2 ramp delivered via hover-on-button mechanism (documented as deliberate choice in SUMMARY)"
    - "FinalCTA frame at Tier 0 (--glass-section-fill / --glass-section-blur)"
    - "FinalCTA primary CTA at line 26 (`from-mu-blue to-mu-accent-blue`) untouched (Archetype J)"
    - "FinalCTA phone CTA at line 33 at Tier 3 (--glass-button-fill) → Tier 2 hover (--glass-form-fill)"
    - "FinalCTA mix-blend-multiply violation deferred to Plan 92-08 (NOT touched in this plan)"
    - "pnpm build clean"
  artifacts:
    - path: "next/src/components/sections/FAQSection.tsx"
      provides: "Tier 1 closed accordion items with Tier 2 hover ramp; smooth-anim transition preserved"
    - path: "next/src/components/sections/FinalCTA.tsx"
      provides: "Tier 0 frame + Tier 3 phone CTA glass; mix-blend-multiply violation flagged for 92-08"
  key_links:
    - from: "FAQSection.tsx"
      to: "globals.css --glass-card-{fill,blur} / --glass-form-fill"
      via: "Tailwind arbitrary-value classes; aria-expanded toggle drives max-h transition"
      pattern: "bg-\\[var\\(--glass-card-fill\\)\\]"
    - from: "FinalCTA.tsx"
      to: "globals.css --glass-section-{fill,blur} / --glass-button-{fill,blur} / --glass-form-fill"
      via: "Tailwind arbitrary-value classes"
      pattern: "bg-\\[var\\(--glass-section-fill\\)\\]"
---

<objective>
Sweep FAQSection accordion (Archetype D — closed Tier 1 / open Tier 2 hover-ramp) and FinalCTA (Archetype B frame + Archetype J primary CTA + Tier 3 phone CTA glass). PRESERVE FAQSection's CSS-only smooth-anim accordion transition (Phase 71 era). PRESERVE FinalCTA primary CTA gradient (Archetype J). DEFER FinalCTA `mix-blend-multiply` decoration retirement to Plan 92-08 (Wave 4) — it is documented in 92-02-AUDIT.md and 92-08 carries the explicit retirement task.

Purpose: FAQ + FinalCTA sit on `/` directly above the form. Sweeping them in Wave 3 (parallel with form-safety prep in 92-07) keeps the form panel's tier choice (which depends on neighboring chrome glass intensity) decided by the time form-safety contrast measurement runs in 92-07.
Output: 2 modified TSX files; pnpm build clean; mix-blend-multiply still present at FinalCTA:14 (Plan 92-08 retires it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md
@DESIGN.md
@CLAUDE.md
@next/src/components/sections/FAQSection.tsx
@next/src/components/sections/FinalCTA.tsx

<interfaces>
<!-- Archetype D — FAQ open-state-toggle (PATTERNS.md §Archetype D): -->
- closed wrapper: `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-2xl border border-glass-border shadow-glass-sm overflow-hidden`
- button hover: `hover:bg-[var(--glass-form-fill)]` (Tier 2 hover ramp via form-fill token)
- max-height transition: `transition-[max-height] duration-300 ease-in-out` — PRESERVE verbatim
- Open-state Tier 2 ramp choice (PATTERNS.md says planner chooses): we use **option (a) — hover-ramp-while-open as the visual cue** (simplest; no Tailwind v4 aria-* uncertainty; no data-state attribute addition). Document in SUMMARY.md.

<!-- FinalCTA archetypes: B (Tier 0 frame), J (primary CTA never sweep), Tier 3 (phone CTA glass), I (mix-blend-multiply violation deferred to 92-08) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAQSection.tsx — closed Tier 1 wrapper, Tier 2 hover ramp; preserve max-height accordion transition</name>
  <files>next/src/components/sections/FAQSection.tsx</files>
  <read_first>
    - next/src/components/sections/FAQSection.tsx (FULL FILE — closed wrapper at line 115, button hover at line 119, max-height transition at line 141)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-06 → FAQSection.tsx table; Archetype D template)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md (Decision A row 8: closed Tier 1 / open Tier 2 with smooth-anim accordion preserved)
  </read_first>
  <action>
    Per PATTERNS.md §Plan 92-06 → FAQSection.tsx table:

    1. **Line 115 (closed item wrapper — Archetype D)**:
       - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-2xl border border-glass-border shadow-glass-sm overflow-hidden`
       - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-2xl border border-glass-border shadow-glass-sm overflow-hidden`

    2. **Line 119 (button hover — Tier 2 ramp via form-fill token)**:
       - BEFORE: `w-full text-left px-6 py-5 font-bold text-mu-text-900 flex items-center justify-between transition-colors hover:bg-white/80`
       - AFTER:  `w-full text-left px-6 py-5 font-bold text-mu-text-900 flex items-center justify-between transition-colors hover:bg-[var(--glass-form-fill)]`

    3. **Line 141 (max-height transition wrapper — Phase 71 accordion)**:
       - `overflow-hidden transition-[max-height] duration-300 ease-in-out ...` — **NO CHANGE — preserve smooth-anim accordion**.

    4. **Open-state Tier 2 fill (Decision A: open=Tier 2)** — PATTERNS.md gives 3 options. **Choice locked here: option (a)** — accept hover-ramp-while-open as the visual cue. Rationale: Tailwind v4 `aria-expanded:` arbitrary variant support is uncertain (verify-later cost), and adding `data-state="open"` plus a CSS rule introduces a new pattern that has to be re-applied across Phase 93 LeadFormSection. Hover-while-open is functionally indistinguishable for keyboard users (focus styling carries it) and visually adequate per UI-SPEC.

    PRESERVE all other classes (`<button>` `aria-expanded={isOpen}`, ChevronUp/Down icon swap, the answer-text `<div>` inside the max-h transition wrapper).
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/60\|hover:bg-white/80' next/src/components/sections/FAQSection.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/FAQSection.tsx` returns ≥1
    - `grep -c 'backdrop-blur-\[var(--glass-card-blur)\]' next/src/components/sections/FAQSection.tsx` returns ≥1
    - `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/FAQSection.tsx` returns ≥1
    - `grep -c 'backdrop-blur-2xl' next/src/components/sections/FAQSection.tsx` returns 0
    - `grep -c 'transition-\[max-height\]' next/src/components/sections/FAQSection.tsx` returns ≥1 (accordion preserved)
    - `grep -c 'aria-expanded' next/src/components/sections/FAQSection.tsx` returns ≥1 (a11y contract preserved)
  </acceptance_criteria>
  <done>
    FAQSection closed Tier 1 / button hover Tier 2; max-height accordion preserved; aria-expanded preserved; open-state choice (option a) documented in SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: FinalCTA.tsx — frame Tier 0, phone CTA Tier 3 → Tier 2 hover; primary CTA + mix-blend-multiply UNTOUCHED (deferred to 92-08)</name>
  <files>next/src/components/sections/FinalCTA.tsx</files>
  <read_first>
    - next/src/components/sections/FinalCTA.tsx (FULL FILE — frame at line 8, decorative blob with mix-blend-multiply at line 14, primary CTA at line 26, phone CTA at line 33, image gradient overlay at line 59)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-08 → FinalCTA.tsx table — note: PATTERNS originally bundled FinalCTA into 92-08; per orchestrator-locked decomposition this plan does the FinalCTA SWEEP in Wave 3, leaving only the mix-blend-multiply RETIREMENT for 92-08)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (FinalCTA.tsx:14 mix-blend-multiply violation flagged for 92-08; FinalCTA.tsx:26 CTA invariant)
  </read_first>
  <action>
    Per PATTERNS.md §Plan 92-08 → FinalCTA.tsx table (sweeping the non-mix-blend-multiply lines now; 92-08 owns the mix-blend-multiply retirement):

    1. **Line 8 (frame — Archetype B)**:
       - BEFORE: `bg-white/60 backdrop-blur-3xl rounded-[3.5rem] overflow-hidden relative shadow-glass-lg border border-glass-border-strong`
       - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[3.5rem] overflow-hidden relative shadow-glass-lg border border-glass-border-strong`

    2. **Line 14 (decorative blob with `mix-blend-multiply` — Archetype I, anti-pattern #8)**:
       - **NO CHANGE in this plan.** Plan 92-08 is the explicit owner of the retirement decision. Leaving it for 92-08 keeps the retirement traceable as a single Key Decision moment.
       - Document in SUMMARY.md: "FinalCTA:14 mix-blend-multiply still present; Plan 92-08 retires."

    3. **Line 26 (primary CTA gradient — Archetype J)**:
       - `bg-gradient-to-r from-mu-blue to-mu-accent-blue ...` — **NO CHANGE — opaque-forever**.
       - VERIFY className contains zero `backdrop-*` after the swap.

    4. **Line 33 (phone CTA glass — Tier 3 → Tier 2 hover)**:
       - BEFORE: `w-full sm:w-auto bg-white/60 backdrop-blur-xl text-mu-text-900 px-8 py-4 rounded-3xl font-bold border border-glass-border hover:bg-white/80 transition-all flex items-center justify-center gap-2 shadow-glass-sm shadow-glass-inner-strong text-lg`
       - AFTER:  `w-full sm:w-auto bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] text-mu-text-900 px-8 py-4 rounded-3xl font-bold border border-glass-border hover:bg-[var(--glass-form-fill)] transition-all flex items-center justify-center gap-2 shadow-glass-sm shadow-glass-inner-strong text-lg`
       - Tier 3 default → Tier 2 hover via form-fill token.

    5. **Line 59 (image gradient overlay)**:
       - `absolute inset-0 bg-gradient-to-r from-white/60 to-transparent w-1/3` — **NO CHANGE** (image-overlay gradient, not glass surface).
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/60' next/src/components/sections/FinalCTA.tsx` returns ≤1 (line 59 image-overlay gradient may keep `bg-gradient-to-r from-white/60`; the frame and phone CTA fills are swept)
    - `grep -c 'backdrop-blur-3xl\|backdrop-blur-xl' next/src/components/sections/FinalCTA.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/FinalCTA.tsx` returns ≥1 (frame)
    - `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/FinalCTA.tsx` returns ≥1 (phone CTA)
    - `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/FinalCTA.tsx` returns ≥1
    - `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/FinalCTA.tsx` returns ≥1 (primary CTA preserved)
    - CTA invariant: `grep 'from-mu-blue to-mu-accent-blue' next/src/components/sections/FinalCTA.tsx | grep -c 'backdrop'` returns 0
    - **mix-blend-multiply STILL PRESENT** (deferred to 92-08): `grep -c 'mix-blend-multiply' next/src/components/sections/FinalCTA.tsx` returns ≥1
  </acceptance_criteria>
  <done>
    FinalCTA frame Tier 0 + phone CTA Tier 3; primary CTA preserved; mix-blend-multiply still present (Plan 92-08 retires).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FAQSection accordion ↔ aria-expanded keyboard handler | Open/close state must respond to keyboard + screen reader |
| FinalCTA primary CTA ↔ blob behind it | Final conversion moment — CTA must remain visually opaque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-06-01 | Tampering | FAQSection accordion `transition-[max-height]` accidentally swept (visual jank) | mitigate | Acceptance: `transition-[max-height]` grep ≥1 |
| T-92-06-02 | Tampering | FAQ `aria-expanded` removed (a11y regression — security-adjacent for KZ medical content) | mitigate | Acceptance: `aria-expanded` grep ≥1 |
| T-92-06-03 | Tampering | FinalCTA primary CTA paired with `backdrop-*` (CTA disappear-into-blob — PITFALL #3.1) | mitigate | CTA invariant negative-grep gate in Task 2 acceptance |
| T-92-06-04 | Denial of service | Mobile blur >12px on FAQ + FinalCTA (45+ audience battery) | mitigate | Token consumption clamps ≤12px on mobile |
| T-92-06-05 | Information disclosure | Anti-pattern #8 (`mix-blend-multiply` on FinalCTA decorative blob) still present after this plan | accept | Deferral is intentional — Plan 92-08 owns the retirement; 92-02-AUDIT.md documents the trail |
</threat_model>

<verification>
1. `pnpm --dir next build` exits 0.
2. Both tasks' acceptance criteria pass.
3. Manual: `/`, click each FAQ item — accordion smooth-opens; closed Tier 1, hover/open Tier 2.
4. Manual: scroll to FinalCTA — frame Tier 0, primary CTA fully opaque, phone CTA Tier 3.
5. Manual DevTools Recipe 4: 375px viewport — every swept surface resolves to `blur(12px)`.
</verification>

<success_criteria>
- FAQSection closed Tier 1 / hover Tier 2 / accordion preserved / aria-expanded preserved
- FinalCTA frame Tier 0 + phone CTA Tier 3 + primary CTA preserved
- mix-blend-multiply still present (Plan 92-08 retires)
- `pnpm build` clean
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-06-SUMMARY.md`. Document:
1. FAQSection open-state Tier 2 choice = "option (a) hover-ramp-while-open" with rationale.
2. FinalCTA mix-blend-multiply still present; 92-08 retires.
</output>
