---
phase: 92-glass-rework-chrome-index-sections
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - next/src/components/layout/HeaderClient.tsx
  - next/src/components/layout/MobileMenu.tsx
  - next/src/components/layout/StickyBar.tsx
  - next/src/components/layout/Footer.tsx
autonomous: true
requirements:
  - GLASS-01
  - GLASS-09
must_haves:
  truths:
    - "HeaderClient (default + scrolled) consumes --glass-section-fill / --glass-section-blur"
    - "MobileMenu burger + drawer consume --glass-section-fill / --glass-section-blur; HIG 44pt tap target (h-11 w-11) preserved; ESC dismissal preserved"
    - "MobileMenu CTA at line 94 (`from-mu-blue to-mu-accent-blue`) untouched"
    - "StickyBar wrapper consumes --glass-section-fill / --glass-section-blur"
    - "StickyBar CTA at line 58 untouched"
    - "Footer outer card consumes --glass-section-fill / --glass-section-blur"
    - "Footer inner contact icon chips consume --glass-button-fill / --glass-button-blur (Tier 3 inner)"
    - "Mobile blur on every chrome surface ≤12px (verified by token consumption — clamp ceiling)"
    - "pnpm build clean"
  artifacts:
    - path: "next/src/components/layout/HeaderClient.tsx"
      provides: "Tier 0 glass header (sticky, scroll-state-aware)"
    - path: "next/src/components/layout/MobileMenu.tsx"
      provides: "Tier 0 burger + drawer; opaque CTA preserved"
    - path: "next/src/components/layout/StickyBar.tsx"
      provides: "Tier 0 mobile sticky bar; opaque CTA preserved"
    - path: "next/src/components/layout/Footer.tsx"
      provides: "Tier 0 footer card with Tier 3 inner icon chips"
  key_links:
    - from: "HeaderClient.tsx / MobileMenu.tsx / StickyBar.tsx / Footer.tsx"
      to: "globals.css :root --glass-section-fill / --glass-section-blur / --glass-button-fill / --glass-button-blur"
      via: "Tailwind arbitrary-value classes bg-[var(...)] / backdrop-blur-[var(...)]"
      pattern: "bg-\\[var\\(--glass-section-fill\\)\\]"
---

<objective>
Sweep all 4 always-visible chrome components (HeaderClient, MobileMenu, StickyBar, Footer) from direct Tailwind opacity classes (`bg-white/{30,55,60,68}`) and hardcoded blurs (`backdrop-blur-{xl,2xl,3xl,[40px],[60px],[80px]}`) to v9.0 tier-token arbitrary-value classes. Preserve all interactive contracts (HIG 44pt tap target, ESC dismissal, scroll-state transition, sticky positioning, opaque CTA gradients).

Purpose: Chrome is visible on every page. Sweeping it first (Wave 2) unblocks per-section sweeps in Wave 2/3 — section components reference chrome heights and `≤2 glass per viewport` budgets. After this plan, mobile blur on every chrome surface is automatically capped at 12px via `--glass-{tier}-blur` clamp.
Output: 4 modified TSX files; pnpm build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md
@DESIGN.md
@CLAUDE.md
@next/src/app/globals.css
@next/src/styles/liquid-glass.css
@next/src/components/layout/HeaderClient.tsx
@next/src/components/layout/MobileMenu.tsx
@next/src/components/layout/StickyBar.tsx
@next/src/components/layout/Footer.tsx

<interfaces>
<!-- Tier-token resolution table — PATTERNS.md (Phase 90 frozen) -->
- `bg-[var(--glass-section-fill)]` → desktop 0.06 / mobile 0.10 fill
- `backdrop-blur-[var(--glass-section-blur)]` → desktop ≤24px / mobile ≤12px (clamp)
- `bg-[var(--glass-button-fill)]` → desktop 0.12 / mobile 0.16 fill (Tier 3)
- `backdrop-blur-[var(--glass-button-blur)]` → desktop ≤16px / mobile ≤12px (clamp)

<!-- Archetype A — Chrome (PATTERNS.md §Archetype A): -->
- BEFORE: `bg-white/{30..68}` + `backdrop-blur-{[40px]|[60px]|[80px]|2xl|3xl|xl}` + optional `backdrop-saturate-[150%..200%]`
- AFTER: `bg-[var(--glass-section-fill)]` + `backdrop-blur-[var(--glass-section-blur)]`. Saturate stays (cosmetic). Borders & shadow tokens stay.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeaderClient.tsx — Tier 0 sweep on default + scrolled states; preserve scroll-state transition contract</name>
  <files>next/src/components/layout/HeaderClient.tsx</files>
  <read_first>
    - next/src/components/layout/HeaderClient.tsx (FULL FILE)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-02 → HeaderClient table — exact line+before+after)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (CTA invariant baseline; Header.tsx render-status — note: this file is HeaderClient, not legacy Header)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md (§Component Inventory — HeaderClient row)
  </read_first>
  <action>
    Per PATTERNS.md §Plan 92-02 → HeaderClient table:

    1. **Line 17 (default state classes)** — locate via `grep -n 'bg-white/30' next/src/components/layout/HeaderClient.tsx`:
       - BEFORE: `bg-white/30 backdrop-blur-[40px] backdrop-saturate-[150%] py-5`
       - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] backdrop-saturate-[150%] py-5`

    2. **Line 19 (scrolled state — inside `cn()` conditional)** — locate via `grep -n 'bg-white/50' next/src/components/layout/HeaderClient.tsx`:
       - BEFORE: `'bg-white/50 backdrop-blur-[60px] backdrop-saturate-[180%] py-3'`
       - AFTER:  `'bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] backdrop-saturate-[180%] py-3'`

    3. **Line 12 (transition list — anti-pattern #6 review per Decision H)** — `'transition-[padding,background-color,box-shadow,backdrop-filter] duration-300 ...'`:
       - BOTH default and scrolled now consume the same `--glass-section-fill` and `--glass-section-blur` tokens, so neither `background-color` nor `backdrop-filter` actually changes between scroll states (only `backdrop-saturate` differs: 150% ↔ 180%, plus `py-{5,3}`).
       - **DROP `backdrop-filter` from the transition list** to remove the anti-pattern #6 risk: AFTER `'transition-[padding,background-color,box-shadow] duration-300 ...'`.
       - `background-color` may also be dropped (no longer animates), but optional — leaving it is harmless. Drop both for cleanliness: AFTER `'transition-[padding,box-shadow] duration-300 ...'`.

    4. **Line 13** (`border-[0.5px] border-white/50`) — NO CHANGE (cosmetic border, not a glass-tier surface).

    5. **Line 14** (`shadow-glass-header`) — NO CHANGE (token-based shadow stays).

    Preserve all other classes verbatim (`fixed top-0`, `z-50`, etc.). Do NOT add any `backdrop-*` to the wordmark, nav links, or any inner element that doesn't already carry it.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/30\|bg-white/50' next/src/components/layout/HeaderClient.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/layout/HeaderClient.tsx` returns 2 (default + scrolled)
    - `grep -c 'backdrop-blur-\[var(--glass-section-blur)\]' next/src/components/layout/HeaderClient.tsx` returns 2
    - `grep -c 'backdrop-blur-\[40px\]\|backdrop-blur-\[60px\]' next/src/components/layout/HeaderClient.tsx` returns 0
    - `grep -c 'transition-\[.*backdrop-filter' next/src/components/layout/HeaderClient.tsx` returns 0 (anti-pattern #6 retired)
    - `shadow-glass-header` still present (`grep -c 'shadow-glass-header' next/src/components/layout/HeaderClient.tsx` ≥1)
  </acceptance_criteria>
  <done>
    HeaderClient consumes Tier 0 tokens; scroll-state still toggles `backdrop-saturate` and padding; anti-pattern #6 retired.
  </done>
</task>

<task type="auto">
  <name>Task 2: MobileMenu.tsx — Tier 0 burger + drawer sweep; preserve 44pt tap target, ESC handler, opaque CTA gradient at line 94</name>
  <files>next/src/components/layout/MobileMenu.tsx</files>
  <read_first>
    - next/src/components/layout/MobileMenu.tsx (FULL FILE — note ESC handler around line 14; tap-target h-11 w-11 around line 38; drawer panel around line 52; CTA gradient around line 94)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-02 → MobileMenu table — line-by-line)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (MobileMenu.tsx:94 CTA invariant)
  </read_first>
  <action>
    Per PATTERNS.md §Plan 92-02 → MobileMenu table:

    1. **Line 38 (burger button)** — locate via `grep -n 'flex h-11 w-11' next/src/components/layout/MobileMenu.tsx`:
       - BEFORE: `flex h-11 w-11 ... border border-white/55 bg-white/55 ... shadow-glass-sm backdrop-blur-xl backdrop-saturate-[180%] transition-[transform,background-color,box-shadow] duration-200 active:scale-[0.96] lg:hidden`
       - AFTER:  `flex h-11 w-11 ... border border-white/55 bg-[var(--glass-section-fill)] ... shadow-glass-sm backdrop-blur-[var(--glass-section-blur)] backdrop-saturate-[180%] transition-[transform,background-color,box-shadow] duration-200 active:scale-[0.96] lg:hidden`
       - **PRESERVE `h-11 w-11`** (44pt HIG tap target — REQ GLASS-01).

    2. **Line 47 (overlay backdrop)** — `absolute inset-0 bg-mu-text-900/35 backdrop-blur-sm`:
       - **NO CHANGE** — dark dimmer, not glass-tier (PATTERNS.md §Anti-pattern #13 sanctioned exception 2).

    3. **Line 52 (drawer nav panel)**:
       - BEFORE: `... border-[0.5px] border-white/55 bg-white/68 shadow-glass-lg backdrop-blur-[80px] backdrop-saturate-[200%]`
       - AFTER:  `... border-[0.5px] border-white/55 bg-[var(--glass-section-fill)] shadow-glass-lg backdrop-blur-[var(--glass-section-blur)] backdrop-saturate-[200%]`

    4. **Lines 60, 69, 82 (nav-link hover backgrounds)** — `hover:bg-white/45`:
       - **NO CHANGE** by default. RATIONALE: these are interactive hover backgrounds on transparent nav strips, not glass-tier surfaces. Per PATTERNS.md, leaving them is sanctioned. (If executor judges visual regression on the new transparent drawer, optionally migrate to `hover:bg-[var(--glass-button-fill)]` and document in SUMMARY.md.)

    5. **Line 77 (divider)** — `h-[0.5px] bg-white/40 my-2`: NO CHANGE (decorative line).

    6. **Line 94 (CTA gradient)** — `bg-gradient-to-r from-mu-blue to-mu-accent-blue`: **NO CHANGE — opaque-forever (Archetype J)**. Per 92-02-AUDIT.md baseline. **Verify** className string contains NO `backdrop-*` after the swap.

    7. **Preserve ESC handler** at top of component (around line 14): `useEffect` with `keydown` listener calling `onClose`. NO CHANGE.

    8. **Preserve `aria-modal`, `aria-labelledby`, `role="dialog"`** on the drawer.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'bg-white/55\|bg-white/68' next/src/components/layout/MobileMenu.tsx` returns 0 (burger + drawer migrated)
    - `grep -c 'backdrop-blur-\[80px\]\|backdrop-blur-xl' next/src/components/layout/MobileMenu.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/layout/MobileMenu.tsx` returns ≥2 (burger + drawer)
    - `grep -c 'backdrop-blur-\[var(--glass-section-blur)\]' next/src/components/layout/MobileMenu.tsx` returns ≥2
    - `grep -c 'h-11 w-11' next/src/components/layout/MobileMenu.tsx` returns ≥1 (44pt tap target preserved)
    - `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/layout/MobileMenu.tsx` returns ≥1 (CTA preserved)
    - CTA invariant negative grep: `grep 'from-mu-blue to-mu-accent-blue' next/src/components/layout/MobileMenu.tsx | grep -c 'backdrop'` returns 0
    - ESC handler still present: `grep -c "key.*Escape\|'keydown'" next/src/components/layout/MobileMenu.tsx` ≥1
    - `bg-mu-text-900/35` (overlay) still present (NO CHANGE: `grep -c 'bg-mu-text-900/35' next/src/components/layout/MobileMenu.tsx` ≥1)
  </acceptance_criteria>
  <done>
    MobileMenu burger + drawer at Tier 0; tap target preserved; ESC handler preserved; CTA opaque; overlay dimmer untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: StickyBar.tsx + Footer.tsx — Tier 0 wrappers + Tier 3 inner Footer chips</name>
  <files>next/src/components/layout/StickyBar.tsx, next/src/components/layout/Footer.tsx</files>
  <read_first>
    - next/src/components/layout/StickyBar.tsx (FULL FILE — wrapper at line 44, CTA at line 58)
    - next/src/components/layout/Footer.tsx (FULL FILE — wrapper at line 18, contact icon chips at lines 89, 100)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (§Plan 92-02 → StickyBar + Footer tables)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (StickyBar.tsx:58 CTA invariant)
  </read_first>
  <action>
    **StickyBar.tsx:**

    1. **Line 44 (wrapper)** — locate via `grep -n 'fixed' next/src/components/layout/StickyBar.tsx`:
       - BEFORE: `fixed ... rounded-2xl border border-white/60 bg-white/68 p-3 shadow-glass-lg backdrop-blur-3xl transition-transform duration-300 lg:hidden ${isHidden ? 'translate-y-...' : ''}`
       - AFTER:  `fixed ... rounded-2xl border border-white/60 bg-[var(--glass-section-fill)] p-3 shadow-glass-lg backdrop-blur-[var(--glass-section-blur)] transition-transform duration-300 lg:hidden ${isHidden ? 'translate-y-...' : ''}`

    2. **Line 58 (CTA)** — `bg-gradient-to-r from-mu-blue to-mu-accent-blue`: **NO CHANGE — opaque-forever (Archetype J)**.

    **Footer.tsx:**

    3. **Line 18 (outer glass card wrapper)** — locate via `grep -n 'rounded-\[3rem\]' next/src/components/layout/Footer.tsx`:
       - BEFORE: `bg-white/60 backdrop-blur-3xl rounded-[3rem] p-12 border border-white/60 shadow-glass-lg`
       - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[3rem] p-12 border border-white/60 shadow-glass-lg`

    4. **Line 89 (phone icon chip)** — Tier 3 inner decorative chip:
       - BEFORE: `bg-white/60 backdrop-blur-md p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong`
       - AFTER:  `bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong`

    5. **Line 100 (mail icon chip)** — same pattern as phone chip:
       - BEFORE: `bg-white/60 backdrop-blur-md p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong`
       - AFTER:  `bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong`

    Preserve all other classes (`fixed`, `z-40`, `transition-transform`, etc.).

    Anti-pattern enforcement (Decision H):
    - #4 (fills > 0.16): Tier 0 = 0.06/0.10 ✓; Tier 3 = 0.12/0.16 ✓ (mobile 0.16 is exact ceiling, sanctioned)
    - #12 (mobile blur >12px): tokens clamp ✓
    - #13 (≤2 glass per viewport): StickyBar+Header on mobile = 2 sticky-chrome glass surfaces, sanctioned exception per RESEARCH §Anti-pattern table
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - StickyBar: `grep -c 'bg-white/68' next/src/components/layout/StickyBar.tsx` returns 0
    - StickyBar: `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/layout/StickyBar.tsx` returns ≥1
    - StickyBar: `grep -c 'backdrop-blur-3xl' next/src/components/layout/StickyBar.tsx` returns 0
    - StickyBar: `grep -c 'backdrop-blur-\[var(--glass-section-blur)\]' next/src/components/layout/StickyBar.tsx` returns ≥1
    - StickyBar CTA invariant: `grep 'from-mu-blue to-mu-accent-blue' next/src/components/layout/StickyBar.tsx | grep -c 'backdrop'` returns 0
    - Footer: `grep -c 'bg-white/60' next/src/components/layout/Footer.tsx` returns 0
    - Footer: `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/layout/Footer.tsx` returns ≥1
    - Footer: `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/layout/Footer.tsx` returns ≥2 (phone + mail chips)
    - Footer: `grep -c 'backdrop-blur-3xl\|backdrop-blur-md' next/src/components/layout/Footer.tsx` returns 0 (all swept)
    - Footer: `grep -c 'shadow-glass-lg\|shadow-glass-inner-strong' next/src/components/layout/Footer.tsx` ≥3 (token shadows preserved)
    - Russian copy / `&nbsp;` preservation (baseline counts must hold post-sweep — Russian subject+verb binding + orphan prevention per project memory):
      - `grep -c '&nbsp;' next/src/components/layout/HeaderClient.tsx` returns 0 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/layout/MobileMenu.tsx` returns 0 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/layout/StickyBar.tsx` returns 0 (unchanged from pre-sweep baseline)
      - `grep -c '&nbsp;' next/src/components/layout/Footer.tsx` returns 5 (unchanged from pre-sweep baseline)
  </acceptance_criteria>
  <done>
    StickyBar wrapper at Tier 0; Footer outer at Tier 0 with Tier 3 inner chips; both CTAs preserved opaque.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sticky chrome ↔ scroll-driven state mutation | HeaderClient toggles classes; transition contract preserved |
| MobileMenu drawer ↔ ESC keydown handler | Accessibility-critical contract (keyboard dismissal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03-01 | Denial of service | Mobile blur >12px on chrome (45+ audience battery / scroll perf threat) | mitigate | Token consumption: `--glass-section-blur` clamps to 12px on mobile (Phase 90 frozen). Acceptance: zero hardcoded `backdrop-blur-{[40px],[60px],[80px],3xl,2xl}` survive in chrome files. |
| T-92-03-02 | Tampering | CTA disappear-into-blob regression (PITFALL #3.1) on MobileMenu:94 / StickyBar:58 | mitigate | Negative-grep gates in acceptance criteria; 92-02-AUDIT.md baseline locked |
| T-92-03-03 | Denial of service | HIG 44pt tap target on MobileMenu burger lost during sweep (a11y regression — security-adjacent for KZ medical content) | mitigate | Acceptance: `h-11 w-11` grep returns ≥1 |
| T-92-03-04 | Elevation of privilege | ESC dismissal handler removed during sweep (modal escape lost) | mitigate | Acceptance: ESC handler grep ≥1 |
| T-92-03-05 | Denial of service | Anti-pattern #6 (animated backdrop-filter on HeaderClient transition) keeps compositing cost high | mitigate | Task 1 drops `backdrop-filter` from transition list |
</threat_model>

<verification>
1. `pnpm --dir next build` exits 0.
2. All 3 tasks' acceptance criteria pass.
3. Manual DevTools Recipe 1 (Chrome legibility at heat=0): force `static` blob via `window.__blobDebug.setMode?.('static')` after Phase 91 ships; Computed `background-color` resolves to `rgba(255,255,255,0.06)` desktop / `0.10` mobile on each chrome surface.
4. Manual DevTools Recipe 4 (Mobile blur cap): 375px viewport → Computed `backdrop-filter: blur(12px)` on each chrome surface.
5. Manual functional smoke: ESC closes MobileMenu; scroll changes HeaderClient padding; StickyBar appears on mobile only.
</verification>

<success_criteria>
- 4 chrome files swept to v9.0 tiers
- Mobile blur ≤12px verified by token consumption
- HIG 44pt tap target preserved
- ESC dismissal preserved
- Both CTAs (MobileMenu, StickyBar) untouched (Archetype J)
- HeaderClient anti-pattern #6 retired
- `pnpm build` clean
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-03-SUMMARY.md`. Note Header.tsx (legacy) handling per 92-02-AUDIT.md Section 3 — if RENDERED, document follow-up; if NOT RENDERED, document skip.
</output>
