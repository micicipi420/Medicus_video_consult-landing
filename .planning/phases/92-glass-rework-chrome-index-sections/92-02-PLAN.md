---
phase: 92-glass-rework-chrome-index-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - GLASS-02
  - GLASS-08
must_haves:
  truths:
    - "All 5 in-scope CTA call-sites (HeroHub:48, MobileMenu:94, StickyBar:58, ContactForm:247, FinalCTA:26) carry the brand gradient `from-mu-blue to-mu-accent-blue` and are NEVER paired with `backdrop-blur-*` or `backdrop-filter` in the same className"
    - "FinalCTA.tsx line 14 currently violates anti-pattern #8 (`mix-blend-multiply`); flagged for retirement in Plan 92-08"
    - "Header.tsx (legacy) rendered-status documented (rendered → Plan 92-03 sweep applies; not rendered → dead-code skip)"
    - "Audit report appended to Phase 92 STATE/notes for downstream wave gating"
  artifacts:
    - path: ".planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md"
      provides: "CTA invariant baseline + Header.tsx rendered-status + FinalCTA mix-blend-multiply violation flag"
      contains: "CTA invariant audit"
  key_links:
    - from: "92-02-AUDIT.md"
      to: "Plans 92-03..92-08 (every wave that touches a CTA-bearing component)"
      via: "documented baseline that downstream sweeps must NOT regress"
      pattern: "AUDIT.md referenced in 92-03 onwards"
---

<objective>
Audit (read-only) the CTA opaque-forever invariant and FinalCTA anti-pattern violations BEFORE any component sweep ships. Produce `92-02-AUDIT.md` documenting:

1. The 5 in-scope CTA call-sites (file:line + exact gradient string).
2. Whether `Header.tsx` (legacy) is rendered (determines whether Plan 92-03 includes it).
3. The current `mix-blend-multiply` violation on `FinalCTA.tsx:14` (retirement deferred to Plan 92-08, but baseline recorded here).

Purpose: Wave 2/3/4 sweeps must NOT regress CTA opacity. By recording the baseline now, every later plan's `<acceptance_criteria>` can grep against this baseline and fail loudly if a sweep accidentally adds `backdrop-blur-*` to a CTA className. Read-only — zero source files modified.
Output: New `92-02-AUDIT.md` in phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@DESIGN.md
@CLAUDE.md
@next/src/components/sections/HeroHub.tsx
@next/src/components/sections/ContactForm.tsx
@next/src/components/sections/FinalCTA.tsx
@next/src/components/layout/MobileMenu.tsx
@next/src/components/layout/StickyBar.tsx
@next/src/components/layout/Header.tsx

<interfaces>
<!-- CTA opaque-forever 7-component master list — DESIGN.md `## v9.0 Custom Rules` (Phase 90 frozen) -->
<!-- Phase 92 in-scope CTAs (Archetype J): HeroHub.tsx:48, MobileMenu.tsx:94, StickyBar.tsx:58, ContactForm.tsx:247, FinalCTA.tsx:26 -->
<!-- Out of scope (Phase 93): service/ServiceHero.tsx, consultations/ConsultationPricing.tsx, checkup/CheckupB2B.tsx -->

CTA gradient classes (both forms — both opaque-forever):
- `bg-gradient-to-r from-mu-blue to-mu-accent-blue` (current `/` route)
- `from-mu-cta-from-v6 to-mu-cta-to-v6` (legacy / DESIGN.md reference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run CTA invariant grep audit + Header.tsx render check + mix-blend-multiply baseline; produce 92-02-AUDIT.md</name>
  <files>.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (Archetype J definition + cross-cutting "CTA opaque-forever invariant" verification grep)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md (CTA grep targets table — file:line per call-site)
    - DESIGN.md (`## v9.0 Custom Rules` master list — full CTA opaque-forever section + 15 anti-patterns)
    - next/src/components/sections/HeroHub.tsx (lines 40–60 — primary CTA + secondary CTA distinction)
    - next/src/components/sections/ContactForm.tsx (lines 240–260 — submit gradient)
    - next/src/components/sections/FinalCTA.tsx (lines 1–40 — frame, decorative blob with mix-blend-multiply, primary CTA, phone CTA glass)
    - next/src/components/layout/MobileMenu.tsx (lines 90–100 — drawer CTA)
    - next/src/components/layout/StickyBar.tsx (lines 50–65 — sticky-bar CTA)
    - next/src/components/layout/Header.tsx (lines 1–60 — wordmark gradient, phone CTA)
  </read_first>
  <action>
    Read-only audit. Produce `.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md` with these sections:

    ### Section 1: CTA invariant grep results

    Run two greps and embed verbatim output:

    1. `grep -rn 'from-mu-blue to-mu-accent-blue' next/src/components/sections/ next/src/components/layout/` → list every match with file:line
    2. `grep -rn 'from-mu-cta-from to-mu-cta-to\|from-mu-cta-from-v6 to-mu-cta-to-v6' next/src/components/sections/ next/src/components/layout/` → list every match with file:line

    For each match, mark:
    - **IN-SCOPE** (Phase 92): HeroHub.tsx:48, MobileMenu.tsx:94, StickyBar.tsx:58, ContactForm.tsx:247, FinalCTA.tsx:26
    - **OUT-OF-SCOPE** (Phase 93): all `service/`, `consultations/`, `checkup/`, `treatment/` matches

    For every IN-SCOPE match, embed the FULL `className=` string from source. This is the baseline — downstream plans assert these strings remain free of `backdrop-blur` / `backdrop-filter`.

    ### Section 2: Negative-grep baseline (CTA never paired with backdrop)

    Run the cross-cutting verification grep from PATTERNS.md "Shared Patterns" section:

    ```bash
    grep -rn 'backdrop-filter\|backdrop-blur' \
      next/src/components/sections/HeroHub.tsx \
      next/src/components/layout/MobileMenu.tsx \
      next/src/components/layout/StickyBar.tsx \
      next/src/components/sections/ContactForm.tsx \
      next/src/components/sections/FinalCTA.tsx \
      | grep -E 'gradient-to-r|from-mu-blue|from-mu-cta'
    ```

    Expected: **zero matches**. Document actual output. If non-zero, **block planning of dependent waves** until 92-03..92-08 acceptance criteria explicitly forbid the regression.

    ### Section 3: Header.tsx (legacy) rendered-status check

    Run:
    ```bash
    grep -rn "from '@/components/layout/Header'" next/src/app/ next/src/components/
    grep -rn 'import.*Header[^C]' next/src/app/ next/src/components/
    ```

    Document conclusion as one of:
    - **RENDERED** — Plan 92-03 includes Header.tsx in chrome sweep (Archetype A apply).
    - **NOT RENDERED (dead code)** — Plan 92-03 documents skip; flag candidate for separate cleanup quick task.

    Provide the grep evidence inline.

    ### Section 4: FinalCTA mix-blend-multiply violation baseline

    Read `next/src/components/sections/FinalCTA.tsx` lines 10–18 verbatim. Embed the offending `<div>` (line 14: `bg-mu-blue/30 ... mix-blend-multiply`). Mark:

    > **Anti-pattern #8 violation present at FinalCTA.tsx:14.** Retirement deferred to Plan 92-08 (Wave 4). Plan 92-08 must either remove the entire decorative `<div>` OR drop only `mix-blend-multiply` and explicitly sanction with a Key Decision in PROJECT.md.

    ### Section 5: Sign-off

    Append:
    ```
    AUDIT COMPLETE: 2026-04-30
    Baseline locked. Plans 92-03 through 92-08 must:
    - Preserve every IN-SCOPE CTA className verbatim (no backdrop-* added)
    - Treat Header.tsx per Section 3 conclusion
    - Plan 92-08 retires FinalCTA:14 mix-blend-multiply violation
    ```

    **DO NOT MODIFY** any source file. This plan is read-only audit. Write only `92-02-AUDIT.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md && grep -c 'AUDIT COMPLETE' .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md` exists
    - File contains at least 5 IN-SCOPE CTA call-sites with file:line citations
    - File contains the negative-grep result (expect "zero matches" if baseline is clean — if not, AUDIT documents the regression and the plan blocks)
    - File contains a definitive Header.tsx render-status conclusion (RENDERED or NOT RENDERED with grep evidence)
    - File flags FinalCTA.tsx:14 mix-blend-multiply violation with deferral note to Plan 92-08
    - File ends with "AUDIT COMPLETE: <date>" sign-off line
    - `git status --porcelain next/src/` shows ZERO modified files (read-only contract)
    - `pnpm --dir next build` still exits 0 (no source touched)
  </acceptance_criteria>
  <done>
    Baseline AUDIT.md written; downstream plans (92-03..92-08) can grep against documented invariants.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code state ↔ planner downstream waves | AUDIT.md establishes the baseline; later plans must not regress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Repudiation | Downstream wave silently regresses CTA opacity (CTA disappears into blob — PITFALL #3.1, brand-trust threat) | mitigate | AUDIT.md is the immutable baseline; Plans 92-03..92-08 acceptance criteria reference its invariants |
| T-92-02-02 | Tampering | Audit modifies source files (read-only contract violated) | mitigate | Acceptance: `git status --porcelain next/src/` returns empty |
| T-92-02-03 | Information disclosure | None applicable (no PII; documentation file only) | accept | n/a |
</threat_model>

<verification>
1. `92-02-AUDIT.md` exists, contains all 5 sections, ends with "AUDIT COMPLETE: <date>".
2. No source file modified — `git status` confirms.
3. `pnpm --dir next build` still passes (no regression — read-only).
</verification>

<success_criteria>
- AUDIT.md written with 5 numbered sections
- 5 IN-SCOPE CTA call-sites documented with full className strings
- Header.tsx rendered-status conclusion + grep evidence
- FinalCTA mix-blend-multiply violation flagged for Plan 92-08
- Zero source files touched
- `pnpm build` still exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-02-SUMMARY.md`. AUDIT.md is the primary artifact; SUMMARY.md notes which downstream plans consume it.
</output>
