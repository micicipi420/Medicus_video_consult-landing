---
phase: 92-glass-rework-chrome-index-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/styles/liquid-glass.css
autonomous: true
requirements:
  - GLASS-10
must_haves:
  truths:
    - ".liquid-regular fills consume var(--glass-section-fill) and blur consumes var(--glass-section-blur)"
    - ".liquid-card fills consume var(--glass-card-fill) and blur consumes var(--glass-card-blur)"
    - ".liquid-nav fills consume var(--glass-section-fill) and blur consumes var(--glass-section-blur)"
    - ".liquid-btn-secondary fills consume var(--glass-button-fill) and blur consumes var(--glass-button-blur)"
    - ".stats-glass fills consume var(--glass-card-fill) and blur consumes var(--glass-card-blur)"
    - "Heat-leak radial-gradient rules already shipped (commit 9c93b9f) on .liquid-card (alpha 0.06) and .liquid-regular (alpha 0.04) remain untouched"
    - ".liquid-clear, .liquid-fluted, .liquid-btn-primary preserved (special-purpose / CTA opaque-forever)"
    - "@a11y-layer-coverage block (lines 79-157) untouched"
    - "pnpm --dir next build exits 0"
  artifacts:
    - path: "next/src/styles/liquid-glass.css"
      provides: "v9.0 tier-token-consuming utilities + preserved heat-leak rules"
      contains: "var(--glass-section-fill), var(--glass-card-fill), var(--glass-button-fill), radial-gradient(... at var(--blob-x"
  key_links:
    - from: "next/src/styles/liquid-glass.css :: .liquid-card / .liquid-regular"
      to: ":root --blob-x / --blob-y / --blob-heat (Phase 91 engine writes)"
      via: "radial-gradient(... at var(--blob-x) var(--blob-y), hsla(150,60%,50%,calc(α * var(--blob-heat))))"
      pattern: "radial-gradient.*var\\(--blob-x"
---

<objective>
Re-point liquid-glass.css utility classes (`.liquid-regular`, `.liquid-card`, `.liquid-nav`, `.liquid-btn-secondary`, `.stats-glass`) from legacy `--liquid-bg` / `--liquid-blur-*` variables to v9.0 tier tokens (`--glass-{section,card,button}-{fill,blur}`). Verify the heat-leak radial-gradient rules that already shipped in commit 9c93b9f on `.liquid-card` and `.liquid-regular` remain intact and correctly bound to `var(--blob-x/y/heat)`.

Purpose: Establish the CSS foundation every subsequent Phase 92 wave consumes. Plans 92-02..92-07 sweep Tailwind classes in TSX components — but any component already using `.liquid-card` / `.liquid-regular` utility wrappers (Phase 93 service pages, ContactMethodGrid) inherits the tier-token consumption automatically once this plan ships. Heat-leak verification is the GLASS-10 contract.
Output: Modified `next/src/styles/liquid-glass.css` with v9.0 tier-token consumption in 5 utilities; heat-leak rules preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@next/src/app/globals.css
@next/src/styles/liquid-glass.css

<interfaces>
<!-- v9.0 tier tokens already registered in globals.css :root by Phase 90 (FROZEN). -->
<!-- Phase 92 consumes these via var(); never redefines. -->

From next/src/app/globals.css :root:
```css
--glass-section-fill: rgba(255,255,255,0.06);  /* desktop */
--glass-section-blur: clamp(12px, ..., 24px);
--glass-card-fill:    rgba(255,255,255,0.10);
--glass-card-blur:    clamp(12px, ..., 20px);
--glass-form-fill:    rgba(255,255,255,0.14);
--glass-form-blur:    clamp(12px, ..., 18px);
--glass-button-fill:  rgba(255,255,255,0.12);
--glass-button-blur:  clamp(12px, ..., 16px);
```

From :root each frame (Phase 91 engine writes — FROZEN):
```css
--blob-x:    <pixel value>;
--blob-y:    <pixel value>;
--blob-heat: 0..1;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point .liquid-regular, .liquid-card, .liquid-nav, .liquid-btn-secondary, .stats-glass utilities to v9.0 tier tokens</name>
  <files>next/src/styles/liquid-glass.css</files>
  <read_first>
    - next/src/styles/liquid-glass.css (FULL FILE — must locate every `var(--liquid-bg)`, `var(--liquid-nav-bg)`, `var(--liquid-blur-md)`, `var(--liquid-blur-lg)`, `var(--liquid-nav-blur)` occurrence inside the 5 target utilities)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (Archetype G utility re-pointing table — exact line numbers + before/after var names)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md (Decisions C, D, H, I)
    - next/src/app/globals.css (verify --glass-* tokens exist at :root; FROZEN — DO NOT EDIT)
  </read_first>
  <action>
    Per PATTERNS.md Archetype G table, edit exactly 5 utility blocks in `next/src/styles/liquid-glass.css`:

    1. **`.liquid-regular`** (block around lines 170–200; locate the rule via `grep -n '\.liquid-regular' next/src/styles/liquid-glass.css`):
       - Inside the `linear-gradient(...)` background and the standalone `background:` declarations, BEFORE: `var(--liquid-bg)` (×2 occurrences) → AFTER: `var(--glass-section-fill)`
       - BEFORE: `backdrop-filter: blur(var(--liquid-blur-md))` → AFTER: `backdrop-filter: blur(var(--glass-section-blur))`
       - PRESERVE the heat-leak `radial-gradient(ellipse 600px 400px at var(--blob-x, 50vw) var(--blob-y, 50vh), hsla(150, 60%, 50%, calc(0.04 * var(--blob-heat, 0))), transparent 70%)` block VERBATIM (shipped commit 9c93b9f).

    2. **`.liquid-card`** (block around lines 340–370):
       - BEFORE: `var(--liquid-bg)` (×2) → AFTER: `var(--glass-card-fill)`
       - BEFORE: `backdrop-filter: blur(var(--liquid-blur-md))` → AFTER: `backdrop-filter: blur(var(--glass-card-blur))`
       - PRESERVE heat-leak `radial-gradient(... calc(0.06 * var(--blob-heat, 0)) ...)` block VERBATIM (shipped commit 9c93b9f).

    3. **`.liquid-nav`** (block around lines 215–230):
       - BEFORE: `var(--liquid-nav-bg)` (×2) → AFTER: `var(--glass-section-fill)`
       - BEFORE: `backdrop-filter: blur(var(--liquid-nav-blur))` → AFTER: `backdrop-filter: blur(var(--glass-section-blur))`

    4. **`.liquid-btn-secondary`** (block around lines 420–435):
       - BEFORE: `var(--liquid-bg)` (×2) → AFTER: `var(--glass-button-fill)`
       - BEFORE: `backdrop-filter: blur(var(--liquid-blur-md))` → AFTER: `backdrop-filter: blur(var(--glass-button-blur))`

    5. **`.stats-glass`** (block around lines 460–476):
       - BEFORE: `var(--liquid-bg)` (×2) → AFTER: `var(--glass-card-fill)`
       - BEFORE: `backdrop-filter: blur(var(--liquid-blur-lg))` → AFTER: `backdrop-filter: blur(var(--glass-card-blur))`

    **DO NOT TOUCH (Decision D / Decision I freezes):**
    - `.liquid-clear` (special-purpose modal/overlay material)
    - `.liquid-fluted` (special-purpose texture)
    - `.liquid-btn-primary` (CTA opaque-forever — gradient unchanged)
    - `.liquid-header-backdrop` (Josh-Comeau pattern; leave hardcoded blur)
    - `@a11y-layer-coverage` block (lines 79–157)
    - Any heat-leak `radial-gradient(...)` rule (already shipped, must remain verbatim)

    Per Decision E (locked): the `--liquid-*` legacy CSS custom properties STAY defined in globals.css for defensive consumption — do NOT remove them from globals.css (frozen anyway).

    Anti-pattern enforcement (Decision H): zero new glass classes added → no `@a11y-layer-coverage` update needed (anti-pattern #14 compliant by no-op). Heat-leak gradient is on `background-image`, not `backdrop-filter` → anti-pattern #6 compliant. Per D-C, gradient α stays at 0.04 / 0.06 — no animation of `backdrop-filter`.
  </action>
  <verify>
    <automated>pnpm --dir next build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `grep -c 'var(--glass-section-fill)' next/src/styles/liquid-glass.css` returns ≥4 (2 in `.liquid-regular`, 2 in `.liquid-nav`)
    - `grep -c 'var(--glass-card-fill)' next/src/styles/liquid-glass.css` returns ≥4 (2 in `.liquid-card`, 2 in `.stats-glass`)
    - `grep -c 'var(--glass-button-fill)' next/src/styles/liquid-glass.css` returns ≥2 (in `.liquid-btn-secondary`)
    - `grep -c 'blur(var(--glass-section-blur))' next/src/styles/liquid-glass.css` returns ≥2 (`.liquid-regular`, `.liquid-nav`)
    - `grep -c 'blur(var(--glass-card-blur))' next/src/styles/liquid-glass.css` returns ≥2 (`.liquid-card`, `.stats-glass`)
    - `grep -c 'blur(var(--glass-button-blur))' next/src/styles/liquid-glass.css` returns ≥1 (`.liquid-btn-secondary`)
    - `grep -c 'radial-gradient.*var(--blob-x' next/src/styles/liquid-glass.css` returns ≥2 (heat-leak preserved on `.liquid-card` and `.liquid-regular`)
    - `grep -c 'var(--liquid-bg)' next/src/styles/liquid-glass.css` returns 0 inside the 5 swept utilities (run with `sed -n '170,478p' next/src/styles/liquid-glass.css | grep -c 'var(--liquid-bg)'` → 0; outside the swept range it may persist for `.liquid-clear` / `.liquid-fluted`)
    - The lines 79–157 `@a11y-layer-coverage` block is byte-identical to its pre-edit state (`git diff next/src/styles/liquid-glass.css` shows no changes in that line range)
  </acceptance_criteria>
  <done>
    5 utility classes consume v9.0 tier tokens; heat-leak rules preserved verbatim; `@a11y-layer-coverage` block unchanged; `pnpm build` clean; defensive `--liquid-*` legacy vars still defined in globals.css.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser CSS engine ↔ runtime `:root` vars (Phase 91) | One-way read; Phase 92 consumes `var(--blob-x/y/heat)` only |
| Build-time Tailwind class generation ↔ runtime CSS | Token strings must resolve at paint; arbitrary-value classes verified via `pnpm build` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01-01 | Tampering | `.liquid-card` / `.liquid-regular` heat-leak rule accidentally removed | mitigate | Acceptance criteria require ≥2 `radial-gradient.*var(--blob-x` matches post-edit |
| T-92-01-02 | Denial of service | Mobile blur >12px (battery drain on 45+ audience devices) | mitigate | Token consumption: `--glass-{tier}-blur` already clamps to 12px on mobile (Phase 90 frozen). No hardcoded blur values introduced. |
| T-92-01-03 | Elevation of privilege | `@a11y-layer-coverage` block edited (anti-pattern #14 / #15 — cheat-passing a11y) | mitigate | Acceptance: byte-identical lines 79–157 verified via `git diff` |
| T-92-01-04 | Information disclosure | None applicable (CSS file, no PII) | accept | Phase 92 is visual class swap; no data flow |
</threat_model>

<verification>
1. `pnpm --dir next build` exits 0 (compile gate).
2. All grep gates in acceptance criteria pass.
3. Manual: open `/` in dev mode; chrome and any `.liquid-card` consumer (Phase 93 service pages or `ContactMethodGrid` if rendered) visually unchanged from baseline (token values are designed to look continuous).
4. DevTools Recipe 2 (heat-leak liveness) — find any `.liquid-card` consumer in app, move cursor; Computed `background-image` shows `radial-gradient(... at <px>px <px>px ...)` updating with cursor.
</verification>

<success_criteria>
- 5 utilities re-pointed to v9.0 tier tokens
- Heat-leak `radial-gradient` rules byte-identical (commit 9c93b9f preserved)
- `@a11y-layer-coverage` block byte-identical
- `pnpm build` clean
- No new dependencies, no new classes, no new tokens
</success_criteria>

<output>
After completion, create `.planning/phases/92-glass-rework-chrome-index-sections/92-01-SUMMARY.md`.
</output>
