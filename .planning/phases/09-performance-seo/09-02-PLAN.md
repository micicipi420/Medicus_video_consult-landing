---
phase: 09-performance-seo
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified: []
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04]

must_haves:
  truths:
    - "Page loads in under 3 seconds on simulated slow 3G"
    - "Meta tags (title, description, Open Graph) are present and correct"
    - "HTML uses semantic elements with correct heading hierarchy and landmarks"
    - "No render-blocking resources beyond the single CSS file"
  artifacts:
    - path: "index.html"
      provides: "Complete landing page with SEO meta tags and semantic HTML"
      contains: "og:title"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "single stylesheet link"
      pattern: "link.*stylesheet.*styles\\.css"
    - from: "index.html"
      to: "js/main.js"
      via: "deferred script"
      pattern: "script.*defer.*main\\.js"
---

<objective>
Verify all Phase 9 success criteria are met after the work done in plan 09-01.

Purpose: Plan 09-01 added SEO meta tags and verified semantic HTML. This plan runs final verification to confirm all four PERF requirements hold and the page meets the "under 3 seconds on 3G" performance target.

Output: Verification confirmation that all Phase 9 success criteria pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-seo/09-CONTEXT.md
@.planning/phases/09-performance-seo/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SEO meta tags, semantic HTML, and performance characteristics</name>
  <files></files>
  <read_first>index.html, css/styles.css, js/main.js</read_first>
  <action>
Run verification checks against index.html to confirm all Phase 9 requirements hold:

**PERF-03 - Meta tags verification:**
- Confirm title tag contains "MedicusUnion KZ"
- Confirm meta description exists and is non-empty
- Confirm og:type, og:locale, og:title, og:description, og:url, og:site_name all present
- Confirm twitter:card, twitter:title, twitter:description present
- Confirm canonical link present pointing to https://medicusunion.kz

**PERF-04 - Semantic HTML verification:**
- Confirm exactly one h1 element exists
- Confirm h2 elements used for section headings (no skipped heading levels)
- Confirm landmark elements present: header, main, footer
- Confirm all form inputs have associated label elements (for/id match)
- Confirm lang="ru" on html element

**PERF-01 - Performance characteristics verification:**
- Confirm only one CSS file is loaded (no extra stylesheets)
- Confirm JS file uses defer attribute (not render-blocking)
- Confirm font preloads use crossorigin attribute
- Count total file size: index.html + styles.css + main.js (should be well under 500KB combined)
- No external resource requests (no CDN fonts, no analytics, no third-party scripts)

**PERF-02 - Image audit:**
- Confirm no img tags without alt attribute exist
- Note: page currently has no images (text + CSS + emoji only), so this requirement is satisfied by absence

Run these commands for automated verification:
```
grep -c '<h1' index.html          # should be 1
grep -c 'og:title' index.html     # should be >= 1
grep -c 'og:description' index.html  # should be >= 1
grep '<html' index.html | grep 'lang="ru"'  # should match
grep 'rel="canonical"' index.html    # should match
grep '<header' index.html           # should match
grep '<main' index.html             # should match
grep '<footer' index.html           # should match
grep 'defer' index.html             # JS should be deferred
wc -c index.html css/styles.css js/main.js  # total size check
```

If any check fails, fix the issue in index.html. Based on 09-01-SUMMARY, all should already pass.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && test $(grep -c '<h1' index.html) -eq 1 && grep -q 'og:title' index.html && grep -q 'og:description' index.html && grep -q 'lang="ru"' index.html && grep -q 'rel="canonical"' index.html && grep -q '<header' index.html && grep -q '<main' index.html && grep -q '<footer' index.html && grep -q 'defer' index.html && echo "ALL PERF CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'og:title' index.html returns match
    - grep 'og:description' index.html returns match
    - grep 'og:url' index.html returns match
    - grep 'og:locale' index.html returns match
    - grep 'twitter:card' index.html returns match
    - grep 'rel="canonical"' index.html returns match
    - grep -c 'h1' index.html shows exactly 1 h1
    - grep 'lang="ru"' index.html returns match
    - grep '<header' index.html returns match
    - grep '<main' index.html returns match
    - grep '<footer' index.html returns match
    - grep 'defer' index.html returns match for JS
    - Combined file size of index.html + styles.css + main.js is under 500KB
  </acceptance_criteria>
  <done>All four PERF requirements verified: meta tags present (PERF-03), semantic HTML correct (PERF-04), page is lightweight with no render-blocking resources (PERF-01), no images needing optimization (PERF-02). Combined asset size confirms sub-3-second load on 3G is achievable.</done>
</task>

</tasks>

<verification>
All Phase 9 success criteria from ROADMAP.md are confirmed:
1. Page loads in under 3 seconds on 3G -- verified by lightweight asset audit (no images, single CSS/JS, fonts preloaded)
2. All images are WebP with fallback -- N/A (no images on page)
3. Meta tags (title, description, Open Graph) present and correct -- verified by grep checks
4. HTML uses semantic elements -- verified (single h1, landmarks, form labels)
</verification>

<success_criteria>
- All automated grep checks pass for meta tags, semantic HTML, and performance characteristics
- Combined file size is under 500KB
- No render-blocking JS (defer attribute confirmed)
- Phase 9 can be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-seo/09-02-SUMMARY.md`
</output>
