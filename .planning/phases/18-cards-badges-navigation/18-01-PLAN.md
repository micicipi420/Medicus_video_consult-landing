---
phase: 18-cards-badges-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
autonomous: true
requirements:
  - CARD-04
  - CARD-05
  - CARD-06
  - NAV-01

must_haves:
  truths:
    - "Cards have border-radius 30px (not 20px) in all states"
    - "Cards have no box-shadow in default or hover state (flat design)"
    - "The pricing badge has mint background (#d0fae4) and dark green text (#007955)"
    - "Desktop navigation is 76px tall and has no gradient accent line"
  artifacts:
    - path: "css/styles.css"
      provides: "All visual changes — radius token, card shadow removal, badge colors, nav height"
      contains: "--radius-lg: 1.875rem"
  key_links:
    - from: ".card"
      to: "--radius-lg"
      via: "border-radius: var(--radius-lg)"
      pattern: "border-radius: var\\(--radius-lg\\)"
    - from: ".pricing__badge"
      to: "#d0fae4 / #007955"
      via: "background-color and color properties"
      pattern: "color-badge-bg"
    - from: ".site-header desktop media query"
      to: "76px height"
      via: "height: 76px on min-width: 768px"
      pattern: "height: 76px"
---

<objective>
Apply four CSS-only design alignment changes to match medicusunion.kz visual style: increase card border-radius to 30px, remove all card shadows (flat design), switch the pricing badge to a mint color palette, and set desktop navigation height to 76px without the gradient accent line.

Purpose: Complete the v1.3 KZ Design Alignment milestone by bringing cards, badges, and navigation in line with the reference site's visual style.
Output: Updated css/styles.css with all four requirements satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cards-badges-navigation/18-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Card radius and shadow removal (CARD-04, CARD-05)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css lines 117-121 (--radius-lg token in :root)
    - css/styles.css lines 295-309 (.card and .card:hover)
  </read_first>
  <action>
    Make two targeted changes in css/styles.css:

    1. In the `:root` block (around line 120), change:
       `--radius-lg: 1.25rem;  /* 20px */`
       to:
       `--radius-lg: 1.875rem;  /* 30px */`

    2. In `.card` (around line 295-303):
       - Remove the line: `box-shadow: var(--shadow-md);`
       - Update the transition property to remove box-shadow from it (per CARD-05 flat design — no shadow in any state):
         Change: `transition: transform var(--transition-normal), box-shadow var(--transition-normal), border-left-color var(--transition-normal);`
         To: `transition: transform var(--transition-normal), border-left-color var(--transition-normal);`

    3. In `.card:hover` (around line 305-309):
       - Remove the line: `box-shadow: var(--shadow-lg);`
       - Keep `transform: translateY(-2px);` and `border-left-color: var(--color-primary);`

    Do NOT change any other properties. The border-left accent on hover stays.
  </action>
  <verify>
    grep -n "radius-lg" css/styles.css | head -5
    grep -n "box-shadow" css/styles.css | grep -i card
  </verify>
  <acceptance_criteria>
    - `grep "radius-lg" css/styles.css` contains `1.875rem` (not `1.25rem`)
    - `grep -A5 "^.card {" css/styles.css` does NOT contain `box-shadow`
    - `grep -A5 "^.card:hover {" css/styles.css` does NOT contain `box-shadow`
    - `.card:hover` still contains `transform: translateY(-2px)`
  </acceptance_criteria>
  <done>--radius-lg is 1.875rem (30px). Neither .card nor .card:hover contains box-shadow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mint badge palette (CARD-06)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css lines 1-30 (:root color tokens section)
    - css/styles.css lines 859-872 (.pricing__badge)
  </read_first>
  <action>
    Make two changes in css/styles.css:

    1. In the `:root` color tokens block (after the existing color variables, before the `--space-*` variables), add two new badge tokens on their own lines:
       ```
       --color-badge-bg: #d0fae4;
       --color-badge-text: #007955;
       ```
       Insert them after the last color token line (look for `--color-text-on-cta` or similar, then insert after it).

    2. In `.pricing__badge` (around line 859-872):
       - Change `background-color: var(--color-primary);` to `background-color: var(--color-badge-bg);`
       - Change `color: var(--color-text-on-primary);` to `color: var(--color-badge-text);`

    Do NOT change any other properties of .pricing__badge (padding, font-size, position etc. stay).
  </action>
  <verify>
    grep -n "color-badge" css/styles.css
    grep -n -A10 "pricing__badge {" css/styles.css
  </verify>
  <acceptance_criteria>
    - `grep "color-badge-bg" css/styles.css` returns `--color-badge-bg: #d0fae4;`
    - `grep "color-badge-text" css/styles.css` returns `--color-badge-text: #007955;`
    - `grep -A8 "pricing__badge {" css/styles.css` contains `background-color: var(--color-badge-bg)`
    - `grep -A8 "pricing__badge {" css/styles.css` contains `color: var(--color-badge-text)`
    - `grep -A8 "pricing__badge {" css/styles.css` does NOT contain `var(--color-primary)` or `var(--color-text-on-primary)`
  </acceptance_criteria>
  <done>Badge tokens declared in :root. .pricing__badge uses --color-badge-bg (#d0fae4) and --color-badge-text (#007955).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Desktop navigation 76px height, remove gradient line (NAV-01)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css lines 347-422 (.site-header block and its desktop media query)
  </read_first>
  <action>
    Make two changes in css/styles.css:

    1. Remove the `.site-header::after` block entirely (lines 366-371):
       ```css
       .site-header::after {
         content: '';
         display: block;
         height: 3px;
         background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
       }
       ```
       Delete all 6 lines including the opening and closing braces. This removes the decorative gradient accent line from the header bottom.

    2. Inside the existing `@media (min-width: 768px)` block for the header (around line 403-422), add a new rule for `.site-header`:
       ```css
       .site-header {
         height: 76px;
         display: flex;
         align-items: center;
       }
       ```
       Insert this BEFORE the existing `.site-header__nav` rule inside the same media query block. The `display: flex` and `align-items: center` ensure the header container is vertically centred at exactly 76px. Do NOT remove `padding-block` from the base `.site-header` rule — only the desktop height is being set.
  </action>
  <verify>
    grep -n "site-header::after" css/styles.css
    grep -n "height: 76px" css/styles.css
  </verify>
  <acceptance_criteria>
    - `grep "site-header::after" css/styles.css` returns no results (block is gone)
    - `grep "height: 76px" css/styles.css` returns at least one result inside a `min-width: 768px` media query
    - `grep "linear-gradient.*color-primary.*color-secondary" css/styles.css` returns no results in the header section
  </acceptance_criteria>
  <done>.site-header::after gradient block is deleted. Desktop .site-header has height: 76px inside the 768px media query.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run these checks:

```bash
# CARD-04: radius token is 30px
grep "radius-lg" css/styles.css

# CARD-05: no card shadows
grep -n "box-shadow" css/styles.css | grep -i "card\|shadow-md\|shadow-lg" | grep -v "pricing\|is-scrolled\|site-header"

# CARD-06: badge tokens and mint colors
grep -n "color-badge" css/styles.css

# NAV-01: 76px height, no gradient line
grep -n "site-header::after\|height: 76px" css/styles.css
```

Expected results:
- `--radius-lg: 1.875rem` present
- No box-shadow lines referencing .card (outside of .is-scrolled or unrelated contexts)
- `--color-badge-bg: #d0fae4` and `--color-badge-text: #007955` present
- `site-header::after` absent, `height: 76px` present
</verification>

<success_criteria>
1. --radius-lg equals 1.875rem (30px) — CARD-04 done
2. .card and .card:hover have zero box-shadow declarations — CARD-05 done
3. .pricing__badge uses --color-badge-bg (#d0fae4) background and --color-badge-text (#007955) text — CARD-06 done
4. Desktop .site-header has height: 76px inside @media (min-width: 768px) and the ::after gradient line block is deleted — NAV-01 done
</success_criteria>

<output>
After completion, create `.planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md` following the summary template at `@~/.claude/get-shit-done/templates/summary.md`.
</output>
