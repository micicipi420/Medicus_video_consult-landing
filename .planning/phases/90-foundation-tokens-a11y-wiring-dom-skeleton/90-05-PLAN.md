---
phase: 90-foundation-tokens-a11y-wiring-dom-skeleton
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - next/src/app/test-glass/page.tsx
autonomous: true
requirements: [FND-07]
tags: [key-decision, brand-approval, swatch, design-tooling, foundation, v9.0]
must_haves:
  truths:
    - ".planning/PROJECT.md Key Decisions table gains a row for KD-v9-001 with value --blob-hot #4FE098, status pending, references TZ §5 and medicusunion.com / .kz"
    - "Existing 25 Key Decisions table rows are untouched"
    - "/test-glass route renders a comparison swatch row with 3 labeled colored divs: --mu-primary #35B678, --blob-hot #4FE098, medicusunion.kz reference green #1AC67E"
    - "Existing /test-glass swatches (gradient-test container, Squircle Shapes, Section Tints) are untouched"
    - "KD-v9-001 status remains pending after this plan ships — gate is for user (Phase 91 planner halts if still pending)"
    - "pnpm build passes"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "KD-v9-001 row in Key Decisions table — gates Phase 91"
      contains: "KD-v9-001"
    - path: "next/src/app/test-glass/page.tsx"
      provides: "Visual comparison surface for the user to evaluate --blob-hot against current brand greens"
      contains: "#4FE098"
  key_links:
    - from: ".planning/PROJECT.md KD-v9-001 row"
      to: "next/src/app/test-glass/page.tsx blob-hot swatch"
      via: "user views /test-glass; updates KD-v9-001 status from pending to approved (or proposes alternate hex)"
      pattern: "KD-v9-001"
    - from: "Plan 01 globals.css --blob-hot: #4FE098"
      to: ".planning/PROJECT.md KD-v9-001 row"
      via: "documentation provenance — every new brand color requires a KD entry per CLAUDE.md > Design Contract"
      pattern: "#4FE098"
---

<objective>
Log the `KD-v9-001: --blob-hot #4FE098` Key Decision in `.planning/PROJECT.md` and add a comparison swatch row to the `/test-glass` route so the user can visually evaluate the new color against current brand greens before Phase 91 unlocks.

Purpose: CLAUDE.md > Design Contract requires every new brand color to trace to medicusunion.com / medicusunion.kz, with a Key Decision entry for non-trace additions. `--blob-hot: #4FE098` is the only v9.0 color that does not directly mirror an existing `--mu-*` token (it's a slightly cooler/brighter green derived from `--mu-green-600 #35B678`). User must approve (or propose alternate hex) by updating `KD-v9-001` status from `pending` to `approved` before `/gsd-discuss-phase 91`. **This plan does NOT block Phase 90 ship** — Phase 91 planner is the gate.

Output:
- `.planning/PROJECT.md`: ONE new row appended to the Key Decisions table.
- `next/src/app/test-glass/page.tsx`: ONE new section appended after the existing "Section Tints" block — three labeled colored swatches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-RESEARCH.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-PATTERNS.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-VALIDATION.md
@next/src/app/test-glass/page.tsx
@design/LIQUID_GLASS_BLOB_TZ.md

<interfaces>
<!-- PROJECT.md Key Decisions table format (existing) and target row to append. -->

Existing Key Decisions table format (lines 161-187 in PROJECT.md per 90-PATTERNS.md):
```markdown
| Decision | Rationale | Outcome |
|----------|-----------|---------|
| HTML/CSS/JS вместо фреймворка | Простота, скорость загрузки, лёгкий деплой | ✓ Good — 64KB total, no build step |
...
| Dark mode disables backdrop-filter (glass-off) | Murky smear on navy #0F1923; opaque surface better on dark backgrounds | ✓ Good — v1.4 |
```

Target row to append (after the last existing row, before the next `## Evolution` heading):
```markdown
| KD-v9-001: --blob-hot #4FE098 | TZ §5 — heat-state highlight; brand parity check pending against medicusunion.com / medicusunion.kz | pending — user must view /test-glass and approve (or propose alternate hex) before /gsd-discuss-phase 91 |
```

The status word `pending` (lowercase) is grep-required — Phase 91 planner halts on `KD-v9-001.*pending`.

Existing /test-glass page swatch pattern (90-PATTERNS.md, "Squircle Shapes" rows lines 82-98):
```tsx
<div>
  <h2 className="text-xl font-heading mb-4">Squircle Shapes</h2>
  <div className="flex gap-4 flex-wrap">
    <div className="squircle-md bg-mu-green-100 w-24 h-24 flex items-center justify-center text-xs text-mu-text-700">
      .squircle-md
    </div>
    ...
  </div>
</div>
```

The new swatch section uses inline `style={{ background: '#XXXXXX' }}` (NOT Tailwind utilities) so the literal hex values are visible to the user without any abstraction.

Three colors to compare:
- `#35B678` — `--mu-primary` (existing brand primary green, alias of `--mu-green-600`)
- `#4FE098` — `--blob-hot` (KD-v9-001 candidate, slightly cooler/brighter green)
- `#1AC67E` — `cta-gradient-from` reference, present on medicusunion.kz CTA gradient
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append KD-v9-001 row to .planning/PROJECT.md Key Decisions table</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (read the Key Decisions section — locate the last data row and the next heading after the table)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md (Decision B — KD-v9-001 flow; format)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-PATTERNS.md (`.planning/PROJECT.md (MD-project-state, append KD row)` — verbatim row + frozen-range warning)
  </read_first>
  <action>
1. Locate the Key Decisions table in `.planning/PROJECT.md`. Identify the LAST existing data row of the table (immediately before the next heading, typically `## Evolution`).

2. Append ONE new row immediately after the last existing data row, BEFORE any subsequent heading or blank-line-then-heading sequence:

```markdown
| KD-v9-001: --blob-hot #4FE098 | TZ §5 — heat-state highlight; brand parity check pending against medicusunion.com / medicusunion.kz | pending — user must view /test-glass and approve (or propose alternate hex) before /gsd-discuss-phase 91 |
```

3. DO NOT modify any existing row. DO NOT modify the table header (`| Decision | Rationale | Outcome |` and the `|----|----|----|` separator). DO NOT modify the surrounding sections (`## Constraints` above, `## Evolution` below).

4. Verify status word is lowercase `pending` (Phase 91 planner grep contract).
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -cE 'KD-v9-001.*--blob-hot.*#4FE098.*pending' .planning/PROJECT.md | grep -q '^1$' && grep -E 'KD-v9-001' .planning/PROJECT.md | grep -q 'TZ §5' && grep -E 'KD-v9-001' .planning/PROJECT.md | grep -qE 'medicusunion\.(com|kz)' && echo OK</automated>
  </verify>
  <done>
    - Exactly one row in PROJECT.md matches `KD-v9-001.*--blob-hot.*#4FE098.*pending`.
    - Row mentions `TZ §5` (rationale provenance).
    - Row references `medicusunion.com` and/or `medicusunion.kz` (brand parity link).
    - No existing Key Decisions table row was modified.
  </done>
  <acceptance_criteria>
    - grep -cE 'KD-v9-001.*--blob-hot.*#4FE098.*pending' .planning/PROJECT.md → 1
    - grep -E 'KD-v9-001' .planning/PROJECT.md | grep -c 'TZ §5' → ≥1
    - grep -E 'KD-v9-001' .planning/PROJECT.md | grep -cE 'medicusunion\.(com|kz)' → ≥1
    - grep -E 'KD-v9-001' .planning/PROJECT.md | grep -c '/test-glass' → ≥1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append blob-hot comparison swatch row to /test-glass page</name>
  <files>next/src/app/test-glass/page.tsx</files>
  <read_first>
    - next/src/app/test-glass/page.tsx (full read — locate end of "Section Tints" block at ~line 123 and any existing footer at ~line 125)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md (Decision B step 3 — comparison swatch contents)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-PATTERNS.md (`/test-glass/page.tsx (TSX-route, append swatch row)` — verbatim JSX + frozen-range warnings)
  </read_first>
  <action>
1. Locate the existing "Section Tints" `<div>` block in `next/src/app/test-glass/page.tsx` (approximately lines 100-123). Identify the END of that section (its closing `</div>`) and the next element (existing footer at approximately line 125).

2. Insert the following NEW section AFTER the "Section Tints" block and BEFORE the existing footer:

```tsx
        {/* v9.0 Phase 90 — Blob palette comparison (KD-v9-001 brand approval surface) */}
        <div>
          <h2 className="text-xl font-heading mb-4">v9.0 Blob Palette — KD-v9-001 Comparison</h2>
          <p className="text-sm text-mu-text-500 mb-4">
            Compare <code>--blob-hot</code> against current brand greens.
            User must approve <code>KD-v9-001</code> in <code>.planning/PROJECT.md</code> before Phase 91.
          </p>
          <div className="flex gap-4 flex-wrap">
            <div
              className="w-32 h-32 flex items-end justify-center text-xs text-white p-2"
              style={{ background: '#35B678' }}
            >
              --mu-primary
              <br />
              #35B678
            </div>
            <div
              className="w-32 h-32 flex items-end justify-center text-xs text-white p-2"
              style={{ background: '#4FE098' }}
            >
              --blob-hot
              <br />
              #4FE098 (KD-v9-001)
            </div>
            <div
              className="w-32 h-32 flex items-end justify-center text-xs text-white p-2"
              style={{ background: '#1AC67E' }}
            >
              cta-gradient-from
              <br />
              #1AC67E (medicusunion.kz ref)
            </div>
          </div>
        </div>
```

(Adjust the leading whitespace to match the existing file's indentation style — likely 8 spaces inside the `<main>` wrapper.)

3. DO NOT modify the existing `GlassInteraction` import (line 1), the gradient-test container (lines ~12-79), the "Squircle Shapes" section (lines ~82-98), the "Section Tints" section (lines ~100-123), or the existing footer (lines ~125-127).

4. Run `cd next && pnpm build` — must pass.

5. Optional sanity (does not block plan): `pnpm dev`, navigate to http://localhost:3000/test-glass, confirm the new section renders with three distinguishable green swatches.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && [ "$(grep -c '#4FE098' next/src/app/test-glass/page.tsx)" -ge "1" ] && [ "$(grep -c '#35B678' next/src/app/test-glass/page.tsx)" -ge "1" ] && [ "$(grep -c '#1AC67E' next/src/app/test-glass/page.tsx)" -ge "1" ] && grep -q 'KD-v9-001' next/src/app/test-glass/page.tsx && grep -q 'blob-hot' next/src/app/test-glass/page.tsx && grep -q 'mu-primary' next/src/app/test-glass/page.tsx && cd next && pnpm build > /tmp/build-90-05-task2.log 2>&1 && echo OK</automated>
  </verify>
  <done>
    - All three hex values (`#4FE098`, `#35B678`, `#1AC67E`) appear at least once in `test-glass/page.tsx`.
    - References to `KD-v9-001`, `blob-hot`, `mu-primary` present.
    - `pnpm build` passes.
    - No existing swatch section modified.
  </done>
  <acceptance_criteria>
    - grep -c '#4FE098' next/src/app/test-glass/page.tsx → ≥1
    - grep -c '#35B678' next/src/app/test-glass/page.tsx → ≥1
    - grep -c '#1AC67E' next/src/app/test-glass/page.tsx → ≥1
    - grep -c 'KD-v9-001' next/src/app/test-glass/page.tsx → ≥1
    - grep -c 'blob-hot' next/src/app/test-glass/page.tsx → ≥1
    - cd next && pnpm build → exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none direct) | Documentation row in PROJECT.md (no runtime effect); static JSX swatch on an internal `/test-glass` route (no user input, no network calls). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none direct) | — | PROJECT.md row + /test-glass swatch JSX | accept | Internal design tooling only. `/test-glass` is not linked from any production page; it exists for design-system swatch viewing (per Phase 59 origin). The KD-v9-001 row is a markdown documentation entry — no runtime effect. Hex values are static string literals — no user input, no template injection. |

**Note:** Phase 90 Plan 05 ships zero new runtime JS, no listeners, no network calls. The brand-color approval gate is a process control (Phase 91 planner halts on `pending` status), not a runtime security control. No XSS / SSRF / injection / authn / authz / crypto threats.
</threat_model>

<verification>
- Task 1: `grep -cE 'KD-v9-001.*--blob-hot.*#4FE098.*pending' .planning/PROJECT.md` returns 1.
- Task 2: All 3 hex values present in `test-glass/page.tsx`; build passes.
- No existing PROJECT.md row or /test-glass section modified.
- Plan 05 does NOT block Phase 90 ship — KD-v9-001 status remains `pending`. Phase 91 planner is the gate.
</verification>

<success_criteria>
1. `.planning/PROJECT.md` Key Decisions table has exactly one row matching `KD-v9-001.*--blob-hot.*#4FE098.*pending`, with `TZ §5` and `medicusunion.com` / `.kz` references.
2. `next/src/app/test-glass/page.tsx` has a new "v9.0 Blob Palette — KD-v9-001 Comparison" section with three swatches at `#35B678`, `#4FE098`, `#1AC67E`.
3. `pnpm build` passes.
4. No existing PROJECT.md row, /test-glass section, or table header modified.
5. Plan ships independently in Wave 1 (no dependency on Plans 01-04).
</success_criteria>

<output>
After completion, create `.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-05-SUMMARY.md` per `templates/summary.md`. Note: (a) line number where KD-v9-001 row was inserted, (b) line number where the swatch section was inserted, (c) reminder that the user must view /test-glass and update KD-v9-001 status to `approved` (or propose alternate hex) before `/gsd-discuss-phase 91` — Phase 91 planner halts if status is still `pending`.
</output>
