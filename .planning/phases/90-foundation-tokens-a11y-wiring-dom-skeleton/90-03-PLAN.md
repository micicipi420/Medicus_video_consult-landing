---
phase: 90-foundation-tokens-a11y-wiring-dom-skeleton
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
autonomous: true
requirements: [FND-04, FND-05]
tags: [design-doc, anti-patterns, z-index-contract, cta-opaque, foundation, v9.0]
must_haves:
  truths:
    - "DESIGN.md YAML front matter gains a top-level antiPatterns: key with exactly 15 entries (name, why, addedIn)"
    - "DESIGN.md markdown body gains ## v9.0 Custom Rules section containing the z-index contract (z-0 / z-1..10 / z-50+ / z-100+) AND the CTA opaque-forever 7-component master list"
    - "DESIGN.md markdown body gains ## v9.0 Anti-Patterns section with 15 numbered entries expanding the YAML field (name, why, where it manifests, what to do instead)"
    - "Existing colors:, typography:, rounded:, spacing:, shapes:, components: YAML keys are untouched"
    - "Existing markdown body sections (preceding the new v9.0 sections) are untouched"
  artifacts:
    - path: "DESIGN.md"
      provides: "z-index contract, CTA opaque-forever master list (7 components), 15 anti-patterns (YAML + body)"
      contains: "## v9.0 Custom Rules"
  key_links:
    - from: "DESIGN.md ## v9.0 Custom Rules CTA opaque-forever list"
      to: "liquid-glass.css @a11y-layer-coverage CTA exclusion comment (Plan 02)"
      via: "documentation cross-reference; comment in CSS points back to DESIGN.md"
      pattern: "see DESIGN\\.md > v9\\.0 Custom Rules"
    - from: "DESIGN.md antiPatterns: YAML"
      to: "DESIGN.md ## v9.0 Anti-Patterns body section"
      via: "machine-readable + human-rationale parity"
      pattern: "antiPatterns:"
---

<objective>
Add the v9.0 design contract additions to `DESIGN.md`: the z-index contract, the CTA opaque-forever rule with its 7-component master list, and the v9.0 anti-pattern appendix in BOTH machine-readable YAML form AND human-readable markdown body form (Decision D — both formats).

Purpose: Phases 91-94 planners will grep DESIGN.md before generating tasks. The CTA opaque-forever master list is the documentation half of the T-90-02 mitigation (Plan 02 carries the CSS-comment half). The 15-entry anti-pattern list codifies TZ §16 and project-history failure modes (mobile blur >12px regression, >2 glass per viewport, cheat-pass on a11y).

Output:
- DESIGN.md YAML: NEW top-level `antiPatterns:` key with 15 entries.
- DESIGN.md body: NEW `## v9.0 Custom Rules` section (z-index contract + CTA opaque-forever 7-component list).
- DESIGN.md body: NEW `## v9.0 Anti-Patterns` section with 15 numbered entries (each: name, why, where it manifests, what to do instead).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@DESIGN.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-RESEARCH.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-PATTERNS.md
@.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-VALIDATION.md
@design/LIQUID_GLASS_BLOB_TZ.md

<interfaces>
<!-- The 15 anti-pattern entries from CONTEXT.md Decision D — exact content to use. -->

11 entries from TZ §16 reject list:
  1. Multiple blobs — TZ §1 single-protagonist rule; multiple blobs read as decoration not protagonist
  2. Cursor trail (N copies) — TZ §14 explicit ban; trails read as gaming/playful, violate medical tone
  3. Per-frame React useState from pointer events — TZ §16; consume rAF + CSS vars instead
  4. Solid-white / milky glass fills above 0.16 — TZ §9; breaks transparent register
  5. Green tint on cards — TZ §9; brand parity violation; tint must come from blob behind glass
  6. Animated backdrop-filter blur values — TZ §16; GPU-poison on mobile
  7. Animated box-shadow on dozens of elements — TZ §16; layer-thrash
  8. mix-blend-mode on glass surfaces — TZ §16; unpredictable contrast under varied backgrounds
  9. Spring physics with overshoot — tone violation; medical tone is calm not playful
  10. Heat-driven color shift toward red/orange — brand parity violation; brand greens only
  11. backdrop-filter on .living-blob-field itself — blob is BEHIND glass, not glass

4 from project history:
  12. Mobile blur >12px — DESIGN.md hard constraint; regressed historically (Phase 79 set the cap)
  13. >2 glass layers per viewport — DESIGN.md hard constraint; mobile-first ЦА 45+ readability
  14. Adding new glass class without registering it in liquid-glass.css @a11y-layer-coverage block — Phase 89 cheat-pass failure mode
  15. Cheat-passing a11y verification (declaring rules without live OS-toggle test) — Phase 89 ACC-01..05 lesson

CTA opaque-forever master list (7 components, from CONTEXT.md Decision C):
  1. HeroHub primary CTA (заявка на консультацию)
  2. FinalCTA primary submit
  3. ContactForm submit button
  4. StickyBar primary action (mobile sticky CTA)
  5. Header phone CTA (where rendered with gradient)
  6. Service-page LeadFormSection submit
  7. Any future component using .btn-primary or v6 gradient utility classes (from-mu-cta-from-v6, to-mu-cta-to-v6)

Z-index contract (FND-04):
  - .living-blob-field: z-0
  - <main> content: z-1..10
  - Header / StickyBar: z-50+
  - Modals / dialogs: z-100+
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append antiPatterns: YAML key to DESIGN.md front matter</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (read full YAML front matter — locate closing `---` of front matter)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md (Decision D — 15-entry list, both formats; Folded scope clarifications)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-PATTERNS.md (`DESIGN.md (MD-design-doc, modify YAML + body)` — frozen-range warnings)
  </read_first>
  <action>
Inside DESIGN.md YAML front matter, AFTER the existing top-level keys (`colors:`, `glass:` from Plan 02, `typography:`, `rounded:`, `spacing:`, `shapes:`, `components:`) and BEFORE the closing `---` of the front matter, insert a new top-level `antiPatterns:` key:

```yaml

# v9.0 Phase 90 (FND-05) — anti-pattern register; full rationale in body section "## v9.0 Anti-Patterns"
antiPatterns:
  - name: "Multiple blobs"
    why: "TZ §1 single-protagonist rule; multiple blobs read as decoration not protagonist"
    addedIn: "v9.0 Phase 90"
  - name: "Cursor trail (N copies)"
    why: "TZ §14 explicit ban; trails read as gaming/playful, violate medical tone"
    addedIn: "v9.0 Phase 90"
  - name: "Per-frame React useState from pointer events"
    why: "TZ §16; consume rAF + CSS vars instead of state"
    addedIn: "v9.0 Phase 90"
  - name: "Solid-white / milky glass fills above 0.16"
    why: "TZ §9; breaks the transparent register the blob is meant to inhabit"
    addedIn: "v9.0 Phase 90"
  - name: "Green tint on cards"
    why: "TZ §9; brand parity violation; tint must come from blob behind glass, not from card surface"
    addedIn: "v9.0 Phase 90"
  - name: "Animated backdrop-filter blur values"
    why: "TZ §16; GPU-poison on mobile, especially Safari iOS"
    addedIn: "v9.0 Phase 90"
  - name: "Animated box-shadow on dozens of elements"
    why: "TZ §16; layer-thrash on mid-scroll"
    addedIn: "v9.0 Phase 90"
  - name: "mix-blend-mode on glass surfaces"
    why: "TZ §16; unpredictable contrast under varied backgrounds"
    addedIn: "v9.0 Phase 90"
  - name: "Spring physics with overshoot"
    why: "Tone violation; medical tone is calm and confident, not playful"
    addedIn: "v9.0 Phase 90"
  - name: "Heat-driven color shift toward red/orange"
    why: "Brand parity violation; v9.0 stays within brand greens (TZ §5)"
    addedIn: "v9.0 Phase 90"
  - name: "backdrop-filter on .living-blob-field itself"
    why: "Blob is BEHIND glass, not glass; applying backdrop-filter inverts the optical model"
    addedIn: "v9.0 Phase 90"
  - name: "Mobile blur >12px"
    why: "DESIGN.md hard constraint (Phase 79 cap); regressed historically; GPU + readability budget"
    addedIn: "v9.0 Phase 90"
  - name: ">2 glass layers per viewport"
    why: "DESIGN.md hard constraint; mobile-first ЦА 45+ readability + GPU budget"
    addedIn: "v9.0 Phase 90"
  - name: "Adding a new glass class without registering it in liquid-glass.css @a11y-layer-coverage block"
    why: "Phase 89 cheat-pass failure mode; coverage grep MUST find the new class between markers"
    addedIn: "v9.0 Phase 90"
  - name: "Cheat-passing a11y verification (declaring rules without live OS-toggle test)"
    why: "Phase 89 ACC-01..05 lesson; v9.0 Phase 94 forbids cheat-pass — real-device UAT required"
    addedIn: "v9.0 Phase 90"
```

Verify count: exactly 15 `- name:` entries inside the new `antiPatterns:` block.

DO NOT modify any other YAML key. DO NOT touch the `---` delimiters.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -qE '^antiPatterns:' DESIGN.md && [ "$(awk '/^antiPatterns:/{flag=1;next} /^[a-z]+:/{flag=0} flag' DESIGN.md | grep -c '^  - name:')" = "15" ] && [ "$(awk '/^antiPatterns:/{flag=1;next} /^[a-z]+:/{flag=0} flag' DESIGN.md | grep -c '^    why:')" = "15" ] && [ "$(awk '/^antiPatterns:/{flag=1;next} /^[a-z]+:/{flag=0} flag' DESIGN.md | grep -c '^    addedIn: "v9.0 Phase 90"')" = "15" ] && echo OK</automated>
  </verify>
  <done>
    - Top-level `antiPatterns:` key present in YAML front matter.
    - Exactly 15 entries; each has `name`, `why`, `addedIn` fields.
    - All `addedIn` values equal `"v9.0 Phase 90"`.
    - No other YAML key was modified.
  </done>
  <acceptance_criteria>
    - grep -E '^antiPatterns:' DESIGN.md → ≥1 match
    - awk '/^antiPatterns:/{flag=1;next} /^[a-z]+:/{flag=0} flag' DESIGN.md | grep -c '^  - name:' → 15
    - awk '/^antiPatterns:/{flag=1;next} /^[a-z]+:/{flag=0} flag' DESIGN.md | grep -c '^    why:' → 15
    - awk '/^antiPatterns:/{flag=1;next} /^[a-z]+:/{flag=0} flag' DESIGN.md | grep -c '^    addedIn: "v9.0 Phase 90"' → 15
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append `## v9.0 Custom Rules` section to DESIGN.md body (z-index contract + CTA opaque-forever 7-component list)</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (read body — last few sections to find the natural append point at the end of the document, before any trailing references/footer)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md (Decision C — CTA 7-component list verbatim; Folded scope: z-index contract codified verbatim from REQUIREMENTS.md FND-04)
    - .planning/REQUIREMENTS.md (FND-04 line — z-index integers)
  </read_first>
  <action>
Append the following new markdown section at the END of DESIGN.md body (after the last existing section, before any reference list / footer if present — otherwise simply at end-of-file). Use a top-level `## v9.0 Custom Rules` heading:

```markdown

## v9.0 Custom Rules

Added 2026-04-30 (Phase 90, FND-04 + FND-05). Reference: `.planning/REQUIREMENTS.md` v9.0 section, `design/LIQUID_GLASS_BLOB_TZ.md` §1, §5, §9, §11, §16, §17.

### Z-index contract (FND-04)

The v9.0 living-blob composition has a fixed z-stacking order. Every layer in the app MUST fit one of these bands:

| Layer                                      | z-index   | Notes |
|--------------------------------------------|-----------|-------|
| `.living-blob-field` (blob field)          | `z-0`     | Always behind everything; `position: fixed; inset: 0`. Single instance, mounted in `layout.tsx`. |
| `<main>` content + glass surfaces          | `z-1..10` | Page-content cards, sections, forms. Existing `<main className="relative z-10">` is the anchor. |
| `Header`, `StickyBar`, sticky chrome       | `z-50+`   | Always-visible chrome rides above content; below modals. |
| Modals, dialogs, popovers                  | `z-100+`  | Top of stack; backdrop sits above all other UI. |

Any new component MUST declare its `z-index` (or use a Tailwind utility like `z-0`, `z-10`, `z-50`, `z-[100]`) within these bands. Numbers outside these bands require a Key Decision in `.planning/PROJECT.md`.

### CTA opaque-forever rule (FND-05)

The v6 CTA gradient (`from-mu-cta-from-v6 → to-mu-cta-to-v6`, brand greens) is the page's primary conversion signal. It MUST stay fully opaque under every condition — no `backdrop-filter`, no `bg-white/{n}` glass utilities, no transparency from the v9.0 4-tier glass system. Rationale: `.planning/research/PITFALLS.md` §3.1 — CTA disappears into the green blob ambient when glass is applied.

**Master list of CTA-opaque components (every consumer of the v6 gradient or `.btn-primary`):**

1. `HeroHub` primary CTA (заявка на консультацию)
2. `FinalCTA` primary submit
3. `ContactForm` submit button
4. `StickyBar` primary action (mobile sticky CTA)
5. `Header` phone CTA (where rendered with gradient)
6. Service-page `LeadFormSection` submit
7. Any future component using `.btn-primary` or v6 gradient utility classes (`from-mu-cta-from-v6`, `to-mu-cta-to-v6`)

**Enforcement (Phase 90):**
- `next/src/styles/liquid-glass.css` `@a11y-layer-coverage:start/end` block carries a comment block documenting the rule and excludes `.btn-primary` / `.liquid-btn-primary` from a11y enumeration.
- `next/src/styles/blob.css` does NOT define any CTA-related class.
- Phase 92 (Glass Rework) will physically verify each component matches the rule during the glass sweep.
- Stylelint / ESLint enforcement is deferred to v9.1+ (per CONTEXT.md `<deferred>`).

### Source artifacts

- TZ §1 (single-protagonist), §5 (palette), §9 (alpha tiers), §11 (mobile compensation), §16 (anti-patterns), §17 (heat timing): `design/LIQUID_GLASS_BLOB_TZ.md`
- Phase 90 locked decisions: `.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md`
- Mobile blur cap (Phase 79 hard constraint, ≤12px): `.planning/research/PITFALLS.md` §1.1
- Cheat-pass failure mode (Phase 89 ACC-01..05): `.planning/phases/89-milestone-closeout/89-VERIFICATION.md`
```

DO NOT modify any existing section above this insertion point.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -qE '^## v9\.0 Custom Rules' DESIGN.md && [ "$(awk '/^## v9.0 Custom Rules/,/^## /' DESIGN.md | head -n -1 | grep -cE '^### (Z-index contract|CTA opaque-forever rule|Source artifacts)')" -ge "3" ] && [ "$(awk '/^### Z-index contract/,/^### /' DESIGN.md | head -n -1 | grep -oE 'z-(0|10|50|100)' | sort -u | wc -l)" -ge "4" ] && [ "$(awk '/^### CTA opaque-forever rule/,/^### /' DESIGN.md | head -n -1 | grep -cE '^[0-9]+\. ')" -ge "7" ] && echo OK</automated>
  </verify>
  <done>
    - `## v9.0 Custom Rules` heading appears once in DESIGN.md.
    - Three subsections: `### Z-index contract`, `### CTA opaque-forever rule`, `### Source artifacts`.
    - Z-index table contains all 4 z-index values: z-0, z-10, z-50, z-100.
    - CTA opaque-forever master list contains ≥7 numbered entries.
    - No existing section modified.
  </done>
  <acceptance_criteria>
    - grep -E '^## v9\.0 Custom Rules' DESIGN.md → ≥1 match
    - awk '/^## v9.0 Custom Rules/,/^## /' DESIGN.md | grep -cE '^### (Z-index contract|CTA opaque-forever rule|Source artifacts)' → ≥3
    - awk '/^### Z-index contract/,/^### /' DESIGN.md | grep -oE 'z-(0|10|50|100)' | sort -u | wc -l → ≥4
    - awk '/^### CTA opaque-forever rule/,/^### /' DESIGN.md | grep -cE '^[0-9]+\. ' → ≥7
    - awk '/^### CTA opaque-forever rule/,/^### /' DESIGN.md | grep -cE '(HeroHub|FinalCTA|ContactForm|StickyBar|Header|LeadFormSection|btn-primary)' → ≥7
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Append `## v9.0 Anti-Patterns` body section with 15 expanded entries</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (locate end of `## v9.0 Custom Rules` section from Task 2 — append immediately after it)
    - .planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-CONTEXT.md (Decision D — 15 entries; format: name, why, where it manifests, what to do instead)
    - design/LIQUID_GLASS_BLOB_TZ.md (§16 anti-pattern reject list — source-of-truth for entries 1-11)
  </read_first>
  <action>
Append the following new markdown section AFTER the `## v9.0 Custom Rules` section (added in Task 2), at end of DESIGN.md:

```markdown

## v9.0 Anti-Patterns

Added 2026-04-30 (Phase 90, FND-05). Machine-readable mirror in YAML front matter `antiPatterns:` field. Each entry expands the YAML with: where it would manifest, what to do instead.

Phase 91-94 planners MUST grep this section before generating tasks; any task that would violate an entry must be rewritten.

1. **Multiple blobs.** Why: TZ §1 single-protagonist rule; multiple blobs read as decoration not protagonist. Where it manifests: tempting to add per-section accent blobs in `*Section.tsx` components or duplicate `.living-blob-field` per route. Do instead: single `.living-blob-field` in `layout.tsx`; section accents come via heat-leak `radial-gradient` on glass cards (Phase 92).

2. **Cursor trail (N copies).** Why: TZ §14 explicit ban; trails read as gaming/playful, violate medical tone. Where it manifests: tempting to render N afterimages as the blob lerps. Do instead: 4 sublayers (core/body/halo/glint) with different lerp factors deliver the impression of a trail without the playful read.

3. **Per-frame React useState from pointer events.** Why: TZ §16; consumes the React tree on every pointer move; causes scroll/INP regressions. Where it manifests: `onPointerMove` calling `setState` for blob position. Do instead: write directly to `document.documentElement.style.setProperty('--blob-x', ...)` inside a single `pointermove` listener and a single rAF loop; CSS consumes the var (Phase 91).

4. **Solid-white / milky glass fills above 0.16.** Why: TZ §9; breaks the transparent register the blob is meant to inhabit. Where it manifests: copying pre-v9.0 `--liquid-bg: rgba(255,255,255,0.42)` into new components. Do instead: use the v9.0 `--glass-{section,card,form,button}-fill` tier tokens (≤0.16 desktop / ≤0.18 mobile) introduced in Phase 90 (FND-02).

5. **Green tint on cards.** Why: TZ §9; brand parity violation; tint must come from blob behind glass. Where it manifests: applying `bg-mu-green-100/{n}` directly to a card. Do instead: leave card surface neutral; the `.liquid-card` heat-leak `radial-gradient(... at var(--blob-x) var(--blob-y), ...)` (Phase 92) tints the surface optically when the blob is behind it.

6. **Animated `backdrop-filter` blur values.** Why: TZ §16; GPU-poison on mobile, especially Safari iOS. Where it manifests: tempting to ramp `backdrop-filter: blur(N)` from 8px to 24px on hover. Do instead: keep `backdrop-filter` value static; animate `opacity` or `transform` instead.

7. **Animated `box-shadow` on dozens of elements.** Why: TZ §16; layer-thrash on mid-scroll. Where it manifests: card hover effects with `box-shadow` transitions across many cards simultaneously. Do instead: animate `transform: scale()` or `opacity`; use `box-shadow` as a static accent only.

8. **`mix-blend-mode` on glass surfaces.** Why: TZ §16; unpredictable contrast under varied backgrounds (especially with the moving blob). Where it manifests: copy-paste from `MeshBackground.tsx` (deleted in Phase 90). Do instead: use solid alpha-channel colors; rely on the heat-leak `radial-gradient` for compositional response.

9. **Spring physics with overshoot.** Why: tone violation; medical tone is calm and confident, not playful. Where it manifests: framer-motion springs with `stiffness > 200, damping < 15`. Do instead: lerp toward target each frame (linear interpolation, no overshoot — Phase 91 BLOB-02).

10. **Heat-driven color shift toward red/orange.** Why: brand parity violation; v9.0 stays within brand greens (TZ §5). Where it manifests: tempting to drive `--blob-heat` toward red/orange to signal "high engagement." Do instead: let `--blob-hot: #4FE098` (KD-v9-001) be the only "hot" color — slightly cooler/brighter green than `--blob-core: #35B678`.

11. **`backdrop-filter` on `.living-blob-field` itself.** Why: blob is BEHIND glass, not glass; applying `backdrop-filter` inverts the optical model. Where it manifests: copying glass-component CSS to the blob field. Do instead: `.living-blob-field` is `position: fixed; z-index: 0; pointer-events: none`. NO `backdrop-filter`. Verified by `grep -A 30 '.living-blob-field {' next/src/styles/blob.css | grep -v backdrop-filter`.

12. **Mobile blur >12px.** Why: `DESIGN.md` hard constraint (Phase 79 cap); regressed historically. Where it manifests: hardcoded `filter: blur(40px)` or `backdrop-filter: blur(24px)` without a `clamp(12px, …)` mobile floor. Do instead: every blur value uses `clamp(12px, fluid-vw, desktop-ceiling)`; mobile media queries cap `filter: blur` at 12px (see `next/src/styles/blob.css` Phase 90 mobile media query).

13. **>2 glass layers per viewport.** Why: `DESIGN.md` hard constraint; mobile-first ЦА 45+ readability + GPU budget. Where it manifests: nested glass cards inside a glass section inside a glass container. Do instead: respect responsive-glass-nesting (mobile = 1 wrapper, desktop = N cards). Phase 82 `StatsBar` is the reference implementation.

14. **Adding a new glass class without registering it in `liquid-glass.css` `@a11y-layer-coverage` block.** Why: Phase 89 cheat-pass failure mode; coverage grep MUST find the new class between markers. Where it manifests: shipping a new `.glass-foo` utility for one component, forgetting to enumerate it under `prefers-reduced-motion` / `-transparency` / `prefers-contrast: more`. Do instead: every new `.liquid-*`, `.glass-*`, `.blob-*`, `.stats-*`, `.living-blob-*` class MUST be appended to all 3 `@media` blocks inside the `@a11y-layer-coverage:start/end` markers (CTA classes excepted: `.btn-primary`, `.liquid-btn-primary`).

15. **Cheat-passing a11y verification (declaring rules without live OS-toggle test).** Why: Phase 89 ACC-01..05 lesson; v9.0 Phase 94 forbids cheat-pass — real-device UAT required. Where it manifests: marking ACC-* tasks complete because the CSS block exists, without toggling macOS / iOS accessibility settings and reloading the page. Do instead: Phase 94 hard gate — record screenshot/video evidence of `prefers-reduced-motion` / `-transparency` / `prefers-contrast: more` actually applied; phase blocked without artifacts.
```

DO NOT modify any other section.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -qE '^## v9\.0 Anti-Patterns' DESIGN.md && [ "$(awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -cE '^[0-9]+\. \*\*')" -ge "15" ] && awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -qE '^11\. \*\*.*backdrop-filter on .*\.living-blob-field' && awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -qE '^12\. \*\*Mobile blur >12px' && awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -qE '^14\. \*\*Adding a new glass class' && echo OK</automated>
  </verify>
  <done>
    - `## v9.0 Anti-Patterns` heading appears once.
    - Section contains ≥15 numbered entries formatted as `N. **Name.**`.
    - Entries 11, 12, 14 (key project-history items) verified by content match.
    - YAML `antiPatterns:` field count (15, from Task 1) matches body entry count (15).
  </done>
  <acceptance_criteria>
    - grep -E '^## v9\.0 Anti-Patterns' DESIGN.md → ≥1 match
    - awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -cE '^[0-9]+\. \*\*' → ≥15
    - YAML count == body count: `awk '/^antiPatterns:/{flag=1;next} /^[a-z]+:/{flag=0} flag' DESIGN.md | grep -c '^  - name:'` equals `awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -cE '^[0-9]+\. \*\*'` → both = 15
    - awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -E '^11\. \*\*.*backdrop-filter on .*\.living-blob-field' → ≥1
    - awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -E '^12\. \*\*Mobile blur >12px' → ≥1
    - awk '/^## v9.0 Anti-Patterns/,0' DESIGN.md | grep -E '^14\. \*\*Adding a new glass class' → ≥1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none direct) | Documentation-only changes to DESIGN.md. No runtime code, no user input, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02 (documentation half) | Conversion-loss / CTA invisibility regression | Future plans (Phase 92, 93) might apply glass utilities to CTA components, making them disappear into the green blob ambient. Severity HIGH for conversion. | mitigate (documentation) | DESIGN.md `## v9.0 Custom Rules > CTA opaque-forever rule` enumerates all 7 components and reaffirms the no-`backdrop-filter` / no-`bg-white/{n}` rule. Plan 02 carries the CSS-comment half (in `liquid-glass.css` `@a11y-layer-coverage` block). Phase 92 will physically verify each component. |
| T-90-01 (knowledge half) | A11y bypass | Future plans might add a new glass class without registering it in the a11y `@layer` block. | mitigate (documentation) | Anti-pattern entry #14 codifies the rule; Phase 94 verification grep enforces it. |

**Note:** No runtime code; no XSS / SSRF / injection / authn / authz / crypto threats apply. Documentation changes do not introduce technical attack surface but DO reduce conversion-loss and a11y-regression risk for downstream phases.
</threat_model>

<verification>
- DESIGN.md YAML has top-level `antiPatterns:` key with exactly 15 entries (each with `name`, `why`, `addedIn`).
- DESIGN.md body has `## v9.0 Custom Rules` section with `### Z-index contract`, `### CTA opaque-forever rule`, `### Source artifacts` subsections.
- DESIGN.md body has `## v9.0 Anti-Patterns` section with ≥15 numbered entries (`N. **Name.**`).
- YAML count and body count both equal 15.
- No existing DESIGN.md content modified — `git diff DESIGN.md` shows only appended sections.
</verification>

<success_criteria>
1. DESIGN.md YAML `antiPatterns:` field has 15 entries with correct shape.
2. DESIGN.md body `## v9.0 Custom Rules` documents z-index contract (4 bands) and CTA opaque-forever 7-component master list.
3. DESIGN.md body `## v9.0 Anti-Patterns` has 15 numbered entries with name + why + where it manifests + what to do instead.
4. YAML and body anti-pattern counts both equal 15.
5. No existing colors/typography/rounded/spacing/shapes/components keys modified; no existing markdown sections modified.
</success_criteria>

<output>
After completion, create `.planning/phases/90-foundation-tokens-a11y-wiring-dom-skeleton/90-03-SUMMARY.md` per `templates/summary.md`. Note: (a) line ranges of the inserted YAML key and the two new body sections, (b) confirmation that YAML count == body count (== 15), (c) any deviations from the verbatim 15-entry list (there should be none).
</output>
