---
phase: 14-visual-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
autonomous: true
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "Country flags in doctors section are rendered as colored SVG rectangles, not emoji characters"
    - "Знакомо section displays 3 icon+text cards in a horizontal grid on desktop"
    - "Problem section icons use the same duotone style as existing benefit icons"
    - "Both sections render consistently across browsers (no emoji font dependency)"
  artifacts:
    - path: "index.html"
      provides: "Inline SVG flags replacing emoji, icon-card markup for problem section"
      contains: "doctors__flag-svg"
    - path: "css/styles.css"
      provides: "Flag SVG sizing, problem card grid layout"
      contains: "problem__card"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes"
      pattern: "problem__card|doctors__flag-svg"
---

<objective>
Replace emoji country flags with inline SVG flags in the doctors section and redesign the "Знакомо?" section from bordered text blocks to compact icon+text cards.

Purpose: Eliminate cross-browser emoji inconsistency and create a visually polished, icon-driven layout matching the established duotone design language.
Output: Updated index.html with SVG flags and icon cards; updated styles.css with new layout rules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-visual-consistency/14-CONTEXT.md
@index.html
@css/styles.css
</context>

<interfaces>
<!-- Existing CSS patterns to follow -->

From css/styles.css:
```css
/* Duotone icon pattern */
.icon { display: inline-flex; align-items: center; justify-content: center; flex-shrink: 0; }
.icon svg { width: var(--icon-size-md); height: var(--icon-size-md); } /* 48px */

/* Card pattern */
.card { background-color: var(--color-white); border-radius: var(--radius-lg); box-shadow: var(--shadow-md); padding: var(--space-4); }

/* Duotone SVG colors: stroke="#38C6F4" fill="rgba(56,198,244,0.1)" */

/* Existing flag container */
.doctors__flag { font-size: 2.5rem; line-height: 1; }

/* Existing problem section */
.problem__text { font-size: var(--font-size-lg); padding-left: var(--space-3); border-left: 3px solid var(--color-primary); }
```

From index.html (lines 158-165) -- current problem section:
```html
<section class="section problem" id="problem">
  <div class="container">
    <h2 class="problem__heading">Знакомо?</h2>
    <div class="problem__items">
      <p class="problem__text">Получили диагноз... (3 text blocks with border-left)</p>
    </div>
  </div>
</section>
```

From index.html (lines 272-308) -- current doctors grid:
```html
<div class="doctors__grid">
  <div class="card doctors__card">
    <div class="doctors__flag" aria-hidden="true">&#x1F1E9;&#x1F1EA;</div>
    <h3 class="card__title">Германия</h3>
    ...
  </div>
  <!-- 7 cards total with emoji flags -->
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace emoji flags with inline SVG flags (VIS-01)</name>
  <files>index.html, css/styles.css</files>
  <read_first>
    - index.html (lines 267-310 -- doctors section with emoji flags)
    - css/styles.css (lines 653-713 -- doctors CSS including .doctors__flag)
  </read_first>
  <action>
In index.html, replace each of the 7 `.doctors__flag` emoji contents with an inline SVG flag. Keep the `.doctors__flag` wrapper div but add class `doctors__flag-svg` alongside it for SVG-specific sizing.

Each SVG flag: `viewBox="0 0 48 32"` (3:2 ratio), simplified 2-3 stripe design using real flag colors. Use `rx="3"` on the outer rect for slightly rounded corners.

**Germany** (replace `&#x1F1E9;&#x1F1EA;`):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 32" aria-hidden="true"><rect width="48" height="32" rx="3" fill="#000"/><rect y="11" width="48" height="10" fill="#DD0000"/><rect y="21" width="48" height="11" rx="0 0 3 3" fill="#FFCC00"/></svg>
```

**Israel** (replace `&#x1F1EE;&#x1F1F1;`):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 32" aria-hidden="true"><rect width="48" height="32" rx="3" fill="#fff"/><rect y="4" width="48" height="4" fill="#0038B8"/><rect y="24" width="48" height="4" fill="#0038B8"/><polygon points="24,9 27.5,18 20.5,18" fill="none" stroke="#0038B8" stroke-width="1.5"/><polygon points="24,23 20.5,14 27.5,14" fill="none" stroke="#0038B8" stroke-width="1.5"/></svg>
```

**Switzerland** (replace `&#x1F1E8;&#x1F1ED;`):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 32" aria-hidden="true"><rect width="48" height="32" rx="3" fill="#DA291C"/><rect x="20" y="8" width="8" height="16" fill="#fff"/><rect x="16" y="12" width="16" height="8" fill="#fff"/></svg>
```

**Austria** (replace `&#x1F1E6;&#x1F1F9;`):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 32" aria-hidden="true"><rect width="48" height="32" rx="3" fill="#ED2939"/><rect y="11" width="48" height="10" fill="#fff"/></svg>
```

**UAE** (replace `&#x1F1E6;&#x1F1EA;`):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 32" aria-hidden="true"><rect width="48" height="32" rx="3" fill="#000"/><rect x="12" width="36" height="11" fill="#00732F"/><rect x="12" y="11" width="36" height="10" fill="#fff"/><rect x="12" y="21" width="36" height="11" fill="#000"/><rect width="12" height="32" rx="3 0 0 3" fill="#FF0000"/></svg>
```

**South Korea** (replace `&#x1F1F0;&#x1F1F7;`):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 32" aria-hidden="true"><rect width="48" height="32" rx="3" fill="#fff"/><circle cx="24" cy="16" r="8" fill="#CD2E3A"/><path d="M24 16c0-4.4 3.6-8 3.6-8s-3.6 3.6-3.6 8 3.6 8 3.6 8-3.6-3.6-3.6-8z" fill="#0047A0"/><ellipse cx="24" cy="16" rx="8" ry="8" fill="none" stroke="none"/><path d="M16 16a8 8 0 0 0 16 0" fill="#0047A0"/></svg>
```

**Turkey** (replace `&#x1F1F9;&#x1F1F7;`):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 32" aria-hidden="true"><rect width="48" height="32" rx="3" fill="#E30A17"/><circle cx="19" cy="16" r="8" fill="#fff"/><circle cx="21.5" cy="16" r="6.5" fill="#E30A17"/><polygon points="27,16 30,13.5 28,16 30,18.5" fill="#fff"/></svg>
```

In css/styles.css, update the `.doctors__flag` rule. Replace the existing rule (lines 683-686):
```css
.doctors__flag {
  width: 48px;
  height: 32px;
  flex-shrink: 0;
  margin-bottom: var(--space-1);
}

.doctors__flag svg {
  width: 100%;
  height: 100%;
  display: block;
}
```

Remove `font-size: 2.5rem;` and `line-height: 1;` from `.doctors__flag` since emoji text sizing is no longer needed.
  </action>
  <verify>
    <automated>grep -c "doctors__flag.*svg\|viewBox.*48 32" /Users/mikhail/Projects/Medicus_video_consult-landing/index.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - grep "viewBox" index.html returns at least 7 matches within the doctors section (lines 267-310)
    - grep "&#x1F1" index.html returns 0 matches (no emoji flag codes remain)
    - grep "doctors__flag" css/styles.css shows width: 48px and height: 32px (not font-size)
    - grep "doctors__flag svg" css/styles.css shows width: 100% rule exists
  </acceptance_criteria>
  <done>All 7 emoji flags replaced with inline SVG flag graphics. CSS updated for SVG sizing. No emoji character codes remain in the doctors section.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign problem section with icon+text cards (VIS-02)</name>
  <files>index.html, css/styles.css</files>
  <read_first>
    - index.html (lines 158-166 -- problem section)
    - css/styles.css (lines 545-571 -- problem section CSS)
    - css/styles.css (lines 291-337 -- card and icon patterns for reference)
  </read_first>
  <action>
In index.html, replace the `.problem__items` div and its 3 `.problem__text` children (lines 161-165) with a grid of 3 icon+text cards. Use BEM naming consistent with existing patterns.

Replace with:
```html
<div class="problem__grid">
  <div class="card problem__card">
    <div class="problem__icon icon" aria-hidden="true">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
        <circle cx="20" cy="20" r="14" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
        <line x1="30" y1="30" x2="42" y2="42" stroke="#38C6F4" stroke-width="2.5" stroke-linecap="round"/>
        <line x1="14" y1="20" x2="26" y2="20" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
        <line x1="20" y1="14" x2="20" y2="26" stroke="#38C6F4" stroke-width="2" stroke-linecap="round"/>
      </svg>
    </div>
    <h3 class="card__title">Сомнения в&nbsp;диагнозе</h3>
    <p class="card__text">Получили диагноз&nbsp;&mdash; и&nbsp;не&nbsp;уверены, что он&nbsp;правильный. Разные врачи говорят разное.</p>
  </div>
  <div class="card problem__card">
    <div class="problem__icon icon" aria-hidden="true">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
        <circle cx="24" cy="24" r="18" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
        <ellipse cx="24" cy="24" rx="8" ry="18" stroke="#38C6F4" stroke-width="1.5" fill="none"/>
        <line x1="6" y1="16" x2="42" y2="16" stroke="#38C6F4" stroke-width="1.5" stroke-linecap="round"/>
        <line x1="6" y1="32" x2="42" y2="32" stroke="#38C6F4" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
    </div>
    <h3 class="card__title">Лечение за&nbsp;рубежом</h3>
    <p class="card__text">Слышали, что за&nbsp;границей лечат лучше&nbsp;&mdash; но&nbsp;лететь дорого, долго и&nbsp;страшно.</p>
  </div>
  <div class="card problem__card">
    <div class="problem__icon icon" aria-hidden="true">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" fill="none">
        <circle cx="24" cy="24" r="18" stroke="#38C6F4" stroke-width="2.5" fill="rgba(56,198,244,0.1)"/>
        <polyline points="24,12 24,24 32,28" stroke="#38C6F4" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
      </svg>
    </div>
    <h3 class="card__title">Время уходит</h3>
    <p class="card__text">Время идёт&nbsp;&mdash; а&nbsp;решение всё ещё не&nbsp;принято. Нужна ясность&nbsp;&mdash; сейчас.</p>
  </div>
</div>
```

Icons used (all duotone, `stroke="#38C6F4"`, `fill="rgba(56,198,244,0.1)"`):
1. **Magnifying glass with plus** -- diagnosis uncertainty (extends existing magnifier icon with + crosshair)
2. **Globe** -- foreign doctors/treatment abroad (circle with meridians and parallels)
3. **Clock** -- time pressure (circle with clock hands)

In css/styles.css, replace the problem section CSS (lines 547-571) with:

```css
.problem {
  background-color: var(--color-white);
}

.problem__heading {
  margin-bottom: var(--space-5);
}

.problem__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-3);
}

.problem__card {
  text-align: center;
}

.problem__icon {
  margin-bottom: var(--space-2);
}

@media (min-width: 768px) {
  .problem__grid {
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
  }
}
```

Remove the old `.problem__items`, `.problem__text`, and `.problem__text:last-child` rules entirely since those elements no longer exist.

Place the `@media (min-width: 768px)` rule for `.problem__grid` inside the existing 768px media query block if one exists nearby, or add it inline after the base rules.
  </action>
  <verify>
    <automated>grep -c "problem__card\|problem__grid\|problem__icon" /Users/mikhail/Projects/Medicus_video_consult-landing/index.html</automated>
  </verify>
  <acceptance_criteria>
    - grep "problem__grid" index.html returns at least 1 match
    - grep "problem__card" index.html returns at least 3 matches (3 cards)
    - grep "problem__icon" index.html returns at least 3 matches (3 icons)
    - grep "problem__text" index.html returns 0 matches (old markup removed)
    - grep "problem__items" index.html returns 0 matches (old wrapper removed)
    - grep "problem__grid" css/styles.css returns at least 1 match (grid layout defined)
    - grep "problem__card" css/styles.css returns at least 1 match (card styling defined)
    - grep "38C6F4" index.html shows duotone icon colors in problem section SVGs
  </acceptance_criteria>
  <done>Problem section redesigned from 3 bordered text blocks to 3 icon+text cards in a responsive grid. Icons follow established duotone SVG pattern. 3-column layout on desktop, single column on mobile.</done>
</task>

</tasks>

<verification>
1. Open index.html in browser -- doctors section shows colored flag rectangles (not emoji) for all 7 countries
2. Open index.html in browser -- "Знакомо?" section shows 3 cards with icons in a grid
3. Resize browser to mobile width -- problem cards stack to single column, flags remain visible
4. Check no emoji flag HTML entities remain: `grep "&#x1F1" index.html` returns 0 results
5. Check no old problem markup remains: `grep "problem__text\|problem__items" index.html` returns 0 results
</verification>

<success_criteria>
- All 7 emoji flags replaced with inline SVG flags using real country colors
- Problem section uses 3 icon+text cards with duotone SVG icons in a responsive grid
- Both changes follow existing BEM naming and duotone SVG conventions
- No regression in other sections (changes are scoped to .doctors__flag and .problem elements)
</success_criteria>

<output>
After completion, create `.planning/phases/14-visual-consistency/14-01-SUMMARY.md`
</output>
