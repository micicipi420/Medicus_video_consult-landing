---
phase: 72-service-pages
plan: 04
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - next/src/components/sections/checkup/CheckupAdvantages.tsx
  - next/src/components/sections/checkup/CheckupB2B.tsx
  - next/src/components/sections/checkup/CheckupProblem.tsx
  - next/src/components/sections/checkup/CheckupProcess.tsx
  - next/src/components/sections/checkup/CheckupProgramsKorea.tsx
  - next/src/components/sections/checkup/CheckupProgramsTurkey.tsx
  - next/src/components/sections/checkup/CheckupWhyUs.tsx
  - next/src/app/checkup/page.tsx
autonomous: true
requirements: [SRV-03]

must_haves:
  truths:
    - "/checkup page renders with glass design for all section components"
    - "Problem section shows 3 glass cards about why checkup matters"
    - "Advantages section shows 4 glass cards about world-class clinic benefits"
    - "WhyUs section shows 5 numbered glass cards about MedicusUnion advantages"
    - "ProgramsKorea section shows pricing glass cards with tier badges"
    - "ProgramsTurkey section shows pricing glass cards with included items"
    - "Process section shows numbered step cards"
    - "B2B section shows corporate checkup glass card"
  artifacts:
    - path: "next/src/components/sections/checkup/CheckupAdvantages.tsx"
      provides: "4 glass cards about world-class clinic advantages"
    - path: "next/src/components/sections/checkup/CheckupB2B.tsx"
      provides: "Corporate checkup glass card"
    - path: "next/src/components/sections/checkup/CheckupProblem.tsx"
      provides: "3 glass cards about why not wait for symptoms"
    - path: "next/src/components/sections/checkup/CheckupProcess.tsx"
      provides: "Numbered step cards for how it works"
    - path: "next/src/components/sections/checkup/CheckupProgramsKorea.tsx"
      provides: "Korean clinic pricing cards with tier badges"
    - path: "next/src/components/sections/checkup/CheckupProgramsTurkey.tsx"
      provides: "Turkish clinic pricing cards"
    - path: "next/src/components/sections/checkup/CheckupWhyUs.tsx"
      provides: "5 numbered cards about MedicusUnion platform"
    - path: "next/src/app/checkup/page.tsx"
      provides: "Updated page with correct glass design integration"
  key_links:
    - from: "next/src/app/checkup/page.tsx"
      to: "all 7 checkup section components"
      via: "import and JSX render"
      pattern: "import.*checkup/"
---

<objective>
Restyle all 7 checkup-specific section components and update page.tsx to match the new glass design from feat/new-design:checkup.html.

Purpose: The /checkup page describes health screening packages. It has the most content-rich sections (pricing tables with multiple tiers). All must use glass cards, gradient headings, and the new typography.
Output: 7 restyled section .tsx files + updated page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-service-pages/72-01-SUMMARY.md

Source of truth: `git show feat/new-design:checkup.html`
Key sections:
- Hero (lines 102-178): handled by shared ServiceHero
- Stats Bar (lines 180-202): handled by shared SocialProof
- Section 2 PROBLEM (lines 204-234): maps to CheckupProblem -- 3 glass cards
- Section 3 WHY ABROAD (lines 236-273): maps to CheckupAdvantages -- 4 glass cards
- Section 4 WHY MEDICUSUNION (lines 275-309): maps to CheckupWhyUs -- 5 numbered cards
- Section 5 PROGRAMS KOREA (lines 311-407): maps to CheckupProgramsKorea -- pricing cards with tiers
- Section 6 PROGRAMS TURKEY (lines 409-481): maps to CheckupProgramsTurkey -- pricing cards
- Section 7 HOW IT WORKS (lines 483-516): maps to CheckupProcess -- numbered step cards
- Section 8 B2B (lines 518-569): maps to CheckupB2B -- corporate checkup card
- FAQ, Form, CTA: handled by shared components

<interfaces>
Glass card patterns (same as Plans 02-03):
- Standard card: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
- Pricing card: `bg-white/60 backdrop-blur-2xl rounded-[3rem] p-8 border border-white/60 shadow-glass flex flex-col`
- Featured pricing card (highlighted): `bg-white/60 backdrop-blur-2xl rounded-[3rem] p-8 border border-mu-blue/40 shadow-[0_16px_48px_color-mix(in_oklch,var(--color-mu-blue)_15%,transparent)] flex flex-col`
- Tier badge (standard): `inline-flex items-center gap-2 bg-white/50 backdrop-blur-md border border-glass-border px-4 py-1.5 rounded-full shadow-sm w-fit mb-4` with `text-sm font-bold text-mu-accent-blue`
- Tier badge (featured): `inline-flex items-center gap-2 bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-4 py-1.5 rounded-full shadow-sm w-fit mb-4` with `text-sm font-bold`
- Price: `text-2xl font-extrabold bg-gradient-to-r from-mu-blue to-mu-accent-blue bg-clip-text text-transparent mb-3`
- Numbered gradient: `text-4xl font-extrabold bg-gradient-to-r from-mu-blue to-mu-accent-blue bg-clip-text text-transparent mb-4`
- Icon box: `w-14 h-14 bg-white/50 backdrop-blur-xl rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle all 7 checkup section components</name>
  <files>
    next/src/components/sections/checkup/CheckupAdvantages.tsx,
    next/src/components/sections/checkup/CheckupB2B.tsx,
    next/src/components/sections/checkup/CheckupProblem.tsx,
    next/src/components/sections/checkup/CheckupProcess.tsx,
    next/src/components/sections/checkup/CheckupProgramsKorea.tsx,
    next/src/components/sections/checkup/CheckupProgramsTurkey.tsx,
    next/src/components/sections/checkup/CheckupWhyUs.tsx
  </files>
  <action>
**read_first**: Read ALL 7 existing component files. Run `git show feat/new-design:checkup.html | sed -n '204,570p'` for the exact new design classes and content for sections 2-8.

**Global replacements (same as consultations/treatment):**
- Section wrapper: `py-12 lg:py-[6.25rem] bg-white` (or similar) -> `container mx-auto px-4 lg:px-6 mb-16` (or `section py-16 relative z-10` for sections using that pattern -- check source)
- For checkup sections, the source HTML uses `section class="section py-16 relative z-10"` with inner `container mx-auto px-4 lg:px-6`. Use this wrapper pattern: outer `<section className="py-16 relative z-10" id={id}>` with inner `<div className="container mx-auto px-4 lg:px-6">`.
- Remove inner container div if doubled
- h2 headings -> `text-4xl md:text-5xl font-extrabold text-center mb-4` (some with subtitle below, use mb-4 not mb-12 when subtitle exists). Heading text in gradient span. Some headings have `text-mu-text-900` prefix before gradient span -- match source.
- h3 headings -> `text-xl font-extrabold text-mu-text-900 mb-2`
- Body text -> `text-mu-text-700 font-medium`
- Card wrappers -> glass card classes

**Per-component specifics:**

1. **CheckupProblem.tsx** (Source Section 2: "Почему не стоит ждать симптомов", lines 204-234)
   - Heading: gradient span "Почему не стоит ждать симптомов"
   - Subtitle paragraph: `text-mu-text-700 text-lg text-center mb-12 max-w-3xl mx-auto leading-relaxed font-medium`
   - Grid: `grid md:grid-cols-3 gap-6`
   - 3 glass cards, each with icon box (`w-14 h-14 bg-white/50 backdrop-blur-xl rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`):
     - Card 1: "Я нормально себя чувствую" -- AlertCircle icon (circle with exclamation)
     - Card 2: "У меня нет времени" -- Clock icon
     - Card 3: "Не знаю, что проверять" -- HelpCircle icon (circle with question mark)
   - Keep existing SVG icons or replace with lucide-react equivalents. Use the exact content from source HTML.

2. **CheckupAdvantages.tsx** (Source Section 3: "Что даёт чек-ап в клинике мирового уровня", lines 236-273)
   - Heading: gradient span "Что даёт чек-ап в клинике мирового уровня"
   - Subtitle paragraph
   - Grid: `grid md:grid-cols-2 lg:grid-cols-4 gap-6`
   - 4 glass cards with icon boxes:
     - Card 1: "Оборудование последнего поколения" -- Monitor/Screen icon
     - Card 2: "Врачи с международной репутацией" -- User icon
     - Card 3: "Программы, а не отдельные анализы" -- ClipboardList icon
     - Card 4: "Скорость и точность" -- CheckCircle icon

3. **CheckupWhyUs.tsx** (Source Section 4: "Не просто посредник -- медицинская платформа", lines 275-309)
   - Heading: gradient span "Не просто посредник -- медицинская платформа"
   - Subtitle paragraph: long text about direct vs MedicusUnion
   - Grid: `grid md:grid-cols-2 lg:grid-cols-3 gap-6`
   - 5 glass cards (NOT 6 -- source has 5). Each with numbered gradient header:
     ```
     <div className="text-4xl font-extrabold bg-gradient-to-r from-mu-blue to-mu-accent-blue bg-clip-text text-transparent mb-4" aria-hidden="true">01</div>
     ```
     - Card 1: "Организация от А до Я"
     - Card 2: "Личный кабинет и приложение"
     - Card 3: "Страховочная сетка: 43 клиники, 11 стран"
     - Card 4: "Юридическая надёжность"
     - Card 5: "Два направления -- под ваш бюджет"

4. **CheckupProgramsKorea.tsx** (Source Section 5: "Чек-ап в Южной Корее", lines 311-407)
   - This is the largest/most complex component. Structure from source HTML:
     a. Heading: gradient "Чек-ап в Южной Корее"
     b. Blue info line: `text-mu-blue font-bold text-lg text-center mb-4` -- "Samsung Medical Center . Severance Hospital..."
     c. Description paragraph
     d. Subheading: `text-2xl font-extrabold text-mu-text-900 mb-6` -- "Комплексные программы"
     e. Grid: `grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12`
     f. 5 pricing cards (some with 2nd row). Each card:
        - Standard card: `bg-white/60 backdrop-blur-2xl rounded-[3rem] p-8 border border-white/60 shadow-glass flex flex-col`
        - Featured card ("Executive on Cancer"): `border border-mu-blue/40 shadow-[0_16px_48px_color-mix(in_oklch,var(--color-mu-blue)_15%,transparent)]`
        - Tier badge: `inline-flex items-center gap-2 bg-white/50 backdrop-blur-md border border-glass-border px-4 py-1.5 rounded-full shadow-sm w-fit mb-4` with `text-sm font-bold text-mu-accent-blue`
        - Featured badge: `bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white` instead
        - Price: `text-2xl font-extrabold bg-gradient-to-r from-mu-blue to-mu-accent-blue bg-clip-text text-transparent mb-3` with subtitle in `text-base font-bold text-mu-text-700`
        - Description: `text-mu-text-700 font-medium flex-grow`
     g. Programs: Базовая (~$760), Primary (~$1400), Executive (~$2270), Executive on Cancer (~$2150, featured), Премиум (~$2410)
     h. Then "Дополнительные обследования" subheading with additional items grid
   - Keep existing content and pricing data. Restyle to match glass pattern. The current component likely has this data -- update classes only.

5. **CheckupProgramsTurkey.tsx** (Source Section 6: "Чек-ап в Турции", lines 409-481)
   - Similar pricing card structure but for Turkish clinics
   - "Что входит в каждую программу" section with included items list
   - Pricing cards for Turkish programs
   - Restyle to same glass pattern as Korea cards

6. **CheckupProcess.tsx** (Source Section 7: "Как это работает", lines 483-516)
   - Heading: gradient "Как это работает"
   - 4 numbered step cards (same pattern as TreatmentSteps but simpler -- no timeline badges, just numbered):
     ```
     <div className="text-4xl font-extrabold bg-gradient-to-r from-mu-blue to-mu-accent-blue bg-clip-text text-transparent mb-4" aria-hidden="true">01</div>
     ```
   - Grid: `grid md:grid-cols-2 lg:grid-cols-4 gap-6`
   - Steps: Заявка, Подбор, Организация, Обследование

7. **CheckupB2B.tsx** (Source Section 8: "Корпоративные чек-апы", lines 518-569)
   - Heading: gradient "Корпоративные чек-апы"
   - Large glass container card: `bg-white/60 backdrop-blur-3xl rounded-[3.5rem] p-8 md:p-12 border border-white/60 shadow-glass-lg max-w-4xl mx-auto`
   - Content inside: description paragraphs + feature list with glass checkmarks
   - CTA button at bottom: gradient primary button

**acceptance_criteria**: All 7 components use glass classes. Pricing cards render with tier badges (standard and featured). Numbered cards use gradient numbers. No old production tokens remain.
  </action>
  <verify>
    <automated>cd next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 7 checkup section components render with glass design: pricing cards with tier badges, numbered gradient cards, icon box cards, glass B2B section. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update checkup page.tsx with glass design integration</name>
  <files>next/src/app/checkup/page.tsx</files>
  <action>
**read_first**: Read the current page.tsx. Read the source HTML hero: `git show feat/new-design:checkup.html | sed -n '102,202p'`.

Update page.tsx:

1. **Hero**: ServiceHero props are already correct for checkup:
   - eyebrow="Чек-ап за рубежом" -- correct
   - title about Samsung Medical Center -- correct
   - subtitle content -- correct
   - CTAs (Подобрать программу / Смотреть программы) -- correct
   - trustLine -- correct
   - illustration -- keep existing CheckupIllustration (clipboard SVG)
   - ServiceHero was restyled in Plan 01 -- no prop changes needed.

2. **SocialProof**: Already uses socialProofItems correctly. Stats match source HTML.

3. **Section ordering**: Verify against source HTML:
   - ServiceHero (hero)
   - SocialProof (stats)
   - CheckupProblem ("Почему не стоит ждать симптомов")
   - CheckupAdvantages ("Что даёт чек-ап в клинике мирового уровня")
   - CheckupWhyUs ("Не просто посредник")
   - CheckupProgramsKorea ("Чек-ап в Южной Корее")
   - CheckupProgramsTurkey ("Чек-ап в Турции")
   - CheckupProcess ("Как это работает")
   - CheckupB2B ("Корпоративные чек-апы")
   - FAQ
   - LeadFormSection (form)
   - FinalCTA

   Current order: Hero, SocialProof, CheckupProblem, CheckupAdvantages, CheckupWhyUs, CheckupProgramsKorea, CheckupProgramsTurkey, CheckupProcess, CheckupB2B, FAQ, LeadFormSection, FinalCTA. This matches! Keep as-is.

4. **socialProofItems**: Already correct: `[{number: '43', label: 'клиники'}, {number: '11', label: 'стран'}, {number: '1-2', label: 'дня обследование'}, {number: 'ISO', label: '27001'}]`.

5. **faqItems**: Already correct with 7 items. Content matches.

6. **ScrollReveal wrappers**: Keep all existing wrappers.

7. **FinalCTA**: Currently NOT wrapped in ScrollReveal (line 226). Wrap it: `<ScrollReveal><FinalCTA /></ScrollReveal>` for consistency.

8. **Metadata**: Keep as-is (correct).

**acceptance_criteria**: page.tsx compiles, renders all sections in correct order with glass design. FinalCTA wrapped in ScrollReveal.
  </action>
  <verify>
    <automated>cd next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>/checkup page renders with glass design throughout. All 7 checkup sections + shared sections display glass styling. Page compiles without errors. FinalCTA wrapped in ScrollReveal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI restyling, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04 | I (Information Disclosure) | CheckupProgramsKorea/Turkey | accept | Pricing data is public marketing content, intentionally displayed |
</threat_model>

<verification>
1. `cd next && npx tsc --noEmit` -- zero errors
2. `cd next && npm run build` -- production build succeeds
3. Visual: /checkup renders all sections with glass design language, pricing cards display correctly
</verification>

<success_criteria>
All 7 checkup section components + page.tsx use glass design. Every section on /checkup shows glass cards, gradient headings, pricing tiers with badges, numbered step cards. No old production classes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/72-service-pages/72-04-SUMMARY.md`
</output>
