---
phase: 72-service-pages
plan: 02
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - next/src/components/sections/consultations/ConsultationAdvantages.tsx
  - next/src/components/sections/consultations/ConsultationBenefits.tsx
  - next/src/components/sections/consultations/ConsultationDoctors.tsx
  - next/src/components/sections/consultations/ConsultationPricing.tsx
  - next/src/components/sections/consultations/ConsultationProblem.tsx
  - next/src/components/sections/consultations/ConsultationProcess.tsx
  - next/src/components/sections/consultations/ConsultationScenarios.tsx
  - next/src/app/consultations/page.tsx
autonomous: true
requirements: [SRV-01]

must_haves:
  truths:
    - "/consultations page renders with glass design for all section components"
    - "Hero displays gradient title, glass pill badge with price, two rounded CTAs"
    - "Features grid shows 6 glass cards with colored icon backgrounds"
    - "Problem section shows glass card with empathetic copy"
    - "Benefits section shows 4 glass cards in 2x2 grid"
    - "Process section shows 3 numbered glass step cards"
    - "Doctors section shows country cards with flag SVGs and specialization badges"
    - "Pricing section shows glass pricing card"
  artifacts:
    - path: "next/src/components/sections/consultations/ConsultationAdvantages.tsx"
      provides: "Glass advantage cards with icon boxes and hover states"
    - path: "next/src/components/sections/consultations/ConsultationBenefits.tsx"
      provides: "Glass benefit cards in 2-col grid"
    - path: "next/src/components/sections/consultations/ConsultationDoctors.tsx"
      provides: "Country flag cards, specialization badges, doctor link"
    - path: "next/src/components/sections/consultations/ConsultationPricing.tsx"
      provides: "Glass pricing card with feature list"
    - path: "next/src/components/sections/consultations/ConsultationProblem.tsx"
      provides: "Glass card with problem text"
    - path: "next/src/components/sections/consultations/ConsultationProcess.tsx"
      provides: "3 numbered glass step cards"
    - path: "next/src/components/sections/consultations/ConsultationScenarios.tsx"
      provides: "Glass scenario/trigger cards"
    - path: "next/src/app/consultations/page.tsx"
      provides: "Updated page with glass hero props and correct section ordering"
  key_links:
    - from: "next/src/app/consultations/page.tsx"
      to: "all 7 consultation section components"
      via: "import and JSX render"
      pattern: "import.*consultations/"
---

<objective>
Restyle all 7 consultations-specific section components and update page.tsx to match the new glass design from feat/new-design:online-consultations.html.

Purpose: The /consultations page is the primary service page. All sections must use glass cards, gradient headings, and the new typography scale established in Phases 68-71.
Output: 7 restyled section .tsx files + updated page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-service-pages/72-01-SUMMARY.md

Source of truth for exact HTML: `git show feat/new-design:online-consultations.html`
The file has 12 sections (lines 105-761). Key sections:
- Section 1 (HERO): lines 105-164 -- handled by shared ServiceHero
- Section 2 (FEATURES): lines 166-218 -- maps to ConsultationAdvantages (6 glass cards in 3-col grid)
- Section 3 (PROBLEM): lines 220-230 -- maps to ConsultationProblem
- Section 4 (VALUE/BENEFITS): lines 232-274 -- maps to ConsultationBenefits (4 glass cards in 2-col grid)
- Section 5 (PROCESS): lines 276-301 -- maps to ConsultationProcess (3 step cards)
- Section 6 (DOCTORS): lines 303-383 -- maps to ConsultationDoctors (country cards + specialization badges)
- Section 7 (WHY MEDICUSUNION): lines 385-469 -- this is the ADVANTAGES section (6 glass cards in 3-col grid)
- Section 8 (TRIGGERS): lines 471-510 -- maps to ConsultationScenarios
- Section 9 (PRICING): lines 512-550 -- maps to ConsultationPricing
- Section 10-12 (FORM, FAQ, CTA): handled by shared components from Plan 01

<interfaces>
Glass card pattern (standard for all cards):
```
bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass
group hover:border-white/80 hover:shadow-glass-lg transition-all duration-500
```

Icon box pattern (colored background variants):
```
w-14 h-14 bg-mu-blue/10 backdrop-blur-xl rounded-2xl flex items-center justify-center
text-mu-accent-blue shadow-glass-sm border border-white/60 mb-5
group-hover:scale-110 group-hover:rotate-3 transition-transform duration-500
```

Color variants for icon boxes:
- Blue: bg-mu-blue/10 text-mu-accent-blue
- Teal: bg-mu-accent-teal-bg text-mu-accent-teal
- Green: bg-mu-green-50 text-mu-green-600
- Orange: bg-mu-accent-orange-bg text-mu-accent-orange
- Red: bg-mu-accent-red-bg text-mu-accent-red

Section heading gradient:
```
text-4xl md:text-5xl font-extrabold text-center mb-12
  <span className="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent">...</span>
```

Numbered step pattern:
```
<div className="text-6xl font-extrabold text-mu-accent-blue opacity-20 mb-4
  group-hover:opacity-40 group-hover:-translate-y-2 group-hover:scale-105
  origin-left transition-all duration-500">01</div>
```

Specialization badge:
```
bg-white/50 backdrop-blur-md border border-white/60 px-6 py-3 rounded-full
font-bold text-mu-text-900 shadow-glass-inner
hover:bg-mu-green-50 hover:text-mu-green-700 transition-colors cursor-default
```

Country card (smaller):
```
bg-white/60 backdrop-blur-2xl rounded-2xl shadow-glass-sm border border-white/60 p-5
text-center hover:shadow-glass hover:border-white/80 transition-all duration-300
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle all 7 consultation section components</name>
  <files>
    next/src/components/sections/consultations/ConsultationAdvantages.tsx,
    next/src/components/sections/consultations/ConsultationBenefits.tsx,
    next/src/components/sections/consultations/ConsultationDoctors.tsx,
    next/src/components/sections/consultations/ConsultationPricing.tsx,
    next/src/components/sections/consultations/ConsultationProblem.tsx,
    next/src/components/sections/consultations/ConsultationProcess.tsx,
    next/src/components/sections/consultations/ConsultationScenarios.tsx
  </files>
  <action>
**read_first**: Read ALL 7 existing component files. Run `git show feat/new-design:online-consultations.html` and extract lines 166-550 (sections 2-9) for the exact new design classes and content.

Apply the following transformations to EACH component. The pattern is identical for all -- replace old production classes with glass classes:

**Global replacements across ALL components:**
- Section wrapper: `py-12 lg:py-[6.25rem] bg-white` -> `container mx-auto px-4 lg:px-6 mb-16`
- Remove inner container div (`max-w-[1200px] mx-auto px-4 md:px-8`) -- section IS the container now
- All h2 headings: `font-heading text-[clamp(1.75rem,3.5vw,2.75rem)] font-bold text-[#18212C] leading-[1.2] tracking-[-0.02em]` -> `text-4xl md:text-5xl font-extrabold`. Wrap text in gradient span: `<span className="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent">...</span>`
- All h3 headings: `font-heading text-[clamp(1.375rem,2.5vw,2rem)] font-bold text-[#18212C] leading-[1.2]` -> `text-xl font-extrabold text-mu-text-900`
- All body text: `text-[1.125rem] text-[rgba(24,33,44,0.55)] leading-relaxed` -> `text-mu-text-700 font-medium`
- All cards: `card-prod p-8` -> `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass group hover:border-white/80 hover:shadow-glass-lg transition-all duration-500`

**Per-component specifics:**

1. **ConsultationAdvantages.tsx** (Section 7: "Почему через MedicusUnion")
   - Grid: `grid-cols-1 md:grid-cols-2` -> `grid md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto`
   - 4 existing cards get glass treatment. The source HTML has 6 cards (adding "Специализированная платформа" and "Собственная IT-платформа" -- check source). Keep content as-is but restyle cards.
   - Icon boxes: Add the colored background icon box pattern from the interfaces. Each card gets a different accent color. Use the `group-hover:scale-110 group-hover:rotate-3` pattern.
   - SVG icons already exist in the component -- wrap each in: `<div className="w-14 h-14 bg-mu-blue/10 backdrop-blur-xl rounded-2xl flex items-center justify-center text-mu-accent-blue shadow-glass-sm border border-white/60 mb-5 group-hover:scale-110 group-hover:rotate-3 transition-transform duration-500">`. Alternate colors: card 1 blue, card 2 teal, card 3 green, card 4 orange.
   - Add `flex gap-5` layout for each card (icon left, text right) as in the source HTML section 7.

2. **ConsultationBenefits.tsx** (Section 4: "Что вы получите за одну консультацию")
   - Grid: `grid md:grid-cols-2 gap-6 max-w-5xl mx-auto`
   - 4 glass cards with icon boxes. Colors: card 1 blue, card 2 teal, card 3 green, card 4 orange.
   - h3 with `mb-3`, paragraph below.

3. **ConsultationDoctors.tsx** (Section 6: Doctors from 7 countries)
   - This is the largest component. Structure from source HTML:
     a. Heading: gradient "Врачи из Германии, Израиля, Швейцарии и ещё 4 стран"
     b. Description glass card: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-4xl mx-auto mb-12 space-y-4` with 3 paragraphs
     c. Country cards grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 max-w-4xl mx-auto mb-8`. Each card: `bg-white/60 backdrop-blur-2xl rounded-2xl shadow-glass-sm border border-white/60 p-5 text-center hover:shadow-glass hover:border-white/80 transition-all duration-300`. Include the SVG flag + country name + specializations.
     d. Specializations card: `bg-white/60 backdrop-blur-3xl rounded-[3rem] p-12 border border-white/60 shadow-glass-lg max-w-4xl mx-auto mb-8`. Inside: flex-wrap badges `bg-white/50 backdrop-blur-md border border-white/60 px-6 py-3 rounded-full font-bold text-mu-text-900 shadow-glass-inner hover:bg-mu-green-50 hover:text-mu-green-700 transition-colors cursor-default`.
     e. "Все врачи" link: glass button `bg-white/50 backdrop-blur-xl text-mu-text-900 px-8 py-4 rounded-3xl font-semibold shadow-glass hover:bg-white/60 transition-all border border-white/60 text-lg` with external link icon.
   - Keep the existing SVG flags and content. Restyle the wrapper classes.

4. **ConsultationPricing.tsx** (Section 9: Pricing)
   - From source HTML lines 512-550: large glass card with pricing tiers.
   - Single glass container card: `bg-white/60 backdrop-blur-3xl rounded-[3.5rem] p-8 md:p-12 border border-white/60 shadow-glass-lg max-w-4xl mx-auto`
   - Price value: `text-5xl font-extrabold bg-gradient-to-r from-mu-blue to-mu-accent-blue bg-clip-text text-transparent`
   - Feature list with glass checkmark circles (same as LeadFormSection pattern).
   - CTA button inside the card: gradient primary CTA.

5. **ConsultationProblem.tsx** (Section 3: "Знакомо?")
   - From source HTML lines 220-230: single glass card, not multiple cards.
   - Heading: gradient span "Знакомо?"
   - Content: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-3xl mx-auto space-y-6`
   - 3 paragraphs of text: `text-mu-text-700 font-medium text-lg leading-relaxed`. Last paragraph bold: `text-mu-text-900 font-bold`.

6. **ConsultationProcess.tsx** (Section 5: "Три шага")
   - Grid: `grid sm:grid-cols-2 lg:grid-cols-3 gap-6`
   - 3 step cards with numbered pattern:
     ```
     <div className="text-6xl font-extrabold text-mu-accent-blue opacity-20 mb-4
       group-hover:opacity-40 group-hover:-translate-y-2 group-hover:scale-105
       origin-left transition-all duration-500">01</div>
     ```
   - Step 1 color: `text-mu-accent-blue`, Step 2: `text-mu-green-500`, Step 3: `text-mu-accent-teal`
   - Each card gets glass styling + `hover:bg-white/80 hover:border-white/80 hover:shadow-glass-lg`.

7. **ConsultationScenarios.tsx** (Section 8: "Когда имеет смысл получить второе мнение")
   - From source HTML lines 471-510: glass card with scenario list.
   - Heading: gradient text.
   - Content in glass card: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-3xl mx-auto`
   - List items with glass checkmark circles.

**acceptance_criteria**: All 7 components use glass classes. No old production tokens remain (`card-prod`, `#18212C`, `rgba(24,33,44,0.55)`, `font-heading`, `bg-white` as section background). Each component matches the HTML source section structure.
  </action>
  <verify>
    <automated>cd next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 7 consultation section components render with glass design: glass cards with backdrop-blur, gradient headings, colored icon boxes, hover states. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update consultations page.tsx with new hero content and section ordering</name>
  <files>next/src/app/consultations/page.tsx</files>
  <action>
**read_first**: Read the current page.tsx. Read the hero section from source: `git show feat/new-design:online-consultations.html | sed -n '105,164p'`.

Update page.tsx:

1. **Hero section**: The ServiceHero component was restyled in Plan 01. Update the props to match the source HTML hero:
   - Remove the `illustration={<DoctorAtLaptopIllustration />}` inline SVG function. Replace with an `<img>` tag wrapped in a glass frame (the source HTML uses an Unsplash image). Use: `illustration={<div className="rounded-[3rem] overflow-hidden shadow-glass-lg border-[8px] border-white/40 bg-white/20"><img src="https://images.unsplash.com/photo-1758691463620-188ca7c1a04f?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w3Nzg4Nzd8MHwxfHNlYXJjaHwxfHxkb2N0b3IlMjB2aWRlbyUyMGNhbGwlMjB0ZWxlbWVkaWNpbmUlMjBsYXB0b3B8ZW58MXx8fHwxNzc1MjM4Mjg0fDA&ixlib=rb-4.1.0&q=80&w=1080" alt="Видеоконсультация с врачом" className="w-full h-[400px] lg:h-[500px] object-cover" /></div>}`.
   Actually -- to avoid external images in production, keep the SVG illustration approach but use the existing DoctorAtLaptopIllustration. The hero content text is already correct.

   - Add a `socialProofItems` array below hero for the stats badges: `{items: [{icon: CheckCircle, text: "7 стран"}, {icon: CheckCircle, text: "50+ врачей"}, {icon: CheckCircle, text: "15+ специализаций"}]}` -- these are inline in the hero in the source HTML (not SocialProof component). Add them as a flex row after the CTAs in the ServiceHero. Actually, looking at the source HTML, these stats are inline in the hero div, not a separate SocialProof.

   Wait -- the current page already passes these to SocialProof as a separate section. The source HTML has them inline in the hero. For simplicity, keep the existing pattern (separate SocialProof component) since ServiceHero was already restyled with the proper interface. The SocialProof was restyled in Plan 01 to be glass cards.

2. **Remove the DoctorAtLaptopIllustration function** (lines 29-101). Replace with a simpler telemedicine illustration or keep it but it's fine as-is since it's already SVG-based and decorative.

   Actually, keep the DoctorAtLaptopIllustration -- it's functional and distinctive. The hero wrapper in ServiceHero now handles the glass frame.

3. **Section ordering**: Verify the sections match the source HTML order:
   - ServiceHero (hero)
   - SocialProof (stats)
   - ConsultationProblem (problem "Знакомо?")  -- this is Section 3 in HTML but currently correct
   - ConsultationBenefits (value "Что вы получите") -- Section 4
   - ConsultationProcess (process "Три шага") -- Section 5
   - ConsultationDoctors (doctors) -- Section 6
   - ConsultationAdvantages (why MedicusUnion) -- Section 7
   - ConsultationScenarios (triggers) -- Section 8
   - ConsultationPricing (pricing) -- Section 9
   - LeadFormSection (form) -- Section 10
   - FAQ (faq) -- Section 11
   - FinalCTA (cta) -- Section 12

   The current page.tsx order already matches this! Verify and leave as-is.

4. **Remove the Features section import**: The source HTML has a "Features Grid" (Section 2) that maps to 6 advantage cards. But the current page.tsx does not have this section -- it uses ConsultationAdvantages for "Почему через MedicusUnion" (Section 7). The Features Grid content is different (speed, countries, translation, language, ISO, second opinion). 

   The current ConsultationAdvantages has 4 cards about "why MedicusUnion" (documents, translation, app, next steps). The source HTML Section 7 has 6 cards with different content. AND the source has Section 2 Features Grid with 6 different cards.

   Resolution: ConsultationAdvantages currently maps to Section 7. Keep it. The Features Grid (Section 2) is new content that doesn't have a current component. Since the page.tsx doesn't currently render a Features section between SocialProof and ConsultationProblem, and adding a new component would be content expansion rather than restyling -- skip adding Section 2 Features Grid. The CONTEXT.md says "Preserve page structure and content, update only styling." So keep existing components, just restyle them.

5. **ScrollReveal wrappers**: Keep all existing ScrollReveal wrappers.

6. **FAQ items**: Already defined correctly with 6 items. Keep as-is.

7. **Metadata**: Keep as-is (correct).

**acceptance_criteria**: page.tsx compiles, renders all sections in correct order, hero uses glass ServiceHero. No unused imports. DoctorAtLaptopIllustration still works.
  </action>
  <verify>
    <automated>cd next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>/consultations page renders with glass design throughout. All 7 consultation sections + shared sections display glass styling. Page compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI restyling, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02 | T (Tampering) | ConsultationDoctors | accept | External link to medicusunion.com/doctors unchanged; rel="noopener noreferrer" preserved |
</threat_model>

<verification>
1. `cd next && npx tsc --noEmit` -- zero errors
2. `cd next && npm run build` -- production build succeeds
3. Visual: /consultations renders all sections with glass design language
</verification>

<success_criteria>
All 7 consultation section components + page.tsx use glass design. Every section on /consultations shows glass cards, gradient headings, colored icon boxes, and proper hover states. No old production classes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/72-service-pages/72-02-SUMMARY.md`
</output>
