---
phase: 72-service-pages
plan: 03
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - next/src/components/sections/treatment/TreatmentAboutUs.tsx
  - next/src/components/sections/treatment/TreatmentClinics.tsx
  - next/src/components/sections/treatment/TreatmentSteps.tsx
  - next/src/components/sections/treatment/TreatmentReviews.tsx
  - next/src/app/treatment-abroad/page.tsx
autonomous: true
requirements: [SRV-02]

must_haves:
  truths:
    - "/treatment-abroad page renders with glass design for all section components"
    - "AboutUs section shows 4 glass cards with icon boxes"
    - "Clinics section shows 8 country cards with flag SVGs in a grid"
    - "Steps section shows 4 numbered step cards with timeline badges"
    - "Reviews section shows patient testimonial glass cards"
  artifacts:
    - path: "next/src/components/sections/treatment/TreatmentAboutUs.tsx"
      provides: "4 glass cards with icon boxes for company advantages"
    - path: "next/src/components/sections/treatment/TreatmentClinics.tsx"
      provides: "8 country cards with flags, clinic names, specializations"
    - path: "next/src/components/sections/treatment/TreatmentSteps.tsx"
      provides: "4 numbered step cards with colored timeline badges"
    - path: "next/src/components/sections/treatment/TreatmentReviews.tsx"
      provides: "Patient review glass cards"
    - path: "next/src/app/treatment-abroad/page.tsx"
      provides: "Updated page with correct section ordering"
  key_links:
    - from: "next/src/app/treatment-abroad/page.tsx"
      to: "all 4 treatment section components"
      via: "import and JSX render"
      pattern: "import.*treatment/"
---

<objective>
Restyle all 4 treatment-abroad-specific section components and update page.tsx to match the new glass design from feat/new-design:treatment-abroad.html.

Purpose: The /treatment-abroad page describes medical tourism services. All sections must use glass cards, gradient headings, and the new typography scale.
Output: 4 restyled section .tsx files + updated page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-service-pages/72-01-SUMMARY.md

Source of truth: `git show feat/new-design:treatment-abroad.html`
Key sections:
- Hero (lines 109-197): handled by shared ServiceHero
- Stats Bar (lines 199-223): handled by shared SocialProof
- About Us (lines 225-286): maps to TreatmentAboutUs -- 4 glass cards with icon boxes
- Platform (lines 288-335): NEW section -- "Ваши документы, снимки и связь с врачом -- в одном месте". This section exists on the index page as PlatformSection but NOT currently on treatment page. The source HTML adds it. Add content directly in page.tsx or create inline.
- Clinics (lines 337-455): maps to TreatmentClinics -- 8 country cards
- What's Included (lines 457-507): maps to TreatmentSteps (renamed to cover "what's included" + steps). The source has separate "What's Included" and "4 Steps" sections. Current TreatmentSteps covers the 4-step process.
- 4 Steps (lines 509-625): maps to TreatmentSteps
- Reviews (lines ~627-700): maps to TreatmentReviews
- FAQ, Form, CTA: handled by shared components

<interfaces>
Glass card patterns (same as Plan 02):
- Standard card: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
- Country card: `bg-white/60 backdrop-blur-2xl rounded-[2rem] p-6 border border-white/60 shadow-glass`
- Large container card: `bg-white/60 backdrop-blur-3xl rounded-[3.5rem] p-8 md:p-12 border border-white/60 shadow-glass-lg`
- Icon box: `w-14 h-14 bg-white/50 backdrop-blur-xl rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`
- Numbered step: `text-6xl font-extrabold text-mu-blue/15 mb-4`
- Timeline badge: `inline-flex items-center bg-mu-blue/10 text-mu-blue font-bold text-sm px-3 py-1 rounded-full mb-4`
- Checklist item: `flex items-center gap-2 text-sm text-mu-text-900 font-medium` with green check SVG
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle all 4 treatment section components</name>
  <files>
    next/src/components/sections/treatment/TreatmentAboutUs.tsx,
    next/src/components/sections/treatment/TreatmentClinics.tsx,
    next/src/components/sections/treatment/TreatmentSteps.tsx,
    next/src/components/sections/treatment/TreatmentReviews.tsx
  </files>
  <action>
**read_first**: Read ALL 4 existing component files. Run `git show feat/new-design:treatment-abroad.html | sed -n '225,700p'` for the exact new design classes and content.

**Global replacements (same as consultations):**
- Section wrapper: `py-12 lg:py-[6.25rem] bg-white` -> `container mx-auto px-4 lg:px-6 mb-16`
- Remove inner container div
- h2 headings -> `text-4xl md:text-5xl font-extrabold` with gradient span
- h3 headings -> `text-xl font-extrabold text-mu-text-900`
- Body text -> `text-mu-text-700 font-medium`
- Card wrappers -> glass card classes

**Per-component specifics:**

1. **TreatmentAboutUs.tsx** (Source Section: "About Us", lines 225-286)
   - Heading: gradient "MedicusUnion -- ваш надёжный партнёр в лечении за границей"
   - Subtitle paragraph: `text-mu-text-700 text-lg leading-relaxed font-medium text-center max-w-3xl mx-auto mb-12`
   - Grid: `grid sm:grid-cols-2 lg:grid-cols-4 gap-6`
   - 4 glass cards with icon boxes. Each card has a custom SVG icon in `w-14 h-14 bg-white/50 backdrop-blur-xl rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`.
   - Card 1: "15+ лет практики" -- circle-check SVG
   - Card 2: "Выбор страны под задачу" -- globe SVG
   - Card 3: "Прямые партнёрства" -- calendar SVG
   - Card 4: "Медицинская экспертиза" -- clipboard SVG
   - Keep existing SVG icons, just restyle wrapper classes.

2. **TreatmentClinics.tsx** (Source Section: "Clinics / Geography", lines 337-455)
   - Heading: plain text (not gradient) "Подбираем клинику под ваш диагноз, а не продаём 'свою'"
   - Subtitle paragraph below heading
   - Grid: `grid grid-cols-2 md:grid-cols-4 gap-4`
   - 8 country cards (Austria, Germany, Switzerland, Israel, India, UAE, Turkey, South Korea). Each card structure:
     ```
     <div className="bg-white/60 backdrop-blur-2xl rounded-[2rem] p-6 border border-white/60 shadow-glass">
       <div className="w-12 h-8 mb-4" aria-hidden="true">{flag SVG}</div>
       <h3 className="text-lg font-extrabold text-mu-text-900 mb-1">{country}</h3>
       <p className="text-mu-text-700 font-medium text-sm mb-3">{specializations}</p>
       <ul className="space-y-1.5 text-sm text-mu-text-900 font-medium">{clinics}</ul>
     </div>
     ```
   - Keep existing flag SVGs and clinic data. Restyle classes.
   - Footer text: `text-center text-mu-text-700 font-medium mt-10 max-w-2xl mx-auto`

3. **TreatmentSteps.tsx** (Source Section: "4 Steps / How it works", lines 509-625)
   - Heading: gradient "Как это работает"
   - Grid: `grid sm:grid-cols-2 lg:grid-cols-4 gap-6`
   - 4 step cards with numbered headers and timeline badges. Each card:
     ```
     <div className="bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass">
       <div className="text-6xl font-extrabold text-mu-blue/15 mb-4" aria-hidden="true">01</div>
       <h3 className="text-xl font-extrabold text-mu-text-900 mb-3">{title}</h3>
       <span className="inline-flex items-center bg-mu-blue/10 text-mu-blue font-bold text-sm px-3 py-1 rounded-full mb-4">{duration}</span>
       <p className="text-mu-text-700 font-medium mb-4">{description}</p>
       <ul className="space-y-2" role="list">
         {items with green check SVG}
       </ul>
     </div>
     ```
   - Step 1 "Планирование" (2-4 дня) -- badge: `bg-mu-blue/10 text-mu-blue`
   - Step 2 "Логистика" (7-10 дней) -- badge: `bg-mu-accent-teal-bg text-mu-accent-teal`
   - Step 3 "Координация лечения" (По плану) -- badge: `bg-mu-accent-orange-bg text-mu-accent-orange`
   - Step 4 "Реабилитация" (Долгосрочно) -- badge: `bg-mu-green-50 text-mu-green-600`
   - Each step has a checklist of 3-4 items with green check SVG.
   - Keep existing content, add the timeline badges and checklist items from source HTML.

4. **TreatmentReviews.tsx** (Source Section: Reviews)
   - This component renders patient testimonials. Restyle cards to glass:
   - Each review card: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
   - Patient name: `text-lg font-extrabold text-mu-text-900`
   - Review text: `text-mu-text-700 font-medium`
   - Stars or rating: keep existing or add star icons in `text-mu-accent-orange`.
   - If the source HTML doesn't have a reviews section on treatment-abroad, check -- the current page.tsx imports TreatmentReviews. Keep the component functional, just restyle.

**acceptance_criteria**: All 4 components use glass classes. 8 country cards render with flags. 4 step cards render with timeline badges. No old production tokens remain.
  </action>
  <verify>
    <automated>cd next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 4 treatment section components render with glass design: glass cards, gradient headings, flag country cards, numbered steps with timeline badges. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update treatment-abroad page.tsx with glass design integration</name>
  <files>next/src/app/treatment-abroad/page.tsx</files>
  <action>
**read_first**: Read the current page.tsx. Read the source HTML hero: `git show feat/new-design:treatment-abroad.html | sed -n '109,197p'`.

Update page.tsx:

1. **Hero**: ServiceHero props are already correct for treatment-abroad:
   - eyebrow="Медицинский туризм" -- correct
   - title, subtitle content -- already correct
   - CTAs -- already correct
   - illustration -- keep existing HeroIllustration (globe + airplane SVG)
   - The ServiceHero was restyled in Plan 01 -- no changes needed to hero props.

2. **SocialProof**: Already uses SOCIAL_PROOF_ITEMS correctly. The SocialProof component was restyled in Plan 01.

3. **Section ordering**: Verify against source HTML:
   - ServiceHero (hero)
   - SocialProof (stats)
   - TreatmentAboutUs (about us / 4 cards)
   - TreatmentClinics (clinics / 8 countries)
   - TreatmentSteps (how it works / 4 steps)
   - TreatmentReviews (reviews)
   - FAQ
   - LeadFormSection (form)
   - FinalCTA

   The current order is: Hero, SocialProof, TreatmentAboutUs, TreatmentClinics, TreatmentSteps, TreatmentReviews, FAQ, LeadFormSection, FinalCTA. This matches! Keep as-is.

4. **Note**: The source HTML has two additional sections not in current page (Platform section and What's Included section). Per CONTEXT.md "Preserve page structure and content, update only styling" -- do NOT add new sections. The restyling of existing components is sufficient.

5. **SOCIAL_PROOF_ITEMS**: Update to match source HTML stats bar (source has emoji-based cards):
   - Current: `[{number: '100+', label: 'клиник'}, {number: '500+', label: 'врачей-экспертов'}, {number: '15+', label: 'лет практики'}, {number: '10 000+', label: 'пациентов'}]`
   - Source HTML stats bar uses same data pattern. Keep as-is.

6. **FAQ_ITEMS, LEAD_FORM_TRUST_ITEMS**: Keep content as-is. The FAQ and LeadFormSection components were restyled in Plan 01.

7. **ScrollReveal wrappers**: Keep all existing wrappers.

8. **Metadata**: Keep as-is.

**acceptance_criteria**: page.tsx compiles, renders all sections in correct order with glass design from Plan 01 shared components and this plan's section components.
  </action>
  <verify>
    <automated>cd next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>/treatment-abroad page renders with glass design throughout. All 4 treatment sections + shared sections display glass styling. Page compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI restyling, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03 | I (Information Disclosure) | TreatmentClinics | accept | Clinic names and specializations are public marketing content |
</threat_model>

<verification>
1. `cd next && npx tsc --noEmit` -- zero errors
2. `cd next && npm run build` -- production build succeeds
3. Visual: /treatment-abroad renders all sections with glass design language
</verification>

<success_criteria>
All 4 treatment section components + page.tsx use glass design. Every section on /treatment-abroad shows glass cards, gradient headings, country flag cards, and step timeline badges. No old production classes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/72-service-pages/72-03-SUMMARY.md`
</output>
