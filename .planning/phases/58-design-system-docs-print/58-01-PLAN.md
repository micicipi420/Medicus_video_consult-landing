---
phase: 58-design-system-docs-print
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styleguide.html
  - docs/DESIGN-SYSTEM.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "styleguide.html shows all 4 glass variants (liquid-regular, liquid-nav, liquid-clear, liquid-fluted) with live rendered examples"
    - "styleguide.html has a dedicated do/don't guidance section with correct and incorrect usage patterns"
    - "styleguide.html demonstrates adaptive tinting by placing glass variants over section-tint-cool and section-tint-warm backgrounds"
    - "styleguide.html demonstrates interaction states (hover brightens, press darkens, focus ring)"
    - "styleguide.html documents specular highlight, refraction, viewport budget, and .glass-idle"
    - "Print stylesheet renders all glass variants as opaque white with 1px solid #ccc border"
    - "docs/DESIGN-SYSTEM.md documents all v5.0 classes and anti-patterns"
  artifacts:
    - path: "styleguide.html"
      provides: "Complete v5.0 design system visual reference"
      contains: "v5.0"
    - path: "docs/DESIGN-SYSTEM.md"
      provides: "Updated design system documentation"
      contains: "liquid-nav"
  key_links:
    - from: "styleguide.html"
      to: "css/styles.css"
      via: "link rel=stylesheet"
      pattern: "css/styles.css"
    - from: "styleguide.html"
      to: "src/styles/liquid-glass.css"
      via: "Tailwind build pipeline"
      pattern: "liquid-regular|liquid-nav|liquid-clear|liquid-fluted"
---

<objective>
Update the styleguide page and DESIGN-SYSTEM.md documentation to cover the complete v5.0 Liquid Glass system -- all glass variants, hierarchy, interaction states, specular physics, viewport budget, adaptive tinting, and print fallback behavior.

Purpose: Phase 58 is the final v5.0 phase. All visual effects from Phases 51-57 are implemented; this phase documents them for future maintainers and verifies print coverage.

Output: Updated styleguide.html with v5.0 demos and do/don't guidance; updated docs/DESIGN-SYSTEM.md with all v5.0 additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@styleguide.html
@src/styles/liquid-glass.css
@docs/DESIGN-SYSTEM.md

<interfaces>
<!-- Key classes and patterns the executor needs to document -->

From src/styles/liquid-glass.css:
- .liquid-regular -- base material (Section 1), blur 24px
- .liquid-nav -- nav-weight material (Section 1.1), blur 16px, no outer shadow
- .liquid-clear -- clear-weight material (Section 1.2), blur 20px, dimming ::after layer
- .liquid-fluted -- fluted material (Section 1.3), regular-weight + ::after ribbed streaks
- .liquid-card -- regular + padding + animated glint border (Section 2/8)
- .liquid-btn-primary -- gradient CTA, NOT glass (Section 3)
- .liquid-btn-secondary -- glass button (Section 3)
- .stats-glass -- grouped stats, blur-lg 40px (Section 4)
- .shimmer-sweep -- hero CTA hover shimmer (Section 5)
- .scroll-fade-top, .scroll-fade-bottom -- mask-image fade (Section 6)
- .liquid-header-backdrop -- double-height blur (Section 9)
- .glass-idle -- viewport budget idle state (Section 18)

Interaction states (Section 16 / VFEX-03):
- :hover on all glass surfaces: filter: brightness(1.04)
- :active on all glass surfaces: filter: brightness(0.96), transform: scale(0.985)
- :focus-visible on all glass surfaces: outline 2px solid var(--mu-blue-text), offset 4px

Specular highlight (Section 17 / VFEX-02):
- ::after on regular, nav, stats-glass: radial-gradient at --mouse-x/--mouse-y
- ::before on clear: radial-gradient (::after occupied by dimming)
- Fluted: background-layer composite (already in background property)
- JS: initMouseSpecular() sets --mouse-x/--mouse-y on hover

Print (Section 11):
- All glass classes: backdrop-filter: none, background: white, border: 1px solid #ccc
- All pseudo-elements (::after, ::before) hidden
- shimmer hidden, scroll-fade mask removed

Section tint classes (Section 12):
- .section-tint-cool -- blue-green tint (--liquid-tint-h: 197)
- .section-tint-warm -- peach-amber tint (--liquid-tint-h: 28)
- .section-tint-mint -- green tint

Hierarchy levels (GLAS-03):
  Level 1 - Nav: lightest blur (16px), highest transparency, no outer shadow
  Level 2 - Regular: medium blur (24px), standard opacity, full shadow
  Level 3 - Clear: highest transparency, dimming layer, subtle shadow
  Texture - Fluted: regular-weight + vertical ribbed streaks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Styleguide v5.0 -- add do/don't section, interaction demos, and v5.0 feature documentation</name>
  <files>styleguide.html</files>
  <action>
Update styleguide.html with the following additions. All content in Russian.

1. **Version bump**: Change the page title and description text from "v4.0" to "v5.0 Liquid Glass". Update the h1 subtitle paragraph to mention v5.0. The meta description should read "Визуальный справочник дизайн-системы MedicusUnion KZ v5.0 Liquid Glass."

2. **New Section: "Usage Guidelines / Do-Don't" (insert after Section 8 Shadow-Wrap, before Section 9 Scroll-Edge Fade)**:
   Create a new `<section id="usage-guidelines">` with class `sg-section py-16`. Contents:

   a) **Heading**: "Рекомендации по&nbsp;использованию" (h2, same heading classes as other sections)
   b) **Subtitle**: "Правильные и&nbsp;запрещённые паттерны работы со&nbsp;стеклянными поверхностями."

   c) **Do/Don't Grid** -- use `sg-comparison` (2-column grid, existing class) for each pair. Create 6 do/don't pairs:

   Pair 1 -- "Glass + squircle":
   - DO (green checkmark icon): `<article class="squircle-lg liquid-card">` with text "Стекло + squircle на&nbsp;одном элементе"
   - DON'T (red X icon): Code showing `<div style="backdrop-filter: blur(24px); border-radius: 16px;">` with text "Кастомный blur без&nbsp;токенов класса"

   Pair 2 -- "Нет вложенного стекла":
   - DO: Single glass card with content
   - DON'T: Glass card inside glass card, text "Двойной backdrop-filter = нечитаемый текст"

   Pair 3 -- "Shadow на&nbsp;glass":
   - DO: .liquid-card (shadow built into class)
   - DON'T: Manual box-shadow + mask-image on same element, text "box-shadow обрезается mask-image"

   Pair 4 -- "will-change":
   - DO: "will-change только на&nbsp;:hover/:active" with code `.liquid-card:hover { will-change: filter, transform; }`
   - DON'T: "will-change на&nbsp;статичных элементах" with code `.liquid-card { will-change: backdrop-filter; }`

   Pair 5 -- "Shimmer":
   - DO: "Максимум 1&nbsp;shimmer на&nbsp;viewport" showing single hero CTA
   - DON'T: "Несколько shimmer = визуальный шум" showing multiple shimmer buttons

   Pair 6 -- "drop-shadow на&nbsp;предках":
   - DO: "Тень на&nbsp;самом glass-элементе через box-shadow"
   - DON'T: "filter: drop-shadow() на&nbsp;обёртке ломает backdrop-filter потомков"

   Use the same green checkmark / red X SVG icons from the existing shadow-wrap section for consistency.

3. **New Section: "Interaction States" (insert after the new Usage Guidelines section)**:
   Create `<section id="interaction-states">` with class `sg-section py-16`:

   a) **Heading**: "Состояния взаимодействия"
   b) **Subtitle**: "Hover, press и&nbsp;focus-visible на&nbsp;стеклянных поверхностях. Наведите курсор для&nbsp;проверки."
   c) **Demo grid** (4 columns on lg, 2 on md, 1 on mobile): Show 4 interactive glass cards:
      - liquid-nav squircle-lg p-6 with text "Nav: hover me"
      - liquid-regular squircle-lg p-6 with text "Regular: hover me"
      - liquid-clear squircle-lg p-6 with text "Clear: hover me"
      - liquid-fluted squircle-lg p-6 with text "Fluted: hover me"
   d) Below the grid, add an explanation note: "hover = brightness(1.04), press = brightness(0.96) + scale(0.985), focus-visible = outline 2px solid var(--mu-blue-text). Для&nbsp;prefers-reduced-motion анимации мгновенные."
   e) Add a tabindex="0" on each card so focus-visible can be demonstrated via keyboard.

4. **New Section: "Specular Highlight & Refraction" (insert after Interaction States)**:
   Create `<section id="specular-refraction">` with class `sg-section py-16`:

   a) **Heading**: "Спекулярный блик и&nbsp;преломление"
   b) **Subtitle**: "Мышь управляет положением specular highlight на&nbsp;стеклянных поверхностях (только desktop)."
   c) Show 2 large glass cards side by side (sg-comparison grid):
      - .liquid-regular squircle-xl p-8 with text explaining specular: "Наведите мышь &mdash; блик следует за&nbsp;курсором. Работает через CSS --mouse-x/--mouse-y, обновляемые JS (initMouseSpecular)."
      - .liquid-card squircle-xl p-8 with text explaining refraction: "Chrome 139+: SVG feTurbulence + feDisplacementMap создают эффект преломления стекла. Три калибровки: sm (бейджи), md (карточки), lg (полноширинные)."
   d) Note: "prefers-reduced-motion отключает параллакс-эффект. Статичные состояния (hover, focus) сохраняются."

5. **New Section: "Viewport Budget" (insert after Specular)**:
   Create `<section id="viewport-budget">` with class `sg-section py-16`:

   a) **Heading**: "Бюджет viewport"
   b) **Subtitle**: "Максимум 6&nbsp;элементов с&nbsp;backdrop-filter одновременно видны в&nbsp;viewport."
   c) **Explanation paragraph**: "IntersectionObserver отслеживает видимые glass-элементы. Если видно более 6, избыточные получают класс .glass-idle &mdash; backdrop-filter заменяется непрозрачным фоном-аппроксимацией. Приоритет: header &gt; nav &gt; cards &gt; buttons."
   d) Show a single .glass-idle demo card: `<div class="glass-idle liquid-regular squircle-lg p-6">` with text "Элемент в&nbsp;состоянии .glass-idle &mdash; backdrop-filter отключён, фон-аппроксимация."
   e) Next to it, a regular card: `<div class="liquid-regular squircle-lg p-6">` with text "Активный элемент &mdash; backdrop-filter включён."

6. **New Section: "Print Fallback" (insert as last section before footer)**:
   Create `<section id="print-fallback">` with class `sg-section py-16`:

   a) **Heading**: "Печать"
   b) **Subtitle**: "Все стеклянные поверхности рендерятся как непрозрачные в&nbsp;@media print."
   c) **Explanation**: "backdrop-filter: none, background: white, border: 1px solid #ccc. Pseudo-элементы (specular, shimmer, fluted stripes, dimming) скрыты. Squircle-маски сняты (mask-image: none)."
   d) **Verification note**: "Откройте Ctrl+P / Cmd+P для&nbsp;проверки. Все карточки на&nbsp;этой странице должны отображаться как белые прямоугольники с&nbsp;серой рамкой."

Apply standard nbsp binding: all Russian prepositions (в, на, с, к, у, о, за, по, из, до, от, при, для, без, про) and conjunctions (не, ни, и, а, но) bound to the next word via `&nbsp;`. Subject+verb pairs also bound.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -c 'id="usage-guidelines"' styleguide.html && grep -c 'id="interaction-states"' styleguide.html && grep -c 'id="specular-refraction"' styleguide.html && grep -c 'id="viewport-budget"' styleguide.html && grep -c 'id="print-fallback"' styleguide.html && grep -c 'v5.0' styleguide.html && grep -c 'glass-idle' styleguide.html</automated>
  </verify>
  <done>
    - styleguide.html contains sections: usage-guidelines, interaction-states, specular-refraction, viewport-budget, print-fallback
    - Page title and description updated to v5.0
    - Do/don't section has 6 comparison pairs with green checkmark / red X icons
    - Interaction states demo shows all 4 glass variants with tabindex for keyboard testing
    - .glass-idle demo card present
    - Print section explains fallback behavior
    - All Russian text has proper nbsp binding
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DESIGN-SYSTEM.md to v5.0 -- add new classes, hierarchy, interaction states, anti-patterns</name>
  <files>docs/DESIGN-SYSTEM.md</files>
  <action>
Update docs/DESIGN-SYSTEM.md with v5.0 additions. The document is in Russian. Preserve existing structure and add/update sections:

1. **Section 1 (Overview)**: Change "Liquid Design System v4.0" to "Liquid Design System v5.0" in the h1 heading. Update the overview paragraph to mention v5.0 additions: glass material hierarchy (nav/regular/clear/fluted), adaptive tinting, specular highlights, interaction states, GPU viewport budget, and SVG refraction. Keep the rest of the overview paragraph intact.

2. **Section 2.3 and 2.4 (Liquid Glass Tokens)**: Add new tokens that were introduced in v5.0 phases:
   - `--liquid-nav-bg` -- Nav background (more transparent than --liquid-bg)
   - `--liquid-nav-blur` -- Nav blur (16px light)
   - `--liquid-clear-bg` -- Clear background
   - `--liquid-clear-blur` -- Clear blur (20px light)
   - `--liquid-clear-shadow-outer` -- Clear outer shadow (lighter than regular)
   - `--liquid-clear-dim` -- Clear dimming layer color
   - `--liquid-fluted-stripe-color` -- Fluted stripe color
   - `--liquid-fluted-stripe-width` -- Fluted stripe width
   - `--liquid-fluted-stripe-gap` -- Fluted stripe gap
   - `--liquid-tint-h`, `--liquid-tint-s`, `--liquid-tint-l`, `--liquid-tint-a` -- Tint HSLA components

3. **Section 3.2 (Liquid Glass Classes table)**: Add these rows:
   | `.liquid-nav` | Nav-weight стекло (Level 1). Минимальный blur, без внешней тени | `.squircle-lg` или `.squircle-xl` |
   | `.liquid-clear` | Clear-weight стекло (Level 3). Максимальная прозрачность + dimming layer | `.squircle-lg` или `.squircle-xl` |
   | `.liquid-fluted` | Рифлёное стекло. Regular-weight + вертикальные полосы (repeating-linear-gradient) | `.squircle-lg` или `.squircle-xl` |
   | `.glass-idle` | Viewport budget idle state. Заменяет backdrop-filter непрозрачным фоном | Добавляется JS (initViewportBudget) |

4. **New Section 3.3 "Glass Hierarchy (GLAS-03)"**: Add after Section 3.2:
   Document the 3-level hierarchy:
   - Level 1: `.liquid-nav` -- навигация, тулбары. blur 16px, alpha 0.28, без внешней тени
   - Level 2: `.liquid-regular` -- карточки, контейнеры. blur 24px, стандартный alpha, полная тень
   - Level 3: `.liquid-clear` -- модалки, оверлеи. blur 20px, alpha 0.18, dimming ::after layer
   - Текстура: `.liquid-fluted` -- декоративные панели. Regular-weight + вертикальные рёбра

5. **New Section 3.4 "Adaptive Tinting (VFEX-01)"**: Document section-tint classes:
   - `.section-tint-cool` sets --liquid-tint-* to blue-green
   - `.section-tint-warm` sets --liquid-tint-* to peach-amber
   - `.section-tint-mint` sets --liquid-tint-* to green
   - Default (no class): --liquid-tint-a: 0 (no tint)
   - Тинт composited as background-gradient layer, not mix-blend-mode

6. **New Section 3.5 "Interaction States (VFEX-03)"**: Document:
   - hover: `filter: brightness(1.04)` on all glass surfaces
   - active/press: `filter: brightness(0.96)`, `transform: scale(0.985)`
   - focus-visible: `outline: 2px solid var(--mu-blue-text)`, `outline-offset: 4px`
   - Buttons excluded (have their own states)
   - Reduced motion: durations zeroed but state changes preserved

7. **New Section 3.6 "Specular Highlight (VFEX-02)"**: Document:
   - Mouse-tracking radial-gradient via --mouse-x/--mouse-y (JS: initMouseSpecular)
   - ::after on regular, nav, stats-glass
   - ::before on clear (::after occupied by dimming)
   - Fluted: baked into background property
   - Desktop-only: touch devices get static default (30%, 0%)

8. **New Section 3.7 "Viewport Budget (PERF-01)"**: Document:
   - Max 6 simultaneous backdrop-filter elements per viewport
   - IntersectionObserver adds/removes .glass-idle class
   - Priority: header > nav > cards > buttons
   - .glass-idle replaces backdrop-filter with opaque approximation

9. **New Section 3.8 "SVG Refraction (PERF-03)"**: Document:
   - 3 filter sizes: sm (badges, scale 0), md (cards, scale 18), lg (full-width, scale 12)
   - Gated by html[data-refract="true"] (JS: initRefractionProbe)
   - Chrome 139+ only
   - SVG defs in partials/svg-defs.html

10. **Section 5 (Anti-Patterns)**: Add new anti-patterns:
    - NEVER use filter: drop-shadow() on ancestors of glass elements. Creates backdrop root breaking child backdrop-filter.
    - NEVER exceed 6 simultaneous backdrop-filter elements per viewport. Use .glass-idle for offscreen/excess elements.

11. **Section 9 (Scope Creep Guards)**: Update "v4.0" to "v5.0".

Apply standard nbsp binding for Russian text.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -c 'v5.0' docs/DESIGN-SYSTEM.md && grep -c 'liquid-nav' docs/DESIGN-SYSTEM.md && grep -c 'liquid-clear' docs/DESIGN-SYSTEM.md && grep -c 'liquid-fluted' docs/DESIGN-SYSTEM.md && grep -c 'glass-idle' docs/DESIGN-SYSTEM.md && grep -c 'VFEX-01\|VFEX-02\|VFEX-03\|PERF-01\|PERF-03' docs/DESIGN-SYSTEM.md && grep -c 'section-tint' docs/DESIGN-SYSTEM.md</automated>
  </verify>
  <done>
    - Document title reads "Liquid Design System v5.0"
    - New tokens documented: --liquid-nav-*, --liquid-clear-*, --liquid-fluted-*, --liquid-tint-*
    - Glass hierarchy section with 3 levels + fluted texture documented
    - Adaptive tinting section with section-tint-cool/warm/mint
    - Interaction states section with hover/press/focus-visible specs
    - Specular highlight section with mouse-tracking docs
    - Viewport budget section with .glass-idle and priority rules
    - SVG refraction section with 3 size categories
    - Anti-patterns updated with drop-shadow and 6-element budget rules
    - Scope guards updated to v5.0
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify print coverage and build -- ensure all glass variants render correctly in print and make build passes</name>
  <files>styleguide.html</files>
  <action>
Final verification task. Run `make build` and verify:

1. **Build passes**: `make build` exits 0. The styleguide.html is processed by the splicer pipeline (it is in PAGES list in Makefile).

2. **Print CSS coverage audit**: Verify that every glass class in liquid-glass.css is covered by the @media print block. Run grep to confirm:
   - All classes in the print block: .liquid-regular, .liquid-card, .liquid-nav, .liquid-clear, .liquid-fluted, .liquid-btn-primary, .liquid-btn-secondary, .stats-glass
   - All pseudo-elements hidden: .liquid-clear::after, .liquid-fluted::after, .liquid-regular::after, .liquid-nav::after, .stats-glass::after, .liquid-clear::before, .shimmer-sweep::before
   - .liquid-card::after (specular glint) -- check if it is covered in the print block. If NOT present, add it to the print block selectors alongside the other ::after elements.
   - .liquid-card::before (animated glint border) -- check if it is covered. If NOT present, add it.
   - .glass-idle should NOT need print special handling (it already has no backdrop-filter).

3. **If any print coverage gap is found** in liquid-glass.css, add the missing selectors to the @media print block following the existing pattern (display: none !important for pseudo-elements).

4. **Byte-identity check**: After build, verify that styleguide.html in the build output contains all new sections added in Task 1.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build 2>&1 | tail -5 && echo "---" && grep -c 'liquid-card::before\|liquid-card::after' src/styles/liquid-glass.css && echo "print block check:" && sed -n '/@media print/,/^}/p' src/styles/liquid-glass.css | grep -c 'liquid-card'</automated>
  </verify>
  <done>
    - make build exits 0
    - Every glass pseudo-element selector is covered in @media print (including .liquid-card::before and .liquid-card::after)
    - styleguide.html builds correctly via splicer
    - No glass class is missing from print fallback
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| styleguide.html | Dev-only page (noindex, nofollow) -- no user input, no data processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | I (Information Disclosure) | styleguide.html | accept | Page is noindex/nofollow, internal dev reference only. No sensitive data exposed. |
</threat_model>

<verification>
1. `make build` exits 0
2. `grep -c 'id="usage-guidelines"' styleguide.html` returns 1
3. `grep -c 'id="interaction-states"' styleguide.html` returns 1
4. `grep -c 'id="specular-refraction"' styleguide.html` returns 1
5. `grep -c 'id="viewport-budget"' styleguide.html` returns 1
6. `grep -c 'id="print-fallback"' styleguide.html` returns 1
7. `grep -c 'v5.0' styleguide.html` >= 2
8. `grep 'v5.0' docs/DESIGN-SYSTEM.md` returns matches
9. `grep 'liquid-nav' docs/DESIGN-SYSTEM.md` returns matches
10. `grep 'glass-idle' docs/DESIGN-SYSTEM.md` returns matches
11. Print @media block in liquid-glass.css covers .liquid-card::before and .liquid-card::after
</verification>

<success_criteria>
- styleguide.html is a complete v5.0 visual reference with all glass variants, do/don't guidance, interaction state demos, specular/refraction docs, viewport budget demo, and print fallback note
- docs/DESIGN-SYSTEM.md documents all v5.0 additions: hierarchy, tinting, interaction states, specular, viewport budget, refraction, updated anti-patterns
- Print stylesheet covers every glass class and pseudo-element -- verified by grep
- make build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/58-design-system-docs-print/58-01-SUMMARY.md`
</output>
