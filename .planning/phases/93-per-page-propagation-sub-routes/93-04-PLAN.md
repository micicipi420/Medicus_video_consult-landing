---
phase: 93-per-page-propagation-sub-routes
plan: 04
type: execute
wave: 2
subsystem: route-treatment-abroad
depends_on:
  - 00
  - 01
files_modified:
  - next/src/components/sections/treatment/TreatmentAboutUs.tsx
  - next/src/components/sections/treatment/TreatmentClinics.tsx
  - next/src/components/sections/treatment/TreatmentReviews.tsx
  - next/src/components/sections/treatment/TreatmentSteps.tsx
autonomous: true
requirements:
  - ROUTE-03
must_haves:
  truths:
    - "All 4 /treatment-abroad section components consume --glass-card-fill / --glass-card-blur"
    - "TreatmentAboutUs decorative inner icon chips consume --glass-button-fill / --glass-button-blur (Tier 3)"
    - "TreatmentReviews data-driven review.gradient field UNTOUCHED (decorative ribbon, NOT a CTA)"
    - "Heading text gradients (bg-clip-text text-transparent) UNTOUCHED — text fill"
    - "No Archetype-J CTA in /treatment-abroad files (route-level CTA is the shared LeadFormSection covered by Plan 01)"
    - "Per-route Playwright visual diff for /treatment-abroad is GREEN against Wave 0 baseline (only blob-region mask delta acceptable)"
    - "pnpm build green; pnpm lint green"
  artifacts:
    - path: "next/src/components/sections/treatment/TreatmentAboutUs.tsx"
      provides: "v9.0 tokenized 4-card about-us grid + inner icon chips"
    - path: "next/src/components/sections/treatment/TreatmentClinics.tsx"
      provides: "v9.0 tokenized 8-country card grid"
    - path: "next/src/components/sections/treatment/TreatmentReviews.tsx"
      provides: "v9.0 tokenized review cards (data-driven gradient ribbon preserved)"
    - path: "next/src/components/sections/treatment/TreatmentSteps.tsx"
      provides: "v9.0 tokenized 4-step section panels"
  key_links:
    - from: "next/src/app/treatment-abroad/page.tsx (composition shell — unmodified)"
      to: "4 swept section components in next/src/components/sections/treatment/"
      via: "ESM imports preserved verbatim"
      pattern: "from '@/components/sections/treatment/'"
---

<objective>
Sweep the 4 `/treatment-abroad` route section components to v9.0 4-tier glass tokens. Composition shell `next/src/app/treatment-abroad/page.tsx` is NOT modified. Plan runs in parallel agent/worktree alongside Plans 02/03/05 with zero file overlap.

**Three deliverables:**

1. Apply Archetype C swaps verbatim from `93-PATTERNS.md` §"Wave 2C — `/treatment-abroad`" tables across 4 files. TreatmentAboutUs decorative inner chips also swept to Tier 3.
2. **Special handling for TreatmentReviews:** data-driven `review.gradient` field (e.g. `'from-mu-blue to-mu-accent-blue'`) is used as a decorative accent ribbon/bar fill, NOT a CTA — leave verbatim. The review CARD wrapper is swept; the gradient ribbon is preserved.
3. Confirm there is no Archetype J CTA in any /treatment-abroad file — the route-level CTA is the shared `LeadFormSection` (covered by Plan 01) and the `FinalCTA` (Phase 92 territory, frozen).

**Exact line/before/after pairs:** See `93-PATTERNS.md` §"Wave 2C — `/treatment-abroad`" tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md
@.planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md
@.planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-05-PLAN.md
@DESIGN.md
@CLAUDE.md
@next/src/app/treatment-abroad/page.tsx
@next/src/components/sections/treatment/TreatmentAboutUs.tsx
@next/src/components/sections/treatment/TreatmentClinics.tsx
@next/src/components/sections/treatment/TreatmentReviews.tsx
@next/src/components/sections/treatment/TreatmentSteps.tsx

<interfaces>
<!-- Per-line BEFORE/AFTER strings: see 93-PATTERNS.md "Wave 2C — /treatment-abroad" section. -->

<!-- Special case (TreatmentReviews): -->
<!-- review.gradient = 'from-mu-blue to-mu-accent-blue' is a DATA-DRIVEN decorative ribbon -->
<!-- (e.g., a top-bar accent on the review card), NOT a <button> or <a> CTA. -->
<!-- Identification: the gradient string appears in a data structure (`reviews = [...]`) -->
<!-- not in a button/anchor element's className. Leave verbatim. -->

<!-- No Archetype-J CTA in this plan: -->
- Route-level CTA = shared LeadFormSection (Plan 01 covers)
- Below-fold CTA = shared FinalCTA (Phase 92, frozen)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep TreatmentAboutUs + TreatmentSteps (4 cards each + decorative inner chips on AboutUs)</name>
  <files>next/src/components/sections/treatment/TreatmentAboutUs.tsx, next/src/components/sections/treatment/TreatmentSteps.tsx</files>
  <read_first>
    - Both files (whole file each)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2C — TreatmentAboutUs + TreatmentSteps tables)
  </read_first>
  <action>
    **TreatmentAboutUs.tsx (lines 15, 31, 49, 68 — 4 cards; lines 16, 32, 50, 69 — 4 decorative inner chips):**

    Cards (Archetype C):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass`

    Decorative inner chips (Tier 3):
    - BEFORE: `w-14 h-14 bg-white/50 backdrop-blur-xl rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`
    - AFTER:  `w-14 h-14 bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`

    **TreatmentSteps.tsx (lines 18, 48, 78, 104 — 4 step section panels):**

    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass`

    Note: 93-PATTERNS.md classifies TreatmentSteps as Archetype C (cards), not B (frames). Keep the Tier 1 `--glass-card-fill` mapping.

    Use Edit tool. Preserve all Russian copy + `&nbsp;` / `\u00A0`. Preserve all `text-mu-blue`, brand-color text classes. Preserve all `border-white/N`, `shadow-glass*`.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/treatment/TreatmentAboutUs.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/treatment/TreatmentSteps.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/treatment/TreatmentAboutUs.tsx` ≥ 4 (4 cards)
    - `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/treatment/TreatmentAboutUs.tsx` ≥ 4 (4 inner chips)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/treatment/TreatmentSteps.tsx` ≥ 4 (4 step panels)
    - No hardcoded blur literals: `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/treatment/TreatmentAboutUs.tsx next/src/components/sections/treatment/TreatmentSteps.tsx` returns 0
  </acceptance_criteria>
  <done>
    Two files swept; AboutUs decorative chips Tier 3; Steps cards Tier 1.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sweep TreatmentClinics (8 country cards — single repeated className) + TreatmentReviews (cards swept; data-driven review.gradient ribbon NEVER touched)</name>
  <files>next/src/components/sections/treatment/TreatmentClinics.tsx, next/src/components/sections/treatment/TreatmentReviews.tsx</files>
  <read_first>
    - Both files (whole file each)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2C — TreatmentClinics + TreatmentReviews tables; see "data-driven review.gradient" special case in No Analog Found / Special Cases section)
  </read_first>
  <action>
    **TreatmentClinics.tsx (lines 15, 32, 50, 66, 86, 105, 123, 142 — 8 country cards, identical className):**

    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2rem] p-6 border border-white/60 shadow-glass`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2rem] p-6 border border-white/60 shadow-glass`

    Since the 8 className strings are identical, a single global Edit (or multiple with `replace_all: true` semantics) suffices. Verify all 8 instances are swept, not just the first.

    **TreatmentReviews.tsx (line 43 — review cards in `.map()`; data-driven `review.gradient` UNTOUCHED):**

    Card wrapper (Archetype C):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass`

    **CRITICAL preservation:** The data-driven `review.gradient = 'from-mu-blue to-mu-accent-blue'` field is used as a decorative ribbon/accent bar (e.g., a top-of-card colored stripe), NOT a CTA. Confirm by inspecting the JSX: the `review.gradient` interpolation appears in a `<div className={\`bg-gradient-to-r ${review.gradient} ...\`}>` decoration, NOT in a `<button>` or `<a>`. Leave the data structure untouched. Do NOT modify the data array. Do NOT swap the gradient string.

    Verify with grep: after sweep, `grep -c 'review.gradient' next/src/components/sections/treatment/TreatmentReviews.tsx` returns ≥1 (data field still referenced).
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/treatment/TreatmentClinics.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/treatment/TreatmentReviews.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/treatment/TreatmentClinics.tsx` ≥ 8 (8 country cards swept)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/treatment/TreatmentReviews.tsx` ≥ 1
    - Data-driven gradient field preserved: `grep -c 'review.gradient\|review\\.gradient' next/src/components/sections/treatment/TreatmentReviews.tsx` ≥ 1 (review.gradient still referenced)
    - No CTA-style gradient swept by accident: there is NO standalone `from-mu-blue to-mu-accent-blue` literal in this file outside the data structure (verify by grep + manual file inspection)
    - No hardcoded blur literals: `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/treatment/TreatmentClinics.tsx next/src/components/sections/treatment/TreatmentReviews.tsx` returns 0
  </acceptance_criteria>
  <done>
    All 8 country cards swept; review cards swept; data-driven `review.gradient` ribbon untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: /treatment-abroad wave-end gates — full build + lint + Playwright visual diff for /treatment-abroad</name>
  <files>(no source modifications — verification only)</files>
  <read_first>
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Per-Task Verification Map — 93-02-03 row)
  </read_first>
  <action>
    Run wave-end verification scoped to `/treatment-abroad`:

    1. `pnpm --dir next build`
    2. `pnpm --dir next lint`
    3. `pnpm --dir next exec playwright test tests/visual --grep "treatment-abroad"` — visual diff vs Wave 0 baseline. Expected diffs from token consumption; document for Plan 07. Do NOT regenerate baseline.
    4. CTA invariant cross-cutting grep (negative — should be ZERO HITS because there is no Archetype-J CTA in this directory):
       ```bash
       grep -rn 'from-mu-blue to-mu-accent-blue' next/src/components/sections/treatment/ \
         | xargs -I {} sh -c 'grep -E "(backdrop-blur|backdrop-filter)" {} 2>/dev/null && echo "HIT: {}"' 2>/dev/null
       ```
       Expected: zero `HIT:` lines. Note: TreatmentReviews's `from-mu-blue to-mu-accent-blue` lives inside a data array, not a glass-tier className — it should not co-locate with `backdrop-*`.

    5. Russian-copy / nbsp regression check: `grep -rc '\\u00A0' next/src/components/sections/treatment/`. Compare against pre-sweep baseline.

    6. Confirm `review.gradient` data preserved across TreatmentReviews.tsx.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - CTA invariant cross-cutting grep returns 0 HIT lines
    - Russian-copy nbsp counts unchanged from pre-sweep baseline
    - All 4 /treatment-abroad files have ZERO `bg-white/N` residue
    - `pnpm --dir next exec playwright test tests/visual --grep "treatment-abroad"` either passes within `maxDiffPixelRatio: 0.01` OR produces an EXPECTED diff documented in SUMMARY
    - `review.gradient` data field still referenced in TreatmentReviews.tsx (data preservation confirmed)
  </acceptance_criteria>
  <done>
    /treatment-abroad route swept across 4 files; data-driven gradient ribbon preserved; visual diff documented.
  </done>
</task>

</tasks>

<acceptance_gates>
**Positive gates (per-file token consumption):**
- `grep -rcE 'bg-\[var\(--glass-(card|section|button)-fill\)\]' next/src/components/sections/treatment/` ≥ 21 (matches RESEARCH inventory: 21 sweep occurrences total)

**Negative gates (CTA invariant + residue):**
- `grep -rcE 'bg-white/[0-9]+' next/src/components/sections/treatment/` → 0
- `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/treatment/` → 0
- CTA invariant: `grep -rE 'from-mu-blue to-mu-accent-blue' next/src/components/sections/treatment/ | grep -cE 'backdrop-blur|backdrop-filter'` → 0
- TreatmentReviews data preservation: `grep -c 'review.gradient' next/src/components/sections/treatment/TreatmentReviews.tsx` ≥ 1
- No `mix-blend-*` introduced: `grep -rciE 'mix-blend-(multiply|screen|overlay)' next/src/components/sections/treatment/` → 0

**Build gates:**
- `pnpm --dir next build` exit 0
- `pnpm --dir next lint` exit 0
- `pnpm --dir next exec playwright test tests/visual --grep "treatment-abroad"` either passes OR produces only documented expected diffs
</acceptance_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /treatment-abroad route bundle ↔ shared service primitives (Plan 01) | This route imports ServiceHero/SocialProof/FAQ/LeadFormSection from `service/`. Plan 01 covers those; Plan 04 covers treatment/* sections. |
| Decorative data-driven gradient ribbon ↔ CTA opaque-forever invariant | `review.gradient` is a decorative ribbon, NOT a CTA. The string-form match is identical to a CTA gradient, but the JSX context disambiguates. |
| Wave 2 parallel plans (02/03/04/05) ↔ each other | Plan 04 owns ONLY `treatment/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04-01 | Tampering | Visual-regression in non-blob region | mitigate | Wave 0 baseline + Task 3 per-route Playwright diff |
| T-93-04-02 | Tampering | Data-driven `review.gradient` accidentally swept (mistaken for CTA) | mitigate | Task 2 explicit "preserve data field" rule; acceptance gate confirms `review.gradient` reference still present + manual JSX inspection rule |
| T-93-04-03 | Tampering | Only some of the 8 identical TreatmentClinics country cards swept (regex-replace edge case) | mitigate | Acceptance gate `bg-\[var(--glass-card-fill)\]` count ≥ 8 in TreatmentClinics |
| T-93-04-04 | Tampering | A11y branch coverage regression — arbitrary-value class bypasses Phase 92 BL-02 fix | mitigate | Token-only sweep |
</threat_model>

<deviation_rules>
- The planner does NOT introduce new archetypes; cite Archetype C from `92-PATTERNS.md`.
- The planner does NOT touch `next/src/app/treatment-abroad/page.tsx` (composition shell — no glass surfaces).
- The planner does NOT modify the `review.gradient` data structure in TreatmentReviews.tsx.
- The planner does NOT touch heading text gradients (`bg-clip-text text-transparent`).
- The planner does NOT modify files outside `next/src/components/sections/treatment/`.
- The planner does NOT regenerate Wave 0 baseline snapshots.
- If a target file already matches the archetype pattern, the corresponding task becomes a verify-only no-op for that file.
</deviation_rules>

<verification>
1. `pnpm --dir next build` exits 0.
2. `pnpm --dir next lint` exits 0.
3. All 4 /treatment-abroad files: `bg-white/N` residue = 0; hardcoded blur literals = 0.
4. CTA invariant grep returns 0 hits.
5. TreatmentReviews `review.gradient` data preserved.
6. Playwright visual-regression for /treatment-abroad: passes OR expected diffs documented.
</verification>

<success_criteria>
- ROUTE-03 satisfied (partial — Plan 01 primitives + Plan 04 sections together cover /treatment-abroad)
- 4 /treatment-abroad section components swept
- TreatmentReviews data-driven gradient ribbon untouched
- All 8 TreatmentClinics country cards swept
- Build + lint green
</success_criteria>

<output>
After completion, create `.planning/phases/93-per-page-propagation-sub-routes/93-04-SUMMARY.md`. Note: (a) per-file grep counts (especially TreatmentClinics 8 cards), (b) confirmation `review.gradient` preserved, (c) Playwright visual diff status.
</output>
