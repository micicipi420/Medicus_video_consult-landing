---
phase: 93-per-page-propagation-sub-routes
plan: 01
type: execute
wave: 1
subsystem: service-primitives
depends_on:
  - 00
files_modified:
  - next/src/components/sections/service/ServiceHero.tsx
  - next/src/components/sections/service/SocialProof.tsx
  - next/src/components/sections/service/FAQ.tsx
  - next/src/components/sections/service/LeadFormSection.tsx
autonomous: true
requirements:
  - ROUTE-05
must_haves:
  truths:
    - "ServiceHero eyebrow pill (line 35) consumes --glass-section-fill / --glass-section-blur (Archetype B)"
    - "ServiceHero secondary CTA glass (line 73) consumes --glass-section-fill with Tier-2 hover ramp via --glass-card-fill"
    - "ServiceHero primary CTA gradient (line 50) UNTOUCHED — opaque-forever invariant (Archetype J)"
    - "SocialProof stat card (line 19) consumes --glass-card-fill / --glass-card-blur with Tier-2 hover via --glass-form-fill"
    - "FAQ closed item wrapper (line 44) consumes --glass-card-fill / --glass-card-blur (Archetype D Tier 1)"
    - "FAQ button hover (line 48) consumes --glass-form-fill (Tier 2 hover ramp)"
    - "LeadFormSection outer Tier 0 wrapper (line 47) FLATTENED per Decision A — drop bg/blur/shadow/border, preserve rounded radius + padding"
    - "LeadFormSection inner Tier 2 form panel (line 72) consumes --glass-form-fill (KD-v9-002 alpha=0.50) + --glass-form-blur"
    - "LeadFormSection GlassCheckmark chip (line 19) consumes --glass-button-fill / --glass-button-blur"
    - "LeadFormSection ContactForm import UNTOUCHED (Phase 92 territory frozen)"
    - "NO honeypot/timing pattern propagated to LeadFormSection (Decision C — BL-04 anti-bot stays deferred)"
    - "pnpm build green; pnpm lint green"
  artifacts:
    - path: "next/src/components/sections/service/ServiceHero.tsx"
      provides: "v9.0 tokenized hero primitive — propagates to /checkup, /consultations, /treatment-abroad via import"
    - path: "next/src/components/sections/service/SocialProof.tsx"
      provides: "v9.0 tokenized stat-grid primitive"
    - path: "next/src/components/sections/service/FAQ.tsx"
      provides: "v9.0 tokenized accordion (Archetype D)"
    - path: "next/src/components/sections/service/LeadFormSection.tsx"
      provides: "v9.0 form primitive with flattened outer + KD-v9-002 inner panel"
  key_links:
    - from: "ServiceHero.tsx (Wave 1)"
      to: "/checkup, /consultations, /treatment-abroad page.tsx (Wave 2)"
      via: "import graph — service primitive ships once, propagates to 3 routes"
      pattern: "import.*ServiceHero"
    - from: "LeadFormSection.tsx inner Tier 2 panel"
      to: "globals.css :root --glass-form-fill = 0.50 (KD-v9-002)"
      via: "Tailwind arbitrary-value class consuming the token at paint time"
      pattern: "bg-\\[var\\(--glass-form-fill\\)\\]"
---

<objective>
Sweep the four service-page primitives in `next/src/components/sections/service/` to v9.0 4-tier glass tokens. These primitives are imported by 3 of 4 sub-routes (`/checkup`, `/consultations`, `/treatment-abroad`); sweeping them once propagates to all three route bundles automatically via the import graph. `LeadFormSection` is also imported by `/contacts/page.tsx`, so this plan touches all 4 routes' lead-capture surface.

**Three deliverables:**

1. Apply Archetypes B + C + D + J swaps verbatim from `92-PATTERNS.md` (referenced by letter, not re-derived) across `ServiceHero.tsx`, `SocialProof.tsx`, `FAQ.tsx`.
2. Execute Decision A on `LeadFormSection.tsx`: flatten the outer Tier 0 wrapper at line 47 (drop `bg-white/60 backdrop-blur-3xl border shadow-glass-lg`, preserve `rounded-[3.5rem] p-6 md:p-12`); sweep the inner Tier 2 form panel at line 72 to `--glass-form-fill` (KD-v9-002 α=0.50) + `--glass-form-blur`; sweep the `GlassCheckmark` chip at line 19 to `--glass-button-fill` + `--glass-button-blur`.
3. Verify CTA opaque-forever invariant on `ServiceHero.tsx:50` (Archetype J — never sweep) and confirm NO honeypot/timing anti-bot pattern is propagated to `LeadFormSection` (Decision C — BL-04 stays deferred for separate triage; `LeadFormSection` reuses the frozen `<ContactForm />` component, but no NEW form-handling code is added by this plan).

**Exact line/before/after pairs:** See `93-PATTERNS.md` §"Wave 1 — Service Primitives" tables (per-file). Cite Archetype letter + line number from those tables; do not paraphrase or invent strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md
@.planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md
@.planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-CONTEXT.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md
@DESIGN.md
@CLAUDE.md
@next/src/components/sections/service/ServiceHero.tsx
@next/src/components/sections/service/SocialProof.tsx
@next/src/components/sections/service/FAQ.tsx
@next/src/components/sections/service/LeadFormSection.tsx

<interfaces>
<!-- Tier-token resolution table — REFERENCED from 92-PATTERNS.md (FROZEN), not re-derived: -->
<!-- bg-[var(--glass-section-fill)]  +  backdrop-blur-[var(--glass-section-blur)]   → Tier 0 -->
<!-- bg-[var(--glass-card-fill)]     +  backdrop-blur-[var(--glass-card-blur)]      → Tier 1 -->
<!-- bg-[var(--glass-form-fill)]     +  backdrop-blur-[var(--glass-form-blur)]      → Tier 2 (KD-v9-002 alpha=0.50) -->
<!-- bg-[var(--glass-button-fill)]   +  backdrop-blur-[var(--glass-button-blur)]    → Tier 3 -->

<!-- Hover-ramp template (cards): hover:bg-[var(--glass-form-fill)] -->
<!-- Hover-ramp template (CTA secondary glass): hover:bg-[var(--glass-card-fill)] -->

<!-- ContactForm import inside LeadFormSection (frozen — Phase 92 territory): -->
<!--   import { ContactForm } from '@/components/sections/ContactForm';  ← do NOT modify -->

<!-- Phase 93 line tables (use these — do not re-derive): -->
- ServiceHero.tsx:35 (eyebrow B), :50 (CTA J — UNTOUCHED), :73 (secondary CTA B)
- SocialProof.tsx:19 (stat card C with hover)
- FAQ.tsx:44 (closed wrapper D), :48 (button hover), :34 (text gradient — UNTOUCHED)
- LeadFormSection.tsx:19 (GlassCheckmark Tier 3), :47 (outer FLATTEN), :72 (inner Tier 2 form)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep ServiceHero.tsx + SocialProof.tsx (Archetypes B, B-secondary, C — CTA at :50 NEVER touched)</name>
  <files>next/src/components/sections/service/ServiceHero.tsx, next/src/components/sections/service/SocialProof.tsx</files>
  <read_first>
    - next/src/components/sections/service/ServiceHero.tsx (whole file — small)
    - next/src/components/sections/service/SocialProof.tsx (whole file — small)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§"Wave 1 — Service Primitives" → ServiceHero + SocialProof line tables — verbatim before/after strings)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (Archetypes B, C definitions — referenced by letter)
  </read_first>
  <action>
    **ServiceHero.tsx — apply 93-PATTERNS.md table:**

    - Line 35 (eyebrow pill — Archetype B):
      BEFORE: `mb-5 inline-flex max-w-full items-center gap-2 rounded-full border border-white/60 bg-white/40 px-4 py-2.5 shadow-glass-inner backdrop-blur-xl sm:px-5`
      AFTER: `mb-5 inline-flex max-w-full items-center gap-2 rounded-full border border-white/60 bg-[var(--glass-section-fill)] px-4 py-2.5 shadow-glass-inner backdrop-blur-[var(--glass-section-blur)] sm:px-5`

    - Line 50 (primary CTA gradient — Archetype J — **NEVER SWEEP**): leave verbatim. Verify no co-located `backdrop-blur-*` or `backdrop-filter` in the same className string.

    - Line 73 (secondary CTA glass — Archetype B):
      BEFORE: `w-full rounded-3xl border border-white/60 bg-white/50 px-8 py-4 ... shadow-glass backdrop-blur-xl transition-[background-color,border-color,box-shadow,transform] duration-200 hover:bg-white/60 sm:w-auto`
      AFTER: `w-full rounded-3xl border border-white/60 bg-[var(--glass-section-fill)] px-8 py-4 ... shadow-glass backdrop-blur-[var(--glass-section-blur)] transition-[background-color,border-color,box-shadow,transform] duration-200 hover:bg-[var(--glass-card-fill)] sm:w-auto`

    The `transition-[...]` list MUST NOT contain `backdrop-filter` (anti-pattern #6 watch). Verify via grep after edit.

    **SocialProof.tsx — apply 93-PATTERNS.md table:**

    - Line 19 (stat card — Archetype C with explicit hover):
      BEFORE: `relative group flex flex-col items-center justify-center p-8 bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-white/60 shadow-glass hover:shadow-glass-lg hover:bg-white/70 transition-all duration-500 overflow-hidden`
      AFTER: `relative group flex flex-col items-center justify-center p-8 bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] border border-white/60 shadow-glass hover:shadow-glass-lg hover:bg-[var(--glass-form-fill)] transition-all duration-500 overflow-hidden`

    Use Edit tool. Preserve all Russian copy + `&nbsp;` / `\u00A0` JSX patterns verbatim. Preserve `text-balance`, all `text-mu-text-*` color classes, all border-* / shadow-* tokens.

    Do NOT modify CTA gradient at ServiceHero:50. Do NOT touch any text-gradient (`bg-clip-text text-transparent`) — none in these two files, but verify via grep.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/service/ServiceHero.tsx` returns 0 (eyebrow + secondary CTA glass swept)
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/service/SocialProof.tsx` returns 0
    - `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/service/ServiceHero.tsx` returns ≥1 (CTA gradient preserved)
    - CTA invariant: `grep -E 'from-mu-blue to-mu-accent-blue' next/src/components/sections/service/ServiceHero.tsx | grep -cE 'backdrop-blur|backdrop-filter'` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/service/ServiceHero.tsx` returns ≥2 (eyebrow + secondary CTA)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/service/SocialProof.tsx` returns ≥1
    - `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/service/SocialProof.tsx` returns ≥1 (Tier-2 hover ramp present)
    - No hardcoded blur literals after sweep: `grep -cE 'backdrop-blur-(xl|2xl|3xl)' next/src/components/sections/service/ServiceHero.tsx next/src/components/sections/service/SocialProof.tsx` returns 0
    - Russian copy preserved: `grep -c '\\u00A0' next/src/components/sections/service/ServiceHero.tsx` matches pre-sweep baseline (record baseline before edit; assert unchanged after)
  </acceptance_criteria>
  <done>
    Two primitive files swept; CTA at ServiceHero:50 verifiably opaque; build + lint green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sweep FAQ.tsx (Archetype D — closed Tier 1, button hover Tier 2; text gradient at :34 NEVER touched)</name>
  <files>next/src/components/sections/service/FAQ.tsx</files>
  <read_first>
    - next/src/components/sections/service/FAQ.tsx (whole file)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§FAQ.tsx line table)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-06-PLAN.md (FAQSection analog — Archetype D was applied there too; mirror the approach)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (Archetype D definition)
  </read_first>
  <action>
    Apply 93-PATTERNS.md FAQ table:

    - Line 44 (closed item wrapper — Archetype D Tier 1):
      BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2rem] border border-white/60 shadow-glass overflow-hidden`
      AFTER: `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2rem] border border-white/60 shadow-glass overflow-hidden`

    - Line 48 (button hover — Tier 2 ramp):
      BEFORE: `w-full flex items-center justify-between p-6 text-left text-lg font-extrabold text-mu-text-900 cursor-pointer hover:bg-white/80 transition-colors`
      AFTER: `w-full flex items-center justify-between p-6 text-left text-lg font-extrabold text-mu-text-900 cursor-pointer hover:bg-[var(--glass-form-fill)] transition-colors`

    - Line 34 (heading text gradient — TEXT FILL — **NEVER SWEEP**): leave verbatim. The `bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent` pattern is a text-fill mechanism (per RESEARCH §Pattern 4), NOT a glass surface.

    Preserve the accordion `aria-expanded` toggle, the smooth `max-height` transition (or whatever animation pattern the file uses — match Phase 92's FAQSection idiom if not already present). Preserve all Russian copy + `&nbsp;` / `\u00A0`.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/service/FAQ.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/service/FAQ.tsx` returns ≥1
    - `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/service/FAQ.tsx` returns ≥1
    - Text gradient preserved: `grep -c 'bg-clip-text' next/src/components/sections/service/FAQ.tsx` returns ≥1
    - No hardcoded blur literals: `grep -cE 'backdrop-blur-(xl|2xl|3xl)' next/src/components/sections/service/FAQ.tsx` returns 0
  </acceptance_criteria>
  <done>
    FAQ accordion swept Archetype D; closed Tier 1 + open hover Tier 2 ramp present; text gradient preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sweep LeadFormSection.tsx — Decision A (FLATTEN outer Tier 0); Decision C (NO honeypot/timing); KD-v9-002 inner panel</name>
  <files>next/src/components/sections/service/LeadFormSection.tsx</files>
  <read_first>
    - next/src/components/sections/service/LeadFormSection.tsx (whole file — currently 86 lines)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§LeadFormSection.tsx line table — Decision A + KD-v9-002 inner)
    - .planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md (Decision A, Decision C, Decision E)
    - .planning/PROJECT.md (KD-v9-002 — alpha=0.50 form-fill rationale)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-07-PLAN.md (ContactSection Path A precedent — flatten Tier 0 outer when no occluding gradient)
    - next/src/components/sections/ContactForm.tsx (FROZEN — read for reference only; do NOT modify)
  </read_first>
  <action>
    Three swaps, in order:

    **A — GlassCheckmark chip (line 19, Tier 3 decorative inner):**

    BEFORE: `w-6 h-6 bg-white/60 backdrop-blur-md border border-white/60 rounded-full flex items-center justify-center flex-shrink-0 shadow-glass-inner-strong`
    AFTER: `w-6 h-6 bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] border border-white/60 rounded-full flex items-center justify-center flex-shrink-0 shadow-glass-inner-strong`

    **B — Outer Tier 0 wrapper (line 47) — FLATTEN per Decision A:**

    BEFORE: `<div className="rounded-[3.5rem] border border-white/60 bg-white/60 p-6 shadow-glass-lg backdrop-blur-3xl md:p-12">`
    AFTER:  `<div className="rounded-[3.5rem] p-6 md:p-12">`

    Drop ALL of: `border border-white/60`, `bg-white/60`, `shadow-glass-lg`, `backdrop-blur-3xl`. Preserve `rounded-[3.5rem]`, `p-6`, `md:p-12`. The result is a non-glass container that holds the two-column grid; the inner Tier 2 form panel becomes the only glass surface in this component (anti-pattern #13 cleared — eliminates the outer-Tier-0 + inner-Tier-2 stack).

    **C — Inner Tier 2 form panel (line 72) — KD-v9-002 alpha=0.50 inheritance:**

    BEFORE: `rounded-[2.5rem] border border-white/60 bg-white/42 p-5 shadow-glass backdrop-blur-2xl sm:p-7 md:p-9`
    AFTER:  `rounded-[2.5rem] border border-white/60 bg-[var(--glass-form-fill)] p-5 shadow-glass backdrop-blur-[var(--glass-form-blur)] sm:p-7 md:p-9`

    The `bg-white/42` (custom 0.42 alpha) is replaced by the `--glass-form-fill` token, which resolves to `rgba(255,255,255,0.50)` per KD-v9-002 (see `globals.css:249`). This is a deliberate alpha BUMP from 0.42 → 0.50 to inherit Phase 92's form-safety contract (≥4.5:1 body-copy contrast over the worst-case blob luminance).

    **DO NOT propagate ContactForm BL-04 honeypot/timing pattern (Decision C):**

    The `<ContactForm />` import at line ~73 stays as-is. DO NOT add a honeypot field, a 3-second timing trap, a fake-success behavior, or any anti-bot instrumentation to LeadFormSection itself. ContactForm's BL-04 stays deferred for separate triage. If you find yourself tempted to add anti-bot — STOP. This is explicit per Decision C in 93-CONTEXT.md.

    Preserve: ScrollReveal wrapping in callers (out of scope here), Russian copy + `&nbsp;` / `\u00A0`, the entire 2-column grid layout, the trust-items list rendering, the `privacyText` conditional, the `id` prop default `'form'`.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/service/LeadFormSection.tsx` returns 0 (all 3 surfaces swept)
    - `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/service/LeadFormSection.tsx` returns ≥1 (GlassCheckmark)
    - `grep -c 'bg-\[var(--glass-form-fill)\]' next/src/components/sections/service/LeadFormSection.tsx` returns ≥1 (inner panel)
    - Outer wrapper flattened: `grep -E 'rounded-\[3\.5rem\].*backdrop-blur' next/src/components/sections/service/LeadFormSection.tsx | wc -l` returns 0 (the rounded-[3.5rem] container has NO backdrop-blur after flatten)
    - Outer wrapper preserves layout props: `grep -c 'rounded-\[3\.5rem\] p-6 md:p-12' next/src/components/sections/service/LeadFormSection.tsx` returns ≥1
    - No hardcoded blur literals: `grep -cE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/service/LeadFormSection.tsx` returns 0
    - ContactForm import preserved + frozen: `grep -c "from '@/components/sections/ContactForm'" next/src/components/sections/service/LeadFormSection.tsx` returns ≥1; `git diff next/src/components/sections/ContactForm.tsx` is EMPTY (Phase 92 territory frozen)
    - Decision C honored: `grep -ciE '(honeypot|timing|antibot|anti-bot|bot.detect)' next/src/components/sections/service/LeadFormSection.tsx` returns 0 (no anti-bot pattern propagated)
    - Russian copy preserved: nbsp count unchanged from pre-sweep baseline
  </acceptance_criteria>
  <done>
    LeadFormSection: outer flattened, inner KD-v9-002 alpha=0.50, GlassCheckmark Tier 3, no honeypot propagation. Anti-pattern #13 cleared on this component.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wave 1 close-out — full build + lint + Wave 0 visual diff for service-primitive consumers</name>
  <files>(no source modifications — verification only)</files>
  <read_first>
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Per-Task Verification Map — 93-01-* rows)
  </read_first>
  <action>
    Run the wave-end verification suite:

    1. `pnpm --dir next build` — full build green.
    2. `pnpm --dir next lint` — zero new warnings vs pre-sweep baseline.
    3. Wave 0 visual-diff smoke (sub-routes that import the swept primitives now look different — this is EXPECTED and correctly captured by Phase 93 baseline diffs in Wave 2; Wave 1 itself is not expected to be visual-diff-clean against the Wave 0 baseline because primitives have changed).

       Run `pnpm --dir next exec playwright test tests/visual --reporter=list` and DOCUMENT the failures. The expectation: at least `/checkup`, `/consultations`, `/treatment-abroad` will show diffs (they import ServiceHero / FAQ / SocialProof / LeadFormSection). `/contacts` will diff because it imports LeadFormSection. THIS IS EXPECTED. Do NOT regenerate the baseline (`--update-snapshots` is forbidden in Wave 1; baseline is locked at Wave 0).

       Capture the diff list in the SUMMARY: which routes diffed, which pixel ratios, on which projects. Wave 2 plans will accept those diffs as the "expected delta" once each route's per-route sweep also lands.

    4. CTA invariant cross-cutting grep:
       ```bash
       grep -rn 'from-mu-blue to-mu-accent-blue' next/src/components/sections/service/ \
         | xargs -I {} sh -c 'echo "{}"; grep -E "(backdrop-blur|backdrop-filter)" {}' 2>/dev/null
       ```
       Expected: zero CTA lines co-located with `backdrop-*`. If non-zero — fail and return to Task 1 to fix.

    5. Anti-pattern #13 audit on LeadFormSection: `grep -cE 'backdrop-blur|backdrop-filter' next/src/components/sections/service/LeadFormSection.tsx` should return ≤2 (chip + inner panel = exactly 2 glass surfaces inside this component; outer wrapper now non-glass per Decision A).
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0 with no new warnings vs pre-sweep baseline
    - CTA invariant cross-cutting grep returns 0 hits across `service/` directory
    - LeadFormSection backdrop-blur count = exactly 2 (anti-pattern #13 cleared on this component)
    - SUMMARY.md documents visual-diff status per route (expected diffs from primitive sweep recorded for Wave 2 consumption)
  </acceptance_criteria>
  <done>
    Wave 1 closed; primitives swept; CTA invariant intact; expected visual diffs documented for Wave 2 plans.
  </done>
</task>

</tasks>

<acceptance_gates>
**Positive gates (per-file):**
- `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/service/ServiceHero.tsx` ≥ 2
- `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/service/SocialProof.tsx` ≥ 1
- `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/service/FAQ.tsx` ≥ 1
- `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/service/FAQ.tsx` ≥ 1
- `grep -c 'bg-\[var(--glass-form-fill)\]' next/src/components/sections/service/LeadFormSection.tsx` ≥ 1
- `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/service/LeadFormSection.tsx` ≥ 1

**Negative gates (CTA invariant + residue):**
- `grep -rcE 'bg-white/[0-9]+' next/src/components/sections/service/` → 0
- `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/service/` → 0 (only `[var(...)]` arbitrary-value form survives)
- CTA opaque-forever: `grep -E 'from-mu-blue to-mu-accent-blue' next/src/components/sections/service/ServiceHero.tsx | grep -cE 'backdrop-blur|backdrop-filter'` → 0
- Decision C honored: `grep -irciE '(honeypot|timing.{0,5}trap|antibot|anti-bot)' next/src/components/sections/service/` → 0
- LeadFormSection outer flattened: `grep -E 'rounded-\[3\.5rem\].*backdrop' next/src/components/sections/service/LeadFormSection.tsx` returns ZERO matches

**Build gates:**
- `pnpm --dir next build` exit 0
- `pnpm --dir next lint` exit 0
</acceptance_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LeadFormSection ↔ ContactForm (frozen) | LeadFormSection wraps ContactForm; this plan changes only the wrapper, never the form internals. ContactForm validation/submission contract unchanged. |
| Service primitive ↔ 4 sub-route consumers | One sweep here propagates to /checkup, /consultations, /treatment-abroad, /contacts via import graph. A regression here breaks 4 routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-01 | Tampering | Form submission integrity (LeadFormSection) — sweep accidentally breaks the `<ContactForm />` integration (e.g., removes the wrapper that supplies the form, breaks layout, intercepts the form via JS) | mitigate | Tasks 3 explicitly frozen `<ContactForm />` import; Wave 3 Plan 07 submission-path E2E confirms Directus arrival from all 4 routes |
| T-93-01-02 | Tampering | Visual-regression in non-blob region (CTA position shifts, primitive layout breaks) | mitigate | Wave 0 baseline + Wave 2 per-route diff catches; Task 4 explicitly captures the expected primitive-sweep diff list for Wave 2 consumption |
| T-93-01-03 | Tampering | CTA invariant breakage (ServiceHero:50 accidentally swept to glass tier; CTA disappears into blob — PITFALL #3.1, brand-trust threat) | mitigate | Task 1 explicit "NEVER SWEEP" callout on line 50; acceptance criteria greps the gradient + co-location check |
| T-93-01-04 | Information disclosure | A11y branch coverage regression — sweep accidentally introduces an arbitrary-value class that bypasses the Phase 92 BL-02 fix in globals.css :root (which rewrites tokens under prefers-reduced-transparency / prefers-contrast: more) | mitigate | Class-swap pattern (Decision E) means EVERY new class is `bg-[var(--glass-*)]` — token consumer — automatically covered by the BL-02 fix. Negative grep for `bg-[#hexcode]` and `bg-white/N` confirms zero arbitrary-value bypass |
| T-93-01-05 | Tampering | Decision C violation — anti-bot pattern (honeypot/timing) sneaks in despite explicit prohibition | mitigate | Acceptance gate greps for honeypot/timing/antibot keywords across `service/` directory; returns 0 by contract |
</threat_model>

<deviation_rules>
- The planner does NOT introduce new archetypes. All swaps cite Archetype letters from `92-PATTERNS.md`.
- The planner does NOT modify Phase 92 deliverables (`ContactForm.tsx`, `ContactSection.tsx`, `liquid-glass.css`, `globals.css`).
- The planner does NOT propagate ContactForm BL-04 honeypot/timing to LeadFormSection (Decision C — separate triage).
- The planner does NOT touch ServiceHero CTA gradient at line 50 (Archetype J — opaque-forever invariant).
- The planner does NOT touch FAQ heading text gradient at line 34 (text-fill, not glass).
- The planner does NOT regenerate Wave 0 baseline snapshots (`--update-snapshots` forbidden in Wave 1).
- If the planner discovers any of the four files already matches the archetype pattern (no `bg-white/N` or hardcoded `backdrop-blur-*` residue), the corresponding task becomes a verify-only no-op for that file.
</deviation_rules>

<verification>
1. `pnpm --dir next build` exits 0.
2. `pnpm --dir next lint` exits 0.
3. All acceptance gates above return the expected counts.
4. CTA invariant negative-grep returns 0 hits across `next/src/components/sections/service/`.
5. ContactForm.tsx unchanged: `git diff --stat next/src/components/sections/ContactForm.tsx` empty.
6. LeadFormSection backdrop-blur count = 2 (anti-pattern #13 cleared on this component).
</verification>

<success_criteria>
- ROUTE-05 satisfied: 4 service primitives swept to v9.0 tier tokens
- Decision A executed: LeadFormSection outer Tier 0 flattened
- Decision C honored: no anti-bot pattern propagated
- Decision E inherited: KD-v9-002 alpha=0.50 on inner form panel via token consumption
- CTA opaque-forever invariant intact (ServiceHero:50)
- Build + lint green
- Visual-diff status per route documented for Wave 2 consumption
</success_criteria>

<output>
After completion, create `.planning/phases/93-per-page-propagation-sub-routes/93-01-SUMMARY.md`. Note: (a) per-file grep counts, (b) which sub-route Playwright tests showed expected diffs (for Wave 2 to absorb), (c) confirmation Decision C was honored (no honeypot grep hits), (d) confirmation ContactForm.tsx was NOT modified.
</output>
