---
phase: 93-per-page-propagation-sub-routes
plan: 00
type: execute
wave: 0
subsystem: test-infrastructure
depends_on: []
files_modified:
  - next/package.json
  - next/playwright.config.ts
  - next/tests/visual/baseline.spec.ts
  - next/tests/e2e/submission.spec.ts
  - next/tests/visual/__snapshots__/ (initial baseline PNGs)
  - .gitignore (if needed for test-results/)
autonomous: false
requirements:
  - ROUTE-07
must_haves:
  truths:
    - "Playwright dev dependency installed in next/package.json (locked version)"
    - "Chromium browser binary available locally for Playwright"
    - "next/playwright.config.ts defines exactly two projects: desktop (1280x800) and mobile-375 (375x667)"
    - "next/tests/visual/baseline.spec.ts captures 4 routes (`/checkup`, `/consultations`, `/treatment-abroad`, `/contacts`) x 2 breakpoints = 8 screenshots, with `.living-blob-field` masked"
    - "Determinism strategy applied: window.__blobDebug.setMode?.('static') if available, else page.emulateMedia({ reducedMotion: 'reduce' })"
    - "next/tests/e2e/submission.spec.ts skeleton exists (will run in Wave 3 against live Directus)"
    - "Initial baseline snapshots committed (or stored per repo convention) BEFORE any Wave 1+ commits"
  artifacts:
    - path: "next/playwright.config.ts"
      provides: "Two-project Playwright config (desktop, mobile-375); test-results output; expected snapshots dir"
      contains: "defineConfig"
    - path: "next/tests/visual/baseline.spec.ts"
      provides: "Per-route visual-regression spec with blob mask + deterministic blob mode"
    - path: "next/tests/e2e/submission.spec.ts"
      provides: "Submission-path E2E spec skeleton (executed in Wave 3 Plan 07)"
  key_links:
    - from: "next/tests/visual/baseline.spec.ts"
      to: "Wave 0 baseline snapshots"
      via: "expect(page).toHaveScreenshot({ mask: [page.locator('.living-blob-field')] })"
      pattern: "toHaveScreenshot.*mask"
    - from: "Wave 1+ task verification"
      to: "Wave 0 baseline"
      via: "playwright test --project=visual-regression --grep \"<route>\""
      pattern: "playwright test"
---

<objective>
Establish Phase 93 test infrastructure (Playwright install, configuration, baseline capture) BEFORE any class-sweep work begins. This plan is a hard prerequisite for Waves 1, 2, and 3.

**Three deliverables:**

1. Install `@playwright/test` as a dev dependency in `next/package.json` and install the Chromium browser binary.
2. Create `next/playwright.config.ts` with two projects (desktop 1280x800, mobile-375 375x667), `outputDir: test-results/`, and snapshot path config.
3. Create `next/tests/visual/baseline.spec.ts` (per-route visual-regression spec masking `.living-blob-field` and pinning blob mode for determinism) plus `next/tests/e2e/submission.spec.ts` skeleton, then capture initial baseline snapshots from `feat/v3.1` HEAD (post-Phase-92 merge) by running with `--update-snapshots`.

**Why autonomous=false:** Step 1 requires `pnpm add -D` (writes to `package.json` and lockfile) plus `playwright install chromium` which downloads a browser binary (~150MB). User confirmation is appropriate before mutating dependencies and pulling binaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md
@.planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md
@.planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md
@.planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md
@CLAUDE.md
@next/package.json

<interfaces>
<!-- Phase 91 dev surface (verify availability at runtime in Task 1): -->
<!--   window.__blobDebug?.setMode?.('static')  → pins the blob to a static position -->
<!--   window.__blobDebug?.rafCount             → reused later in Phase 94 leak tests -->
<!-- If __blobDebug is not exposed, fallback: page.emulateMedia({ reducedMotion: 'reduce' }) -->

<!-- Routes under Phase 93 sweep (target visual-regression coverage): -->
- /checkup
- /consultations
- /treatment-abroad
- /contacts

<!-- Determinism contract (RESEARCH §Pattern 4 + 93-PATTERNS.md "Wave 0 Playwright determinism"): -->
- Mask: page.locator('.living-blob-field')
- Viewport: desktop 1280x800, mobile-375 375x667 (Pixel 5 user-agent acceptable)
- Mode pin: __blobDebug.setMode('static') OR reducedMotion: 'reduce' fallback
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Confirm dev-dependency install + browser binary download</name>
  <what-built>Wave 0 will run `pnpm --dir next add -D @playwright/test` followed by `pnpm --dir next exec playwright install chromium`. The first writes to `next/package.json` + `pnpm-lock.yaml`. The second downloads the Chromium browser binary (~150MB) into the local Playwright cache.</what-built>
  <how-to-verify>
    Confirm:
    1. You are okay adding `@playwright/test` as a dev dependency (production deps unchanged).
    2. You are okay downloading the Chromium browser binary locally (the binary is not committed; it lives in the Playwright user cache).
    3. CLAUDE.md "production npm dependencies forbidden, dev tools only for testing" rule allows this — Playwright is a dev tool, not a production dep.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe a different install strategy.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install Playwright dev dependency + Chromium binary; verify __blobDebug surface availability</name>
  <files>next/package.json, pnpm-lock.yaml</files>
  <read_first>
    - next/package.json (existing devDependencies block)
    - .planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md (§Standard Stack — Wave 0 NEW devDependency)
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <action>
    Execute these commands in order (cwd = repo root):

    1. `pnpm --dir next add -D @playwright/test` — adds latest stable Playwright (1.49+ as of 2026-04-30) to `next/package.json` devDependencies.
    2. `pnpm --dir next exec playwright install chromium` — downloads the Chromium browser binary required for `expect(page).toHaveScreenshot()`.
    3. Verify install: `pnpm --dir next exec playwright --version` should print a version string (≥1.49.0). Echo the resolved version into the SUMMARY for traceability.
    4. **Verify Phase 91 `__blobDebug` surface availability** — start the dev server (`pnpm --dir next dev` in background), navigate to `http://localhost:3000/` in a Playwright instance, evaluate `await page.evaluate(() => typeof window.__blobDebug?.setMode === 'function')`. Capture the boolean result. If `true` → use `__blobDebug.setMode('static')` for determinism in Task 3. If `false` → use `page.emulateMedia({ reducedMotion: 'reduce' })` fallback.

    Do NOT modify production dependencies. Do NOT add Playwright to `dependencies` (must stay in `devDependencies`).
  </action>
  <verify>
    <automated>pnpm --dir next exec playwright --version | grep -E 'Version 1\.(49|5[0-9]|6[0-9])\.'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next exec playwright --version` exits 0 and prints `Version 1.49+`
    - `grep -c '"@playwright/test"' next/package.json` returns ≥1, AND the entry sits inside the `"devDependencies"` block (not `"dependencies"`)
    - `ls ~/Library/Caches/ms-playwright/ 2>/dev/null | grep -c chromium` returns ≥1 (binary present in user cache; macOS path)
    - Determinism strategy decided and recorded in the task's SUMMARY: `STRATEGY=__blobDebug` OR `STRATEGY=reducedMotion`. Used by Task 3.
  </acceptance_criteria>
  <done>
    Playwright installed; Chromium binary cached; determinism strategy chosen and recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create next/playwright.config.ts with desktop + mobile-375 projects</name>
  <files>next/playwright.config.ts</files>
  <read_first>
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Test Infrastructure table — viewports, projects)
    - .planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md (§Standard Stack — devDependency notes)
  </read_first>
  <action>
    Create `next/playwright.config.ts` using `defineConfig` from `@playwright/test` with these properties:

    - `testDir: './tests'`
    - `outputDir: './test-results'`
    - `snapshotPathTemplate: '{testDir}/{testFilePath}/__snapshots__/{arg}-{projectName}{ext}'`
    - `fullyParallel: true`
    - `forbidOnly: !!process.env.CI`
    - `retries: process.env.CI ? 2 : 0`
    - `reporter: [['list']]`
    - `use: { baseURL: 'http://localhost:3000', trace: 'retain-on-failure' }`
    - `webServer: { command: 'pnpm dev', url: 'http://localhost:3000', reuseExistingServer: !process.env.CI, timeout: 120_000 }`
    - `projects:`
      - `{ name: 'desktop', use: { viewport: { width: 1280, height: 800 } } }`
      - `{ name: 'mobile-375', use: { viewport: { width: 375, height: 667 }, deviceScaleFactor: 2 } }`

    Add `next/test-results/` and `next/playwright-report/` to `.gitignore` if not already present (use Edit tool on root `.gitignore`).

    Do NOT introduce additional projects. Do NOT enable Firefox / WebKit browsers (CLAUDE.md and 93-RESEARCH §Standard Stack standardize on Chromium-only for Phase 93; multi-browser is Phase 94 territory).
  </action>
  <verify>
    <automated>pnpm --dir next exec playwright test --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `next/playwright.config.ts` exists
    - `grep -c "name: 'desktop'" next/playwright.config.ts` returns 1
    - `grep -c "name: 'mobile-375'" next/playwright.config.ts` returns 1
    - `grep -c 'webServer' next/playwright.config.ts` returns ≥1
    - `pnpm --dir next exec playwright test --list` exits 0 (config parses cleanly even before specs exist)
    - Root `.gitignore` contains `test-results/` and `playwright-report/` entries (or already gitignored equivalents)
  </acceptance_criteria>
  <done>
    Playwright config valid; two projects registered; output dirs gitignored.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create visual-regression spec + submission E2E skeleton; capture initial baseline snapshots</name>
  <files>next/tests/visual/baseline.spec.ts, next/tests/e2e/submission.spec.ts, next/tests/visual/__snapshots__/ (PNGs)</files>
  <read_first>
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Wave 0 Requirements + Manual-Only Verifications)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 0 Playwright determinism)
    - .planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md (Per-Route Inventory — confirms 4 sub-routes)
    - Task 1 SUMMARY (read the determinism `STRATEGY` value chosen)
  </read_first>
  <action>
    **3a — Create `next/tests/visual/baseline.spec.ts`:**

    A `test.describe.parallel('visual-regression', ...)` block iterating over an array `routes = ['/checkup', '/consultations', '/treatment-abroad', '/contacts']`. For each route:

    - `test(\`${route} — visual baseline\`, async ({ page }) => { ... })`
    - In `beforeEach`: if `STRATEGY=__blobDebug`, call `await page.addInitScript(() => { window.__BLOB_FORCE_STATIC__ = true; })` then after `goto`, `await page.evaluate(() => window.__blobDebug?.setMode?.('static'))`. If `STRATEGY=reducedMotion`, call `await page.emulateMedia({ reducedMotion: 'reduce' })` BEFORE `goto`.
    - `await page.goto(route, { waitUntil: 'networkidle' })`
    - `await page.waitForTimeout(500)` to settle any post-load animations.
    - `await expect(page).toHaveScreenshot(\`${route.slice(1) || 'home'}.png\`, { mask: [page.locator('.living-blob-field')], maxDiffPixelRatio: 0.01 })`

    The spec MUST use `route.slice(1) || 'home'` for the snapshot filename so `/checkup` → `checkup.png` etc.

    Add `test.describe.configure({ mode: 'serial' })` if you observe baseline flake from parallel runs against a single dev server.

    **3b — Create `next/tests/e2e/submission.spec.ts` (skeleton only — full body executed in Wave 3 Plan 07):**

    ```ts
    import { test, expect } from '@playwright/test';

    test.describe.skip('submission-e2e (Wave 3 Plan 07 only)', () => {
      const routes = ['/checkup', '/consultations', '/treatment-abroad', '/contacts'];
      for (const route of routes) {
        test(`${route} — submission reaches Directus`, async ({ page }) => {
          // Plan 07 Task: implement submission flow + Directus arrival check + cleanup
          // Marker: [E2E] test record with timestamp suffix
          test.fail(); // placeholder so the spec is harmless on Wave 0 baseline runs
        });
      }
    });
    ```

    The `.skip` ensures it does not execute during Wave 0 / Wave 1 / Wave 2 verification runs. Plan 07 will replace `.skip` with `()` and implement the body.

    **3c — Capture initial baseline snapshots:**

    Run `pnpm --dir next exec playwright test tests/visual --update-snapshots`. This:
    - Boots the dev server via `webServer.command`
    - Navigates to each route on each project (4 routes × 2 projects = 8 snapshots)
    - Writes 8 PNGs into `next/tests/visual/baseline.spec.ts/__snapshots__/`
    - Snapshots reflect post-Phase-92, pre-Phase-93 state (current `feat/v3.1` HEAD)

    **3d — Re-run WITHOUT `--update-snapshots` to confirm green:**

    `pnpm --dir next exec playwright test tests/visual`. Must pass (8/8 green). If any test fails on this re-run, investigate non-determinism (likely `__blobDebug.setMode` not actually pinning, or `network idle` returning before late hydration); fix before sign-off.

    Do NOT commit `test-results/` or `playwright-report/` directories. Do commit the `__snapshots__/` PNGs (they are the pre-Phase-93 source of truth Phase 93 sweeps validate against).
  </action>
  <verify>
    <automated>pnpm --dir next exec playwright test tests/visual --reporter=list 2>&1 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - File `next/tests/visual/baseline.spec.ts` exists; contains all 4 routes
    - File `next/tests/e2e/submission.spec.ts` exists with `test.describe.skip(...)` (skipped placeholder)
    - `ls next/tests/visual/baseline.spec.ts/__snapshots__/ | wc -l` returns ≥8 (4 routes × 2 projects)
    - `pnpm --dir next exec playwright test tests/visual --reporter=list` exits 0 (8 passed)
    - `grep -c "page.locator('.living-blob-field')" next/tests/visual/baseline.spec.ts` returns ≥1 (mask applied)
    - `git status next/tests/visual/__snapshots__/ next/playwright.config.ts next/tests/` shows the new files staged for commit
    - `pnpm --dir next build` exits 0 (no production-bundle regression — test files are dev-only)
  </acceptance_criteria>
  <done>
    Pre-Phase-93 baseline locked. Wave 1+ visual diffs run against this baseline. Submission E2E skeleton ready for Plan 07.
  </done>
</task>

</tasks>

<acceptance_gates>
**Positive gates:**
- `pnpm --dir next exec playwright --version` → ≥1.49.0
- `pnpm --dir next exec playwright test tests/visual --list` → 8 tests listed (4 routes × 2 projects)
- `pnpm --dir next exec playwright test tests/visual` → 8 passed (deterministic re-run)
- `pnpm --dir next build` → exit 0
- `pnpm --dir next lint` → exit 0

**Negative gates:**
- `grep '"@playwright/test"' next/package.json | grep -v devDependencies` → must be empty (Playwright NOT in production deps)
- `git diff next/src/` → must be empty (Wave 0 touches ZERO source files; only `next/package.json`, `next/playwright.config.ts`, `next/tests/`, root `.gitignore`)
</acceptance_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev cache (Playwright) ↔ repo working tree | Browser binary downloaded into `~/Library/Caches/ms-playwright`; not committed. Repo working tree only gains the dev dep entry + config + spec + baseline PNGs. |
| Wave 0 baseline ↔ Wave 1+ diffs | Baseline PNGs are the golden source-of-truth Phase 93 sweeps validate against. If baseline is captured POST-sweep accidentally, every Phase 93 diff is meaningless. Sequencing matters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-00-01 | Tampering | Phase 93 sweep work commits BEFORE Wave 0 baseline is captured (baseline reflects swept state, not pre-sweep state — Wave 1+ diffs become meaningless) | mitigate | Plan 00 ships before Plan 01 (depends_on chain enforces); Task 3c captures snapshot before any Wave 1 commit; checkpoint Task 0 confirms user is aware |
| T-93-00-02 | Tampering | Playwright accidentally added to `dependencies` (production) instead of `devDependencies` (CLAUDE.md rule violation) | mitigate | Task 1 explicitly uses `-D` flag; Acceptance gate greps for `"@playwright/test"` outside the `devDependencies` block |
| T-93-00-03 | DoS | `__blobDebug.setMode('static')` is unavailable, baselines flake on rerun, blocking the entire phase | mitigate | Task 1 step 4 verifies surface availability ahead of time; if unavailable, fall back to `reducedMotion` and document strategy in Task 1 SUMMARY |
| T-93-00-04 | Repudiation | Wave 0 ships without `submission.spec.ts` skeleton; Plan 07 has no contract to extend, blocking Wave 3 | mitigate | Task 3b creates the `.skip`'d spec so Plan 07 just removes `.skip` and fills the body |
</threat_model>

<deviation_rules>
- The planner does not modify Phase 92 files or any source TSX file in this plan. Wave 0 is exclusively test-infrastructure.
- Playwright stays a dev dependency. Never `pnpm add` without `-D`.
- Browser binary is NOT committed; user-cache only.
- No Firefox or WebKit projects (Phase 94 territory).
- Determinism via `__blobDebug.setMode('static')` is preferred; `reducedMotion` is the documented fallback.
- The `submission-e2e` describe block stays `.skip`'d in this plan; Plan 07 unskips and implements the body.
</deviation_rules>

<verification>
1. `pnpm --dir next exec playwright --version` exits 0 with version ≥1.49.0.
2. `pnpm --dir next exec playwright test tests/visual` exits 0 (8/8 green) on a fresh `git stash` && `git stash pop` cycle.
3. `pnpm --dir next build` exits 0.
4. `git diff next/src/ next/public/` is empty (zero source files touched).
5. Determinism strategy recorded in plan SUMMARY (`__blobDebug` or `reducedMotion`).
</verification>

<success_criteria>
- Playwright (`@playwright/test`) installed as devDependency in `next/package.json`
- `next/playwright.config.ts` defines exactly 2 projects (desktop, mobile-375)
- `next/tests/visual/baseline.spec.ts` covers 4 sub-routes with `.living-blob-field` mask
- `next/tests/e2e/submission.spec.ts` skeleton exists (skipped, awaiting Plan 07)
- 8 baseline snapshots committed at `next/tests/visual/baseline.spec.ts/__snapshots__/`
- Re-running `playwright test tests/visual` (without `--update-snapshots`) is green
- ROUTE-07 unblocked for Wave 1+
</success_criteria>

<output>
After completion, create `.planning/phases/93-per-page-propagation-sub-routes/93-00-SUMMARY.md`. Note: (a) Playwright version pinned, (b) determinism strategy chosen (`__blobDebug` or `reducedMotion`), (c) baseline snapshot count (expected 8), (d) any flake observed during Task 3d re-run + how it was resolved.
</output>
