---
phase: 93-per-page-propagation-sub-routes
plan: 06
type: execute
wave: 3
subsystem: shadcn-verification
depends_on:
  - 00
  - 01
  - 02
  - 03
  - 04
  - 05
files_modified:
  - .planning/phases/93-per-page-propagation-sub-routes/93-06-SHADCN-AUDIT.md
autonomous: true
requirements:
  - ROUTE-06
must_haves:
  truths:
    - "shadcn primitives (card, dialog, input, select, textarea) verified as admin-only consumer impact (sole consumer = next/src/app/admin/submissions-table.tsx)"
    - "next/src/components/ui/dialog.tsx:34 modal overlay future-proof note RECORDED in audit (Tier 0 token swap recipe documented; not executed because no public consumer exists today)"
    - "next/src/components/ui/{card,input,select,textarea}.tsx — verify-only no-op (admin-only; bg-card / bg-transparent; not glass-tier in v9.0 contract)"
    - "next/src/app/admin/submissions-table.tsx — verify-only no-op (private admin route; out of v9.0 scope)"
    - "Source files in next/src/components/ui/ UNTOUCHED in this plan"
  artifacts:
    - path: ".planning/phases/93-per-page-propagation-sub-routes/93-06-SHADCN-AUDIT.md"
      provides: "Read-only audit confirming shadcn = admin-only impact; per-primitive disposition table; future-proof recipe for dialog.tsx:34 modal overlay"
  key_links:
    - from: "Phase 93 future modal landings"
      to: "93-06-SHADCN-AUDIT.md §dialog.tsx:34 future-proof recipe"
      via: "documented Tier 0 token swap when a public modal lands"
      pattern: "bg-\\[var\\(--glass-section-fill\\)\\]"
---

<objective>
Verify shadcn primitives compliance with v9.0 token contract WITHOUT modifying any shadcn source file. This plan is intentionally a verify-only audit:

- Research and Phase 93 PATTERNS confirmed: shadcn primitives in `next/src/components/ui/` are consumed ONLY by the private admin route at `next/src/app/admin/submissions-table.tsx`. Public routes use direct Tailwind classes (Decision E from Phase 92 — class-swap, not utility migration).
- The single public-impactful surface in shadcn is `dialog.tsx:34` (modal overlay using `bg-black/10 ... supports-backdrop-filter:backdrop-blur-xs`). No public consumer triggers this dialog today; future-proofing it is recorded in this audit but NOT executed (the swap would change admin behavior with no public benefit).

**Three deliverables:**

1. Read-only audit producing `93-06-SHADCN-AUDIT.md` with per-primitive disposition: SKIP (verify-only) for `card.tsx`, `input.tsx`, `select.tsx`, `textarea.tsx`; SKIP-WITH-DOC for `dialog.tsx`.
2. Repo-wide consumer grep confirming `next/src/app/admin/submissions-table.tsx` is the SOLE consumer of shadcn primitives.
3. Future-proof recipe for `dialog.tsx:34` documented: when/if a public modal lands, swap to `bg-[var(--glass-section-fill)] supports-backdrop-filter:backdrop-blur-[var(--glass-section-blur)]` (Tier 0 token).

**Why files_modified is just the audit doc:** Per Decision F (extended) + Discretion item 6 from 93-RESEARCH, shadcn migration is deferred to a separate "shadcn audit" phase if/when public routes start consuming. Phase 93 ROUTE-06 verifies admin-only impact + records future-proof recipe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md
@.planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md
@.planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md
@.planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@next/src/components/ui/card.tsx
@next/src/components/ui/dialog.tsx
@next/src/components/ui/input.tsx
@next/src/components/ui/select.tsx
@next/src/components/ui/textarea.tsx
@next/src/app/admin/submissions-table.tsx

<interfaces>
<!-- shadcn = admin-only impact (verified by 93-RESEARCH §Standard Stack — Sub-route component file inventory): -->
<!-- Sole consumer: next/src/app/admin/submissions-table.tsx -->
<!-- Public routes do NOT import from @/components/ui/{card,dialog,input,select,textarea}. -->

<!-- dialog.tsx:34 modal overlay (current state): -->
<!--   fixed inset-0 isolate z-50 bg-black/10 duration-100 supports-backdrop-filter:backdrop-blur-xs ... -->
<!-- Future-proof Tier 0 swap (documented, NOT executed): -->
<!--   fixed inset-0 isolate z-50 bg-[var(--glass-section-fill)] duration-100 supports-backdrop-filter:backdrop-blur-[var(--glass-section-blur)] ... -->

<!-- Other shadcn primitives use bg-card / bg-transparent — not glass-tier; out of v9.0 scope. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit shadcn primitives consumer reach + per-primitive glass-tier classification; produce 93-06-SHADCN-AUDIT.md</name>
  <files>.planning/phases/93-per-page-propagation-sub-routes/93-06-SHADCN-AUDIT.md</files>
  <read_first>
    - next/src/components/ui/card.tsx (whole file)
    - next/src/components/ui/dialog.tsx (whole file — focus on line 34)
    - next/src/components/ui/input.tsx
    - next/src/components/ui/select.tsx
    - next/src/components/ui/textarea.tsx
    - next/src/app/admin/submissions-table.tsx (consumer reference — read-only)
    - .planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md (§shadcn primitives table)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 3 — shadcn primitives)
  </read_first>
  <action>
    Produce `.planning/phases/93-per-page-propagation-sub-routes/93-06-SHADCN-AUDIT.md` with these sections:

    **Section 1: Repo-wide consumer reach grep**

    Run and embed verbatim:
    ```bash
    grep -rln "from '@/components/ui/card'" next/src/
    grep -rln "from '@/components/ui/dialog'" next/src/
    grep -rln "from '@/components/ui/input'" next/src/
    grep -rln "from '@/components/ui/select'" next/src/
    grep -rln "from '@/components/ui/textarea'" next/src/
    ```
    Expected: each grep returns ONLY `next/src/app/admin/submissions-table.tsx` (or zero matches if a primitive isn't even imported by admin). NO public-route file should appear.

    Document the result. If a NON-ADMIN public-route consumer is discovered, STOP — this contradicts 93-RESEARCH. Surface to user.

    **Section 2: Per-primitive glass-tier classification**

    For each of the 5 primitives, embed the relevant lines from the source file and classify:

    | File | Line(s) | Token used | Glass-tier? | Disposition |
    |------|---------|------------|-------------|-------------|
    | `card.tsx` | (locate `bg-card` reference) | `bg-card` (project token, not v9 glass token) | NO | SKIP — verify-only no-op |
    | `dialog.tsx` | 34 | `bg-black/10 ... supports-backdrop-filter:backdrop-blur-xs` | YES (modal overlay = Tier 0 candidate) | SKIP-WITH-DOC — future-proof recipe recorded; not executed (no public consumer today) |
    | `input.tsx` | (locate background ref) | `bg-transparent` | NO | SKIP |
    | `select.tsx` | (locate background ref) | `bg-transparent` or `bg-input/50` | NO | SKIP |
    | `textarea.tsx` | (locate background ref) | `bg-transparent` | NO | SKIP |

    For each row, embed the actual className snippet from source as evidence.

    **Section 3: dialog.tsx:34 future-proof recipe (Pitfall #5 prevention)**

    Document the swap recipe for when/if a public modal lands:

    ```
    BEFORE (current admin-only):
      fixed inset-0 isolate z-50 bg-black/10 duration-100 supports-backdrop-filter:backdrop-blur-xs data-open:animate-in data-open:fade-in-0 data-closed:animate-out data-closed:fade-out-0

    AFTER (recommended when first public modal lands):
      fixed inset-0 isolate z-50 bg-[var(--glass-section-fill)] duration-100 supports-backdrop-filter:backdrop-blur-[var(--glass-section-blur)] data-open:animate-in data-open:fade-in-0 data-closed:animate-out data-closed:fade-out-0
    ```

    Rationale (record verbatim): "The current `bg-black/10` would let the v9.0 living blob bleed through visibly, breaking modal-as-occlusion-layer expectation. Tier 0 token (`--glass-section-fill` ~0.06 desktop / 0.10 mobile) is the closest semantic match for a modal scrim that needs to dim background while remaining visually consistent with the v9.0 4-tier system. Swap is opt-in — execute only when a public modal trigger lands."

    Tag this as a "Pitfall-5 future-proofing note" per 93-PATTERNS.md.

    **Section 4: admin/submissions-table.tsx out-of-scope evidence**

    Confirm that:
    - `next/src/app/admin/submissions-table.tsx` is in the admin route group (private; not in the public v9.0 token migration scope per 93-CONTEXT.md `<deferred>` block).
    - The admin route does NOT render the `<LivingBlobField />` (which only mounts in the public layout). Therefore admin glass surfaces have no blob behind them — the v9.0 transparency contract has no observable effect there.
    - Verify-only: no sweep is required.

    Embed the route group structure (e.g., `next/src/app/admin/layout.tsx` if present; show that it wraps differently from the public `next/src/app/layout.tsx`).

    **Section 5: Sign-off**

    Append:
    ```
    SHADCN AUDIT COMPLETE: <date>
    shadcn primitives verified as admin-only consumer impact.
    SOLE consumer: next/src/app/admin/submissions-table.tsx.
    Public-route consumers: ZERO.
    dialog.tsx:34 future-proof recipe recorded; not executed.
    Source files modified by this plan: ZERO (only the audit doc).
    ROUTE-06 satisfied as verify-only.
    ```

    **DO NOT MODIFY** any shadcn source file or any source file under `next/src/`. This plan is read-only audit + future-proof documentation. Write only `93-06-SHADCN-AUDIT.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/93-per-page-propagation-sub-routes/93-06-SHADCN-AUDIT.md && grep -c 'SHADCN AUDIT COMPLETE' .planning/phases/93-per-page-propagation-sub-routes/93-06-SHADCN-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/93-per-page-propagation-sub-routes/93-06-SHADCN-AUDIT.md` exists
    - File contains 5 sections (consumer reach, per-primitive classification, dialog.tsx:34 future-proof recipe, admin out-of-scope evidence, sign-off)
    - File ends with "SHADCN AUDIT COMPLETE: <date>" line
    - `git status --porcelain next/src/components/ui/` returns EMPTY (zero shadcn source files modified)
    - `git status --porcelain next/src/app/admin/` returns EMPTY (zero admin source files modified)
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - Section 1 confirms ZERO public-route consumers of shadcn primitives
    - Section 3 contains the dialog.tsx:34 BEFORE/AFTER swap recipe
  </acceptance_criteria>
  <done>
    shadcn audit committed; admin-only impact confirmed; future-proof recipe for dialog.tsx:34 recorded.
  </done>
</task>

</tasks>

<acceptance_gates>
**Positive gates:**
- `93-06-SHADCN-AUDIT.md` exists with all 5 sections
- Audit Section 1 grep results embedded (ZERO public-route consumers)
- Audit Section 3 contains dialog.tsx:34 future-proof BEFORE/AFTER recipe

**Negative gates (read-only contract):**
- `git diff next/src/components/ui/` → EMPTY
- `git diff next/src/app/admin/` → EMPTY
- No new shadcn primitive sweep applied (Decision deferred to future phase)

**Build gates:**
- `pnpm --dir next build` exit 0
- `pnpm --dir next lint` exit 0
</acceptance_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn primitives ↔ public routes | Public routes do not consume shadcn primitives; admin-only consumer. v9.0 transparency contract has no observable effect on admin route (no blob behind). |
| Future modal trigger ↔ dialog.tsx:34 overlay | If/when a future phase introduces a public modal, the documented future-proof recipe applies. |
| Read-only audit ↔ source tree | Plan 06 contract: zero source-file modifications. Audit document is the only output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-06-01 | Tampering | Plan accidentally edits a shadcn primitive (e.g., applies the dialog.tsx:34 swap eagerly without a public consumer) | mitigate | Read-only contract; acceptance gate `git diff next/src/components/ui/` empty |
| T-93-06-02 | Information disclosure | Audit overlooks a public-route shadcn consumer (false negative on the consumer-reach grep) | mitigate | Section 1 embeds verbatim grep output; if non-empty for a non-admin file, plan stops + surfaces to user |
| T-93-06-03 | Repudiation | dialog.tsx:34 future-proof recipe lost if a future phase ships a public modal without consulting this audit | mitigate | Section 3 records the recipe in the phase directory; SUMMARY references it; Phase 94 verification phase consumes it as input |
</threat_model>

<deviation_rules>
- The planner does NOT modify any shadcn source file (`next/src/components/ui/*.tsx`).
- The planner does NOT modify the admin route (`next/src/app/admin/*`).
- The planner does NOT execute the dialog.tsx:34 future-proof swap (no public consumer exists today).
- The planner does NOT introduce new archetypes.
- If the audit reveals a public-route shadcn consumer (contradicting 93-RESEARCH), STOP and surface to user.
</deviation_rules>

<verification>
1. `pnpm --dir next build` exits 0.
2. `pnpm --dir next lint` exits 0.
3. `git diff next/src/components/ui/` is empty.
4. `git diff next/src/app/admin/` is empty.
5. 93-06-SHADCN-AUDIT.md exists with all 5 sections + sign-off line.
6. Audit confirms ZERO public-route consumers of shadcn primitives.
7. Audit Section 3 contains dialog.tsx:34 BEFORE/AFTER recipe.
</verification>

<success_criteria>
- ROUTE-06 satisfied: shadcn primitives verified as admin-only impact
- dialog.tsx:34 future-proof recipe recorded
- Zero shadcn source files modified
- Build + lint green
</success_criteria>

<output>
After completion, create `.planning/phases/93-per-page-propagation-sub-routes/93-06-SUMMARY.md`. Note: (a) confirmation of zero source modifications, (b) the consumer grep result (admin-only consumer), (c) future-proof recipe summary for Phase 94 consumption.
</output>
