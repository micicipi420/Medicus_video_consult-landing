---
phase: 93-per-page-propagation-sub-routes
plan: 02
type: execute
wave: 2
subsystem: route-checkup
depends_on:
  - 00
  - 01
files_modified:
  - next/src/components/sections/checkup/CheckupAdvantages.tsx
  - next/src/components/sections/checkup/CheckupB2B.tsx
  - next/src/components/sections/checkup/CheckupProblem.tsx
  - next/src/components/sections/checkup/CheckupProcess.tsx
  - next/src/components/sections/checkup/CheckupProgramsKorea.tsx
  - next/src/components/sections/checkup/CheckupProgramsTurkey.tsx
  - next/src/components/sections/checkup/CheckupWhyUs.tsx
autonomous: true
requirements:
  - ROUTE-01
must_haves:
  truths:
    - "All 7 /checkup section components consume --glass-card-fill / --glass-card-blur (cards) or --glass-section-fill / --glass-section-blur (frames/badges) per Archetype B/C"
    - "Decorative inner icon chips consume --glass-button-fill / --glass-button-blur (Tier 3)"
    - "CheckupB2B.tsx:85 CTA gradient UNTOUCHED (Archetype J — opaque-forever)"
    - "CheckupProgramsTurkey.tsx:132 CTA gradient UNTOUCHED (Archetype J)"
    - "Brand-color heading text gradients (bg-clip-text text-transparent) UNTOUCHED — text fill, not glass"
    - "Per-route Playwright visual diff for /checkup is GREEN against Wave 0 baseline (only blob-region mask delta acceptable; non-blob region pixel-stable)"
    - "pnpm build green; pnpm lint green"
  artifacts:
    - path: "next/src/components/sections/checkup/CheckupAdvantages.tsx"
      provides: "v9.0 tokenized 4-card advantage grid + inner icon chips"
    - path: "next/src/components/sections/checkup/CheckupB2B.tsx"
      provides: "v9.0 tokenized B2B section frames + opaque CTA preserved"
    - path: "next/src/components/sections/checkup/CheckupProblem.tsx"
      provides: "v9.0 tokenized 3-card problem grid"
    - path: "next/src/components/sections/checkup/CheckupProcess.tsx"
      provides: "v9.0 tokenized 4-card process grid"
    - path: "next/src/components/sections/checkup/CheckupProgramsKorea.tsx"
      provides: "v9.0 tokenized program cards (default + highlighted variants)"
    - path: "next/src/components/sections/checkup/CheckupProgramsTurkey.tsx"
      provides: "v9.0 tokenized B2B frame + program cards + opaque CTA preserved"
    - path: "next/src/components/sections/checkup/CheckupWhyUs.tsx"
      provides: "v9.0 tokenized 4-card why-us grid"
  key_links:
    - from: "next/src/app/checkup/page.tsx (composition shell — unmodified)"
      to: "7 swept section components in next/src/components/sections/checkup/"
      via: "ESM imports preserved verbatim"
      pattern: "from '@/components/sections/checkup/'"
    - from: "/checkup route render at runtime"
      to: "globals.css :root --glass-* tokens (FROZEN by Phase 90/92)"
      via: "Tailwind arbitrary-value classes resolved at paint time"
      pattern: "bg-\\[var\\(--glass-(card|section|button)-fill\\)\\]"
---

<objective>
Sweep the 7 `/checkup` route section components to v9.0 4-tier glass tokens. The composition shell `next/src/app/checkup/page.tsx` is NOT modified (no glass surfaces; verify-only). Wave 2 plans 02/03/04/05 are designed to run in parallel agents/worktrees with zero file-modification overlap; this plan owns ONLY the `next/src/components/sections/checkup/` directory.

**Three deliverables:**

1. Apply Archetypes B + C swaps verbatim from `93-PATTERNS.md` §"Wave 2A — `/checkup`" tables across 7 files. Each task in this plan covers 1-3 files grouped by similar pattern density.
2. Verify Archetype J CTA opaque-forever invariant on `CheckupB2B.tsx:85` and `CheckupProgramsTurkey.tsx:132` — these CTA gradients are NEVER swept.
3. Run per-route Playwright visual-diff against Wave 0 baseline. Non-blob region must be pixel-stable (only the swapped tokens cause subtle compositing changes; the explicit `.living-blob-field` mask removes blob-region noise). Wave 1 propagation diffs (from primitive sweep in Plan 01) PLUS this plan's per-section sweep should now produce an overall stable rendering for `/checkup`.

**Exact line/before/after pairs:** See `93-PATTERNS.md` §"Wave 2A — `/checkup`" tables. Cite Archetype letter + line numbers; do not invent strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md
@.planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md
@.planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-04-PLAN.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-05-PLAN.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-08-SWEEP-AUDIT.md
@DESIGN.md
@CLAUDE.md
@next/src/app/checkup/page.tsx
@next/src/components/sections/checkup/CheckupAdvantages.tsx
@next/src/components/sections/checkup/CheckupB2B.tsx
@next/src/components/sections/checkup/CheckupProblem.tsx
@next/src/components/sections/checkup/CheckupProcess.tsx
@next/src/components/sections/checkup/CheckupProgramsKorea.tsx
@next/src/components/sections/checkup/CheckupProgramsTurkey.tsx
@next/src/components/sections/checkup/CheckupWhyUs.tsx

<interfaces>
<!-- Tier-token resolution table: see 92-PATTERNS.md (FROZEN). -->
<!-- Per-line BEFORE/AFTER strings: see 93-PATTERNS.md "Wave 2A — /checkup" section. -->

<!-- Archetype-J CTA locations in this plan: -->
- CheckupB2B.tsx:85         → from-mu-blue to-mu-accent-blue (NEVER touch)
- CheckupProgramsTurkey.tsx:132 → from-mu-blue to-mu-accent-blue (NEVER touch)

<!-- Text-gradient locations (NEVER touch — bg-clip-text text-transparent): -->
- Headings throughout 7 files. Identification: bg-gradient-to-* + bg-clip-text + text-transparent.

<!-- Brand-color decorative inner chips (preserve brand-color fill, swap blur to button token): -->
- N/A in /checkup files — checkup decorative chips use bg-white/50 not brand-color tints (per 93-PATTERNS.md tables).
- (Brand-color chip handling applies in /consultations only — see Plan 03.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep CheckupAdvantages + CheckupProblem + CheckupWhyUs (3 card-grid files, Archetype C with optional hover)</name>
  <files>next/src/components/sections/checkup/CheckupAdvantages.tsx, next/src/components/sections/checkup/CheckupProblem.tsx, next/src/components/sections/checkup/CheckupWhyUs.tsx</files>
  <read_first>
    - All 3 files (whole file each — small)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2A — three line tables)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (Archetype C definition)
  </read_first>
  <action>
    Apply 93-PATTERNS.md tables verbatim:

    **CheckupAdvantages.tsx (lines 15, 28, 41, 54 — 4 cards; lines 16, 29, 42, 55 — 4 inner icon chips):**

    Cards (Archetype C):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass`

    The source has no `hover:bg-*` ramp — leave it that way (do NOT add `hover:bg-[var(--glass-form-fill)]` unless you find an existing hover token to upgrade). 93-PATTERNS notes "optional hover ramp ADD by planner" — discretion: SKIP the ADD; the cards stay static-Tier-1 as in source. This avoids introducing a behavioral change that might unexpectedly diff against Wave 0 baseline.

    Decorative inner chips (Tier 3 — line 16 etc.):
    - BEFORE: `w-14 h-14 bg-white/50 backdrop-blur-xl rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`
    - AFTER:  `w-14 h-14 bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] rounded-2xl flex items-center justify-center text-mu-blue border border-white/60 mb-5`

    **CheckupProblem.tsx (lines 15, 28, 41 — 3 cards; lines 16, 29, 42 — 3 inner chips):** Same swap as CheckupAdvantages.

    **CheckupWhyUs.tsx (line 43 — 4 cards mapped):**
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass text-center` (or similar — match what's in file)
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass text-center`

    Use Edit tool. Preserve all Russian copy + `&nbsp;` / `\u00A0`. Preserve all `text-mu-text-*`, `text-mu-blue`, brand-color text classes. Preserve all `border-white/N` (cosmetic). Preserve all `shadow-glass*` tokens.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - Per-file `bg-white/[0-9]` count = 0 for all 3 files
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/checkup/CheckupAdvantages.tsx` ≥ 4
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/checkup/CheckupProblem.tsx` ≥ 3
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/checkup/CheckupWhyUs.tsx` ≥ 1
    - `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/checkup/CheckupAdvantages.tsx` ≥ 4 (4 inner chips)
    - `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/checkup/CheckupProblem.tsx` ≥ 3 (3 inner chips)
    - No hardcoded blur literals: `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/checkup/CheckupAdvantages.tsx next/src/components/sections/checkup/CheckupProblem.tsx next/src/components/sections/checkup/CheckupWhyUs.tsx` returns 0
  </acceptance_criteria>
  <done>
    Three card-grid files swept; cards Tier 1, decorative inner chips Tier 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sweep CheckupProcess + CheckupB2B (process cards + B2B section frames; CTA at CheckupB2B:85 NEVER touched)</name>
  <files>next/src/components/sections/checkup/CheckupProcess.tsx, next/src/components/sections/checkup/CheckupB2B.tsx</files>
  <read_first>
    - Both files (whole file each)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2A — CheckupProcess + CheckupB2B tables)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-02-AUDIT.md (CTA invariant baseline)
  </read_first>
  <action>
    **CheckupProcess.tsx (line 48 — step cards in `.map()`):**

    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass text-center`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass text-center`

    **CheckupB2B.tsx (lines 48, 56, 67 — 3 section frames; line 85 CTA — Archetype J — UNTOUCHED):**

    Section frames (Archetype B):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass`
    - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass`

    Line 85 CTA gradient: leave verbatim. Confirm via grep that the CTA's className does NOT contain `backdrop-blur-*` or `backdrop-filter`.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/checkup/CheckupProcess.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/checkup/CheckupB2B.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/checkup/CheckupProcess.tsx` ≥ 1
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/checkup/CheckupB2B.tsx` ≥ 3
    - CTA preserved: `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/checkup/CheckupB2B.tsx` ≥ 1
    - CTA invariant: `grep -E 'from-mu-blue to-mu-accent-blue' next/src/components/sections/checkup/CheckupB2B.tsx | grep -cE 'backdrop-blur|backdrop-filter'` returns 0
    - No hardcoded blur literals: `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/checkup/CheckupProcess.tsx next/src/components/sections/checkup/CheckupB2B.tsx` returns 0
  </acceptance_criteria>
  <done>
    Process cards + B2B frames swept; CheckupB2B:85 CTA verifiably opaque.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sweep CheckupProgramsKorea + CheckupProgramsTurkey (variant-class strings; CTA at Turkey:132 NEVER touched)</name>
  <files>next/src/components/sections/checkup/CheckupProgramsKorea.tsx, next/src/components/sections/checkup/CheckupProgramsTurkey.tsx</files>
  <read_first>
    - Both files (whole file each)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2A — CheckupProgramsKorea + CheckupProgramsTurkey tables)
  </read_first>
  <action>
    These two files use variant-class strings (data-driven highlighted vs default cards). Both variants get the Archetype B/C swap.

    **CheckupProgramsKorea.tsx (lines 13, 14, 18):**

    Line 13 (highlighted variant card — Archetype C):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[3rem] p-8 border border-mu-blue/40 shadow-[...] flex flex-col${...}`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[3rem] p-8 border border-mu-blue/40 shadow-[...] flex flex-col${...}`

    Line 14 (default variant card — Archetype C):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[3rem] p-8 border border-white/60 shadow-glass flex flex-col`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[3rem] p-8 border border-white/60 shadow-glass flex flex-col`

    Line 18 (badge variant — Archetype B):
    - BEFORE: `inline-flex items-center gap-2 bg-white/50 backdrop-blur-md border border-glass-border px-4 py-1.5 rounded-full shadow-sm w-fit mb-4`
    - AFTER:  `inline-flex items-center gap-2 bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] border border-glass-border px-4 py-1.5 rounded-full shadow-sm w-fit mb-4`

    **CheckupProgramsTurkey.tsx (lines 82, 103, 104, 111, 132):**

    Line 82 (B2B card frame — Archetype B):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass mb-8`
    - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass mb-8`

    Line 103 (highlighted variant — Archetype C): same as Korea line 13 with `border-mu-blue/40`.
    Line 104 (default variant — Archetype C): same as Korea line 14.
    Line 111 (badge variant — Archetype B): same as Korea line 18.
    Line 132 (CTA gradient — Archetype J — **NEVER SWEEP**): leave verbatim.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/checkup/CheckupProgramsKorea.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/checkup/CheckupProgramsTurkey.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/checkup/CheckupProgramsKorea.tsx` ≥ 2 (highlighted + default)
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/checkup/CheckupProgramsKorea.tsx` ≥ 1 (badge)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/checkup/CheckupProgramsTurkey.tsx` ≥ 2
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/checkup/CheckupProgramsTurkey.tsx` ≥ 2 (frame + badge)
    - CTA preserved: `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/checkup/CheckupProgramsTurkey.tsx` ≥ 1
    - CTA invariant: `grep -E 'from-mu-blue to-mu-accent-blue' next/src/components/sections/checkup/CheckupProgramsTurkey.tsx | grep -cE 'backdrop-blur|backdrop-filter'` returns 0
    - No hardcoded blur literals: `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/checkup/CheckupProgramsKorea.tsx next/src/components/sections/checkup/CheckupProgramsTurkey.tsx` returns 0
  </acceptance_criteria>
  <done>
    Both variant-class files swept across all variants; Turkey CTA verifiably opaque.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: /checkup wave-end gates — full build + lint + Playwright visual diff for /checkup</name>
  <files>(no source modifications — verification only)</files>
  <read_first>
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Per-Task Verification Map — 93-02-01 row)
  </read_first>
  <action>
    Run the wave-end verification suite scoped to `/checkup`:

    1. `pnpm --dir next build` — full build green.
    2. `pnpm --dir next lint` — zero new warnings.
    3. `pnpm --dir next exec playwright test tests/visual --grep "checkup"` — visual diff vs Wave 0 baseline.
       Expectation: After Plan 01 (primitives) + this plan (checkup sections) both shipped, /checkup should produce a stable visual diff against Wave 0 baseline modulo:
       - The `.living-blob-field` mask (already excluded)
       - Sub-pixel anti-aliasing changes from token consumption (acceptable; `maxDiffPixelRatio: 0.01` from playwright.config absorbs)
       - Token alpha changes from `bg-white/60` (0.60) → `bg-[var(--glass-card-fill)]` (resolves to 0.10 desktop / 0.14 mobile per Phase 90 tokens) — this IS a visual change but it's the intended Phase 93 behavior; the snapshot should be updated AFTER Wave 3 close-out (Plan 07), not now.

       **If Playwright reports diffs:** Document the diff per breakpoint in SUMMARY. The diffs are EXPECTED. Do NOT regenerate baseline (Wave 0 baseline is locked until Plan 07).

    4. CTA invariant cross-cutting grep:
       ```bash
       grep -rn 'from-mu-blue to-mu-accent-blue' next/src/components/sections/checkup/ \
         | xargs -I {} sh -c 'grep -E "(backdrop-blur|backdrop-filter)" {} 2>/dev/null && echo "HIT: {}"' 2>/dev/null
       ```
       Expected: zero `HIT:` lines. If any → fail and return to fix.

    5. Russian-copy / nbsp regression check:
       ```bash
       grep -rc '\\u00A0' next/src/components/sections/checkup/
       ```
       Compare against pre-sweep baseline (capture before edits started). Counts must match exactly.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - CTA invariant cross-cutting grep returns 0 HIT lines
    - Russian-copy nbsp counts unchanged from pre-sweep baseline
    - `pnpm --dir next exec playwright test tests/visual --grep "checkup"` either passes within `maxDiffPixelRatio: 0.01` OR produces an EXPECTED diff documented in SUMMARY (Wave 0 baseline reflects pre-token state; final reconciliation in Plan 07)
    - All 7 /checkup files have ZERO `bg-white/N` residue
  </acceptance_criteria>
  <done>
    /checkup route swept across 7 files; CTA invariants intact; visual diff documented; Russian copy preserved.
  </done>
</task>

</tasks>

<acceptance_gates>
**Positive gates (per-file token consumption):**
- `grep -rcE 'bg-\[var\(--glass-(card|section|button)-fill\)\]' next/src/components/sections/checkup/` ≥ 26 (matches RESEARCH inventory: 26 sweep occurrences total)

**Negative gates (CTA invariant + residue):**
- `grep -rcE 'bg-white/[0-9]+' next/src/components/sections/checkup/` → 0
- `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/checkup/` → 0
- CTA invariant: `grep -rE 'from-mu-blue to-mu-accent-blue' next/src/components/sections/checkup/ | grep -cE 'backdrop-blur|backdrop-filter'` → 0
- No new `mix-blend-*` introduced (anti-pattern #8): `grep -rciE 'mix-blend-(multiply|screen|overlay)' next/src/components/sections/checkup/` → 0
- No animated backdrop-filter (anti-pattern #6): `grep -rE 'transition-\[' next/src/components/sections/checkup/ | grep -cE 'backdrop-filter|backdrop-blur'` → 0

**Build gates:**
- `pnpm --dir next build` exit 0
- `pnpm --dir next lint` exit 0
- `pnpm --dir next exec playwright test tests/visual --grep "checkup"` either passes OR produces only documented expected diffs (Wave 0 baseline absorbed in Plan 07)
</acceptance_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /checkup route bundle ↔ shared service primitives (Plan 01) | This route imports ServiceHero/SocialProof/FAQ/LeadFormSection from `service/`. Plan 01's primitive sweep affects this route's render. Plan 02 sweeps section components only. Together they fully cover /checkup. |
| Wave 2 parallel plans (02/03/04/05) ↔ each other | These four plans MUST run with zero shared file modifications. files_modified frontmatter for each plan is disjoint by directory: checkup/, consultations/, treatment/, contacts/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-01 | Tampering | Visual-regression in non-blob region (token swap accidentally produces a layout/color shift outside the mask) | mitigate | Wave 0 baseline + Task 4 per-route Playwright diff catches; expected diffs documented in SUMMARY for Plan 07 reconciliation |
| T-93-02-02 | Tampering | CTA invariant breakage (CheckupB2B:85 or CheckupProgramsTurkey:132 swept to glass tier — CTA disappears into blob) | mitigate | Tasks 2/3 explicit "NEVER SWEEP" callouts; Task 4 cross-cutting grep + acceptance gates |
| T-93-02-03 | Tampering | A11y branch coverage regression — accidental introduction of `bg-[#hexcode]/N` arbitrary color that bypasses Phase 92 BL-02 fix | mitigate | Negative gate `bg-white/N` → 0 + token-only sweep ensures every new class is a `--glass-*` consumer |
| T-93-02-04 | DoS | Parallel Wave 2 plan (03/04/05) accidentally modifies a checkup/* file (file ownership violation) | mitigate | files_modified frontmatter scope; orchestrator merges check overlapping diffs at wave boundary |
</threat_model>

<deviation_rules>
- The planner does NOT introduce new archetypes; cite Archetypes B + C from `92-PATTERNS.md`.
- The planner does NOT touch `next/src/app/checkup/page.tsx` (composition shell — no glass surfaces).
- The planner does NOT touch CheckupB2B.tsx:85 CTA gradient (Archetype J).
- The planner does NOT touch CheckupProgramsTurkey.tsx:132 CTA gradient (Archetype J).
- The planner does NOT touch heading text gradients (`bg-clip-text text-transparent` — text fill, not glass).
- The planner does NOT modify files outside `next/src/components/sections/checkup/`.
- The planner does NOT regenerate Wave 0 baseline snapshots.
- If the planner adds an `optional hover ramp` (per 93-PATTERNS.md note for Archetype C cards in CheckupAdvantages), that's a behavioral change — DEFAULT IS SKIP, do not add.
- If a target file already matches the archetype pattern (no `bg-white/N` or hardcoded `backdrop-blur-*` residue), task is verify-only no-op for that file.
</deviation_rules>

<verification>
1. `pnpm --dir next build` exits 0.
2. `pnpm --dir next lint` exits 0.
3. All 7 /checkup files: `bg-white/N` residue = 0; hardcoded blur literals = 0.
4. CTA invariant grep returns 0 hits.
5. Playwright visual-regression for /checkup: either passes OR documented expected delta in SUMMARY.
6. Russian copy nbsp counts preserved.
</verification>

<success_criteria>
- ROUTE-01 satisfied (partial — Plan 01 primitives + Plan 02 sections together fully cover /checkup)
- 7 /checkup section components swept
- CTA invariants intact at lines 85 (B2B) and 132 (Turkey)
- Build + lint green
- Per-route visual-diff status documented for Plan 07 reconciliation
</success_criteria>

<output>
After completion, create `.planning/phases/93-per-page-propagation-sub-routes/93-02-SUMMARY.md`. Note: (a) per-file grep counts (token consumption + residue), (b) Playwright visual diff status per breakpoint, (c) confirmation CheckupB2B:85 and CheckupProgramsTurkey:132 CTAs preserved, (d) Russian copy nbsp counts pre/post.
</output>
