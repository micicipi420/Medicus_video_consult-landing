---
phase: 93-per-page-propagation-sub-routes
plan: 03
type: execute
wave: 2
subsystem: route-consultations
depends_on:
  - 00
  - 01
files_modified:
  - next/src/components/sections/consultations/ConsultationAdvantages.tsx
  - next/src/components/sections/consultations/ConsultationBenefits.tsx
  - next/src/components/sections/consultations/ConsultationDoctors.tsx
  - next/src/components/sections/consultations/ConsultationPricing.tsx
  - next/src/components/sections/consultations/ConsultationProblem.tsx
  - next/src/components/sections/consultations/ConsultationProcess.tsx
  - next/src/components/sections/consultations/ConsultationScenarios.tsx
autonomous: true
requirements:
  - ROUTE-02
must_haves:
  truths:
    - "All 7 /consultations section components consume --glass-card-fill / --glass-card-blur (cards) or --glass-section-fill / --glass-section-blur (frames/badges) per Archetype B/C"
    - "Brand-color decorative inner chips (bg-mu-blue/10, bg-mu-accent-teal-bg, bg-mu-accent-orange-bg, bg-mu-green-50) PRESERVE brand-color fill; ONLY backdrop-blur swapped to --glass-button-blur (anti-pattern #5 NOT triggered)"
    - "ConsultationProcess explicit hover:bg-white/80 → hover:bg-[var(--glass-form-fill)] (Tier-2 ramp)"
    - "ConsultationDoctors outer panel + spec-pills container Archetype B; 7 doctor cards Archetype C; secondary CTA glass Archetype B with hover ramp"
    - "ConsultationPricing.tsx:43 CTA gradient UNTOUCHED (Archetype J — opaque-forever)"
    - "Heading text gradients (bg-clip-text text-transparent) UNTOUCHED — text fill"
    - "Per-route Playwright visual diff for /consultations is GREEN against Wave 0 baseline (only blob-region mask delta acceptable)"
    - "Anti-pattern #13 (≤2 glass per viewport) — ConsultationDoctors stacks Tier 0 outer + 7 Tier 1 doctor cards + Tier 0 spec-pills container; documented Phase 82 grandfathered exception (siblings, not nested)"
    - "pnpm build green; pnpm lint green"
  artifacts:
    - path: "next/src/components/sections/consultations/ConsultationAdvantages.tsx"
      provides: "v9.0 tokenized 5-card advantage grid with brand-color inner chips preserved"
    - path: "next/src/components/sections/consultations/ConsultationBenefits.tsx"
      provides: "v9.0 tokenized 4-card benefit grid"
    - path: "next/src/components/sections/consultations/ConsultationDoctors.tsx"
      provides: "v9.0 tokenized doctor section: outer Tier 0, 7 doctor cards Tier 1, spec-pills Tier 0, secondary CTA Tier 0"
    - path: "next/src/components/sections/consultations/ConsultationPricing.tsx"
      provides: "v9.0 tokenized pricing frame + badge + opaque CTA preserved"
    - path: "next/src/components/sections/consultations/ConsultationProblem.tsx"
      provides: "v9.0 tokenized single-panel problem section"
    - path: "next/src/components/sections/consultations/ConsultationProcess.tsx"
      provides: "v9.0 tokenized 3-card process grid with explicit Tier 1 → Tier 2 hover ramp"
    - path: "next/src/components/sections/consultations/ConsultationScenarios.tsx"
      provides: "v9.0 tokenized panel + checkmark chip"
  key_links:
    - from: "next/src/app/consultations/page.tsx (composition shell — unmodified)"
      to: "7 swept section components in next/src/components/sections/consultations/"
      via: "ESM imports preserved verbatim"
      pattern: "from '@/components/sections/consultations/'"
---

<objective>
Sweep the 7 `/consultations` route section components to v9.0 4-tier glass tokens. Composition shell `next/src/app/consultations/page.tsx` is NOT modified. Plan runs in parallel agent/worktree alongside Plans 02/04/05 with zero file overlap.

**Three deliverables:**

1. Apply Archetypes B + C swaps verbatim from `93-PATTERNS.md` §"Wave 2B — `/consultations`" tables across 7 files. Brand-color decorative inner chips (`bg-mu-blue/10`, `bg-mu-accent-teal-bg`, `bg-mu-accent-orange-bg`, `bg-mu-green-50`) PRESERVE the brand-color fill — only the blur is swapped to `--glass-button-blur` (per 93-PATTERNS.md § anti-pattern #5 note).
2. Verify Archetype J CTA opaque-forever invariant on `ConsultationPricing.tsx:43`.
3. Note ConsultationDoctors stacks 3 glass tiers visible simultaneously on desktop (outer panel + 7 doctor cards + spec-pills container). This is the documented Phase 82 grandfathered exception (siblings, not nested) — verify in SUMMARY but do NOT attempt to refactor.

**Exact line/before/after pairs:** See `93-PATTERNS.md` §"Wave 2B — `/consultations`" tables. Cite Archetype letter + line numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md
@.planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md
@.planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-04-PLAN.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-05-PLAN.md
@DESIGN.md
@CLAUDE.md
@next/src/app/consultations/page.tsx
@next/src/components/sections/consultations/ConsultationAdvantages.tsx
@next/src/components/sections/consultations/ConsultationBenefits.tsx
@next/src/components/sections/consultations/ConsultationDoctors.tsx
@next/src/components/sections/consultations/ConsultationPricing.tsx
@next/src/components/sections/consultations/ConsultationProblem.tsx
@next/src/components/sections/consultations/ConsultationProcess.tsx
@next/src/components/sections/consultations/ConsultationScenarios.tsx

<interfaces>
<!-- Per-line BEFORE/AFTER strings: see 93-PATTERNS.md "Wave 2B — /consultations" section. -->

<!-- Archetype-J CTA locations in this plan: -->
- ConsultationPricing.tsx:43 → from-mu-blue to-mu-accent-blue (NEVER touch)

<!-- Brand-color inner-chip rule (per 93-PATTERNS.md anti-pattern #5 note): -->
<!-- Preserve brand-color FILL (bg-mu-blue/10, bg-mu-accent-teal-bg, bg-mu-accent-orange-bg, bg-mu-green-50). -->
<!-- ONLY swap blur: backdrop-blur-xl → backdrop-blur-[var(--glass-button-blur)]. -->
<!-- These are brand-color chips inside cards, NOT green-on-card glass-tier violations. -->

<!-- ConsultationProcess explicit hover ramp (line 11/22/33): -->
- BEFORE hover token: hover:bg-white/80
- AFTER hover token: hover:bg-[var(--glass-form-fill)]

<!-- ConsultationDoctors anti-pattern #13 status: -->
- Outer panel (line 30) + 7 doctor cards (lines 45..126) + spec-pills container (line 141) = 3+ tiered surfaces visible on desktop scroll
- Documented Phase 82 grandfathered exception (siblings of each other, not nested) — preserve as-is
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep ConsultationAdvantages + ConsultationBenefits + ConsultationProblem + ConsultationScenarios (4 files; brand-color chip rule applies)</name>
  <files>next/src/components/sections/consultations/ConsultationAdvantages.tsx, next/src/components/sections/consultations/ConsultationBenefits.tsx, next/src/components/sections/consultations/ConsultationProblem.tsx, next/src/components/sections/consultations/ConsultationScenarios.tsx</files>
  <read_first>
    - All 4 files (whole file each)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2B — Advantages, Benefits, Problem, Scenarios tables)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (Archetypes B + C definitions)
  </read_first>
  <action>
    **ConsultationAdvantages.tsx (lines 14, 33, 52, 71, 90 — 5 cards with hover; lines 17, 36, 55, 74, 93 — 5 brand-color inner chips):**

    Cards (Archetype C with hover):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass group hover:border-white/80 hover:shadow-glass-lg transition-all duration-500`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass group hover:border-white/80 hover:shadow-glass-lg transition-all duration-500`

    Note: Source has no `hover:bg-*` ramp. Per 93-PATTERNS.md "optional hover ramp ADD by planner" — DEFAULT SKIP (do not add hover-bg behavioral change). Cards stay static-Tier-1.

    Line 90 has trailing `md:col-span-2 lg:col-span-1` — preserve.

    Brand-color inner chips (lines 17, 36, 55, 74, 93 — preserve brand-color fill, swap blur only):
    - BEFORE: `w-14 h-14 bg-mu-blue/10 backdrop-blur-xl rounded-2xl ...` (or `bg-mu-accent-teal-bg`, `bg-mu-accent-orange-bg`, `bg-mu-green-50`, `bg-mu-blue/10`)
    - AFTER:  same className but `backdrop-blur-xl` → `backdrop-blur-[var(--glass-button-blur)]`. Brand-color fill UNCHANGED.

    **ConsultationBenefits.tsx (lines 14, 27, 40, 53 — 4 cards with hover; lines 15, 28, 41, 54 — 4 brand-color chips):**

    Same pattern as ConsultationAdvantages: cards Archetype C (hover preserved minus `hover:bg-*` add); brand-color chips preserve fill, swap blur.

    **ConsultationProblem.tsx (line 7 — single panel — Archetype B):**

    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-3xl mx-auto space-y-6`
    - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-3xl mx-auto space-y-6`

    **ConsultationScenarios.tsx (line 17 — panel; line 21 — decorative checkmark chip):**

    Line 17 (panel — Archetype B):
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-3xl mx-auto`
    - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-3xl mx-auto`

    Line 21 (decorative checkmark chip — Tier 3):
    - BEFORE: `flex-shrink-0 w-7 h-7 bg-white/60 backdrop-blur-md border border-white/60 rounded-full flex items-center justify-center shadow-glass-inner-strong mt-0.5`
    - AFTER:  `flex-shrink-0 w-7 h-7 bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] border border-white/60 rounded-full flex items-center justify-center shadow-glass-inner-strong mt-0.5`

    Use Edit tool. Preserve all Russian copy + `&nbsp;` / `\u00A0`. Preserve all brand-color fill on inner chips. Preserve all `border-white/N`, `shadow-glass*` tokens.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/consultations/ConsultationAdvantages.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/consultations/ConsultationBenefits.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/consultations/ConsultationProblem.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/consultations/ConsultationScenarios.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/consultations/ConsultationAdvantages.tsx` ≥ 5
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/consultations/ConsultationBenefits.tsx` ≥ 4
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/consultations/ConsultationProblem.tsx` ≥ 1
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/consultations/ConsultationScenarios.tsx` ≥ 1
    - Brand-color chips preserved: `grep -cE '(bg-mu-blue/10|bg-mu-accent-teal-bg|bg-mu-accent-orange-bg|bg-mu-green-50)' next/src/components/sections/consultations/ConsultationAdvantages.tsx next/src/components/sections/consultations/ConsultationBenefits.tsx` returns ≥ 9 (5 + 4)
    - All chips swept blur: `grep -c 'backdrop-blur-\[var(--glass-button-blur)\]' next/src/components/sections/consultations/ConsultationAdvantages.tsx next/src/components/sections/consultations/ConsultationBenefits.tsx next/src/components/sections/consultations/ConsultationScenarios.tsx` returns ≥ 10 (5 + 4 + 1)
    - No hardcoded blur literals: `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/consultations/ConsultationAdvantages.tsx next/src/components/sections/consultations/ConsultationBenefits.tsx next/src/components/sections/consultations/ConsultationProblem.tsx next/src/components/sections/consultations/ConsultationScenarios.tsx` returns 0
  </acceptance_criteria>
  <done>
    Four files swept; brand-color chips preserve fill; hardcoded blur literals eliminated.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sweep ConsultationDoctors (outer panel Tier 0; 7 doctor cards Tier 1; spec-pills Tier 0; spec pills Tier 3; secondary CTA glass Tier 0 with hover ramp)</name>
  <files>next/src/components/sections/consultations/ConsultationDoctors.tsx</files>
  <read_first>
    - next/src/components/sections/consultations/ConsultationDoctors.tsx (whole file — large; line 30 outer, 45/58/73/86/98/113/126 doctor cards, 141 spec-pills, 145 pills, 156 CTA glass)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2B — ConsultationDoctors table)
  </read_first>
  <action>
    Six surfaces in this file:

    **Line 30 (outer panel — Archetype B):**
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-4xl mx-auto mb-12 space-y-4`
    - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[2.5rem] p-8 md:p-12 border border-white/60 shadow-glass max-w-4xl mx-auto mb-12 space-y-4`

    **Lines 45, 58, 73, 86, 98, 113, 126 (7 doctor cards with hover — Archetype C):**
    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-2xl shadow-glass-sm border border-white/60 p-5 text-center hover:shadow-glass hover:border-white/80 transition-all duration-300`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-2xl shadow-glass-sm border border-white/60 p-5 text-center hover:shadow-glass hover:border-white/80 transition-all duration-300`

    **Line 141 (spec-pills container — Archetype B):**
    - BEFORE: `bg-white/60 backdrop-blur-3xl rounded-[3rem] p-12 border border-white/60 shadow-glass-lg max-w-4xl mx-auto mb-8`
    - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[3rem] p-12 border border-white/60 shadow-glass-lg max-w-4xl mx-auto mb-8`

    **Line 145 (spec pills `.map()` — Tier 3 decorative inner; preserve `hover:bg-mu-green-50` brand-tint hover):**
    - BEFORE: `bg-white/50 backdrop-blur-md border border-white/60 px-6 py-3 rounded-full font-bold text-mu-text-900 shadow-glass-inner hover:bg-mu-green-50 hover:text-mu-green-700 transition-colors cursor-default`
    - AFTER:  `bg-[var(--glass-button-fill)] backdrop-blur-[var(--glass-button-blur)] border border-white/60 px-6 py-3 rounded-full font-bold text-mu-text-900 shadow-glass-inner hover:bg-mu-green-50 hover:text-mu-green-700 transition-colors cursor-default`

    The `hover:bg-mu-green-50` is a semantic brand-tint hover, NOT a glass-tier ramp — preserve verbatim.

    **Line 156 (secondary CTA glass — Archetype B; mirrors HeroHub.tsx:56 secondary CTA):**
    - BEFORE: `inline-flex items-center gap-2 bg-white/50 backdrop-blur-xl text-mu-text-900 px-8 py-4 rounded-3xl font-semibold shadow-glass hover:bg-white/60 transition-all border border-white/60 text-lg`
    - AFTER:  `inline-flex items-center gap-2 bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] text-mu-text-900 px-8 py-4 rounded-3xl font-semibold shadow-glass hover:bg-[var(--glass-card-fill)] transition-all border border-white/60 text-lg`

    Use Edit tool with precise pattern matching (the doctor card string repeats 7 times — global replace is fine here because the className is identical across all 7 instances).

    **Anti-pattern #13 status:** Document in SUMMARY that ConsultationDoctors stacks Tier 0 outer + 7 Tier 1 doctor cards + Tier 0 spec-pills container — three tiered surfaces visible simultaneously on desktop scroll. This is the Phase 82 grandfathered exception (siblings, not nested). Do NOT refactor.
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/consultations/ConsultationDoctors.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/consultations/ConsultationDoctors.tsx` ≥ 3 (outer panel + spec-pills container + secondary CTA)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/consultations/ConsultationDoctors.tsx` ≥ 7 (7 doctor cards)
    - `grep -c 'bg-\[var(--glass-button-fill)\]' next/src/components/sections/consultations/ConsultationDoctors.tsx` ≥ 1 (spec pills — could be more if .map renders multiple)
    - `grep -c 'hover:bg-\[var(--glass-card-fill)\]' next/src/components/sections/consultations/ConsultationDoctors.tsx` ≥ 1 (secondary CTA hover ramp)
    - Brand hover preserved: `grep -c 'hover:bg-mu-green-50' next/src/components/sections/consultations/ConsultationDoctors.tsx` ≥ 1 (spec-pill semantic hover preserved)
    - No hardcoded blur literals: `grep -cE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/consultations/ConsultationDoctors.tsx` returns 0
  </acceptance_criteria>
  <done>
    ConsultationDoctors: 6 distinct surface types swept; secondary CTA hover ramps to Tier 1; anti-pattern #13 status documented.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sweep ConsultationPricing + ConsultationProcess (CTA at Pricing:43 NEVER touched; Process has explicit Tier 1→Tier 2 hover ramp)</name>
  <files>next/src/components/sections/consultations/ConsultationPricing.tsx, next/src/components/sections/consultations/ConsultationProcess.tsx</files>
  <read_first>
    - Both files (whole file each)
    - .planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md (§Wave 2B — ConsultationPricing + ConsultationProcess tables)
  </read_first>
  <action>
    **ConsultationPricing.tsx (lines 21, 22, 43):**

    Line 21 (Tier 0 frame — Archetype B):
    - BEFORE: `bg-white/60 backdrop-blur-3xl rounded-[3rem] p-8 md:p-12 border border-white/60 shadow-glass-lg max-w-xl mx-auto`
    - AFTER:  `bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] rounded-[3rem] p-8 md:p-12 border border-white/60 shadow-glass-lg max-w-xl mx-auto`

    Line 22 (inner badge — Archetype B):
    - BEFORE: `inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-white/60 px-5 py-2.5 rounded-full shadow-glass-inner mb-6 text-sm font-bold text-mu-text-900`
    - AFTER:  `inline-flex items-center gap-2 bg-[var(--glass-section-fill)] backdrop-blur-[var(--glass-section-blur)] border border-white/60 px-5 py-2.5 rounded-full shadow-glass-inner mb-6 text-sm font-bold text-mu-text-900`

    Line 43 (CTA gradient — Archetype J — **NEVER SWEEP**): leave verbatim. Confirm via grep that the CTA's className does NOT contain `backdrop-blur-*` or `backdrop-filter`.

    **ConsultationProcess.tsx (lines 11, 22, 33 — 3 cards with explicit Tier 1 → Tier 2 hover ramp — Archetype C):**

    - BEFORE: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 border border-white/60 shadow-glass relative group hover:bg-white/80 hover:border-white/80 hover:shadow-glass-lg transition-all duration-500`
    - AFTER:  `bg-[var(--glass-card-fill)] backdrop-blur-[var(--glass-card-blur)] rounded-[2.5rem] p-8 border border-white/60 shadow-glass relative group hover:bg-[var(--glass-form-fill)] hover:border-white/80 hover:shadow-glass-lg transition-all duration-500`

    Note: ConsultationProcess is the only /consultations file with explicit `hover:bg-white/80`. The Tier 1 → Tier 2 hover ramp is the canonical Archetype C with hover (mirrors `LeadFormSection`-style ramp via `--glass-form-fill`).
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/consultations/ConsultationPricing.tsx` returns 0
    - `grep -c 'bg-white/[0-9]' next/src/components/sections/consultations/ConsultationProcess.tsx` returns 0
    - `grep -c 'bg-\[var(--glass-section-fill)\]' next/src/components/sections/consultations/ConsultationPricing.tsx` ≥ 2 (frame + badge)
    - `grep -c 'bg-\[var(--glass-card-fill)\]' next/src/components/sections/consultations/ConsultationProcess.tsx` ≥ 3 (3 cards)
    - `grep -c 'hover:bg-\[var(--glass-form-fill)\]' next/src/components/sections/consultations/ConsultationProcess.tsx` ≥ 3 (Tier-2 hover ramp on each card)
    - CTA preserved: `grep -c 'from-mu-blue to-mu-accent-blue' next/src/components/sections/consultations/ConsultationPricing.tsx` ≥ 1
    - CTA invariant: `grep -E 'from-mu-blue to-mu-accent-blue' next/src/components/sections/consultations/ConsultationPricing.tsx | grep -cE 'backdrop-blur|backdrop-filter'` returns 0
    - No hardcoded blur literals: `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/consultations/ConsultationPricing.tsx next/src/components/sections/consultations/ConsultationProcess.tsx` returns 0
  </acceptance_criteria>
  <done>
    Pricing frame + badge swept; CTA preserved opaque; Process explicit hover ramp Tier 1 → Tier 2 wired.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: /consultations wave-end gates — full build + lint + Playwright visual diff for /consultations</name>
  <files>(no source modifications — verification only)</files>
  <read_first>
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Per-Task Verification Map — 93-02-02 row)
  </read_first>
  <action>
    Run wave-end verification scoped to `/consultations`:

    1. `pnpm --dir next build`
    2. `pnpm --dir next lint`
    3. `pnpm --dir next exec playwright test tests/visual --grep "consultations"` — visual diff vs Wave 0 baseline. Same expectation as Plan 02 — diffs are EXPECTED (token consumption changes alpha values); document for Plan 07 reconciliation. Do NOT regenerate baseline.
    4. CTA invariant cross-cutting grep:
       ```bash
       grep -rn 'from-mu-blue to-mu-accent-blue' next/src/components/sections/consultations/ \
         | xargs -I {} sh -c 'grep -E "(backdrop-blur|backdrop-filter)" {} 2>/dev/null && echo "HIT: {}"' 2>/dev/null
       ```
       Expected: zero `HIT:` lines.

    5. Russian-copy / nbsp regression check:
       ```bash
       grep -rc '\\u00A0' next/src/components/sections/consultations/
       ```
       Compare against pre-sweep baseline; counts must match.

    6. Brand-color chip preservation grep:
       ```bash
       grep -rcE '(bg-mu-blue/10|bg-mu-accent-teal-bg|bg-mu-accent-orange-bg|bg-mu-green-50)' next/src/components/sections/consultations/
       ```
       Compare against pre-sweep baseline; brand-color fill counts must match (anti-pattern #5 NOT triggered — preserve all brand chips).

    7. ConsultationDoctors anti-pattern #13 documentation: confirm in SUMMARY that the 3-tier sibling stack is the documented Phase 82 grandfathered exception (NOT a regression).
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - CTA invariant cross-cutting grep returns 0 HIT lines
    - Russian-copy nbsp counts unchanged from pre-sweep baseline
    - Brand-color chip counts unchanged from pre-sweep baseline
    - All 7 /consultations files have ZERO `bg-white/N` residue
    - `pnpm --dir next exec playwright test tests/visual --grep "consultations"` either passes within `maxDiffPixelRatio: 0.01` OR produces an EXPECTED diff documented in SUMMARY
    - SUMMARY notes ConsultationDoctors anti-pattern #13 grandfathered status
  </acceptance_criteria>
  <done>
    /consultations route swept across 7 files; CTA invariant + brand chips intact; visual diff documented.
  </done>
</task>

</tasks>

<acceptance_gates>
**Positive gates (per-file token consumption):**
- `grep -rcE 'bg-\[var\(--glass-(card|section|button)-fill\)\]' next/src/components/sections/consultations/` ≥ 37 (matches RESEARCH inventory: 37 sweep occurrences total)

**Negative gates (CTA invariant + residue):**
- `grep -rcE 'bg-white/[0-9]+' next/src/components/sections/consultations/` → 0
- `grep -rcE 'backdrop-blur-(xl|2xl|3xl|md)' next/src/components/sections/consultations/` → 0
- CTA invariant: `grep -rE 'from-mu-blue to-mu-accent-blue' next/src/components/sections/consultations/ | grep -cE 'backdrop-blur|backdrop-filter'` → 0
- Brand-color chip preservation: `grep -rcE '(bg-mu-blue/10|bg-mu-accent-teal-bg|bg-mu-accent-orange-bg|bg-mu-green-50)' next/src/components/sections/consultations/` matches pre-sweep baseline (no brand chip accidentally swept to glass tier)
- No `mix-blend-*` introduced: `grep -rciE 'mix-blend-(multiply|screen|overlay)' next/src/components/sections/consultations/` → 0

**Build gates:**
- `pnpm --dir next build` exit 0
- `pnpm --dir next lint` exit 0
- `pnpm --dir next exec playwright test tests/visual --grep "consultations"` either passes OR produces only documented expected diffs
</acceptance_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /consultations route bundle ↔ shared service primitives (Plan 01) | This route imports ServiceHero/SocialProof/FAQ/LeadFormSection from `service/`. Plan 01 covers those; Plan 03 covers consultations/* sections. |
| Brand-color chip fill ↔ token-driven button blur | The chip's brand-color fill is preserved (semantic accent); only the blur is swapped. Anti-pattern #5 (statically-painted green tint) does NOT apply because chips are explicit brand-color accent inside cards, not card surfaces themselves. |
| Wave 2 parallel plans (02/03/04/05) ↔ each other | files_modified strictly disjoint by directory. Plan 03 owns ONLY `consultations/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-01 | Tampering | Visual-regression in non-blob region | mitigate | Wave 0 baseline + Task 4 per-route Playwright diff |
| T-93-03-02 | Tampering | CTA invariant breakage at ConsultationPricing:43 | mitigate | Task 3 explicit "NEVER SWEEP" callout; Task 4 cross-cutting grep |
| T-93-03-03 | Tampering | Brand-color inner-chip fills accidentally swept to glass tier (anti-pattern #5 violation introduced) | mitigate | Task 1 explicit "preserve brand-color fill" rule; acceptance gate greps brand-color counts unchanged |
| T-93-03-04 | Tampering | A11y branch coverage regression — arbitrary-value class bypasses Phase 92 BL-02 fix | mitigate | Token-only sweep ensures every new class is `--glass-*` consumer |
| T-93-03-05 | DoS | Anti-pattern #13 (3-tier sibling stack on ConsultationDoctors) ESCALATES to nested violation if any tier is mistakenly nested instead of sibling | mitigate | Task 2 documents the sibling structure; Phase 82 grandfathered exception holds |
</threat_model>

<deviation_rules>
- The planner does NOT introduce new archetypes; cite Archetypes B + C from `92-PATTERNS.md`.
- The planner does NOT touch `next/src/app/consultations/page.tsx` (composition shell — no glass surfaces).
- The planner does NOT touch ConsultationPricing.tsx:43 CTA gradient (Archetype J).
- The planner does NOT touch heading text gradients.
- The planner does NOT modify files outside `next/src/components/sections/consultations/`.
- The planner does NOT change brand-color fill on inner chips (preserve `bg-mu-blue/10`, `bg-mu-accent-teal-bg`, etc.).
- The planner does NOT regenerate Wave 0 baseline snapshots.
- The planner does NOT refactor ConsultationDoctors's 3-tier sibling stack — it's a Phase 82 grandfathered exception.
- The planner does NOT add `hover:bg-*` ramps to cards that don't already have them (default SKIP for `optional hover ramp` notes in 93-PATTERNS.md).
- If a target file already matches the archetype pattern, the corresponding task becomes a verify-only no-op for that file.
</deviation_rules>

<verification>
1. `pnpm --dir next build` exits 0.
2. `pnpm --dir next lint` exits 0.
3. All 7 /consultations files: `bg-white/N` residue = 0; hardcoded blur literals = 0.
4. CTA invariant grep returns 0 hits.
5. Brand-color chip counts unchanged from pre-sweep baseline.
6. Playwright visual-regression for /consultations: passes OR expected diffs documented.
7. ConsultationDoctors anti-pattern #13 status documented.
</verification>

<success_criteria>
- ROUTE-02 satisfied (partial — Plan 01 primitives + Plan 03 sections together cover /consultations)
- 7 /consultations section components swept
- CTA invariant intact at ConsultationPricing.tsx:43
- Brand-color chips preserved (anti-pattern #5 NOT triggered)
- Process Tier 1 → Tier 2 hover ramp wired
- ConsultationDoctors 3-tier sibling status documented
- Build + lint green
</success_criteria>

<output>
After completion, create `.planning/phases/93-per-page-propagation-sub-routes/93-03-SUMMARY.md`. Note: (a) per-file grep counts, (b) brand-color chip count preservation, (c) Playwright visual diff status, (d) confirmation ConsultationPricing:43 CTA preserved, (e) ConsultationDoctors anti-pattern #13 grandfathered acknowledgement.
</output>
