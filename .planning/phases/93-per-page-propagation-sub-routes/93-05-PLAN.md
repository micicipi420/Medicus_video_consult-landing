---
phase: 93-per-page-propagation-sub-routes
plan: 05
type: execute
wave: 2
subsystem: route-contacts
depends_on:
  - 00
  - 01
files_modified:
  - .planning/phases/93-per-page-propagation-sub-routes/93-05-CONTACTS-AUDIT.md
autonomous: true
requirements:
  - ROUTE-04
must_haves:
  truths:
    - "/contacts/page.tsx is the SOLE rendered file for the /contacts route"
    - "page.tsx hero <section> uses cosmetic page-frame gradient `bg-gradient-to-b from-[#F0F7FF] to-white` — NOT a glass surface; preserve verbatim (precedent: Phase 92 ContactSection Path A non-glass gradient backdrop)"
    - "LeadFormSection on /contacts inherits Plan 01 sweep automatically via import — no additional work in this plan"
    - "Dead-code files contacts/{ContactsHero,ContactMethodGrid,CoordinatorCard,TrustBadges}.tsx UNTOUCHED per Decision F (cleanup deferred to separate todo)"
    - "Per-route Playwright visual diff for /contacts is GREEN against Wave 0 baseline modulo expected primitive-sweep delta from Plan 01"
    - "pnpm build green; pnpm lint green"
  artifacts:
    - path: ".planning/phases/93-per-page-propagation-sub-routes/93-05-CONTACTS-AUDIT.md"
      provides: "Read-only audit confirming /contacts is verify-only no-op for Phase 93; documents dead-code skip per Decision F + page.tsx cosmetic gradient preservation rationale"
  key_links:
    - from: "next/src/app/contacts/page.tsx <LeadFormSection /> JSX"
      to: "next/src/components/sections/service/LeadFormSection.tsx (swept in Plan 01)"
      via: "ESM import"
      pattern: "import.*LeadFormSection.*from '@/components/sections/service/LeadFormSection'"
---

<objective>
Verify /contacts route compliance with v9.0 token contract WITHOUT modifying any source file. This plan is intentionally a verify-only audit:

- `next/src/app/contacts/page.tsx` is the SOLE rendered file. It contains:
  - A cosmetic vertical page-frame gradient `bg-gradient-to-b from-[#F0F7FF] to-white` on its hero `<section>` (line 23) — NOT a glass surface, no `backdrop-filter` involved. Preserved verbatim per Phase 92 ContactSection Path A precedent (non-glass gradient backdrop).
  - A `<LeadFormSection />` component instance — inherits Plan 01's sweep automatically via the import graph.
- `next/src/app/contacts/{ContactsHero,ContactMethodGrid,CoordinatorCard,TrustBadges}.tsx` are dead code (NOT imported by `page.tsx`) — UNTOUCHED per Decision F. A separate todo at `.planning/todos/pending/contacts-route-dead-code-cleanup.md` covers their deletion.

**Three deliverables:**

1. Read-only audit producing `93-05-CONTACTS-AUDIT.md` documenting the rendered tree of /contacts (just `page.tsx` + `LeadFormSection`), the cosmetic page-frame gradient, the dead-code skip per Decision F, and the inheritance link to Plan 01.
2. Run the per-route Playwright visual diff and document the result for Plan 07 reconciliation.
3. Confirm zero source-file modifications to `next/src/app/contacts/` AND zero file modifications to dead-code files.

**Why no source-file modifications:** Plan 01 already swept `LeadFormSection` (the only glass surface on /contacts). The page-frame gradient is non-glass cosmetic. Dead-code files are out-of-scope per Decision F. `93-05-CONTACTS-AUDIT.md` is the only artifact produced by this plan — a read-only documentation audit. No `next/src/` files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md
@.planning/phases/93-per-page-propagation-sub-routes/93-PATTERNS.md
@.planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md
@.planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-07-PLAN.md
@.planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md
@.planning/todos/pending/contacts-route-dead-code-cleanup.md
@DESIGN.md
@CLAUDE.md
@next/src/app/contacts/page.tsx
@next/src/app/contacts/ContactsHero.tsx
@next/src/app/contacts/ContactMethodGrid.tsx
@next/src/app/contacts/CoordinatorCard.tsx
@next/src/app/contacts/TrustBadges.tsx

<interfaces>
<!-- Decision F (locked in 93-CONTEXT.md): -->
<!--   Treat next/src/app/contacts/{ContactsHero,ContactMethodGrid,CoordinatorCard,TrustBadges}.tsx as dead code. -->
<!--   DO NOT sweep. Cleanup is a separate post-Phase-93 todo. -->

<!-- /contacts/page.tsx rendered tree (verified in 93-RESEARCH §Per-Route Inventory): -->
<!--   line 23: <section className="... bg-gradient-to-b from-[#F0F7FF] to-white"> ← NON-GLASS cosmetic page-frame -->
<!--   line 33-43: <ScrollReveal><LeadFormSection ... /></ScrollReveal> ← inherits Plan 01 sweep -->

<!-- Path A non-glass-gradient precedent (Phase 92 ContactSection.tsx:26): -->
<!--   Cosmetic gradient backdrops that are NOT glass-tier are preserved verbatim. -->
<!--   Identification: bg-gradient-* WITHOUT backdrop-filter / backdrop-blur in the same className. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit /contacts route rendered tree; produce 93-05-CONTACTS-AUDIT.md</name>
  <files>.planning/phases/93-per-page-propagation-sub-routes/93-05-CONTACTS-AUDIT.md</files>
  <read_first>
    - next/src/app/contacts/page.tsx (whole file — 47 lines)
    - next/src/app/contacts/ContactsHero.tsx
    - next/src/app/contacts/ContactMethodGrid.tsx
    - next/src/app/contacts/CoordinatorCard.tsx
    - next/src/app/contacts/TrustBadges.tsx
    - .planning/phases/93-per-page-propagation-sub-routes/93-CONTEXT.md (Decision F)
    - .planning/phases/93-per-page-propagation-sub-routes/93-RESEARCH.md (§Per-Route Inventory — /contacts table)
    - .planning/phases/92-glass-rework-chrome-index-sections/92-PATTERNS.md (No Analog Found / Special Cases — /contacts/page.tsx row)
  </read_first>
  <action>
    Produce `.planning/phases/93-per-page-propagation-sub-routes/93-05-CONTACTS-AUDIT.md` with these sections:

    **Section 1: Rendered tree of /contacts (live grep evidence)**

    Show the result of:
    ```bash
    grep -E "^import" next/src/app/contacts/page.tsx
    ```
    Expected: imports `LeadFormSection` from `@/components/sections/service/LeadFormSection` and `ScrollReveal` from `@/components/motion/ScrollReveal`. NO imports of ContactsHero/ContactMethodGrid/CoordinatorCard/TrustBadges.

    **Section 2: Glass-surface inventory in page.tsx (read-only grep)**

    Show the result of:
    ```bash
    grep -n 'backdrop-blur\|backdrop-filter\|bg-white/' next/src/app/contacts/page.tsx
    ```
    Expected: zero matches. The hero `<section>` uses `bg-gradient-to-b from-[#F0F7FF] to-white` which is cosmetic page-frame gradient, NOT glass-tier. No sweep needed.

    Document the page.tsx:23 string verbatim and tag it: "Non-glass cosmetic vertical gradient. Path A precedent (Phase 92 ContactSection.tsx:26). Preserve verbatim. Phase 93 verify-only no-op."

    **Section 3: Dead-code skip evidence (Decision F)**

    For each of the 4 files (ContactsHero, ContactMethodGrid, CoordinatorCard, TrustBadges):
    - Confirm via grep that `next/src/app/contacts/page.tsx` does NOT import it.
    - Confirm via repo-wide grep that no other route/file imports it (run `grep -rn "from '@/app/contacts/{name}'" next/src/`). Expected: zero matches.
    - Tag each as "Dead code per Decision F. Cleanup deferred to `.planning/todos/pending/contacts-route-dead-code-cleanup.md`. Phase 93 explicit skip."

    **Section 4: Plan 01 LeadFormSection inheritance link**

    Document that Plan 01 swept `next/src/components/sections/service/LeadFormSection.tsx` per Decision A (flatten outer Tier 0, inner Tier 2 KD-v9-002 α=0.50 form panel). The /contacts route consumes this component via the import on line 2 of page.tsx — no additional work needed for this route.

    **Section 5: Read-only contract sign-off**

    Append:
    ```
    AUDIT COMPLETE: <date>
    /contacts route Phase 93 disposition: VERIFY-ONLY NO-OP.
    - page.tsx cosmetic gradient: preserved (non-glass).
    - LeadFormSection: inherited from Plan 01.
    - Dead-code files: skipped per Decision F.
    Source files modified by this plan: ZERO.
    ```

    **DO NOT MODIFY** any source file. This plan is read-only audit. Write only `93-05-CONTACTS-AUDIT.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/93-per-page-propagation-sub-routes/93-05-CONTACTS-AUDIT.md && grep -c 'AUDIT COMPLETE' .planning/phases/93-per-page-propagation-sub-routes/93-05-CONTACTS-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/93-per-page-propagation-sub-routes/93-05-CONTACTS-AUDIT.md` exists
    - File contains 5 sections (rendered tree, glass-surface inventory, dead-code skip, Plan 01 inheritance, sign-off)
    - File ends with "AUDIT COMPLETE: <date>" line
    - `git status --porcelain next/src/app/contacts/` returns EMPTY (zero source files modified — read-only contract)
    - `pnpm --dir next build` exits 0 (no source touched)
    - Section 2 grep result confirms ZERO `backdrop-blur` / `backdrop-filter` / `bg-white/` matches in page.tsx
    - Section 3 confirms ZERO repo-wide imports of the 4 dead-code files
  </acceptance_criteria>
  <done>
    /contacts route audit committed; verify-only no-op rationale documented.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: /contacts wave-end gates — Playwright visual diff for /contacts</name>
  <files>(no source modifications — verification only)</files>
  <read_first>
    - .planning/phases/93-per-page-propagation-sub-routes/93-VALIDATION.md (Per-Task Verification Map — 93-02-04 row)
  </read_first>
  <action>
    Run wave-end verification scoped to `/contacts`:

    1. `pnpm --dir next build` — full build green.
    2. `pnpm --dir next lint` — zero new warnings.
    3. `pnpm --dir next exec playwright test tests/visual --grep "contacts"` — visual diff vs Wave 0 baseline. Expected diff sources:
       - `LeadFormSection` flattened outer (Decision A) — significant visual change in the form section. EXPECTED.
       - Inner Tier 2 form panel α=0.50 vs prior `bg-white/42` — minor tonal change. EXPECTED.
       - GlassCheckmark chips swept to `--glass-button-fill`. Minor.

       Document each diff per breakpoint in SUMMARY. The diffs are EXPECTED and absorbed in Plan 07. Do NOT regenerate baseline.

    4. CTA invariant cross-cutting grep across the entire /contacts impact surface:
       ```bash
       grep -rn 'from-mu-blue to-mu-accent-blue' next/src/app/contacts/ \
         | xargs -I {} sh -c 'grep -E "(backdrop-blur|backdrop-filter)" {} 2>/dev/null && echo "HIT: {}"' 2>/dev/null
       ```
       Expected: zero `HIT:` lines. (page.tsx has no CTA gradient; LeadFormSection ships ContactForm CTA which is Phase 92 frozen.)

    5. Russian-copy / nbsp regression check on page.tsx:
       ```bash
       grep -c '\\u00A0' next/src/app/contacts/page.tsx
       ```
       Expected: matches pre-sweep baseline (file is unmodified).

    6. Confirm dead-code files unmodified:
       ```bash
       git diff --stat next/src/app/contacts/ContactsHero.tsx next/src/app/contacts/ContactMethodGrid.tsx next/src/app/contacts/CoordinatorCard.tsx next/src/app/contacts/TrustBadges.tsx
       ```
       Expected: empty diff (zero modifications).
  </action>
  <verify>
    <automated>pnpm --dir next build && pnpm --dir next lint && git diff --quiet next/src/app/contacts/</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir next build` exits 0
    - `pnpm --dir next lint` exits 0
    - `git diff next/src/app/contacts/` is EMPTY (zero source files modified)
    - CTA invariant grep returns 0 HIT lines
    - Russian-copy nbsp count on page.tsx unchanged
    - Dead-code files unmodified per `git diff --stat`
    - `pnpm --dir next exec playwright test tests/visual --grep "contacts"` either passes within `maxDiffPixelRatio: 0.01` OR produces an EXPECTED diff documented in SUMMARY (LeadFormSection flatten + KD-v9-002 alpha bump)
  </acceptance_criteria>
  <done>
    /contacts route verified as verify-only no-op; visual diff documented for Plan 07.
  </done>
</task>

</tasks>

<acceptance_gates>
**Positive gates:**
- 93-05-CONTACTS-AUDIT.md exists with all 5 sections
- Audit Section 1 confirms imports limited to `LeadFormSection` + `ScrollReveal`

**Negative gates (read-only contract):**
- `git diff next/src/app/contacts/` → EMPTY
- `git status --porcelain next/src/app/contacts/` → EMPTY
- `git diff next/src/app/contacts/ContactsHero.tsx next/src/app/contacts/ContactMethodGrid.tsx next/src/app/contacts/CoordinatorCard.tsx next/src/app/contacts/TrustBadges.tsx` → EMPTY
- CTA invariant: `grep -rE 'from-mu-blue to-mu-accent-blue' next/src/app/contacts/ | grep -cE 'backdrop-blur|backdrop-filter'` → 0
- No glass surface introduced in page.tsx: `grep -cE 'backdrop-blur|backdrop-filter|bg-white/[0-9]' next/src/app/contacts/page.tsx` → 0

**Build gates:**
- `pnpm --dir next build` exit 0
- `pnpm --dir next lint` exit 0
- `pnpm --dir next exec playwright test tests/visual --grep "contacts"` either passes OR documented expected diffs (LeadFormSection delta from Plan 01)
</acceptance_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /contacts page.tsx ↔ LeadFormSection (Plan 01) | The route inherits Plan 01's flatten + KD-v9-002 sweep automatically via the import graph. No additional sweep work in this plan. |
| Dead-code files ↔ active route bundle | Dead-code files are NOT imported by page.tsx; bundler does not include them. Decision F defers cleanup to a separate todo. Plan 93-05 must NOT delete them (delete is a destructive operation requiring a dedicated cleanup phase). |
| Read-only audit ↔ source tree | Plan 05 contract: zero source-file modifications. Audit document is the only output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05-01 | Tampering | Plan accidentally edits page.tsx (e.g., "improves" the cosmetic gradient by adding backdrop-blur) | mitigate | Read-only contract; acceptance gate `git diff next/src/app/contacts/` empty |
| T-93-05-02 | Tampering | Plan accidentally deletes a dead-code file (Decision F deferral violated) | mitigate | Acceptance gate confirms dead-code files unmodified per `git diff --stat` |
| T-93-05-03 | Tampering | Visual regression on /contacts from primitive sweep (Plan 01 LeadFormSection flatten) | mitigate | Wave 0 baseline + Task 2 per-route Playwright diff documents the expected delta for Plan 07 reconciliation |
| T-93-05-04 | Information disclosure | Audit doc claims "no glass surfaces" without grep evidence | mitigate | Audit Section 2 embeds verbatim grep output |
</threat_model>

<deviation_rules>
- The planner does NOT modify any source file. Only `93-05-CONTACTS-AUDIT.md` is written.
- The planner does NOT touch the 4 dead-code files in `next/src/app/contacts/` (Decision F deferral).
- The planner does NOT modify `next/src/app/contacts/page.tsx` (the cosmetic gradient is non-glass; preserved per Phase 92 Path A precedent).
- The planner does NOT regenerate Wave 0 baseline snapshots.
- The planner does NOT delete dead-code files (separate cleanup todo).
- If the audit reveals that page.tsx DOES contain a glass-tier surface (e.g., a `backdrop-blur-*` class slipped in unnoticed), STOP and surface to user — this would be a research finding inconsistent with 93-RESEARCH and would require an updated plan.
</deviation_rules>

<verification>
1. `pnpm --dir next build` exits 0.
2. `pnpm --dir next lint` exits 0.
3. `git diff next/src/app/contacts/` is empty.
4. 93-05-CONTACTS-AUDIT.md exists with all 5 sections + sign-off line.
5. Playwright visual-regression for /contacts: passes OR expected diffs from Plan 01 primitive sweep documented.
</verification>

<success_criteria>
- ROUTE-04 satisfied: /contacts route audit confirms verify-only no-op disposition
- Decision F honored: dead-code files untouched
- Plan 01 inheritance link documented
- Build + lint green
- Zero source files modified
</success_criteria>

<output>
After completion, create `.planning/phases/93-per-page-propagation-sub-routes/93-05-SUMMARY.md`. Note: (a) confirmation of zero source modifications, (b) Playwright visual diff status (expected delta from LeadFormSection flatten), (c) Decision F dead-code skip evidence, (d) Plan 01 inheritance link.
</output>
