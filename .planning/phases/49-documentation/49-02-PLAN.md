---
phase: 49-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - styleguide.html
  - Makefile
  - scripts/build-pages.sh
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "styleguide.html renders all design system components with real CSS (not screenshots)"
    - "make build exits 0 with styleguide.html in the page list"
    - "build-pages.sh splices all 5 partials into styleguide.html (header, footer, sticky-bar, mobile-menu, svg-defs)"
    - "The 7th-page invariant is proven: a new page can be authored using only BUILD markers + body content"
  artifacts:
    - path: "styleguide.html"
      provides: "Live visual reference page for all v4.0 design system components"
      contains: "BUILD:vars"
    - path: "Makefile"
      provides: "Updated PAGES list including styleguide.html"
      contains: "styleguide.html"
    - path: "scripts/build-pages.sh"
      provides: "Updated DEFAULT_PAGES including styleguide.html"
      contains: "styleguide.html"
  key_links:
    - from: "styleguide.html"
      to: "scripts/build-pages.sh"
      via: "BUILD markers parsed by splicer"
      pattern: "BUILD:header"
    - from: "styleguide.html"
      to: "css/styles.css"
      via: "link rel stylesheet"
      pattern: "css/styles.css"
    - from: "Makefile"
      to: "styleguide.html"
      via: "PAGES variable"
      pattern: "styleguide.html"
---

<objective>
Create styleguide.html as a live visual reference page proving the 7th-page invariant. The page demonstrates all v4.0 design system components rendered with real CSS, and is built by the splicer pipeline like every other page.

Purpose: Proves that a new page can be authored using only documented components and the splicer pipeline (zero chrome duplication). Serves as a dev reference for all squircle, liquid glass, typography, button, and form components.

Output: styleguide.html (spliced by build-pages.sh), updated Makefile and build-pages.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-documentation/49-CONTEXT.md

@404.html (reference for BUILD marker structure and page skeleton)
@Makefile (PAGES variable on line ~51)
@scripts/build-pages.sh (DEFAULT_PAGES on line 11, CURRENT_PAGE cases on lines 146-170)

<interfaces>
<!-- BUILD marker structure required for every page (from 404.html): -->

Line structure:
1. <!DOCTYPE html> + <html lang="ru"> + <head> block
2. <body> + skip-to-content link
3. <!-- BUILD:vars CTA_HREF=... CTA_LABEL="..." CURRENT_PAGE=... -->
4. <!-- BUILD:svg-defs --> ... <!-- /BUILD:svg-defs -->
5. Mesh background div
6. <!-- BUILD:header --> ... <!-- /BUILD:header -->
7. <!-- BUILD:mobile-menu --> ... <!-- /BUILD:mobile-menu -->
8. [PAGE BODY CONTENT]
9. <!-- BUILD:footer --> ... <!-- /BUILD:footer -->
10. <!-- BUILD:sticky-bar --> ... <!-- /BUILD:sticky-bar -->
11. <script src="js/main.js"></script>
12. </body></html>

Splicer expects:
- Exactly 1 BUILD:vars block per page
- All 5 partial marker pairs (svg-defs, header, footer, sticky-bar, mobile-menu)
- CURRENT_PAGE must match a case in build-pages.sh (index|online|treatment|checkup|contacts|404)
- Marker lines must be at column 0 (no leading whitespace)

Makefile PAGES (line ~51):
  PAGES := index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html

build-pages.sh DEFAULT_PAGES (line 11):
  DEFAULT_PAGES="index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html"

build-pages.sh CURRENT_PAGE case (lines 146-170) -- must handle "styleguide" value.
The styleguide has no nav presence (like 404), so the case should be added alongside 404:
  index|404|styleguide)

CSS classes available for demo sections:
  Squircle: .squircle-md, .squircle-lg, .squircle-xl, .squircle-full
  Glass: .liquid-regular, .liquid-card, .liquid-card-wrap, .liquid-btn-primary, .liquid-btn-secondary, .stats-glass, .shimmer-sweep, .scroll-fade-top, .scroll-fade-bottom
  Grid: max-w-content via --container-content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create styleguide.html with BUILD markers and component demos</name>
  <files>styleguide.html</files>
  <read_first>
    - 404.html (full file -- use as structural template for page skeleton, BUILD markers, head block, mesh background, script includes)
    - src/styles/squircles.css (class names for demo sections)
    - src/styles/liquid-glass.css (class names for demo sections)
    - contacts.html lines 130-305 (form elements pattern for form demo section)
  </read_first>
  <action>
Create styleguide.html at project root. Use 404.html as the structural skeleton -- same head block, same BUILD marker order, same mesh background, same script includes.

**BUILD:vars line:**
```
<!-- BUILD:vars CTA_HREF=#contact CTA_LABEL="Оставить заявку" CURRENT_PAGE=styleguide -->
```

CURRENT_PAGE=styleguide means no nav item is highlighted (same behavior as 404).

**Head block:**
- Title: "Styleguide -- MedicusUnion Design System"
- meta robots: noindex, nofollow (dev reference, not production)
- Same CSS/font/favicon links as 404.html
- Same inline style block (header scroll state, mobile menu, form validation, etc.)

**Page body content** (between mobile-menu close and footer open markers).
Use `<main id="page-content">` wrapper.

Create sections with consistent structure. Each section: H2 title + description + live component demos.
Use the same grid/spacing patterns as the production pages: `mx-auto max-w-[1200px] px-4 md:px-8`.

**Required sections (in order):**

### Section 1: Typography Scale
- H1 example: `<h1 class="font-heading text-4xl md:text-5xl font-extrabold tracking-tight leading-tight">Heading 1</h1>`
- H2 example with same pattern
- H3 example
- Body text paragraph (`<p>` with default styling)
- Small text / caption
- Show font-family names in a note

### Section 2: Color Palette
- Grid of color swatches showing brand colors:
  - mu-blue (#38C6F4)
  - mu-cta-from (#0E8FB5) to mu-cta-to (#3B6DD0) gradient
  - mu-green-600 (#35B678)
  - mu-text-900, mu-text-700, mu-text-500, mu-text-300
  - mu-accent-blue, mu-accent-red, mu-accent-orange, mu-accent-teal
- Each swatch: a `<div>` with the background color, text label with name and hex value
- Use `squircle-md` on color swatches for visual consistency

### Section 3: Squircle Masks
- 4 demo elements side by side, each with a different squircle class
- Each: colored background (mu-blue/20), visible shape, label underneath
- Sizes: squircle-md (16px), squircle-lg (24px), squircle-xl (40px), squircle-full (circle)
- Note about three-tier progressive enhancement

### Section 4: Glass Cards (Light)
- Demo card using the shadow-wrap pattern:
  ```html
  <div class="liquid-card-wrap">
    <article class="squircle-lg liquid-card">
      <h3>Card Title</h3>
      <p>Card body text example.</p>
    </article>
  </div>
  ```
- Stats glass demo:
  ```html
  <div class="squircle-xl stats-glass">
    <div class="flex gap-8">
      <div><span class="text-3xl font-bold">43</span><br>clinics</div>
      <div><span class="text-3xl font-bold">11</span><br>countries</div>
    </div>
  </div>
  ```
- Base liquid-regular surface demo

### Section 5: Button Variants
- Primary CTA: `<button class="liquid-btn-primary squircle-md px-8 py-3">Primary CTA</button>`
- Primary with shimmer: `<button class="liquid-btn-primary squircle-md shimmer-sweep px-8 py-3">Shimmer CTA</button>`
- Secondary glass: `<button class="liquid-btn-secondary squircle-md px-8 py-3">Secondary</button>`
- Show both with and without shadow-wrap pattern

### Section 6: Form Elements
- Text input with squircle and glass:
  ```html
  <input type="text" class="squircle-lg w-full px-5 py-4 bg-white/60 backdrop-blur-sm border-0 shadow-form-inset" placeholder="Placeholder text">
  ```
- Select dropdown with same styling
- Textarea with same styling
- Note: form elements use inset shadows (safe inside mask, no wrap needed)

### Section 7: Badges / Chips
- Badge examples with squircle-md:
  ```html
  <span class="squircle-md bg-mu-green-50 text-mu-green-text px-3 py-1 text-sm font-medium">Badge</span>
  ```
- Show color variants: green, blue, orange, teal

### Section 8: Shadow-Wrap Pattern Demo
- Side-by-side comparison:
  - LEFT (correct): shadow-wrap pattern with visible outer shadow
  - RIGHT (anti-pattern): what happens when shadow + mask are on same element
- Code snippet showing correct HTML

### Section 9: Scroll-Edge Fade
- Demo of .scroll-fade-top and .scroll-fade-bottom on a content block

**HTML structure rules:**
- All BUILD marker lines (<!-- BUILD:header --> etc.) must start at column 0 (no leading whitespace)
- Page body uses `<main>` with proper spacing after header (`pt-32` to clear fixed header)
- Sections use `<section>` with `py-16` spacing and `border-b border-mu-text-200/30` separator
- Each section has an `id` for anchor linking (e.g., id="typography", id="squircles")
- All text content in Russian (section titles, descriptions, labels)
- Use &amp;nbsp; for subject+verb bindings in Russian text per typography rules
  </action>
  <verify>
    <automated>test -f styleguide.html && grep -q "BUILD:vars" styleguide.html && grep -q "BUILD:header" styleguide.html && grep -q "BUILD:footer" styleguide.html && grep -q "BUILD:sticky-bar" styleguide.html && grep -q "BUILD:mobile-menu" styleguide.html && grep -q "BUILD:svg-defs" styleguide.html && grep -q "squircle-md" styleguide.html && grep -q "liquid-card" styleguide.html && grep -q "liquid-btn-primary" styleguide.html && grep -q "CURRENT_PAGE=styleguide" styleguide.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - styleguide.html exists at project root
    - Contains exactly 1 BUILD:vars block with CURRENT_PAGE=styleguide
    - Contains all 5 partial marker pairs: BUILD:svg-defs, BUILD:header, BUILD:footer, BUILD:sticky-bar, BUILD:mobile-menu
    - Contains demo sections for: typography, colors, squircles (4 variants), glass cards, buttons (primary + secondary + shimmer), form elements (input + select + textarea), badges, shadow-wrap pattern
    - All marker lines start at column 0 (grep "^<!-- BUILD:" returns 6 matches)
    - Contains meta robots noindex nofollow
    - Contains link to css/styles.css
    - Contains script src js/main.js
  </acceptance_criteria>
  <done>styleguide.html exists with all 5 BUILD marker pairs, CURRENT_PAGE=styleguide, and 9 component demo sections using real CSS classes from the design system.</done>
</task>

<task type="auto">
  <name>Task 2: Wire styleguide.html into build pipeline and verify 7th-page invariant</name>
  <files>Makefile, scripts/build-pages.sh</files>
  <read_first>
    - Makefile (full file -- locate PAGES variable, understand build target)
    - scripts/build-pages.sh (lines 1-15 for DEFAULT_PAGES, lines 146-170 for CURRENT_PAGE case)
  </read_first>
  <action>
**Step 1: Update Makefile PAGES variable.**

Find the line (approximately line 51):
```
PAGES := index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html
```

Append `styleguide.html` to the end:
```
PAGES := index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html styleguide.html
```

**Step 2: Update scripts/build-pages.sh DEFAULT_PAGES.**

Find line 11:
```
DEFAULT_PAGES="index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html"
```

Append `styleguide.html`:
```
DEFAULT_PAGES="index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html styleguide.html"
```

**Step 3: Add "styleguide" to the CURRENT_PAGE case statement.**

Find the case block (around lines 146-170). The `index|404)` case handles pages with no active nav link. Add styleguide alongside 404:

Change:
```
    index|404)
      # no nav link is current: index uses logo aria-current, 404 has no nav presence
      ;;
```

To:
```
    index|404|styleguide)
      # no nav link is current: index uses logo aria-current, 404/styleguide have no nav presence
      ;;
```

**Step 4: Run `make build` and verify exit 0.**

Run `make build`. This proves:
1. Tailwind CSS compiles (styleguide.html is scanned for classes via @source)
2. build-pages.sh processes styleguide.html (splices all 5 partials)
3. The 7th-page invariant holds: a new page works with only BUILD markers + body content

**Step 5: Verify the spliced content.**

After make build, verify that styleguide.html now contains the actual partial content (not just empty marker blocks). Check:
- grep for "header__logo" in styleguide.html (proves header partial was spliced)
- grep for "footer" content in styleguide.html (proves footer was spliced)

**Step 6: Verify byte-identity gate.**

Run `make check` to confirm no chrome drift on existing pages. The styleguide.html is new so it won't trigger the drift gate, but the existing 6 pages must remain identical.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build 2>&1 && grep -q "styleguide.html" Makefile && grep -q "styleguide.html" scripts/build-pages.sh && grep -c "^<!-- BUILD:" styleguide.html | grep -q "6" && grep -q "header__logo" styleguide.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Makefile PAGES variable includes styleguide.html (grep "styleguide.html" Makefile)
    - scripts/build-pages.sh DEFAULT_PAGES includes styleguide.html (grep "styleguide.html" scripts/build-pages.sh)
    - scripts/build-pages.sh CURRENT_PAGE case handles "styleguide" (grep "styleguide)" scripts/build-pages.sh)
    - make build exits 0
    - styleguide.html contains spliced header content (grep "header__logo" styleguide.html)
    - styleguide.html contains spliced footer content (grep "footer" styleguide.html)
    - Existing 6 pages have no chrome drift (make check passes or git diff --quiet on existing HTML files)
  </acceptance_criteria>
  <done>styleguide.html is wired into the build pipeline. make build exits 0, all 5 partials are spliced into the page, and the 7th-page invariant is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-pages.sh eval | BUILD:vars line in styleguide.html is parsed by eval in splicer; must contain only safe KEY=VALUE pairs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | T (Tampering) | styleguide.html BUILD:vars | mitigate | BUILD:vars uses only CTA_HREF=#contact CTA_LABEL="text" CURRENT_PAGE=styleguide -- no shell metacharacters; splicer's existing metacharacter guard rejects $, backtick, ;, &, <, >, backslash |
| T-49-03 | I (Information Disclosure) | styleguide.html noindex | mitigate | meta robots noindex,nofollow prevents search engine indexing of dev reference page |
</threat_model>

<verification>
1. `make build` exits 0 -- proves Tailwind + splicer handle the new page
2. `grep -c "^<!-- BUILD:" styleguide.html` returns 6 -- all 5 partials + vars marker
3. `grep "header__logo" styleguide.html` -- proves header partial was spliced (not empty markers)
4. `grep "styleguide.html" Makefile` -- proves page is in build pipeline
5. `grep "styleguide" scripts/build-pages.sh` -- proves CURRENT_PAGE case exists
6. Existing pages: `git diff --quiet -- index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html` -- no chrome drift
</verification>

<success_criteria>
styleguide.html exists as a live visual reference page, is successfully built by `make build`, all 5 partials are spliced in, and the 7th-page invariant is proven (a new page authored with only BUILD markers + body content works in the pipeline). Existing pages have no chrome drift.
</success_criteria>

<output>
After completion, create `.planning/phases/49-documentation/49-02-SUMMARY.md`
</output>
