---
phase: 01-apply-redesign-from-redesign-folder-to-main-project
plan: 07
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
  - 01-05
  - 01-06
files_modified:
  - index.html
  - online-consultations.html
  - treatment-abroad.html
  - checkups.html
  - contacts.html
autonomous: false
requirements:
  - NAV-01
  - CLEANUP-01

must_haves:
  truths:
    - "Every internal link across all 5 pages resolves to a file that exists"
    - "Old files (consultations.html, checkup.html, test-design-system.html) are removed"
    - "Visual spot-check confirms glassmorphism header, mesh background, hero layout, and glass cards render correctly"
  artifacts:
    - path: "index.html"
      provides: "Complete home page with all correct links"
      contains: "online-consultations.html"
    - path: "online-consultations.html"
      provides: "Online consultations service page"
      contains: "Онлайн-консультации"
    - path: "treatment-abroad.html"
      provides: "Treatment abroad service page"
      contains: "Лечение за рубежом"
    - path: "checkups.html"
      provides: "Checkups service page"
      contains: "Чек-ап"
    - path: "contacts.html"
      provides: "Contacts page"
      contains: "Контакты"
  key_links:
    - from: "index.html"
      to: "online-consultations.html"
      via: "service card CTA"
      pattern: "href=\"online-consultations\\.html\""
    - from: "index.html"
      to: "checkups.html"
      via: "service card CTA"
      pattern: "href=\"checkups\\.html\""
    - from: "all pages"
      to: "contacts.html"
      via: "header CTA"
      pattern: "href=\"contacts\\.html\""
---

<objective>
Final cleanup: verify and fix all internal links across all 5 pages, remove old HTML files (consultations.html, checkup.html, test-design-system.html), and do a visual verification checkpoint.

Purpose: The old file names (consultations.html, checkup.html) conflict with the new names (online-consultations.html, checkups.html). Dead files must be removed. All links must be audited to prevent 404s. A human visual check confirms the glassmorphism design renders as expected.

Output: Clean file tree with only the 5 correct HTML pages and verified links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-apply-redesign-from-redesign-folder-to-main-project/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix all internal links + remove old files</name>
  <files>index.html, online-consultations.html, treatment-abroad.html, checkups.html, contacts.html</files>
  <read_first>
    - index.html (check all href and src attributes)
    - online-consultations.html (check all href and src attributes)
    - treatment-abroad.html (check all href and src attributes)
    - checkups.html (check all href and src attributes)
    - contacts.html (check all href and src attributes)
  </read_first>
  <action>
**Step 1: Audit all internal links across all 5 HTML files.**

Run grep for all `href=` attributes that point to local files (not external URLs, not anchors, not tel:, not mailto:). The valid targets are:
- `index.html`
- `online-consultations.html`
- `treatment-abroad.html`
- `checkups.html`
- `contacts.html`
- `css/styles.css`
- `js/main.js`
- `js/animations.js`
- `#section-id` (anchor links)
- External URLs (Unsplash, CDN)

**Invalid references to fix if found:**
- `consultations.html` -> change to `online-consultations.html`
- `checkup.html` -> change to `checkups.html`
- Any reference to `test-design-system.html` -> remove
- Any reference to old font files (`assets/fonts/`) -> remove
- Any reference to old CSS class names -> fix

For each file, verify:
1. Header nav links: Услуги (#services on index, or specific page links), Почему мы (#why-us on index), Контакты (contacts.html)
2. Header CTA: contacts.html
3. Mobile menu links: same as header nav
4. Footer service links: online-consultations.html, treatment-abroad.html, checkups.html
5. Footer nav links: index.html, contacts.html
6. Service card CTAs on index: online-consultations.html, treatment-abroad.html, checkups.html
7. Guide card links on index: online-consultations.html, treatment-abroad.html, checkups.html
8. All CTA buttons on service pages: contacts.html

Fix any discrepancies found.

**Step 2: Remove old files.**

Delete these files:
- `consultations.html` (replaced by online-consultations.html)
- `checkup.html` (replaced by checkups.html, note singular -> plural)
- `test-design-system.html` (design system test page no longer relevant)

Use `git rm` to stage the deletion:
```bash
git rm consultations.html checkup.html test-design-system.html
```

If any file doesn't exist, skip it (it may already be deleted).

**Step 3: Verify no broken references remain.**

Run a grep across all .html files for the old filenames:
```bash
grep -rn "consultations\.html\|checkup\.html\|test-design-system\.html" *.html
```

`consultations.html` appearing in `online-consultations.html` is a FALSE POSITIVE (substring match). The grep should check for `href="consultations.html"` specifically, not just the substring. So use:
```bash
grep -rn 'href="consultations\.html"' *.html
grep -rn 'href="checkup\.html"' *.html
```

Both should return 0 results.

**Step 4: Verify file tree.**

Run `ls *.html` to confirm only these 5 files exist:
- index.html
- online-consultations.html
- treatment-abroad.html
- checkups.html
- contacts.html
  </action>
  <verify>
    <automated>ls index.html online-consultations.html treatment-abroad.html checkups.html contacts.html && ! test -f consultations.html && ! test -f checkup.html && ! test -f test-design-system.html && ! grep -q 'href="consultations\.html"' *.html && ! grep -q 'href="checkup\.html"' *.html</automated>
  </verify>
  <acceptance_criteria>
    - All 5 HTML files exist: index.html, online-consultations.html, treatment-abroad.html, checkups.html, contacts.html
    - consultations.html does NOT exist (deleted)
    - checkup.html does NOT exist (deleted)
    - test-design-system.html does NOT exist (deleted)
    - No file contains `href="consultations.html"` (old link)
    - No file contains `href="checkup.html"` (old link)
    - Every footer in all 5 pages has links to online-consultations.html, treatment-abroad.html, checkups.html
    - Every header CTA in all 5 pages links to contacts.html
  </acceptance_criteria>
  <done>All internal links are correct across all 5 pages. Old files removed. No broken references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete redesign</name>
  <what-built>Complete redesign migration: 5 HTML pages with glassmorphism design, new color palette, SF Pro system fonts, Lucide SVG icons, motion-powered animations, glass surfaces, mesh background, coordinator contact section, FAQ accordion, pricing card, 3 service pages, contacts page with form. All linked correctly.</what-built>
  <how-to-verify>
    1. Open index.html in Chrome
    2. Verify: mesh background with 3 slowly-moving colored blobs visible behind content
    3. Verify: glassmorphism header floats at top with gradient "MedicusUnion" logo, nav links, phone, CTA button
    4. Scroll down -- header should gain slightly more opaque background
    5. Verify: Hero has 2-column layout -- left has badge pill, large heading with gradient text, subtitle, 2 CTA buttons, trust indicators; right has overlapping doctor photos with floating badges
    6. Verify: Stats section shows 4 glass cards with colored numbers (43, 11, 500+, 15+) -- numbers should count up on scroll
    7. Verify: Services section has 3 image cards with rounded inner images, icon overlays, feature lists, glass CTA buttons
    8. Verify: Guide section has 3 cards with images, floating icons at boundary, dramatic hover effect
    9. Verify: WhyUs section has advantage cards on left, image collage on right (desktop only)
    10. Verify: Contact section has coordinator "Айгерим" card with photo on left, glass form on right
    11. Verify: FAQ has 6 expandable items -- click to toggle
    12. Verify: Pricing card shows "от 450 EUR" with included items
    13. Verify: CTA section has glass wrapper with glow animation, doctor image on right (desktop)
    14. Verify: Footer has 4-column grid in glass wrapper
    15. Click "Онлайн-консультации" in footer -> opens online-consultations.html
    16. Click back, then "Лечение за рубежом" -> opens treatment-abroad.html
    17. Click back, then "Чек-ап" -> opens checkups.html -- verify 3 pricing program cards
    18. Click "Оставить заявку" header CTA -> opens contacts.html with coordinator card and form
    19. On mobile (Chrome DevTools 375px): verify single-column layouts, hamburger menu works, sticky bottom bar visible
    20. Test form: fill in name, phone (verify mask works), select interest, submit -- verify it attempts POST (may fail without Directus backend, but form should not silently do nothing)
  </how-to-verify>
  <resume-signal>Type "approved" if the design looks correct and all pages work, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `ls *.html` shows exactly 5 files
- All links resolve (no 404s when clicking nav)
- Visual design matches the glassmorphism aesthetic from the Redesign prototype
- Mobile layout works at 375px
- FAQ accordion toggles
- Form fields render and accept input
</verification>

<success_criteria>
The complete redesign migration is verified: 5 clean HTML pages, no dead files, all internal links working, glassmorphism visual language applied consistently, and the user has visually approved the result.
</success_criteria>

<output>
After completion, create `.planning/phases/01-apply-redesign-from-redesign-folder-to-main-project/01-07-SUMMARY.md`
</output>
