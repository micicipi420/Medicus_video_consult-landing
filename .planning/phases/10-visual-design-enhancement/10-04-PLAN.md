---
phase: 10-visual-design-enhancement
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - index.html
  - css/styles.css
autonomous: true
requirements:
  - D-16
  - D-18
  - D-20
  - D-21
must_haves:
  truths:
    - "Wave SVG dividers appear between alternating sections"
    - "Process steps are connected by dashed lines on desktop"
    - "Form section has a soft radial gradient halo behind the wrapper"
    - "Final CTA section has a gradient dark background instead of flat"
  artifacts:
    - path: "index.html"
      provides: "Wave SVG divider elements between sections"
      contains: "section-divider"
    - path: "css/styles.css"
      provides: "Wave divider styling, process connector, form halo, CTA gradient"
      contains: "section-divider"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "section-divider class and process connector pseudo-elements"
      pattern: "section-divider"
---

<objective>
Add decorative visual elements: wave section dividers, process step connector lines, form section radial gradient halo, and final CTA gradient background.

Purpose: These decorative touches elevate the page from "functional template" to "polished professional site." Wave dividers soften section transitions. The process connector guides the eye through steps. The form halo draws attention to the conversion point.

Output: Polished visual enrichment across the page with wave dividers, connectors, gradients.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-design-enhancement/10-CONTEXT.md
@index.html
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave dividers, process connector, form halo, and CTA gradient CSS</name>
  <files>css/styles.css</files>
  <read_first>css/styles.css, .planning/phases/10-visual-design-enhancement/10-CONTEXT.md</read_first>
  <action>
In css/styles.css, add to the "10. ANIMATIONS" section (or create a new "11. DECORATIVE ELEMENTS" section after it):

```css
/* ==========================================================================
   11. DECORATIVE ELEMENTS (Phase 10)
   ========================================================================== */

/* -- Wave section dividers (per D-16) -- */

.section-divider {
  display: block;
  width: 100%;
  height: 60px;
  position: relative;
  overflow: hidden;
  line-height: 0;
}

.section-divider--light-to-white {
  background-color: var(--color-light);
}
.section-divider--light-to-white svg {
  fill: var(--color-white);
}

.section-divider--white-to-light {
  background-color: var(--color-white);
}
.section-divider--white-to-light svg {
  fill: var(--color-light);
}

.section-divider svg {
  display: block;
  width: 100%;
  height: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
}

/* -- Process step connector (per D-18) -- */

@media (min-width: 768px) {
  .process__steps {
    position: relative;
  }

  .process__step {
    position: relative;
  }

  .process__step:not(:last-child)::after {
    content: '';
    position: absolute;
    top: 28px;
    right: -14%;
    width: 28%;
    height: 0;
    border-top: 2px dashed var(--color-primary);
    opacity: 0.4;
    pointer-events: none;
  }
}

/* -- Form section radial gradient halo (per D-20) -- */

.lead-form-section {
  position: relative;
  overflow: hidden;
}

.lead-form-section::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 25%;
  width: 600px;
  height: 600px;
  transform: translate(-50%, -50%);
  background: radial-gradient(circle, rgba(56, 198, 244, 0.08) 0%, transparent 70%);
  pointer-events: none;
  z-index: 0;
}

.lead-form__wrapper {
  position: relative;
  z-index: 1;
}

/* -- Final CTA gradient background (per D-21) -- */

.final-cta {
  background: linear-gradient(135deg, #18212C 0%, #1e2d3d 50%, #18212C 100%);
}
```

Additionally, modify the existing `.lead-form-section` rule (around line 714). The existing rule sets `background-color: var(--color-light)`. Keep that. The new `.lead-form-section` block above adds `position: relative` and `overflow: hidden` — merge these into the existing rule rather than creating a duplicate.

Similarly, the `.final-cta` gradient replaces the background coming from `.section--dark`. Since `.final-cta` already has `.section--dark` class, the specific `.final-cta` selector with `background` will override the `.section--dark` `background-color`. This works with CSS specificity — no changes needed to `.section--dark`.
  </action>
  <verify>
    <automated>grep -q "section-divider" css/styles.css && grep -q "process__step:not(:last-child)::after" css/styles.css && grep -q "lead-form-section::before" css/styles.css && grep -q "radial-gradient" css/styles.css && grep -q "final-cta" css/styles.css && grep -q "#1e2d3d" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - css/styles.css contains `.section-divider` with `height: 60px`
    - css/styles.css contains `.section-divider--light-to-white` with `background-color: var(--color-light)`
    - css/styles.css contains `.section-divider--white-to-light` with `background-color: var(--color-white)`
    - css/styles.css contains `.process__step:not(:last-child)::after` inside `@media (min-width: 768px)` with `border-top: 2px dashed var(--color-primary)` and `opacity: 0.4`
    - css/styles.css contains `.lead-form-section::before` with `radial-gradient(circle, rgba(56, 198, 244, 0.08)`
    - css/styles.css contains `.lead-form-section` with `position: relative` and `overflow: hidden`
    - css/styles.css contains `.final-cta` with `background: linear-gradient(135deg, #18212C 0%, #1e2d3d`
  </acceptance_criteria>
  <done>CSS for all decorative elements in place: wave dividers, process connectors, form halo, CTA gradient</done>
</task>

<task type="auto">
  <name>Task 2: Add wave SVG dividers between sections in HTML</name>
  <files>index.html</files>
  <read_first>index.html, css/styles.css</read_first>
  <action>
In index.html, add wave SVG divider elements between the alternating-background sections. The wave SVG is a smooth sine curve that creates a soft visual transition between sections (per D-16).

The section background pattern is:
- Hero: light (#F8FAFB)
- Problem: white
- Benefits: light
- Process: white
- Doctors: light
- Advantages: white
- Scenarios: light
- Pricing: white
- Form: light
- FAQ: light (same as form, so NO divider between form and FAQ)
- Final CTA: dark (no wave — different treatment)

Add wave dividers at these transitions (6 total):

1. **After hero section, before problem** (light → white):
   Insert between `</section>` of hero and `<!-- Problem Section -->`:
   ```html
   <div class="section-divider section-divider--light-to-white" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 60" preserveAspectRatio="none">
       <path d="M0,0 C360,60 1080,0 1440,40 L1440,60 L0,60 Z"/>
     </svg>
   </div>
   ```

2. **After problem section, before benefits** (white → light):
   ```html
   <div class="section-divider section-divider--white-to-light" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 60" preserveAspectRatio="none">
       <path d="M0,0 C360,60 1080,0 1440,40 L1440,60 L0,60 Z"/>
     </svg>
   </div>
   ```

3. **After benefits section, before process** (light → white):
   Same as pattern #1 (section-divider--light-to-white).

4. **After process section, before doctors** (white → light):
   Same as pattern #2 (section-divider--white-to-light).

5. **After doctors section, before advantages** (light → white):
   Same as pattern #1.

6. **After advantages section, before scenarios** (white → light):
   Same as pattern #2.

7. **After scenarios section, before pricing** (light → white):
   Same as pattern #1.

8. **After pricing section, before form** (white → light):
   Same as pattern #2.

That is 8 wave dividers total. Do NOT add a divider between form and FAQ (both light), or before/after the dark final-cta section.

Each divider uses the same SVG wave path but different modifier classes for background/fill colors. All dividers have `aria-hidden="true"` since they are purely decorative.
  </action>
  <verify>
    <automated>grep -c "section-divider" index.html | xargs -I{} test {} -ge 8 && grep -c "section-divider--light-to-white" index.html | xargs -I{} test {} -ge 4 && grep -c "section-divider--white-to-light" index.html | xargs -I{} test {} -ge 4 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly 8 `.section-divider` elements
    - 4 have class `section-divider--light-to-white` (after hero, benefits, doctors, scenarios)
    - 4 have class `section-divider--white-to-light` (after problem, process, advantages, pricing)
    - Each divider contains an SVG with `viewBox="0 0 1440 60"` and `preserveAspectRatio="none"`
    - Each divider has `aria-hidden="true"`
    - No divider between form section and FAQ section (both light background)
    - No divider between FAQ and final-cta (transition to dark)
  </acceptance_criteria>
  <done>Wave SVG dividers smoothly transition between alternating section backgrounds, process steps connected by dashed lines on desktop, form has halo gradient, final CTA has gradient background</done>
</task>

</tasks>

<verification>
1. Open page in browser — subtle wave shapes appear between adjacent sections with different backgrounds
2. Waves use appropriate fill colors matching the next section's background
3. On desktop (768px+), process steps show dashed blue connector lines between steps
4. On mobile, process connector lines are hidden
5. Form section has a soft blue radial glow behind the form area
6. Final CTA has a subtle gradient (not flat dark) — visible as slight color variation
</verification>

<success_criteria>
- 8 wave dividers between alternating sections (4 light-to-white, 4 white-to-light)
- No divider between same-background sections (form/FAQ)
- Process steps connected by dashed lines on desktop, hidden on mobile
- Form section has visible (but subtle) radial blue gradient halo
- Final CTA background is gradient, not flat dark
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-design-enhancement/10-04-SUMMARY.md`
</output>
