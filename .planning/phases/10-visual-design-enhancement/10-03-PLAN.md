---
phase: 10-visual-design-enhancement
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02
files_modified:
  - js/main.js
  - css/styles.css
autonomous: true
requirements:
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
  - D-19
must_haves:
  truths:
    - "Section content fades in from below when scrolling into viewport"
    - "Grid cards stagger their entrance animation with 100ms delay per child"
    - "All animations are disabled when prefers-reduced-motion is set"
    - "Buttons lift slightly on hover with shadow increase"
    - "FAQ answer toggle is a smooth height transition, not abrupt show/hide"
    - "Pricing CTA button has a pulsing glow that stops after 3 cycles"
  artifacts:
    - path: "js/main.js"
      provides: "initScrollAnimations function using IntersectionObserver"
      contains: "initScrollAnimations"
    - path: "css/styles.css"
      provides: "Animation keyframes, fade-in-up class, button hover lift, FAQ transition, pricing glow"
      contains: "fade-in-up"
  key_links:
    - from: "js/main.js"
      to: "css/styles.css"
      via: "JS adds .is-visible class, CSS animates it"
      pattern: "is-visible"
    - from: "js/main.js"
      to: "index.html"
      via: "IntersectionObserver targets .section elements"
      pattern: "querySelectorAll.*section"
---

<objective>
Add scroll-triggered entrance animations, smooth FAQ accordion, button hover lift, and pricing CTA glow animation.

Purpose: Subtle entrance animations guide attention and create perceived quality. The FAQ transition improves UX. The pricing glow draws the eye to the primary conversion action.

Output: Smooth scroll animations throughout the page, polished micro-interactions on buttons and FAQ, attention-drawing pricing CTA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-design-enhancement/10-CONTEXT.md
@index.html
@js/main.js
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animation CSS — fade-in-up, button hover lift, FAQ smooth toggle, pricing glow</name>
  <files>css/styles.css</files>
  <read_first>css/styles.css, .planning/phases/10-visual-design-enhancement/10-CONTEXT.md</read_first>
  <action>
In css/styles.css, add a new section "10. ANIMATIONS" after the "9. MEDIA QUERIES" section (at the end of the file). Add all animation styles in this one block:

```css
/* ==========================================================================
   10. ANIMATIONS (Phase 10)
   ========================================================================== */

/* -- Scroll entrance: fade-in-up -- */

.animate-on-scroll {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity 600ms ease-out, transform 600ms ease-out;
}

.animate-on-scroll.is-visible {
  opacity: 1;
  transform: translateY(0);
}

/* Stagger children in grids (per D-11): 100ms delay per child */
.stagger-children > .animate-on-scroll:nth-child(1) { transition-delay: 0ms; }
.stagger-children > .animate-on-scroll:nth-child(2) { transition-delay: 100ms; }
.stagger-children > .animate-on-scroll:nth-child(3) { transition-delay: 200ms; }
.stagger-children > .animate-on-scroll:nth-child(4) { transition-delay: 300ms; }
.stagger-children > .animate-on-scroll:nth-child(5) { transition-delay: 400ms; }
.stagger-children > .animate-on-scroll:nth-child(6) { transition-delay: 500ms; }
.stagger-children > .animate-on-scroll:nth-child(7) { transition-delay: 600ms; }

/* -- Button hover lift (per D-14) -- */

.button:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

.button:active {
  transform: translateY(0) scale(0.98);
}

/* -- FAQ smooth height transition (per D-15) -- */

.faq__answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height 300ms ease-out, padding 300ms ease-out;
  padding-bottom: 0;
}

.faq__answer.is-open {
  max-height: 500px;
  padding-bottom: 0;
}

/* -- Pricing CTA pulse glow (per D-19) -- */

@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(56, 198, 244, 0.4);
  }
  50% {
    box-shadow: 0 0 20px 8px rgba(56, 198, 244, 0.25);
  }
}

.pricing__card .button--primary {
  animation: pulse-glow 2s ease-in-out 3;
}

/* -- Reduced motion override (per D-13) -- */

@media (prefers-reduced-motion: reduce) {
  .animate-on-scroll {
    opacity: 1;
    transform: none;
    transition: none;
  }

  .faq__answer {
    transition: none;
  }

  .pricing__card .button--primary {
    animation: none;
  }

  .button:hover {
    transform: none;
  }

  .card:hover {
    transform: none;
  }
}
```

Also modify the existing FAQ answer rule. Find `.faq__answer[hidden]` (around line 960) and update the approach:
- Remove the `display: none` from `.faq__answer[hidden]` — we will use JS to manage visibility via class instead of hidden attribute
- Keep the `.no-js .faq__answer[hidden] { display: block; }` fallback

NOTE: The existing `.faq__answer` rule (around line 955) already has transition. REPLACE that existing transition/overflow with the new animation system above. Remove the duplicate to avoid conflicts. The new `.faq__answer` in the animations section replaces the old one — so remove `transition` and `overflow` from the original `.faq__answer` rule at line 955-958.

Additionally, update the existing `.button:active` rule (around line 267) to remove `transform: scale(0.98)` since the new animation section handles both hover and active states. Replace the existing block with just the active override.
  </action>
  <verify>
    <automated>grep -q "animate-on-scroll" css/styles.css && grep -q "is-visible" css/styles.css && grep -q "pulse-glow" css/styles.css && grep -q "translateY(-2px)" css/styles.css && grep -q "prefers-reduced-motion" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - css/styles.css contains `.animate-on-scroll` with `opacity: 0` and `transform: translateY(24px)`
    - css/styles.css contains `.animate-on-scroll.is-visible` with `opacity: 1` and `transform: translateY(0)`
    - css/styles.css contains `.stagger-children > .animate-on-scroll:nth-child(N)` rules for children 1-7 with 100ms increments
    - css/styles.css contains `.button:hover` with `transform: translateY(-2px)` and `box-shadow: var(--shadow-md)`
    - css/styles.css contains `.faq__answer` with `max-height: 0` and `overflow: hidden`
    - css/styles.css contains `.faq__answer.is-open` with `max-height: 500px`
    - css/styles.css contains `@keyframes pulse-glow` with `box-shadow` using `rgba(56, 198, 244`
    - css/styles.css contains `.pricing__card .button--primary` with `animation: pulse-glow 2s ease-in-out 3`
    - css/styles.css contains `@media (prefers-reduced-motion: reduce)` block that sets `.animate-on-scroll` to `opacity: 1; transform: none; transition: none`
  </acceptance_criteria>
  <done>All animation CSS is in place: fade-in-up, stagger, button hover, FAQ transition, pricing glow, reduced-motion override</done>
</task>

<task type="auto">
  <name>Task 2: Scroll animation JS + FAQ smooth toggle refactor</name>
  <files>js/main.js, index.html</files>
  <read_first>js/main.js, index.html, css/styles.css</read_first>
  <action>
In js/main.js, add two new functions inside the IIFE and wire them into `initAll()`:

**1. initScrollAnimations function (per D-10, D-11, D-12):**

Add this function after the `initStickyBar` function:

```javascript
/**
 * Scroll Animations
 * - Adds fade-in-up animation when elements enter viewport
 * - Uses IntersectionObserver for performance
 * - Stagger delay on grid children (100ms per child)
 * - Per D-10, D-11, D-12
 */
function initScrollAnimations() {
  // Bail if no IntersectionObserver support or reduced motion preferred
  if (!('IntersectionObserver' in window)) return;
  if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

  // Target all major content sections for animation
  var sections = document.querySelectorAll('.benefits, .process, .doctors, .advantages, .scenarios, .pricing, .lead-form-section, .faq, .final-cta');

  sections.forEach(function (section) {
    // Find grid containers for stagger effect
    var grids = section.querySelectorAll('.benefits__grid, .process__steps, .doctors__grid, .advantages__grid, .scenarios__list, .pricing__includes, .faq__list');
    grids.forEach(function (grid) {
      grid.classList.add('stagger-children');
      // Add animate-on-scroll to each direct child
      var children = grid.children;
      for (var i = 0; i < children.length; i++) {
        children[i].classList.add('animate-on-scroll');
      }
    });

    // Also animate section headings and descriptions
    var headings = section.querySelectorAll('h2, .doctors__description, .pricing__description, .pricing__card, .doctors__specializations, .doctors__note, .doctors__action, .final-cta__heading, .final-cta__text, .final-cta__actions, .lead-form__wrapper');
    headings.forEach(function (el) {
      if (!el.classList.contains('animate-on-scroll')) {
        el.classList.add('animate-on-scroll');
      }
    });
  });

  // Observe all animated elements
  var animatedElements = document.querySelectorAll('.animate-on-scroll');
  if (!animatedElements.length) return;

  var observer = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.2,
    rootMargin: '0px 0px -40px 0px'
  });

  animatedElements.forEach(function (el) {
    observer.observe(el);
  });
}
```

**2. Refactor initAccordion for smooth height transition (per D-15):**

Replace the current FAQ toggle logic. Instead of using the `hidden` attribute, use the `.is-open` CSS class with max-height transition.

Replace the existing `initAccordion` function entirely with:

```javascript
function initAccordion() {
  var buttons = document.querySelectorAll('.faq__question');
  if (!buttons.length) return;

  // Remove hidden attribute and set initial state via CSS class
  buttons.forEach(function (button) {
    var answer = button.nextElementSibling;
    if (answer && answer.hasAttribute('hidden')) {
      answer.removeAttribute('hidden');
      // Start closed — CSS max-height: 0 handles this
    }
  });

  buttons.forEach(function (button) {
    button.addEventListener('click', function () {
      var isOpen = this.getAttribute('aria-expanded') === 'true';
      var answer = this.nextElementSibling;

      // Close all other items
      buttons.forEach(function (otherButton) {
        if (otherButton !== button) {
          otherButton.setAttribute('aria-expanded', 'false');
          var otherAnswer = otherButton.nextElementSibling;
          if (otherAnswer) {
            otherAnswer.classList.remove('is-open');
          }
        }
      });

      // Toggle current item
      if (isOpen) {
        this.setAttribute('aria-expanded', 'false');
        answer.classList.remove('is-open');
      } else {
        this.setAttribute('aria-expanded', 'true');
        answer.classList.add('is-open');
      }
    });
  });
}
```

**3. Wire into initAll:**

Add `initScrollAnimations();` to the `initAll` function, after `initStickyBar();`:

```javascript
function initAll() {
  initAccordion();
  initSmoothScroll();
  initStickyBar();
  initScrollAnimations();
  initPhoneMask();
  initSpamProtection();
  initFormValidation();
}
```

**4. Add `stagger-children` class to grid containers and `animate-on-scroll` to children in index.html:**

NO — the JS does this dynamically. No HTML changes needed for scroll animations. The JS adds classes at runtime before the observer starts.

**5. Add a pricing CTA button for the glow animation:**

In index.html, find the pricing card (`.pricing__card`, around line 241-268). There is no CTA button inside the pricing card currently. Add one AFTER the `</ul>` closing tag of `.pricing__includes`, inside `.pricing__card`:

```html
<a href="#form" class="button button--primary pricing__cta">Получить консультацию</a>
```

Add CSS for `.pricing__cta` in the pricing section of styles.css:
```css
.pricing__cta {
  margin-top: var(--space-4);
  display: inline-flex;
}
```
  </action>
  <verify>
    <automated>grep -q "initScrollAnimations" js/main.js && grep -q "is-open" js/main.js && grep -q "stagger-children" js/main.js && grep -q "observer.unobserve" js/main.js && grep -q "prefers-reduced-motion" js/main.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - js/main.js contains `function initScrollAnimations()` with IntersectionObserver
    - js/main.js initScrollAnimations checks `prefers-reduced-motion: reduce` and bails early if set
    - js/main.js initScrollAnimations uses `threshold: 0.2`
    - js/main.js initScrollAnimations calls `observer.unobserve(entry.target)` (animations play once per D-12)
    - js/main.js initScrollAnimations adds `stagger-children` class to grid containers
    - js/main.js initScrollAnimations adds `animate-on-scroll` class to grid children
    - js/main.js `initAccordion` uses `classList.add('is-open')` / `classList.remove('is-open')` instead of `hidden` attribute
    - js/main.js `initAccordion` calls `answer.removeAttribute('hidden')` on init
    - js/main.js `initAll` calls `initScrollAnimations()`
    - index.html pricing card contains a `.button--primary.pricing__cta` link to `#form`
  </acceptance_criteria>
  <done>Scroll animations trigger on viewport entry with stagger, FAQ smoothly animates open/close, pricing CTA pulses with glow</done>
</task>

</tasks>

<verification>
1. Scroll through page slowly — each section's content fades in from below as it enters the viewport
2. Grid cards (benefits, doctors, advantages) stagger their entrance with visible delay between items
3. Open DevTools > Rendering > Enable "prefers-reduced-motion: reduce" — all animations are disabled, content shows immediately
4. Click FAQ question — answer smoothly expands (not instant toggle). Click again — smoothly collapses
5. Hover over any button — lifts 2px with shadow increase, no color flash
6. Pricing CTA button pulses blue glow 3 times, then stops
7. All animations play only once (scroll up and back down — no re-trigger)
</verification>

<success_criteria>
- Sections fade-in-up when 20% visible in viewport
- Grid children stagger at 100ms intervals
- prefers-reduced-motion disables all animations
- FAQ accordion uses smooth max-height transition
- Buttons lift on hover (translateY -2px)
- Pricing CTA glows 3 times then stops
- Animations play once only (observer.unobserve)
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-design-enhancement/10-03-SUMMARY.md`
</output>
