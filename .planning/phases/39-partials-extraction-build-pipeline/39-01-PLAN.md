---
phase: 39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 404.html
autonomous: true
requirements:
  - LAYOUT-05
must_haves:
  truths:
    - "404.html header uses the same BEM class structure as contacts.html (header__inner, header__logo, no decorative HTML comments)."
    - "404.html footer uses the same BEM class structure and the same content as contacts.html (footer__wrapper, footer__grid, footer__col, footer__logo, footer__desc, footer__heading, footer__links, footer__link, footer__contact-item, footer__contact-icon, footer__bottom, footer__copyright, footer__badges, footer__badge-item, footer__badge-dot)."
    - "404.html sticky-bar has the outer .sticky-bar BEM class and inner .sticky-bar__container, .sticky-bar__phone, .sticky-bar__cta classes matching contacts.html."
    - "404.html mobile-menu phone link uses .mobile-menu__phone BEM class (already present in checkout — verify)."
    - "404.html footer copy and link list match contacts.html exactly: 4 columns (Company / Услуги / Навигация / Контакты), 2-link Навигация column (Главная + Контакты), no App Store / Google Play links, no Vienna address paragraphs, normalized bottom bar (ISO 27001 Certified · Astana Hub Resident)."
    - "Footer body diff between 404.html and contacts.html is empty (byte-identical structure and copy after normalization)."
    - "404.html header CTA href stays as `contacts.html` (404 has no inline form anchor) and CTA label stays as `Оставить заявку`."
    - "404.html nav links have no `aria-current=\"page\"` attribute (404 is not represented in primary nav)."
  artifacts:
    - path: "404.html"
      provides: "Drift-normalized chrome (header, footer, sticky-bar, mobile-menu) matching the canonical contacts.html BEM pattern from Phase 36a, ready for partial extraction"
      contains: "header__inner"
  key_links:
    - from: "404.html"
      to: "contacts.html"
      via: "BEM class parity (footer body diff empty)"
      pattern: "footer__wrapper.*footer__grid.*footer__col.*footer__bottom"
---

<objective>
Bring 404.html chrome (header, footer, sticky-bar, mobile-menu) into the canonical BEM pattern that Phase 36a established for the other 5 pages. This is a strict prerequisite for Phase 39 partial extraction: until 404.html is structurally identical to the canonical pattern, the byte-identity gate (LAYOUT-12) is unachievable, because the splicer cannot produce the current 404.html from a single canonical partial.

Purpose: 404.html was added in v3.0/v3.1 after Phase 36a normalized the other 5 pages. It still carries the pre-normalization shape: header without `header__inner`/`header__logo` BEM, footer with the old 5-link Навигация column / App Store / Google Play / Vienna address / GDPR badge text, sticky-bar without any sticky-bar BEM classes, decorative HTML comments throughout. This plan deletes that drift so 404.html becomes structurally a peer of contacts.html — only the per-page CTA href (`contacts.html`) and absence of `aria-current` differ.

Output: One file modified — 404.html. After this plan, `diff <(awk '/<footer/,/<\/footer>/' contacts.html) <(awk '/<footer/,/<\/footer>/' 404.html)` is empty, and `awk '/<header/,/<\/header>/' 404.html` line count equals 26 (matching the other 5 pages), with the only diffs being the absence of `aria-current` and the `contacts.html` CTA href.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v3.1-phases/36-shared-layout-primitives/36-SUMMARY.md
@CLAUDE.md
@404.html
@contacts.html

<interfaces>
<!-- Canonical BEM contract (extracted from contacts.html). 404.html must match this. -->

Header outer wrapper:
  <header class="header fixed z-50 transition-all duration-500 top-4 left-4 right-4 mx-auto max-w-7xl rounded-[2.5rem] px-4 md:px-8 border-[0.5px] border-white/50 shadow-glass-header bg-white/30 backdrop-blur-[40px] backdrop-saturate-[150%] py-5" id="header">
    <div class="header__inner flex items-center justify-between">
      <a href="index.html" class="header__logo ...">MedicusUnion</a>
      <nav class="hidden lg:flex items-center gap-8">
        <a href="online-consultations.html" class="text-mu-text-700 hover:text-mu-blue-text transition-colors font-medium tracking-tight">Консультации</a>
        <a href="treatment-abroad.html" class="text-mu-text-700 hover:text-mu-blue-text transition-colors font-medium tracking-tight">Лечение за рубежом</a>
        <a href="checkup.html" class="text-mu-text-700 hover:text-mu-blue-text transition-colors font-medium tracking-tight">Чек-ап</a>
        <a href="#why-us" class="text-mu-text-700 hover:text-mu-blue-text transition-colors font-medium tracking-tight">О нас</a>
        <a href="contacts.html" class="text-mu-text-700 hover:text-mu-blue-text transition-colors font-medium tracking-tight">Контакты</a>
      </nav>
      <div class="hidden lg:flex items-center gap-4">
        <a href="tel:+77015322478" class="text-mu-text-900 font-semibold tracking-tight">+7&nbsp;701&nbsp;532&nbsp;24&nbsp;78</a>
        <a href="{{CTA_HREF}}" class="header__cta bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white px-6 py-2.5 rounded-full font-semibold shadow-lg shadow-mu-blue/25 hover:shadow-xl hover:shadow-mu-blue/30 transition-shadow inline-block tracking-tight">{{CTA_LABEL}}</a>
      </div>
      <button class="header__menu-btn lg:hidden ..." ...>...</button>
    </div>
  </header>

  Note: 404.html does NOT have `#why-us` as a same-page anchor, so the О нас link should be `index.html#why-us` (the index.html convention for cross-page references). Verify this matches what the other non-index pages use.

Footer body shape (must match contacts.html — diff must be empty):
  Lines: 61 (counted via `awk '/<footer/,/<\/footer>/' contacts.html | wc -l`)
  Top wrapper: <footer class="footer relative overflow-hidden z-10 py-16" id="footer">
  Glass wrapper: <div class="footer__wrapper bg-white/60 backdrop-blur-3xl rounded-[3rem] p-12 border border-white/60 shadow-glass-lg">
  Grid: <div class="footer__grid grid md:grid-cols-2 lg:grid-cols-4 gap-12 mb-12">
  Column 1 (Company): h3.footer__logo + p.footer__desc — single sentence "Международный медицинский сервис. Австрия · Казахстан"
  Column 2 (Услуги): h4.footer__heading + ul.footer__links + 3 a.footer__link items (Онлайн-консультации, Лечение за рубежом, Чек-ап)
  Column 3 (Навигация): h4.footer__heading + ul.footer__links + 2 a.footer__link items (Главная, Контакты) — NO clinics, NO О компании, NO Политика конфиденциальности
  Column 4 (Контакты): h4.footer__heading + ul.footer__links + 2 a.footer__contact-item items (phone + email), each containing span.footer__contact-icon (NOT div) wrapping the SVG, with stroke="var(--mu-blue)"
  Bottom bar: <div class="footer__bottom border-t border-mu-text-300/30 pt-8 mt-8"> + p.footer__copyright + p.footer__badges containing span.footer__badge-item (with shield SVG + "ISO 27001 Certified") + span.footer__badge-dot + span ("Astana Hub Resident")
  NO App Store / Google Play link block
  NO Vienna / GmbH address paragraphs
  NO decorative HTML comments inside the footer body
  Bottom-bar wrapper: <div class="flex flex-col md:flex-row justify-between items-center gap-4">

Sticky-bar canonical shape:
  <div class="sticky-bar fixed bottom-4 left-4 right-4 z-50 bg-white/60 backdrop-blur-3xl rounded-2xl border border-white/60 shadow-glass-lg p-3 lg:hidden" id="sticky-bar" role="complementary" aria-label="Быстрые действия">
    <div class="container sticky-bar__container flex items-center justify-between gap-3">
      <a href="tel:+77015322478" class="sticky-bar__phone text-mu-text-900 font-semibold text-sm" aria-label="Позвонить +7 701 532 24 78">+7&nbsp;701&nbsp;532&nbsp;24&nbsp;78</a>
      <a href="{{CTA_HREF}}" class="btn-primary sticky-bar__cta bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-mu-blue/30 text-sm">{{CTA_LABEL}}</a>
    </div>
  </div>

  404.html currently has `<div class="fixed bottom-4 ..." id="sticky-bar">` (no .sticky-bar class on outer), `<div class="flex ...">` (no .container .sticky-bar__container), and bare `<a>` elements with no .sticky-bar__phone / .sticky-bar__cta / .btn-primary classes. All four must be added.

Mobile-menu canonical shape (verify against contacts.html):
  Outer overlay: .mobile-menu-overlay
  Inner panel: .mobile-menu
  Nav: .mobile-menu__nav
  Links: .mobile-menu__link (5 nav links)
  Divider: .mobile-menu__divider
  Phone link: .mobile-menu__phone
  CTA button: .mobile-menu__cta with `{{CTA_HREF}}` and `{{CTA_LABEL}}`
  404.html mobile-menu CTA currently points to `contacts.html` and label is `Оставить заявку` — both correct for 404 and should be preserved exactly.

For 404.html specifically:
  - `aria-current="page"` MUST NOT appear on any nav link or logo (404 is not a primary nav target)
  - Header CTA href: `contacts.html` (preserve current value — 404 has no inline form)
  - Header CTA label: `Оставить заявку`
  - Sticky-bar CTA href: `contacts.html`
  - Sticky-bar CTA label: `Оставить заявку`
  - Mobile-menu CTA href: `contacts.html`
  - Mobile-menu CTA label: `Оставить заявку`
  - The "О нас" nav link points to `index.html#why-us` (cross-page anchor), matching other non-index pages
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Normalize 404.html chrome to canonical BEM pattern</name>
  <files>404.html</files>
  <read_first>
    - 404.html (full file — current pre-normalization shape)
    - contacts.html (canonical reference for header / footer / sticky-bar / mobile-menu BEM and content)
    - .planning/milestones/v3.1-phases/36-shared-layout-primitives/36-SUMMARY.md (Phase 36a normalization stats and decisions, especially "Footer Drift (Before → After)" table and the contacts.html canonical justification)
  </read_first>
  <action>
    Edit `404.html` to bring its chrome (header, footer, sticky-bar, mobile-menu) into byte-identity-ready alignment with the canonical pattern from contacts.html. This is a structural normalization — preserve the per-page values listed below, change everything else to match contacts.html exactly.

    **Per-page values to preserve in 404.html (do not change):**
    - Header CTA: `<a href="contacts.html" class="header__cta ...">Оставить заявку</a>`
    - Sticky-bar CTA: `<a href="contacts.html" class="btn-primary sticky-bar__cta ...">Оставить заявку</a>`
    - Mobile-menu CTA: `<a href="contacts.html" class="mobile-menu__cta ...">Оставить заявку</a>`
    - No `aria-current="page"` anywhere (404 is not in nav)
    - The 404-specific `<main id="page-content">` body content (404 number, "Страница не найдена" H1, "На главную" CTA) — leave entirely alone

    **Header changes:**
    1. Replace `<div class="flex items-center justify-between">` with `<div class="header__inner flex items-center justify-between">`
    2. On the logo `<a href="index.html" class="text-2xl font-bold ...">`, prepend `header__logo` to the class list so it becomes `<a href="index.html" class="header__logo text-2xl font-bold tracking-tight bg-gradient-to-r from-mu-cta-from to-mu-cta-to bg-clip-text text-transparent">MedicusUnion</a>`
    3. Delete the three decorative HTML comment lines: `<!-- Desktop Navigation -->`, `<!-- Desktop Phone & CTA -->`, `<!-- Mobile Menu Button -->`
    4. Verify the desktop nav `<a>` order matches contacts.html: Консультации → Лечение за рубежом → Чек-ап → О нас → Контакты, all using `class="text-mu-text-700 hover:text-mu-blue-text transition-colors font-medium tracking-tight"` (no `aria-current`, no active styling)
    5. Verify "О нас" link href is `index.html#why-us` (cross-page anchor — same convention as other non-index pages)
    6. After all edits, the header block from `<header class="header ...">` through `</header>` must be exactly 26 lines (same as the other 5 pages — confirmed via `awk '/<header/,/<\/header>/' contacts.html | wc -l`)

    **Footer changes (this is the largest change — ~30 lines removed, BEM classes added throughout):**

    The current 404.html footer is the pre-Phase-36a shape (81 lines, drifted content). Replace the ENTIRE footer block — from `<footer class="footer relative overflow-hidden z-10 py-16" id="footer">` through `</footer>` — with the byte-identical footer from contacts.html.

    The mechanical procedure:
    a. Extract contacts.html's footer block: `awk '/<footer/,/<\/footer>/' contacts.html > /tmp/p39-canonical-footer.txt` (61 lines)
    b. Use the Edit tool with old_string = the entire current 404.html footer (lines from `  <!-- FOOTER (Footer.tsx) -->` or wherever the current footer block starts, through `</footer>`) and new_string = the contents of the canonical footer block from contacts.html
    c. The replacement removes: `<!-- Column 1: Company -->`, `<!-- Column 2: Services -->`, `<!-- Column 3: Navigation -->`, `<!-- Column 4: Contacts -->`, `<!-- Bottom bar -->` decorative comments; the Vienna `MedicusUnion GmbH ... Billrothstrasse 78, 1190 Vienna, Austria` paragraph; the `ТОО «MedicusUnion KZ» ... Резидент Astana Hub` paragraph; the 3 extra Навигация links (`index.html#clinics`, `index.html#why-us`, `#` privacy link); the App Store / Google Play link block; the GDPR badge text variant
    d. The replacement adds all `footer__*` BEM classes: `footer__wrapper`, `footer__grid`, `footer__col`, `footer__logo`, `footer__desc`, `footer__heading`, `footer__links`, `footer__link`, `footer__contact-item`, `footer__contact-icon` (changes inner wrapping `<div>` to `<span>`), `footer__bottom`, `footer__copyright`, `footer__badges`, `footer__badge-item`, `footer__badge-dot`
    e. After the edit, verify with `diff <(awk '/<footer/,/<\/footer>/' contacts.html) <(awk '/<footer/,/<\/footer>/' 404.html)` — the output MUST be empty (byte-identical)

    Also delete the `<!-- FOOTER (Footer.tsx) -->` decorative comment that appears immediately before the footer (it's outside the awk range but Phase 36a removed all such Tsx-origin comments from the other pages — keep 404.html consistent).

    **Sticky-bar changes:**
    1. Outer `<div class="fixed bottom-4 left-4 right-4 z-50 bg-white/60 backdrop-blur-3xl rounded-2xl border border-white/60 shadow-glass-lg p-3 lg:hidden" id="sticky-bar" role="complementary" aria-label="Быстрые действия">` → prepend `sticky-bar` to the class list, becoming `<div class="sticky-bar fixed bottom-4 left-4 right-4 z-50 bg-white/60 backdrop-blur-3xl rounded-2xl border border-white/60 shadow-glass-lg p-3 lg:hidden" id="sticky-bar" role="complementary" aria-label="Быстрые действия">`
    2. Inner `<div class="flex items-center justify-between gap-3">` → `<div class="container sticky-bar__container flex items-center justify-between gap-3">`
    3. Phone link `<a href="tel:+77015322478" class="text-mu-text-900 font-semibold text-sm" aria-label="...">` → add `sticky-bar__phone` to class list: `<a href="tel:+77015322478" class="sticky-bar__phone text-mu-text-900 font-semibold text-sm" aria-label="Позвонить +7 701 532 24 78">+7&nbsp;701&nbsp;532&nbsp;24&nbsp;78</a>`
    4. CTA link `<a href="contacts.html" class="bg-gradient-to-r ...">Оставить заявку</a>` → add both `btn-primary` and `sticky-bar__cta` to class list: `<a href="contacts.html" class="btn-primary sticky-bar__cta bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-mu-blue/30 text-sm">Оставить заявку</a>`
    5. Delete the `<!-- Sticky Mobile CTA Bar -->` decorative comment immediately above the sticky-bar block
    6. After all edits, the diff against contacts.html sticky-bar must show only one line difference: the CTA href (`contacts.html` vs contacts.html's `#contact-section`)

    **Mobile-menu changes:**
    Read the current 404.html mobile-menu block (already starts with `.mobile-menu-overlay` and `.mobile-menu` BEM classes — confirmed during planning) and verify that the inner BEM classes match contacts.html: `.mobile-menu__nav`, `.mobile-menu__link` (5 links), `.mobile-menu__divider`, `.mobile-menu__phone`, `.mobile-menu__cta`. If any are missing, add them. Keep the CTA href as `contacts.html` and the label as `Оставить заявку`. Do NOT add `aria-current="page"` to any mobile-menu link. Verify the "О нас" mobile-menu link uses `index.html#why-us` (cross-page anchor).

    **Why this matters:** Phase 39 Plan 02 will extract a single canonical `partials/header.html`, `partials/footer.html`, etc. and splice them into all 6 pages. If 404.html still has the pre-normalization shape, the splicer either fails the byte-identity gate or has to special-case 404.html — both of which violate the "single source of truth" requirement (LAYOUT-01) and the "pure additive layer" decision locked at v3.2 kickoff. Normalizing 404.html now makes the splicer trivially page-uniform.

    Do NOT touch any non-chrome content: the `<main id="page-content">` 404 number, H1, paragraph, "На главную" button must remain byte-identical. Do NOT touch the `<head>`, the `<style>` block, the mesh-bg div, or any script tags.
  </action>
  <verify>
    <automated>
      cd /Users/mikhail/Projects/Medicus_video_consult-landing &amp;&amp; \
      test -f 404.html &amp;&amp; \
      grep -q 'class="header__inner flex' 404.html &amp;&amp; \
      grep -q 'class="header__logo text-2xl' 404.html &amp;&amp; \
      grep -q 'class="footer__wrapper' 404.html &amp;&amp; \
      grep -q 'class="footer__grid' 404.html &amp;&amp; \
      grep -q 'class="footer__bottom' 404.html &amp;&amp; \
      grep -q 'class="sticky-bar fixed' 404.html &amp;&amp; \
      grep -q 'class="container sticky-bar__container' 404.html &amp;&amp; \
      grep -q 'class="sticky-bar__phone' 404.html &amp;&amp; \
      grep -q 'class="btn-primary sticky-bar__cta' 404.html &amp;&amp; \
      ! grep -q 'App.Store' 404.html &amp;&amp; \
      ! grep -q 'Google.Play' 404.html &amp;&amp; \
      ! grep -q 'Billrothstrasse' 404.html &amp;&amp; \
      ! grep -q 'aria-current="page"' 404.html &amp;&amp; \
      ! grep -q '&lt;!-- Desktop Navigation --&gt;' 404.html &amp;&amp; \
      ! grep -q '&lt;!-- Column 1: Company --&gt;' 404.html &amp;&amp; \
      ! grep -q '&lt;!-- Sticky Mobile CTA Bar --&gt;' 404.html &amp;&amp; \
      [ "$(awk '/&lt;header/,/&lt;\/header&gt;/' 404.html | wc -l | tr -d ' ')" = "26" ] &amp;&amp; \
      [ "$(awk '/&lt;footer/,/&lt;\/footer&gt;/' 404.html | wc -l | tr -d ' ')" = "61" ] &amp;&amp; \
      diff &lt;(awk '/&lt;footer/,/&lt;\/footer&gt;/' contacts.html) &lt;(awk '/&lt;footer/,/&lt;\/footer&gt;/' 404.html) &amp;&amp; \
      grep -q 'href="contacts.html" class="header__cta' 404.html &amp;&amp; \
      grep -q 'href="contacts.html" class="btn-primary sticky-bar__cta' 404.html
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f 404.html` exits 0
    - `grep -q 'class="header__inner flex' 404.html` exits 0 (header BEM wrapper present)
    - `grep -q 'class="header__logo text-2xl' 404.html` exits 0 (header logo BEM class present)
    - `grep -q 'class="footer__wrapper' 404.html` exits 0
    - `grep -q 'class="footer__grid' 404.html` exits 0
    - `grep -q 'class="footer__bottom' 404.html` exits 0
    - `grep -q 'class="sticky-bar fixed' 404.html` exits 0 (outer sticky-bar class added)
    - `grep -q 'class="container sticky-bar__container' 404.html` exits 0
    - `grep -q 'class="sticky-bar__phone' 404.html` exits 0
    - `grep -q 'class="btn-primary sticky-bar__cta' 404.html` exits 0
    - `! grep -q 'App.Store' 404.html` (App Store link block removed)
    - `! grep -q 'Google.Play' 404.html` (Google Play link block removed)
    - `! grep -q 'Billrothstrasse' 404.html` (Vienna address paragraph removed)
    - `! grep -q 'aria-current="page"' 404.html` (404 has no nav presence)
    - `! grep -q '<!-- Desktop Navigation -->' 404.html` (decorative comment removed)
    - `! grep -q '<!-- Column 1: Company -->' 404.html` (decorative comment removed)
    - `! grep -q '<!-- Sticky Mobile CTA Bar -->' 404.html` (decorative comment removed)
    - Header line count exactly 26: `[ "$(awk '/<header/,/<\/header>/' 404.html | wc -l | tr -d ' ')" = "26" ]`
    - Footer line count exactly 61: `[ "$(awk '/<footer/,/<\/footer>/' 404.html | wc -l | tr -d ' ')" = "61" ]`
    - Footer body byte-identical to contacts.html: `diff <(awk '/<footer/,/<\/footer>/' contacts.html) <(awk '/<footer/,/<\/footer>/' 404.html)` exits 0 with empty output
    - 404 CTA hrefs preserved: `grep -q 'href="contacts.html" class="header__cta' 404.html` AND `grep -q 'href="contacts.html" class="btn-primary sticky-bar__cta' 404.html` both exit 0
    - The `<main id="page-content">` block is unchanged (404 H1, "На главную" button intact) — verifiable by `grep -q 'Страница&nbsp;не&nbsp;найдена' 404.html`
  </acceptance_criteria>
  <done>
    404.html chrome (header, footer, sticky-bar, mobile-menu) is structurally indistinguishable from contacts.html except for the 404-specific CTA hrefs (`contacts.html`) and the absence of `aria-current="page"`. The footer body diff against contacts.html is empty. All BEM classes from the Phase 36a canonical pattern are present. All decorative `<!-- ... -->` Tsx-origin comments are removed from the chrome regions. The 404 main content block is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→repo | Static HTML normalization — no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Tampering | 404.html chrome content | mitigate | Edit is a structural BEM-class normalization plus content-pruning to match the canonical contacts.html. Verification gate is `diff <(awk '/<footer/,/<\/footer>/' contacts.html) <(awk '/<footer/,/<\/footer>/' 404.html)` returning empty — any unintended drift fails the verify block. |
| T-39-01-02 | Information Disclosure | Vienna business address (`Billrothstrasse 78, 1190 Vienna, Austria`) currently in 404.html footer | accept | The address is the company's public business address (already in canonical contacts.html on the contact line) and is intentionally pruned from 404.html footer to match the canonical single-source pattern; no information leak — same data still public on the canonical contact path. |
| T-39-01-03 | Denial of Service | 404 main content (`Страница не найдена` H1, "На главную" link) | mitigate | The action explicitly forbids touching the `<main id="page-content">` block; verification grep `grep -q 'Страница&nbsp;не&nbsp;найдена' 404.html` confirms the H1 survives the edit. |
</threat_model>

<verification>
1. After editing, run all `<verify>` automated commands above; every command must exit 0.
2. Visually inspect 404.html in a browser (file://) at desktop and mobile viewports; the page must look identical to contacts.html chrome except for the body content. The CTA "Оставить заявку" in header / sticky-bar / mobile-menu must navigate to contacts.html.
3. `git diff 404.html` must show: deletions for the old footer block + decorative comments + Vienna paragraphs + App Store block; additions for the canonical footer block + BEM-prefixed header classes + BEM-prefixed sticky-bar classes. NO additions or deletions outside the 4 chrome regions.
</verification>

<success_criteria>
1. `404.html` parses cleanly (no broken markup) and renders correctly in a browser.
2. Footer body diff against contacts.html is empty: `diff <(awk '/<footer/,/<\/footer>/' contacts.html) <(awk '/<footer/,/<\/footer>/' 404.html)` exits 0.
3. Header / sticky-bar / mobile-menu use the same BEM class names as contacts.html, with only the per-page CTA href and absence of `aria-current` differing.
4. The 404 main content block and the document `<head>` are byte-identical to their pre-edit state (verifiable via line-by-line diff of those regions before/after).
5. Phase 39 Plan 02 can extract a single canonical `partials/{header,footer,sticky-bar,mobile-menu}.html` and splice it into all 6 pages (including 404.html) without per-page special cases.
</success_criteria>

<output>
After completion, create `.planning/phases/39-partials-extraction-build-pipeline/39-01-SUMMARY.md` documenting:
- Lines changed (header / footer / sticky-bar / mobile-menu deltas)
- Verification gate results (footer diff empty, header/footer line counts)
- Confirmation that 404.html is now extraction-ready
- Any deviations from the planned BEM normalization (should be none)
</output>
