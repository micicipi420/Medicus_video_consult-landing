---
phase: 87-real-content
plan: 01
type: execute
wave: 1
depends_on: ["81", "84"]
files_modified:
  - next/src/components/sections/HeroHub.tsx
  - next/src/components/sections/ContactSection.tsx
autonomous: true
requirements:
  - CNT-01
  - CNT-02
must_haves:
  truths:
    - "HeroHub no longer references 'Ferdinand'"
    - "HeroHub uses a stable doctor name (Dr. Stefan Mayr · Vienna)"
    - "HeroHub has a TODO(content) comment flagging name for marketing replacement"
    - "ContactSection has a coordinator block with avatar + name + role + phone/email"
    - "Coordinator avatar is rendered without external image dependency"
    - "Coordinator avatar has aria-hidden='true' (decorative)"
    - "ContactSection has a TODO(content) comment flagging coordinator placeholder"
    - "pnpm build compiles cleanly"
  artifacts:
    - path: "next/src/components/sections/HeroHub.tsx"
      provides: "Stable doctor name placeholder + content TODO marker"
    - path: "next/src/components/sections/ContactSection.tsx"
      provides: "Coordinator presence with initials avatar (no external image)"
---

<objective>
Replace v8.0 placeholders without introducing third-party image dependencies.
</objective>

<verification>
```bash
grep "Ferdinand" next/src/components/sections/HeroHub.tsx       # 0
grep "Stefan.Mayr" next/src/components/sections/HeroHub.tsx     # 1
grep "TODO(content)" next/src/components/sections/HeroHub.tsx   # 1
grep "TODO(content)" next/src/components/sections/ContactSection.tsx  # 1
grep "Айгерим" next/src/components/sections/ContactSection.tsx  # 1
grep 'aria-hidden="true"' next/src/components/sections/ContactSection.tsx  # ≥4 (icons + avatar)
cd next && pnpm build  # clean
```
</verification>
