---
phase: 74.1-integration-cleanup-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 74.1-01
files_modified:
  - .planning/phases/74-accessibility-hardening/74-VERIFICATION.md
  - .planning/DECISIONS.md
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
  - ACC-04
  - ACC-05

must_haves:
  truths:
    - "A 74-VERIFICATION.md file exists in the phase 74 directory, declaring ACC-01 through ACC-05 SATISFIED with code evidence (grep patterns, line numbers, commit hashes)"
    - "The header--scrolled vs .is-scrolled class divergence is documented as an architectural decision in .planning/DECISIONS.md"
  artifacts:
    - path: ".planning/phases/74-accessibility-hardening/74-VERIFICATION.md"
      provides: "Formal Phase 74 verification confirming ACC-01..ACC-05 satisfied"
      contains: "ACC-05"
    - path: ".planning/DECISIONS.md"
      provides: "Architectural decision record for header class naming divergence"
      contains: "is-scrolled"
  key_links:
    - from: "74-VERIFICATION.md ACC-04 entry"
      to: "css/styles.css canonical focus-visible rule"
      via: "grep evidence with line number"
      pattern: "faq__question:focus-visible.*3px"
    - from: "DECISIONS.md header class entry"
      to: "js/main.js .is-scrolled toggle"
      via: "architectural documentation"
      pattern: "is-scrolled"
---

<objective>
Formally verify Phase 74 by writing 74-VERIFICATION.md with grep-backed evidence for each ACC requirement, and document the header--scrolled vs .is-scrolled class naming divergence as an explicit architectural decision.

Purpose: Phase 74 code is committed and correct but was never formally verified. This plan closes that paperwork gap and captures the two-layer CSS architecture finding before it confuses future maintainers.
Output: .planning/phases/74-accessibility-hardening/74-VERIFICATION.md (Phase 74 formal verification), .planning/DECISIONS.md (architecture decision record).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/74.1-integration-cleanup-verification/74.1-CONTEXT.md
@.planning/phases/74-accessibility-hardening/74-02-SUMMARY.md
@.planning/phases/74.1-integration-cleanup-verification/74.1-01-SUMMARY.md

<interfaces>
Phase 74 commits (from .continue-here.md):
- 651775e: Section 14.5 prefers-contrast:more in liquid-glass.css (ACC-01)
- 59b153e: --liquid-bg opacity raised to 0.55, vanilla prefers-contrast block (ACC-01, ACC-03)
- e701faf: global :focus-visible system 3px solid cyan (ACC-04)
- f9cd6ba: 44px min touch targets (ACC-05)

ACC-01 production wiring:
- css/styles.css contains @media (prefers-contrast: more) block at end of file (line ~2720)
- Covers: .card, .pricing__card, .lead-form__wrapper, .button, .advantages__icon
- liquid-glass.css Section 14.5 covers glass classes (design system layer only)

ACC-02 production wiring (after Plan 01 runs):
- css/styles.css will contain @media (prefers-reduced-transparency: reduce) at end of file
- Covers: .problem, .benefits, .lead-form-section::before
- liquid-glass.css Section 14 covers glass classes (design system layer only)

ACC-03 wiring:
- src/styles/theme.css: --liquid-bg raised to 55% (color-mix opacity floor)
- Production pages (css/styles.css) use solid white backgrounds -- no glass transparency
- Design system layer provides the opacity floor for Next.js scaffold glass elements

ACC-04 production wiring:
- css/styles.css lines ~1566-1595: global :focus-visible + .lead-form__input + .faq__question
- After Plan 01: single canonical .faq__question:focus-visible rule (3px cyan)
- src/styles/liquid-glass.css Section 16: glass elements use 3px solid #38C6F4
- src/styles/theme.css (after Plan 01): updated to 3px solid #38C6F4, outline-offset: 2px

ACC-05 production wiring:
- css/styles.css lines ~1602-1611: min-height: 44px on 5 selectors
- .site-header__link, .footer__link, .footer__app-link, .site-header__brand-link, .hub-guide__link

Header class divergence:
- theme.css (design system layer): .header--scrolled (Next.js component naming)
- js/main.js (production vanilla): .is-scrolled (BEM state modifier)
- css/styles.css: .site-header.is-scrolled (production uses .is-scrolled)
- These serve different layers -- NOT a bug, IS an architectural property
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 74-VERIFICATION.md confirming ACC-01 through ACC-05</name>
  <files>.planning/phases/74-accessibility-hardening/74-VERIFICATION.md</files>
  <read_first>
    - css/styles.css (run grep for: "prefers-contrast", "prefers-reduced-transparency", "focus-visible", "min-height: 44px", "#38C6F4" to gather current line numbers and match counts)
    - src/styles/theme.css (run grep for "#38C6F4" to confirm Plan 01 fix)
    - src/styles/liquid-glass.css (grep for "Section 14.5", "prefers-contrast: more", "#38C6F4")
    - .planning/phases/74-accessibility-hardening/.continue-here.md (commit hashes)
  </read_first>
  <action>
Create `.planning/phases/74-accessibility-hardening/74-VERIFICATION.md`.

Before writing, run these grep commands and record the actual line numbers and counts:
```bash
grep -n "prefers-contrast: more" css/styles.css
grep -n "prefers-reduced-transparency" css/styles.css
grep -n "#38C6F4" css/styles.css
grep -c "#38C6F4" css/styles.css
grep -n "faq__question:focus-visible" css/styles.css
grep -n "min-height: 44px" css/styles.css
grep -c "focus-visible" css/styles.css
grep -n "#38C6F4" src/styles/theme.css
grep -n "Section 14.5" src/styles/liquid-glass.css
grep -n "prefers-contrast: more" src/styles/liquid-glass.css
```

Write the verification file with the structure below. Replace every [INSERT LINE] and [INSERT COUNT] placeholder with the actual values from the grep commands above — do NOT leave any placeholder text.

The file MUST start with a YAML frontmatter block (between triple-dash separators) containing:
- phase: 74-accessibility-hardening
- verified: 2026-04-14
- status: VERIFIED
- requirements listing ACC-01 through ACC-05 each marked SATISFIED
- verified_by: Phase 74.1 integration cleanup (gap closure)
- commits: 651775e, 59b153e, e701faf, f9cd6ba

Then the body:

Title: "Phase 74: Accessibility Hardening — Verification"

Include a section "Architecture Note" explaining that Phase 74 accessibility work spans TWO CSS layers:
1. Production layer (css/styles.css): Loaded by all HTML pages. ACC-01, ACC-02, ACC-04, ACC-05 are fully wired here.
2. Design system layer (src/styles/liquid-glass.css, src/styles/theme.css): Serves Next.js scaffold via Tailwind compilation. Glass class accessibility (Section 14.5, Section 14) is here.
Note that production pages use solid backgrounds (no glass classes), so design system layer blocks apply only to the Next.js scaffold.

Include a section "Requirement Verification" with subsections for each ACC requirement:

**ACC-01: prefers-contrast:more**
- Status: SATISFIED
- Production layer: @media (prefers-contrast: more) block at line [INSERT LINE] covers .card, .pricing__card, .lead-form__wrapper, .button, .advantages__icon
- Design system layer: Section 14.5 at line [INSERT LINE] (from liquid-glass.css grep) covers all glass classes with tiered solid fills, dark mode overrides, pseudo-elements disabled
- Commits: 651775e, 59b153e
- Verify command: grep -n "prefers-contrast: more" css/styles.css src/styles/liquid-glass.css

**ACC-02: prefers-reduced-transparency:reduce**
- Status: SATISFIED
- Production layer: @media (prefers-reduced-transparency: reduce) block at line [INSERT LINE] covers .problem, .benefits, .lead-form-section::before (added by Phase 74.1 Plan 01)
- Design system layer: Section 14 at line ~768 covers all glass classes with backdrop-filter: none
- Note: Chrome/Edge 118+ only -- progressive enhancement
- Verify command: grep -n "prefers-reduced-transparency" css/styles.css src/styles/liquid-glass.css

**ACC-03: WCAG AA contrast on glass surfaces**
- Status: SATISFIED
- --liquid-bg raised from 42%/45% to 55%/55% in src/styles/theme.css (commit 59b153e)
- Text color #18212C (dark navy) against 55% opacity white composite exceeds 4.5:1
- Production HTML pages use solid backgrounds -- no glass surfaces
- Verify command: grep "--liquid-bg.*55" src/styles/theme.css

**ACC-04: :focus-visible indicators**
- Status: SATISFIED
- Production layer: global rule at line [INSERT LINE], lead form at line [INSERT LINE], FAQ at line [INSERT LINE], total [INSERT COUNT] occurrences of #38C6F4 in css/styles.css
- Dead rule removed (Phase 74.1 Plan 01): old .faq__question:focus-visible (2px blue) at former line 1253 deleted
- Spec aligned: src/styles/theme.css updated from 2px blue to 3px solid #38C6F4 (Phase 74.1 Plan 01)
- Design system layer: liquid-glass.css Section 16 uses 3px solid #38C6F4
- Commits: e701faf (implementation), Phase 74.1 (dead rule removal + theme.css alignment)
- Verify command: grep -c "#38C6F4" css/styles.css

**ACC-05: 44px minimum touch targets**
- Status: SATISFIED
- Touch target block at line [INSERT LINE]: min-height: 44px on .site-header__link, .footer__link, .footer__app-link, .site-header__brand-link, .hub-guide__link
- Applied on ALL viewports (no @media wrapper)
- Unchanged: .button (48px), .faq__question (48px), .site-header__phone (48px)
- Commit: f9cd6ba
- Verify command: grep -n "min-height: 44px" css/styles.css

Include a section "Visual Verification" noting that visual verification was deferred by user during Phase 74 execution (per .continue-here.md context), and visual checks will be performed in Phase 78:
- prefers-contrast:more: tiered solid fills visible in DevTools Rendering emulation
- prefers-reduced-transparency:reduce: section gradient overlays gone
- Keyboard Tab: cyan ring on every interactive element across all 4 pages
- Mobile 375px: all header/footer links 44px+ computed height
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && ls .planning/phases/74-accessibility-hardening/74-VERIFICATION.md && grep -c "SATISFIED" .planning/phases/74-accessibility-hardening/74-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/74-accessibility-hardening/74-VERIFICATION.md exists
    - grep "status: VERIFIED" .planning/phases/74-accessibility-hardening/74-VERIFICATION.md returns 1 match
    - grep -c "SATISFIED" .planning/phases/74-accessibility-hardening/74-VERIFICATION.md returns at least 5 matches (one per ACC requirement)
    - No [INSERT LINE] or [INSERT COUNT] placeholder text remains in the file (grep "INSERT" returns 0)
    - The file contains commit hashes 651775e, 59b153e, e701faf, f9cd6ba
    - Frontmatter status field is "VERIFIED" (not "partial" or "pending")
    - The file contains at least one actual line number reference proving real grep was run
  </acceptance_criteria>
  <done>
    74-VERIFICATION.md exists with ACC-01 through ACC-05 all marked SATISFIED. Evidence includes actual grep results with real line numbers and commit hashes. No placeholder text remains. Phase 74 is formally verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document header--scrolled vs .is-scrolled architecture in DECISIONS.md</name>
  <files>.planning/DECISIONS.md</files>
  <read_first>
    - .planning/DECISIONS.md (check if file exists and read current content -- if exists, note the last AD-NN number)
    - js/main.js lines 458-475 (initStickyHeader function -- .is-scrolled toggle)
    - css/styles.css lines 388-392 (.site-header.is-scrolled rule)
    - src/styles/theme.css lines 468-480 (.header--scrolled rule)
  </read_first>
  <action>
Check if `.planning/DECISIONS.md` exists. If it does not exist, create it. If it does exist, append a new decision entry after the last entry (incrementing the AD-NN number).

The file should follow this structure (create it from scratch if missing):

Title: "Architectural Decisions: MedicusUnion KZ"
Subtitle: "This file documents intentional architectural choices that could appear as inconsistencies to future maintainers."

Decision entry: AD-01 (or next available number)
Title: "Dual header scroll class naming (design system vs production)"
Date: 2026-04-14
Context: Phase 74.1 integration audit identified a class name divergence between layers.
Finding: src/styles/theme.css targets .header--scrolled (Next.js component naming convention) while js/main.js toggles .is-scrolled on the header element (BEM state modifier convention). Production CSS (css/styles.css) uses .site-header.is-scrolled.

Decision: This is intentional and NOT a bug. The two layers serve distinct runtimes:
- src/styles/theme.css serves the Next.js scaffold (Tailwind compilation, component-based naming)
- js/main.js + css/styles.css serve vanilla HTML production pages (BEM conventions)

The .header--scrolled class in theme.css is unreachable by the vanilla JS because the vanilla header never receives that class. The .is-scrolled toggle in main.js is unreachable by theme.css because Next.js does not load main.js.

Resolution: No code change needed. The two layers are architecturally isolated. They would only need alignment if the Next.js scaffold were extended to reuse the same JS scroll handler, which is out of scope for v7.0.

Affected files:
- src/styles/theme.css line ~470: .header--scrolled (design system layer)
- js/main.js line ~470: classList.add('is-scrolled') (production layer)
- css/styles.css line ~390: .site-header.is-scrolled (production layer)

Related: v7.0 Milestone Audit finding #3 (integration section)
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && ls .planning/DECISIONS.md && grep -c "is-scrolled" .planning/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/DECISIONS.md exists
    - grep "is-scrolled" .planning/DECISIONS.md returns at least 2 matches
    - grep "header--scrolled" .planning/DECISIONS.md returns at least 1 match
    - grep "NOT a bug" .planning/DECISIONS.md returns 1 match
    - The entry explains both the finding AND the resolution (no code change needed)
    - The affected files list covers all 3 files: theme.css, main.js, css/styles.css
  </acceptance_criteria>
  <done>
    .planning/DECISIONS.md contains an entry documenting the .header--scrolled vs .is-scrolled class naming divergence as an intentional architectural property of the dual-layer CSS system. Future maintainers will not treat this as a bug.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification document -> future maintainers | Document accuracy matters; grep evidence prevents false claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74.1-03 | Information Disclosure | VERIFICATION.md accuracy | mitigate | Executor must run grep commands and insert real values; no placeholder text allowed in final file |
| T-74.1-04 | Repudiation | Architecture decisions | mitigate | DECISIONS.md provides audit trail for intentional divergence; prevents future "fix" of correct behavior |
</threat_model>

<verification>
1. ls .planning/phases/74-accessibility-hardening/74-VERIFICATION.md -- file exists
2. grep "status: VERIFIED" .planning/phases/74-accessibility-hardening/74-VERIFICATION.md -- 1 match
3. grep -c "SATISFIED" .planning/phases/74-accessibility-hardening/74-VERIFICATION.md -- 5 matches
4. grep "INSERT" .planning/phases/74-accessibility-hardening/74-VERIFICATION.md -- 0 matches (no placeholders)
5. grep "is-scrolled" .planning/DECISIONS.md -- at least 2 matches
</verification>

<success_criteria>
- 74-VERIFICATION.md exists with all 5 ACC requirements marked SATISFIED, backed by grep evidence with real line numbers
- No [INSERT *] placeholder text remains in any created file
- DECISIONS.md documents the header class divergence as intentional architecture
- Phase 74 is now formally verified -- audit finding "74-VERIFICATION.md missing" is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/74.1-integration-cleanup-verification/74.1-02-SUMMARY.md`
</output>
