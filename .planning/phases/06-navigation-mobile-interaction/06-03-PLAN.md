---
phase: 06-navigation-mobile-interaction
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - index.html
  - css/styles.css
autonomous: true
requirements:
  - STRUC-01
  - UX-01

must_haves:
  truths:
    - "All 11 sections from brief present in correct order on a single page"
    - "Page renders correctly at mobile (375px), tablet (768px), desktop (1024px+)"
    - "No horizontal overflow at any viewport width"
    - "Form placeholder section exists with id=form for CTA anchor targets"
  artifacts:
    - path: "index.html"
      provides: "Complete page with all sections including form placeholder"
      contains: 'id="form"'
    - path: "css/styles.css"
      provides: "Form placeholder styling"
      contains: "form-placeholder"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "all 11 section IDs present"
      pattern: 'id="(hero|problem|benefits|process|doctors|advantages|scenarios|pricing|form|faq|final-cta)"'
---

<objective>
Add a form placeholder section (for Phase 7) and verify the complete page assembly: all 11 sections present in correct order, responsive at all breakpoints, no layout issues.

Purpose: STRUC-01 requires all 11 sections. UX-01 requires full responsive verification. The form section doesn't exist yet but needs a placeholder with id="form" so CTA smooth-scroll works immediately.
Output: Form placeholder in HTML, any responsive fixes discovered during audit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-navigation-mobile-interaction/06-01-SUMMARY.md
@.planning/phases/06-navigation-mobile-interaction/06-02-SUMMARY.md
@index.html
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add form placeholder section and verify all 11 sections present</name>
  <read_first>index.html, css/styles.css</read_first>
  <files>index.html, css/styles.css</files>
  <action>
1. In index.html, add a form placeholder section AFTER the pricing section (after `</section>` closing the pricing section, id="pricing") and BEFORE the FAQ section (id="faq"). Insert this exact HTML:

```html
<!-- Form Section (placeholder for Phase 7) -->
<section class="section form-placeholder" id="form">
  <div class="container">
    <h2 class="form-placeholder__heading">Оставить заявку на&nbsp;консультацию</h2>
    <p class="form-placeholder__text">Заполните форму&nbsp;&mdash; мы&nbsp;перезвоним в&nbsp;течение 24&nbsp;часов. Бесплатно и&nbsp;без обязательств.</p>
    <!-- Form fields will be added in Phase 7 -->
  </div>
</section>
```

2. In css/styles.css, add form placeholder styles after the pricing section styles and before the FAQ styles:

```css
/* -- Form Placeholder (replaced in Phase 7) -- */

.form-placeholder {
  background-color: var(--color-light);
}

.form-placeholder__heading {
  margin-bottom: var(--space-3);
}

.form-placeholder__text {
  font-size: var(--font-size-lg);
  max-width: 640px;
  color: var(--color-text-primary);
}
```

3. Verify the section order in index.html matches the brief requirement (STRUC-01). The correct order must be:
   1. header (site-header)
   2. Hero (id="hero")
   3. Problem (id="problem")
   4. Benefits/What You Get (id="benefits")
   5. Process/How It Works (id="process")
   6. Doctors/Who Consults (id="doctors")
   7. Advantages/Why Us (id="advantages")
   8. Scenarios/When Needed (id="scenarios")
   9. Pricing (id="pricing")
   10. Form (id="form")
   11. FAQ (id="faq")
   12. Final CTA (id="final-cta")
   13. Footer (id="footer")

If any section is out of order, reorder it. Based on current HTML, all sections are in correct order -- just need the form placeholder inserted between pricing and FAQ.

4. Audit for responsive issues at 375px. Check that:
   - No element has a fixed width that could cause horizontal overflow
   - All grid layouts collapse to single column on mobile
   - Text doesn't overflow containers

If any issues found, add CSS fixes. Based on current code review, no issues expected -- all grids use responsive breakpoints.
  </action>
  <acceptance_criteria>
    - grep -q 'id="form"' index.html
    - grep -q "form-placeholder" index.html
    - grep -q "form-placeholder__heading" css/styles.css
    - grep -q 'id="hero"' index.html
    - grep -q 'id="problem"' index.html
    - grep -q 'id="benefits"' index.html
    - grep -q 'id="process"' index.html
    - grep -q 'id="doctors"' index.html
    - grep -q 'id="advantages"' index.html
    - grep -q 'id="scenarios"' index.html
    - grep -q 'id="pricing"' index.html
    - grep -q 'id="faq"' index.html
    - grep -q 'id="final-cta"' index.html
    - grep -q 'id="footer"' index.html
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'id="form"' index.html && grep -c 'id="hero"' index.html && grep -c 'id="faq"' index.html && grep -c 'id="final-cta"' index.html && grep -c "form-placeholder" css/styles.css</automated>
  </verify>
  <done>All 11 sections from the brief are present in index.html in the correct order. Form placeholder with id="form" exists so CTA smooth-scroll works. Page is responsive at all breakpoints with no horizontal overflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete page</name>
  <what-built>Complete page assembly with header, all 11 content sections, sticky mobile bar, smooth scroll CTAs, and click-to-call phone numbers.</what-built>
  <how-to-verify>
    1. Open index.html in a browser
    2. Verify header shows "MedicusUnion" and phone number at top
    3. Scroll through: all sections visible in order (Hero, Problem, Benefits, Process, Doctors, Advantages, Scenarios, Pricing, Form placeholder, FAQ, Final CTA, Footer)
    4. Click any "Получить консультацию" button -- should smooth-scroll to form placeholder
    5. Resize to mobile (375px): sticky bar visible at bottom with phone + button
    6. Resize to desktop (1024px+): sticky bar disappears
    7. On mobile: scroll to footer area -- sticky bar should hide
    8. Click phone number in header, sticky bar, or footer -- should trigger tel: action
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 11 section IDs present in index.html in correct order
- Form placeholder has id="form" for CTA targeting
- No horizontal overflow at 375px, 768px, 1024px viewports
- Complete visual verification by user
</verification>

<success_criteria>
- STRUC-01: All 11 sections present and in order
- UX-01: Responsive at mobile, tablet, desktop
- Form placeholder ready for Phase 7
- User approves visual result
</success_criteria>

<output>
After completion, create `.planning/phases/06-navigation-mobile-interaction/06-03-SUMMARY.md`
</output>
