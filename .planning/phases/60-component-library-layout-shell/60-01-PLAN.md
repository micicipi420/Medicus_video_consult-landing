---
phase: 60-component-library-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/components.json
  - next/src/lib/utils.ts
  - next/src/components/ui/button.tsx
  - next/src/components/ui/card.tsx
  - next/src/components/ui/input.tsx
  - next/src/components/ui/select.tsx
  - next/src/components/ui/textarea.tsx
  - next/src/components/ui/dialog.tsx
  - next/src/components/layout/SvgRefractionDefs.tsx
  - next/src/lib/navigation.ts
  - next/src/app/globals.css
  - next/package.json
autonomous: true
requirements: [SCAF-03]

must_haves:
  truths:
    - "shadcn/ui CLI is initialized and components.json exists with correct Tailwind v4 config"
    - "Button, Card, Input, Select, Textarea, Dialog components are importable from @/components/ui/*"
    - "cn() utility exists at @/lib/utils and correctly merges Tailwind classes"
    - "SvgRefractionDefs component renders 3 SVG refraction filters (sm, md, lg) as a Server Component"
    - "Shared NAV_LINKS array is importable from @/lib/navigation"
    - "All existing MedicusUnion CSS tokens (--mu-*, --liquid-*, --section-*) are preserved in globals.css after shadcn init"
  artifacts:
    - path: "next/components.json"
      provides: "shadcn/ui configuration"
      contains: "new-york"
    - path: "next/src/lib/utils.ts"
      provides: "cn() class merging utility"
      exports: ["cn"]
    - path: "next/src/components/ui/button.tsx"
      provides: "shadcn Button component"
    - path: "next/src/components/ui/card.tsx"
      provides: "shadcn Card component"
    - path: "next/src/components/ui/input.tsx"
      provides: "shadcn Input component"
    - path: "next/src/components/ui/select.tsx"
      provides: "shadcn Select component"
    - path: "next/src/components/ui/textarea.tsx"
      provides: "shadcn Textarea component"
    - path: "next/src/components/ui/dialog.tsx"
      provides: "shadcn Dialog component"
    - path: "next/src/components/layout/SvgRefractionDefs.tsx"
      provides: "SVG refraction filter definitions"
      exports: ["SvgRefractionDefs"]
    - path: "next/src/lib/navigation.ts"
      provides: "Shared navigation link configuration"
      exports: ["NAV_LINKS", "PHONE_NUMBER", "PHONE_DISPLAY", "EMAIL"]
  key_links:
    - from: "next/src/lib/utils.ts"
      to: "clsx + tailwind-merge"
      via: "import"
      pattern: "import.*clsx|import.*tailwind-merge"
    - from: "next/components.json"
      to: "next/src/app/globals.css"
      via: "css config path"
      pattern: "src/app/globals.css"
---

<objective>
Initialize shadcn/ui component library with 6 base components and create shared infrastructure (SVG defs, navigation config, cn utility) that all layout components will consume.

Purpose: Establishes the component library foundation (SCAF-03) and shared data/utilities needed by Plan 02's layout components.
Output: shadcn/ui configured and importable, SvgRefractionDefs ready for root layout, NAV_LINKS shared across Header/MobileMenu/Footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-next-js-scaffold-css-foundation/59-01-SUMMARY.md
@.planning/phases/59-next-js-scaffold-css-foundation/59-02-SUMMARY.md
@.planning/phases/60-component-library-layout-shell/60-RESEARCH.md

<interfaces>
<!-- From Phase 59: existing files this plan must preserve/extend -->

From next/src/app/globals.css (CRITICAL -- must survive shadcn init):
- Lines 1-4: @import chain (tailwindcss, tw-animate-css, liquid-glass.css, squircles.css)
- Lines 14-136: :root tokens (--mu-*, --liquid-*, --section-*, --font-*, --grid-*, glass hierarchy)
- Lines 138-183: .dark overrides
- Lines 186-265: @theme inline block with all Tailwind mappings
- Lines 267-416: @layer base/utilities/components rules

From next/src/app/layout.tsx:
```typescript
import type { Metadata } from 'next';
import localFont from 'next/font/local';
import './globals.css';
// inter and manrope font declarations
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru" className={`${inter.variable} ${manrope.variable}`}>
      <body className="relative bg-mu-text-50 text-mu-text-900 overflow-x-clip">
        {children}
      </body>
    </html>
  );
}
```

From next/package.json:
```json
{
  "sideEffects": ["*.css"],
  "dependencies": {
    "next": "15.5.15",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "tw-animate-css": "^1.4.0"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and add 6 base components</name>
  <files>next/components.json, next/src/lib/utils.ts, next/src/components/ui/button.tsx, next/src/components/ui/card.tsx, next/src/components/ui/input.tsx, next/src/components/ui/select.tsx, next/src/components/ui/textarea.tsx, next/src/components/ui/dialog.tsx, next/src/app/globals.css, next/package.json</files>
  <read_first>
    next/src/app/globals.css
    next/package.json
    next/src/app/layout.tsx
  </read_first>
  <action>
CRITICAL PREREQUISITE: Before running shadcn init, save a backup of globals.css content in memory. The CLI may overwrite tokens.

1. Change to next/ directory. Run `npx shadcn@latest init` with these answers:
   - Style: new-york (only option in CLI v4)
   - Base color: neutral
   - CSS file: src/app/globals.css
   - CSS variables: yes
   - Tailwind config: (blank -- Tailwind v4, no config file)
   - Components path: @/components/ui
   - RSC: true
   - TSX: true

   If interactive prompts are an issue, try: `npx shadcn@latest init --style new-york --base-color neutral --css src/app/globals.css --css-variables --rsc --tsx`

2. IMMEDIATELY after init, diff globals.css against the pre-init version. Verify ALL these tokens are present:
   - @import "tailwindcss" (line 4)
   - @import "../styles/liquid-glass.css" (line 8)
   - @import "../styles/squircles.css" (line 9)
   - :root block with --mu-blue: #38C6F4 (line 21)
   - :root block with --liquid-bg: rgba(255, 255, 255, 0.42) (line 104)
   - @theme inline block with --color-mu-blue: var(--mu-blue) (line 187)
   - @layer components .header--scrolled block (line 385)
   - @layer base * { @apply border-border } rule

   If ANY MedicusUnion tokens are missing, restore globals.css from git (`git checkout -- next/src/app/globals.css`) and manually merge ONLY the new shadcn additions (typically --background, --foreground, --primary color variables and @theme inline mappings for them -- but Phase 59 already ported these, so shadcn may add nothing new).

3. Run `npx shadcn@latest add button card input select textarea dialog` to install all 6 required components.

4. Verify components.json was created with correct configuration:
   - "style": "new-york"
   - "rsc": true
   - "tsx": true
   - aliases.utils: "@/lib/utils"
   - aliases.ui: "@/components/ui"

5. Verify utils.ts was created at src/lib/utils.ts containing cn() function using clsx + tailwind-merge.

6. Run `cd next && npm run build` to verify no build errors.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `next/components.json` exists and contains `"style": "new-york"`
    - `next/src/lib/utils.ts` exists and exports `cn` function
    - `next/src/components/ui/button.tsx` exists
    - `next/src/components/ui/card.tsx` exists
    - `next/src/components/ui/input.tsx` exists
    - `next/src/components/ui/select.tsx` exists
    - `next/src/components/ui/textarea.tsx` exists
    - `next/src/components/ui/dialog.tsx` exists
    - `next/src/app/globals.css` still contains `--mu-blue: #38C6F4` (MedicusUnion tokens preserved)
    - `next/src/app/globals.css` still contains `@import "../styles/liquid-glass.css"` (glass CSS import preserved)
    - `next/src/app/globals.css` still contains `--liquid-bg: rgba(255, 255, 255, 0.42)` (glass tokens preserved)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>shadcn/ui initialized with new-york style. All 6 base components (Button, Card, Input, Select, Textarea, Dialog) installed and importable. cn() utility available. All existing MedicusUnion CSS tokens preserved in globals.css. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared navigation config and SvgRefractionDefs component</name>
  <files>next/src/lib/navigation.ts, next/src/components/layout/SvgRefractionDefs.tsx</files>
  <read_first>
    next/src/app/globals.css
    next/src/styles/liquid-glass.css
  </read_first>
  <action>
1. Create `next/src/lib/navigation.ts` -- the single source of truth for nav links, phone, email shared across Header, MobileMenu, Footer, StickyBar:

```typescript
export const NAV_LINKS = [
  { href: '/consultations', label: 'Консультации' },
  { href: '/treatment-abroad', label: 'Лечение' },
  { href: '/checkup', label: 'Чек-ап' },
  { href: '#contact', label: 'Контакты' },
] as const;

export const FOOTER_NAV_LINKS = [
  { href: '/consultations', label: 'Онлайн-консультации' },
  { href: '/treatment-abroad', label: 'Лечение за\u00A0рубежом' },
  { href: '/checkup', label: 'Чек-ап за\u00A0рубежом' },
] as const;

export const PHONE_NUMBER = '+77015322478';
export const PHONE_DISPLAY = '+7\u00A0701\u00A0532\u00A024\u00A078';
export const EMAIL = 'kz@medicusunion.com';
export const COMPANY_NAME = 'MedicusUnion';
export const TAGLINE = 'Международный медицинский сервис. Австрия\u00A0\u00B7\u00A0Казахстан';
```

Note: \u00A0 = nbsp for subject+verb binding per project convention. \u00B7 = middle dot.

2. Create `next/src/components/layout/SvgRefractionDefs.tsx` -- Server Component (NO "use client") rendering SVG filter definitions. Values extracted from the deleted partials/svg-defs.html (verified via git show e13da0a^:partials/svg-defs.html):

```tsx
export function SvgRefractionDefs() {
  return (
    <svg width="0" height="0" style={{ position: 'absolute' }} aria-hidden="true">
      <defs>
        <filter id="liquid-refract-sm" colorInterpolationFilters="sRGB">
          <feTurbulence type="fractalNoise" baseFrequency="0.02" numOctaves={1} seed={92} result="noise" />
          <feGaussianBlur in="noise" stdDeviation={1} result="blurred" />
          <feDisplacementMap in="SourceGraphic" in2="blurred" scale={0} xChannelSelector="R" yChannelSelector="G" />
        </filter>
        <filter id="liquid-refract-md" colorInterpolationFilters="sRGB">
          <feTurbulence type="fractalNoise" baseFrequency="0.012" numOctaves={2} seed={92} result="noise" />
          <feGaussianBlur in="noise" stdDeviation={2} result="blurred" />
          <feDisplacementMap in="SourceGraphic" in2="blurred" scale={18} xChannelSelector="R" yChannelSelector="G" />
        </filter>
        <filter id="liquid-refract-lg" colorInterpolationFilters="sRGB">
          <feTurbulence type="fractalNoise" baseFrequency="0.006" numOctaves={3} seed={92} result="noise" />
          <feGaussianBlur in="noise" stdDeviation={3} result="blurred" />
          <feDisplacementMap in="SourceGraphic" in2="blurred" scale={12} xChannelSelector="R" yChannelSelector="G" />
        </filter>
      </defs>
    </svg>
  );
}
```

3. Create the directories if they don't exist: `next/src/components/layout/` and `next/src/lib/`.

4. Run `cd next && npm run build` to verify both files compile correctly.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `next/src/lib/navigation.ts` exists and contains `export const NAV_LINKS`
    - `next/src/lib/navigation.ts` contains `export const PHONE_DISPLAY` with `\u00A0` nbsp characters
    - `next/src/lib/navigation.ts` contains `export const FOOTER_NAV_LINKS` with `\u00A0` nbsp in labels
    - `next/src/components/layout/SvgRefractionDefs.tsx` exists and does NOT contain `"use client"`
    - `next/src/components/layout/SvgRefractionDefs.tsx` contains `id="liquid-refract-sm"`, `id="liquid-refract-md"`, `id="liquid-refract-lg"`
    - `next/src/components/layout/SvgRefractionDefs.tsx` contains `export function SvgRefractionDefs`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Navigation config (NAV_LINKS, FOOTER_NAV_LINKS, PHONE_NUMBER, PHONE_DISPLAY, EMAIL, COMPANY_NAME, TAGLINE) available as shared imports. SvgRefractionDefs Server Component renders 3-tier SVG refraction filters (sm: 0.02 freq/scale 0, md: 0.012 freq/scale 18, lg: 0.006 freq/scale 12). Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. All components are static/presentational. No user input, no data processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | T (Tampering) | globals.css | accept | shadcn init may modify globals.css; mitigated by git backup + manual verification of token preservation |
</threat_model>

<verification>
1. `cd next && npm run build` passes with exit code 0
2. `ls next/src/components/ui/{button,card,input,select,textarea,dialog}.tsx` shows all 6 files
3. `grep "cn" next/src/lib/utils.ts` shows cn function export
4. `grep "liquid-refract-sm" next/src/components/layout/SvgRefractionDefs.tsx` confirms filter IDs
5. `grep "NAV_LINKS" next/src/lib/navigation.ts` confirms shared config
6. `grep "\-\-mu-blue" next/src/app/globals.css` confirms MedicusUnion tokens survived shadcn init
</verification>

<success_criteria>
- shadcn/ui initialized with new-york style, 6 base components importable
- cn() utility working at @/lib/utils
- SvgRefractionDefs renders 3 SVG refraction filters as Server Component
- NAV_LINKS, FOOTER_NAV_LINKS, PHONE_NUMBER, PHONE_DISPLAY, EMAIL shared from @/lib/navigation
- All existing CSS tokens and glass imports preserved
- Build green
</success_criteria>

<output>
After completion, create `.planning/phases/60-component-library-layout-shell/60-01-SUMMARY.md`
</output>
