---
phase: 29-404-page-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 404.html
autonomous: true
requirements:
  - 404-01
  - 404-02

must_haves:
  truths:
    - "User navigating to a non-existent URL sees a branded 404 page with gradient 404 heading, 'Stranitsa ne naidena' message, and working 'Na glavnuyu' link"
    - "The 404 page has the same header, footer, mesh background, mobile menu, sticky bar, and shared scripts as all other pages"
  artifacts:
    - path: "404.html"
      provides: "Branded 404 error page matching site design"
      contains: "404"
  key_links:
    - from: "404.html"
      to: "index.html"
      via: "anchor href"
      pattern: 'href="index.html"'
    - from: "404.html"
      to: "js/main.js"
      via: "script tag"
      pattern: 'src="js/main.js"'
---

<objective>
Create a branded 404.html error page from the Redesign/src/app/pages/NotFoundPage.tsx design, using the same shared elements (header, footer, mesh background, mobile menu, sticky bar, scripts) as the rest of the site.

Purpose: Users who hit a broken URL see a professional, on-brand page that guides them back instead of a generic browser error.
Output: 404.html in project root
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Redesign/src/app/pages/NotFoundPage.tsx
@index.html (lines 1-105 for head/header/mesh/mobile-menu structure)
@index.html (lines 980-1074 for footer/sticky-bar/scripts structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 404.html with shared site shell and 404 content</name>
  <files>404.html</files>
  <read_first>
    - Redesign/src/app/pages/NotFoundPage.tsx (404 design source)
    - index.html lines 1-105 (head, header, mesh bg, mobile menu)
    - index.html lines 980-1074 (footer, sticky bar, scripts)
  </read_first>
  <action>
Create 404.html in the project root. The page structure is:

1. **Head section** -- Copy from index.html. Change title to "Страница не найдена -- MedicusUnion". Change meta description to "Запрашиваемая страница не найдена. Вернитесь на главную MedicusUnion." Remove OG tags and canonical (404 pages should not be indexed). Keep the inline style block with header scroll, mobile menu, form validation, FAQ, visually-hidden styles (same as index.html lines 17-43).

2. **Body + mesh background** -- Copy the mesh background div (lines 47-52 of index.html) exactly as-is.

3. **Header** -- Copy the full header block from index.html (lines 55-83), including `class="header fixed z-50..."` with the `header` class present. Copy the mobile menu overlay (lines 86-103) exactly as-is.

4. **Main content** -- Translate NotFoundPage.tsx to static HTML with Tailwind:
   ```html
   <main class="relative z-10 pt-32 pb-16 min-h-[80vh] flex items-center justify-center">
     <div class="text-center max-w-lg mx-auto px-4">
       <div class="text-8xl font-extrabold bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent mb-6">
         404
       </div>
       <h1 class="text-3xl font-extrabold text-mu-text-900 mb-4">Страница не найдена</h1>
       <p class="text-mu-text-700 font-medium mb-8">
         Возможно, страница была перемещена или удалена.
       </p>
       <a href="index.html" class="bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-8 py-4 rounded-3xl font-bold shadow-lg shadow-mu-blue/30 inline-flex items-center gap-2 mx-auto hover:shadow-xl hover:shadow-mu-blue/40 transition-shadow">
         <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 19-7-7 7-7"/><path d="M19 12H5"/></svg>
         На главную
       </a>
     </div>
   </main>
   ```
   Note: Use an `<a>` tag (not button) since it navigates to index.html. The arrow icon is ArrowLeft from lucide (path d="m12 19-7-7 7-7" + path d="M19 12H5"). No Motion animation on the button -- use pure CSS hover transition (per static HTML pattern).

5. **Footer** -- Copy the full footer block from index.html (lines 980-1060). Use `class="footer relative overflow-hidden z-10 py-16"` (with the `footer` class for consistency with other pages).

6. **Sticky bar** -- Copy from index.html (lines 1063-1068). The CTA should link to `index.html#contact` instead of `#contact` since 404 page has no contact form.

7. **Scripts** -- Include all 3 scripts:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/motion@12.23.24/dist/motion.js"></script>
   <script src="js/main.js"></script>
   <script src="js/animations.js"></script>
   ```
  </action>
  <verify>
    <automated>grep -c "404" 404.html && grep -q 'Страница не найдена' 404.html && grep -q 'href="index.html"' 404.html && grep -q 'id="header"' 404.html && grep -q 'id="footer"' 404.html && grep -q 'id="sticky-bar"' 404.html && grep -q 'js/main.js' 404.html && grep -q 'js/animations.js' 404.html && grep -q 'motion@12' 404.html && grep -q 'mobile-menu-overlay' 404.html && grep -q 'mesh-bg__blob' 404.html && echo "PASS"</automated>
  </verify>
  <done>
    - 404.html exists in project root
    - Contains gradient "404" heading with from-mu-blue via-mu-accent-blue to-mu-green-600
    - Contains h1 "Страница не найдена"
    - Contains "На главную" link pointing to index.html
    - Has identical header (with `header` class), mobile menu, mesh background, footer, sticky bar, and scripts to index.html
    - Sticky bar CTA links to index.html#contact
  </done>
</task>

</tasks>

<verification>
- File 404.html exists in project root
- Contains the same shared elements as other pages (header, footer, mesh, mobile menu, sticky bar, 3 script tags)
- The 404 content matches the NotFoundPage.tsx design (gradient 404, heading, subtitle, button)
- "На главную" link navigates to index.html
</verification>

<success_criteria>
A user opening 404.html in a browser sees a branded error page with the same header/footer/mesh as the rest of the site, a large gradient "404", and a clear "На главную" button leading to index.html.
</success_criteria>

<output>
After completion, create `.planning/phases/29-404-page-ui-polish/29-01-SUMMARY.md`
</output>
