---
phase: 08-directus-backend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - BACK-03
  - BACK-04
must_haves:
  truths:
    - "docker-compose.yml defines Directus 11 and PostgreSQL 16 services"
    - ".env.example documents all required environment variables"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Directus + PostgreSQL stack"
      contains: "directus"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "DIRECTUS"
---

<objective>
Create Docker Compose configuration for Directus 11 with PostgreSQL 16, including collection schema bootstrap, public create-only permissions, and CORS configuration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose and environment setup</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
Create docker-compose.yml and .env.example at project root.
  </action>
</task>

</tasks>
