---
phase: 45-simple-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contacts.html
autonomous: true
requirements:
  - MIGRATE-02

must_haves:
  truths:
    - "contacts.html hero section uses max-w-[1200px] grid wrapper"
    - "contacts.html contact section uses max-w-[1200px] grid wrapper with 12-col grid"
    - "Coordinator card uses liquid-card-wrap + liquid-card squircle-xl"
    - "4 contact method cards each use liquid-card-wrap + liquid-card squircle-lg"
    - "4 icon boxes use liquid-regular squircle-md"
    - "Form container uses liquid-card-wrap + liquid-card squircle-xl"
    - "Form inputs use squircle-md instead of rounded-2xl"
    - "Submit button uses liquid-btn-primary squircle-md"
    - "Trust badges use liquid-regular squircle-full"
    - "Hero badge uses liquid-regular squircle-full"
    - "All 18 nbsp entities preserved"
    - "Honeypot div untouched"
    - "4 role=alert aria-live=polite containers untouched"
    - "Form submission still works (novalidate, autocomplete, inputmode preserved)"
    - "Chrome BUILD blocks untouched"
    - "make build exits 0"
  artifacts:
    - path: "contacts.html"
      provides: "v4.0 migrated contacts page with grid + squircle + liquid glass"
      contains: "liquid-card"
  key_links:
    - from: "contacts.html main content"
      to: "css/styles.css"
      via: "liquid-card, liquid-card-wrap, squircle-xl/lg/md, liquid-btn-primary, liquid-regular classes"
      pattern: "liquid-card.*squircle"
    - from: "contacts.html form"
      to: "js/main.js"
      via: "contact-form class, form validation, Directus POST"
      pattern: "contact-form"
---

<objective>
Migrate contacts.html main content area to v4.0 Liquid Design System: grid wrappers with max-w-[1200px], liquid glass surfaces on coordinator card / contact cards / form container, squircle shapes on all cards / inputs / buttons, and design system badge treatment. Preserve all Protected Legacy items (18 nbsp entities, honeypot, ARIA, form validation infrastructure).

Purpose: Second canary page -- validates the v4.0 migration pattern on a form-heavy page with stacking context complexity (glass form container containing backdrop-blur inputs, success overlay above glass).
Output: contacts.html with v4.0 design system classes applied to main content; all form functionality preserved; build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-simple-pages/45-RESEARCH.md

@contacts.html
@src/styles/liquid-glass.css
@src/styles/squircles.css

<interfaces>
<!-- Available design system classes from Phase 42-43 -->

From src/styles/liquid-glass.css:
- .liquid-card — glass card surface (backdrop-filter + tint + rim lighting + padding 1.5rem)
- .liquid-card-wrap — outer shadow wrapper (box-shadow + border-radius: 24px)
- .liquid-btn-primary — gradient CTA (specular edge, hover brightness, active scale)
- .liquid-regular — base glass material (no padding)

From src/styles/squircles.css:
- .squircle-md — 16px radius + mask-image SVG
- .squircle-lg — 24px radius + mask-image SVG
- .squircle-xl — 40px radius + mask-image SVG
- .squircle-full — 9999px radius, no mask (circle/pill)

Shadow-wrap pattern:
  <div class="liquid-card-wrap">
    <div class="liquid-card squircle-xl">content</div>
  </div>

CRITICAL: Use max-w-[1200px] NOT max-w-content (token namespace bug)
CRITICAL: Form inputs keep their bg-white/50 backdrop-blur-md -- do NOT add .liquid-regular or .liquid-card to inputs. Only replace rounded-2xl with squircle-md.
CRITICAL: Honeypot div (visually-hidden) and role="alert" aria-live="polite" spans must not be modified.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate contacts.html main content to v4.0 design system</name>
  <files>contacts.html</files>
  <read_first>
    - contacts.html (full file -- especially lines 130-290 main content area)
    - src/styles/liquid-glass.css (class inventory and anti-patterns)
    - src/styles/squircles.css (class inventory and shadow-wrap pattern)
    - .planning/phases/45-simple-pages/45-RESEARCH.md (migration map table)
  </read_first>
  <action>
Edit ONLY the main content area of contacts.html (lines 130-290, between /BUILD:mobile-menu and BUILD:footer markers). DO NOT touch anything between BUILD markers. DO NOT touch the head, style block, body tag, mesh background, or script tags.

**Step 1: Record baseline counts for Protected Legacy gate.**
Run:
```
grep -c '&amp;nbsp;' contacts.html        # Expected: 18
grep -c 'aria-live' contacts.html     # Expected: 4
grep -c 'visually-hidden' contacts.html  # Expected: 2 (CSS rule + div)
grep -c 'role="alert"' contacts.html  # Expected: 4
```
Record all counts.

**Step 2: Migrate Hero Section (lines 131-145).**

Current: `<section class="container mx-auto px-4 lg:px-6 mb-16">`
New: `<section class="max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8 mb-16">`

Replace `container mx-auto px-4 lg:px-6` with `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8`.

Glass badge (line 134): Replace `bg-white/40 backdrop-blur-xl border border-white/60 px-5 py-2.5 rounded-full shadow-sm shadow-glass-inner` with `liquid-regular squircle-full px-5 py-2.5`. Keep the `glass-badge inline-flex items-center gap-2` and `mb-6` classes. Keep the inner span and SVG unchanged.

**Step 3: Migrate Contact Section container (line 148).**

Current: `<section class="container mx-auto px-4 lg:px-6" id="contact-section">`
New: `<section class="max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8" id="contact-section">`

**Step 4: Migrate 2-column grid to 12-col responsive grid (line 149).**

Current: `<div class="grid lg:grid-cols-2 gap-12 items-start">`
New: `<div class="grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-8 lg:gap-12 items-start">`

Add column spans to the two children:
- Left info column (line 152): Add `md:col-span-4 lg:col-span-5` to the existing classes.
- Right form wrapper (line 223): Add `md:col-span-4 lg:col-span-7` to the existing classes.

**Step 5: Migrate Coordinator Card (line 155).**

Current:
```html
<div class="coordinator-card bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-8 shadow-glass border border-white/60 shadow-glass-inner">
```

New -- shadow-wrap pattern:
```html
<div class="liquid-card-wrap">
  <div class="coordinator-card liquid-card squircle-xl p-8">
```

Remove: `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] shadow-glass border border-white/60 shadow-glass-inner`. These are replaced by `liquid-card squircle-xl`.
The liquid-card class provides its own padding (1.5rem) but we override with p-8 explicitly.
Add closing `</div>` for the liquid-card-wrap after the coordinator card's closing div.

Avatar circle (line 157): Replace `rounded-full overflow-hidden shadow-glass-sm flex-shrink-0 border-4 border-white/60` with `squircle-full overflow-hidden flex-shrink-0`. Keep `w-28 h-28`.

**Step 6: Migrate 4 Contact Method Cards (lines 169-198).**

Each of the 4 cards follows this pattern. Current:
```html
<div class="bg-white/60 backdrop-blur-2xl rounded-[2rem] p-6 border border-white/60 shadow-glass">
```

New:
```html
<div class="liquid-card-wrap">
  <div class="liquid-card squircle-lg p-6">
```

Add closing `</div>` for each liquid-card-wrap after each card's closing div.

Icon boxes inside each card. Current:
```html
<div class="w-10 h-10 bg-white/50 backdrop-blur-md rounded-xl flex items-center justify-center text-mu-blue border border-white/60 mb-3">
```

New:
```html
<div class="w-10 h-10 liquid-regular squircle-md flex items-center justify-center text-mu-blue mb-3">
```

Remove: `bg-white/50 backdrop-blur-md rounded-xl border border-white/60`. Replace with `liquid-regular squircle-md`.

**Step 7: Migrate Trust Badges (lines 201-218).**

Each of the 4 trust badges. Current:
```html
<div class="inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-white/60 text-mu-text-900 px-5 py-2.5 rounded-full text-sm font-bold shadow-sm shadow-glass-inner">
```

New:
```html
<div class="inline-flex items-center gap-2 liquid-regular squircle-full text-mu-text-900 px-5 py-2.5 text-sm font-bold">
```

Remove: `bg-white/40 backdrop-blur-xl border border-white/60 rounded-full shadow-sm shadow-glass-inner`. Replace with `liquid-regular squircle-full`.

**Step 8: Migrate Form Container (line 224).**

Current:
```html
<div class="bg-white/70 backdrop-blur-3xl rounded-[3rem] p-8 shadow-glass-lg border border-white/60 relative overflow-hidden shadow-glass-inner">
```

New -- shadow-wrap pattern:
```html
<div class="liquid-card-wrap">
  <div class="liquid-card squircle-xl p-8 relative overflow-hidden">
```

Remove: `bg-white/70 backdrop-blur-3xl rounded-[3rem] shadow-glass-lg border border-white/60 shadow-glass-inner`. Replace with `liquid-card squircle-xl`.
Keep: `relative overflow-hidden` (needed for success overlay positioning).
Add closing `</div>` for the liquid-card-wrap. The liquid-card-wrap goes inside the `.form-wrapper` div (line 223), wrapping the form container.

NOTE on overflow-hidden + squircle: The squircle-xl applies mask-image which already clips content. The `overflow-hidden` is redundant for visual clipping but needed for the success overlay's absolute positioning. Keeping it is safe.

**Step 9: Migrate Form Inputs (lines 240, 246, 252, 264).**

For each `<input>`, `<select>`, and `<textarea>`: Replace `rounded-2xl` with `squircle-md`.

DO NOT change anything else on these elements. Keep: `border border-white/40 bg-white/50 backdrop-blur-md focus:bg-white/70 focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-all ... font-medium text-mu-text-900 shadow-form-inset`.

Specifically:
- Line 240: `<input ... class="form__input w-full px-5 py-4 squircle-md border border-white/40 ...">`
- Line 246: `<input ... class="form__input w-full px-5 py-4 squircle-md border border-white/40 ...">`
- Line 252: `<select ... class="form__select w-full px-5 py-4 squircle-md border border-white/40 ...">`
- Line 264: `<textarea ... class="form__textarea w-full px-5 py-4 squircle-md border border-white/40 ...">`

**Step 10: Migrate Submit Button (line 273).**

Current:
```html
<button class="form__submit w-full bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white py-4 rounded-2xl font-bold shadow-lg shadow-mu-blue/30 hover:shadow-xl hover:shadow-mu-blue/40 transition-all flex items-center justify-center gap-2 text-lg mt-4" type="submit">
```

New:
```html
<button class="form__submit w-full liquid-btn-primary squircle-md py-4 font-bold flex items-center justify-center gap-2 text-lg mt-4" type="submit">
```

Remove: `bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white rounded-2xl shadow-lg shadow-mu-blue/30 hover:shadow-xl hover:shadow-mu-blue/40 transition-all`. Replace with `liquid-btn-primary squircle-md` (provides gradient, color, shadow, hover, press).

**Step 11: DO NOT TOUCH these elements:**
- Honeypot div (lines 267-271): `<div class="visually-hidden" aria-hidden="true">` -- LEAVE EXACTLY AS-IS
- Form error containers: All `<span class="form__field-error ..." role="alert" aria-live="polite">` -- LEAVE EXACTLY AS-IS
- Form-level error: `<div class="form__error" role="alert" aria-live="polite">` -- LEAVE EXACTLY AS-IS
- Success overlay: `<div class="form__success" id="form-success" hidden>` -- LEAVE EXACTLY AS-IS (content inside unchanged)
- All text content with &amp;nbsp; entities -- LEAVE EXACTLY AS-IS
- Privacy text paragraph -- LEAVE EXACTLY AS-IS
- Form element attributes: `novalidate`, `required`, `autocomplete`, `inputmode` -- PRESERVE ALL

**Step 12: Run build and verify.**
Run `make build` -- must exit 0.

**Step 13: Verify Protected Legacy gate.**
Run all baseline count checks and verify they match:
```
grep -c '&amp;nbsp;' contacts.html        # Must equal 18
grep -c 'aria-live' contacts.html     # Must equal 4
grep -c 'visually-hidden' contacts.html  # Must equal 2
grep -c 'role="alert"' contacts.html  # Must equal 4
```

Also verify migration applied:
```
grep -c 'squircle-' contacts.html     # Must be >= 15 (cards + inputs + badges + chrome)
grep -c 'liquid-card' contacts.html   # Must be >= 7 (coordinator + 4 method cards + form + footer)
grep -c 'liquid-btn-primary' contacts.html  # Must be >= 1
grep -c 'max-w-\[1200px\]' contacts.html  # Must be >= 2 (hero + contact section)
grep -c 'liquid-regular' contacts.html  # Must be >= 5 (badge + 4 icon boxes + trust badges + chrome)
```
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "BUILD_OK" && test "$(grep -c '&amp;nbsp;' contacts.html)" -eq 18 && echo "NBSP_OK" && test "$(grep -c 'aria-live' contacts.html)" -eq 4 && echo "ARIA_OK" && test "$(grep -c 'visually-hidden' contacts.html)" -eq 2 && echo "HONEYPOT_OK" && test "$(grep -c 'liquid-card' contacts.html)" -ge 7 && echo "LIQUID_OK" && test "$(grep -c 'squircle-' contacts.html)" -ge 15 && echo "SQUIRCLE_OK" && test "$(grep -c 'liquid-btn-primary' contacts.html)" -ge 1 && echo "BTN_OK" && test "$(grep -c 'max-w-\[1200px\]' contacts.html)" -ge 2 && echo "GRID_OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '&amp;nbsp;' contacts.html equals 18 (Protected Legacy preserved)
    - grep -c 'aria-live' contacts.html equals 4 (ARIA preserved)
    - grep -c 'visually-hidden' contacts.html equals 2 (honeypot preserved)
    - grep -c 'role="alert"' contacts.html equals 4 (form errors preserved)
    - grep 'liquid-card squircle-xl' contacts.html returns matches (coordinator card + form container)
    - grep 'liquid-card squircle-lg' contacts.html returns matches (4 contact method cards)
    - grep 'liquid-card-wrap' contacts.html returns matches (6 wraps: coordinator + 4 cards + form)
    - grep 'liquid-regular squircle-md' contacts.html returns matches (4 icon boxes)
    - grep 'liquid-regular squircle-full' contacts.html returns matches (badge + trust badges)
    - grep 'liquid-btn-primary squircle-md' contacts.html returns match (submit button)
    - grep 'max-w-\[1200px\]' contacts.html returns >= 2 matches (hero + contact section)
    - grep 'grid-cols-12' contacts.html returns match (12-col grid on contact section)
    - grep 'rounded-\[2rem\]\|rounded-\[2.5rem\]\|rounded-\[3rem\]' contacts.html returns NO matches in main content (old radius classes removed)
    - grep 'rounded-2xl' contacts.html returns NO matches in main content (old input radius removed; may exist in chrome)
    - make build exits 0
  </acceptance_criteria>
  <done>contacts.html main content uses v4.0 design system: grid wrappers (max-w-[1200px] + 12-col grid), liquid glass surfaces (liquid-card on coordinator/method cards/form, liquid-regular on icon boxes/badges), squircle shapes (squircle-xl on large cards, squircle-lg on method cards, squircle-md on inputs/buttons/icons, squircle-full on badges). All Protected Legacy items preserved: 18 nbsp entities, honeypot, 4 ARIA containers, form validation attributes. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input -> Directus API | User input submitted to backend (UNCHANGED by this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Tampering | contacts.html honeypot | mitigate | Honeypot div explicitly preserved -- do-not-touch rule in task action, verified by grep count gate |
| T-45-03 | Information Disclosure | Form error messages | accept | ARIA containers preserved; no new info exposure from CSS class changes |
| T-45-04 | Tampering | Form validation | mitigate | All form attributes (novalidate, required, autocomplete, inputmode) explicitly preserved; no form logic changes |
</threat_model>

<verification>
1. `make build` exits 0 (CSS compiles, chrome spliced)
2. `grep -c '&amp;nbsp;' contacts.html` equals 18 (Protected Legacy: nbsp)
3. `grep -c 'aria-live' contacts.html` equals 4 (Protected Legacy: ARIA)
4. `grep -c 'visually-hidden' contacts.html` equals 2 (Protected Legacy: honeypot)
5. `grep -c 'role="alert"' contacts.html` equals 4 (Protected Legacy: form errors)
6. `grep 'liquid-card-wrap' contacts.html` finds 6+ matches (shadow-wrap on all cards + form)
7. `grep 'liquid-btn-primary' contacts.html` finds 1+ match (submit button)
8. `grep 'max-w-\[1200px\]' contacts.html` finds 2+ matches (grid wrappers)
9. `grep 'grid-cols-12' contacts.html` finds 1+ match (12-col responsive grid)
10. No `rounded-[2rem]`, `rounded-[2.5rem]`, `rounded-[3rem]` in main content (old classes removed)
</verification>

<success_criteria>
contacts.html renders with v4.0 Liquid Design System: glass surfaces on coordinator card, contact method cards, and form container; squircle shapes on all cards, inputs, and buttons; 12-col responsive grid layout; design system badges. Form submission to Directus still works. All Protected Legacy items (18 nbsp, honeypot, 4 ARIA containers, form validation) preserved. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/45-simple-pages/45-02-SUMMARY.md`
</output>
