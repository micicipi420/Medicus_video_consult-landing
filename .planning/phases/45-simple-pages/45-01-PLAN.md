---
phase: 45-simple-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 404.html
autonomous: true
requirements:
  - MIGRATE-01

must_haves:
  truths:
    - "404.html main content area uses max-w-[1200px] grid wrapper"
    - "404 message block is wrapped in liquid-card squircle-xl surface"
    - "CTA button uses liquid-btn-primary squircle-md instead of rounded-3xl"
    - "Gradient 404 text, heading, description text unchanged"
    - "All nbsp entities preserved (11 total across page)"
    - "Chrome BUILD blocks untouched"
    - "make build exits 0"
  artifacts:
    - path: "404.html"
      provides: "v4.0 migrated 404 page with grid + squircle + liquid glass"
      contains: "liquid-card"
  key_links:
    - from: "404.html main content"
      to: "css/styles.css"
      via: "liquid-card, squircle-xl, squircle-md, liquid-btn-primary classes"
      pattern: "liquid-card.*squircle"
---

<objective>
Migrate 404.html main content area to v4.0 Liquid Design System visual language: add grid wrapper with max-w-[1200px], wrap the 404 message block in a liquid-card squircle-xl surface, and replace the CTA button's rounded-3xl with liquid-btn-primary squircle-md.

Purpose: First canary page for the v4.0 migration pattern -- validates that grid wrapper, liquid card surface, and squircle CTA render correctly without breaking protected legacy items.
Output: 404.html with v4.0 design system classes applied to main content; build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-simple-pages/45-RESEARCH.md

@404.html
@src/styles/liquid-glass.css
@src/styles/squircles.css

<interfaces>
<!-- Available design system classes from Phase 42-43 -->

From src/styles/liquid-glass.css:
- .liquid-card — glass card surface (backdrop-filter + tint + rim lighting + padding 1.5rem)
- .liquid-card-wrap — outer shadow wrapper (box-shadow + border-radius: 24px)
- .liquid-btn-primary — gradient CTA (specular edge, hover brightness, active scale)
- .liquid-regular — base glass material (no padding)

From src/styles/squircles.css:
- .squircle-md — 16px radius + mask-image SVG
- .squircle-lg — 24px radius + mask-image SVG
- .squircle-xl — 40px radius + mask-image SVG
- .squircle-full — 9999px radius, no mask (circle/pill)

Shadow-wrap pattern:
  <div class="liquid-card-wrap">
    <div class="liquid-card squircle-xl">content</div>
  </div>

CRITICAL: Use max-w-[1200px] NOT max-w-content (token namespace bug, see 41-REVIEW.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 404.html main content to v4.0 design system</name>
  <files>404.html</files>
  <read_first>
    - 404.html (lines 123-138 -- the main content between chrome blocks)
    - src/styles/liquid-glass.css (class inventory)
    - src/styles/squircles.css (class inventory)
  </read_first>
  <action>
Edit ONLY the main content area of 404.html (lines 123-138, between the /BUILD:mobile-menu and BUILD:footer markers). DO NOT touch anything between BUILD markers (chrome blocks). DO NOT touch the head, style block, body tag, mesh background, or script tags.

**Step 1: Record baseline counts for Protected Legacy gate.**
Run:
```
grep -c '&amp;nbsp;' 404.html
```
Expected: 11. Record this number.

**Step 2: Replace the main content block (lines 123-138) with this migrated version:**

The current structure:
```html
<main id="page-content" class="relative z-10 pt-section-pt lg:pt-section-pt-lg pb-section-pb min-h-section-hero-compact flex items-center justify-center">
  <div class="text-center max-w-lg mx-auto px-4">
    <div class="text-8xl font-extrabold bg-gradient-to-r ...">404</div>
    <h1 ...>Страница&nbsp;не&nbsp;найдена</h1>
    <p ...>...</p>
    <a href="index.html" class="bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white px-8 py-4 rounded-3xl font-bold shadow-lg shadow-mu-blue/30 inline-flex items-center gap-2 mx-auto hover:shadow-xl hover:shadow-mu-blue/40 transition-shadow">
  </div>
</main>
```

Becomes:
```html
<main id="page-content" class="relative z-10 pt-section-pt lg:pt-section-pt-lg pb-section-pb min-h-section-hero-compact flex items-center justify-center">
  <div class="max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8">
    <div class="liquid-card-wrap max-w-lg mx-auto">
      <div class="liquid-card squircle-xl text-center p-8 md:p-12">
        ...gradient 404 text div UNCHANGED...
        ...h1 with &nbsp; UNCHANGED...
        ...p with &nbsp; UNCHANGED...
        <a href="index.html" class="liquid-btn-primary squircle-md px-8 py-4 font-bold inline-flex items-center gap-2 mx-auto">
          ...svg icon UNCHANGED...
          На главную
        </a>
      </div>
    </div>
  </div>
</main>
```

Specific changes:
1. **Grid wrapper**: Replace `<div class="text-center max-w-lg mx-auto px-4">` with `<div class="max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8">` as the outermost content wrapper. This satisfies GRID-01 grid wrapper requirement.
2. **Shadow-wrap + liquid card**: Add `<div class="liquid-card-wrap max-w-lg mx-auto">` as the shadow wrapper (preserves the existing max-w-lg centering). Inside it, add `<div class="liquid-card squircle-xl text-center p-8 md:p-12">` for the glass card surface.
3. **CTA button**: Replace the `<a>` tag's class from `bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white px-8 py-4 rounded-3xl font-bold shadow-lg shadow-mu-blue/30 inline-flex items-center gap-2 mx-auto hover:shadow-xl hover:shadow-mu-blue/40 transition-shadow` with `liquid-btn-primary squircle-md px-8 py-4 font-bold inline-flex items-center gap-2 mx-auto`. The liquid-btn-primary class already provides gradient, shadow, hover, and press states.
4. **Preserve exactly**: The gradient "404" div, h1 with `&nbsp;`, p with `&nbsp;`, SVG icon inside the CTA link, and the text content "На главную".

**Step 3: Run build and verify.**
Run `make build` -- must exit 0.

**Step 4: Verify Protected Legacy gate.**
Run `grep -c '&amp;nbsp;' 404.html` -- must equal the baseline from Step 1 (11).
Run `grep -c 'squircle-' 404.html` -- must be >= 2 (squircle-xl on card, squircle-md on CTA, plus chrome squircles).
Run `grep -c 'liquid-card' 404.html` -- must be >= 2 (liquid-card-wrap + liquid-card on the main content, plus footer liquid-card from chrome).
Run `grep -c 'liquid-btn-primary' 404.html` -- must be >= 1.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "BUILD_OK" && test "$(grep -c '&amp;nbsp;' 404.html)" -eq 11 && echo "NBSP_OK" && test "$(grep -c 'liquid-card' 404.html)" -ge 2 && echo "LIQUID_OK" && test "$(grep -c 'squircle-' 404.html)" -ge 2 && echo "SQUIRCLE_OK" && test "$(grep -c 'liquid-btn-primary' 404.html)" -ge 1 && echo "BTN_OK" && test "$(grep -c 'max-w-\[1200px\]' 404.html)" -ge 1 && echo "GRID_OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'liquid-card squircle-xl' 404.html returns match in main content area
    - grep 'liquid-card-wrap' 404.html returns match in main content area
    - grep 'liquid-btn-primary squircle-md' 404.html returns match on CTA link
    - grep 'max-w-\[1200px\]' 404.html returns match on grid wrapper
    - grep -c '&amp;nbsp;' 404.html equals 11 (unchanged from baseline)
    - grep 'rounded-3xl' 404.html returns NO match in main content (old class removed)
    - grep 'shadow-glass' 404.html returns NO match in main content (old class removed -- shadow-glass may exist in chrome blocks which is fine)
    - make build exits 0
  </acceptance_criteria>
  <done>404.html main content uses v4.0 grid wrapper (max-w-[1200px]), liquid card surface (liquid-card-wrap + liquid-card squircle-xl), and squircle CTA (liquid-btn-primary squircle-md). All nbsp entities preserved. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed -- this is a purely visual CSS class migration on a static HTML page.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | 404.html content | accept | Static HTML, no user input processed; visual-only changes |
</threat_model>

<verification>
1. `make build` exits 0 (CSS compiles, chrome spliced)
2. `grep -c '&amp;nbsp;' 404.html` equals 11 (Protected Legacy preserved)
3. `grep 'liquid-card squircle-xl' 404.html` finds match (card surface applied)
4. `grep 'liquid-btn-primary squircle-md' 404.html` finds match (CTA upgraded)
5. `grep 'max-w-\[1200px\]' 404.html` finds match (grid wrapper applied)
6. No `rounded-3xl` in main content area (old styling removed from CTA)
</verification>

<success_criteria>
404.html renders with v4.0 Liquid Design System: glass card surface behind the 404 message, squircle-shaped CTA button with specular edge treatment, grid-aligned content wrapper. Protected Legacy items (nbsp entities, chrome blocks, metadata) are untouched. Build pipeline passes.
</success_criteria>

<output>
After completion, create `.planning/phases/45-simple-pages/45-01-SUMMARY.md`
</output>
