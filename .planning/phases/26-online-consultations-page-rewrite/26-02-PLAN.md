---
phase: 26-online-consultations-page-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - online-consultations.html
autonomous: false
requirements:
  - CONSULT-08
  - CONSULT-09
  - CONSULT-10
  - CONSULT-11
  - CROSS-02
  - CROSS-03

must_haves:
  truths:
    - "User sees pricing section with 'от 450€' and 5 included items"
    - "User can fill out consultation form (name, phone, specialization dropdown, description) and submit to Directus"
    - "User can expand/collapse all 6 FAQ items with smooth accordion animation"
    - "User sees final CTA with 'Не откладывайте решение' and two buttons"
    - "Form submission works identically to the main page form (validation, honeypot, Directus POST)"
    - "FAQ answers match copywriting document text exactly"
  artifacts:
    - path: "online-consultations.html"
      provides: "Complete page with all 11 sections, form, FAQ"
      contains: "consultation-form"
  key_links:
    - from: "online-consultations.html form"
      to: "js/main.js initFormValidation()"
      via: "class contact-form triggers JS form handler"
      pattern: "contact-form"
    - from: "online-consultations.html FAQ"
      to: "js/main.js initAccordion()"
      via: "class faq__question triggers JS accordion"
      pattern: "faq__question"
    - from: "js/main.js"
      to: "Directus API"
      via: "fetch POST to API_URL"
      pattern: "consultation_requests"
---

<objective>
Add the bottom half of online-consultations.html (Sections 8-11): Pricing, Consultation Form, FAQ accordion, and Final CTA.

Purpose: Complete the page with the conversion-focused sections. The form MUST use the same HTML structure as contacts.html/index.html so that js/main.js handles validation/submission automatically. The FAQ MUST use the same .faq__item/.faq__question structure so the accordion JS works automatically. All text from copywriting document.

Output: Fully complete online-consultations.html with all 11 sections, working form, working FAQ accordion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-online-consultations-page-rewrite/26-CONTEXT.md
@.planning/phases/26-online-consultations-page-rewrite/copywriting-source.txt
@.planning/phases/26-online-consultations-page-rewrite/26-01-SUMMARY.md
@online-consultations.html
@index.html
@contacts.html
</context>

<interfaces>
<!-- Form HTML structure from index.html/contacts.html (js/main.js expects this EXACT structure): -->

```html
<form class="contact-form space-y-6" novalidate>
  <div class="form__group">
    <label class="form__label ..." for="contact-name">Ваше имя</label>
    <input type="text" id="contact-name" name="name" class="form__input ..." required>
    <span class="form__field-error" hidden></span>
  </div>
  <div class="form__group">
    <label class="form__label ..." for="contact-phone">Телефон</label>
    <input type="tel" id="contact-phone" name="phone" class="form__input ..." required inputmode="numeric">
    <span class="form__field-error" hidden></span>
  </div>
  <div class="form__group">
    <label ... for="contact-interest">...</label>
    <select id="contact-interest" name="interest" class="form__select ..." required>...</select>
    <span class="form__field-error" hidden></span>
  </div>
  <div class="form__group">
    <label ... for="contact-description">...</label>
    <textarea id="contact-description" name="description" class="form__textarea ..."></textarea>
  </div>
  <!-- Honeypot -->
  <div class="visually-hidden" aria-hidden="true">
    <label for="contact-website">Website</label>
    <input type="text" id="contact-website" name="website" tabindex="-1" autocomplete="off">
  </div>
  <button class="form__submit ..." type="submit">Отправить заявку</button>
  <p class="form__privacy ...">...</p>
  <div class="form__error" hidden></div>
</form>
```

<!-- FAQ accordion HTML structure from index.html (js/main.js expects this): -->

```html
<div class="faq__item bg-white/60 backdrop-blur-2xl rounded-2xl border border-glass-border shadow-glass-sm overflow-hidden">
  <button class="faq__question w-full text-left px-6 py-5 font-bold text-mu-text-900 flex items-center justify-between transition-colors hover:bg-white/80" type="button" aria-expanded="false">
    Question text here
    <svg class="faq__question-icon" ...><path d="m6 9 6 6 6-6"/></svg>
  </button>
  <div class="faq__answer px-6 pb-5 text-mu-text-700 font-medium leading-relaxed">
    <div class="faq__answer-inner">Answer text here</div>
  </div>
</div>
```

<!-- CSS required in <style> for FAQ (check if already present): -->
```css
.faq__answer { max-height: 0; overflow: hidden; transition: max-height 0.3s ease; }
.faq__item.is-open .faq__answer { max-height: 500px; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Sections 8-9 (Pricing + Consultation Form with Directus integration)</name>
  <read_first>
    - .planning/phases/26-online-consultations-page-rewrite/copywriting-source.txt (THE copywriting source -- use text VERBATIM)
    - online-consultations.html (current file with sections 1-7 from plan 01)
    - contacts.html (REFERENCE for form HTML structure -- copy EXACTLY)
    - index.html (REFERENCE for form HTML structure)
  </read_first>
  <files>online-consultations.html</files>
  <action>
Insert Sections 8 and 9 into online-consultations.html, AFTER section 7 (triggers) and BEFORE `</main>`.

CRITICAL: Use VERBATIM text from copywriting-source.txt. Do NOT paraphrase.

**Ensure CSS rules exist in the `<style>` block in `<head>`:**
Add these if not already present (check first):
```css
/* FAQ accordion */
.faq__answer { max-height: 0; overflow: hidden; transition: max-height 0.3s ease; }
.faq__item.is-open .faq__answer { max-height: 500px; }
/* Form success/error messages */
.form__success, .form__error { display: none; }
.form__success.is-visible, .form__error.is-visible { display: block; }
```

**Section 8 -- PRICING (per CONSULT-08):**
- id="pricing"
- Section heading: "Прозрачная цена, никаких сюрпризов" (gradient text)
- Subtext: "Стоимость зависит от специализации врача и сложности случая. Вы узнаете точную цену до оплаты." (VERBATIM)
- Large glass container with price block:
  - "от 450€" in large bold text (text-5xl or text-6xl font-extrabold) with gradient coloring
  - "видеоконсультация" as subtitle
- "В стоимость входит:" label followed by 5 items with checkmark icons:
  1. "Перевод ваших медицинских документов" (VERBATIM)
  2. "Подготовка врача к консультации (изучение вашего кейса)" (VERBATIM)
  3. "Видеовстреча с переводчиком" (VERBATIM)
  4. "Письменное заключение врача" (VERBATIM)
  5. "Доступ к личному кабинету" (VERBATIM)
- CTA button at bottom: "Получить консультацию" linking to #consultation-form

**Section 9 -- FORM (per CONSULT-09 + CROSS-02):**
- id="consultation-form"
- Section heading: "Записаться на консультацию" (gradient text)
- Subtext: "Оставьте заявку — мы свяжемся в течение 24 часов, обсудим ваш случай и подберём врача. Бесплатно и без обязательств." (VERBATIM)
- Two-column layout (lg:grid-cols-2):
  - LEFT column: coordinator card (reuse pattern from contacts.html -- Айгерим, Старший медицинский координатор) + trust badges (На связи 24/7, ISO 27001, 10 000+ пациентов)
  - RIGHT column: the form
- Form structure MUST match index.html/contacts.html EXACTLY for js/main.js compatibility:
  - class="contact-form" (this triggers initFormValidation in main.js)
  - Success overlay div with id="form-success"
  - Fields: name (contact-name), phone (contact-phone), specialization dropdown, description textarea
  - The specialization dropdown per copywriting: "Специализация" label, options: "Выберите специализацию" (disabled default), "Онкология", "Кардиология", "Нейрохирургия", "Другое"
    - NOTE: the dropdown name should be "specialization" (not "interest") since this page is specifically for consultations
    - BUT js/main.js uses name="interest" for form submission. Check main.js carefully -- if it collects fields by name, use the name that main.js expects. The safest approach: use name="interest" with label "Специализация" and consultation-specific options.
  - Honeypot field (visually-hidden, name="website")
  - Submit button: "Отправить заявку" (same as contacts.html pattern)
  - Privacy text: "Мы перезвоним в течение 24 часов. Ваши данные защищены." (VERBATIM)
  - Error div hidden

IMPORTANT: The form IDs (contact-name, contact-phone, etc.) must match those used in main.js. Read main.js to confirm the exact IDs and field names it expects. If main.js queries by class (.contact-form, .form__input, etc.) then use those classes. If it queries by ID, use matching IDs. The goal is that submitting this form posts to Directus identically to the contacts.html form.
  </action>
  <verify>
    <automated>grep -c "Прозрачная цена" online-consultations.html | grep -q "1" && grep -c "от 450" online-consultations.html | grep -q "[1-9]" && grep -c "contact-form" online-consultations.html | grep -q "[1-9]" && grep -c "consultation-form" online-consultations.html | grep -q "1" && grep -c "form__submit" online-consultations.html | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Прозрачная цена, никаких сюрпризов" online-consultations.html returns match
    - grep "от 450" online-consultations.html returns match (in pricing section)
    - grep "Перевод ваших медицинских документов" online-consultations.html returns match
    - grep "Подготовка врача к консультации" online-consultations.html returns match
    - grep "Видеовстреча с переводчиком" online-consultations.html returns match
    - grep "Письменное заключение врача" online-consultations.html returns match
    - grep "Доступ к личному кабинету" online-consultations.html returns match
    - grep "Записаться на консультацию" online-consultations.html returns match
    - grep 'id="consultation-form"' online-consultations.html returns match
    - grep "contact-form" online-consultations.html returns match (form class for JS binding)
    - grep "form__submit" online-consultations.html returns match
    - grep "visually-hidden" online-consultations.html returns match (honeypot present)
    - grep "Мы перезвоним в течение 24 часов" online-consultations.html returns match
  </acceptance_criteria>
  <done>Pricing section shows "от 450€" with 5 included items. Form section has contact form with Directus-compatible HTML structure (class="contact-form", field names matching main.js expectations, honeypot, success overlay). Form anchored at #consultation-form so hero CTA scroll works.</done>
</task>

<task type="auto">
  <name>Task 2: Add Sections 10-11 (FAQ Accordion + Final CTA)</name>
  <read_first>
    - .planning/phases/26-online-consultations-page-rewrite/copywriting-source.txt (THE copywriting source -- use text VERBATIM)
    - online-consultations.html (current file with sections 1-9 from plan 01 + Task 1)
    - index.html (REFERENCE for FAQ accordion HTML pattern -- copy structure EXACTLY)
  </read_first>
  <files>online-consultations.html</files>
  <action>
Insert Sections 10 and 11 into online-consultations.html, AFTER section 9 (form) and BEFORE `</main>`.

CRITICAL: Use VERBATIM text from copywriting-source.txt. Do NOT paraphrase.

**Section 10 -- FAQ (per CONSULT-10 + CROSS-03):**
- id="faq"
- Section heading: "Частые вопросы" (gradient text, matching index.html pattern)
- Container: max-w-3xl mx-auto
- 6 FAQ items using EXACT same HTML structure as index.html FAQ (this is critical for js/main.js accordion to work):
  - Each item: div.faq__item with glass styling
  - Button.faq__question with aria-expanded="false", chevron SVG icon
  - div.faq__answer with div.faq__answer-inner containing the answer text

The 6 Q&As (ALL TEXT VERBATIM from copywriting):

1. Q: "Как проходит консультация технически?"
   A: "Видеозвонок через приложение MedicusUnion. Работает на телефоне, планшете и компьютере. Нужен только интернет."

2. Q: "Сколько длится консультация?"
   A: "Обычно 30--60 минут. Врач не торопится -- это не поликлиника с 10-минутным приёмом."

3. Q: "А если я не говорю по-английски?"
   A: "Не нужно. Мы обеспечиваем перевод во время консультации. Документы тоже переводим сами."

4. Q: "Можно показать заключение своему врачу?"
   A: "Конечно. Вы получите письменное заключение -- его можно скачать, распечатать, показать любому врачу."

5. Q: "Консультация обязывает к лечению?"
   A: "Нет. Это ваше решение. Консультация даёт информацию для принятия решения, а не обязательство."

6. Q: "Можно выбрать конкретного врача?"
   A: "Да. Вы можете выбрать врача на платформе сами, или мы подберём подходящего специалиста под ваш случай."

Use HTML entities for non-breaking spaces where appropriate (same as index.html does): &amp;nbsp; between prepositions and following words, &amp;mdash; for em-dash, &amp;ndash; for en-dash.

**Section 11 -- FINAL CTA (per CONSULT-11):**
- Large glass CTA card (same pattern as current page's CTA section: bg-white/60 backdrop-blur-3xl rounded-[3.5rem] p-12 lg:p-20 text-center, with decorative blur blobs)
- Heading: "Не откладывайте решение" (VERBATIM)
- Paragraph: "Второе мнение -- это не роскошь, а возможность принять правильное решение. От 450 EUR, за 5 дней, из дома." (VERBATIM)
- Two CTA buttons:
  1. Primary: "Получить консультацию" -- links to #consultation-form (gradient button)
  2. Secondary: "Оставить заявку" -- links to contacts.html (outline/secondary button style)

Ensure the `</main>` tag properly closes after section 11, followed by footer, sticky bar, and scripts (all unchanged from current file).
  </action>
  <verify>
    <automated>grep -c "faq__question" online-consultations.html | xargs test 6 -le && grep -c "faq__item" online-consultations.html | xargs test 6 -le && grep -q "Не откладывайте решение" online-consultations.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Как проходит консультация технически" online-consultations.html returns match
    - grep "Сколько длится консультация" online-consultations.html returns match
    - grep "А если я не говорю по-английски" online-consultations.html returns match
    - grep "Можно показать заключение своему врачу" online-consultations.html returns match
    - grep "Консультация обязывает к лечению" online-consultations.html returns match
    - grep "Можно выбрать конкретного врача" online-consultations.html returns match
    - grep -c "faq__item" online-consultations.html returns 6 (exactly 6 FAQ items)
    - grep -c "faq__question" online-consultations.html returns 6 (exactly 6 question buttons)
    - grep "Не откладывайте решение" online-consultations.html returns match
    - grep "Второе мнение" online-consultations.html returns match in final CTA paragraph
    - The HTML is well-formed: </main> closes properly before footer
  </acceptance_criteria>
  <done>FAQ accordion has 6 items with verbatim Q&A text, using faq__item/faq__question class structure for JS compatibility. Final CTA shows "Не откладывайте решение" with two buttons. Page is complete with all 11 sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete page</name>
  <files>online-consultations.html</files>
  <action>
Human verifies the complete online-consultations.html page in browser.

What was built: complete rewrite with all 11 sections from the copywriting document, inline consultation form with Directus integration, and 6-item FAQ accordion.

Verification steps:
1. Open online-consultations.html in browser (use live server or file://)
2. Verify hero section: H1 "Мнение немецкого врача -- за 5 дней, без перелёта", CTA button "Получить консультацию -- от 450 EUR"
3. Scroll through all 11 sections in order: Hero, Знакомо?, Что вы получите, Три шага, Врачи, Почему MedicusUnion, Когда нужна консультация, Прозрачная цена, Форма, FAQ, Финальный CTA
4. Click "Получить консультацию" button in hero -- should smooth-scroll to the form section
5. Test FAQ accordion: click each of the 6 questions -- answer should expand/collapse smoothly, only one open at a time
6. Test the form: fill in name, phone, select specialization, submit -- verify validation works (try submitting empty)
7. Check mobile view (resize to 375px width): sections stack vertically, text readable, form usable
8. Verify glassmorphism design: glass cards have blur/transparency, mesh background visible, gradient headings work

Resume signal: Type "approved" or describe specific issues to fix.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: requires human visual verification"</automated>
  </verify>
  <done>User confirms all 11 sections display correctly, FAQ accordion works, form validates and submits, mobile layout is usable, glassmorphism design is consistent.</done>
</task>

</tasks>

<verification>
1. All 11 section headings present and match copywriting document verbatim
2. `grep -c "faq__item" online-consultations.html` returns exactly 6
3. `grep "contact-form" online-consultations.html` returns match (form works with main.js)
4. `grep "consultation-form" online-consultations.html` returns match (anchor for hero CTA)
5. FAQ accordion expands/collapses (requires browser test)
6. Form submits to Directus (requires browser test or curl)
7. Page structure: head + header + mobile menu + mesh bg + main(11 sections) + footer + sticky bar + scripts
</verification>

<success_criteria>
- All 11 copywriting sections appear in document order with verbatim text
- Hero CTA "Получить консультацию -- от 450 EUR" scrolls to #consultation-form
- Consultation form posts to Directus via js/main.js (class="contact-form" binding)
- FAQ accordion works (6 items, expand/collapse, one-at-a-time)
- Final CTA "Не откладывайте решение" has two buttons
- Mobile responsive (readable at 375px width)
- Glassmorphism design consistent with index.html
</success_criteria>

<output>
After completion, create `.planning/phases/26-online-consultations-page-rewrite/26-02-SUMMARY.md`
</output>
