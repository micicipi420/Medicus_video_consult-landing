---
phase: 43-liquid-glass-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/liquid-glass.css
  - src/styles/tailwind.css
  - css/styles.css
autonomous: true
requirements: [LIQUID-01, LIQUID-02, LIQUID-03, LIQUID-04, LIQUID-06, LIQUID-07, DIFF-01, DIFF-02, DIFF-03]

must_haves:
  truths:
    - ".liquid-regular class applies backdrop-filter blur+saturate+brightness with tint and rim lighting using Phase 41 tokens"
    - "Dark mode glass is automatic via --liquid-* token cascade (no explicit .dark selectors in liquid-glass.css)"
    - "Primary CTA class retains gradient fill with specular edge, NOT clear glass"
    - "Secondary button class uses glass material with hover brightening and press scale"
    - "Print stylesheet renders all glass surfaces as opaque white with 1px border"
    - "Reduced-motion downgrades blur to 8px and hides shimmer ::before"
    - "Shimmer sweep class exists with ::before pseudo-element and reduced-motion guard"
    - "Stats-glass class uses --liquid-blur-lg for larger grouped surface"
    - "Scroll-edge fade classes use mask-image linear-gradient"
  artifacts:
    - path: "src/styles/liquid-glass.css"
      provides: "All liquid glass material and differentiator CSS classes"
      contains: "liquid-regular"
    - path: "src/styles/tailwind.css"
      provides: "Import chain including liquid-glass.css"
      contains: "liquid-glass.css"
  key_links:
    - from: "src/styles/liquid-glass.css"
      to: "src/styles/theme.css"
      via: "var(--liquid-*) token references"
      pattern: "var\\(--liquid-"
    - from: "src/styles/tailwind.css"
      to: "src/styles/liquid-glass.css"
      via: "@import './liquid-glass.css'"
      pattern: "@import.*liquid-glass"
---

<objective>
Create src/styles/liquid-glass.css with all Liquid Glass material classes and differentiator component classes, then wire it into the Tailwind CSS import chain.

Purpose: Deliver 9 reusable CSS classes that encode the complete v4.0 glass visual language -- base material, button variants, grouped stats, shimmer, scroll-fade, plus print and reduced-motion fallbacks. These classes will be applied to HTML elements in Phases 44-47. No HTML pages are modified.

Output: src/styles/liquid-glass.css (new file), src/styles/tailwind.css (1 line added), css/styles.css (recompiled)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-liquid-glass-primitives/43-CONTEXT.md
@.planning/phases/43-liquid-glass-primitives/43-RESEARCH.md
@.planning/phases/42-squircle-primitives/42-01-SUMMARY.md
@src/styles/theme.css
@src/styles/squircles.css
@src/styles/tailwind.css

<interfaces>
<!-- Phase 41 liquid tokens in theme.css :root (lines 115-136) -->
--liquid-bg: rgba(255, 255, 255, 0.18);
--liquid-blur-sm: 16px;
--liquid-blur-md: 24px;
--liquid-blur-lg: 40px;
--liquid-blur-xl: 60px;
--liquid-saturate: 180%;
--liquid-brightness: 108%;
--liquid-border-top: rgba(255, 255, 255, 0.9);
--liquid-border-bottom: rgba(255, 255, 255, 0.35);
--liquid-shadow-outer: 0 16px 40px rgba(20, 30, 60, 0.12);
--liquid-shadow-inset-top: inset 0 1px 0 rgba(255, 255, 255, 0.8);
--liquid-shadow-inset-bottom: inset 0 -1px 0 rgba(255, 255, 255, 0.15);

<!-- Phase 41 motion tokens in theme.css :root (lines 129-135) -->
--ease-liquid: cubic-bezier(0.2, 0, 0, 1);
--ease-liquid-out: cubic-bezier(0.16, 1, 0.3, 1);
--dur-press: 120ms;
--dur-hover: 280ms;

<!-- Phase 41 dark recipe in theme.css .dark (lines 174-186) -->
--liquid-bg: rgba(30, 40, 60, 0.45);
--liquid-blur-md: 28px;
--liquid-blur-lg: 44px;
--liquid-saturate: 160%;
--liquid-brightness: 115%;
--liquid-border-top: rgba(255, 255, 255, 0.25);
--liquid-border-bottom: rgba(0, 0, 0, 0.4);
--liquid-shadow-inset-top: inset 0 1px 0 rgba(255, 255, 255, 0.15);
--liquid-shadow-inset-bottom: inset 0 -1px 0 rgba(0, 0, 0, 0.3);

<!-- CTA gradient tokens in theme.css :root (lines 49-50) -->
--mu-cta-from: #0E8FB5;
--mu-cta-to: #3B6DD0;

<!-- Existing reduced-motion guard in theme.css (lines 406-422) -->
<!-- Already zeroes all animation-duration/transition-duration via *, *::before, *::after -->
<!-- Already sets --dur-press/hover/sheet/reveal to 0ms -->

<!-- Phase 42 squircles.css pattern: utility classes, no @layer, @supports PE -->
<!-- Shadow-wrap pattern: outer div = shadow, inner div = mask + glass -->

<!-- Current tailwind.css import chain (5 lines total) -->
@import './fonts.css';
@import 'tailwindcss' source(none);
@source '../../*.html';
@import './theme.css';
@import './squircles.css';    /* Phase 42: squircle primitives */
<!-- ADD: @import './liquid-glass.css'; after squircles.css -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/styles/liquid-glass.css with all glass material and differentiator classes</name>
  <files>src/styles/liquid-glass.css</files>
  <read_first>
    src/styles/theme.css (lines 115-136 for light tokens, 174-186 for dark tokens, 406-422 for reduced-motion)
    src/styles/squircles.css (file structure pattern to follow)
    .planning/phases/43-liquid-glass-primitives/43-RESEARCH.md (Code Examples section has exact CSS for each class)
  </read_first>
  <action>
Create src/styles/liquid-glass.css following the exact pattern of squircles.css (utility classes, no @layer, descriptive file header comment). The file structure must be:

**File header comment block** (like squircles.css lines 1-54):
- Single-variant: Regular only (Clear is anti-feature for medical CA 45+)
- Tokens declared in: theme.css :root and .dark cascade
- Companion: squircles.css (shape primitives)
- Dark mode: automatic via --liquid-* token cascade (no explicit .dark selectors)
- Document shadow-wrap pattern for glass cards
- Document anti-patterns: NEVER will-change: backdrop-filter on static cards, NEVER nest glass inside glass, NEVER shimmer on non-hero elements, NEVER border on masked glass (use inset box-shadow), NEVER box-shadow AND mask-image on same element (use shadow-wrap)
- Note that backdrop-filter creates stacking contexts (PITFALLS H3)

**Section 1 -- Base material** (per D-01 LIQUID-01):
```css
.liquid-regular {
  background: var(--liquid-bg);
  backdrop-filter: blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  -webkit-backdrop-filter: blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  border-top: 1px solid var(--liquid-border-top);
  border-bottom: 1px solid var(--liquid-border-bottom);
  box-shadow:
    var(--liquid-shadow-inset-top),
    var(--liquid-shadow-inset-bottom);
}
```
Dark mode is automatic -- .dark cascade in theme.css overrides all --liquid-* variables. No explicit .dark selectors in this file.

**Section 2 -- Card** (per D-01 LIQUID-01):
```css
.liquid-card {
  background: var(--liquid-bg);
  backdrop-filter: blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  -webkit-backdrop-filter: blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  border-top: 1px solid var(--liquid-border-top);
  border-bottom: 1px solid var(--liquid-border-bottom);
  box-shadow:
    var(--liquid-shadow-inset-top),
    var(--liquid-shadow-inset-bottom);
  padding: 1.5rem;
}
```
Note: .liquid-card is .liquid-regular + padding. In HTML, use composition: `class="squircle-lg liquid-card"`.

```css
.liquid-card-wrap {
  box-shadow: var(--liquid-shadow-outer);
  border-radius: 24px; /* matches squircle-lg fallback radius */
}
```

**Section 3 -- Buttons** (per D-03 LIQUID-03, D-04 LIQUID-04):

Primary CTA (gradient fill, NOT glass -- conversion affordance for 45+):
```css
.liquid-btn-primary {
  background: linear-gradient(135deg, var(--mu-cta-from), var(--mu-cta-to));
  color: white;
  font-weight: 600;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.25),
    inset 0 -1px 0 rgba(0, 0, 0, 0.15),
    0 2px 8px rgba(14, 143, 181, 0.25);
  transition: filter var(--dur-hover) var(--ease-liquid),
              transform var(--dur-press) var(--ease-liquid);
}
.liquid-btn-primary:hover {
  filter: brightness(1.08);
}
.liquid-btn-primary:active {
  transform: scale(0.97);
}
```

Secondary button (glass material):
```css
.liquid-btn-secondary {
  background: var(--liquid-bg);
  backdrop-filter: blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  -webkit-backdrop-filter: blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  border-top: 1px solid var(--liquid-border-top);
  border-bottom: 1px solid var(--liquid-border-bottom);
  box-shadow:
    var(--liquid-shadow-inset-top),
    var(--liquid-shadow-inset-bottom);
  font-weight: 600;
  transition: filter var(--dur-hover) var(--ease-liquid),
              transform var(--dur-press) var(--ease-liquid);
}
.liquid-btn-secondary:hover {
  filter: brightness(1.1) saturate(1.2);
}
.liquid-btn-secondary:active {
  transform: scale(0.97);
}
```

**Section 4 -- Grouped stats backdrop** (per D-10 DIFF-02):
```css
.stats-glass {
  background: var(--liquid-bg);
  backdrop-filter: blur(var(--liquid-blur-lg)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  -webkit-backdrop-filter: blur(var(--liquid-blur-lg)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  border-top: 1px solid var(--liquid-border-top);
  border-bottom: 1px solid var(--liquid-border-bottom);
  box-shadow:
    var(--liquid-shadow-inset-top),
    var(--liquid-shadow-inset-bottom);
  padding: 1.5rem;
}
```
Uses --liquid-blur-lg (40px light / 44px dark) instead of --liquid-blur-md because it is a large surface grouping multiple elements.

**Section 5 -- Shimmer sweep** (per D-08 DIFF-01):
Hero CTA only. Max 1 per viewport. CSS-only hover animation via ::before pseudo-element.
```css
.shimmer-sweep {
  position: relative;
  overflow: hidden;
}
.shimmer-sweep::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(110deg, transparent 40%, rgba(255, 255, 255, 0.25) 50%, transparent 60%);
  transform: translateX(-100%);
  transition: transform 0.8s ease;
  pointer-events: none;
}
.shimmer-sweep:hover::before {
  transform: translateX(100%);
}
```

**Section 6 -- Scroll-edge fade** (per D-09 DIFF-03):
CSS mask-image gradient fade at content/chrome overlap boundaries.
```css
.scroll-fade-top {
  --scroll-fade-size: 80px;
  mask-image: linear-gradient(to bottom, transparent 0%, black var(--scroll-fade-size));
  -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black var(--scroll-fade-size));
}
.scroll-fade-bottom {
  --scroll-fade-size: 80px;
  mask-image: linear-gradient(to top, transparent 0%, black var(--scroll-fade-size));
  -webkit-mask-image: linear-gradient(to top, transparent 0%, black var(--scroll-fade-size));
}
```

**Section 7 -- Refraction progressive enhancement** (per D-05 LIQUID-05, CSS portion):
Chrome 139+ only, gated by html[data-refract="true"] attribute set by JS probe (added in Plan 02).
```css
html[data-refract="true"] .liquid-regular,
html[data-refract="true"] .liquid-card,
html[data-refract="true"] .stats-glass {
  backdrop-filter: url(#liquid-refract) blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  -webkit-backdrop-filter: url(#liquid-refract) blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
}
```

**Section 8 -- Print stylesheet** (per D-06 LIQUID-06):
```css
@media print {
  .liquid-regular,
  .liquid-card,
  .liquid-btn-primary,
  .liquid-btn-secondary,
  .stats-glass {
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
    background: white !important;
    border: 1px solid #ccc !important;
    box-shadow: none !important;
  }
  .shimmer-sweep::before {
    display: none !important;
  }
  .scroll-fade-top,
  .scroll-fade-bottom {
    mask-image: none !important;
    -webkit-mask-image: none !important;
  }
}
```

**Section 9 -- Reduced-motion** (per D-07 LIQUID-07):
Extends existing theme.css reduced-motion guard (which already zeroes animation-duration/transition-duration). This block adds glass-specific downgrades.
```css
@media (prefers-reduced-motion: reduce) {
  .liquid-regular,
  .liquid-card,
  .liquid-btn-secondary,
  .stats-glass {
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  .shimmer-sweep::before {
    display: none;
  }
}
```
  </action>
  <verify>
    <automated>grep -c 'liquid-regular' src/styles/liquid-glass.css && grep -c 'liquid-card-wrap' src/styles/liquid-glass.css && grep -c 'liquid-btn-primary' src/styles/liquid-glass.css && grep -c 'liquid-btn-secondary' src/styles/liquid-glass.css && grep -c 'stats-glass' src/styles/liquid-glass.css && grep -c 'shimmer-sweep' src/styles/liquid-glass.css && grep -c 'scroll-fade-top' src/styles/liquid-glass.css && grep -c 'scroll-fade-bottom' src/styles/liquid-glass.css && grep -c '@media print' src/styles/liquid-glass.css && grep -c 'prefers-reduced-motion' src/styles/liquid-glass.css && grep -c 'data-refract' src/styles/liquid-glass.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'liquid-regular' src/styles/liquid-glass.css returns matches (class exists)
    - grep 'liquid-card' src/styles/liquid-glass.css returns matches (card class + card-wrap class)
    - grep 'liquid-btn-primary' src/styles/liquid-glass.css returns matches (gradient CTA button)
    - grep 'liquid-btn-secondary' src/styles/liquid-glass.css returns matches (glass secondary button)
    - grep 'stats-glass' src/styles/liquid-glass.css returns matches (grouped stats backdrop)
    - grep 'shimmer-sweep' src/styles/liquid-glass.css returns matches (hero CTA shimmer)
    - grep 'scroll-fade-top' src/styles/liquid-glass.css returns matches (scroll-edge top fade)
    - grep 'scroll-fade-bottom' src/styles/liquid-glass.css returns matches (scroll-edge bottom fade)
    - grep '@media print' src/styles/liquid-glass.css returns 1 match (print fallback block)
    - grep 'prefers-reduced-motion' src/styles/liquid-glass.css returns 1 match (reduced-motion block)
    - grep 'data-refract' src/styles/liquid-glass.css returns matches (refraction PE CSS selectors)
    - grep 'backdrop-filter:.*blur(var(--liquid-blur-md))' src/styles/liquid-glass.css returns matches (token-based recipe)
    - grep 'var(--liquid-bg)' src/styles/liquid-glass.css returns matches (tint overlay uses token)
    - grep 'var(--liquid-shadow-inset-top)' src/styles/liquid-glass.css returns matches (rim lighting uses token)
    - grep 'var(--mu-cta-from)' src/styles/liquid-glass.css returns match (primary CTA gradient token)
    - grep 'scale(0.97)' src/styles/liquid-glass.css returns match (press transform)
    - grep 'blur(8px)' src/styles/liquid-glass.css returns match (reduced-motion downgrade)
    - grep 'blur(var(--liquid-blur-lg))' src/styles/liquid-glass.css returns match (stats-glass uses larger blur)
    - No .dark {} selector blocks exist in the file (dark mode is automatic via token cascade)
    - File does NOT contain will-change: backdrop-filter (anti-pattern)
  </acceptance_criteria>
  <done>
    src/styles/liquid-glass.css exists with all 9 class groups (liquid-regular, liquid-card, liquid-card-wrap, liquid-btn-primary, liquid-btn-secondary, stats-glass, shimmer-sweep, scroll-fade-top/bottom), refraction PE selectors, print fallback, and reduced-motion extension. All classes reference Phase 41 tokens via var(). No explicit .dark selectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire liquid-glass.css import into tailwind.css and verify CSS build</name>
  <files>src/styles/tailwind.css, css/styles.css</files>
  <read_first>
    src/styles/tailwind.css (current import chain)
    src/styles/liquid-glass.css (just created in Task 1)
  </read_first>
  <action>
Add one line to src/styles/tailwind.css after the squircles.css import:

```css
@import './liquid-glass.css'; /* Phase 43: liquid glass primitives */
```

The full tailwind.css should become:
```css
@import './fonts.css';
@import 'tailwindcss' source(none);
@source '../../*.html';
@import './theme.css';
@import './squircles.css';    /* Phase 42: squircle primitives */
@import './liquid-glass.css'; /* Phase 43: liquid glass primitives */
```

Then run `make build` to recompile CSS. Verify:
1. make build exits 0
2. All liquid-glass class names appear in css/styles.css compiled output
3. make check exits 0 (no HTML drift -- this phase modifies no HTML files)
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && grep -c 'liquid-regular' css/styles.css && grep -c 'liquid-btn-primary' css/styles.css && grep -c 'stats-glass' css/styles.css && grep -c 'shimmer-sweep' css/styles.css && grep -c 'scroll-fade' css/styles.css && make check</automated>
  </verify>
  <acceptance_criteria>
    - grep '@import.*liquid-glass' src/styles/tailwind.css returns 1 match
    - tailwind.css has exactly 6 lines (fonts, tailwindcss, source, theme, squircles, liquid-glass)
    - make build exits 0 (Tailwind CSS compiles without errors)
    - grep 'liquid-regular' css/styles.css returns matches (class compiled into output)
    - grep 'liquid-btn-primary' css/styles.css returns matches
    - grep 'stats-glass' css/styles.css returns matches
    - grep 'shimmer-sweep' css/styles.css returns matches
    - grep 'scroll-fade' css/styles.css returns matches
    - make check exits 0 (no HTML drift -- byte-identity gate passes)
  </acceptance_criteria>
  <done>
    liquid-glass.css imported in tailwind.css, make build succeeds, all liquid glass classes appear in compiled css/styles.css, make check confirms zero HTML drift.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply. This plan creates CSS utility classes only. No user input processing, no network calls, no authentication, no data storage.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | D (Denial of Service) | backdrop-filter compositing | accept | GPU-heavy but standard CSS; will-change:backdrop-filter explicitly prohibited in anti-patterns comment; real-device testing deferred to Phase 48 VERIFY-03 |
</threat_model>

<verification>
- All 9 class names appear in src/styles/liquid-glass.css source
- @media print block exists with backdrop-filter: none !important
- @media (prefers-reduced-motion: reduce) block exists with blur(8px) downgrade
- html[data-refract="true"] selectors exist for refraction PE
- @import in tailwind.css points to liquid-glass.css
- make build exits 0
- All class names appear in compiled css/styles.css
- make check exits 0 (zero HTML drift)
- No .dark {} selector blocks in liquid-glass.css
- No will-change: backdrop-filter in liquid-glass.css
</verification>

<success_criteria>
CSS primitives file exists with complete Liquid Glass material system (9 class groups + refraction PE + print + reduced-motion), wired into Tailwind build chain, compiles successfully, and no HTML files are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/43-liquid-glass-primitives/43-01-SUMMARY.md`
</output>
