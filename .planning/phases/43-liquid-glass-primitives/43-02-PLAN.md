---
phase: 43-liquid-glass-primitives
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - js/main.js
  - src/styles/liquid-glass.css
autonomous: false
requirements: [LIQUID-05]

must_haves:
  truths:
    - "initRefractionProbe() function exists in js/main.js and is called in initAll()"
    - "JS probe sets html[data-refract='true'] attribute when CSS.supports confirms backdrop-filter url() support"
    - "Refraction CSS in liquid-glass.css is gated by html[data-refract='true'] selector"
    - "Safari/Firefox show blur-only glass (no refraction) because JS probe returns false"
  artifacts:
    - path: "js/main.js"
      provides: "initRefractionProbe() function"
      contains: "initRefractionProbe"
  key_links:
    - from: "js/main.js"
      to: "src/styles/liquid-glass.css"
      via: "html[data-refract] attribute bridges JS detection to CSS selectors"
      pattern: "data-refract"
---

<objective>
Add the refraction JS probe to js/main.js that detects Chrome's support for SVG filters in backdrop-filter, enabling the refraction progressive enhancement CSS selectors already in liquid-glass.css.

Purpose: Complete LIQUID-05 by wiring the JS detection probe that gates the refraction visual effect to Chrome 139+ only. Safari/Firefox get blur-only glass (graceful degradation). The probe is ~10 lines of vanilla JS.

Output: js/main.js with initRefractionProbe() function, verified working with make build
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-liquid-glass-primitives/43-CONTEXT.md
@.planning/phases/43-liquid-glass-primitives/43-RESEARCH.md
@.planning/phases/43-liquid-glass-primitives/43-01-SUMMARY.md
@js/main.js
@src/styles/liquid-glass.css

<interfaces>
<!-- From js/main.js: existing initialization pattern -->
function initAll() {
  initStickyHeader();
  initMobileMenu();
  initSmoothScroll();
  initAccordion();
  initPhoneMask();
  initSpamProtection();
  initFormValidation();
  initAnimatedCounters();
}

<!-- window.MU exports pattern -->
window.MU = window.MU || {};
window.MU.initAll = initAll;
window.MU.reinitPageContent = reinitPageContent;

<!-- From liquid-glass.css: refraction CSS selectors (already written in Plan 01) -->
html[data-refract="true"] .liquid-regular,
html[data-refract="true"] .liquid-card,
html[data-refract="true"] .stats-glass {
  backdrop-filter: url(#liquid-refract) blur(...) saturate(...) brightness(...);
}

<!-- Research-provided JS probe pattern -->
function initRefractionProbe() {
  if (typeof CSS !== 'undefined' && CSS.supports &&
      CSS.supports('backdrop-filter', 'url(#test) blur(1px)')) {
    document.documentElement.setAttribute('data-refract', 'true');
  }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add initRefractionProbe() to js/main.js</name>
  <files>js/main.js</files>
  <read_first>
    js/main.js (full file -- understand initAll() location, module pattern, window.MU exports)
    .planning/phases/43-liquid-glass-primitives/43-RESEARCH.md (Refraction JS Probe section)
  </read_first>
  <action>
Add the initRefractionProbe() function to js/main.js inside the IIFE, placed BEFORE the initAll() function. The function should be:

```js
/**
 * Refraction Probe
 * Detects Chrome/Edge support for backdrop-filter: url(#svg-filter).
 * Sets html[data-refract="true"] when supported, enabling CSS refraction
 * selectors in liquid-glass.css. Safari/Firefox show blur-only glass.
 * ~10 LOC, no dependencies.
 */
function initRefractionProbe() {
  if (typeof CSS !== 'undefined' && CSS.supports &&
      CSS.supports('backdrop-filter', 'url(#test) blur(1px)')) {
    document.documentElement.setAttribute('data-refract', 'true');
  }
}
```

Then add `initRefractionProbe();` as the FIRST call in `initAll()` function (before initStickyHeader). It must run first because other init functions may depend on glass rendering being set up.

Also add `initRefractionProbe` to `window.MU` exports:
```js
window.MU.initRefractionProbe = initRefractionProbe;
```

Do NOT add initRefractionProbe to reinitPageContent() -- it only needs to run once per page load (the html attribute persists).

The initAll() function should become:
```js
function initAll() {
  initRefractionProbe();
  initStickyHeader();
  initMobileMenu();
  initSmoothScroll();
  initAccordion();
  initPhoneMask();
  initSpamProtection();
  initFormValidation();
  initAnimatedCounters();
}
```
  </action>
  <verify>
    <automated>grep -c 'initRefractionProbe' js/main.js && grep 'data-refract' js/main.js && grep 'CSS.supports' js/main.js && grep 'MU.initRefractionProbe' js/main.js</automated>
  </verify>
  <acceptance_criteria>
    - grep 'function initRefractionProbe' js/main.js returns 1 match (function defined)
    - grep 'initRefractionProbe()' js/main.js returns 2+ matches (definition + call in initAll)
    - grep "data-refract.*true" js/main.js returns match (sets attribute)
    - grep "CSS.supports.*backdrop-filter.*url" js/main.js returns match (feature detection)
    - grep 'MU.initRefractionProbe' js/main.js returns match (exported to window.MU)
    - initRefractionProbe is NOT in reinitPageContent() (one-time probe)
    - initRefractionProbe is the FIRST call in initAll()
  </acceptance_criteria>
  <done>
    js/main.js contains initRefractionProbe() function with CSS.supports feature detection, called first in initAll(), exported on window.MU, not in reinitPageContent().
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of glass primitives in browser</name>
  <files>js/main.js, src/styles/liquid-glass.css, css/styles.css</files>
  <action>
Run make build and make check to verify the complete Phase 43 deliverable. The executor should confirm build success and present the verification steps to the user.
  </action>
  <read_first>
    js/main.js (verify initRefractionProbe is present and wired)
    src/styles/liquid-glass.css (verify all 9 class groups exist)
    src/styles/tailwind.css (verify import is wired)
  </read_first>
  <what-built>
Complete Liquid Glass material system:
- src/styles/liquid-glass.css with 9 class groups (liquid-regular, liquid-card, liquid-card-wrap, liquid-btn-primary, liquid-btn-secondary, stats-glass, shimmer-sweep, scroll-fade-top/bottom)
- Refraction CSS selectors gated by html[data-refract="true"]
- Print fallback (@media print)
- Reduced-motion extension (@media prefers-reduced-motion: reduce)
- JS refraction probe in js/main.js
- All wired into Tailwind build chain

No HTML pages modified -- these are primitives only. Visual verification will be done when applied to pages in Phases 44-47. This checkpoint verifies:
1. Build succeeds
2. All classes exist in compiled output
3. No regressions in existing site
  </what-built>
  <how-to-verify>
1. Run `make build` -- should exit 0
2. Run `make check` -- should exit 0 (no HTML drift)
3. Open any existing page (e.g., index.html) in browser -- should look identical to before (no visual changes, since no HTML references the new classes yet)
4. Open browser DevTools, check css/styles.css -- search for "liquid-regular" to confirm class is present
5. In DevTools console, run: `CSS.supports('backdrop-filter', 'url(#test) blur(1px)')` -- should return true on Chrome, false on Safari/Firefox
6. Check `document.documentElement.getAttribute('data-refract')` in Chrome DevTools -- should return "true"

Optional deeper checks:
- In DevTools, temporarily add class="liquid-regular" to any element to see glass effect
- Toggle to dark mode to verify dark glass tokens cascade correctly
  </how-to-verify>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && make check && grep -c 'liquid-regular' css/styles.css && grep -c 'initRefractionProbe' js/main.js</automated>
  </verify>
  <acceptance_criteria>
    - make build exits 0
    - make check exits 0 (no HTML drift)
    - All 9 class names appear in compiled css/styles.css
    - initRefractionProbe function exists in js/main.js
    - Existing pages render identically in browser (no visual regression)
    - User approves the checkpoint
  </acceptance_criteria>
  <done>
    Build passes, all liquid glass classes compiled, JS probe wired, no regressions, user approved.
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS probe -> DOM attribute | JS sets html[data-refract] based on CSS.supports result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | S (Spoofing) | data-refract attribute | accept | Attribute is cosmetic-only (enables visual effect). Spoofing it via console has zero security impact -- worst case: user sees refraction on unsupported browser (visual glitch only). |
</threat_model>

<verification>
- js/main.js contains initRefractionProbe() function
- initRefractionProbe() is called first in initAll()
- initRefractionProbe is exported on window.MU
- CSS.supports('backdrop-filter', 'url(#test) blur(1px)') is the detection method
- html[data-refract="true"] is set on supported browsers
- make build exits 0
- make check exits 0 (no HTML drift)
- Existing pages render identically (no visual regression)
</verification>

<success_criteria>
Refraction JS probe exists in js/main.js and bridges to CSS selectors in liquid-glass.css. Chrome users will see refraction effect when glass classes are applied to HTML in later phases. Safari/Firefox will see blur-only glass. Build passes, no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/43-liquid-glass-primitives/43-02-SUMMARY.md`
</output>
