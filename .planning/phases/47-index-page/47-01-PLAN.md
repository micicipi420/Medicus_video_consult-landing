---
phase: 47-index-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, css/styles.css]
autonomous: true
requirements: [MIGRATE-06]

must_haves:
  truths:
    - "Hero section uses max-w-[1200px] grid-cols-12 layout with floating badges using liquid-regular squircle-lg"
    - "Hero primary CTA has shimmer-sweep class (DIFF-01)"
    - "Stats bar uses single stats-glass squircle-xl grouped wrapper (DIFF-02) with no individual card glass"
    - "Service cards (Section 2) use liquid-card-wrap > liquid-card squircle-xl"
    - "Problem cards (Section 3) use liquid-card-wrap > liquid-card squircle-xl"
    - "How It Works cards (Section 4) use liquid-card-wrap > liquid-card squircle-xl"
    - "Why Us section (Section 5) uses max-w-[1200px] grid-cols-12 with liquid-regular squircle-md icon boxes"
    - "Clinics cards (Section 6) use liquid-card-wrap > liquid-card squircle-xl"
    - "All 15 icon chips with group-hover:rotate-3 keep their border-radius (no squircle applied)"
    - "All protected legacy preserved: nbsp=83, visually-hidden=2, role=alert=4, aria-live=4, honeypot=2, text-wrap:balance=0"
  artifacts:
    - path: "index.html"
      provides: "Sections 1-6 + Stats migrated to v4.0 design language"
      contains: "stats-glass"
    - path: "css/styles.css"
      provides: "Rebuilt Tailwind output with new utility classes"
  key_links:
    - from: "index.html hero CTA"
      to: "liquid-glass.css .shimmer-sweep"
      via: "class attribute"
      pattern: "shimmer-sweep"
    - from: "index.html stats section"
      to: "liquid-glass.css .stats-glass"
      via: "class attribute"
      pattern: "stats-glass squircle-xl"
---

<objective>
Migrate the first 7 sections of index.html (Hero, Stats, Services, Problem, How It Works, Why Us, Clinics) from v3.x manual glass classes to v4.0 Liquid Design System primitives.

Purpose: These are the most complex sections including the hero with floating cards, the grouped stats bar (DIFF-02), the shimmer CTA (DIFF-01), and all 15 rotating icon chips that must keep border-radius.
Output: index.html with sections 1-6 + stats bar fully migrated, build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-index-page/47-RESEARCH.md
@.planning/phases/46-service-pages/46-01-SUMMARY.md
@src/styles/liquid-glass.css
@src/styles/squircles.css
@index.html

<interfaces>
<!-- Available glass/squircle/grid classes from design system -->

From src/styles/liquid-glass.css:
  .liquid-regular    -- base glass material (backdrop-filter + tint + rim)
  .liquid-card       -- glass material + padding (1.5rem)
  .liquid-card-wrap  -- outer shadow wrapper (border-radius: 24px)
  .liquid-btn-primary   -- gradient CTA button with specular edge
  .liquid-btn-secondary -- glass material button
  .stats-glass       -- grouped stats backdrop with --liquid-blur-lg
  .shimmer-sweep     -- hover shimmer animation (::before pseudo-element)
  .scroll-fade-top / .scroll-fade-bottom -- mask-image gradient fades

From src/styles/squircles.css:
  .squircle-md   -- border-radius: 16px + mask-image SVG
  .squircle-lg   -- border-radius: 24px + mask-image SVG
  .squircle-xl   -- border-radius: 40px + mask-image SVG
  .squircle-full -- border-radius: 9999px (no mask)

Shadow-wrap pattern:
  <div class="liquid-card-wrap">
    <div class="liquid-card squircle-xl">content</div>
  </div>

Anti-patterns:
  - NEVER squircle on rotating elements (mask-image distorts)
  - NEVER nest glass inside glass
  - NEVER border on masked glass (use inset shadow)
  - Max 1 shimmer-sweep per viewport
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Hero + Stats + Sections 2-3 (lines 221-543)</name>
  <read_first>
    - index.html (lines 219-543 -- hero through problem section)
    - src/styles/liquid-glass.css (shimmer-sweep and stats-glass classes)
    - .planning/phases/47-index-page/47-RESEARCH.md (section map, z-index map, pattern examples)
  </read_first>
  <files>index.html, css/styles.css</files>
  <action>
Migrate 4 sections inside index.html `<main>` (lines 221-543). Do NOT touch anything outside `<main>`, and do NOT touch BUILD-marked chrome partials.

IMPORTANT CONSTRAINTS:
- Use max-w-[1200px] everywhere (NOT max-w-content)
- Icon chips with group-hover:rotate-3 MUST keep their border-radius (NO squircle on rotating elements)
- Stats bar uses stats-glass grouped (DIFF-02) -- individual stat cells lose all glass classes
- Hero CTA gets shimmer-sweep (DIFF-01)
- Preserve ALL nbsp entities (83 total in file), ARIA attributes, honeypot fields, br tags

**Section 1: HERO (#hero, lines 222-293)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Replace `grid lg:grid-cols-2` with `grid grid-cols-12` and add column spans: left content `col-span-12 lg:col-span-7`, right photo composition `col-span-12 lg:col-span-5`
3. Hero badge pill (line 228): replace `bg-white/40 backdrop-blur-[20px] border border-glass-border px-5 py-2.5 rounded-full shadow-glass-sm` with `liquid-regular squircle-full px-5 py-2.5`
4. Hero primary CTA button (line 242): replace `bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white px-8 py-4 rounded-3xl font-semibold shadow-lg shadow-mu-blue/30 shadow-glass-inner hover:shadow-xl hover:shadow-mu-blue/40 transition-all` with `liquid-btn-primary squircle-md shimmer-sweep px-8 py-4 text-lg` -- keep flex, items-center, justify-center, gap-2, group, w-full, sm:w-auto
5. Hero secondary CTA button (line 246): replace `bg-white/50 backdrop-blur-[20px] text-mu-text-900 px-8 py-4 rounded-3xl font-semibold shadow-glass hover:bg-white/60 transition-all border border-glass-border` with `liquid-btn-secondary squircle-md px-8 py-4 text-lg text-mu-text-900` -- keep w-full, sm:w-auto
6. Main hero image frame (line 258): replace `rounded-[3rem] overflow-hidden shadow-glass-lg border-[8px] border-white/40 backdrop-blur-3xl z-10 bg-white/20` with `squircle-xl overflow-hidden z-10` -- keep absolute, right-0, top-0, w-[85%], h-[85%]
7. Secondary hero image frame (line 264): replace `rounded-[2.5rem] overflow-hidden shadow-glass-lg border-[6px] border-white/50 backdrop-blur-2xl z-20 bg-white/30` with `squircle-xl overflow-hidden z-20` -- keep absolute, left-0, bottom-4, w-3/5, h-[45%]
8. Floating badge 1 (line 269): replace `bg-white/70 backdrop-blur-[40px] p-4 rounded-[2rem] shadow-glass border border-glass-border-strong z-30` with `liquid-regular squircle-lg p-4 z-30` -- keep absolute, -right-6, top-1/4, flex, items-center, gap-4
9. Inner icon box in badge 1 (line 270): keep `rounded-2xl` (no squircle -- decorative icon inside badge, not a rotating element but also no need for squircle here)
10. Floating badge 2 (line 280): replace `bg-white/70 backdrop-blur-[40px] px-6 py-4 rounded-[2rem] shadow-glass border border-glass-border-strong z-30` with `liquid-regular squircle-lg px-6 py-4 z-30` -- keep absolute, left-8, top-12, flex, items-center, gap-3
11. Inner icon box in badge 2 (line 281): keep `rounded-2xl` (same as above)

**STATS SECTION (lines 296-317)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Wrap the grid in a `stats-glass squircle-xl` div (DIFF-02 grouped backdrop)
3. On each of the 4 stat divs: remove ALL glass classes (`bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-glass-border shadow-glass hover:shadow-glass-lg hover:bg-white/70 hover:border-glass-border-strong transition-all duration-500 overflow-hidden`) -- keep only `flex flex-col items-center justify-center p-8`
4. On stat number text: remove `relative z-10` (no longer needed without individual card glass overlays)
5. On stat label text: remove `relative z-10`

**Section 2: SERVICE CARDS (#services, lines 320-480)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Section title badge (line 324): replace manual glass classes with `liquid-regular squircle-full px-5 py-2.5`
3. Replace `grid md:grid-cols-2 lg:grid-cols-3 gap-8` with `grid grid-cols-12 gap-8`
4. Each of 3 service card wrappers: add `col-span-12 md:col-span-4 lg:col-span-4` for GRID-02 compliance
5. Each service card glass div (e.g., line 336): wrap in `liquid-card-wrap` wrapper (move h-full to wrapper), inner div becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:shadow-glass-lg`, keep `transition-all duration-500 hover:-translate-y-2 h-full flex flex-col overflow-hidden`
6. Inner image container rounded (line 339): replace `rounded-[2rem]` with `squircle-lg` -- keep overflow-hidden, border, shadow-inner
7. 3 floating icon chips (lines 344, 394, 440): KEEP `rounded-2xl` -- these have `group-hover:rotate-3` which conflicts with squircle mask-image
8. Price badges (lines 352, 400, 446): replace manual glass with `liquid-regular squircle-full px-4 py-1.5` -- keep w-fit, mb-5
9. Feature checkmark circles (lines 360-376 pattern): keep `rounded-full` (tiny circles, no squircle needed)
10. Card CTA buttons (lines 379, 425, 471): replace manual glass classes with `liquid-btn-secondary squircle-md py-4` -- keep w-full, flex, items-center, justify-center, gap-2, group/btn, mt-auto, font-bold

**Section 3: PROBLEM (#problem, lines 483-543)**
1. Replace `container mx-auto px-4 lg:px-6 relative z-10` with `mx-auto max-w-[1200px] px-4 lg:px-6 relative z-10`
2. Replace `grid md:grid-cols-2 gap-8 max-w-5xl mx-auto` with `grid grid-cols-12 gap-8 max-w-5xl mx-auto`
3. Each of 4 problem card outer divs: add `col-span-12 md:col-span-6 lg:col-span-6`
4. Each problem card glass div (e.g., line 495): wrap in `liquid-card-wrap` wrapper (move h-full to wrapper), inner div becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:shadow-glass-lg`, keep `transition-all duration-500 h-full flex flex-col overflow-hidden p-3`
5. 4 icon chips (lines 497, 509, 521, 533): KEEP `rounded-2xl` or `rounded-[1.5rem]` -- these have `group-hover:rotate-3`

After all edits, run `make build` and verify exit code 0.

Verify protected legacy:
- `grep -c '&nbsp;' index.html` must equal 83
- `grep -c 'visually-hidden' index.html` must equal 2
- `grep -c 'role="alert"' index.html` must equal 4
- `grep -c 'aria-live' index.html` must equal 4
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "BUILD OK" && echo "nbsp=$(grep -c '&amp;nbsp;' index.html)" && echo "vh=$(grep -c 'visually-hidden' index.html)" && echo "alert=$(grep -c 'role=\"alert\"' index.html)" && echo "aria=$(grep -c 'aria-live' index.html)" && echo "shimmer=$(grep -c 'shimmer-sweep' index.html)" && echo "stats-glass=$(grep -c 'stats-glass' index.html)" && echo "max-w-1200=$(grep -c 'max-w-\[1200px\]' index.html)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'shimmer-sweep' index.html >= 1
    - grep -c 'stats-glass' index.html >= 1
    - grep -c '&amp;nbsp;' index.html == 83
    - grep -c 'visually-hidden' index.html == 2
    - grep -c 'role="alert"' index.html == 4
    - grep -c 'aria-live' index.html == 4
    - make build exits 0
  </acceptance_criteria>
  <done>Hero, Stats, Services, Problem sections fully migrated to v4.0. Hero CTA has shimmer-sweep. Stats bar uses grouped stats-glass. All rotating icon chips keep border-radius. Protected legacy counts unchanged. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Sections 4-6 (How It Works, Why Us, Clinics) (lines 545-745)</name>
  <read_first>
    - index.html (lines 545-745 -- sections 4-6)
    - .planning/phases/47-index-page/47-RESEARCH.md (column span recommendations, icon chip resolution)
  </read_first>
  <files>index.html, css/styles.css</files>
  <action>
Migrate 3 sections inside index.html `<main>` (lines 545-745). Same constraints as Task 1.

**Section 4: HOW IT WORKS (#process, lines 546-582)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Replace `grid md:grid-cols-2 lg:grid-cols-4 gap-8 max-w-6xl mx-auto` with `grid grid-cols-12 gap-8 max-w-6xl mx-auto`
3. Each of 4 step cards: add `col-span-12 md:col-span-6 lg:col-span-3`
4. Each step card (e.g., line 557): wrap in `liquid-card-wrap` wrapper, inner div becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:shadow-glass-lg`, keep `transition-all duration-500 p-8 flex flex-col overflow-hidden`
5. 4 step number boxes (lines 558, 564, 570, 576): KEEP `rounded-2xl` -- these have `group-hover:rotate-3`
6. Move `group` class from the step card div to the `liquid-card-wrap` wrapper or keep it on the inner card -- ensure group-hover still works

**Section 5: WHY US (#why-us, lines 584-680)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Replace `grid lg:grid-cols-2 gap-16 items-center` with `grid grid-cols-12 gap-16 items-center`
3. Left content div: add `col-span-12 lg:col-span-7`
4. Right images collage div: add `col-span-12 lg:col-span-5` -- keep `hidden md:block`
5. Section badge (line 592): replace manual glass with `liquid-regular squircle-full px-5 py-2.5`
6. 4 advantage icon boxes (lines 607, 619, 631, 643): KEEP `rounded-[1.5rem]` -- these have `group-hover:rotate-3`
7. 4 image containers in collage (lines 659, 662, 667, 670): replace `rounded-[3rem]` with `squircle-xl` -- keep overflow-hidden, shadow-glass-lg, border, backdrop-blur classes
8. Bottom-right info card (line 670): replace manual glass with `liquid-card squircle-xl` -- remove `bg-white/40 backdrop-blur-2xl`, keep p-8, flex, flex-col, justify-center

**Section 6: CLINICS (#clinics, lines 682-745)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Replace `grid md:grid-cols-2 gap-6 max-w-5xl mx-auto mb-8` with `grid grid-cols-12 gap-6 max-w-5xl mx-auto mb-8`
3. Each of 8 country cards: add `col-span-12 md:col-span-6 lg:col-span-6`
4. Each country card (e.g., line 694): wrap in `liquid-card-wrap` wrapper, inner div becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] shadow-glass border border-glass-border`, keep `p-8 hover:shadow-glass-lg hover:border-glass-border-strong transition-all duration-300` (note: hover shadow/border classes may not work through mask -- move hover states to liquid-card-wrap if needed, or remove manual hover states since liquid-card-wrap already has shadow)

After all edits, run `make build` and verify exit code 0.

Verify no regression:
- `grep -c '&nbsp;' index.html` must equal 83
- All ARIA/honeypot counts unchanged
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "BUILD OK" && echo "nbsp=$(grep -c '&amp;nbsp;' index.html)" && echo "vh=$(grep -c 'visually-hidden' index.html)" && echo "alert=$(grep -c 'role=\"alert\"' index.html)" && echo "aria=$(grep -c 'aria-live' index.html)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '&amp;nbsp;' index.html == 83
    - grep -c 'visually-hidden' index.html == 2
    - grep -c 'role="alert"' index.html == 4
    - grep -c 'aria-live' index.html == 4
    - make build exits 0
  </acceptance_criteria>
  <done>How It Works, Why Us, and Clinics sections fully migrated to v4.0. All rotating icon chips keep border-radius. Image collage uses squircle-xl. Protected legacy counts unchanged. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is CSS class replacement only -- no user input, no API calls, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | I (Information Disclosure) | Form honeypot field | accept | Honeypot preservation verified by grep count -- no new disclosure risk |
| T-47-02 | D (Denial of Service) | backdrop-filter performance | accept | Design system already limits glass nesting; budget Android verification in Phase 48 |
</threat_model>

<verification>
After both tasks:
- `make build` exits 0
- nbsp count = 83
- visually-hidden count = 2
- role="alert" count = 4
- aria-live count = 4
- shimmer-sweep count >= 1
- stats-glass count >= 1
- No `container mx-auto` in sections 1-6 (replaced with max-w-[1200px])
- No `rounded-[` in sections 1-6 main cards (replaced with squircle-*)
- All 15 rotating icon chips still have `rounded-2xl` or `rounded-[1.5rem]`
</verification>

<success_criteria>
Hero through Clinics sections (7 of 13 total sections) are fully migrated to v4.0 Liquid Design System. Hero CTA has shimmer-sweep (DIFF-01). Stats bar uses grouped stats-glass (DIFF-02). All protected legacy is preserved. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/47-index-page/47-01-SUMMARY.md`
</output>
