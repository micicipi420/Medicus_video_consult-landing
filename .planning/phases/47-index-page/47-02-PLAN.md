---
phase: 47-index-page
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified: [index.html, css/styles.css]
autonomous: true
requirements: [MIGRATE-06, GRID-02]

must_haves:
  truths:
    - "Platform section (Section 7) uses liquid-card-wrap > liquid-card squircle-xl"
    - "Review cards (Section 8) use liquid-card-wrap > liquid-card squircle-xl"
    - "FAQ accordion items (Section 9) use liquid-regular squircle-md overflow-hidden"
    - "Form section (Section 10) uses 12-col grid with liquid-card-wrap > liquid-card squircle-xl for form card, form inputs use squircle-md (no nested glass)"
    - "Final CTA section (Section 11) uses liquid-card-wrap > liquid-card squircle-xl with CTA buttons using liquid-btn-primary/secondary squircle-md"
    - "Zero container mx-auto instances in main content (all replaced with max-w-[1200px])"
    - "Zero rounded-[ instances in main content cards"
    - "GRID-02 verified: all text-bearing card grids use md:col-span-4 or wider on tablet across all 6 pages"
    - "All protected legacy preserved: nbsp=83, visually-hidden=2, role=alert=4, aria-live=4, honeypot=2, text-wrap:balance=0"
  artifacts:
    - path: "index.html"
      provides: "All 13 sections migrated to v4.0 design language"
      contains: "liquid-card"
    - path: "css/styles.css"
      provides: "Rebuilt Tailwind output with all v4.0 utility classes"
  key_links:
    - from: "index.html form section"
      to: "Form submission JS"
      via: "form element with .contact-form class"
      pattern: "contact-form"
    - from: "index.html FAQ section"
      to: "FAQ accordion JS"
      via: "faq__item class"
      pattern: "faq__item"
---

<objective>
Migrate the remaining 5 sections of index.html (Platform, Reviews, FAQ, Form, Final CTA) from v3.x manual glass classes to v4.0 Liquid Design System primitives, then run GRID-02 cross-page tablet verification.

Purpose: Complete the index.html migration (Sections 7-11) and verify the responsive grid system works on tablet across all 6 pages.
Output: Fully migrated index.html, verified GRID-02 compliance, build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-index-page/47-RESEARCH.md
@.planning/phases/47-index-page/47-01-SUMMARY.md
@src/styles/liquid-glass.css
@src/styles/squircles.css
@index.html

<interfaces>
<!-- Same design system classes as Plan 01 -->

From src/styles/liquid-glass.css:
  .liquid-regular       -- base glass material
  .liquid-card          -- glass material + padding
  .liquid-card-wrap     -- outer shadow wrapper
  .liquid-btn-primary   -- gradient CTA button
  .liquid-btn-secondary -- glass material button

From src/styles/squircles.css:
  .squircle-md  -- border-radius: 16px + mask-image
  .squircle-lg  -- border-radius: 24px + mask-image
  .squircle-xl  -- border-radius: 40px + mask-image
  .squircle-full -- border-radius: 9999px

Shadow-wrap pattern:
  <div class="liquid-card-wrap">
    <div class="liquid-card squircle-xl">content</div>
  </div>

FAQ pattern (no shadow-wrap needed):
  <div class="faq__item liquid-regular squircle-md overflow-hidden">

Form input pattern (squircle only, no glass):
  <input class="squircle-md border border-white/40 bg-white/50 backdrop-blur-md ...">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Sections 7-11 (Platform, Reviews, FAQ, Form, Final CTA) (lines 747-1091)</name>
  <read_first>
    - index.html (lines 747-1091 -- sections 7-11)
    - .planning/phases/47-index-page/47-RESEARCH.md (FAQ pattern, form pattern, CTA section blob)
    - .planning/phases/46-service-pages/46-01-SUMMARY.md (form section 5+7 grid pattern)
  </read_first>
  <files>index.html, css/styles.css</files>
  <action>
Migrate 5 sections inside index.html `<main>` (lines 747-1091). Same constraints as Plan 01: max-w-[1200px], preserve all protected legacy, no chrome edits.

**Section 7: PLATFORM (#platform, lines 748-784)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Platform card (line 756): wrap in `liquid-card-wrap` wrapper, inner div becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-2xl rounded-[3rem] border border-glass-border shadow-glass`, keep `p-8 md:p-12 max-w-3xl mx-auto`
3. Feature checkmark icons (lines 760-778): keep SVG as-is (no glass treatment needed on list items)

**Section 8: REVIEWS (#reviews, lines 787-842)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Replace `grid md:grid-cols-2 gap-8 max-w-5xl mx-auto` with `grid grid-cols-12 gap-8 max-w-5xl mx-auto`
3. Each of 4 review cards: add `col-span-12 md:col-span-6 lg:col-span-6`
4. Each review card (e.g., line 797): wrap in `liquid-card-wrap` wrapper, inner div becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border`, keep `p-8 hover:shadow-glass-lg hover:border-glass-border-strong transition-all duration-300` (move hover to wrapper or remove since liquid-card-wrap handles shadow)
5. Avatar circles (e.g., line 799): keep `rounded-full` (circle, no squircle needed)

**Section 9: FAQ (#faq, lines 844-930)**
1. Replace `container mx-auto px-4 lg:px-6` with `mx-auto max-w-[1200px] px-4 lg:px-6`
2. Each of 7 FAQ items (e.g., line 853): replace `bg-white/60 backdrop-blur-2xl rounded-2xl border border-glass-border shadow-glass-sm` with `liquid-regular squircle-md` -- keep `overflow-hidden` and `faq__item` class
3. Do NOT use shadow-wrap on FAQ items (they use inset shadows from liquid-regular which work inside mask)

**Section 10: FORM (#contact, lines 932-1050)**
1. Replace `container mx-auto px-4 lg:px-6 relative z-10` with `mx-auto max-w-[1200px] px-4 lg:px-6 relative z-10`
2. Replace `grid lg:grid-cols-2 gap-12 items-start` with `grid grid-cols-12 gap-12 items-start`
3. Left column (info): add `col-span-12 lg:col-span-5`
4. Right column (form): add `col-span-12 lg:col-span-7`
5. Coordinator card (line 945): wrap in `liquid-card-wrap`, inner div becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-white/60 shadow-glass`, keep `p-6 flex flex-col sm:flex-row gap-6 items-center`
6. Coordinator avatar: replace `rounded-full` with `squircle-full` (it's a large circle that can benefit from consistent shape)
7. Contact icon circles in coordinator card (lines 954, 960): keep `rounded-full` for small 8px circles
8. Trust signal badges (lines 971-979): replace manual glass with `liquid-regular squircle-full px-5 py-2.5` -- keep inline-flex, items-center, gap-2, text-sm, font-bold
9. Form container (line 984): wrap in `liquid-card-wrap`, inner becomes `liquid-card squircle-xl` -- remove `bg-white/70 backdrop-blur-3xl rounded-[3rem] border border-white/60 shadow-glass-lg`, keep `p-8 relative overflow-hidden`
10. Form inputs (lines 1001, 1007, 1025): replace `rounded-2xl` with `squircle-md` -- keep ALL other classes (border, bg-white/50, backdrop-blur-md, focus states, etc.)
11. Select (line 1013): replace `rounded-2xl` with `squircle-md` -- keep appearance-none and all other classes
12. Submit button (line 1034): replace `rounded-2xl` with `liquid-btn-primary squircle-md` -- remove manual gradient/shadow classes, keep `w-full py-4 font-bold text-lg mt-8 flex items-center justify-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed`
13. CRITICAL: Preserve honeypot div (lines 1028-1032) exactly as-is -- do not modify visually-hidden class, do not add glass/squircle
14. CRITICAL: Preserve form__success overlay (lines 986-995) -- do not add glass treatment to overlay (it has its own styling for success state)
15. CRITICAL: Preserve all form__field-error spans and role="alert" and aria-live attributes

**Section 11: FINAL CTA (#cta, lines 1052-1089)**
1. Replace `container mx-auto px-4 lg:px-6 relative z-10` with `mx-auto max-w-[1200px] px-4 lg:px-6 relative z-10`
2. Outer card (line 1055): wrap in `liquid-card-wrap`, inner becomes `liquid-card squircle-xl` -- remove `bg-white/60 backdrop-blur-3xl rounded-[3.5rem] shadow-glass-lg border border-glass-border-strong`, keep `overflow-hidden relative`
3. Inner grid: keep `grid lg:grid-cols-2` as-is (already a simple 2-col, no 12-col needed for inner split)
4. Primary CTA button (line 1067): replace manual gradient/shadow classes with `liquid-btn-primary squircle-md px-8 py-4 text-lg` -- keep w-full, sm:w-auto, flex, items-center, justify-center, gap-2, group, font-bold
5. Secondary call button (line 1071): replace manual glass classes with `liquid-btn-secondary squircle-md px-8 py-4 text-lg text-mu-text-900` -- keep w-full, sm:w-auto, flex, items-center, justify-center, gap-2, font-bold
6. Decorative blob (line 1061): keep as-is (-z-10, no conflict with glass)
7. Right image (line 1082): keep as-is (overflow-hidden already, no glass needed on image container)

After all edits, run `make build` and verify exit code 0.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "BUILD OK" && echo "nbsp=$(grep -c '&amp;nbsp;' index.html)" && echo "vh=$(grep -c 'visually-hidden' index.html)" && echo "alert=$(grep -c 'role=\"alert\"' index.html)" && echo "aria=$(grep -c 'aria-live' index.html)" && echo "honeypot=$(grep -c 'contact-website' index.html)" && echo "balance=$(grep -c 'text-balance\|text-wrap.*balance' index.html)" && echo "faq-items=$(grep -c 'faq__item' index.html)" && echo "liquid-card=$(grep -c 'liquid-card' index.html)" && echo "squircle=$(grep -c 'squircle-' index.html)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '&amp;nbsp;' index.html == 83
    - grep -c 'visually-hidden' index.html == 2
    - grep -c 'role="alert"' index.html == 4
    - grep -c 'aria-live' index.html == 4
    - grep -c 'contact-website' index.html == 2
    - grep -c 'text-balance' index.html == 0
    - grep -c 'faq__item' index.html >= 7
    - make build exits 0
  </acceptance_criteria>
  <done>Platform, Reviews, FAQ, Form, and Final CTA sections fully migrated to v4.0. FAQ items use liquid-regular squircle-md. Form inputs use squircle-md. CTA buttons use liquid-btn-primary/secondary squircle-md. All protected legacy counts unchanged. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Full migration verification + GRID-02 cross-page tablet audit</name>
  <read_first>
    - index.html (full -- verify no container mx-auto or rounded-[ remain in main)
    - .planning/phases/47-index-page/47-RESEARCH.md (GRID-02 verification table, protected legacy baselines)
  </read_first>
  <files>index.html</files>
  <action>
Run full verification suite on completed index.html migration and GRID-02 cross-page audit.

**Part A: index.html Full Migration Verification**

1. Count v3.x remnants that should be zero inside `<main>`:
   - `sed -n '/<main/,/<\/main>/p' index.html | grep -c 'container mx-auto'` should be 0
   - `sed -n '/<main/,/<\/main>/p' index.html | grep -c 'rounded-\[3rem\]'` should be 0
   - `sed -n '/<main/,/<\/main>/p' index.html | grep -c 'rounded-\[2\.5rem\]'` should be 0
   - `sed -n '/<main/,/<\/main>/p' index.html | grep -c 'rounded-\[2rem\]'` should be 0
   Note: some `rounded-[1.5rem]` may remain on rotating icon chips (that is correct and expected -- DO NOT remove).
   Note: `rounded-2xl` on icon chips with rotate is also expected.
   Note: `rounded-2xl` on form inputs should be 0 (replaced with squircle-md).

2. Count v4.0 additions in main:
   - `grep -c 'squircle-' index.html` should be >= 50 (cards + badges + buttons + inputs)
   - `grep -c 'liquid-card' index.html` should be >= 30 (card surfaces)
   - `grep -c 'liquid-btn-primary' index.html` should be >= 3 (hero CTA, form submit, final CTA)
   - `grep -c 'liquid-btn-secondary' index.html` should be >= 3 (hero secondary, service card CTAs, final CTA secondary)
   - `grep -c 'max-w-\[1200px\]' index.html` should be >= 11 (1 per section + stats + header/sticky from chrome)
   - `grep -c 'grid-cols-12' index.html` should be >= 8 (most sections)
   - `grep -c 'stats-glass' index.html` should be >= 1
   - `grep -c 'shimmer-sweep' index.html` should be >= 1

3. Full protected legacy:
   - `grep -c '&nbsp;' index.html` == 83
   - `grep -c 'visually-hidden' index.html` == 2
   - `grep -c 'role="alert"' index.html` == 4
   - `grep -c 'aria-live' index.html` == 4
   - `grep -c 'contact-website' index.html` == 2
   - `grep -c 'text-balance' index.html` == 0

4. Rotating icon chips preserved (should all still have border-radius, NOT squircle):
   - `grep -c 'group-hover:rotate-3' index.html` should be 15 (unchanged from pre-migration)

If ANY of the above checks fail, fix the issue before proceeding. Common fixes:
- If rounded-[3rem] still found: missed a card migration, apply liquid-card-wrap pattern
- If container mx-auto still found: missed a section container replacement
- If nbsp count wrong: accidentally deleted text content, restore from git

**Part B: GRID-02 Cross-Page Tablet Verification**

GRID-02 requires text-bearing cards to occupy minimum 4 columns on the 8-column tablet grid (md: breakpoint). This means card grid items should use `md:col-span-4` or wider (md:col-span-6, md:col-span-8, etc.).

For each page, verify card grids use adequate tablet column spans:

1. `grep -c 'md:col-span-' index.html` -- should be >= 20 (many card sections)
2. `grep -c 'md:col-span-' checkup.html` -- verify >= 38 (from Phase 46)
3. `grep -c 'md:col-span-' online-consultations.html` -- verify >= 19 (from Phase 46)
4. `grep -c 'md:col-span-' treatment-abroad.html` -- verify >= 20 (from Phase 46)
5. `grep -c 'md:col-span-' contacts.html` -- verify >= 2 (from Phase 45)

Check there are NO `md:col-span-3` instances on text-bearing cards across any page (3 columns = too narrow for Russian compound words on tablet). `md:col-span-3` on non-text elements (icons, spacers) is acceptable.

If any page has text-bearing cards with md:col-span-3 or narrower, fix them to md:col-span-4 minimum.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "=== MIGRATION COMPLETENESS ===" && echo "container-mx-auto-in-main=$(sed -n '/<main/,/<\/main>/p' index.html | grep -c 'container mx-auto')" && echo "rounded-3rem-in-main=$(sed -n '/<main/,/<\/main>/p' index.html | grep -c 'rounded-\[3rem\]')" && echo "squircle-count=$(grep -c 'squircle-' index.html)" && echo "liquid-card-count=$(grep -c 'liquid-card' index.html)" && echo "shimmer=$(grep -c 'shimmer-sweep' index.html)" && echo "stats-glass=$(grep -c 'stats-glass' index.html)" && echo "rotate-icons=$(grep -c 'group-hover:rotate-3' index.html)" && echo "=== PROTECTED LEGACY ===" && echo "nbsp=$(grep -c '&amp;nbsp;' index.html)" && echo "vh=$(grep -c 'visually-hidden' index.html)" && echo "alert=$(grep -c 'role=\"alert\"' index.html)" && echo "aria=$(grep -c 'aria-live' index.html)" && echo "honeypot=$(grep -c 'contact-website' index.html)" && echo "balance=$(grep -c 'text-balance' index.html)" && echo "=== GRID-02 ===" && echo "index-md-col=$(grep -c 'md:col-span-' index.html)" && echo "checkup-md-col=$(grep -c 'md:col-span-' checkup.html)" && echo "online-md-col=$(grep -c 'md:col-span-' online-consultations.html)" && echo "treatment-md-col=$(grep -c 'md:col-span-' treatment-abroad.html)" && echo "contacts-md-col=$(grep -c 'md:col-span-' contacts.html)"</automated>
  </verify>
  <acceptance_criteria>
    - sed main extract: container mx-auto count == 0
    - sed main extract: rounded-[3rem] count == 0
    - grep -c 'squircle-' index.html >= 50
    - grep -c 'liquid-card' index.html >= 30
    - grep -c 'shimmer-sweep' index.html >= 1
    - grep -c 'stats-glass' index.html >= 1
    - grep -c 'group-hover:rotate-3' index.html == 15
    - grep -c '&amp;nbsp;' index.html == 83
    - grep -c 'visually-hidden' index.html == 2
    - grep -c 'role="alert"' index.html == 4
    - grep -c 'aria-live' index.html == 4
    - grep -c 'contact-website' index.html == 2
    - grep -c 'text-balance' index.html == 0
    - grep -c 'md:col-span-' index.html >= 20
    - grep -c 'md:col-span-' checkup.html >= 38
    - grep -c 'md:col-span-' online-consultations.html >= 19
    - grep -c 'md:col-span-' treatment-abroad.html >= 20
    - grep -c 'md:col-span-' contacts.html >= 2
    - make build exits 0
  </acceptance_criteria>
  <done>Full index.html migration verified: zero v3.x remnants in main, all v4.0 classes present, all protected legacy counts exact, 15 rotating icon chips preserved with border-radius. GRID-02 verified across all 6 pages with adequate tablet column spans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form submission | Form structure and validation preserved exactly -- no input handling changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | T (Tampering) | Form honeypot field | mitigate | grep count verification ensures honeypot div (visually-hidden + contact-website) is preserved exactly |
| T-47-04 | D (Denial of Service) | Nested backdrop-filter | mitigate | Anti-pattern checks ensure no nested glass; GRID-02 verification prevents layout overflow |
</threat_model>

<verification>
After both tasks:
1. `make build` exits 0
2. Zero v3.x remnants in `<main>`:
   - container mx-auto = 0
   - rounded-[3rem] = 0
   - rounded-[2.5rem] = 0
   - rounded-[2rem] = 0
3. v4.0 class counts:
   - squircle-* >= 50
   - liquid-card >= 30
   - liquid-btn-primary >= 3
   - shimmer-sweep >= 1
   - stats-glass >= 1
   - max-w-[1200px] >= 11
   - grid-cols-12 >= 8
4. Protected legacy exact:
   - nbsp = 83, visually-hidden = 2, role="alert" = 4, aria-live = 4, contact-website = 2, text-balance = 0
5. Rotating icon chips preserved: group-hover:rotate-3 = 15
6. GRID-02: all 6 pages have adequate md:col-span- counts for tablet
</verification>

<success_criteria>
All 13 sections of index.html are migrated to v4.0 Liquid Design System. GRID-02 tablet column compliance verified across all 6 pages. Protected legacy fully preserved. Build passes. Phase 47 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/47-index-page/47-02-SUMMARY.md`
</output>
