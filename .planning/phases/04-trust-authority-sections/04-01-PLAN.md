---
phase: 04-trust-authority-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, css/styles.css]
autonomous: true
requirements: [STRUC-06]

must_haves:
  truths:
    - "Visitor sees doctors from 7 countries with specializations listed"
    - "Visitor sees a link to medicusunion.com/doctors for full doctor profiles"
    - "Section renders correctly on mobile (375px), tablet (768px), desktop (1200px+)"
  artifacts:
    - path: "index.html"
      provides: "Doctors section with heading, description, placeholder cards, specialization list, external link"
      contains: "section.*doctors"
    - path: "css/styles.css"
      provides: "Doctors section styles with responsive card grid"
      contains: ".doctors"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes doctors block"
      pattern: "doctors__"
---

<objective>
Build the "Who consults" (Кто консультирует) section showing doctors from 7 countries with specializations and a link to full doctor profiles.

Purpose: Establish trust by showing real international medical expertise from recognized countries.
Output: Doctors section in index.html with responsive CSS styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trust-authority-sections/04-CONTEXT.md
@index.html
@css/styles.css
</context>

<interfaces>
<!-- Existing patterns from codebase that executor MUST follow -->

From css/styles.css:
```css
/* Layout pattern */
.section { padding-block: var(--section-padding-mobile); }
.container { width: 100%; max-width: var(--container-max); margin-inline: auto; padding-inline: var(--space-2); }

/* Card component (reusable) */
.card { background-color: var(--color-white); border-radius: var(--radius-lg); box-shadow: var(--shadow-md); padding: var(--space-4); overflow: hidden; }
.card__title { font-family: var(--font-heading); font-size: var(--font-size-lg); font-weight: 600; }
.card__text { color: var(--color-text-primary); }

/* Button (for external link) */
.button { display: inline-flex; align-items: center; justify-content: center; min-height: 48px; ... }
.button--outline { background-color: transparent; color: var(--color-text-primary); border: 2px solid var(--color-dark); }

/* Responsive pattern: 1fr mobile, repeat(N, 1fr) at 768px */
/* Section background alternation: white/light/white/light */
```

From index.html:
```html
<!-- Last section before insertion point -->
<section class="section process" id="process">...</section>
<!-- New doctors section goes AFTER process, BEFORE </main> -->
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add doctors section HTML and CSS</name>
  <files>index.html, css/styles.css</files>
  <read_first>index.html, css/styles.css</read_first>
  <action>
**HTML** -- Insert a new section AFTER the process section (before closing `</main>` tag) in index.html:

```html
<!-- Doctors Section: Who Consults -->
<section class="section doctors" id="doctors">
  <div class="container">
    <h2 class="doctors__heading">Врачи из&nbsp;Германии, Израиля, Швейцарии и&nbsp;ещё 4&nbsp;стран</h2>
    <p class="doctors__description">На&nbsp;платформе MedicusUnion&nbsp;&mdash; врачи из&nbsp;клиник и&nbsp;медицинских университетов Германии, Израиля, Швейцарии, Австрии, ОАЭ, Южной Кореи и&nbsp;Турции.</p>

    <div class="doctors__grid">
      <div class="card doctors__card">
        <div class="doctors__flag" aria-hidden="true">&#x1F1E9;&#x1F1EA;</div>
        <h3 class="card__title">Германия</h3>
        <p class="card__text">Онкология, кардиология, нейрохирургия</p>
      </div>
      <div class="card doctors__card">
        <div class="doctors__flag" aria-hidden="true">&#x1F1EE;&#x1F1F1;</div>
        <h3 class="card__title">Израиль</h3>
        <p class="card__text">Онкология, ортопедия, ЭКО</p>
      </div>
      <div class="card doctors__card">
        <div class="doctors__flag" aria-hidden="true">&#x1F1E8;&#x1F1ED;</div>
        <h3 class="card__title">Швейцария</h3>
        <p class="card__text">Кардиология, нейрохирургия</p>
      </div>
      <div class="card doctors__card">
        <div class="doctors__flag" aria-hidden="true">&#x1F1E6;&#x1F1F9;</div>
        <h3 class="card__title">Австрия</h3>
        <p class="card__text">Ортопедия, радиология</p>
      </div>
      <div class="card doctors__card">
        <div class="doctors__flag" aria-hidden="true">&#x1F1E6;&#x1F1EA;</div>
        <h3 class="card__title">ОАЭ</h3>
        <p class="card__text">Кардиология, ортопедия</p>
      </div>
      <div class="card doctors__card">
        <div class="doctors__flag" aria-hidden="true">&#x1F1F0;&#x1F1F7;</div>
        <h3 class="card__title">Южная Корея</h3>
        <p class="card__text">Онкология, радиология</p>
      </div>
      <div class="card doctors__card">
        <div class="doctors__flag" aria-hidden="true">&#x1F1F9;&#x1F1F7;</div>
        <h3 class="card__title">Турция</h3>
        <p class="card__text">Ортопедия, ЭКО</p>
      </div>
    </div>

    <p class="doctors__specializations">Специализации: онкология, кардиология, нейрохирургия, ортопедия, радиология, ЭКО и&nbsp;другие</p>
    <p class="doctors__note">Каждый профиль врача содержит: специализация, опыт, клиника, языки консультации, стоимость</p>

    <div class="doctors__action">
      <a href="https://medicusunion.com/doctors" class="button button--outline" target="_blank" rel="noopener noreferrer">Все врачи</a>
    </div>
  </div>
</section>
```

**CSS** -- Append to section 7 (SECTIONS) in css/styles.css, AFTER the process block:

```css
/* -- Doctors (Who Consults) -- */

.doctors {
  background-color: var(--color-light);
}

.doctors__heading {
  margin-bottom: var(--space-3);
}

.doctors__description {
  font-size: var(--font-size-lg);
  max-width: 680px;
  margin-bottom: var(--space-5);
}

.doctors__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-3);
  margin-bottom: var(--space-5);
}

.doctors__card {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
  text-align: center;
}

.doctors__flag {
  font-size: 2.5rem;
  line-height: 1;
}

.doctors__specializations {
  font-weight: 600;
  margin-bottom: var(--space-2);
}

.doctors__note {
  color: var(--color-text-primary);
  font-size: var(--font-size-sm);
  margin-bottom: var(--space-4);
}

.doctors__action {
  margin-top: var(--space-2);
}

@media (min-width: 768px) {
  .doctors__grid {
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
  }
}

@media (min-width: 1024px) {
  .doctors__grid {
    grid-template-columns: repeat(4, 1fr);
  }
}
```

Use `&nbsp;` and `&mdash;` for non-breaking spaces and dashes in Russian text, consistent with prior sections.
Background color: `var(--color-light)` (alternating with process section which uses white).
  </action>
  <acceptance_criteria>
    - grep "section.*doctors" index.html returns a match
    - grep "Врачи из" index.html returns a match
    - grep "medicusunion.com/doctors" index.html returns a match
    - grep "Германии, Израиля, Швейцарии" index.html returns a match (heading text)
    - grep "онкология, кардиология, нейрохирургия" index.html returns a match (specializations)
    - grep ".doctors__grid" css/styles.css returns a match
    - grep ".doctors__flag" css/styles.css returns a match
    - All 7 country cards present in HTML (Германия, Израиль, Швейцария, Австрия, ОАЭ, Южная Корея, Турция)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doctors__card" index.html | grep -q "7" && grep -q "medicusunion.com/doctors" index.html && grep -q ".doctors__grid" css/styles.css && grep -q ".doctors__flag" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Doctors section visible in page with 7 country cards, specialization list, and external "Все врачи" link to medicusunion.com/doctors. Responsive grid: 1 col mobile, 3 cols tablet, 4 cols desktop.</done>
</task>

</tasks>

<verification>
- Open index.html in browser -- doctors section appears after process section
- 7 country flag cards visible with country names and specializations
- "Все врачи" button links to medicusunion.com/doctors (opens in new tab)
- Mobile: cards stack in single column
- Tablet (768px+): 3-column grid
- Desktop (1024px+): 4-column grid
</verification>

<success_criteria>
- Doctors section exists with id="doctors" after process section
- Heading matches ТЗ: "Врачи из Германии, Израиля, Швейцарии и ещё 4 стран"
- All 7 countries represented with flag emoji and specializations
- External link to medicusunion.com/doctors present
- Responsive layout verified at 3 breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/04-trust-authority-sections/04-01-SUMMARY.md`
</output>
