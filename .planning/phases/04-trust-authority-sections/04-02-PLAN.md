---
phase: 04-trust-authority-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html, css/styles.css]
autonomous: true
requirements: [STRUC-07, STRUC-08]

must_haves:
  truths:
    - "Visitor sees 4 advantage cards explaining why MedicusUnion"
    - "Visitor sees 5 trigger scenarios with checkmark icons"
    - "Both sections render correctly on mobile, tablet, and desktop"
  artifacts:
    - path: "index.html"
      provides: "Advantages section (4 cards) and scenarios section (5 items)"
      contains: "section.*advantages"
    - path: "css/styles.css"
      provides: "Advantages and scenarios section styles"
      contains: ".advantages"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes advantages and scenarios blocks"
      pattern: "advantages__|scenarios__"
---

<objective>
Build the "Why through MedicusUnion" (4 advantage cards) and "When you need a consultation" (5 trigger scenarios) sections.

Purpose: Overcome objections (advantages) and help visitor self-identify as the right audience (scenarios).
Output: Two new sections in index.html with responsive CSS styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trust-authority-sections/04-CONTEXT.md
@index.html
@css/styles.css
</context>

<interfaces>
<!-- Existing patterns from codebase that executor MUST follow -->

From css/styles.css:
```css
.section { padding-block: var(--section-padding-mobile); }
.container { width: 100%; max-width: var(--container-max); margin-inline: auto; padding-inline: var(--space-2); }
.card { background-color: var(--color-white); border-radius: var(--radius-lg); box-shadow: var(--shadow-md); padding: var(--space-4); overflow: hidden; }
.card__title { font-family: var(--font-heading); font-size: var(--font-size-lg); font-weight: 600; }
.card__text { color: var(--color-text-primary); }

/* Benefits grid pattern (reuse for advantages): */
.benefits__grid { display: grid; grid-template-columns: 1fr; gap: var(--space-3); }
@media (min-width: 768px) { .benefits__grid { grid-template-columns: repeat(2, 1fr); gap: var(--space-4); } }
```

From index.html:
```html
<!-- After Plan 01 inserts doctors section, these two sections go AFTER doctors, BEFORE </main> -->
<!-- Insertion order: ...process > doctors > advantages > scenarios > </main> -->
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add advantages section HTML and CSS</name>
  <files>index.html, css/styles.css</files>
  <read_first>index.html, css/styles.css</read_first>
  <action>
**HTML** -- Insert AFTER the doctors section (which Plan 01 adds after process), BEFORE `</main>`:

```html
<!-- Advantages Section: Why MedicusUnion -->
<section class="section advantages" id="advantages">
  <div class="container">
    <h2 class="advantages__heading">Почему через MedicusUnion</h2>
    <div class="advantages__grid">
      <div class="card advantages__card">
        <div class="advantages__icon" aria-hidden="true">&#x1F4C4;</div>
        <h3 class="card__title">Документы переведены и&nbsp;подготовлены</h3>
        <p class="card__text">Вы&nbsp;загружаете на&nbsp;русском&nbsp;&mdash; врач получает на&nbsp;своём языке. Не&nbsp;нужно искать переводчика медицинских документов.</p>
      </div>
      <div class="card advantages__card">
        <div class="advantages__icon" aria-hidden="true">&#x1F5E3;</div>
        <h3 class="card__title">Перевод прямо во&nbsp;время консультации</h3>
        <p class="card__text">Вы&nbsp;говорите на&nbsp;русском, врач&nbsp;&mdash; на&nbsp;своём. Переводчик обеспечивает полное понимание&nbsp;&mdash; включая медицинскую терминологию.</p>
      </div>
      <div class="card advantages__card">
        <div class="advantages__icon" aria-hidden="true">&#x1F4F1;</div>
        <h3 class="card__title">Всё в&nbsp;одном приложении</h3>
        <p class="card__text">Документы, расписание, видеозвонок, заключение врача&nbsp;&mdash; в&nbsp;личном кабинете. Ничего не&nbsp;потеряется, всё под рукой.</p>
      </div>
      <div class="card advantages__card">
        <div class="advantages__icon" aria-hidden="true">&#x2708;</div>
        <h3 class="card__title">Нужно больше&nbsp;&mdash; организуем</h3>
        <p class="card__text">Если после консультации нужно лечение за&nbsp;рубежом&nbsp;&mdash; мы&nbsp;организуем всё: клинику, документы, логистику, сопровождение. Но&nbsp;это уже следующий шаг.</p>
      </div>
    </div>
  </div>
</section>
```

**CSS** -- Append AFTER the doctors block in section 7 of css/styles.css:

```css
/* -- Advantages (Why MedicusUnion) -- */

.advantages {
  background-color: var(--color-white);
}

.advantages__heading {
  margin-bottom: var(--space-5);
}

.advantages__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-3);
}

.advantages__card {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.advantages__icon {
  font-size: 2.5rem;
  line-height: 1;
}

@media (min-width: 768px) {
  .advantages__grid {
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
  }
}
```

Background: `var(--color-white)` to alternate with doctors section (light).
Follows same card grid pattern as benefits section (2x2 on tablet+).
  </action>
  <acceptance_criteria>
    - grep "section.*advantages" index.html returns a match
    - grep "Почему через MedicusUnion" index.html returns a match
    - grep "Документы переведены" index.html returns a match
    - grep "Перевод прямо во" index.html returns a match
    - grep "одном приложении" index.html returns a match
    - grep "организуем" index.html returns a match
    - grep -c "advantages__card" index.html returns 4
    - grep ".advantages__grid" css/styles.css returns a match
  </acceptance_criteria>
  <verify>
    <automated>grep -c "advantages__card" index.html | grep -q "4" && grep -q "Почему через MedicusUnion" index.html && grep -q ".advantages__grid" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Advantages section visible with 4 cards: document translation, consultation translation, app, treatment organization. Responsive 2x2 grid on tablet+.</done>
</task>

<task type="auto">
  <name>Task 2: Add consultation scenarios section HTML and CSS</name>
  <files>index.html, css/styles.css</files>
  <read_first>index.html, css/styles.css</read_first>
  <action>
**HTML** -- Insert AFTER the advantages section, BEFORE `</main>`:

```html
<!-- Scenarios Section: When You Need a Consultation -->
<section class="section scenarios" id="scenarios">
  <div class="container">
    <h2 class="scenarios__heading">Когда имеет смысл получить второе мнение</h2>
    <ul class="scenarios__list" role="list">
      <li class="scenarios__item">
        <span class="scenarios__check" aria-hidden="true">&#x2705;</span>
        <span class="scenarios__text">Вам поставили серьёзный диагноз и&nbsp;вы&nbsp;хотите убедиться, что он&nbsp;верный</span>
      </li>
      <li class="scenarios__item">
        <span class="scenarios__check" aria-hidden="true">&#x2705;</span>
        <span class="scenarios__text">Разные врачи дают противоречивые рекомендации</span>
      </li>
      <li class="scenarios__item">
        <span class="scenarios__check" aria-hidden="true">&#x2705;</span>
        <span class="scenarios__text">Нужно понять, какое лечение подходит именно вам</span>
      </li>
      <li class="scenarios__item">
        <span class="scenarios__check" aria-hidden="true">&#x2705;</span>
        <span class="scenarios__text">Рассматриваете лечение за&nbsp;рубежом, но&nbsp;хотите сначала поговорить с&nbsp;врачом</span>
      </li>
      <li class="scenarios__item">
        <span class="scenarios__check" aria-hidden="true">&#x2705;</span>
        <span class="scenarios__text">Хотите показать снимки или анализы специалисту узкого профиля</span>
      </li>
    </ul>
  </div>
</section>
```

**CSS** -- Append AFTER the advantages block in css/styles.css:

```css
/* -- Scenarios (When You Need a Consultation) -- */

.scenarios {
  background-color: var(--color-light);
}

.scenarios__heading {
  margin-bottom: var(--space-5);
}

.scenarios__list {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  max-width: 720px;
}

.scenarios__item {
  display: flex;
  align-items: flex-start;
  gap: var(--space-2);
  font-size: var(--font-size-lg);
  line-height: 1.5;
}

.scenarios__check {
  font-size: 1.5rem;
  line-height: 1;
  flex-shrink: 0;
  margin-top: 0.15em;
}

.scenarios__text {
  color: var(--color-text-primary);
}
```

Background: `var(--color-light)` to alternate with advantages section (white).
Uses `<ul>` with `role="list"` for semantics (existing reset pattern from CSS).
Each item uses green checkmark emoji (&#x2705;) as visual indicator per ТЗ.
  </action>
  <acceptance_criteria>
    - grep "section.*scenarios" index.html returns a match
    - grep "Когда имеет смысл" index.html returns a match
    - grep "серьёзный диагноз" index.html returns a match
    - grep "противоречивые рекомендации" index.html returns a match
    - grep "какое лечение подходит" index.html returns a match
    - grep "лечение за" index.html returns a match (рубежом scenario)
    - grep "специалисту узкого профиля" index.html returns a match
    - grep -c "scenarios__item" index.html returns 5
    - grep ".scenarios__list" css/styles.css returns a match
  </acceptance_criteria>
  <verify>
    <automated>grep -c "scenarios__item" index.html | grep -q "5" && grep -q "Когда имеет смысл" index.html && grep -q ".scenarios__list" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Scenarios section visible with 5 trigger items, each with green checkmark icon. List format, max-width 720px, responsive text sizing.</done>
</task>

</tasks>

<verification>
- Open index.html in browser -- advantages section appears after doctors, scenarios after advantages
- 4 advantage cards visible with correct Russian text from ТЗ
- 5 scenario items visible with checkmark icons
- Mobile: advantage cards stack, scenarios list readable
- Tablet (768px+): advantage cards in 2x2 grid
- All Russian text matches ТЗ verbatim
</verification>

<success_criteria>
- Advantages section exists with id="advantages" containing exactly 4 cards
- Card texts match ТЗ: document translation, consultation translation, app, treatment organization
- Scenarios section exists with id="scenarios" containing exactly 5 items
- Scenario texts match ТЗ verbatim (5 trigger scenarios)
- Both sections responsive across mobile/tablet/desktop
</success_criteria>

<output>
After completion, create `.planning/phases/04-trust-authority-sections/04-02-SUMMARY.md`
</output>
