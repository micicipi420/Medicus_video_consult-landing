---
phase: 13-section-layout-contrast
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
  - index.html
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02

must_haves:
  truths:
    - "No two adjacent sections share the same background color"
    - "Wave dividers are taller and more visually prominent than v1.0"
    - "FAQ section has white background (breaking the previous form-light/faq-light adjacency)"
  artifacts:
    - path: "css/styles.css"
      provides: "Updated section backgrounds, enhanced wave divider height"
      contains: ".faq"
    - path: "index.html"
      provides: "Corrected section-divider variant classes between form and FAQ"
      contains: "section-divider"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "section-divider class variants matching adjacent section backgrounds"
      pattern: "section-divider--"
---

<objective>
Establish clear visual hierarchy through alternating section backgrounds and enhanced wave dividers.

Purpose: Sections currently alternate white/light correctly except FAQ (light) follows Form (light). Fix adjacency violations and make wave dividers more prominent for stronger visual separation.
Output: All sections have correct alternating backgrounds; wave dividers are taller and more visible.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-section-layout-contrast/13-CONTEXT.md

@css/styles.css
@index.html
</context>

<interfaces>
<!-- Current section background assignments in css/styles.css -->
<!-- Section order in index.html (with current backgrounds): -->
<!--   hero (gradient) -> social-proof (dark) -> problem (white) -> benefits (light) -->
<!--   -> process (white) -> doctors (light) -> advantages (white) -> scenarios (light) -->
<!--   -> pricing (white) -> form (light) -> FAQ (light -- BUG) -> final-cta (dark) -->

From css/styles.css:
```css
.section-divider { height: 60px; }
.section-divider--light-to-white { background-color: var(--color-light); }
.section-divider--light-to-white svg { fill: var(--color-white); }
.section-divider--white-to-light { background-color: var(--color-white); }
.section-divider--white-to-light svg { fill: var(--color-light); }
.faq { background-color: var(--color-light); }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix section background alternation and add missing divider</name>
  <read_first>css/styles.css (lines 547-792, 874-878, 1034-1037), index.html (lines 574-577)</read_first>
  <files>css/styles.css, index.html</files>
  <action>
Fix the FAQ section background adjacency issue (per LAYOUT-01):

1. In css/styles.css, change `.faq` background from `var(--color-light)` to `var(--color-white)`:
   ```css
   .faq {
     background-color: var(--color-white);
   }
   ```

2. In index.html, add a wave divider between the form section (line ~574, after closing `</section>`) and the FAQ section. Insert:
   ```html
   <div class="section-divider section-divider--light-to-white" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 60" preserveAspectRatio="none">
       <path d="M0,0 C360,60 1080,0 1440,40 L1440,60 L0,60 Z"/>
     </svg>
   </div>
   ```

3. Also add a wave divider between FAQ (now white) and final-cta (dark). Insert before the final-cta section:
   ```html
   <div class="section-divider section-divider--white-to-dark" aria-hidden="true">
     <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 60" preserveAspectRatio="none">
       <path d="M0,0 C360,60 1080,0 1440,40 L1440,60 L0,60 Z"/>
     </svg>
   </div>
   ```

4. In css/styles.css, add the new divider variant for white-to-dark transition (after the existing divider styles around line 1478):
   ```css
   .section-divider--white-to-dark {
     background-color: var(--color-white);
   }
   .section-divider--white-to-dark svg {
     fill: var(--color-dark);
   }
   ```

Verify final section background sequence:
- hero (gradient) -> social-proof (#0E7490 dark) -> problem (white) -> benefits (light) -> process (white) -> doctors (light) -> advantages (white) -> scenarios (light) -> pricing (white) -> form (light) -> FAQ (white) -> final-cta (dark)

No two adjacent non-accent sections share the same background. The pricing (white) -> form (light) -> FAQ (white) pattern is correct.
  </action>
  <verify>
    <automated>grep -n "background-color.*var(--color-white)" css/styles.css | grep -q "faq" && grep -c "section-divider--light-to-white" index.html | grep -q "[3-9]" && grep -c "section-divider--white-to-dark" index.html | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep ".faq" in css/styles.css shows "background-color: var(--color-white)"
    - index.html contains a "section-divider--light-to-white" between form and FAQ sections
    - index.html contains a "section-divider--white-to-dark" between FAQ and final-cta sections
    - css/styles.css contains ".section-divider--white-to-dark" rule
  </acceptance_criteria>
  <done>No two adjacent sections share the same background color. Wave dividers exist between all section transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance wave divider prominence</name>
  <read_first>css/styles.css (lines 1448-1485)</read_first>
  <files>css/styles.css</files>
  <action>
Make wave dividers more visually prominent (per LAYOUT-02):

1. Increase `.section-divider` height from `60px` to `80px`:
   ```css
   .section-divider {
     display: block;
     width: 100%;
     height: 80px;
     position: relative;
     overflow: hidden;
     line-height: 0;
   }
   ```

2. Update the SVG path in ALL section-divider elements in index.html to use a more pronounced wave curve. Replace the existing path in each divider SVG:
   - Old viewBox: `viewBox="0 0 1440 60"`
   - New viewBox: `viewBox="0 0 1440 80"`
   - Old path: `d="M0,0 C360,60 1080,0 1440,40 L1440,60 L0,60 Z"`
   - New path: `d="M0,0 C240,80 480,10 720,50 C960,90 1200,20 1440,60 L1440,80 L0,80 Z"`

   This creates a double-curve wave instead of the single-curve, making it visually richer.

3. Add a subtle drop shadow to divider SVGs for depth (in css/styles.css):
   ```css
   .section-divider svg {
     display: block;
     width: 100%;
     height: 100%;
     position: absolute;
     bottom: 0;
     left: 0;
     filter: drop-shadow(0 -2px 4px rgba(0, 0, 0, 0.04));
   }
   ```

The `filter: drop-shadow` adds subtle depth without being heavy-handed, appropriate for the calm medical tone.
  </action>
  <verify>
    <automated>grep -q "height: 80px" css/styles.css && grep -q "drop-shadow" css/styles.css && grep -c 'viewBox="0 0 1440 80"' index.html | grep -q "[6-9]\|1[0-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - css/styles.css `.section-divider` has `height: 80px`
    - css/styles.css `.section-divider svg` contains `filter: drop-shadow(0 -2px 4px rgba(0, 0, 0, 0.04))`
    - All section-divider SVGs in index.html use `viewBox="0 0 1440 80"` and the new double-curve path
  </acceptance_criteria>
  <done>Wave dividers are 33% taller (80px vs 60px), use a richer double-curve wave shape, and have subtle drop shadows for depth.</done>
</task>

</tasks>

<verification>
1. Open index.html in browser -- section backgrounds alternate: no two adjacent sections share the same color
2. Wave dividers are visibly taller and more prominent than before
3. New dividers between form/FAQ and FAQ/final-cta render correctly
4. Mobile view: dividers still look clean at narrow widths
</verification>

<success_criteria>
- All section backgrounds alternate correctly (white/light/white/light pattern with accent sections preserved)
- Wave dividers are 80px tall with double-curve shape and subtle shadow
- Dividers exist between all adjacent sections including form-to-FAQ and FAQ-to-final-cta
</success_criteria>

<output>
After completion, create `.planning/phases/13-section-layout-contrast/13-01-SUMMARY.md`
</output>
