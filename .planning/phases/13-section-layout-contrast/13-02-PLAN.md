---
phase: 13-section-layout-contrast
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - css/styles.css
  - index.html
autonomous: true
requirements:
  - LAYOUT-03
  - LAYOUT-04

must_haves:
  truths:
    - "Pricing card is horizontally centered on desktop with visual emphasis"
    - "Pricing card displays a badge label above the price"
    - "Form section on desktop shows two columns: info on left, form on right"
    - "Form section maintains single column on mobile"
  artifacts:
    - path: "css/styles.css"
      provides: "Pricing card centering, shadow, badge styles; form two-column grid"
      contains: "pricing__badge"
    - path: "index.html"
      provides: "Pricing badge HTML, form two-column wrapper structure"
      contains: "pricing__badge"
  key_links:
    - from: "css/styles.css"
      to: "index.html"
      via: "pricing__badge class and lead-form grid layout"
      pattern: "pricing__badge|lead-form__grid"
---

<objective>
Center and emphasize the pricing card, and restructure the form section into a two-column desktop layout.

Purpose: The pricing card needs visual prominence as the key conversion element. The form section benefits from a split layout that shows trust signals alongside form fields on wider screens.
Output: Centered pricing card with badge and shadow; two-column form layout on desktop.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-section-layout-contrast/13-CONTEXT.md
@.planning/phases/13-section-layout-contrast/13-01-SUMMARY.md

@css/styles.css
@index.html
</context>

<interfaces>
<!-- Current pricing card structure (index.html lines 446-501): -->
<!-- section.pricing > .container > h2 + p + .pricing__card -->
<!-- pricing__card has: max-width: 600px, border: 2px solid primary, no margin auto -->

<!-- Current form structure (index.html lines 510-574): -->
<!-- section.lead-form-section > .container > .lead-form__wrapper > h2 + p + form + p.privacy + div.success -->
<!-- lead-form__wrapper has: max-width: 540px, single column -->

From css/styles.css:
```css
.pricing__card {
  background-color: var(--color-light);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
  max-width: 600px;
  border: 2px solid var(--color-primary);
}
.lead-form__wrapper {
  max-width: 540px;
  position: relative;
  z-index: 1;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Center pricing card with badge and visual emphasis</name>
  <read_first>css/styles.css (lines 791-870), index.html (lines 446-501)</read_first>
  <files>css/styles.css, index.html</files>
  <action>
Enhance the pricing card per LAYOUT-03 and CONTEXT.md decisions:

1. In css/styles.css, update `.pricing__card` to center and add shadow:
   ```css
   .pricing__card {
     background-color: var(--color-light);
     border-radius: var(--radius-lg);
     padding: var(--space-5);
     max-width: 520px;
     margin: 0 auto;
     border: 2px solid var(--color-primary);
     border-left: 4px solid var(--color-primary);
     box-shadow: 0 8px 32px rgba(56, 198, 244, 0.12), 0 2px 8px rgba(0, 0, 0, 0.06);
     position: relative;
   }
   ```

2. Add badge styles in css/styles.css (after `.pricing__card` rule):
   ```css
   .pricing__badge {
     display: inline-block;
     background-color: var(--color-primary);
     color: var(--color-text-on-primary);
     font-family: var(--font-heading);
     font-size: var(--font-size-sm);
     font-weight: 700;
     padding: 4px 16px;
     border-radius: var(--radius-md) var(--radius-md) 0 0;
     margin-bottom: 0;
     position: absolute;
     top: -28px;
     left: var(--space-5);
   }
   ```

3. In css/styles.css, add top margin to `.pricing__card` for badge space:
   Add `margin-top: var(--space-5);` to `.pricing__card` (so the badge has room above).

4. In css/styles.css, center the pricing heading and description text:
   ```css
   .pricing__heading {
     margin-bottom: var(--space-3);
     text-align: center;
   }
   .pricing__description {
     font-size: var(--font-size-lg);
     max-width: 640px;
     margin-left: auto;
     margin-right: auto;
     margin-bottom: var(--space-5);
     text-align: center;
   }
   ```

5. In index.html, add badge element inside `.pricing__card` as the first child (before `.pricing__price-block`):
   ```html
   <span class="pricing__badge">Все включено</span>
   ```

The badge text uses "Все включено" per CONTEXT.md specifics suggestion.
  </action>
  <verify>
    <automated>grep -q "pricing__badge" css/styles.css && grep -q "pricing__badge" index.html && grep -q "margin: 0 auto" css/styles.css && grep -q "box-shadow" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - css/styles.css contains `.pricing__badge` rule with `background-color: var(--color-primary)`
    - css/styles.css `.pricing__card` contains `margin: 0 auto` (or `margin-top` + `margin-left: auto; margin-right: auto`)
    - css/styles.css `.pricing__card` contains `box-shadow` with rgba values
    - css/styles.css `.pricing__card` contains `max-width: 520px`
    - css/styles.css `.pricing__card` contains `border-left: 4px solid`
    - index.html contains `<span class="pricing__badge">` inside `.pricing__card`
    - css/styles.css `.pricing__heading` and `.pricing__description` contain `text-align: center`
  </acceptance_criteria>
  <done>Pricing card is centered (max-width 520px, margin auto), has a prominent shadow, left border accent, and a "Все включено" badge positioned at the top.</done>
</task>

<task type="auto">
  <name>Task 2: Two-column form layout on desktop</name>
  <read_first>css/styles.css (lines 874-1030), index.html (lines 510-574)</read_first>
  <files>css/styles.css, index.html</files>
  <action>
Restructure the form section into a two-column desktop layout per LAYOUT-04 and CONTEXT.md decisions:

1. In index.html, restructure the form section content. Replace the current `.lead-form__wrapper` div with a two-column grid wrapper. The new structure inside `.container`:

   ```html
   <div class="lead-form__grid">
     <div class="lead-form__info">
       <h2 class="lead-form__heading">Оставить заявку на&nbsp;консультацию</h2>
       <p class="lead-form__subtext">Заполните форму&nbsp;&mdash; мы&nbsp;перезвоним в&nbsp;течение 24&nbsp;часов.</p>
       <ul class="lead-form__trust" role="list">
         <li class="lead-form__trust-item">
           <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="none" width="20" height="20" aria-hidden="true">
             <path d="M4 10l4 4 8-8" stroke="#047857" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
           </svg>
           <span>Перезвоним в течение 24 часов</span>
         </li>
         <li class="lead-form__trust-item">
           <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="none" width="20" height="20" aria-hidden="true">
             <path d="M4 10l4 4 8-8" stroke="#047857" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
           </svg>
           <span>Бесплатно и без обязательств</span>
         </li>
         <li class="lead-form__trust-item">
           <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="none" width="20" height="20" aria-hidden="true">
             <path d="M4 10l4 4 8-8" stroke="#047857" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
           </svg>
           <span>Ваши данные защищены</span>
         </li>
       </ul>
     </div>
     <div class="lead-form__wrapper">
       <!-- existing form element (#lead-form) stays here unchanged -->
       <!-- existing .lead-form__privacy stays here -->
       <!-- existing .lead-form__success stays here -->
     </div>
   </div>
   ```

   Move the heading and subtext into `.lead-form__info`. Keep the form, privacy note, and success message in `.lead-form__wrapper`. Remove the old `.lead-form__micro` paragraph (its content "Бесплатно и без обязательств" is now in the trust list).

2. In css/styles.css, add the grid layout styles (after the existing `.lead-form-section` block):

   ```css
   .lead-form__grid {
     display: flex;
     flex-direction: column;
     gap: var(--space-5);
     position: relative;
     z-index: 1;
   }

   .lead-form__info {
     max-width: 540px;
   }

   .lead-form__trust {
     list-style: none;
     padding: 0;
     margin: var(--space-4) 0 0 0;
     display: flex;
     flex-direction: column;
     gap: var(--space-2);
   }

   .lead-form__trust-item {
     display: flex;
     align-items: center;
     gap: var(--space-2);
     font-size: var(--font-size-base);
     color: var(--color-text-primary);
   }

   .lead-form__trust-item svg {
     flex-shrink: 0;
     color: var(--color-secondary-dark);
   }
   ```

3. Update the existing `.lead-form__wrapper` to remove `position: relative; z-index: 1;` (now on grid parent):
   ```css
   .lead-form__wrapper {
     max-width: 540px;
   }
   ```

4. Add the desktop two-column grid in the existing `@media (min-width: 768px)` block:
   ```css
   .lead-form__grid {
     display: grid;
     grid-template-columns: 1fr 1fr;
     gap: var(--space-6);
     align-items: start;
   }
   .lead-form__info {
     max-width: none;
     padding-top: var(--space-3);
   }
   .lead-form__wrapper {
     max-width: none;
   }
   ```

5. Update the existing 768px media query for `.lead-form__wrapper` -- remove the `max-width: 540px` override since grid controls width now.

The mobile layout stays single-column (flex-direction: column). Desktop switches to CSS grid two columns.
  </action>
  <verify>
    <automated>grep -q "lead-form__grid" css/styles.css && grep -q "lead-form__grid" index.html && grep -q "lead-form__info" index.html && grep -q "lead-form__trust" css/styles.css && grep -q "grid-template-columns: 1fr 1fr" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `class="lead-form__grid"` wrapping the form section content
    - index.html contains `class="lead-form__info"` with heading, subtext, and trust list
    - index.html contains `class="lead-form__trust"` with 3 list items (24 hours, free, data protected)
    - css/styles.css contains `.lead-form__grid` with `grid-template-columns: 1fr 1fr` inside 768px media query
    - css/styles.css contains `.lead-form__trust-item` with `display: flex`
    - Mobile: `.lead-form__grid` uses `flex-direction: column` (default)
    - The existing `#lead-form` form element and its fields are unchanged
  </acceptance_criteria>
  <done>Form section shows two columns on desktop (info/trust on left, form fields on right). Mobile remains single column. Trust signals are prominently displayed next to the form.</done>
</task>

</tasks>

<verification>
1. Open index.html on desktop -- pricing card is centered with badge and shadow
2. Open index.html on desktop -- form section shows two columns with trust signals on the left
3. Resize to mobile width -- form collapses to single column
4. Pricing badge "Все включено" is visible above the card
5. All existing form functionality (validation, submission) still works
</verification>

<success_criteria>
- Pricing card centered on desktop (max-width 520px, margin auto) with badge and shadow
- Form section uses two-column grid on desktop (768px+), single column on mobile
- Trust signals visible in the left column of the form section
- No regression in form submission behavior
</success_criteria>

<output>
After completion, create `.planning/phases/13-section-layout-contrast/13-02-SUMMARY.md`
</output>
