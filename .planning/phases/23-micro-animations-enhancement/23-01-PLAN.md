---
phase: 23-micro-animations-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
autonomous: false
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03

must_haves:
  truths:
    - "Scroll-reveal elements slide up from 20px offset while fading in — visibly more dynamic than opacity-only fade"
    - "Pressing and holding a CTA button shows scale(0.97) — tactile confirmation of click registered"
    - "With OS prefers-reduced-motion enabled, elements appear at final position immediately — no transform offset, no animation"
    - "After 10 seconds on the loaded page with no interaction, nothing on the page is moving"
    - "Total distinct animation types on page is ≤5"
  artifacts:
    - path: "css/styles.css"
      provides: "translateY(20px) initial state, scale(0.97) active state, reduced-motion transform resets, stagger delay fix"
      contains: "translateY(20px)"
  key_links:
    - from: "css/styles.css (.animate-on-scroll)"
      to: "css/styles.css (@media prefers-reduced-motion)"
      via: "transform: none reset in reduced-motion block"
      pattern: "prefers-reduced-motion.*animate-on-scroll"
    - from: "css/styles.css (.button:active)"
      to: "css/styles.css (@media prefers-reduced-motion)"
      via: "transform: none reset for button active state"
      pattern: "prefers-reduced-motion.*button.*active"
---

<objective>
Extend the existing scroll-reveal animation with a vertical slide component, add tactile button active feedback, fix stagger delays above the 100ms ceiling, and confirm the prefers-reduced-motion guard covers all new and existing transform rules.

Purpose: Phase 23 requirement — animation types stay ≤5, vestibular safety maintained, reduced-motion users see zero motion.
Output: css/styles.css with updated scroll-reveal (translateY 20px, 400ms), button active (scale 0.97, 100ms), corrected stagger delays, and explicit transform resets in the reduced-motion block.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-micro-animations-enhancement/23-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update scroll-reveal to translateY(20px) at 400ms and fix stagger delays (ANIM-01)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css lines 1650–1740 (Section 10: Animations — full block)
    - css/styles.css lines 1655–1673 (existing .animate-on-scroll + stagger-children rules)
  </read_first>
  <action>
    Modify the existing rules in Section 10 of css/styles.css (around lines 1655–1673).

    FINDING: The current codebase already has `transform: translateY(24px)` in `.animate-on-scroll` and `transition: opacity 600ms ease-out, transform 600ms ease-out`. ANIM-01 requires changing to 20px and 400ms.

    CHANGE 1 — `.animate-on-scroll` initial state (currently line 1655):
    Change `transform: translateY(24px)` → `transform: translateY(20px)`
    Change `transition: opacity 600ms ease-out, transform 600ms ease-out` → `transition: opacity 0.4s ease-out, transform 0.4s ease-out`

    Result after change:
    ```css
    .animate-on-scroll {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.4s ease-out, transform 0.4s ease-out;
    }
    ```

    CHANGE 2 — `.animate-on-scroll.is-visible` (currently line 1661):
    Already correct (`opacity: 1; transform: translateY(0);`) — no change needed. Verify only.

    CHANGE 3 — Stagger delays (currently lines 1667–1673):
    Current delays: 0ms, 100ms, 200ms, 300ms, 400ms, 500ms, 600ms
    PITFALL: nth-child(3) through nth-child(7) exceed the 100ms-per-child ceiling from CONTEXT.md.
    Fix: stagger each child by 80ms (within the ≤100ms ceiling):
    ```css
    /* Stagger children in grids (per D-11): 80ms delay per child */
    .stagger-children > .animate-on-scroll:nth-child(1) { transition-delay: 0ms; }
    .stagger-children > .animate-on-scroll:nth-child(2) { transition-delay: 80ms; }
    .stagger-children > .animate-on-scroll:nth-child(3) { transition-delay: 160ms; }
    .stagger-children > .animate-on-scroll:nth-child(4) { transition-delay: 240ms; }
    .stagger-children > .animate-on-scroll:nth-child(5) { transition-delay: 320ms; }
    .stagger-children > .animate-on-scroll:nth-child(6) { transition-delay: 400ms; }
    .stagger-children > .animate-on-scroll:nth-child(7) { transition-delay: 480ms; }
    ```

    NOTE: The comment says "100ms delay per child" — update to "80ms delay per child".
  </action>
  <verify>
    <automated>grep -n "translateY(20px)" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css && grep -n "0.4s ease-out" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css && grep -n "transition-delay: 80ms" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "translateY(20px)" css/styles.css` returns a match in the `.animate-on-scroll` block
    - `grep "0.4s ease-out" css/styles.css` returns exactly 2 matches (opacity and transform)
    - `grep "translateY(24px)" css/styles.css` returns NO matches (old value removed)
    - `grep "600ms ease-out" css/styles.css` returns NO matches (old duration removed)
    - `grep "transition-delay: 80ms" css/styles.css` returns a match (stagger nth-child(2) fixed)
    - `grep "transition-delay: 480ms" css/styles.css` returns a match (stagger nth-child(7) fixed)
    - `grep "transition-delay: 600ms" css/styles.css` returns NO matches (old max stagger removed)
  </acceptance_criteria>
  <done>`.animate-on-scroll` has `transform: translateY(20px)` with `0.4s ease-out` transition. Stagger delay per child is 80ms (max 480ms for 7th child). No 24px or 600ms values remain.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add button :active scale(0.97) tactile feedback (ANIM-02)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css lines 354–375 (`.button` base rule and existing transition)
    - css/styles.css lines 1675–1685 (Section 10: existing `.button:hover` and `.button:active` rules)
  </read_first>
  <action>
    FINDING: The existing `.button:active` rule is at approximately line 1682 in Section 10:
    ```css
    .button:active {
      transform: translateY(0) scale(0.98);
    }
    ```
    This combines a translateY reset with scale — it was paired with the hover lift above it.
    ANIM-02 requires changing to a standalone `scale(0.97)` for CTA tactile feedback.

    The `.button` base rule at line 369 already includes `transform var(--transition-fast)` in its transition (150ms). ANIM-02 requires 100ms specifically for the active feedback. Add an explicit `transform 100ms ease` override on `.button:active` using the `transition` property.

    CHANGE 1 — Update `.button:active` in Section 10 (around line 1682):
    ```css
    .button:active {
      transform: scale(0.97);
      transition: transform 100ms ease;
    }
    ```

    IMPORTANT: The selector `.button:active` is correct — all CTAs in index.html use class `button` (not `.btn`). Do NOT create new `.btn` or `.btn--cta` selectors. The existing `.button:active` rule is the correct place.

    Do NOT apply scale to:
    - `.theme-toggle` (dark mode toggle — already uses separate class)
    - Navigation `<a>` links without `.button` class
    - Accordion trigger buttons (if any use `.button` — check index.html)

    CHANGE 2 — The `.button--outline` variant inherits `.button:active` via cascade. No separate rule needed for `button--outline:active`.
  </action>
  <verify>
    <automated>grep -n "scale(0.97)" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css && grep -n "100ms ease" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "scale(0.97)" css/styles.css` returns a match in `.button:active` rule
    - `grep "scale(0.98)" css/styles.css` returns NO matches (old value replaced)
    - `grep "100ms ease" css/styles.css` returns a match inside `.button:active` block
    - `.button:active` block does NOT contain `translateY` (the hover-lift reset is removed — hover lift on `.button:hover` is already guarded in the reduced-motion block)
  </acceptance_criteria>
  <done>`.button:active` has `transform: scale(0.97)` with `transition: transform 100ms ease`. Old `scale(0.98)` and `translateY(0)` combination removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend prefers-reduced-motion block with button:active transform reset (ANIM-03)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css lines 263–271 (global reduced-motion block at top of file)
    - css/styles.css lines 1715–1739 (Section 10 reduced-motion block — animation-specific overrides)
  </read_first>
  <action>
    FINDING: The Section 10 reduced-motion block at approximately line 1717 already handles:
    - `.animate-on-scroll { opacity: 1; transform: none; transition: none; }` — CORRECT (ANIM-01 is covered)
    - `.faq__answer { transition: none; }` — existing
    - `.pricing__card .button--primary { animation: none; }` — existing
    - `.button:hover { transform: none; }` — existing
    - `.card:hover { transform: none; }` — existing

    MISSING: `.button:active { transform: none; }` — Task 2 adds `scale(0.97)` to `.button:active` which must be reset for reduced-motion users.

    CHANGE 1 — Add `.button:active` transform reset inside the Section 10 `@media (prefers-reduced-motion: reduce)` block, immediately after the existing `.button:hover { transform: none; }` rule:

    ```css
    .button:active {
      transform: none;
    }
    ```

    VERIFICATION CHECK: After this change, the Section 10 reduced-motion block must contain ALL of:
    1. `.animate-on-scroll` — `opacity: 1; transform: none; transition: none;`
    2. `.faq__answer` — `transition: none;`
    3. `.pricing__card .button--primary` — `animation: none;`
    4. `.button:hover` — `transform: none;`
    5. `.button:active` — `transform: none;` (NEWLY ADDED)
    6. `.card:hover` — `transform: none;`

    Do NOT modify the global reduced-motion block at lines 263–271 (the `*, *::before, *::after` duration-zeroing block). That block handles timing. The Section 10 block handles the initial-state problem.

    ANIMATION CATALOGUE AUDIT — verify ≤5 types exist after this phase:
    1. Scroll fade-in + slide-up: `.animate-on-scroll` opacity + translateY (ANIM-01)
    2. FAQ accordion: `.faq__answer` max-height transition (existing)
    3. Card hover lift: `.card:hover { transform: translateY(-2px) }` (existing)
    4. Button active scale: `.button:active { transform: scale(0.97) }` (ANIM-02)
    5. Pulse glow: `.pricing__card .button--primary` @keyframes pulse-glow — runs 3 times then stops (existing, not looping continuously)

    Total: 5 types. Within ≤5 limit. Do NOT add any more animation types.
  </action>
  <verify>
    <automated>grep -n "button:active" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A2 "button:active" css/styles.css` shows TWO `.button:active` blocks: one with `scale(0.97)` (from Task 2), one with `transform: none` (inside the reduced-motion media query)
    - The `transform: none` block for `.button:active` is inside `@media (prefers-reduced-motion: reduce)`
    - `grep -c "prefers-reduced-motion" css/styles.css` returns 2 (one global, one Section 10)
    - `grep "translateY(20px)" css/styles.css` still returns a match (Task 1 not regressed)
    - `grep "scale(0.97)" css/styles.css` still returns a match (Task 2 not regressed)
  </acceptance_criteria>
  <done>The Section 10 reduced-motion block contains `.button:active { transform: none; }`. All new transform properties introduced in Tasks 1 and 2 are explicitly guarded. Animation catalogue audited at ≤5 types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual and accessibility verification of all animation changes</name>
  <what-built>
    Three CSS changes to css/styles.css:
    1. (ANIM-01) scroll-reveal updated to translateY(20px) with 400ms ease-out; stagger delays reduced to 80ms per child
    2. (ANIM-02) .button:active changed to scale(0.97) with 100ms ease transition
    3. (ANIM-03) prefers-reduced-motion block extended with .button:active { transform: none }
  </what-built>
  <how-to-verify>
    Open index.html in a browser (or via local server). Perform each check in order:

    CHECK 1 — Scroll reveal animation (ANIM-01):
    - Scroll slowly through the page from top to bottom
    - Every section element with .animate-on-scroll should slide up from a slight offset while fading in
    - The motion should feel smooth and subtle — not a large jump
    - Verify: no elements stuck in offset position, no layout shifts

    CHECK 2 — Button active feedback (ANIM-02):
    - Click and hold any CTA button (hero "Получить консультацию", pricing "Получить консультацию", sticky bar "Оставить заявку")
    - While holding the click, the button should visibly shrink slightly (scale 0.97)
    - Release — button returns to normal size
    - Verify: navigation links do NOT show this effect, dark mode toggle does NOT show this effect

    CHECK 3 — Animation at rest (no looping):
    - Load the page and wait 10 seconds without interaction
    - Nothing on the page should be moving after the initial scroll-reveals have completed
    - Specifically verify the pricing card button has stopped its pulse-glow animation

    CHECK 4 — prefers-reduced-motion (ANIM-03):
    - Enable "Reduce Motion" in OS accessibility settings (Mac: System Settings → Accessibility → Motion → Reduce Motion)
    - Hard refresh the page
    - Scroll through the entire page — all elements should appear immediately at their final positions
    - No fade-in, no slide-up, no button scale on click
    - Disable Reduce Motion again when done

    CHECK 5 — Animation count audit:
    - Count distinct animation types visible during normal browsing: scroll reveal, FAQ accordion, card hover, button active scale, pricing pulse (5 max)
    - Confirm total is ≤5

    CHECK 6 — Full page layout check at 390px and 1440px:
    - No layout shifts from the transform changes
    - All 11 sections render correctly at both widths
  </how-to-verify>
  <files>css/styles.css</files>
  <action>Human visual and accessibility verification of the CSS changes made in Tasks 1-3. No code changes in this task — verify the results of prior tasks.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; automated checks completed in Tasks 1-3</automated>
  </verify>
  <done>All 6 checks pass: scroll-reveal slides up from 20px, CTA buttons show scale(0.97) on press, no looping animations at rest, prefers-reduced-motion shows elements at final position, animation count ≤5, no layout shifts at 390px and 1440px.</done>
  <resume-signal>Type "approved" if all 6 checks pass. Describe any failing check if issues found.</resume-signal>
</task>

</tasks>

<verification>
All three requirements verified:
- ANIM-01: `grep "translateY(20px)" css/styles.css` returns match; `grep "600ms" css/styles.css` returns no animation-related matches
- ANIM-02: `grep "scale(0.97)" css/styles.css` returns match inside `.button:active`
- ANIM-03: `grep -A2 "button:active" css/styles.css` shows a `transform: none` block inside prefers-reduced-motion; animation catalogue count ≤5
</verification>

<success_criteria>
1. Scroll-reveal elements slide up from 20px while fading in — 400ms ease-out, visibly more dynamic than the prior 600ms opacity-only fade with 24px offset
2. CTA buttons show scale(0.97) on press-and-hold — 100ms transition confirms tactile click registration
3. With OS prefers-reduced-motion enabled: elements appear at final position immediately, no transforms visible
4. After 10 seconds idle on loaded page: nothing moving
5. Stagger delay between sibling animated elements: ≤80ms per child
6. Total distinct animation types: ≤5
</success_criteria>

<output>
After completion, create `.planning/phases/23-micro-animations-enhancement/23-01-SUMMARY.md`
</output>
