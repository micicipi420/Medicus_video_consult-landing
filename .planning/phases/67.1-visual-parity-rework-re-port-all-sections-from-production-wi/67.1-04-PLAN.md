---
phase: 67.1-visual-parity-rework
plan: 04
type: execute
wave: 3
depends_on: ["67.1-01", "67.1-03"]
files_modified:
  - next/src/app/consultations/page.tsx
  - next/src/components/sections/consultations/ConsultationProblem.tsx
  - next/src/components/sections/consultations/ConsultationBenefits.tsx
  - next/src/components/sections/consultations/ConsultationProcess.tsx
  - next/src/components/sections/consultations/ConsultationDoctors.tsx
  - next/src/components/sections/consultations/ConsultationAdvantages.tsx
  - next/src/components/sections/consultations/ConsultationScenarios.tsx
  - next/src/components/sections/consultations/ConsultationPricing.tsx
autonomous: true
requirements:
  - VP-07

must_haves:
  truths:
    - "Visiting /consultations renders a full service page matching production consultations.html"
    - "Page has all sections: Hero, SocialProof, Problem, Benefits, Process, Doctors, Advantages, Scenarios, Pricing, LeadForm, FAQ, FinalCTA"
    - "Page is statically generated (SSG)"
    - "All text content matches production consultations.html verbatim"
  artifacts:
    - path: "next/src/app/consultations/page.tsx"
      provides: "Consultations service page"
      contains: "ConsultationsPage"
    - path: "next/src/components/sections/consultations/ConsultationProblem.tsx"
      provides: "Problem section matching production"
      contains: "Знакомо"
  key_links:
    - from: "next/src/app/consultations/page.tsx"
      to: "next/src/components/sections/service/ServiceHero.tsx"
      via: "imports shared ServiceHero"
      pattern: "import.*ServiceHero"
    - from: "next/src/app/consultations/page.tsx"
      to: "next/src/components/sections/service/FAQ.tsx"
      via: "imports shared FAQ"
      pattern: "import.*FAQ"
    - from: "next/src/app/consultations/page.tsx"
      to: "next/src/components/sections/FinalCTA.tsx"
      via: "imports shared FinalCTA"
      pattern: "import.*FinalCTA"
---

<objective>
Create the consultations service page by porting all sections from production consultations.html to React components, using shared components from Plan 03 and production-matching styling from Plan 01.

Purpose: The consultations page is one of three service pages missing from the Next.js migration. It is linked from the services grid on the index page and must exist to prevent 404 errors and provide the full consultation service information.
Output: Complete /consultations page with all 12 sections matching production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-CONTEXT.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-01-SUMMARY.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-03-SUMMARY.md

<interfaces>
<!-- Shared components from Plan 03 -->
From next/src/components/sections/service/ServiceHero.tsx:
  export function ServiceHero(props: ServiceHeroProps): JSX.Element;

From next/src/components/sections/service/SocialProof.tsx:
  export function SocialProof(props: { items: SocialProofItem[] }): JSX.Element;

From next/src/components/sections/service/FAQ.tsx:
  export function FAQ(props: { heading?: string; items: FAQItem[]; id?: string }): JSX.Element;

From next/src/components/sections/service/LeadFormSection.tsx:
  export function LeadFormSection(props: LeadFormSectionProps): JSX.Element;

<!-- FinalCTA shared component from Plan 02 -->
From next/src/components/sections/FinalCTA.tsx:
  export function FinalCTA(): JSX.Element;
  // Renders navy bg section with btn-primary + btn-outline btn-outline-light buttons
  // Props: none (uses fixed content for index page)
  // For service pages: import and render, OR pass custom props if component supports them

<!-- Shared navigation -->
From next/src/lib/navigation.ts:
  export const PHONE_NUMBER: string;
  export const PHONE_DISPLAY: string;

<!-- CSS classes from Plan 01 -->
.btn-primary, .btn-outline, .btn-hero, .card-prod
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create consultations page-specific section components</name>
  <files>next/src/components/sections/consultations/ConsultationProblem.tsx, next/src/components/sections/consultations/ConsultationBenefits.tsx, next/src/components/sections/consultations/ConsultationProcess.tsx, next/src/components/sections/consultations/ConsultationDoctors.tsx, next/src/components/sections/consultations/ConsultationAdvantages.tsx, next/src/components/sections/consultations/ConsultationScenarios.tsx, next/src/components/sections/consultations/ConsultationPricing.tsx</files>
  <read_first>
    - consultations.html (full file -- read all sections)
    - css/styles.css (search for .problem, .benefits, .process, .doctors, .advantages, .scenarios, .pricing)
    - .planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
  </read_first>
  <action>
Create directory `next/src/components/sections/consultations/`.

Read consultations.html in full to extract ALL section content verbatim. Each component is a Server Component (no "use client") unless it needs client interactivity.

All components must follow the production CSS patterns:
- Section padding: `py-12 lg:py-[6.25rem]`
- Container: `max-w-[1200px] mx-auto px-4 md:px-8`
- H2: `font-heading text-[clamp(1.75rem,3.5vw,2.75rem)] font-bold text-[#18212C] leading-[1.2] tracking-[-0.02em] text-balance`
- H3: `font-heading text-[clamp(1.375rem,2.5vw,2rem)] font-bold text-[#18212C] leading-[1.2]`
- Body text: `text-[1.125rem] text-[rgba(24,33,44,0.55)] leading-relaxed`
- Cards: `card-prod p-8` class
- Buttons: `btn-primary` or `btn-outline` classes

**ConsultationProblem.tsx** (section id="problem"):
- Port the "Знакомо?" section from consultations.html lines ~187-234
- Background: alternating section bg (read production CSS for `.problem` background -- likely white or light)
- Content: H2 heading, list of problem scenarios with icons, each scenario is a card or list item
- Copy ALL text verbatim from HTML, converting HTML entities (&nbsp; -> \u00A0, &mdash; -> \u2014)
- Apply nbsp rules from project memory: bind all subject+verb pairs

**ConsultationBenefits.tsx** (section id="benefits"):
- Port the benefits/advantages section from consultations.html lines ~236-295
- Content: H2 heading, grid of benefit cards with icons, title, description
- Use `card-prod` for cards if production uses `.card` class, or plain styling if production uses a different pattern

**ConsultationProcess.tsx** (section id="process"):
- Port the "how it works" / process section from consultations.html lines ~297-325
- Content: H2 heading, numbered steps (01, 02, 03...) with titles and descriptions
- Use production step numbering pattern

**ConsultationDoctors.tsx** (section id="doctors"):
- Port the doctors section from consultations.html lines ~327-384
- Content: H2 heading, doctor cards with specialty, country, description
- Use `card-prod` for cards

**ConsultationAdvantages.tsx** (section id="advantages"):
- Port the advantages section from consultations.html lines ~386-444
- Content: H2 "Почему через MedicusUnion", grid of advantage items with icons
- Use `card-prod` for cards

**ConsultationScenarios.tsx** (section id="scenarios"):
- Port the scenarios section from consultations.html lines ~446-504
- Content: scenario cards showing different use cases

**ConsultationPricing.tsx** (section id="pricing"):
- Port the pricing section from consultations.html lines ~506-569
- Content: pricing tiers/cards with features lists
- Use `card-prod` for pricing cards

For ALL components:
- Use semantic HTML (section, h2, h3, ul, li, p)
- Preserve ALL production text content exactly (including nbsp entities and em-dashes)
- Include `aria-hidden="true"` on decorative icons (matching production)
- Use production SVG icons inline or create simple icon components
- Apply production spacing tokens from globals.css
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && ls src/components/sections/consultations/*.tsx | wc -l && grep -q "Знакомо" src/components/sections/consultations/ConsultationProblem.tsx && grep -q "Почему через MedicusUnion" src/components/sections/consultations/ConsultationAdvantages.tsx && grep -q "export function" src/components/sections/consultations/ConsultationPricing.tsx && echo "All consultation components OK"</automated>
  </verify>
  <acceptance_criteria>
    - 7 component files exist in next/src/components/sections/consultations/
    - Each component exports a named function
    - ConsultationProblem.tsx contains "Знакомо" (production heading)
    - ConsultationAdvantages.tsx contains "Почему через MedicusUnion"
    - All components contain `lg:py-[6.25rem]` for section padding
    - All components use `max-w-[1200px]` container
    - All components use production typography values (clamp() for headings)
    - No components contain `liquid-` or `squircle-` classes
    - Cards use `card-prod` class where production uses `.card`
    - All Russian text content matches production HTML verbatim (spot-check 3 headings)
  </acceptance_criteria>
  <done>7 consultation-specific section components exist, each matching production consultations.html content and styling verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Compose consultations page with all sections + metadata</name>
  <files>next/src/app/consultations/page.tsx</files>
  <read_first>
    - consultations.html (for section order and metadata)
    - next/src/app/page.tsx (for pattern reference)
    - next/src/components/sections/consultations/ConsultationProblem.tsx
    - next/src/components/sections/service/ServiceHero.tsx
    - next/src/components/sections/service/FAQ.tsx
    - next/src/components/sections/service/LeadFormSection.tsx
    - next/src/components/sections/FinalCTA.tsx
  </read_first>
  <action>
Create `next/src/app/consultations/page.tsx` as a Server Component page.

**Metadata** (from production consultations.html head):
```typescript
export const metadata: Metadata = {
  title: 'Консультации с европейскими врачами',
  description: 'Видеоконсультация с европейским врачом из дома. Второе мнение по диагнозу, перевод документов, письменное заключение. От 450\u20AC, за 5 дней.',
  alternates: { canonical: '/consultations' },
  openGraph: {
    title: 'MedicusUnion KZ \u2014 Консультации с европейскими врачами',
    description: 'Видеоконсультация с европейским врачом из дома. Второе мнение по диагнозу, перевод документов, письменное заключение. От 450\u20AC.',
    url: '/consultations',
  },
};
```

**Section composition order** (matching consultations.html):
1. `<ServiceHero>` -- with consultations hero content:
   - No eyebrow
   - title: "Мнение немецкого врача\u00A0\u2014 за\u00A05\u00A0дней, без\u00A0перелёта"
   - subtitle: "Видеоконсультация с\u00A0европейским специалистом\u00A0\u2014 на\u00A0вашем языке, с\u00A0переводом..."
   - primaryCta: { text: "Получить консультацию\u00A0\u2014 от\u00A0450\u00A0\u20AC", href: "#form" }
   - secondaryCta: { text: "Узнать, подходит\u00A0ли мой случай", href: "#form" }
   - illustration: the doctor-at-laptop SVG from production (port the SVG inline)
2. `<SocialProof>` -- items: [{ number: "7", label: "стран" }, { number: "50+", label: "врачей" }, { number: "15+", label: "специализаций" }]
3. `<ConsultationProblem />`
4. `<ConsultationBenefits />`
5. `<ConsultationProcess />`
6. `<ConsultationDoctors />`
7. `<ConsultationAdvantages />`
8. `<ConsultationScenarios />`
9. `<ConsultationPricing />`
10. `<LeadFormSection>` -- with consultations-specific copy from production:
    - heading: "Оставить заявку на\u00A0консультацию"
    - subtext: "Заполните форму\u00A0\u2014 мы\u00A0перезвоним в\u00A0течение 24\u00A0часов."
    - trustItems matching production trust list items
11. `<FAQ>` -- with consultations FAQ items (6 questions from production)
12. `<FinalCTA />` -- import from `@/components/sections/FinalCTA` and render. Do NOT inline the final CTA JSX. If the shared FinalCTA component needs page-specific text, check if it accepts props; if not, use it as-is (the shared component from Plan 02 already has production-matching styling).

Wrap content sections (except Hero) in `<ScrollReveal>` for entrance animations (per CONTEXT.md decision to keep Framer Motion).
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && grep -q "export default" src/app/consultations/page.tsx && grep -q "ServiceHero" src/app/consultations/page.tsx && grep -q "FAQ" src/app/consultations/page.tsx && grep -q "LeadFormSection" src/app/consultations/page.tsx && grep -q "import.*FinalCTA.*from.*@/components/sections/FinalCTA" src/app/consultations/page.tsx && grep -q "metadata" src/app/consultations/page.tsx && echo "Consultations page OK"</automated>
  </verify>
  <acceptance_criteria>
    - next/src/app/consultations/page.tsx exists and has default export
    - Page imports and renders ServiceHero, SocialProof, FAQ, LeadFormSection from shared service components
    - Page imports FinalCTA from `@/components/sections/FinalCTA` (NOT inlined JSX)
    - Page imports and renders all 7 consultation-specific section components
    - Page has Metadata export with title "Консультации с европейскими врачами"
    - Page has canonical URL '/consultations'
    - All sections are wrapped in ScrollReveal (except Hero)
    - `npm run build` shows /consultations as a static page
    - curl localhost:3000/consultations returns 200 (when dev server running)
  </acceptance_criteria>
  <done>The /consultations page exists with all 12 sections matching production, proper SEO metadata, and static generation. FinalCTA is imported from the shared component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. The page reuses existing ContactForm with its established validation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-04 | N/A | Consultations page | accept | Static content page, no new input handling |
</threat_model>

<verification>
1. `npm run build` succeeds and shows `/consultations` as static
2. All 12 sections render in correct order
3. Text content matches production consultations.html
4. Clicking service card CTA on index page navigates to /consultations (no 404)
</verification>

<success_criteria>
- /consultations page renders all sections from production
- All text content matches production verbatim
- Page is statically generated
- Production typography, button styles, card styles, and spacing are used throughout
- FinalCTA is imported from shared component, not inlined
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-04-SUMMARY.md`
</output>
