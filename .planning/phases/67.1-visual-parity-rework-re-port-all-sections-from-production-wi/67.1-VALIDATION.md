---
phase: 67.1
slug: visual-parity-rework-re-port-all-sections-from-production-wi
status: active
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-11
---

# Phase 67.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | grep/ls file content verification + `npm run build` compilation check |
| **Config file** | none — plans use inline grep commands against source files |
| **Quick run command** | `grep -q "pattern" file && echo OK` |
| **Full suite command** | `npm run build` (compilation validates all imports and types) |
| **Estimated runtime** | ~30 seconds (grep), ~60 seconds (build) |

---

## Sampling Rate

- **After every task commit:** Run task-specific grep/ls verify commands
- **After every plan wave:** `npm run build` must succeed
- **Before `/gsd-verify-work`:** Full build + manual visual comparison at 1440px, 768px, 375px
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| P01-T1 | 01 | 1 | VP-01, VP-02 | T-67.1-01 | N/A | grep | `grep -c "color-cta" globals.css && grep -c "btn-primary" globals.css && grep -c "font-size-h1" globals.css && grep -c "card-prod" globals.css` | N/A (modifies existing) | ⬜ pending |
| P01-T2 | 01 | 1 | VP-05, VP-06 | T-67.1-01 | N/A | grep | `grep -q "shadow-\[0_2px_8px" HeaderClient.tsx && grep -q "#1A365D" Footer.tsx && grep -q "btn-primary" StickyBar.tsx && grep -q "IntersectionObserver" StickyBar.tsx` | N/A (modifies existing) | ⬜ pending |
| P01-T3 | 01 | 1 | VP-08 | T-67.1-01 | N/A | grep | `grep -q "site-header__nav" MobileMenu.tsx && grep -q "Консультации" MobileMenu.tsx && grep -q "Лечение" MobileMenu.tsx` | N/A (modifies existing) | ⬜ pending |
| P02-T1 | 02 | 2 | VP-01, VP-02, VP-03 | T-67.1-02 | N/A | grep | `grep -q "btn-primary btn-hero" HeroHub.tsx && grep -q "clamp(2.5rem,5vw,3.5rem)" HeroHub.tsx && grep -q "bg-white" ServicesGrid.tsx && grep -q "card-prod" ServicesGrid.tsx && grep -q "#FFF8F0" GuideGrid.tsx` | N/A (modifies existing) | ⬜ pending |
| P02-T2 | 02 | 2 | VP-02, VP-04, VP-06 | T-67.1-02 | N/A | grep | `grep -q "#F0F7FF" AdvantagesGrid.tsx && grep -q "card-prod" AdvantagesGrid.tsx && grep -q "#F5F7F9" ContactSection.tsx && grep -q "btn-outline btn-outline-light" FinalCTA.tsx` | N/A (modifies existing) | ⬜ pending |
| P03-T1 | 03 | 2 | VP-07 | T-67.1-03 | N/A | ls+grep | `ls ServiceHero.tsx SocialProof.tsx FAQ.tsx && grep -q "ServiceHeroProps" ServiceHero.tsx && grep -q "aria-expanded" FAQ.tsx` | creates new | ⬜ pending |
| P03-T2 | 03 | 2 | VP-09 | T-67.1-03 | N/A | grep | `grep -q "LeadFormSection" contacts/page.tsx && grep -qv "CoordinatorCard" contacts/page.tsx` | creates new (LeadFormSection) | ⬜ pending |
| P04-T1 | 04 | 3 | VP-07 | T-67.1-04 | N/A | ls+grep | `ls consultations/*.tsx \| wc -l && grep -q "Знакомо" ConsultationProblem.tsx && grep -q "Почему через MedicusUnion" ConsultationAdvantages.tsx` | creates new | ⬜ pending |
| P04-T2 | 04 | 3 | VP-07 | T-67.1-04 | N/A | grep | `grep -q "ServiceHero" consultations/page.tsx && grep -q "FAQ" consultations/page.tsx && grep -q "FinalCTA" consultations/page.tsx` | creates new | ⬜ pending |
| P05-T1 | 05 | 3 | VP-07 | T-67.1-05 | N/A | ls+grep | `ls treatment/*.tsx \| wc -l && grep -q "надёжный партнёр" TreatmentAboutUs.tsx && grep -q "Планирование" TreatmentSteps.tsx` | creates new | ⬜ pending |
| P05-T2 | 05 | 3 | VP-07 | T-67.1-05 | N/A | grep | `grep -q "ServiceHero" treatment-abroad/page.tsx && grep -q "TreatmentClinics" treatment-abroad/page.tsx && grep -q "FinalCTA" treatment-abroad/page.tsx` | creates new | ⬜ pending |
| P06-T1 | 06 | 3 | VP-07 | T-67.1-06 | N/A | ls+grep | `ls checkup/*.tsx \| wc -l && grep -q "Ноблесс" CheckupProgramsKorea.tsx && grep -q "Liv Hospital" CheckupProgramsTurkey.tsx` | creates new | ⬜ pending |
| P06-T2 | 06 | 3 | VP-07 | T-67.1-06 | N/A | grep | `grep -q "ServiceHero" checkup/page.tsx && grep -q "CheckupProgramsKorea" checkup/page.tsx && grep -q "FinalCTA" checkup/page.tsx` | creates new | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. All plans use grep/ls verification against source file content and `npm run build` for compilation checks. No additional test framework setup needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 1:1 visual parity with production | VP-01 through VP-06 | Pixel-level visual comparison requires human judgment | Compare side-by-side screenshots of each section at 1440px, 768px, 375px viewports |
| Service page content completeness | VP-07 | Verifying all Russian text content matches production requires reading | Open each service page, compare section headings and body text with production HTML |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready
