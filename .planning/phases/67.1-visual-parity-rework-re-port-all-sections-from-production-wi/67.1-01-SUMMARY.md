---
phase: 67.1-visual-parity-rework
plan: 01
subsystem: layout-chrome
tags: [css-tokens, header, footer, sticky-bar, mobile-menu, design-system]
dependency_graph:
  requires: []
  provides:
    - "Production CSS tokens (typography, colors, spacing, shadows, radii) in globals.css"
    - "Button utility classes (btn-primary, btn-outline, btn-hero, btn-outline-light, btn-sticky)"
    - "Card utility class (card-prod)"
    - "Container utility class (container-prod)"
    - "Header with box-shadow scroll behavior (no glass)"
    - "Footer with navy #1A365D background"
    - "StickyBar with solid green CTA and IntersectionObserver hide behavior"
    - "MobileMenu with production-matching styling (no glass blur)"
  affects:
    - "All section components (consume CSS tokens)"
    - "Plans 67.1-02 through 67.1-06 (depend on these foundations)"
tech_stack:
  added: []
  patterns:
    - "CSS custom properties for production design tokens"
    - "IntersectionObserver for sticky bar visibility toggle"
    - "Solid green CTA (#35B678) replacing gradient buttons"
key_files:
  created: []
  modified:
    - next/src/app/globals.css
    - next/src/components/layout/HeaderClient.tsx
    - next/src/components/layout/Footer.tsx
    - next/src/components/layout/StickyBar.tsx
    - next/src/components/layout/MobileMenu.tsx
decisions:
  - "Production tokens added alongside existing :root block (not replacing) to avoid breaking Tailwind/shadcn"
  - "radius-prod-sm/md/lg namespaced to avoid conflict with shadcn radius-sm/md/lg"
  - "StickyBar uses Set-based IntersectionObserver tracking for multiple observed elements"
metrics:
  duration: "4 minutes"
  completed: "2026-04-11T11:20:28Z"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 5
---

# Phase 67.1 Plan 01: Design Tokens and Layout Chrome Summary

Production design tokens and layout chrome components updated to match production visual specs -- solid green CTA buttons, navy footer, box-shadow header scroll, IntersectionObserver-driven sticky bar hide behavior.

## What Was Done

### Task 1: Production Design Tokens and Utility Classes
- Added 8 typography tokens (xs through display) with production clamp values
- Added 4 letter-spacing tokens (tight, normal, wide, wider)
- Added 13 color tokens (CTA, navy, backgrounds, text variants, badge colors)
- Added 3 shadow tokens, 3 radius tokens, 8 spacing tokens plus section-padding-desktop
- Created button utility classes: `.btn-primary`, `.btn-outline`, `.btn-hero`, `.btn-outline-light`, `.btn-sticky`
- Created card utility class: `.card-prod` with subtle border and shadow
- Created container utility class: `.container-prod` with 1200px max-width
- All existing CSS (Tailwind imports, liquid-glass imports, shadcn, dark mode) preserved

### Task 2: Header, Footer, StickyBar
- **Header**: Replaced `liquid-nav header--scrolled` glass classes with `bg-white` + `shadow-[0_2px_8px_rgba(0,0,0,0.08)]` on scroll
- **Footer**: Changed background from `bg-mu-text-900` (#1B212C) to `bg-[#1A365D]` (navy). Updated typography sizes and link hover to `#2B6CB0`
- **StickyBar**: Replaced gradient CTA (`bg-gradient-to-r from-mu-cta-from to-mu-cta-to`) with `btn-primary btn-sticky`. Added IntersectionObserver to hide bar when `#contact` section or footer is visible. Updated phone styling to `font-bold text-[#1A4D80]`

### Task 3: MobileMenu
- Removed glass blur from nav panel (`bg-white` instead of `bg-white/95 backdrop-blur-lg`)
- Removed backdrop blur from overlay (kept `bg-black/30`)
- Updated nav link styling to production typography: `text-[1.125rem] text-[#18212C] hover:bg-[#F0F7FF]`
- Updated phone link: `font-heading font-bold text-[#1A4D80]`
- Updated separator: `border-black/[0.06]`
- Preserved body scroll lock, aria-expanded, click-outside-to-close

## Deviations from Plan

None -- plan executed exactly as written.

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 | 1536761 | feat(67.1-01): add production design tokens and utility classes to globals.css |
| 2 | 82cf850 | feat(67.1-01): fix Header scroll, Footer color, and StickyBar to match production |
| 3 | be78855 | feat(67.1-01): update MobileMenu to match production navigation behavior |

## Verification

- `npm run build`: PASS (compiled successfully, 8 static pages generated)
- All acceptance criteria verified via grep checks
- No glass classes remain in layout chrome components
- Production tokens accessible via CSS custom properties for downstream plans

## Self-Check: PASSED

All 5 modified files exist. All 3 task commits verified.
