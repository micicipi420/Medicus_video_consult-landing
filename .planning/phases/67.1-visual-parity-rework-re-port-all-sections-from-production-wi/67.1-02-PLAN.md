---
phase: 67.1-visual-parity-rework
plan: 02
type: execute
wave: 2
depends_on: ["67.1-01"]
files_modified:
  - next/src/components/sections/HeroHub.tsx
  - next/src/components/sections/StatsBar.tsx
  - next/src/components/sections/ServicesGrid.tsx
  - next/src/components/sections/GuideGrid.tsx
  - next/src/components/sections/AdvantagesGrid.tsx
  - next/src/components/sections/ContactSection.tsx
  - next/src/components/sections/FinalCTA.tsx
autonomous: true
requirements:
  - VP-01
  - VP-02
  - VP-03
  - VP-04
  - VP-06
  - VP-08

must_haves:
  truths:
    - "Hero H1 uses clamp(2.5rem,5vw,3.5rem) for 40-56px range"
    - "All H2 headings use clamp(1.75rem,3.5vw,2.75rem) for 28-44px range"
    - "All CTA buttons are solid green #35B678 (not gradient glass)"
    - "Section backgrounds use flat production colors (white, cream, blue tint, gray, navy)"
    - "Cards use flat white style with border and shadow (not glass material)"
    - "Service badges are pill-shaped (rounded-full) not squircle"
  artifacts:
    - path: "next/src/components/sections/HeroHub.tsx"
      provides: "Hero with production typography and solid buttons"
      contains: "btn-primary btn-hero"
    - path: "next/src/components/sections/ServicesGrid.tsx"
      provides: "Services with flat cards and solid green CTAs"
      contains: "card-prod"
    - path: "next/src/components/sections/GuideGrid.tsx"
      provides: "Guide with cream background and no glass"
      contains: "#FFF8F0"
    - path: "next/src/components/sections/AdvantagesGrid.tsx"
      provides: "Advantages with blue tint background and flat cards"
      contains: "#F0F7FF"
    - path: "next/src/components/sections/FinalCTA.tsx"
      provides: "Final CTA with production button styles"
      contains: "btn-outline-light"
  key_links:
    - from: "next/src/components/sections/HeroHub.tsx"
      to: "globals.css"
      via: "btn-primary, btn-hero CSS classes"
      pattern: "btn-primary|btn-hero"
    - from: "next/src/components/sections/ServicesGrid.tsx"
      to: "globals.css"
      via: "card-prod, btn-primary CSS classes"
      pattern: "card-prod|btn-primary"
---

<objective>
Update all 7 index page section components to achieve 1:1 visual parity with the production vanilla HTML/CSS site. Replace glass materials, gradient buttons, and generic Tailwind typography with production-matching values.

Purpose: The index page is the primary conversion surface. Its visual styling must match the production site exactly -- same typography scale, button styles, section backgrounds, card styles, and spacing.
Output: 7 updated section components that visually match production at 1440px, 768px, and 375px viewports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-CONTEXT.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-RESEARCH.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-01-SUMMARY.md

<interfaces>
<!-- CSS classes available from Plan 01 -->
.btn-primary — solid green #35B678 button, min-height 48px, 18px Manrope bold, 8px radius
.btn-outline — transparent bg, 2px solid #18212C border, same sizing as primary
.btn-hero — 56px min-height, 20px font, full-width mobile / auto tablet+
.btn-outline-light — white border/text variant for dark backgrounds
.card-prod — white bg, 16px radius, 1px border rgba(0,0,0,0.06), shadow-sm, hover shadow-md

<!-- Production typography tokens from globals.css -->
--font-size-h1: clamp(2.5rem, 5vw, 3.5rem)    /* 40-56px */
--font-size-h2: clamp(1.75rem, 3.5vw, 2.75rem)  /* 28-44px */
--font-size-h3: clamp(1.375rem, 2.5vw, 2rem)    /* 22-32px */
--font-size-base: 1.125rem                       /* 18px */
--font-size-lg: 1.25rem                          /* 20px */
--font-size-sm: 1rem                             /* 16px */
--section-padding-desktop: 6.25rem               /* 100px */

<!-- Production section backgrounds -->
Hero: linear-gradient(180deg, #F0F7FF 0%, #FFFFFF 100%)
Stats: #1A365D (navy)
Services: #FFFFFF (white)
Guide: #FFF8F0 (cream)
Advantages: #F0F7FF (blue tint)
Contact: #F5F7F9 (gray)
FinalCTA: #1A365D (navy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HeroHub, StatsBar, ServicesGrid, and GuideGrid to production parity</name>
  <files>next/src/components/sections/HeroHub.tsx, next/src/components/sections/StatsBar.tsx, next/src/components/sections/ServicesGrid.tsx, next/src/components/sections/GuideGrid.tsx</files>
  <read_first>
    - next/src/components/sections/HeroHub.tsx
    - next/src/components/sections/StatsBar.tsx
    - next/src/components/sections/ServicesGrid.tsx
    - next/src/components/sections/GuideGrid.tsx
    - index.html
    - .planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
  </read_first>
  <action>
**HeroHub.tsx** -- Apply production values (divergence items 1, 2, 6, 18, 20):
- Section padding: Change `py-16 md:py-24 lg:py-32` to `pt-20 pb-12 lg:pt-[5rem] lg:pb-12` (80px top, 48px bottom -- production uses var(--space-10) top, var(--space-6) bottom)
- Container max-width: Change `max-w-3xl` to `max-w-[800px]` (production hero__content max-width is 800px)
- H1 font size: Change `text-3xl md:text-4xl lg:text-5xl` to `text-[clamp(2.5rem,5vw,3.5rem)]` (production --font-size-h1: 40-56px)
- H1 line-height: Change `leading-tight` to `leading-[1.1]` (production heading line-height)
- H1 letter-spacing: Add `tracking-[-0.02em]` (production --letter-spacing-tight)
- H1 text-wrap: Add `text-balance` class (production uses text-wrap: balance on headings)
- Subtitle font: Change `text-lg md:text-xl` to `text-[1.25rem]` (production --font-size-lg = 20px) and add `max-w-[720px] mx-auto`
- Subtitle line-height: Keep `leading-relaxed` (1.625 close to production 1.6)
- Buttons: Replace BOTH `liquid-btn-primary squircle-md inline-flex items-center justify-center px-8 py-3.5 text-base font-semibold` with `btn-primary btn-hero` class. Replace `liquid-btn-secondary squircle-md ...` with `btn-outline btn-hero`. Remove ALL liquid/squircle classes from buttons.
- Button wrapper: Keep `flex flex-col sm:flex-row items-center justify-center gap-4`
- Background: Keep `bg-gradient-to-b from-[#F0F7FF] to-white` (already matches production)
- Keep the HeroEntrance animation wrappers (per CONTEXT.md decision to keep Framer Motion)
- Body text color: Keep `text-mu-text-500` (production uses muted text for subtitle)

**StatsBar.tsx** -- Verify/adjust (mostly correct already):
- Section bg: Keep `bg-[#1A365D]` (matches production)
- Section padding: Change `py-8` to `py-8 lg:py-8` (production stats bar padding is 32px at all breakpoints)
- Numbers font: Keep `text-[2.5rem]` (matches production --font-size-display)
- Label font: Change `text-lg` to `text-[1.125rem]` (production --font-size-base = 18px). This is the same as `text-lg` in Tailwind so may already match, but be explicit.
- Gap: Change `gap-6 md:gap-16` to `gap-6 md:gap-16` (keep -- 24px mobile, 64px desktop matches production --space-3 / --space-8)
- Label opacity: Keep `text-white/85` (production uses `rgba(255, 255, 255, 0.85)`)

**ServicesGrid.tsx** -- Major changes (divergence items 7, 8, 9, 26):
- Section background: Change `section-tint-cool` to `bg-white` (production uses plain white)
- Section padding: Change `py-12 md:py-24` to `py-12 lg:py-[6.25rem]` (48px mobile, 100px desktop at 1024px+)
- H2 font: Change `text-2xl md:text-3xl` to `text-[clamp(1.75rem,3.5vw,2.75rem)]` (production --font-size-h2: 28-44px). Add `leading-[1.2] tracking-[-0.02em] text-balance`
- Card style: Change `liquid-card squircle-lg p-6 md:p-8` to `card-prod p-8 md:p-10 text-center` (production hub-service uses 40px 32px padding = p-10 p-8, and text-align: center)
- Icon: Change `w-16 h-16` to `w-[72px] h-[72px]` (production icon is 72x72)
- Badge: Change `squircle-md` to `rounded-full` (production badge is pill shape, border-radius: 9999px). Change `text-sm` to `text-[1rem]` (production badge font is 16px). Add `font-heading font-semibold`
- H3 font: Change `text-xl` to `text-[clamp(1.375rem,2.5vw,2rem)]` (production --font-size-h3). Add `leading-[1.2]`
- Body text: Change to `text-[1.125rem]` (18px), add `max-w-[360px] mx-auto` (production hub-service text max-width)
- Feature list: Change `text-sm` to `text-[1rem]` (production feature list is 16px). Add green dot bullets via a `before:` pseudo or inline SVG circle (production uses `::before` with 8px green circles). Use `text-left` alignment for features.
- CTA button: Change `liquid-btn-primary squircle-md inline-flex items-center justify-center w-full px-6 py-3 text-sm font-semibold` to `btn-primary w-full md:w-auto` (solid green, full-width mobile, auto tablet+)
- Card hover: Add `hover:-translate-y-1 hover:shadow-[0_4px_16px_rgba(0,0,0,0.10)]` for production translateY(-4px) + shadow-lg effect
- Grid gap: Change `gap-6 md:gap-8` to `gap-8` (production 32px gap)

**GuideGrid.tsx** -- Significant changes (divergence items 10, 11):
- Section background: Change `section-tint-warm` to `bg-[#FFF8F0]` (production --color-bg-cream)
- Section padding: Change `py-12 md:py-24` to `py-12 lg:py-[6.25rem]`
- H2 font: Change `text-2xl md:text-3xl` to `text-[clamp(1.75rem,3.5vw,2.75rem)]`. Add `leading-[1.2] tracking-[-0.02em] text-balance`
- Card style: Change `liquid-regular squircle-lg p-6 md:p-8` to just `p-6 md:p-8` -- production guide items are plain flex columns with NO glass, NO card background, NO border. They are just content blocks within the grid cell.
- H3 font: Change `text-lg` to `text-[clamp(1.375rem,2.5vw,2rem)]`. Add `leading-[1.2]`
- Body text: add `text-[1.125rem]`
- Link color: Change `text-[#0B7A9A]` to `text-[#1A4D80]` (production uses --color-primary-dark). Change hover to `hover:text-[#18212C]` (production --color-dark)
- Grid gap: Keep `gap-6 md:gap-8`
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && grep -q "btn-primary btn-hero" src/components/sections/HeroHub.tsx && echo "Hero btns OK" && grep -q "clamp(2.5rem,5vw,3.5rem)" src/components/sections/HeroHub.tsx && echo "Hero H1 OK" && grep -q "bg-white" src/components/sections/ServicesGrid.tsx && echo "Services bg OK" && grep -q "card-prod" src/components/sections/ServicesGrid.tsx && echo "Services card OK" && grep -q "#FFF8F0" src/components/sections/GuideGrid.tsx && echo "Guide bg OK" && grep -q "rounded-full" src/components/sections/ServicesGrid.tsx && echo "Badge pill OK"</automated>
  </verify>
  <acceptance_criteria>
    - HeroHub.tsx contains `text-[clamp(2.5rem,5vw,3.5rem)]` for H1
    - HeroHub.tsx contains `leading-[1.1]` for H1
    - HeroHub.tsx contains `btn-primary btn-hero` (NOT `liquid-btn-primary`)
    - HeroHub.tsx contains `btn-outline btn-hero` (NOT `liquid-btn-secondary`)
    - HeroHub.tsx does NOT contain `liquid-btn` or `squircle-md` on buttons
    - HeroHub.tsx contains `max-w-[800px]` (NOT `max-w-3xl`)
    - HeroHub.tsx contains `pt-20 pb-12`
    - ServicesGrid.tsx contains `bg-white` section class (NOT `section-tint-cool`)
    - ServicesGrid.tsx contains `card-prod` (NOT `liquid-card squircle-lg`)
    - ServicesGrid.tsx contains `rounded-full` on badge (NOT `squircle-md`)
    - ServicesGrid.tsx contains `btn-primary` on CTA (NOT `liquid-btn-primary`)
    - ServicesGrid.tsx contains `text-[clamp(1.75rem,3.5vw,2.75rem)]` for H2
    - GuideGrid.tsx contains `bg-[#FFF8F0]` (NOT `section-tint-warm`)
    - GuideGrid.tsx does NOT contain `liquid-regular` or `squircle-lg`
    - GuideGrid.tsx contains `text-[#1A4D80]` for link color
    - StatsBar.tsx contains `text-[1.125rem]` for label
    - All 4 section components use `lg:py-[6.25rem]` for desktop padding (except Hero which has custom padding, and Stats which keeps py-8)
  </acceptance_criteria>
  <done>HeroHub, StatsBar, ServicesGrid, and GuideGrid match production typography, button styles, section backgrounds, and card styles. No glass material or gradient buttons remain on these sections.</done>
</task>

<task type="auto">
  <name>Task 2: Update AdvantagesGrid, ContactSection, and FinalCTA to production parity</name>
  <files>next/src/components/sections/AdvantagesGrid.tsx, next/src/components/sections/ContactSection.tsx, next/src/components/sections/FinalCTA.tsx</files>
  <read_first>
    - next/src/components/sections/AdvantagesGrid.tsx
    - next/src/components/sections/ContactSection.tsx
    - next/src/components/sections/FinalCTA.tsx
    - index.html
    - .planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
  </read_first>
  <action>
**AdvantagesGrid.tsx** -- Changes (divergence items 12, 13):
- Section background: Change `section-tint-mint` to `bg-[#F0F7FF]` (production --color-bg-blue)
- Section padding: Change `py-12 md:py-24` to `py-12 lg:py-[6.25rem]`
- H2 font: Change `text-2xl md:text-3xl` to `text-[clamp(1.75rem,3.5vw,2.75rem)]`. Add `leading-[1.2] tracking-[-0.02em] text-balance`
- Card style: Change `liquid-regular squircle-lg p-6` to `card-prod p-8` (production .card uses 32px padding, white bg, border, shadow)
- H3 font: Change `text-lg` to `text-[clamp(1.375rem,2.5vw,2rem)]`. Add `leading-[1.2]`
- Body text: Change `text-sm` to `text-[1.125rem]` (production uses 18px body text)
- Grid: Change `grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6` to `grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8` (production 32px gap)
- Card hover: Add `hover:-translate-y-1 transition-transform` (production cards lift on hover)

**ContactSection.tsx** -- Changes (divergence items 14, 15):
- Section background: Change `section-tint-cool` to `bg-[#F5F7F9]` (production --color-bg-gray)
- Section padding: Change `py-12 md:py-24` to `py-12 lg:py-[6.25rem]`
- H2 font: Change `text-2xl md:text-3xl` to `text-[clamp(1.75rem,3.5vw,2.75rem)]`. Add `leading-[1.2] tracking-[-0.02em] text-balance`
- Left column heading: Production heading is "Оставьте заявку" or similar from index.html lead-form section. Read the exact copy from index.html `lead-form__heading`. Use that exact text.
- Left column body: Use production `lead-form__subtext` text verbatim from index.html.
- Trust items: Keep the existing green checkmark trust items, they match production pattern. Icon color should use `#047857` (production --color-secondary-dark).
- Form wrapper: Change `liquid-card squircle-xl p-8 md:p-10` to `bg-white border border-black/[0.08] rounded-2xl shadow-[0_2px_8px_rgba(0,0,0,0.08)] p-8 md:p-10` (production lead-form__wrapper style). Max-width on mobile: `max-w-[540px]`.
- Body text: add `text-[1.125rem]`
- Trust item text: keep current size
- Remove `relative` from section if present (production has `relative overflow-hidden` but the overflow-hidden is for decorative elements not present in Next.js)

**FinalCTA.tsx** -- Changes (divergence items 7, 22):
- Section padding: Change `py-12 md:py-24` to `py-12 lg:py-[6.25rem]`
- Container: Change `max-w-2xl` to `max-w-[640px]` (production final-cta container is 640px)
- H2 font: Change `text-2xl md:text-3xl` to `text-[clamp(1.75rem,3.5vw,2.75rem)]`. Add `leading-[1.2] tracking-[-0.02em] text-balance`
- Body text: Change `text-lg` to `text-[1.125rem]`. Change `text-white/80` to `text-white/85` (production rgba(255,255,255,0.85))
- Primary button: Change `liquid-btn-primary squircle-md inline-flex items-center justify-center px-8 py-3.5 text-base font-semibold` to `btn-primary`
- Secondary button: Change `liquid-btn-secondary squircle-md inline-flex items-center justify-center px-8 py-3.5 text-base font-semibold` to `btn-outline btn-outline-light`
- Remove ALL `liquid-btn-*` and `squircle-*` classes from buttons
- Production final CTA secondary button text: "Позвонить нам" (current has just "Позвонить" -- update to match)
- Button wrapper: Keep `flex flex-col sm:flex-row items-center justify-center gap-4`
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && grep -q "#F0F7FF" src/components/sections/AdvantagesGrid.tsx && echo "Advantages bg OK" && grep -q "card-prod" src/components/sections/AdvantagesGrid.tsx && echo "Advantages card OK" && grep -q "#F5F7F9" src/components/sections/ContactSection.tsx && echo "Contact bg OK" && grep -q "btn-outline btn-outline-light" src/components/sections/FinalCTA.tsx && echo "FinalCTA btn OK" && grep -q "btn-primary" src/components/sections/FinalCTA.tsx && echo "FinalCTA primary OK"</automated>
  </verify>
  <acceptance_criteria>
    - AdvantagesGrid.tsx contains `bg-[#F0F7FF]` (NOT `section-tint-mint`)
    - AdvantagesGrid.tsx contains `card-prod` (NOT `liquid-regular squircle-lg`)
    - AdvantagesGrid.tsx contains `text-[1.125rem]` for body text (NOT `text-sm`)
    - AdvantagesGrid.tsx contains `text-[clamp(1.75rem,3.5vw,2.75rem)]` for H2
    - ContactSection.tsx contains `bg-[#F5F7F9]` (NOT `section-tint-cool`)
    - ContactSection.tsx does NOT contain `liquid-card` or `squircle-xl`
    - ContactSection.tsx contains `shadow-[0_2px_8px_rgba(0,0,0,0.08)]` for form wrapper
    - ContactSection.tsx contains `text-[clamp(1.75rem,3.5vw,2.75rem)]` for H2
    - FinalCTA.tsx contains `btn-primary` (NOT `liquid-btn-primary`)
    - FinalCTA.tsx contains `btn-outline btn-outline-light` (NOT `liquid-btn-secondary`)
    - FinalCTA.tsx does NOT contain `liquid-btn` or `squircle`
    - FinalCTA.tsx contains `max-w-[640px]` (NOT `max-w-2xl`)
    - FinalCTA.tsx contains `text-[clamp(1.75rem,3.5vw,2.75rem)]` for H2
    - FinalCTA.tsx contains `Позвонить нам` (full production text)
    - All 3 section components use `lg:py-[6.25rem]` for desktop section padding
  </acceptance_criteria>
  <done>AdvantagesGrid, ContactSection, and FinalCTA match production visual specs. No glass material, gradient buttons, or section tint classes remain. All sections use production backgrounds, card styles, button styles, and typography.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. Visual-only changes to existing components.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-02 | N/A | Section components | accept | Visual-only changes, no security surface |
</threat_model>

<verification>
1. `npm run build` succeeds without errors
2. `npm run dev` renders index page with:
   - Hero H1 is visibly larger (40-56px range)
   - All buttons are solid green, not gradient
   - Services section has white background (no glass tint)
   - Guide section has cream #FFF8F0 background
   - Advantages section has blue tint #F0F7FF background
   - Contact section has gray #F5F7F9 background
   - Service card badges are pill-shaped (rounded) not squircle
   - Cards are flat white with subtle border/shadow
   - Final CTA secondary button has white border on navy background
3. Side-by-side comparison with production at 1440px, 768px, 375px shows matching layout
</verification>

<success_criteria>
- All 7 index page sections use production typography (clamp() headings, 18px body)
- All CTA buttons are solid green #35B678 (primary) or outline (secondary)
- Section backgrounds match production hex values exactly
- Cards use flat white style, not glass material
- Service badges are pill-shaped
- Desktop section padding is 100px (6.25rem) at 1024px+
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-02-SUMMARY.md`
</output>
