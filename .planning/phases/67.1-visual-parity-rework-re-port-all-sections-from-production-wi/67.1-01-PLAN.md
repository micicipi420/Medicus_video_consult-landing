---
phase: 67.1-visual-parity-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/app/globals.css
  - next/src/components/layout/HeaderClient.tsx
  - next/src/components/layout/Footer.tsx
  - next/src/components/layout/StickyBar.tsx
autonomous: true
requirements:
  - VP-01
  - VP-02
  - VP-05
  - VP-06

must_haves:
  truths:
    - "Production design tokens (typography, spacing, colors) are available as CSS classes in globals.css"
    - "Header shows box-shadow on scroll instead of glass material"
    - "Footer background is navy #1A365D not near-black #1B212C"
    - "Sticky bar uses solid green CTA button and hides near form/footer"
    - "Button utility classes (.btn-primary, .btn-outline) match production solid styles"
  artifacts:
    - path: "next/src/app/globals.css"
      provides: "Production design tokens, button classes, card classes, typography utilities"
      contains: "--color-cta: #35B678"
    - path: "next/src/components/layout/HeaderClient.tsx"
      provides: "Header with box-shadow scroll behavior"
      contains: "shadow"
    - path: "next/src/components/layout/Footer.tsx"
      provides: "Footer with navy background"
      contains: "#1A365D"
    - path: "next/src/components/layout/StickyBar.tsx"
      provides: "Sticky bar with solid green CTA and hide behavior"
      contains: "#35B678"
  key_links:
    - from: "next/src/app/globals.css"
      to: "all section components"
      via: "CSS custom properties and utility classes"
      pattern: "--color-cta|--font-size-base|btn-primary"
---

<objective>
Establish production design tokens in globals.css and fix layout chrome (Header, Footer, StickyBar) to match production visual specs.

Purpose: All subsequent plans depend on these CSS foundations -- production typography scale, button classes, card classes, and spacing tokens must exist before section components can reference them.
Output: Updated globals.css with production tokens + 3 fixed layout components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
@.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-CONTEXT.md

<interfaces>
<!-- Production CSS source of truth -->
From css/styles.css (production design tokens):
  --font-size-xs: 0.8125rem (13px)
  --font-size-sm: 1rem (16px)
  --font-size-base: 1.125rem (18px)
  --font-size-lg: 1.25rem (20px)
  --font-size-h3: clamp(1.375rem, 2.5vw, 2rem) (22-32px)
  --font-size-h2: clamp(1.75rem, 3.5vw, 2.75rem) (28-44px)
  --font-size-h1: clamp(2.5rem, 5vw, 3.5rem) (40-56px)
  --font-size-display: 2.5rem (40px)
  --color-cta: #35B678
  --color-cta-hover: #25A467
  --color-navy: #1A365D
  --color-bg-blue: #F0F7FF
  --color-bg-cream: #FFF8F0
  --color-bg-gray: #F5F7F9
  --color-text-primary: #18212C
  --color-text-muted: rgba(24, 33, 44, 0.55)
  --color-primary-dark: #1A4D80
  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06)
  --shadow-md: 0 2px 8px rgba(0, 0, 0, 0.08)
  --shadow-lg: 0 4px 16px rgba(0, 0, 0, 0.10)
  --radius-md: 8px
  --radius-lg: 16px

From next/src/lib/navigation.ts:
  export const PHONE_NUMBER = '+77015322478';
  export const PHONE_DISPLAY = '+7\u00A0701\u00A0532\u00A024\u00A078';

From next/src/hooks/use-scrolled.ts:
  export function useScrolled(): boolean;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add production design tokens and utility classes to globals.css</name>
  <files>next/src/app/globals.css</files>
  <read_first>
    - next/src/app/globals.css
    - css/styles.css
    - .planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
  </read_first>
  <action>
Add production design tokens and utility CSS classes to globals.css. Keep ALL existing content (Tailwind imports, liquid glass tokens, shadcn, dark mode, etc.) intact. Add the following AFTER the existing `:root` block but before the dark mode section:

1. **Production typography tokens** -- add to the existing `:root` block:
```css
/* Production typography tokens (67.1 visual parity) */
--font-size-xs: 0.8125rem;   /* 13px */
--font-size-sm: 1rem;         /* 16px */
--font-size-base: 1.125rem;   /* 18px */
--font-size-lg: 1.25rem;      /* 20px */
--font-size-h3: clamp(1.375rem, 2.5vw, 2rem);   /* 22-32px */
--font-size-h2: clamp(1.75rem, 3.5vw, 2.75rem);  /* 28-44px */
--font-size-h1: clamp(2.5rem, 5vw, 3.5rem);      /* 40-56px */
--font-size-display: 2.5rem;  /* 40px */

/* Production letter spacing */
--letter-spacing-tight: -0.02em;
--letter-spacing-normal: 0;
--letter-spacing-wide: 0.04em;
--letter-spacing-wider: 0.08em;

/* Production color tokens */
--color-cta: #35B678;
--color-cta-hover: #25A467;
--color-text-on-cta: #FFFFFF;
--color-navy: #1A365D;
--color-bg-blue: #F0F7FF;
--color-bg-cream: #FFF8F0;
--color-bg-gray: #F5F7F9;
--color-text-primary: #18212C;
--color-text-muted: rgba(24, 33, 44, 0.55);
--color-primary-dark: #1A4D80;
--color-secondary-dark: #047857;
--color-badge-bg: #d0fae4;
--color-badge-text: #007955;

/* Production shadow tokens */
--shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06);
--shadow-md: 0 2px 8px rgba(0, 0, 0, 0.08);
--shadow-lg: 0 4px 16px rgba(0, 0, 0, 0.10);

/* Production radius tokens */
--radius-prod-sm: 4px;
--radius-prod-md: 8px;
--radius-prod-lg: 16px;

/* Production spacing tokens */
--space-1: 0.5rem;   /* 8px */
--space-2: 1rem;     /* 16px */
--space-3: 1.5rem;   /* 24px */
--space-4: 2rem;     /* 32px */
--space-5: 2.5rem;   /* 40px */
--space-6: 3rem;     /* 48px */
--space-8: 4rem;     /* 64px */
--space-10: 5rem;    /* 80px */
--section-padding-desktop: 6.25rem; /* 100px */
```

2. **Button utility classes** -- add in a `@layer components` block:
```css
/* Production button styles (67.1 visual parity) */
.btn-primary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 48px;
  min-width: 48px;
  padding: 16px 32px;
  background-color: var(--color-cta);
  color: var(--color-text-on-cta);
  font-family: var(--font-family-heading);
  font-size: var(--font-size-base);
  font-weight: 700;
  line-height: 1.2;
  border: none;
  border-radius: var(--radius-prod-md);
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  transition: background-color 250ms ease, transform 150ms ease;
}
.btn-primary:hover {
  background-color: var(--color-cta-hover);
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}
.btn-primary:active {
  transform: scale(0.97);
}

.btn-outline {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 48px;
  min-width: 48px;
  padding: 16px 32px;
  background-color: transparent;
  color: var(--color-text-primary);
  font-family: var(--font-family-heading);
  font-size: var(--font-size-base);
  font-weight: 700;
  line-height: 1.2;
  border: 2px solid var(--color-text-primary);
  border-radius: var(--radius-prod-md);
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  transition: background-color 250ms ease, color 250ms ease, transform 150ms ease;
}
.btn-outline:hover {
  background-color: var(--color-text-primary);
  color: #FFFFFF;
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}
.btn-outline:active {
  transform: scale(0.97);
}

/* Hero button override -- larger */
.btn-hero {
  min-height: 56px;
  font-size: var(--font-size-lg);
  width: 100%;
}
@media (min-width: 768px) {
  .btn-hero {
    width: auto;
  }
}

/* Final CTA outline override -- white border on navy */
.btn-outline-light {
  border-color: #FFFFFF;
  color: #FFFFFF;
}
.btn-outline-light:hover {
  background-color: #FFFFFF;
  color: var(--color-navy);
}

/* Sticky bar CTA -- compact */
.btn-sticky {
  font-size: var(--font-size-sm);
  padding: 8px 24px;
  min-height: auto;
}
```

3. **Card utility class** -- also in `@layer components`:
```css
/* Production card style (67.1 visual parity) */
.card-prod {
  background-color: #FFFFFF;
  border-radius: var(--radius-prod-lg);
  border: 1px solid rgba(0, 0, 0, 0.06);
  box-shadow: var(--shadow-sm);
  overflow: hidden;
  transition: box-shadow 250ms ease, transform 250ms ease;
}
.card-prod:hover {
  box-shadow: var(--shadow-md);
}
```

4. **Section container utility**:
```css
.container-prod {
  max-width: 1200px;
  margin-inline: auto;
  padding-inline: 16px;
}
@media (min-width: 768px) {
  .container-prod {
    padding-inline: 32px;
  }
}
```

Do NOT remove any existing CSS. Do NOT change the `--font-size: 16px` root font-size (Pitfall 5 from RESEARCH -- changing root font size cascades to all Tailwind values). Instead, individual components will reference the `--font-size-base` (18px) token directly.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && grep -c "color-cta" src/app/globals.css && grep -c "btn-primary" src/app/globals.css && grep -c "font-size-h1" src/app/globals.css && grep -c "card-prod" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--color-cta: #35B678`
    - globals.css contains `--color-cta-hover: #25A467`
    - globals.css contains `--color-navy: #1A365D`
    - globals.css contains `--font-size-h1: clamp(2.5rem, 5vw, 3.5rem)`
    - globals.css contains `--font-size-h2: clamp(1.75rem, 3.5vw, 2.75rem)`
    - globals.css contains `--font-size-h3: clamp(1.375rem, 2.5vw, 2rem)`
    - globals.css contains `--font-size-base: 1.125rem`
    - globals.css contains `.btn-primary` with `background-color: var(--color-cta)`
    - globals.css contains `.btn-outline` with `border: 2px solid var(--color-text-primary)`
    - globals.css contains `.btn-hero` with `min-height: 56px`
    - globals.css contains `.btn-outline-light` with `border-color: #FFFFFF`
    - globals.css contains `.btn-sticky` with `font-size: var(--font-size-sm)`
    - globals.css contains `.card-prod` with `border-radius: var(--radius-prod-lg)`
    - globals.css contains `--section-padding-desktop: 6.25rem`
    - globals.css contains `--shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06)`
    - All existing CSS (Tailwind imports, liquid-glass imports, shadcn, dark mode overrides) is preserved
  </acceptance_criteria>
  <done>Production design tokens, button classes (.btn-primary, .btn-outline, .btn-hero, .btn-outline-light, .btn-sticky), card class (.card-prod), and container class (.container-prod) are defined in globals.css. All existing CSS is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Header scroll, Footer color, and StickyBar to match production</name>
  <files>next/src/components/layout/HeaderClient.tsx, next/src/components/layout/Footer.tsx, next/src/components/layout/StickyBar.tsx</files>
  <read_first>
    - next/src/components/layout/HeaderClient.tsx
    - next/src/components/layout/Footer.tsx
    - next/src/components/layout/StickyBar.tsx
    - next/src/components/layout/Header.tsx
    - next/src/hooks/use-scrolled.ts
    - .planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-UI-SPEC.md
  </read_first>
  <action>
**HeaderClient.tsx** -- Replace glass material on scroll with box-shadow:
- Change the scrolled state from `'liquid-nav header--scrolled'` to `'bg-white shadow-[0_2px_8px_rgba(0,0,0,0.08)]'`
- Keep `'bg-white'` for the non-scrolled state
- Keep `sticky top-0 z-50 flex items-center transition-all duration-300 h-16 md:h-[76px]`
- The header should NEVER use `liquid-nav` or `header--scrolled` classes

Updated className logic:
```tsx
className={cn(
  'sticky top-0 z-50 flex items-center transition-all duration-300 h-16 md:h-[76px] bg-white',
  isScrolled && 'shadow-[0_2px_8px_rgba(0,0,0,0.08)]',
)}
```

**Footer.tsx** -- Fix background color:
- Change `bg-mu-text-900` to `bg-[#1A365D]` (navy, per production --color-navy)
- Update tagline text color to `text-white/70` (production uses `rgba(255, 255, 255, 0.7)`)
- Update legal text to `text-[1rem]` (16px, per UI-SPEC) and color `text-white/50`
- Update link text size: footer links should be `text-[1.125rem]` (18px per production)
- Update company brand size: `text-[1.25rem]` (20px per production) with font-heading font-bold
- Update padding: `py-12 pb-8` (48px top, 32px bottom per production)
- Update grid gap: `gap-8 md:gap-10` (32px mobile, 40px desktop)
- Update legal section: `border-t border-white/10 pt-6` (keep as-is, matches production)
- Change footer link hover color from `hover:text-mu-blue` to `hover:text-[#2B6CB0]` (production --color-primary)

**StickyBar.tsx** -- Replace gradient CTA with solid green and add hide behavior:
- Replace the gradient CTA button (`bg-gradient-to-r from-mu-cta-from to-mu-cta-to`) with `btn-primary btn-sticky` classes
- Update phone text: `text-[1rem]` (16px) with `font-heading font-bold text-[#1A4D80]`
- Change `lg:hidden` to `hidden` by default + show only on mobile via `md:hidden` (actually production hides at 1024px+ so keep `lg:hidden`)
- Add IntersectionObserver-based hide behavior: observe the `#contact` section and footer. When either is visible in viewport, apply `translate-y-full` to hide the sticky bar. This requires making StickyBar a client component (it already is). Add a `useEffect` that:
  1. Queries `document.querySelector('#contact')` and `document.querySelector('footer')`
  2. Creates an IntersectionObserver with `threshold: 0`
  3. When either element is intersecting, sets state `isHidden = true`
  4. When neither is intersecting, sets state `isHidden = false`
  5. Applies `transition-transform duration-300` and conditionally `translate-y-full` when `isHidden`
- Keep `fixed bottom-0 left-0 right-0 z-50 bg-white border-t border-black/8 py-2`
- Update shadow to `shadow-[0_-2px_8px_rgba(0,0,0,0.06)]`
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && grep -q "shadow-\[0_2px_8px" src/components/layout/HeaderClient.tsx && echo "Header OK" && grep -q "#1A365D" src/components/layout/Footer.tsx && echo "Footer OK" && grep -q "btn-primary" src/components/layout/StickyBar.tsx && echo "StickyBar OK" && grep -q "IntersectionObserver" src/components/layout/StickyBar.tsx && echo "Hide OK"</automated>
  </verify>
  <acceptance_criteria>
    - HeaderClient.tsx does NOT contain `liquid-nav` or `header--scrolled`
    - HeaderClient.tsx contains `shadow-[0_2px_8px_rgba(0,0,0,0.08)]` for scrolled state
    - HeaderClient.tsx contains `bg-white` for both states
    - Footer.tsx contains `bg-[#1A365D]` (NOT `bg-mu-text-900`)
    - Footer.tsx contains `hover:text-[#2B6CB0]` for link hover (NOT `hover:text-mu-blue`)
    - StickyBar.tsx contains `btn-primary btn-sticky` on the CTA button
    - StickyBar.tsx does NOT contain `bg-gradient-to-r` or `from-mu-cta-from`
    - StickyBar.tsx contains `IntersectionObserver` for hide behavior
    - StickyBar.tsx contains `translate-y-full` for hidden state
    - StickyBar.tsx contains `text-[#1A4D80]` for phone number color
  </acceptance_criteria>
  <done>Header uses box-shadow on scroll (no glass). Footer uses navy #1A365D background. StickyBar uses solid green CTA button and hides near form/footer sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected in this plan. This is a CSS/styling-only change. No data flow, no input handling, no authentication changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-01 | N/A | CSS tokens | accept | Visual-only changes, no security surface |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` renders the site with:
   - Header shows white bg + box-shadow when scrolled (no glass blur)
   - Footer is navy blue (#1A365D), not near-black
   - Sticky bar has green button, hides when scrolling near contact form
3. grep confirms all production tokens exist in globals.css
</verification>

<success_criteria>
- Production design tokens (typography, colors, spacing, shadows) are defined in globals.css
- Button utility classes (.btn-primary, .btn-outline, .btn-hero, .btn-outline-light, .btn-sticky) exist and match UI-SPEC
- Card utility class (.card-prod) exists matching UI-SPEC
- Header, Footer, StickyBar visually match production specs
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/67.1-visual-parity-rework-re-port-all-sections-from-production-wi/67.1-01-SUMMARY.md`
</output>
