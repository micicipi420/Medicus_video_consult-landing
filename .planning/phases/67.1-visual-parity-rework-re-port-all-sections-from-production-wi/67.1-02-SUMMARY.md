---
phase: 67.1-visual-parity-rework
plan: 02
subsystem: index-page-sections
tags: [visual-parity, typography, buttons, cards, section-backgrounds]
dependency_graph:
  requires: [67.1-01]
  provides: [production-parity-index-sections]
  affects: [next/src/components/sections/*]
tech_stack:
  added: []
  patterns: [clamp-typography, card-prod-class, btn-primary-class, production-hex-backgrounds]
key_files:
  created: []
  modified:
    - next/src/components/sections/HeroHub.tsx
    - next/src/components/sections/StatsBar.tsx
    - next/src/components/sections/ServicesGrid.tsx
    - next/src/components/sections/GuideGrid.tsx
    - next/src/components/sections/AdvantagesGrid.tsx
    - next/src/components/sections/ContactSection.tsx
    - next/src/components/sections/FinalCTA.tsx
decisions:
  - "Cards use card-prod + squircle-lg (production flat style with modern squircle radius per CONTEXT.md)"
  - "Service badges changed from squircle-md to rounded-full (pill shape) matching production"
  - "All CTA buttons replaced with btn-primary/btn-outline classes from Plan 01 globals.css"
  - "Section backgrounds use production hex values directly instead of glass tint classes"
  - "Guide items are plain content blocks (no card wrapper) matching production layout"
metrics:
  duration: 3min
  completed: 2026-04-11T11:29:02Z
  tasks: 2
  files: 7
requirements:
  - VP-01
  - VP-02
  - VP-03
  - VP-04
  - VP-06
  - VP-08
---

# Phase 67.1 Plan 02: Index Page Section Parity Summary

All 7 index page section components updated to 1:1 visual parity with production vanilla HTML/CSS site -- clamp() typography, solid green buttons, production hex backgrounds, flat card styling with squircle radius.

## What Was Done

### Task 1: HeroHub, StatsBar, ServicesGrid, GuideGrid (b649145)

**HeroHub.tsx:**
- H1: `text-[clamp(2.5rem,5vw,3.5rem)]` with `leading-[1.1]`, `tracking-[-0.02em]`, `text-balance`
- Container: `max-w-[800px]` (was `max-w-3xl`)
- Padding: `pt-20 pb-12 lg:pt-[5rem] lg:pb-12` (was `py-16 md:py-24 lg:py-32`)
- Subtitle: `text-[1.25rem]` with `max-w-[720px] mx-auto`
- Buttons: `btn-primary btn-hero` and `btn-outline btn-hero` (removed all `liquid-btn-*` and `squircle-md`)

**StatsBar.tsx:**
- Label font: explicit `text-[1.125rem]` (was `text-lg`)
- Padding: added `lg:py-8` for explicitness

**ServicesGrid.tsx:**
- Background: `bg-white` (was `section-tint-cool`)
- Cards: `card-prod squircle-lg p-8 md:p-10 text-center` with hover lift effect
- H2: `text-[clamp(1.75rem,3.5vw,2.75rem)]` with tracking/balance
- H3: `text-[clamp(1.375rem,2.5vw,2rem)]`
- Badges: `rounded-full` pill shape (was `squircle-md`)
- Badge font: `text-[1rem] font-heading font-semibold`
- Icons: `w-[72px] h-[72px]` (was `w-16 h-16`)
- Body text: `text-[1.125rem]` with `max-w-[360px] mx-auto`
- Features: `text-[1rem]` with green dot bullets, left-aligned
- CTA: `btn-primary w-full md:w-auto` (removed `liquid-btn-primary squircle-md`)
- Grid gap: `gap-8` (was `gap-6 md:gap-8`)
- Section padding: `py-12 lg:py-[6.25rem]`

**GuideGrid.tsx:**
- Background: `bg-[#FFF8F0]` (was `section-tint-warm`)
- Cards: plain `p-6 md:p-8 flex flex-col` (removed `liquid-regular squircle-lg`)
- H2/H3: clamp() values with `leading-[1.2]`
- Body text: `text-[1.125rem]`
- Link color: `text-[#1A4D80]` hover `text-[#18212C]` (was `text-[#0B7A9A]`)
- Section padding: `py-12 lg:py-[6.25rem]`

### Task 2: AdvantagesGrid, ContactSection, FinalCTA (e0f2c8a)

**AdvantagesGrid.tsx:**
- Background: `bg-[#F0F7FF]` (was `section-tint-mint`)
- Cards: `card-prod squircle-lg p-8` with `hover:-translate-y-1 transition-transform`
- H2: clamp() with tracking/balance
- H3: `text-[clamp(1.375rem,2.5vw,2rem)]` (was `text-lg`)
- Body text: `text-[1.125rem]` (was `text-sm`)
- Grid gap: `gap-8` (was `gap-6`)

**ContactSection.tsx:**
- Background: `bg-[#F5F7F9]` (was `section-tint-cool`)
- Removed `relative` from section
- Form wrapper: `bg-white border border-black/[0.08] rounded-2xl shadow-[0_2px_8px_rgba(0,0,0,0.08)]` (was `liquid-card squircle-xl`)
- Added `max-w-[540px]` on mobile, `md:max-w-none` on desktop
- H2: clamp() with tracking/balance
- Body text: `text-[1.125rem]`

**FinalCTA.tsx:**
- Container: `max-w-[640px]` (was `max-w-2xl`)
- H2: clamp() with tracking/balance
- Body text: `text-[1.125rem]` with `text-white/85` (was `text-lg text-white/80`)
- Primary button: `btn-primary` (was `liquid-btn-primary squircle-md ...`)
- Secondary button: `btn-outline btn-outline-light` (was `liquid-btn-secondary squircle-md ...`)
- Secondary text: "Позвонить нам" (was "Позвонить")
- Section padding: `py-12 lg:py-[6.25rem]`

## Deviations from Plan

None -- plan executed exactly as written.

## Decisions Made

1. **card-prod + squircle-lg combination**: Per CONTEXT.md, cards use production flat styling (white bg, border, shadow via card-prod) but retain squircle-lg border-radius for modern feel.
2. **Guide items have no card wrapper**: Production guide items are plain flex columns with no background/border, so removed all glass card classes without replacement.
3. **Green dot bullets for service features**: Added inline `<span>` elements with `w-2 h-2 rounded-full bg-[#35B678]` to match production's `::before` green circle pattern.

## Verification Results

All automated checks passed:
- Hero buttons: btn-primary btn-hero (no liquid-btn)
- Hero H1: clamp(2.5rem,5vw,3.5rem), leading-[1.1]
- Hero container: max-w-[800px], pt-20 pb-12
- Services: bg-white, card-prod squircle-lg, rounded-full badges, btn-primary CTA
- Guide: bg-[#FFF8F0], no liquid-regular, text-[#1A4D80] links
- Advantages: bg-[#F0F7FF], card-prod squircle-lg, text-[1.125rem]
- Contact: bg-[#F5F7F9], no liquid-card/squircle-xl, shadow-[0_2px_8px...]
- FinalCTA: btn-primary, btn-outline btn-outline-light, max-w-[640px], "Позвонить нам"
- All sections: lg:py-[6.25rem] desktop padding
- Stats: text-[1.125rem] label

## Self-Check: PASSED

All 7 modified files exist on disk. Both commits (b649145, e0f2c8a) found in git log.
