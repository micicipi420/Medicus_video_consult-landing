---
phase: 70-index-content-sections
plan: 03
type: execute
wave: 2
depends_on: [70-01, 70-02]
files_modified:
  - next/src/components/sections/PlatformSection.tsx
  - next/src/app/page.tsx
autonomous: true
requirements: [SEC-07]
must_haves:
  truths:
    - "PlatformSection renders gradient title 'Ваши документы, снимки и связь с врачом — в одном месте' with a glass container listing 5 platform features"
    - "page.tsx imports and renders all 5 new sections in correct order: HeroHub, StatsBar, ServicesGrid, ProblemSection, ProcessSection, WhyUsSection, ClinicsSection, PlatformSection, ContactSection, FinalCTA"
    - "Old GuideGrid and AdvantagesGrid imports are removed from page.tsx"
    - "Build passes cleanly with no type errors or unused import warnings"
  artifacts:
    - path: "next/src/components/sections/PlatformSection.tsx"
      provides: "Platform features section"
      exports: ["PlatformSection"]
    - path: "next/src/app/page.tsx"
      provides: "Updated index page with all 5 new sections"
  key_links:
    - from: "page.tsx"
      to: "ProblemSection.tsx"
      via: "import"
      pattern: "import.*ProblemSection"
    - from: "page.tsx"
      to: "ProcessSection.tsx"
      via: "import"
      pattern: "import.*ProcessSection"
    - from: "page.tsx"
      to: "WhyUsSection.tsx"
      via: "import"
      pattern: "import.*WhyUsSection"
    - from: "page.tsx"
      to: "ClinicsSection.tsx"
      via: "import"
      pattern: "import.*ClinicsSection"
    - from: "page.tsx"
      to: "PlatformSection.tsx"
      via: "import"
      pattern: "import.*PlatformSection"
---

<objective>
Create PlatformSection and wire all 5 new content sections into page.tsx, replacing GuideGrid and AdvantagesGrid imports with the new section components.

Purpose: Complete the Phase 70 index page content sections by adding the platform feature presentation and updating the page to render all sections in the correct order.

Output: PlatformSection.tsx + updated page.tsx with full section lineup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@next/src/app/page.tsx (current state — will be modified)

<interfaces>
<!-- New components created by Plan 01 and Plan 02: -->
<!-- ProblemSection: export function ProblemSection() — id="problem" -->
<!-- ProcessSection: export function ProcessSection() — id="process" -->
<!-- WhyUsSection: export function WhyUsSection() — id="why-us" -->
<!-- ClinicsSection: export function ClinicsSection() — id="clinics" -->

<!-- Existing components staying: -->
<!-- HeroHub, StatsBar, ServicesGrid, ContactSection, FinalCTA -->
<!-- ScrollReveal (kept for ContactSection, FinalCTA) -->

From next/src/components/sections/ServicesGrid.tsx:
```typescript
import { CheckCircle2 } from 'lucide-react';
// Pattern for checkmark list items
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlatformSection.tsx — platform features glass container</name>
  <files>next/src/components/sections/PlatformSection.tsx</files>
  <action>
Create `next/src/components/sections/PlatformSection.tsx` as a React Server Component (NO 'use client').

Import `CheckCircle2` from lucide-react (for the green checkmark circles in the feature list — maps to the circle-check SVG in source HTML).

Export a named function `PlatformSection`.

The exact JSX structure (port from d450232 HTML, section 7, lines 633-670):

```tsx
<section className="py-16 relative z-10" id="platform">
  <div className="container mx-auto px-4 lg:px-6">
    <div className="text-center mb-12 max-w-3xl mx-auto">
      <h2 className="text-4xl md:text-5xl font-extrabold mb-6">
        <span className="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent">
          Ваши документы, снимки и{'\u00A0'}связь с{'\u00A0'}врачом{'\u00A0'}{'\u2014'} в{'\u00A0'}одном месте
        </span>
      </h2>
    </div>

    <div className="bg-white/60 backdrop-blur-2xl rounded-[3rem] p-8 md:p-12 border border-glass-border shadow-glass max-w-3xl mx-auto">
      <p className="text-mu-text-700 font-medium text-lg leading-relaxed mb-8">
        У{'\u00A0'}MedicusUnion есть то, чего нет ни{'\u00A0'}у{'\u00A0'}одного медицинского агентства{'\u00A0'}{'\u2014'} собственная цифровая платформа. Это защищённый личный кабинет, где вы{'\u00A0'}видите:
      </p>
      <ul className="space-y-4 mb-8">
        <li className="flex items-start gap-3 text-mu-text-900 font-medium">
          <CheckCircle2 size={20} className="flex-shrink-0 mt-1 text-mu-green-600" aria-hidden="true" />
          Результаты анализов и{'\u00A0'}заключения врачей{'\u00A0'}{'\u2014'} с{'\u00A0'}переводом на{'\u00A0'}русский язык
        </li>
        <li className="flex items-start gap-3 text-mu-text-900 font-medium">
          <CheckCircle2 size={20} className="flex-shrink-0 mt-1 text-mu-green-600" aria-hidden="true" />
          Медицинские снимки через встроенный DICOM-вьювер
        </li>
        <li className="flex items-start gap-3 text-mu-text-900 font-medium">
          <CheckCircle2 size={20} className="flex-shrink-0 mt-1 text-mu-green-600" aria-hidden="true" />
          Историю переписки с{'\u00A0'}координатором и{'\u00A0'}врачом
        </li>
        <li className="flex items-start gap-3 text-mu-text-900 font-medium">
          <CheckCircle2 size={20} className="flex-shrink-0 mt-1 text-mu-green-600" aria-hidden="true" />
          План лечения и{'\u00A0'}все документы
        </li>
        <li className="flex items-start gap-3 text-mu-text-900 font-medium">
          <CheckCircle2 size={20} className="flex-shrink-0 mt-1 text-mu-green-600" aria-hidden="true" />
          ИИ-анализ лабораторных исследований
        </li>
      </ul>
      <p className="text-mu-text-700 font-medium text-lg leading-relaxed mb-4">
        Доступно через браузер и{'\u00A0'}мобильное приложение (iOS и{'\u00A0'}Android).
      </p>
      <p className="text-mu-text-900 font-bold">
        Данные защищены по{'\u00A0'}стандартам ISO{'\u00A0'}27001 и{'\u00A0'}GDPR.
      </p>
    </div>
  </div>
</section>
```

This is a direct port — no data array needed since there are only 5 list items and they're all simple text.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep "id=\"platform\"" next/src/components/sections/PlatformSection.tsx && grep "DICOM" next/src/components/sections/PlatformSection.tsx && grep "CheckCircle2" next/src/components/sections/PlatformSection.tsx && ! grep "'use client'" next/src/components/sections/PlatformSection.tsx && echo "PASS"</automated>
  </verify>
  <done>PlatformSection.tsx exists as server component with: gradient title "Ваши документы, снимки и связь с врачом — в одном месте", glass container with intro paragraph, 5 green-checkmark feature items, browser/mobile availability note, ISO 27001/GDPR statement, id="platform"</done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx — wire all 5 new sections, remove old imports</name>
  <files>next/src/app/page.tsx</files>
  <action>
Read the current `next/src/app/page.tsx` first. Then rewrite it with these changes:

**Imports to REMOVE:**
```tsx
import { GuideGrid } from '@/components/sections/GuideGrid';
import { AdvantagesGrid } from '@/components/sections/AdvantagesGrid';
```

**Imports to ADD:**
```tsx
import { ProblemSection } from '@/components/sections/ProblemSection';
import { ProcessSection } from '@/components/sections/ProcessSection';
import { WhyUsSection } from '@/components/sections/WhyUsSection';
import { ClinicsSection } from '@/components/sections/ClinicsSection';
import { PlatformSection } from '@/components/sections/PlatformSection';
```

**Imports to KEEP (unchanged):**
```tsx
import type { Metadata } from 'next';
import { HeroHub } from '@/components/sections/HeroHub';
import { StatsBar } from '@/components/sections/StatsBar';
import { ServicesGrid } from '@/components/sections/ServicesGrid';
import { ContactSection } from '@/components/sections/ContactSection';
import { FinalCTA } from '@/components/sections/FinalCTA';
import { ScrollReveal } from '@/components/motion/ScrollReveal';
```

**Section render order** (matching the feat/new-design index.html section order):
```tsx
export default function Home() {
  return (
    <>
      <HeroHub />
      <StatsBar />
      <ServicesGrid />
      <ScrollReveal delay={0.05}>
        <ProblemSection />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <ProcessSection />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <WhyUsSection />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <ClinicsSection />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <PlatformSection />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <ContactSection />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <FinalCTA />
      </ScrollReveal>
    </>
  );
}
```

The `metadata` export stays exactly as it is — do not modify it.

**Key decisions:**
- New content sections (Problem, Process, WhyUs, Clinics, Platform) get ScrollReveal wrappers with delay={0.05} to match the existing pattern for content sections below the fold.
- HeroHub, StatsBar, ServicesGrid remain WITHOUT ScrollReveal (they are above the fold, matching Phase 69 decisions).
- ContactSection and FinalCTA keep their existing ScrollReveal wrappers.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep "ProblemSection" next/src/app/page.tsx && grep "ProcessSection" next/src/app/page.tsx && grep "WhyUsSection" next/src/app/page.tsx && grep "ClinicsSection" next/src/app/page.tsx && grep "PlatformSection" next/src/app/page.tsx && ! grep "GuideGrid" next/src/app/page.tsx && ! grep "AdvantagesGrid" next/src/app/page.tsx && cd next && npx next build 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <done>page.tsx imports all 5 new sections (ProblemSection, ProcessSection, WhyUsSection, ClinicsSection, PlatformSection), removes GuideGrid and AdvantagesGrid imports, renders sections in correct order with ScrollReveal wrappers on content sections. Build passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static content section + import wiring — no user input or dynamic data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | I (Information Disclosure) | Platform feature descriptions | accept | Public marketing content about platform capabilities — no secrets |
</threat_model>

<verification>
- `npx next build` passes with no errors
- PlatformSection.tsx: gradient title, glass container, 5 checkmark features, ISO/GDPR statement
- page.tsx: all 5 new section imports present, no old GuideGrid/AdvantagesGrid imports
- Section order: Hero > Stats > Services > Problem > Process > WhyUs > Clinics > Platform > Contact > FinalCTA
- All content sections wrapped in ScrollReveal
</verification>

<success_criteria>
- PlatformSection renders "Ваши документы, снимки и связь с врачом — в одном месте" with 5 feature items
- page.tsx renders all 10 sections in correct order
- No references to GuideGrid or AdvantagesGrid remain in page.tsx
- Build passes cleanly with zero errors
- The index page displays the complete content section flow when viewed in browser
</success_criteria>

<output>
After completion, create `.planning/phases/70-index-content-sections/70-03-SUMMARY.md`
</output>
