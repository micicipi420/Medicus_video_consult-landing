---
phase: 61-index-page-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/components/sections/HeroHub.tsx
  - next/src/components/sections/StatsBar.tsx
  - next/src/components/sections/ServicesGrid.tsx
  - next/src/components/sections/GuideGrid.tsx
  - next/src/components/sections/AdvantagesGrid.tsx
  - next/src/components/sections/FinalCTA.tsx
autonomous: true
requirements: [PAGE-01]

must_haves:
  truths:
    - "Hero section renders centered headline, subtitle, and 2 CTA buttons with gradient background"
    - "Stats bar renders 4 stat items (43, 11, 500+, 15+) with white text on dark navy background"
    - "Services grid renders 3 product cards with SVG icons, badges, features lists, and CTA buttons"
    - "Guide grid renders 3 guide items with SVG icons, descriptions, and arrow links"
    - "Advantages grid renders 4 advantage cards with SVG icons and highlighted numbers"
    - "FinalCTA renders dark section with heading, text, and 2 action buttons"
    - "All 6 components are Server Components (no 'use client' directive)"
  artifacts:
    - path: "next/src/components/sections/HeroHub.tsx"
      provides: "Hero section with gradient background, h1, subtitle, 2 CTA links"
    - path: "next/src/components/sections/StatsBar.tsx"
      provides: "Dark navy stats bar with 4 stat items"
    - path: "next/src/components/sections/ServicesGrid.tsx"
      provides: "3-column service cards with inline SVGs, badges, features"
    - path: "next/src/components/sections/GuideGrid.tsx"
      provides: "3-column guide items with SVGs and arrow links"
    - path: "next/src/components/sections/AdvantagesGrid.tsx"
      provides: "4-column advantage cards with SVGs"
    - path: "next/src/components/sections/FinalCTA.tsx"
      provides: "Dark CTA section with 2 buttons"
  key_links:
    - from: "next/src/components/sections/ServicesGrid.tsx"
      to: "/consultations, /treatment-abroad, /checkup"
      via: "next/link for internal routes"
      pattern: "Link href="
    - from: "next/src/components/sections/FinalCTA.tsx"
      to: "#contact, tel:+77015322478"
      via: "plain <a> for hash and tel: links"
      pattern: "href=\"#contact\""
---

<objective>
Create all 6 static Server Component sections for the index page: HeroHub, StatsBar, ServicesGrid, GuideGrid, AdvantagesGrid, and FinalCTA. Each component ports content verbatim from the current index.html, using Tailwind utility classes for layout (since original BEM CSS is not available in Next.js) and existing glass/token CSS where applicable.

Purpose: These 6 sections form the static content backbone of the index page. All are Server Components with zero client-side JS.
Output: 6 component files in next/src/components/sections/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-index-page-migration/61-RESEARCH.md
@index.html
@next/src/app/globals.css
@next/src/lib/navigation.ts
@next/src/components/layout/Footer.tsx
@next/src/components/layout/StickyBar.tsx

<interfaces>
<!-- Established patterns from Phase 60 -->

From next/src/lib/navigation.ts:
```typescript
export const PHONE_NUMBER = '+77015322478';
export const PHONE_DISPLAY = '+7\u00A0701\u00A0532\u00A024\u00A078';
```

From next/src/components/layout/StickyBar.tsx (gradient CTA pattern):
```typescript
className="bg-gradient-to-r from-mu-cta-from to-mu-cta-to"
```

From next/src/components/layout/Footer.tsx (Tailwind layout pattern):
```typescript
className="container mx-auto px-4 md:px-8"
```

CSS Token mapping (from 61-RESEARCH.md):
| Original | Tailwind |
|----------|----------|
| --color-bg-blue (#F0F7FF) | bg-[#F0F7FF] |
| --color-navy (#1A365D) | bg-[#1A365D] |
| --color-bg-gray (#F5F7F9) | bg-[#F5F7F9] |
| --section-padding-mobile (48px) | py-12 |
| --section-padding-desktop (100px) | md:py-24 or lg:py-[100px] |
| CTA gradient | bg-gradient-to-r from-mu-cta-from to-mu-cta-to |
| badge bg (#d0fae4) | bg-[#d0fae4] |
| badge text (#007955) | text-[#007955] |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroHub, StatsBar, and ServicesGrid components</name>
  <files>
    next/src/components/sections/HeroHub.tsx
    next/src/components/sections/StatsBar.tsx
    next/src/components/sections/ServicesGrid.tsx
  </files>
  <read_first>
    index.html (lines 49-162 — Hero, Stats, Services sections)
    next/src/components/layout/StickyBar.tsx (gradient CTA pattern)
    next/src/lib/navigation.ts (constants)
  </read_first>
  <action>
Create 3 React Server Components (NO "use client" directive) in next/src/components/sections/:

**HeroHub.tsx** — Port section from index.html lines 49-60:
- Section: `className="py-16 md:py-24 lg:py-32 bg-gradient-to-b from-[#F0F7FF] to-white"` with `id="hero-hub"`
- Container: `className="container mx-auto px-4 md:px-8 text-center max-w-3xl mx-auto"`
- h1: `className="font-heading text-3xl md:text-4xl lg:text-5xl font-bold text-mu-text-900 leading-tight mb-6"`
  Content: `Медицина мирового уровня{'\u00A0'}{'\u2014'} для{'\u00A0'}Казахстана`
- p subtitle: `className="font-body text-lg md:text-xl text-mu-text-500 mb-10 leading-relaxed"`
  Content verbatim from index.html with \u00A0 replacing &amp;nbsp; entities
- Actions div with 2 links:
  - `<a href="#services">` primary CTA: `className="inline-flex items-center justify-center px-8 py-3.5 rounded-xl text-base font-semibold text-white bg-gradient-to-r from-mu-cta-from to-mu-cta-to hover:opacity-90 transition-opacity"`
    Text: "Выбрать услугу"
  - `<a href="#contact">` outline: `className="inline-flex items-center justify-center px-8 py-3.5 rounded-xl text-base font-semibold text-mu-text-700 border-2 border-mu-text-200 hover:border-mu-text-300 transition-colors"`
    Text: "Оставить заявку"
- Both CTAs are hash anchors (same page) so use plain `<a>`, NOT next/link

**StatsBar.tsx** — Port section from index.html lines 63-82:
- Extract stat data into a `STATS` const array: `[{ number: '43', label: 'клиники' }, { number: '11', label: 'стран' }, { number: '500+', label: 'врачей' }, { number: '15+', label: 'лет опыта' }]`
- Section: `className="bg-[#1A365D] py-8"` with `aria-label="Ключевые цифры"`
- Container: `className="container mx-auto px-4 flex flex-col md:flex-row items-center justify-center gap-6 md:gap-16 text-center"`
- Each stat item: number `className="font-heading text-[2.5rem] font-bold leading-none text-white tabular-nums"` + label `className="font-body text-lg text-white/85"`
- No "use client" — animated counters deferred to Phase 63

**ServicesGrid.tsx** — Port section from index.html lines 85-162:
- Extract 3 service card data into a typed const array. Each card has: svgIcon (JSX element), badge text, title, description, features (3 strings), ctaText, ctaHref
- Section: `className="py-12 md:py-24"` with `id="services"`
- Heading h2: `className="font-heading text-2xl md:text-3xl font-bold text-center text-mu-text-900 mb-10 md:mb-14"` — "Выберите, что\u00A0вам нужно"
- Grid: `className="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8"`
- Each card: `className="bg-white rounded-2xl shadow-md p-6 md:p-8 flex flex-col"`
  - Icon div: 64x64 SVG inline, convert HTML SVG to JSX (camelCase: strokeWidth, strokeLinecap, strokeLinejoin, fillRule)
  - Badge: `className="inline-block bg-[#d0fae4] text-[#007955] text-sm font-semibold px-3 py-1 rounded-full mt-4 mb-3 w-fit"`
  - Title h3: `className="font-heading text-xl font-bold text-mu-text-900 mb-3"`
  - Text p: `className="font-body text-mu-text-500 leading-relaxed mb-4 flex-grow"`
  - Features ul: `className="space-y-2 mb-6 text-mu-text-700 text-sm"` — 3 `<li>` items
  - CTA: use `<Link>` from next/link for the 3 internal routes (consultations.html -> /consultations, treatment-abroad.html -> /treatment-abroad, checkup.html -> /checkup). Style: `className="inline-flex items-center justify-center w-full px-6 py-3 rounded-xl text-sm font-semibold text-white bg-gradient-to-r from-mu-cta-from to-mu-cta-to hover:opacity-90 transition-opacity mt-auto"`
- DO NOT include `animate-on-scroll` class (Phase 63)
- All Russian text content copied verbatim from index.html, with &amp;nbsp; -> \u00A0, &amp;mdash; -> \u2014, &amp;ndash; -> \u2013, &amp;euro; -> \u20AC, &amp;rarr; -> \u2192

CRITICAL SVG conversion rules for all inline SVGs:
- stroke-width -> strokeWidth
- stroke-linecap -> strokeLinecap
- stroke-linejoin -> strokeLinejoin
- fill-rule -> fillRule
- Keep viewBox as-is
- Self-close tags: `<line ... />`, `<circle ... />`
- Keep aria-hidden="true" as-is
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function HeroHub" next/src/components/sections/HeroHub.tsx
    - grep -q "export function StatsBar" next/src/components/sections/StatsBar.tsx
    - grep -q "export function ServicesGrid" next/src/components/sections/ServicesGrid.tsx
    - ! grep -q "use client" next/src/components/sections/HeroHub.tsx
    - ! grep -q "use client" next/src/components/sections/StatsBar.tsx
    - ! grep -q "use client" next/src/components/sections/ServicesGrid.tsx
    - grep -q "hero-hub\|hero" next/src/components/sections/HeroHub.tsx
    - grep -q "Ключевые цифры" next/src/components/sections/StatsBar.tsx
    - grep -q "Выберите" next/src/components/sections/ServicesGrid.tsx
    - grep -q "from-mu-cta-from" next/src/components/sections/HeroHub.tsx
    - grep -q "Link" next/src/components/sections/ServicesGrid.tsx
  </acceptance_criteria>
  <done>
    3 Server Components compile without TypeScript errors. HeroHub renders hero with gradient background and 2 CTA buttons. StatsBar renders 4 stat items on dark navy. ServicesGrid renders 3 product cards with SVGs, badges, features, and CTA links to subpages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GuideGrid, AdvantagesGrid, and FinalCTA components</name>
  <files>
    next/src/components/sections/GuideGrid.tsx
    next/src/components/sections/AdvantagesGrid.tsx
    next/src/components/sections/FinalCTA.tsx
  </files>
  <read_first>
    index.html (lines 164-356 — Guide, Advantages, FinalCTA sections)
    next/src/lib/navigation.ts (PHONE_NUMBER for tel: link in FinalCTA)
  </read_first>
  <action>
Create 3 React Server Components (NO "use client" directive) in next/src/components/sections/:

**GuideGrid.tsx** — Port section from index.html lines 164-206:
- Extract 3 guide items into typed const array. Each: svgIcon (JSX), title, description, linkText, linkHref
- Section: `className="py-12 md:py-24 bg-[#F0F7FF]"` with `id="guide"`
- Heading h2: `className="font-heading text-2xl md:text-3xl font-bold text-center text-mu-text-900 mb-10 md:mb-14"` — "Не\u00A0знаете, с\u00A0чего начать?"
- Grid: `className="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8"`
- Each guide item: `className="bg-white rounded-2xl shadow-sm p-6 md:p-8 flex flex-col"`
  - Icon: 48x48 SVGs (convert to JSX camelCase), `aria-hidden="true"`, `className="w-12 h-12 mb-4"`
  - Title h3: `className="font-heading text-lg font-bold text-mu-text-900 mb-2"`
  - Text p: `className="font-body text-mu-text-500 leading-relaxed mb-4 flex-grow"`
  - Link: use next/link `<Link>` for internal routes:
    - consultations.html -> /consultations
    - treatment-abroad.html -> /treatment-abroad
    - checkup.html -> /checkup
    Style: `className="font-semibold text-[#0B7A9A] hover:text-[#065c75] transition-colors mt-auto"`
    Text with arrow: e.g., "Онлайн-консультация\u00A0\u2192"
- DO NOT include `animate-on-scroll` class
- All text verbatim from index.html with entity conversion

**AdvantagesGrid.tsx** — Port section from index.html lines 209-258:
- Extract 4 advantage cards into typed const array. Each: svgIcon (JSX), title (JSX with `<span>` for number highlights), description
- Section: `className="py-12 md:py-24 bg-[#F0F7FF]"` with `id="why-mu"`
- Heading h2: `className="font-heading text-2xl md:text-3xl font-bold text-center text-mu-text-900 mb-10 md:mb-14"` — "Почему MedicusUnion"
- Grid: `className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6"`
- Each card: `className="bg-white rounded-2xl shadow-sm p-6 flex flex-col"`
  - Icon: 48x48 SVGs (convert to JSX), `aria-hidden="true"`, `className="w-12 h-12 mb-4"`
  - Title h3: `className="font-heading text-lg font-bold text-mu-text-900 mb-2"` — preserve `<span>` elements wrapping numbers (43, 11, 15+, 10 000+)
  - Text p: `className="font-body text-sm text-mu-text-500 leading-relaxed"`
- All text verbatim, entity conversion applied

**FinalCTA.tsx** — Port section from index.html lines 347-356:
- Import PHONE_NUMBER from '@/lib/navigation'
- Section: `className="py-12 md:py-24 bg-[#1A365D]"` with `id="final-cta"`
- Container: `className="container mx-auto px-4 md:px-8 text-center max-w-2xl mx-auto"`
- Heading h2: `className="font-heading text-2xl md:text-3xl font-bold text-white mb-4"` — "Начните с\u00A0бесплатной консультации"
- Text p: `className="font-body text-lg text-white/80 mb-8"` — "Расскажите о\u00A0вашей ситуации\u00A0\u2014 мы\u00A0подберём оптимальное решение. Без\u00A0обязательств."
- Actions div: `className="flex flex-col sm:flex-row items-center justify-center gap-4"`
  - Primary CTA `<a href="#contact">`: same gradient style as HeroHub primary button, text "Оставить заявку"
  - Outline `<a href={`tel:${PHONE_NUMBER}`}>`: `className="inline-flex items-center justify-center px-8 py-3.5 rounded-xl text-base font-semibold text-white border-2 border-white/30 hover:border-white/60 transition-colors"` — text "Позвонить"
- Both links use plain `<a>` (hash anchor and tel: protocol)
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function GuideGrid" next/src/components/sections/GuideGrid.tsx
    - grep -q "export function AdvantagesGrid" next/src/components/sections/AdvantagesGrid.tsx
    - grep -q "export function FinalCTA" next/src/components/sections/FinalCTA.tsx
    - ! grep -q "use client" next/src/components/sections/GuideGrid.tsx
    - ! grep -q "use client" next/src/components/sections/AdvantagesGrid.tsx
    - ! grep -q "use client" next/src/components/sections/FinalCTA.tsx
    - grep -q "Не.*знаете" next/src/components/sections/GuideGrid.tsx
    - grep -q "Почему MedicusUnion" next/src/components/sections/AdvantagesGrid.tsx
    - grep -q "бесплатной консультации" next/src/components/sections/FinalCTA.tsx
    - grep -q "PHONE_NUMBER" next/src/components/sections/FinalCTA.tsx
    - grep -q "Link" next/src/components/sections/GuideGrid.tsx
  </acceptance_criteria>
  <done>
    3 Server Components compile without TypeScript errors. GuideGrid renders 3 guide items with SVGs and links to subpages. AdvantagesGrid renders 4 advantage cards with SVGs and number highlights. FinalCTA renders dark section with primary CTA and tel: call button.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All 6 components render static content with no user input, no API calls, no dynamic data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | SVG inline content | accept | SVGs are static inline JSX, no external sources, no user input. No risk. |
| T-61-02 | Info Disclosure | Link hrefs | accept | All links are public routes (consultations, treatment-abroad, checkup) and hash anchors. No sensitive URLs. |
</threat_model>

<verification>
- `cd next && npx tsc --noEmit` passes with zero errors
- All 6 files exist in next/src/components/sections/
- No file contains "use client"
- All 6 files export named functions
</verification>

<success_criteria>
6 Server Components created with correct Russian content, inline SVGs converted to JSX, Tailwind utility classes for layout, and next/link for internal routes. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/61-index-page-migration/61-01-SUMMARY.md`
</output>
