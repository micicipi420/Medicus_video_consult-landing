---
phase: 62-contacts-page-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/app/contacts/page.tsx
  - next/src/components/sections/contacts/ContactsHero.tsx
  - next/src/components/sections/contacts/CoordinatorCard.tsx
  - next/src/components/sections/contacts/ContactMethodGrid.tsx
  - next/src/components/sections/contacts/TrustBadges.tsx
  - next/src/app/layout.tsx
  - next/src/app/page.tsx
  - next/src/lib/navigation.ts
  - next/next.config.ts
autonomous: false
requirements: [PAGE-02, PAGE-03]

must_haves:
  truths:
    - "/contacts page renders a centered hero with glass badge, gradient H1, and subtitle"
    - "/contacts page renders coordinator card with avatar, name, title, and bio in the left column"
    - "/contacts page renders 2x2 contact method grid (phone, email, office, schedule) with glass cards"
    - "/contacts page renders 4 trust badges below contact methods"
    - "/contacts page renders ContactForm in the right column inside a white card wrapper"
    - "Navigation link 'Контакты' in header points to /contacts (not #contact)"
    - "Both / and /contacts export correct SEO metadata (title, description, OG tags, canonical)"
    - "Index page title uses absolute format: 'MedicusUnion KZ -- Медицина мирового уровня для Казахстана'"
    - "Contacts page title uses template: 'Контакты -- MedicusUnion KZ'"
    - "og:description and meta description differ for index page (they are different in the original HTML)"
    - "Both pages are statically generated (SSG) -- npm run build shows both as static"
  artifacts:
    - path: "next/src/app/contacts/page.tsx"
      provides: "Contacts page with metadata export and section composition"
      exports: ["default", "metadata"]
    - path: "next/src/components/sections/contacts/ContactsHero.tsx"
      provides: "Centered hero with glass badge, gradient H1, subtitle"
    - path: "next/src/components/sections/contacts/CoordinatorCard.tsx"
      provides: "Avatar + name + title + bio card"
    - path: "next/src/components/sections/contacts/ContactMethodGrid.tsx"
      provides: "2x2 grid of phone, email, office, schedule cards"
    - path: "next/src/components/sections/contacts/TrustBadges.tsx"
      provides: "4 trust badges with green checkmarks"
    - path: "next/src/app/layout.tsx"
      provides: "Root layout with metadataBase, title template, shared OG, viewport"
      exports: ["default", "metadata", "viewport"]
    - path: "next/src/app/page.tsx"
      provides: "Index page with page-specific metadata"
      exports: ["default", "metadata"]
  key_links:
    - from: "next/src/app/contacts/page.tsx"
      to: "next/src/components/sections/ContactForm.tsx"
      via: "import { ContactForm }"
      pattern: "import.*ContactForm"
    - from: "next/src/app/contacts/page.tsx"
      to: "next/src/components/sections/contacts/*"
      via: "imports 4 contacts-specific section components"
      pattern: "import.*from.*sections/contacts/"
    - from: "next/src/app/layout.tsx"
      to: "next/src/app/page.tsx"
      via: "Next.js metadata merging (layout defaults + page overrides)"
      pattern: "metadataBase"
    - from: "next/src/lib/navigation.ts"
      to: "next/src/components/layout/Header.tsx"
      via: "NAV_LINKS consumed by header for nav rendering"
      pattern: "href.*contacts"
---

<objective>
Create the /contacts page with 4 new server components (ContactsHero, CoordinatorCard, ContactMethodGrid, TrustBadges) reusing the existing ContactForm client component, and add SEO metadata to both / and /contacts pages via Next.js static Metadata API exports.

Purpose: Completes the second page of the site (contacts) and adds production-ready SEO metadata to both pages, matching the current production meta tags.
Output: contacts/page.tsx, 4 new section components, updated layout.tsx + page.tsx with metadata, updated navigation link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-contacts-page-seo/62-RESEARCH.md
@.planning/phases/61-index-page-migration/61-02-SUMMARY.md
@next/src/app/layout.tsx
@next/src/app/page.tsx
@next/src/lib/navigation.ts
@next/src/components/sections/ContactForm.tsx
@next/src/components/sections/ContactSection.tsx
@next/next.config.ts

<interfaces>
<!-- Existing components available for reuse -->

From next/src/components/sections/ContactForm.tsx:
```typescript
// Client Component with "use client"
export function ContactForm(): JSX.Element;
// Renders: name, phone (with +7 mask), interest select, description textarea, honeypot
// Handles: validation, spam protection, success state
// No changes needed -- reuse as-is
```

From next/src/lib/navigation.ts:
```typescript
export const NAV_LINKS: readonly { href: string; label: string }[];
// Currently: { href: '#contact', label: 'Контакты' } -- needs update to '/contacts'

export const PHONE_NUMBER: string;   // '+77015322478'
export const PHONE_DISPLAY: string;  // '+7\u00A0701\u00A0532\u00A024\u00A078'
export const EMAIL: string;          // 'kz@medicusunion.com'
export const COMPANY_NAME: string;   // 'MedicusUnion'
export const TAGLINE: string;
```

From next/src/app/layout.tsx:
```typescript
import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'MedicusUnion -- ...',
  description: '...',
};
// Needs restructuring: add metadataBase, title template, shared OG, viewport
```

<!-- Content extracted from contacts.html (verified in research) -->

Hero section:
- Badge text: "Свяжитесь с нами" (with chat icon)
- H1: "Контакты" (gradient text)
- Subtitle: "Оставьте заявку или свяжитесь напрямую -- наш координатор ответит в течение 24 часов"

Coordinator card:
- Avatar: Unsplash URL (download to public/images/)
- Name: "Айгерим"
- Title: "Старший медицинский координатор"
- Bio: "Выслушаю вашу ситуацию и помогу выбрать оптимальное решение. Бесплатно и без обязательств."

Contact methods (2x2 grid):
1. Phone: +7 701 532 24 78 (tel: link)
2. Email: kz@medicusunion.com (mailto: link)
3. Office: Астана, Казахстан (text)
4. Schedule: Пн--Пт 9:00--18:00 (text)

Trust badges (4 items):
1. "На связи 24/7"
2. "ISO 27001"
3. "Astana Hub Resident"
4. "10 000+ пациентов"

SEO meta tags (from research extraction):
- Index: title "MedicusUnion KZ -- Медицина мирового уровня для Казахстана"
- Index: description "Онлайн-консультации с европейскими врачами..."
- Index: og:description is DIFFERENT from meta description
- Contacts: title "Контакты -- MedicusUnion KZ"
- Contacts: description "Оставьте заявку или свяжитесь напрямую..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contacts page components and compose contacts/page.tsx</name>
  <files>
    next/src/components/sections/contacts/ContactsHero.tsx
    next/src/components/sections/contacts/CoordinatorCard.tsx
    next/src/components/sections/contacts/ContactMethodGrid.tsx
    next/src/components/sections/contacts/TrustBadges.tsx
    next/src/app/contacts/page.tsx
    next/src/lib/navigation.ts
    next/next.config.ts
  </files>
  <read_first>
    .planning/phases/62-contacts-page-seo/62-RESEARCH.md (full content extraction from contacts.html)
    next/src/components/sections/ContactForm.tsx (reused as-is)
    next/src/components/sections/ContactSection.tsx (reference for layout pattern)
    next/src/lib/navigation.ts (update #contact to /contacts)
    next/next.config.ts (add Unsplash remote pattern for coordinator avatar)
    next/src/app/globals.css (check available utility classes, glass token names)
  </read_first>
  <action>
**Step 1: Update navigation.ts** -- Change NAV_LINKS contacts entry from `{ href: '#contact', label: 'Контакты' }` to `{ href: '/contacts', label: 'Контакты' }`. This fixes Pitfall 4 from research. Leave all other exports unchanged.

**Step 2: Update next.config.ts** -- Add `images.remotePatterns` for Unsplash so the coordinator avatar works with next/image:
```typescript
images: {
  formats: ['image/avif', 'image/webp'],
  remotePatterns: [
    {
      protocol: 'https',
      hostname: 'images.unsplash.com',
    },
  ],
},
```

**Step 3: Create directory** `next/src/components/sections/contacts/` for contacts-specific components.

**Step 4: ContactsHero.tsx** -- Server Component (NO "use client"):
```tsx
import { MessageCircle } from 'lucide-react';
```
- Section: `className="pt-32 pb-12 md:pt-40 md:pb-16 text-center"` (pt-32 accounts for fixed header)
- Glass badge: `<div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/60 backdrop-blur-sm border border-white/80 text-sm font-medium text-mu-text-700 shadow-glass-sm mb-6">`
  - `<MessageCircle className="w-4 h-4 text-mu-blue" />`
  - `<span>Свяжитесь с{'\u00A0'}нами</span>`
- H1: `className="font-heading text-3xl md:text-5xl font-bold mb-4"` with inline gradient:
  - Wrap text in `<span className="bg-gradient-to-r from-mu-blue to-[#38C6F4] bg-clip-text text-transparent">Контакты</span>`
- Subtitle: `<p className="font-body text-lg text-mu-text-500 max-w-xl mx-auto">Оставьте заявку или свяжитесь напрямую{'\u00A0'}{'\u2014'}{'\u00A0'}наш координатор ответит в{'\u00A0'}течение 24{'\u00A0'}часов</p>`
- Container: `className="container mx-auto px-4 md:px-8"`
- Export as named: `export function ContactsHero()`

**Step 5: CoordinatorCard.tsx** -- Server Component (NO "use client"):
```tsx
import Image from 'next/image';
```
- Outer div: `className="flex items-start gap-4 mb-8"`
- Avatar: `<Image src="https://images.unsplash.com/photo-1559839734-2b71ea197ec2?auto=format&fit=crop&w=200&h=200&q=80" alt="Айгерим -- медицинский координатор" width={64} height={64} className="rounded-full object-cover shrink-0" />`
  - Use 200x200 crop version of the Unsplash URL (smaller than the 1080 in original)
- Info div:
  - Name: `<p className="font-heading text-lg font-bold text-mu-text-900">Айгерим</p>`
  - Title: `<p className="text-sm text-mu-blue font-semibold">Старший медицинский координатор</p>`
  - Bio: `<p className="text-sm text-mu-text-500 mt-1">Выслушаю вашу ситуацию и{'\u00A0'}помогу выбрать оптимальное решение. Бесплатно и{'\u00A0'}без обязательств.</p>`
- Export as named: `export function CoordinatorCard()`

**Step 6: ContactMethodGrid.tsx** -- Server Component (NO "use client"):
```tsx
import { Phone, Mail, MapPin, Clock } from 'lucide-react';
import { PHONE_NUMBER, PHONE_DISPLAY, EMAIL } from '@/lib/navigation';
```
- Outer grid: `className="grid grid-cols-2 gap-3 mb-6"`
- Each card: `className="liquid-card rounded-2xl p-5"` (uses existing glass card from liquid-glass.css)
- Card structure for each:
  - Icon: `className="w-5 h-5 text-mu-blue mb-3"`
  - Label: `<p className="text-xs text-mu-text-500 font-semibold mb-1">`
  - Value: text or link depending on type

Card 1 -- Phone:
  - `<Phone />` icon
  - Label: "Телефон"
  - Value: `<a href={\`tel:${PHONE_NUMBER}\`} className="text-sm text-mu-text-900 font-bold no-underline hover:text-mu-blue transition-colors">{PHONE_DISPLAY}</a>`

Card 2 -- Email:
  - `<Mail />` icon
  - Label: "Email"
  - Value: `<a href={\`mailto:${EMAIL}\`} className="text-sm text-mu-text-900 font-bold no-underline hover:text-mu-blue transition-colors">{EMAIL}</a>`

Card 3 -- Office:
  - `<MapPin />` icon
  - Label: "Офис"
  - Value: `<p className="text-sm text-mu-text-900 font-bold">Астана, Казахстан</p>`

Card 4 -- Schedule:
  - `<Clock />` icon
  - Label: "График"
  - Value: `<p className="text-sm text-mu-text-900 font-bold">Пн{'\u2013'}Пт 9:00{'\u2013'}18:00</p>`

- Export as named: `export function ContactMethodGrid()`

**Step 7: TrustBadges.tsx** -- Server Component (NO "use client"):
- Outer div: `className="flex flex-wrap gap-3"`
- 4 badges, each: `className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-emerald-50 text-xs font-semibold text-emerald-700"`
  - Green check SVG (16x16): `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="none" width="16" height="16" aria-hidden="true" className="shrink-0"><path d="M3.5 8l3 3 6-6" stroke="currentColor" strokeWidth="2" fill="none" strokeLinecap="round" strokeLinejoin="round"/></svg>`
  - `<span>` with badge text
- Badge texts: "На{'\u00A0'}связи 24/7", "ISO{'\u00A0'}27001", "Astana Hub Resident", "10{'\u00A0'}000+ пациентов"
- Export as named: `export function TrustBadges()`

**Step 8: Create app/contacts/page.tsx** -- Server Component page:
```tsx
import type { Metadata } from 'next';
import { ContactsHero } from '@/components/sections/contacts/ContactsHero';
import { CoordinatorCard } from '@/components/sections/contacts/CoordinatorCard';
import { ContactMethodGrid } from '@/components/sections/contacts/ContactMethodGrid';
import { TrustBadges } from '@/components/sections/contacts/TrustBadges';
import { ContactForm } from '@/components/sections/ContactForm';
```

Metadata export (static, not generateMetadata):
```typescript
export const metadata: Metadata = {
  title: 'Контакты',
  description: 'Оставьте заявку или свяжитесь напрямую. Медицинский координатор ответит в течение 24 часов. Телефон: +7 701 532 24 78.',
  alternates: {
    canonical: '/contacts',
  },
  openGraph: {
    title: 'Контакты \u2014 MedicusUnion KZ',
    description: 'Оставьте заявку или свяжитесь напрямую. Медицинский координатор ответит в течение 24 часов.',
    url: '/contacts',
  },
};
```

Page composition:
```tsx
export default function ContactsPage() {
  return (
    <>
      <ContactsHero />
      <section className="pb-12 md:pb-24" id="contact-section">
        <div className="container mx-auto px-4 md:px-8">
          <div className="grid md:grid-cols-2 gap-10 md:gap-12 items-start">
            {/* Left column: coordinator + contact methods + trust badges */}
            <div>
              <CoordinatorCard />
              <ContactMethodGrid />
              <TrustBadges />
            </div>
            {/* Right column: form card */}
            <div className="bg-white border border-black/8 rounded-2xl shadow-md p-8 md:p-10">
              <h2 className="font-heading text-2xl font-bold text-mu-text-900 mb-6">
                Оставить заявку
              </h2>
              <ContactForm />
            </div>
          </div>
        </div>
      </section>
    </>
  );
}
```

Page MUST:
- NOT use "use client" (Server Component -- ContactForm brings its own client boundary)
- NOT use FinalCTA (contacts.html does NOT have FinalCTA -- verified in research)
- NOT import any dynamic functions (must remain SSG)
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'/contacts'" next/src/lib/navigation.ts
    - test -f next/src/components/sections/contacts/ContactsHero.tsx
    - test -f next/src/components/sections/contacts/CoordinatorCard.tsx
    - test -f next/src/components/sections/contacts/ContactMethodGrid.tsx
    - test -f next/src/components/sections/contacts/TrustBadges.tsx
    - test -f next/src/app/contacts/page.tsx
    - grep -q "export function ContactsHero" next/src/components/sections/contacts/ContactsHero.tsx
    - grep -q "export function CoordinatorCard" next/src/components/sections/contacts/CoordinatorCard.tsx
    - grep -q "export function ContactMethodGrid" next/src/components/sections/contacts/ContactMethodGrid.tsx
    - grep -q "export function TrustBadges" next/src/components/sections/contacts/TrustBadges.tsx
    - grep -q "export const metadata" next/src/app/contacts/page.tsx
    - grep -q "export default function ContactsPage" next/src/app/contacts/page.tsx
    - grep -q "import.*ContactForm" next/src/app/contacts/page.tsx
    - ! grep -q "use client" next/src/app/contacts/page.tsx
    - ! grep -q "FinalCTA" next/src/app/contacts/page.tsx
    - grep -q "images.unsplash.com" next/next.config.ts
    - grep -q "PHONE_NUMBER\|PHONE_DISPLAY" next/src/components/sections/contacts/ContactMethodGrid.tsx
    - grep -q "liquid-card" next/src/components/sections/contacts/ContactMethodGrid.tsx
  </acceptance_criteria>
  <done>
    /contacts route exists with ContactsHero (glass badge + gradient H1), CoordinatorCard (avatar + bio), ContactMethodGrid (4 glass cards with phone/email/office/schedule), TrustBadges (4 green badges), and ContactForm (reused from Phase 61). Navigation "Контакты" link points to /contacts. TypeScript compiles. All contacts-specific components are Server Components (no "use client"). Unsplash remote pattern configured in next.config.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SEO metadata to layout.tsx and index page.tsx</name>
  <files>
    next/src/app/layout.tsx
    next/src/app/page.tsx
  </files>
  <read_first>
    next/src/app/layout.tsx (current metadata -- needs restructuring)
    next/src/app/page.tsx (add metadata export)
    .planning/phases/62-contacts-page-seo/62-RESEARCH.md (exact meta tag values from index.html and contacts.html)
  </read_first>
  <action>
**Step 1: Update layout.tsx metadata** -- Restructure the existing `metadata` export to use `metadataBase`, `title.template`, and shared OG fields. Add a separate `viewport` export for themeColor.

Replace the current metadata export block:
```typescript
import type { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  metadataBase: new URL('https://medicusunion.kz'),
  title: {
    default: 'MedicusUnion KZ \u2014 Медицина мирового уровня для Казахстана',
    template: '%s \u2014 MedicusUnion KZ',
  },
  openGraph: {
    type: 'website',
    locale: 'ru_RU',
    siteName: 'MedicusUnion',
  },
};

export const viewport: Viewport = {
  themeColor: '#38C6F4',
};
```

Key points:
- `metadataBase` resolves all relative URLs (canonical, og:url) to absolute
- `title.default` is used when page doesn't export its own title
- `title.template` wraps page-level titles: "Контакты" becomes "Контакты -- MedicusUnion KZ"
- `openGraph` shared fields (type, locale, siteName) merge with page-level OG
- `viewport` is a SEPARATE export (themeColor is deprecated in Metadata type in Next.js 15)
- Keep all existing layout.tsx code (fonts, body class, component imports) -- ONLY replace the metadata block

**Step 2: Add metadata to page.tsx** -- Add a `metadata` export to the index page. The title must use `absolute` to bypass the template (Pitfall 1 from research: index title should NOT get wrapped in template).

Add ABOVE the existing `export default function Home()`:
```typescript
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: {
    absolute: 'MedicusUnion KZ \u2014 Медицина мирового уровня для Казахстана',
  },
  description: 'Онлайн-консультации с европейскими врачами, лечение за рубежом, чек-ап в Samsung Medical Center и клиниках Стамбула. Организация под ключ.',
  alternates: {
    canonical: '/',
  },
  openGraph: {
    title: 'MedicusUnion KZ \u2014 Медицина мирового уровня для Казахстана',
    description: 'Онлайн-консультации с врачами Европы, лечение за рубежом, чек-ап в ведущих клиниках мира. 43 клиники, 11 стран, организация под ключ.',
    url: '/',
  },
};
```

CRITICAL: The `description` and `openGraph.description` are DIFFERENT for the index page (Pitfall 3 from research). Do NOT use the same string for both:
- `description` (meta): "Онлайн-консультации с европейскими врачами, лечение за рубежом, чек-ап в Samsung Medical Center и клиниках Стамбула. Организация под ключ."
- `openGraph.description`: "Онлайн-консультации с врачами Европы, лечение за рубежом, чек-ап в ведущих клиниках мира. 43 клиники, 11 стран, организация под ключ."

Keep the existing page component and all section imports unchanged. Only add the metadata + Metadata type import.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | grep -E "Route|Static|\/\s|\/contacts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "metadataBase" next/src/app/layout.tsx
    - grep -q "template.*MedicusUnion" next/src/app/layout.tsx
    - grep -q "export const viewport" next/src/app/layout.tsx
    - grep -q "themeColor" next/src/app/layout.tsx
    - grep -q "export const metadata" next/src/app/page.tsx
    - grep -q "absolute" next/src/app/page.tsx
    - grep -q "alternates" next/src/app/page.tsx
    - grep -q "openGraph" next/src/app/page.tsx
    - grep -q "Samsung Medical Center" next/src/app/page.tsx
    - grep -q "43 клиники" next/src/app/page.tsx
    - ! grep -q "use client" next/src/app/page.tsx
  </acceptance_criteria>
  <done>
    layout.tsx has metadataBase (https://medicusunion.kz), title template (%s -- MedicusUnion KZ), shared OG (type, locale, siteName), and separate viewport export (themeColor #38C6F4). page.tsx has absolute title (bypasses template), meta description, different OG description, canonical /, and OG url. Both pages are SSG (npm run build shows static). TypeScript compiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual parity verification for contacts page and SEO metadata</name>
  <files>none</files>
  <action>
Human verifies visual parity and SEO metadata correctness for the contacts page and both page metadata exports.

Steps:
1. Run `cd next && npm run dev` and open http://localhost:3000/contacts
2. Verify contacts page structure:
   - Hero: glass badge pill with chat icon + "Свяжитесь с нами", gradient "Контакты" heading, subtitle text
   - Left column: Coordinator photo (round avatar) + name "Айгерим" + title + bio text
   - Left column: 4 glass cards in 2x2 grid (Phone with tel link, Email with mailto link, Office, Schedule)
   - Left column: 4 green trust badges below cards
   - Right column: White card with "Оставить заявку" heading + contact form
3. Test form on contacts page:
   - Fill and submit with valid data -> success checkmark
   - Reload page to reset form state
4. Test responsive:
   - 375px: single column (info stacks above form)
   - 1440px: 2-column grid
5. Test navigation: Click "Контакты" in header -> navigates to /contacts (not scroll to #contact)
6. Verify SEO metadata:
   - View source of http://localhost:3000 -> check title, description, og:title, og:description (should be DIFFERENT from description), canonical
   - View source of http://localhost:3000/contacts -> check title contains "Контакты -- MedicusUnion KZ", description, og tags, canonical /contacts
7. Verify SSG: `cd next && npm run build` -> both / and /contacts shown as static (circle icon)
  </action>
  <verify>Human visual inspection at 1440px, 768px, 375px viewports + page source SEO metadata check</verify>
  <done>Human approves contacts page visual parity, navigation link works, SEO metadata correct for both pages, both pages are SSG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External image (Unsplash) -> Next.js Image | Coordinator avatar loaded from external CDN |
| User input -> ContactForm | Form accepts user input (reused from Phase 61, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Information Disclosure | SEO metadata | accept | Meta tags contain only public marketing content -- no sensitive data exposed |
| T-62-02 | Tampering | External image URL | accept | Unsplash is a trusted CDN; image is decorative (coordinator avatar). If Unsplash becomes unavailable, avatar fails gracefully with alt text |
| T-62-03 | Spoofing | ContactForm on /contacts | accept | Same form as index page -- no API submission in this phase. Server-side validation added in Phase 65 |
| T-62-04 | Denial of Service | Honeypot + timing | mitigate | Reuses Phase 61 ContactForm spam protection (hidden honeypot field + 3-second timing check) |
</threat_model>

<verification>
- `cd next && npx tsc --noEmit` passes with no errors
- `cd next && npm run build` shows both `/` and `/contacts` as static (SSG)
- `curl -s http://localhost:3000 | grep '<title>'` shows correct index title
- `curl -s http://localhost:3000/contacts | grep '<title>'` shows "Контакты -- MedicusUnion KZ"
- `curl -s http://localhost:3000 | grep 'og:description'` shows OG description (different from meta description)
- `curl -s http://localhost:3000/contacts | grep 'canonical'` shows /contacts (not /contacts.html)
- Navigation "Контакты" link href is "/contacts"
- No "use client" in contacts/page.tsx or any contacts/ section component
- ContactForm is imported and rendered on contacts page without modification
</verification>

<success_criteria>
/contacts page renders with glass-styled hero, coordinator card, 4 contact method glass cards, 4 trust badges, and reused ContactForm. Both / and /contacts have correct SEO metadata (title, description, OG, canonical) via static Metadata exports. Both pages are SSG. Navigation link updated. Human approves visual parity and metadata correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/62-contacts-page-seo/62-01-SUMMARY.md`
</output>
