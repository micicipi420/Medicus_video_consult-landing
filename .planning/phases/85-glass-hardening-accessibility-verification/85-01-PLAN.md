---
phase: 85-glass-hardening-accessibility-verification
plan: 01
type: execute
wave: 1
depends_on: ["80", "81", "82", "83", "84"]
files_modified:
  - next/src/app/globals.css
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
  - ACC-04
  - ACC-05
must_haves:
  truths:
    - "globals.css has @media (prefers-reduced-transparency: reduce) block"
    - "globals.css has @media (prefers-contrast: more) block"
    - "Both blocks disable backdrop-filter via !important"
    - "prefers-contrast forces [class*='bg-white/'] to opaque white"
    - "prefers-contrast forces [class*='border-white/'] to dark visible border"
    - "prefers-reduced-motion block (Phase 79) still present"
    - "v8.0 components have zero transition-all"
    - "Tap targets ≥44pt established in MobileMenu (5+) and StickyBar (2+)"
    - "focus-visible coverage for global form/link/button selectors in globals.css"
  artifacts:
    - path: "next/src/app/globals.css"
      provides: "v8.0 a11y hardening block covering prefers-reduced-transparency and prefers-contrast for utility-class glass surfaces"
  key_links:
    - from: "globals.css @media prefers-reduced-transparency"
      to: "every backdrop-filter consumer site-wide"
      via: "* { backdrop-filter: none !important }"
      pattern: "@media \\(prefers-reduced-transparency: reduce\\)"
    - from: "globals.css @media prefers-contrast"
      to: "Tailwind translucent white surfaces"
      via: "[class*='bg-white/'] { background-color: rgb(255 255 255) !important }"
      pattern: "@media \\(prefers-contrast: more\\)"
---

<objective>
Add the missing prefers-contrast / prefers-reduced-transparency hardening for utility-class glass surfaces, then audit static evidence for v8.0 a11y compliance. Live browser verification is the user's responsibility.
</objective>

<verification>
```bash
grep -c "prefers-reduced-transparency" next/src/app/globals.css   # 1
grep -c "prefers-contrast" next/src/app/globals.css                # 1
grep -c "prefers-reduced-motion" next/src/app/globals.css          # 1

# v8.0 components still have zero transition-all
grep -l "transition-all" next/src/components/sections/HeroHub.tsx next/src/components/sections/StatsBar.tsx next/src/components/sections/ServicesGrid.tsx next/src/components/sections/ProcessSection.tsx next/src/components/sections/ContactSection.tsx next/src/components/layout/HeaderClient.tsx next/src/components/layout/MobileMenu.tsx next/src/components/layout/StickyBar.tsx
# Expect: empty (no files match)

# focus-visible coverage
grep -c "focus-visible" next/src/app/globals.css   # 7
```
</verification>
