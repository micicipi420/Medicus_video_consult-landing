---
phase: 44-chrome-partials-upgrade
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - partials/header.html
  - partials/footer.html
  - partials/mobile-menu.html
  - partials/sticky-bar.html
  - index.html
  - online-consultations.html
  - treatment-abroad.html
  - checkup.html
  - contacts.html
  - 404.html
autonomous: false
requirements: [CHROME-01]
must_haves:
  truths:
    - "Header partial uses .liquid-regular glass material + .squircle-xl shape + max-w-content grid alignment"
    - "Footer partial uses .liquid-card glass material + .squircle-xl shape with shadow-wrap if outer shadow needed"
    - "Mobile menu partial uses .liquid-regular glass material + .squircle-xl shape on panel + .squircle-lg on menu items"
    - "Sticky bar partial uses .liquid-regular glass material + .squircle-lg shape"
    - "Per-page header--scrolled inline styles use CSS custom property overrides (--liquid-bg, --liquid-blur-md) instead of raw property values"
    - "Opening any of the 6 pages shows glass-styled chrome with no visual regression"
    - "make build + make check pass after all changes"
  artifacts:
    - path: "partials/header.html"
      provides: "Glass header with squircle shape and grid alignment"
      contains: "liquid-regular"
    - path: "partials/footer.html"
      provides: "Glass footer with squircle shape"
      contains: "liquid-card"
    - path: "partials/mobile-menu.html"
      provides: "Glass mobile menu with squircle shapes"
      contains: "liquid-regular"
    - path: "partials/sticky-bar.html"
      provides: "Glass sticky bar with squircle shape"
      contains: "liquid-regular"
  key_links:
    - from: "partials/header.html"
      to: "src/styles/liquid-glass.css"
      via: ".liquid-regular class"
      pattern: "liquid-regular"
    - from: "per-page inline <style>"
      to: "src/styles/theme.css liquid tokens"
      via: "CSS custom property overrides in .header--scrolled"
      pattern: "--liquid-bg"
    - from: "partials/footer.html"
      to: "src/styles/liquid-glass.css + squircles.css"
      via: ".liquid-card + .squircle-xl classes"
      pattern: "liquid-card"
---

<objective>
Upgrade all 4 chrome partials (header, footer, mobile-menu, sticky-bar) to use v4.0 Liquid Glass and squircle classes, and update the per-page header--scrolled inline styles to use CSS custom property overrides. This delivers CHROME-01: every page inherits the v4.0 visual language through the splicer pipeline.

Purpose: The glass/squircle primitives from Phases 42-43 exist as CSS classes but are not yet applied to any visible element. This plan wires them into the 4 shared chrome elements that appear on every page, achieving maximum visual impact from a single set of edits.

Output: Updated header/footer/mobile-menu/sticky-bar partials with glass+squircle classes, updated per-page inline styles, successful make build + make check, visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-chrome-partials-upgrade/44-RESEARCH.md
@.planning/phases/44-chrome-partials-upgrade/44-01-SUMMARY.md
@partials/header.html
@partials/footer.html
@partials/mobile-menu.html
@partials/sticky-bar.html
@src/styles/liquid-glass.css
@src/styles/squircles.css
</context>

<interfaces>
<!-- CSS classes available from Phase 42-43. Executor should use these directly. -->

From src/styles/liquid-glass.css:
```css
/* Base glass material -- inset-only shadows, safe inside mask */
.liquid-regular {
  background: var(--liquid-bg);
  backdrop-filter: blur(var(--liquid-blur-md)) saturate(var(--liquid-saturate)) brightness(var(--liquid-brightness));
  border-top: 1px solid var(--liquid-border-top);
  border-bottom: 1px solid var(--liquid-border-bottom);
  box-shadow: var(--liquid-shadow-inset-top), var(--liquid-shadow-inset-bottom);
}

/* Card variant -- glass + padding */
.liquid-card {
  /* same as .liquid-regular + padding: 1.5rem */
}

/* Shadow wrapper for outer shadow on squircle elements */
.liquid-card-wrap {
  box-shadow: var(--liquid-shadow-outer);
  border-radius: 24px;
}
```

From src/styles/squircles.css:
```css
.squircle-md  { border-radius: 16px; mask-image: var(--squircle-mask-md); }
.squircle-lg  { border-radius: 24px; mask-image: var(--squircle-mask-lg); }
.squircle-xl  { border-radius: 40px; mask-image: var(--squircle-mask-xl); }
.squircle-full { border-radius: 9999px; /* no mask */ }
```

From src/styles/theme.css (liquid tokens that .header--scrolled will override):
```css
:root {
  --liquid-bg: rgba(255, 255, 255, 0.18);
  --liquid-blur-md: 24px;
  --liquid-saturate: 180%;
  --liquid-brightness: 108%;
}
.dark {
  --liquid-bg: rgba(30, 40, 60, 0.45);
  --liquid-blur-md: 28px;
}
```

From src/styles/theme.css (legacy shadow being replaced):
```css
--shadow-glass-header: inset 0 1px 1px rgba(255,255,255,0.8), inset 0 -1px 1px rgba(0,0,0,0.05), 0 8px 32px rgba(0,0,0,0.05);
```

Current .header--scrolled inline style (identical in all 6 pages):
```css
.header--scrolled {
  background: rgba(255,255,255,0.5);
  backdrop-filter: blur(60px) saturate(180%);
  -webkit-backdrop-filter: blur(60px) saturate(180%);
  padding-top: 0.75rem;
  padding-bottom: 0.75rem;
}
```

Anti-patterns from liquid-glass.css + squircles.css:
- NEVER nest glass inside glass (header is glass; CTA inside is opaque gradient -- OK)
- NEVER apply box-shadow AND mask-image on same element (use inset-only or shadow-wrap)
- NEVER apply border to squircle element (use inset box-shadow instead; .liquid-regular already does this)
- NEVER use will-change: backdrop-filter
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade 4 chrome partials with glass + squircle classes</name>
  <files>partials/header.html, partials/footer.html, partials/mobile-menu.html, partials/sticky-bar.html</files>
  <read_first>partials/header.html, partials/footer.html, partials/mobile-menu.html, partials/sticky-bar.html, src/styles/liquid-glass.css, src/styles/squircles.css</read_first>
  <action>
Modify class attributes on the 4 chrome partials. Only change class attributes and wrapper structure -- NEVER modify text content, nbsp entities, SVG elements, {{...}} token placeholders, href values, or ARIA attributes.

### Header (partials/header.html)

On the `<header>` element (line 1), replace the class list:

REMOVE these classes:
- `max-w-7xl` (1280px -- replacing with 1200px token)
- `rounded-[2.5rem]` (replacing with squircle)
- `border-[0.5px] border-white/50` (replacing with liquid-regular inset border)
- `shadow-glass-header` (outer shadow clips under mask; replacing with liquid-regular inset shadows)
- `bg-white/30` (replacing with liquid-regular token-driven bg)
- `backdrop-blur-[40px]` (replacing with liquid-regular token-driven blur)
- `backdrop-saturate-[150%]` (replacing with liquid-regular token-driven saturate)

ADD these classes:
- `liquid-regular` (full glass material recipe with inset shadows)
- `squircle-xl` (40px superellipse shape)
- `max-w-[1200px]` (grid token alignment -- use Tailwind arbitrary value since max-w-content may be a CSS custom property, not a Tailwind utility)

KEEP these classes unchanged:
- `header` (BEM block)
- `fixed z-50 transition-all duration-500 top-4 left-4 right-4 mx-auto px-4 md:px-8 py-5`

Result header class string:
```
header liquid-regular squircle-xl fixed z-50 transition-all duration-500 top-4 left-4 right-4 mx-auto max-w-[1200px] px-4 md:px-8 py-5
```

On the menu button (the `<button class="header__menu-btn ...">` element), replace glass classes:
REMOVE: `bg-white/50 rounded-full backdrop-blur-xl backdrop-saturate-[180%] border border-white/50`
ADD: `liquid-regular squircle-full`
KEEP: `header__menu-btn lg:hidden p-2 text-mu-text-700`

Result menu button class string:
```
header__menu-btn lg:hidden p-2 text-mu-text-700 liquid-regular squircle-full
```

Do NOT change any classes on the inner header__inner div, nav links, logo, CTA button, phone link, or SVG icons. The CTA stays as an opaque gradient button (per LIQUID-03 -- primary CTA must NOT be glass).

### Footer (partials/footer.html)

On the `.footer__wrapper` div (line 3), replace the class list:

REMOVE these classes:
- `bg-white/60` (replacing with liquid-card)
- `backdrop-blur-3xl` (replacing with liquid-card)
- `rounded-[3rem]` (replacing with squircle)
- `border border-white/60` (replacing with liquid-card inset border)
- `shadow-glass-lg` (outer shadow -- use liquid-card inset-only shadows; if visual depth is insufficient, the wrapper approach below handles it)

ADD these classes:
- `liquid-card` (glass material + padding)
- `squircle-xl` (40px superellipse, close to the original 3rem=48px)

KEEP: `footer__wrapper p-12` (override liquid-card's default 1.5rem padding with the original 3rem)

Result footer__wrapper class string:
```
footer__wrapper liquid-card squircle-xl p-12
```

Note: liquid-card includes `padding: 1.5rem` but the Tailwind `p-12` (3rem) will override it. The footer needs more padding for its content layout.

On the 2 footer contact icon spans (`.footer__contact-icon` elements, lines 29 and 37), replace glass classes:
REMOVE: `glass-icon bg-white/60 backdrop-blur-md p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong`
ADD: `liquid-regular squircle-md p-2.5`
KEEP: `footer__contact-icon`

Result contact icon class string:
```
footer__contact-icon liquid-regular squircle-md p-2.5
```

Do NOT change text content (especially nbsp entities in phone number, email, copyright, badges), link hrefs, SVG icons, or any structural HTML.

### Mobile Menu (partials/mobile-menu.html)

On the `.mobile-menu` div (line 3, the inner panel), replace glass classes:

REMOVE these classes:
- `bg-white/60` (replacing with liquid-regular)
- `backdrop-blur-[80px]` (replacing with liquid-regular; will add Tailwind override for higher blur)
- `backdrop-saturate-[200%]` (replacing with liquid-regular)
- `shadow-glass-lg` (outer shadow clips under mask)
- `rounded-3xl` (replacing with squircle)
- `border-[0.5px] border-white/50` (replacing with liquid-regular inset border)

ADD these classes:
- `liquid-regular` (base glass material)
- `squircle-xl` (40px superellipse)
- `backdrop-blur-[80px]` (override liquid-regular's 24px blur to maintain the mobile menu's heavier blur for better readability over the overlay)

KEEP: `mobile-menu absolute top-24 left-4 right-4 overflow-hidden`

Result mobile-menu class string:
```
mobile-menu absolute top-24 left-4 right-4 liquid-regular squircle-xl backdrop-blur-[80px] overflow-hidden
```

Note: The `backdrop-blur-[80px]` Tailwind utility overrides `.liquid-regular`'s `blur(var(--liquid-blur-md))` because inline utility specificity is equal and order-dependent in the cascade. Since the Tailwind utility generates a separate property declaration that comes after the class, it will override. If it doesn't override (because liquid-regular uses var()), the executor should use `style="--liquid-blur-md: 80px"` as an inline style attribute instead.

On the nav links (lines 5-9, the `<a>` elements with `rounded-2xl` class), replace border-radius:
For each link that has `rounded-2xl` in its class list, REMOVE `rounded-2xl` and ADD `squircle-lg`.

On the CTA link (line 15, `.mobile-menu__cta`):
REMOVE: `rounded-2xl`
ADD: `squircle-lg`

On the phone link (line 11, `.mobile-menu__phone`):
REMOVE: `rounded-2xl`
ADD: `squircle-lg`

Do NOT change the overlay wrapper structure (`.mobile-menu-overlay` div). Do NOT change z-index values. Do NOT change text content or nbsp entities.

### Sticky Bar (partials/sticky-bar.html)

On the `.sticky-bar` div (line 1), replace glass classes:

REMOVE these classes:
- `bg-white/60` (replacing with liquid-regular)
- `backdrop-blur-3xl` (replacing with liquid-regular)
- `rounded-2xl` (replacing with squircle)
- `border border-white/60` (replacing with liquid-regular inset border)
- `shadow-glass-lg` (outer shadow clips under mask)

ADD these classes:
- `liquid-regular` (base glass material)
- `squircle-lg` (24px superellipse, matching original rounded-2xl)

KEEP: `sticky-bar fixed bottom-4 left-4 right-4 z-50 p-3 lg:hidden`
KEEP: `id="sticky-bar" role="complementary" aria-label="Быстрые действия"`

Result sticky-bar class string:
```
sticky-bar fixed bottom-4 left-4 right-4 z-50 liquid-regular squircle-lg p-3 lg:hidden
```

On the CTA button inside sticky bar:
REMOVE: `rounded-xl`
ADD: `squircle-md` (16px superellipse)

Do NOT change text content, phone nbsp entities, href, or ARIA attributes.

After editing all 4 partials, run `make build` to splice updated partials into all 6 pages, then run `make check`.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -q 'liquid-regular' partials/header.html && grep -q 'squircle-xl' partials/header.html && grep -q 'liquid-card' partials/footer.html && grep -q 'squircle-xl' partials/footer.html && grep -q 'liquid-regular' partials/mobile-menu.html && grep -q 'squircle-xl' partials/mobile-menu.html && grep -q 'liquid-regular' partials/sticky-bar.html && grep -q 'squircle-lg' partials/sticky-bar.html && make build && make check && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - partials/header.html contains `liquid-regular` AND `squircle-xl` AND `max-w-[1200px]`
    - partials/header.html does NOT contain `shadow-glass-header` or `bg-white/30` or `backdrop-blur-[40px]` or `max-w-7xl` or `rounded-[2.5rem]`
    - partials/footer.html contains `liquid-card` AND `squircle-xl`
    - partials/footer.html does NOT contain `bg-white/60` or `backdrop-blur-3xl` or `shadow-glass-lg` or `rounded-[3rem]`
    - partials/footer.html text content unchanged (grep -c '&amp;nbsp;' returns same count as before)
    - partials/mobile-menu.html contains `liquid-regular` AND `squircle-xl`
    - partials/mobile-menu.html does NOT contain `bg-white/60` or `shadow-glass-lg` or `rounded-3xl`
    - partials/sticky-bar.html contains `liquid-regular` AND `squircle-lg`
    - partials/sticky-bar.html does NOT contain `bg-white/60` or `backdrop-blur-3xl` or `shadow-glass-lg` or `rounded-2xl`
    - `make build` exits 0
    - `make check` exits 0
  </acceptance_criteria>
  <done>All 4 chrome partials use glass + squircle classes; no legacy inline glass properties remain; build + check pass</done>
</task>

<task type="auto">
  <name>Task 2: Update per-page header--scrolled inline styles to use liquid token overrides</name>
  <files>index.html, online-consultations.html, treatment-abroad.html, checkup.html, contacts.html, 404.html</files>
  <read_first>index.html, online-consultations.html, treatment-abroad.html, checkup.html, contacts.html, 404.html</read_first>
  <action>
In ALL 6 HTML pages, find the `.header--scrolled` block inside the `<style>` tag in the `<head>` section. The current definition is identical in all 6 pages:

```css
    .header--scrolled {
      background: rgba(255,255,255,0.5);
      backdrop-filter: blur(60px) saturate(180%);
      -webkit-backdrop-filter: blur(60px) saturate(180%);
      padding-top: 0.75rem;
      padding-bottom: 0.75rem;
    }
```

Replace with this CSS custom property override approach that works with `.liquid-regular`'s token-based values and respects the dark mode cascade:

```css
    .header--scrolled {
      --liquid-bg: rgba(255, 255, 255, 0.45);
      --liquid-blur-md: 60px;
      --liquid-saturate: 200%;
      padding-top: 0.75rem;
      padding-bottom: 0.75rem;
    }
    .dark .header--scrolled {
      --liquid-bg: rgba(30, 40, 60, 0.6);
      --liquid-blur-md: 60px;
      --liquid-saturate: 200%;
    }
```

This approach:
- Overrides the CSS custom properties that .liquid-regular reads via var(), so the glass material automatically applies the denser scroll-state values
- Removes raw `background` and `backdrop-filter` properties that would conflict with .liquid-regular
- Adds dark mode variant so scroll state works correctly when .dark class is on html
- Keeps padding reduction for the compressed scroll state
- The -webkit-backdrop-filter line is removed because .liquid-regular already includes -webkit-backdrop-filter with the same var() references -- the override propagates automatically

Apply this change to all 6 pages at their respective line positions:
1. index.html (lines 29-35)
2. online-consultations.html (lines 32-38)
3. treatment-abroad.html (lines 38-44)
4. checkup.html (lines 29-35)
5. contacts.html (lines 28-34)
6. 404.html (lines 18-24)

Do NOT modify any other inline styles in the `<style>` block (mobile-menu-overlay, is-invalid, FAQ accordion, form messages etc. must stay identical).

After updating all 6 pages, run `make build` then `make check`.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -c '\-\-liquid-bg' index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html | grep -v ':0$' | wc -l | xargs test 6 -eq && grep -c 'dark .header--scrolled' index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html | grep -v ':0$' | wc -l | xargs test 6 -eq && make build && make check && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 pages contain `--liquid-bg` in the .header--scrolled block (CSS custom property override)
    - All 6 pages contain `.dark .header--scrolled` block for dark mode scroll state
    - No page contains raw `background: rgba(255,255,255,0.5)` in header--scrolled anymore
    - No page contains raw `backdrop-filter: blur(60px)` in header--scrolled anymore
    - All other inline styles in each page's `<style>` block remain unchanged (mobile-menu-overlay, is-invalid, FAQ, form messages)
    - `make build` exits 0
    - `make check` exits 0
  </acceptance_criteria>
  <done>All 6 pages use CSS custom property overrides for header scroll state; dark mode scroll state defined; build + check pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of glass chrome across all pages</name>
  <files>index.html</files>
  <action>
Present the user with instructions to visually verify the glass chrome upgrade across all pages. No automated changes in this task -- it is a human verification checkpoint. The executor should open a browser and guide the user through the verification steps described in how-to-verify below.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make check && echo "Build gate OK"</automated>
  </verify>
  <what-built>
All 4 shared chrome partials (header, footer, mobile-menu, sticky-bar) now use v4.0 Liquid Glass material and squircle shapes. The SVG refraction filter is spliced into every page. The header scroll state uses token overrides for both light and dark mode.
  </what-built>
  <how-to-verify>
1. Open index.html in browser. Verify:
   - Header shows glass material (translucent with blur visible over the mesh background)
   - Header has squircle (rounded superellipse) shape, not standard border-radius
   - Header is centered with ~1200px max width
   - Scroll down -- header compresses (padding reduces) and glass becomes denser
   - Footer shows glass card treatment with squircle shape
   - Footer contact icons show glass material with squircle shape
   - Footer text is unchanged (phone number, email, copyright, badges)

2. On mobile viewport (< 1024px), verify:
   - Sticky bar at bottom shows glass material with squircle shape
   - Tap hamburger menu -- mobile menu panel shows glass material with squircle shape
   - Menu links have squircle-shaped hover areas
   - Menu CTA button has squircle shape

3. Toggle dark mode (if dark toggle exists). Verify:
   - Header glass shows dark recipe (darker tint, not murky)
   - Scroll state works in dark mode (glass becomes denser on scroll)
   - Footer glass shows dark recipe
   - Mobile menu glass shows dark recipe

4. Open at least one other page (e.g., contacts.html or checkup.html) and verify chrome looks identical to index.html.

5. Check no visual regression:
   - Text is readable over glass surfaces
   - CTA buttons remain opaque gradient (NOT glass)
   - No z-index issues (mobile menu overlay covers header, sticky bar is above content)
   - No visible border artifacts on squircle elements
  </how-to-verify>
  <resume-signal>Type "approved" if glass chrome looks correct, or describe any visual issues</resume-signal>
  <done>User has visually verified glass chrome renders correctly across all pages with no regressions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS specificity | .header--scrolled custom property overrides must beat .liquid-regular defaults |
| Mask + shadow | squircle mask clips outer shadows; all partials use inset-only (.liquid-regular) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03 | Information Disclosure | Glass transparency reveals content behind chrome | accept | .liquid-regular uses opaque-enough tint (18% opacity); text contrast verified in Phase 48 |
| T-44-04 | Denial of Service | backdrop-filter GPU cost on all chrome elements | accept | 4 glass surfaces is within GPU budget (research says up to 8 safe); Phase 48 measures FPS on budget Android |
| T-44-05 | Tampering | Per-page inline style edits could break other styles | mitigate | Only modify .header--scrolled block; verify all other inline styles unchanged via diff review |
</threat_model>

<verification>
1. `grep 'liquid-regular' partials/header.html` -- header uses glass material
2. `grep 'liquid-card' partials/footer.html` -- footer uses glass card material
3. `grep 'liquid-regular' partials/mobile-menu.html` -- mobile menu uses glass material
4. `grep 'liquid-regular' partials/sticky-bar.html` -- sticky bar uses glass material
5. `grep '\-\-liquid-bg' index.html` -- header--scrolled uses token overrides
6. `make build` exits 0
7. `make check` exits 0
8. Visual verification (checkpoint task)
</verification>

<success_criteria>
- All 4 chrome partials reference liquid glass classes (liquid-regular or liquid-card)
- All 4 chrome partials reference squircle classes (squircle-xl or squircle-lg)
- No legacy inline glass properties remain (bg-white/*, backdrop-blur-*, shadow-glass-*)
- Per-page header--scrolled uses CSS custom property overrides with dark mode variant
- make build exits 0 and make check exits 0
- Visual verification confirms glass-styled chrome with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/44-chrome-partials-upgrade/44-02-SUMMARY.md`
</output>
