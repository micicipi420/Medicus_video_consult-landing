---
phase: 44-chrome-partials-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - partials/svg-defs.html
  - scripts/build-pages.sh
  - index.html
  - online-consultations.html
  - treatment-abroad.html
  - checkup.html
  - contacts.html
  - 404.html
autonomous: true
requirements: [CHROME-02]
must_haves:
  truths:
    - "partials/svg-defs.html exists with hidden SVG containing filter id='liquid-refract'"
    - "scripts/build-pages.sh PARTIALS list includes svg-defs"
    - "All 6 HTML pages contain BUILD:svg-defs / /BUILD:svg-defs marker pairs"
    - "make build exits 0 and splices svg-defs content into all 6 pages"
    - "make check exits 0 (byte-identity gate passes)"
  artifacts:
    - path: "partials/svg-defs.html"
      provides: "Hidden SVG with feTurbulence + feDisplacementMap refraction filter"
      contains: "filter id=\"liquid-refract\""
    - path: "scripts/build-pages.sh"
      provides: "Splicer with svg-defs in PARTIALS list"
      contains: "svg-defs"
  key_links:
    - from: "partials/svg-defs.html"
      to: "all 6 HTML pages"
      via: "BUILD:svg-defs markers + splicer"
      pattern: "<!-- BUILD:svg-defs -->"
    - from: "liquid-glass.css refraction selectors"
      to: "partials/svg-defs.html"
      via: "url(#liquid-refract) references resolved by SVG defs in DOM"
      pattern: "filter id=\"liquid-refract\""
---

<objective>
Create the SVG defs partial for the liquid glass refraction filter and integrate it into the build pipeline, so that all 6 pages contain the refraction SVG filter element via automatic splicer distribution.

Purpose: CHROME-02 requires a new partials/svg-defs.html spliced into all pages. The refraction CSS selectors in liquid-glass.css reference url(#liquid-refract) which needs this SVG filter element present in the DOM. Without it, Chrome refraction PE silently fails.

Output: partials/svg-defs.html, updated build-pages.sh, BUILD:svg-defs markers in all 6 pages, successful make build + make check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-chrome-partials-upgrade/44-RESEARCH.md
@scripts/build-pages.sh
@partials/header.html
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From scripts/build-pages.sh:
```sh
# Line 19 -- PARTIALS list (currently 4, will be 5):
PARTIALS="header footer sticky-bar mobile-menu"

# Line 187-192 -- Marker validation (both open+close must exist):
OPEN_COUNT=$(grep -c "^<!-- BUILD:$NAME -->" "$FILE" || true)
CLOSE_COUNT=$(grep -c "^<!-- /BUILD:$NAME -->" "$FILE" || true)

# Line 224 -- awk matching (markers must be at column 0):
if ($0 == "<!-- BUILD:" partial " -->") {

# Line 243 -- Output message (hardcoded "4 partials"):
echo "[build-pages] $FILE updated (4 partials)"
```

From liquid-glass.css (refraction selectors that reference the SVG filter):
```css
html[data-refract="true"] .liquid-regular,
html[data-refract="true"] .liquid-card,
html[data-refract="true"] .stats-glass {
  backdrop-filter: url(#liquid-refract) blur(var(--liquid-blur-md)) ...;
}
```

BUILD marker format across all 6 pages (column 0, no leading whitespace):
```
<!-- BUILD:vars CTA_HREF=... CTA_LABEL="..." CURRENT_PAGE=... -->
  [mesh background content]
<!-- BUILD:header -->
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create svg-defs.html partial and update splicer</name>
  <files>partials/svg-defs.html, scripts/build-pages.sh</files>
  <read_first>scripts/build-pages.sh, partials/header.html</read_first>
  <action>
1. Create partials/svg-defs.html with this exact content (per CHROME-02 and ARCHITECTURE.md E.1):

```html
  <svg width="0" height="0" style="position:absolute" aria-hidden="true">
    <defs>
      <filter id="liquid-refract">
        <feTurbulence type="fractalNoise" baseFrequency="0.02" numOctaves="2" seed="4"/>
        <feDisplacementMap in="SourceGraphic" scale="8" xChannelSelector="R" yChannelSelector="G"/>
      </filter>
    </defs>
  </svg>
```

No script block -- initRefractionProbe() already runs from js/main.js initAll() on DOMContentLoaded. Adding a script would risk double-initialization.

2. In scripts/build-pages.sh, update line 19 to add svg-defs to PARTIALS:
   Change: `PARTIALS="header footer sticky-bar mobile-menu"`
   To:     `PARTIALS="header footer sticky-bar mobile-menu svg-defs"`

3. In scripts/build-pages.sh, update the comment block above PARTIALS (lines 14-18) to include svg-defs in the vocabulary documentation:
   Add to the comment:
   ```
   #   <!-- BUILD:svg-defs -->     ... <!-- /BUILD:svg-defs -->
   ```

4. In scripts/build-pages.sh, update the hardcoded "4 partials" on line 243:
   Change: `echo "[build-pages] $FILE updated (4 partials)"`
   To:     `echo "[build-pages] $FILE updated (5 partials)"`

Do NOT change any other lines in build-pages.sh. The existing awk splice loop handles any partial name -- no splice logic changes needed. Token substitution runs but is a no-op since svg-defs.html has no {{...}} placeholders.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && test -f partials/svg-defs.html && grep -q 'filter id="liquid-refract"' partials/svg-defs.html && grep -q 'svg-defs' scripts/build-pages.sh && grep -q '5 partials' scripts/build-pages.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - partials/svg-defs.html exists and contains `filter id="liquid-refract"` with feTurbulence + feDisplacementMap
    - scripts/build-pages.sh line 19 PARTIALS includes "svg-defs"
    - scripts/build-pages.sh output message says "5 partials" not "4 partials"
    - scripts/build-pages.sh comment block documents BUILD:svg-defs marker
    - No script block in svg-defs.html (refraction probe runs from main.js)
  </acceptance_criteria>
  <done>svg-defs.html exists with refraction filter; splicer recognizes it as a partial; output message is accurate</done>
</task>

<task type="auto">
  <name>Task 2: Add BUILD:svg-defs markers to all 6 pages and run build</name>
  <files>index.html, online-consultations.html, treatment-abroad.html, checkup.html, contacts.html, 404.html</files>
  <read_first>index.html, online-consultations.html, treatment-abroad.html, checkup.html, contacts.html, 404.html</read_first>
  <action>
Insert BUILD:svg-defs marker pair into each of the 6 HTML pages. The markers go on the line immediately after the BUILD:vars line, before any mesh background or other content. Both markers must be at column 0 (no leading whitespace) per splicer awk matching rules.

Insert these two lines immediately after the BUILD:vars line in each file:
```
<!-- BUILD:svg-defs -->
<!-- /BUILD:svg-defs -->
```

Exact insertion points (line numbers as of current state):

1. **index.html** -- After line 143 (`<!-- BUILD:vars ... CURRENT_PAGE=index -->`), before line 144 (mesh bg comment)
2. **online-consultations.html** -- After line 61 (`<!-- BUILD:vars ... CURRENT_PAGE=online -->`), before line 62 (mesh bg comment)
3. **treatment-abroad.html** -- After line 67 (`<!-- BUILD:vars ... CURRENT_PAGE=treatment -->`), before line 68 (mesh bg comment)
4. **checkup.html** -- After line 58 (`<!-- BUILD:vars ... CURRENT_PAGE=checkup -->`), before line 59 (mesh bg comment)
5. **contacts.html** -- After line 54 (`<!-- BUILD:vars ... CURRENT_PAGE=contacts -->`), before line 55 (mesh bg comment)
6. **404.html** -- After line 47 (`<!-- BUILD:vars ... CURRENT_PAGE=404 -->`), before line 48 (mesh bg comment)

CRITICAL RULES:
- Markers MUST be at column 0 (no spaces before `<!--`)
- Both opening and closing markers must be present (splicer validates 1 each)
- Do NOT modify any text content, nbsp entities, or ARIA attributes in any page
- After inserting markers in all 6 pages, run `make build` to splice svg-defs content
- Then run `make check` to verify byte-identity gate passes
- Verify: `grep -c '<!-- BUILD:svg-defs -->' index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html` must return exactly 1 for each file
- Verify: `grep 'filter id="liquid-refract"' index.html` confirms SVG defs were spliced in
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && make check && COUNT=$(grep -c '<!-- BUILD:svg-defs -->' index.html online-consultations.html treatment-abroad.html checkup.html contacts.html 404.html | grep -v ':1$' | wc -l) && test "$COUNT" -eq 0 && grep -q 'filter id="liquid-refract"' index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 pages have exactly 1 `<!-- BUILD:svg-defs -->` and 1 `<!-- /BUILD:svg-defs -->` marker
    - Markers are at column 0 (no leading whitespace) in each page
    - `make build` exits 0 and reports "5 partials" per page
    - `make check` exits 0 (byte-identity gate passes)
    - `grep 'filter id="liquid-refract"' index.html` returns a match (SVG defs spliced in)
    - Same grep on all 6 pages returns matches
    - No text content, nbsp entities, or ARIA attributes modified in any page
  </acceptance_criteria>
  <done>All 6 pages have BUILD:svg-defs markers; make build splices refraction filter into all pages; make check passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Splicer input | partials/svg-defs.html content spliced into all 6 pages -- trusted (repo-tracked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | svg-defs.html partial content | accept | File is repo-tracked; byte-identity pre-commit hook catches any drift |
| T-44-02 | Denial of Service | Missing BUILD:svg-defs markers causing build failure | mitigate | Verify all 6 pages have exactly 1 marker pair before build (grep count check) |
</threat_model>

<verification>
1. `test -f partials/svg-defs.html` -- partial file exists
2. `grep -q 'svg-defs' scripts/build-pages.sh` -- splicer knows about svg-defs
3. `grep -c '<!-- BUILD:svg-defs -->' *.html` -- all 6 pages have markers (6 lines, each showing :1)
4. `make build` exits 0
5. `make check` exits 0
6. `grep 'filter id="liquid-refract"' index.html` -- SVG defs spliced in
</verification>

<success_criteria>
- partials/svg-defs.html exists with refraction filter SVG
- scripts/build-pages.sh includes svg-defs in PARTIALS and reports "5 partials"
- All 6 HTML pages contain BUILD:svg-defs marker pairs at column 0
- make build exits 0 and splices SVG defs into all 6 pages
- make check exits 0 (byte-identity gate passes)
- grep confirms filter id="liquid-refract" present in all 6 built pages
</success_criteria>

<output>
After completion, create `.planning/phases/44-chrome-partials-upgrade/44-01-SUMMARY.md`
</output>
