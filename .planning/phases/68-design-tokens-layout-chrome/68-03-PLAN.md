---
phase: 68-design-tokens-layout-chrome
plan: 03
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - next/src/components/layout/Footer.tsx
  - next/src/components/layout/StickyBar.tsx
autonomous: true
requirements:
  - LAY-01
  - LAY-02
must_haves:
  truths:
    - "Footer renders as glass card (bg-white/60 backdrop-blur-3xl rounded-[3rem]) on light background instead of dark #1A365D"
    - "Footer has 4-column grid layout: Company, Services (Uslugi), Navigation (Navigatsiya), Contacts (Kontakty)"
    - "Footer includes legal entities: MedicusUnion GmbH (Wien) and TOO MedicusUnion KZ (Almaty, Astana Hub)"
    - "Footer bottom bar shows ISO 27001 / GDPR / Astana Hub trust badges with Shield icon"
    - "StickyBar uses glass styling (bg-white/60 backdrop-blur-3xl rounded-2xl) with gradient CTA"
    - "StickyBar CTA links to #contact on index page and /contacts on other pages via usePathname"
  artifacts:
    - path: "next/src/components/layout/Footer.tsx"
      provides: "Glass card footer with 4 columns, legal entities, trust badges"
      contains: "MedicusUnion GmbH"
    - path: "next/src/components/layout/StickyBar.tsx"
      provides: "Glass sticky bar with smart anchor routing"
      contains: "usePathname"
  key_links:
    - from: "next/src/components/layout/Footer.tsx"
      to: "next/src/lib/navigation.ts"
      via: "import FOOTER_SERVICES_LINKS, FOOTER_NAV_LINKS, PHONE_NUMBER, etc."
      pattern: "FOOTER_SERVICES_LINKS"
    - from: "next/src/components/layout/StickyBar.tsx"
      to: "next/navigation"
      via: "usePathname for smart CTA routing"
      pattern: "usePathname"
---

<objective>
Rewrite Footer to glass card design with 4-column layout and update StickyBar to glass styling with smart anchor routing. These are the final two layout chrome components completing Phase 68.

Purpose: LAY-01 requires the footer to match the new design (glass card, 4 columns, legal entities, trust badges). LAY-02 requires the StickyBar to use glass styling and link to #contact on index vs /contacts on service pages. Both depend on Plan 01's navigation.ts and globals.css updates.

Output: Rewritten Footer.tsx and StickyBar.tsx matching d450232 source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-design-tokens-layout-chrome/68-CONTEXT.md
@.planning/phases/68-design-tokens-layout-chrome/68-RESEARCH.md
@.planning/phases/68-design-tokens-layout-chrome/68-UI-SPEC.md
@.planning/phases/68-design-tokens-layout-chrome/68-01-SUMMARY.md

<interfaces>
<!-- Exports from navigation.ts AFTER Plan 01 updates it -->

From next/src/lib/navigation.ts (post-Plan-01):
```typescript
export const NAV_LINKS = [
  { href: '/consultations', label: 'Консультации' },
  { href: '/treatment-abroad', label: 'Лечение' },
  { href: '/checkup', label: 'Чек-ап' },
  { href: '/#why-us', label: 'О\u00A0компании' },
  { href: '/contacts', label: 'Контакты' },
] as const;

export const FOOTER_SERVICES_LINKS = [
  { href: '/consultations', label: 'Онлайн-консультации' },
  { href: '/treatment-abroad', label: 'Лечение за\u00A0рубежом' },
  { href: '/checkup', label: 'Чек-апы' },
] as const;

export const FOOTER_NAV_LINKS = [
  { href: '/', label: 'Главная' },
  { href: '/#clinics', label: 'Клиники и\u00A0врачи' },
  { href: '/#why-us', label: 'О\u00A0компании' },
  { href: '/contacts', label: 'Контакты' },
  { href: '/privacy', label: 'Политика конфиденциальности' },
] as const;

export const PHONE_NUMBER = '+77015322478';
export const PHONE_DISPLAY = '+7\u00A0701\u00A0532\u00A024\u00A078';
export const EMAIL = 'kz@medicusunion.com';
export const COMPANY_NAME = 'MedicusUnion';
export const TAGLINE = 'Международная медицинская платформа';
```

Glass shadow tokens available (from globals.css @theme inline):
```css
--shadow-glass-header: 0 8px 32px rgba(56, 198, 244, 0.08), 0 2px 8px rgba(0, 0, 0, 0.04);
--shadow-glass-lg: 0 12px 40px rgba(0, 0, 0, 0.06), 0 4px 12px rgba(0, 0, 0, 0.03);
--shadow-glass-inner-strong: inset 0 1px 2px rgba(255, 255, 255, 0.5), inset 0 -1px 2px rgba(0, 0, 0, 0.05);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Footer.tsx to glass card with 4-column layout</name>
  <files>next/src/components/layout/Footer.tsx</files>
  <read_first>
    next/src/components/layout/Footer.tsx
    next/src/lib/navigation.ts
    .planning/phases/68-design-tokens-layout-chrome/68-UI-SPEC.md
    .planning/phases/68-design-tokens-layout-chrome/68-CONTEXT.md
  </read_first>
  <action>
**Footer.tsx -- complete rewrite:**

Replace the entire file. The footer changes from dark `bg-[#1A365D]` to a glass card on light background. Uses 4-column grid layout with Company, Services, Navigation, and Contacts columns. Includes legal entities and trust badges in bottom bar.

```typescript
import Link from 'next/link';
import { Phone, Mail, Shield, Globe } from 'lucide-react';
import {
  FOOTER_SERVICES_LINKS,
  FOOTER_NAV_LINKS,
  PHONE_NUMBER,
  PHONE_DISPLAY,
  EMAIL,
  COMPANY_NAME,
  TAGLINE,
} from '@/lib/navigation';

export function Footer() {
  return (
    <footer className="relative overflow-hidden z-10 py-16">
      <div className="mx-auto max-w-7xl px-4 md:px-8">
        {/* Glass card container */}
        <div className="bg-white/60 backdrop-blur-3xl rounded-[3rem] p-12 border border-white/60 shadow-glass-lg">
          {/* 4-column grid */}
          <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-12 mb-12">
            {/* Column 1: Company */}
            <div>
              <h3 className="text-3xl font-extrabold tracking-tight bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent mb-4 drop-shadow-sm">
                {COMPANY_NAME}
              </h3>
              <p className="text-mu-text-700 font-medium leading-relaxed mb-4">
                {TAGLINE}
              </p>
              <div className="space-y-1">
                <p className="text-mu-text-700 text-sm font-medium leading-relaxed">
                  MedicusUnion GmbH&nbsp;&middot;&nbsp;Bruno-Marek-Allee&nbsp;20/50, 1020&nbsp;Wien, Austria
                </p>
                <p className="text-mu-text-700 text-sm font-medium leading-relaxed">
                  ТОО&nbsp;&laquo;MedicusUnion&nbsp;KZ&raquo;&nbsp;&middot;&nbsp;Казахстан, Алматы&nbsp;&middot;&nbsp;Резидент Astana&nbsp;Hub
                </p>
              </div>
            </div>

            {/* Column 2: Services (Услуги) */}
            <div>
              <h4 className="font-extrabold text-lg text-mu-text-900 mb-4">Услуги</h4>
              <div className="space-y-3">
                {FOOTER_SERVICES_LINKS.map((link) => (
                  <Link
                    key={link.href}
                    href={link.href}
                    className="block text-mu-text-700 hover:text-mu-blue transition-colors font-medium"
                  >
                    {link.label}
                  </Link>
                ))}
              </div>
            </div>

            {/* Column 3: Navigation (Навигация) */}
            <div>
              <h4 className="font-extrabold text-lg text-mu-text-900 mb-4">Навигация</h4>
              <div className="space-y-3">
                {FOOTER_NAV_LINKS.map((link) =>
                  link.href.startsWith('/') && !link.href.includes('#') ? (
                    <Link
                      key={link.href}
                      href={link.href}
                      className="block text-mu-text-700 hover:text-mu-blue transition-colors font-medium"
                    >
                      {link.label}
                    </Link>
                  ) : (
                    <a
                      key={link.href}
                      href={link.href}
                      className="block text-mu-text-700 hover:text-mu-blue transition-colors font-medium"
                    >
                      {link.label}
                    </a>
                  ),
                )}
              </div>
            </div>

            {/* Column 4: Contacts (Контакты) */}
            <div>
              <h4 className="font-extrabold text-lg text-mu-text-900 mb-4">Контакты</h4>
              <div className="space-y-4">
                <a
                  href={`tel:${PHONE_NUMBER}`}
                  className="flex items-center gap-3 group"
                >
                  <span className="bg-white/60 backdrop-blur-md p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong">
                    <Phone size={16} className="text-mu-blue" />
                  </span>
                  <span className="text-mu-text-900 font-medium group-hover:text-mu-blue transition-colors">
                    {PHONE_DISPLAY}
                  </span>
                </a>
                <a
                  href={`mailto:${EMAIL}`}
                  className="flex items-center gap-3 group"
                >
                  <span className="bg-white/60 backdrop-blur-md p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong">
                    <Mail size={16} className="text-mu-blue" />
                  </span>
                  <span className="text-mu-text-900 font-medium group-hover:text-mu-blue transition-colors">
                    {EMAIL}
                  </span>
                </a>
              </div>
            </div>
          </div>

          {/* Bottom bar */}
          <div className="border-t border-mu-text-300/30 pt-8 mt-8 flex flex-col md:flex-row md:items-center md:justify-between gap-4">
            <p className="text-mu-text-700 font-medium text-sm">
              &copy;&nbsp;2026 {COMPANY_NAME}. Все&nbsp;права защищены.
            </p>
            <div className="flex items-center gap-4 text-mu-text-700 text-sm font-medium">
              <span className="flex items-center gap-1.5">
                <Shield size={14} className="text-mu-green-600" strokeWidth={2} />
                ISO&nbsp;27001
              </span>
              <span>&middot;</span>
              <span>GDPR</span>
              <span>&middot;</span>
              <span className="flex items-center gap-1.5">
                <Globe size={14} className="text-mu-green-600" strokeWidth={2} />
                Astana&nbsp;Hub
              </span>
            </div>
          </div>
        </div>
      </div>
    </footer>
  );
}
```

Key differences from current:
- Outer wrapper: `relative overflow-hidden z-10 py-16` on light background instead of `bg-[#1A365D] text-white py-12 pb-8`
- Inner glass card: `bg-white/60 backdrop-blur-3xl rounded-[3rem] p-12 border border-white/60 shadow-glass-lg` (NEW)
- Grid: `grid md:grid-cols-2 lg:grid-cols-4 gap-12 mb-12` instead of `grid-cols-1 md:grid-cols-4 gap-8 md:gap-10 mb-10`
- Column 1 logo: gradient text `from-mu-blue via-mu-accent-blue to-mu-green-600` with `text-3xl font-extrabold tracking-tight` and `drop-shadow-sm`
- Column 1 legal: TWO legal entity lines (MedicusUnion GmbH + TOO MedicusUnion KZ) -- NEW content
- Column 2 (Services): uses `FOOTER_SERVICES_LINKS` (renamed from old FOOTER_NAV_LINKS)
- Column 3 (Navigation): uses NEW `FOOTER_NAV_LINKS` with 5 entries (Glavnaya, Kliniki i vrachi, O kompanii, Kontakty, Politika konfidentsialnosti)
- Column 4 (Contacts): phone and email with glass pill icons (`bg-white/60 backdrop-blur-md p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong`) -- replaces plain links
- Bottom bar: `border-t border-mu-text-300/30 pt-8 mt-8` with flex row, trust badges (Shield icon + ISO 27001, GDPR, Globe icon + Astana Hub) -- replaces simple white/10 border
- All text colors: `text-mu-text-700` and `text-mu-text-900` instead of `text-white` and `text-white/70`
- Link hover: `hover:text-mu-blue` instead of `hover:text-[#2B6CB0]`
- App Store / Google Play badges: NOT included (CONTEXT decision: skip -- no live apps)
- Import changes: `FOOTER_SERVICES_LINKS` and `FOOTER_NAV_LINKS` from navigation.ts; `Phone, Mail, Shield, Globe` from lucide-react; `Link` from next/link
- nbsp usage in legal text: `&nbsp;` between subject+verb and in addresses to prevent orphan line breaks per project convention
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Footer.tsx does NOT contain `bg-[#1A365D]` (old dark background removed)
    - Footer.tsx does NOT contain `text-white` as a standalone class (no longer dark footer)
    - Footer.tsx contains `bg-white/60 backdrop-blur-3xl rounded-[3rem]` (glass card)
    - Footer.tsx contains `shadow-glass-lg` (glass shadow)
    - Footer.tsx contains `grid md:grid-cols-2 lg:grid-cols-4 gap-12` (4-column layout)
    - Footer.tsx contains `MedicusUnion GmbH` (Austrian legal entity)
    - Footer.tsx contains `MedicusUnion\u00A0KZ` or `MedicusUnion KZ` (Kazakh legal entity)
    - Footer.tsx contains `Резидент Astana` (Astana Hub mention)
    - Footer.tsx contains `Услуги` (Services column heading)
    - Footer.tsx contains `Навигация` (Navigation column heading)
    - Footer.tsx contains `Контакты` (Contacts column heading)
    - Footer.tsx contains `import { Phone, Mail, Shield, Globe } from 'lucide-react'`
    - Footer.tsx contains `FOOTER_SERVICES_LINKS` import
    - Footer.tsx contains `FOOTER_NAV_LINKS` import
    - Footer.tsx contains `shadow-glass-inner-strong` (contact icon glass pills)
    - Footer.tsx contains `ISO\u00A027001` or `ISO 27001` (trust badge)
    - Footer.tsx contains `GDPR` (trust badge)
    - Footer.tsx contains `Astana\u00A0Hub` or `Astana Hub` (trust badge)
    - Footer.tsx contains `from-mu-blue via-mu-accent-blue to-mu-green-600` (logo gradient)
    - Footer.tsx contains `border-t border-mu-text-300/30` (bottom bar border)
    - Footer.tsx does NOT contain any App Store or Google Play references
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Footer renders as glass card on light background with 4 columns (Company with legal entities, Services, Navigation, Contacts with glass pill icons), gradient logo, and trust badges in bottom bar. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite StickyBar.tsx with glass styling and smart anchor routing</name>
  <files>next/src/components/layout/StickyBar.tsx</files>
  <read_first>
    next/src/components/layout/StickyBar.tsx
    .planning/phases/68-design-tokens-layout-chrome/68-UI-SPEC.md
    .planning/phases/68-design-tokens-layout-chrome/68-CONTEXT.md
  </read_first>
  <action>
**StickyBar.tsx -- rewrite the component:**

Replace the entire file. The StickyBar transitions from a solid white bottom bar to a glass floating bar with smart anchor routing via `usePathname`.

```typescript
'use client';

import { useState, useEffect } from 'react';
import { usePathname } from 'next/navigation';
import { PHONE_NUMBER, PHONE_DISPLAY } from '@/lib/navigation';

export function StickyBar() {
  const [isHidden, setIsHidden] = useState(false);
  const pathname = usePathname();
  const ctaHref = pathname === '/' ? '#contact' : '/contacts';

  useEffect(() => {
    const contactSection = document.querySelector('#contact');
    const footerElement = document.querySelector('footer');

    const targets = [contactSection, footerElement].filter(Boolean) as Element[];
    if (targets.length === 0) return;

    const visibleSet = new Set<Element>();

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            visibleSet.add(entry.target);
          } else {
            visibleSet.delete(entry.target);
          }
        });
        setIsHidden(visibleSet.size > 0);
      },
      { threshold: 0 },
    );

    targets.forEach((target) => observer.observe(target));

    return () => {
      observer.disconnect();
    };
  }, []);

  return (
    <div
      className={`fixed bottom-4 left-4 right-4 z-50 bg-white/60 backdrop-blur-3xl rounded-2xl border border-white/60 shadow-glass-lg p-3 lg:hidden transition-transform duration-300 ${isHidden ? 'translate-y-[calc(100%+16px)]' : ''}`}
      role="complementary"
      aria-label="Быстрые действия"
    >
      <div className="flex items-center justify-between gap-3">
        <a
          href={`tel:${PHONE_NUMBER}`}
          className="text-mu-text-900 font-medium text-sm"
          aria-label={`Позвонить ${PHONE_DISPLAY}`}
        >
          {PHONE_DISPLAY}
        </a>
        <a
          href={ctaHref}
          className="bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-6 py-3 rounded-xl font-extrabold shadow-lg shadow-mu-blue/30 text-sm"
        >
          Обсудить случай
        </a>
      </div>
    </div>
  );
}
```

Key differences from current:
- Position: `fixed bottom-4 left-4 right-4` instead of `fixed bottom-0 left-0 right-0` (floating with 16px margins)
- Glass: `bg-white/60 backdrop-blur-3xl rounded-2xl border border-white/60 shadow-glass-lg` instead of `bg-white border-t border-black/8 shadow-[0_-2px_8px_rgba(0,0,0,0.06)]`
- Padding: `p-3` instead of `py-2`
- Hide translation: `translate-y-[calc(100%+16px)]` instead of `translate-y-full` (accounts for bottom-4 offset so bar fully hides below viewport)
- Inner layout: `flex items-center justify-between gap-3` directly (removed container div wrapper)
- Phone: `text-mu-text-900 font-medium text-sm` instead of `font-heading text-[1rem] font-bold text-[#1A4D80]` (removed min-h-12, inline-flex)
- CTA: gradient `bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-6 py-3 rounded-xl font-extrabold shadow-lg shadow-mu-blue/30 text-sm` instead of `btn-primary btn-sticky`
- CTA text: "Обсудить случай" instead of "Оставить заявку" (matches header and mobile menu CTA)
- CTA href: SMART ROUTING via `usePathname()` -- `#contact` on index page (`/`), `/contacts` on all other pages
- aria-label: "Быстрые действия" instead of English "Quick actions"
- Import: added `usePathname` from `next/navigation`
- IntersectionObserver logic: preserved exactly from current implementation (same visibleSet pattern)
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - StickyBar.tsx contains `import { usePathname } from 'next/navigation'`
    - StickyBar.tsx contains `const pathname = usePathname()`
    - StickyBar.tsx contains `pathname === '/' ? '#contact' : '/contacts'`
    - StickyBar.tsx contains `fixed bottom-4 left-4 right-4 z-50`
    - StickyBar.tsx contains `bg-white/60 backdrop-blur-3xl rounded-2xl`
    - StickyBar.tsx contains `border border-white/60 shadow-glass-lg`
    - StickyBar.tsx contains `translate-y-[calc(100%+16px)]`
    - StickyBar.tsx contains `Обсудить случай` (CTA text)
    - StickyBar.tsx contains `from-mu-blue to-mu-accent-blue text-white` (gradient CTA)
    - StickyBar.tsx contains `rounded-xl font-extrabold shadow-lg shadow-mu-blue/30 text-sm` (CTA style)
    - StickyBar.tsx contains `Быстрые действия` (Russian aria-label)
    - StickyBar.tsx contains `href={ctaHref}` (smart routing)
    - StickyBar.tsx does NOT contain `btn-primary` or `btn-sticky`
    - StickyBar.tsx does NOT contain `bottom-0 left-0 right-0` (old flush positioning)
    - StickyBar.tsx IntersectionObserver pattern is preserved (visibleSet, threshold: 0)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>StickyBar renders as floating glass bar with gradient CTA that routes to #contact on index and /contacts on service pages. IntersectionObserver hide logic preserved. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 68 Plan 03 is purely presentational layout component changes. No data flow, no form handling, no authentication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | I (Information Disclosure) | Footer legal entities | accept | Legal entity information is intentionally public (builds trust with 45+ audience per CONTEXT.md decision). |
| T-68-04 | I (Information Disclosure) | Footer/StickyBar contact info | accept | Phone number and email are intentionally public for marketing purposes. |
</threat_model>

<verification>
1. `cd next && npx next build` exits 0 (TypeScript compilation + Next.js build)
2. Footer renders as glass card with `bg-white/60 backdrop-blur-3xl rounded-[3rem]`
3. Footer has 4-column grid (Company, Uslugi, Navigatsiya, Kontakty)
4. Footer includes MedicusUnion GmbH and TOO MedicusUnion KZ legal entities
5. Footer bottom bar has ISO 27001 / GDPR / Astana Hub trust badges with icons
6. Footer does not have App Store / Google Play badges
7. StickyBar uses glass styling with `bg-white/60 backdrop-blur-3xl rounded-2xl`
8. StickyBar CTA routes to `#contact` on `/` and `/contacts` on other pages
9. StickyBar CTA text is "Obsudit sluchay" with gradient
10. StickyBar IntersectionObserver hide behavior preserved
</verification>

<success_criteria>
- Build succeeds with zero errors
- Footer is a glass card, not a dark solid block
- 4 columns with all content from UI-SPEC copywriting contract
- Legal entities present in Company column
- Trust badges present in bottom bar
- StickyBar is a floating glass bar, not a flush bottom bar
- StickyBar smart routing works (index -> #contact, other pages -> /contacts)
</success_criteria>

<output>
After completion, create `.planning/phases/68-design-tokens-layout-chrome/68-03-SUMMARY.md`
</output>
