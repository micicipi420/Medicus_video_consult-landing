---
phase: 95-audit-and-verification
plan: 00
type: execute
wave: 0
subsystem: phase-bootstrap
depends_on: []
files_modified:
  - next/package.json
  - next/pnpm-lock.yaml
  - .planning/phases/95-audit-and-verification/lighthouse/.gitkeep
  - .planning/phases/95-audit-and-verification/axe/.gitkeep
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-04
must_haves:
  truths:
    - "All audit tooling is installed in next/package.json devDependencies BEFORE any audit plan runs — single coordinated install avoids Wave 1 lockfile conflicts on the four parallel audit plans"
    - "@lhci/cli installed (AUDIT-01 driver)"
    - "@axe-core/playwright + axe-core installed (AUDIT-02 + AUDIT-04 a11y emulation driver)"
    - "Phase 95 audit output directories pre-created with .gitkeep so per-plan writes do not race on mkdir"
    - "pnpm --dir next build green; existing Playwright + visual baseline + submission-e2e suites remain green (no regression from new devDeps)"
  artifacts:
    - path: "next/package.json"
      provides: "devDependencies entries for @lhci/cli, @axe-core/playwright, axe-core"
      contains: "@lhci/cli"
    - path: ".planning/phases/95-audit-and-verification/lighthouse/.gitkeep"
      provides: "Output dir for AUDIT-01 (95-01) — Lighthouse HTML/JSON reports per route"
    - path: ".planning/phases/95-audit-and-verification/axe/.gitkeep"
      provides: "Output dir for AUDIT-02 (95-02) — axe violation JSON per route × a11y mode"
  key_links:
    - from: "next/package.json"
      to: "Wave 1 plans 95-01 and 95-02"
      via: "shared devDependency installation"
      pattern: "@lhci/cli|@axe-core/playwright"
---

<objective>
Bootstrap Phase 95 with a single coordinated devDependency install + output directory scaffolding so the four Wave 1 audit plans (95-01 Lighthouse, 95-02 axe-core, 95-03 brand review, 95-04 v9.0 VER-* rollover) can execute fully in parallel without lockfile races on `next/package.json`.

Purpose: Wave 1 plans 95-01 and 95-02 both need to add devDependencies. If each plan ran its own `pnpm add -D` they would race on `next/pnpm-lock.yaml` when executed in parallel. Concentrating the install + output-dir scaffolding here keeps the Wave 1 plans purely additive (config files + spec files + report writes) and eliminates the only file-conflict point.

Output: Updated `next/package.json` with `@lhci/cli`, `@axe-core/playwright`, `axe-core` as devDependencies; `.planning/phases/95-audit-and-verification/lighthouse/.gitkeep` and `.planning/phases/95-audit-and-verification/axe/.gitkeep` committed.

**Note on canonical Next.js path:** This phase depends on Phase 94 — specifically POL-05 (single canonical Next.js source location). At plan-write time the dev server lives at `next/` (`next/src/app/...`, `next/playwright.config.ts`). All paths in 95-01..04 reference `next/...`. If Phase 94 POL-05 keeps `next/` canonical, no further action. If POL-05 moves the canonical Next.js app to repo-root `src/`, all 95-NN plans must be re-pathed before Wave 1 runs. Document the resolution in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@next/package.json
@next/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install audit tooling devDependencies in next/package.json</name>
  <files>next/package.json, next/pnpm-lock.yaml</files>
  <action>
Run a SINGLE pnpm install that adds all three audit tools at once (one lockfile mutation, not three):

```bash
pnpm --dir next add -D @lhci/cli @axe-core/playwright axe-core
```

Verify resulting `next/package.json` devDependencies block contains all three packages with non-`*` versions:
- `@lhci/cli` — Lighthouse CI runner (AUDIT-01)
- `@axe-core/playwright` — Playwright integration of axe-core (AUDIT-02, AUDIT-04)
- `axe-core` — explicit peer pin so the @axe-core/playwright wrapper does not pull in a divergent version on later installs

After install, run `pnpm --dir next build` and confirm exit 0 — the new devDeps must not break Next.js's build (Next is sensitive to ESM/CJS shape changes in its dev tree). Run `pnpm --dir next lint` and confirm exit 0.

Re-run the existing Playwright visual-baseline suite to confirm no regression from the new devDeps:
```bash
pnpm --dir next exec playwright test --project=desktop tests/visual
```
Expect 4/4 green (existing baseline).
  </action>
  <verify>
    <automated>pnpm --dir next list @lhci/cli @axe-core/playwright axe-core --depth=0 | grep -E "@lhci/cli|@axe-core/playwright|axe-core" | grep -c "^" | grep -q "^3$" && pnpm --dir next build && pnpm --dir next lint</automated>
  </verify>
  <done>Three devDeps present in `next/package.json`; build green; lint green; existing Playwright visual-regression suite green; lockfile committed.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Phase 95 audit output directories</name>
  <files>.planning/phases/95-audit-and-verification/lighthouse/.gitkeep, .planning/phases/95-audit-and-verification/axe/.gitkeep</files>
  <action>
Create empty placeholder directories so Wave 1 plans 95-01 and 95-02 can write reports without racing on `mkdir -p`:

```bash
mkdir -p .planning/phases/95-audit-and-verification/lighthouse \
         .planning/phases/95-audit-and-verification/axe
touch    .planning/phases/95-audit-and-verification/lighthouse/.gitkeep \
         .planning/phases/95-audit-and-verification/axe/.gitkeep
```

(`brand-review.md` and `95-VERIFICATION.md` are single files written by 95-03 and 95-04 respectively; no pre-create needed.)

Add a brief README to each directory header (in `.gitkeep` itself as a comment is fine, or a sibling `README.md`) noting which audit plan owns the directory and the expected file shape:

```
.planning/phases/95-audit-and-verification/lighthouse/.gitkeep
  # Owner: 95-01-PLAN (AUDIT-01). Expected outputs:
  #   {route-slug}.report.html      (Lighthouse HTML report per route)
  #   {route-slug}.report.json      (Lighthouse JSON report per route)
  #   summary.md                    (per-route budget pass/fail table)

.planning/phases/95-audit-and-verification/axe/.gitkeep
  # Owner: 95-02-PLAN (AUDIT-02). Expected outputs:
  #   {route-slug}--{a11y-mode}.json   (axe violations JSON per route × mode)
  #   summary.md                       (per-route + per-mode violation table)
```
  </action>
  <verify>
    <automated>test -f .planning/phases/95-audit-and-verification/lighthouse/.gitkeep && test -f .planning/phases/95-audit-and-verification/axe/.gitkeep</automated>
  </verify>
  <done>Two output directories created with `.gitkeep` placeholders; ready for Wave 1 plan writes.</done>
</task>

</tasks>

<acceptance_gates>

**Gate 1 — devDeps land cleanly:** All three packages (`@lhci/cli`, `@axe-core/playwright`, `axe-core`) appear in `next/package.json` devDependencies; lockfile updated; build green; lint green. **If any pin fails resolution** (e.g., npm registry hiccup, peer-dep conflict): retry once with `--no-frozen-lockfile`; if still failing, document the error in SUMMARY and halt — Wave 1 cannot start.

**Gate 2 — no regression on existing suites:** Existing Playwright visual baseline (4 routes × 2 breakpoints = 8 PNGs) still green. **If any baseline pixel-diff regresses after install** (extremely unlikely — devDeps shouldn't touch runtime), document the failing snapshot, do NOT update baselines, and halt — investigate before Wave 1.

**Gate 3 — POL-05 path resolution acknowledged:** SUMMARY explicitly states which canonical Next.js path Phase 94 chose (or "Phase 94 not yet run; defaulting to next/") and confirms 95-01..04 paths align.

</acceptance_gates>

<verification>

After completion:
1. `pnpm --dir next list @lhci/cli @axe-core/playwright axe-core --depth=0` shows all three.
2. `pnpm --dir next build && pnpm --dir next lint` exit 0.
3. `ls .planning/phases/95-audit-and-verification/{lighthouse,axe}/.gitkeep` returns 2 files.
4. `pnpm --dir next exec playwright test --project=desktop tests/visual` 4/4 green.

</verification>

<success_criteria>

- `next/package.json` contains `@lhci/cli`, `@axe-core/playwright`, `axe-core` in devDependencies.
- Output directories pre-created.
- Build + lint + existing Playwright suites all green.
- Wave 1 plans (95-01..04) can run fully in parallel — they touch disjoint file sets.

</success_criteria>

<output>

Create `.planning/phases/95-audit-and-verification/95-00-SUMMARY.md` per the standard SUMMARY template with:
- Resolved versions of the three new devDeps (paste from `pnpm list`).
- POL-05 path resolution status (which canonical Next.js path Phase 94 chose, or "TBD — defaulting to next/").
- Confirmation that existing Playwright suite did not regress.
- Hand-off note: Wave 1 plans 95-01..04 are now unblocked and can execute in parallel.

</output>
