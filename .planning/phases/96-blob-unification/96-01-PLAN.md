---
phase: 96-blob-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/lib/blob-engine/canvas-renderer.ts
  - next/tests/visual/blob-halo-feather.spec.ts
autonomous: true
requirements: [BR-01]
must_haves:
  truths:
    - "User scrolling at 200% zoom on /index sees no visible alpha-stop ring at the halo edge"
    - "Mobile 375x667 ambient blob shows smooth halo falloff with no banded boundary"
    - "Halo gradient extends visibly beyond the 0.7 stop position previously used"
  artifacts:
    - path: next/src/lib/blob-engine/canvas-renderer.ts
      provides: "Updated drawHalo with longer feather + intermediate stop"
      contains: "drawHalo"
    - path: next/tests/visual/blob-halo-feather.spec.ts
      provides: "Playwright zoom-in screenshot proving smooth alpha falloff (desktop + mobile)"
  key_links:
    - from: "next/src/lib/blob-engine/canvas-renderer.ts:drawHalo"
      to: "createRadialGradient + addColorStop chain"
      via: "stop array tuned from [0.0, 0.7, 1.0] -> [0.0, 0.35, 0.65, 1.0] with mid-feather"
      pattern: "addColorStop\\("
---

<objective>
Feather the Living Blob halo so the gradient stop boundary is no longer
visible at any zoom level. Replace the current 3-stop hard transition
(inner -> 0.7 stop -> transparent) with a 4-stop smooth alpha curve and
extend the radius slightly so the falloff has more space to die.

Purpose: Closes BR-01. User reported visible halo edge ("видны границы
свечения блоба") — the cause is the single intermediate stop at 0.7 of
the radius, which compresses the visible alpha gradient into the outer
30% of the disk and makes the transition read as a ring.

Output: One renderer file edit (drawHalo only — body/core/glint
unchanged) + one Playwright visual spec that proves the feather at
zoomed desktop + mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@design/LIQUID_GLASS_BLOB_TZ.md
@next/src/lib/blob-engine/canvas-renderer.ts
@next/tests/visual/baseline.spec.ts

<!-- INVESTIGATION FINDINGS (2026-04-30, planner) -->
<!-- Renderer file: next/src/components/effects/LivingBlobField.tsx (thin shell) -->
<!--                + next/src/lib/blob-engine/* (engine modules) -->
<!-- Halo current state — canvas-renderer.ts:70-85: -->
<!--   const baseRadius = 300 + 100 * heat;                            (max ~400px) -->
<!--   const radius = baseRadius * (1 + Math.min(0.6, velocity/1500)); -->
<!--   grad.addColorStop(0,    haloColor);     // rgba(98,221,177,0.5) -->
<!--   grad.addColorStop(0.7,  edgeColor);     // rgba(125,205,255,0.18)  <- ring shows here -->
<!--   grad.addColorStop(1,    'rgba(0,0,0,0)'); -->
<!-- Body uses 0.6 stop, also worth gentle review but not in BR-01 scope. -->
<!-- Globals palette source: next/src/app/globals.css:240-244 — DO NOT mutate palette. -->

<interfaces>
From next/src/lib/blob-engine/canvas-renderer.ts:
```typescript
function drawHalo(
  ctx: CanvasRenderingContext2D,
  x: number, y: number,
  heat: number, velocity: number,
  haloColor: string, edgeColor: string,
): void
```
Called from drawFrame() with composite-op `'screen'` already set by caller.
Other layers (drawBody / drawCore / drawGlint) MUST remain untouched in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Soften halo gradient stops in canvas-renderer.ts</name>
  <files>next/src/lib/blob-engine/canvas-renderer.ts</files>
  <action>
  Edit ONLY the drawHalo function (lines 70-85). Replace the 3-stop chain:
    grad.addColorStop(0,   haloColor);
    grad.addColorStop(0.7, edgeColor);
    grad.addColorStop(1,   'rgba(0,0,0,0)');
  with a 4-stop feather chain:
    grad.addColorStop(0.00, haloColor);                 // inner full alpha
    grad.addColorStop(0.35, haloColor);                 // hold inner color longer (smaller bright disk)
    grad.addColorStop(0.65, edgeColor);                 // mid-feather using existing edge token
    grad.addColorStop(1.00, 'rgba(0,0,0,0)');           // outer fully transparent
  Also extend baseRadius from `300 + 100 * heat` to `360 + 100 * heat` so the
  outer falloff has ~20% more room to die smoothly (radius cap of 580px at
  full heat + max velocity stretch — still well within a 1280x800 / 375x667
  viewport).
  Do NOT change drawBody, drawCore, drawGlint, drawFrame, resizeCanvas, or readColors.
  Do NOT mutate any --blob-* CSS variables or DESIGN.md tokens.
  Add a brief inline comment block above the addColorStop chain explaining
  "Phase 96 BR-01: 4-stop feather to eliminate visible halo edge ring".
  </action>
  <verify>
    <automated>cd next &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <done>
  drawHalo now uses 4 addColorStop calls at offsets 0, 0.35, 0.65, 1.0;
  baseRadius = `360 + 100 * heat`; tsc clean; no other functions changed
  (verifiable via `git diff next/src/lib/blob-engine/canvas-renderer.ts` showing
  drawHalo-only delta).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright visual spec for halo feather (desktop + mobile)</name>
  <files>next/tests/visual/blob-halo-feather.spec.ts</files>
  <action>
  Create a new Playwright visual spec dedicated to BR-01 verification.
  Unlike `baseline.spec.ts` which hides the blob, this spec MUST keep the
  blob visible and pin its position deterministically so the snapshot is
  reproducible.

  Determinism approach (per Phase 93 finding F1: __blobDebug.setMode is
  read-only, use reducedMotion fallback):
  - Use `test.use({ reducedMotion: 'reduce' })` at file scope to lock the
    blob into 'static' mode (Decision G priority chain: reduced-motion ->
    static, no rAF, no follow). The static mode still renders the canvas
    with whatever the last-painted layer positions were, which the engine
    initializes to viewport center (innerWidth/2, innerHeight/2).
  - Wait for `[data-blob-mode="static"]` on `<html>` before snapshotting.

  Two test cases:

  1. test('halo feather smooth — desktop 1280x800 zoom', ...)
     - Project: 'desktop' (1280x800)
     - Navigate to '/'
     - Wait for `<html[data-blob-mode="static"]>` selector
     - Use page.evaluate to set CSS `zoom: 2` on document.body (or
       page.setViewportSize to 640x400 to simulate zoom — pick zoom for
       crispness)
     - Crop to a 400x400 region centered on viewport center where the halo
       lives
     - expect(canvasCrop).toMatchSnapshot('halo-feather-desktop-zoom2x.png',
       { maxDiffPixelRatio: 0.005 })

  2. test('halo feather smooth — mobile 375x667', ...)
     - Project: 'mobile-375' (375x667, deviceScaleFactor: 2)
     - Same flow, no zoom needed (deviceScaleFactor handles density)
     - Crop to 320x320 centered on viewport center
     - expect(canvasCrop).toMatchSnapshot('halo-feather-mobile.png',
       { maxDiffPixelRatio: 0.005 })

  Snapshot path: tests/visual/__snapshots__/blob-halo-feather.spec.ts/
  (matches existing snapshotPathTemplate in playwright.config.ts).

  First run is the baseline; commit the generated PNGs alongside the spec.
  </action>
  <verify>
    <automated>cd next &amp;&amp; pnpm exec playwright test visual/blob-halo-feather --update-snapshots --reporter=list</automated>
  </verify>
  <done>
  Spec file exists with 2 test cases; both pass with --update-snapshots on
  first run; baseline PNGs committed under
  next/tests/visual/__snapshots__/blob-halo-feather.spec.ts/. Re-running
  without --update-snapshots produces 0 diffs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  4-stop feather applied to drawHalo (offsets 0 / 0.35 / 0.65 / 1.0); halo
  baseRadius extended from 300 to 360. Baseline screenshots captured at
  desktop-zoom-2x and mobile-375x667.
  </what-built>
  <how-to-verify>
  1. Run `cd next &amp;&amp; pnpm dev`, open http://localhost:3000/
  2. Hold the cursor in one spot for ~3s to let heat ramp up; observe halo edge
  3. Browser zoom Cmd-+ to 200% — confirm there is NO visible ring at the halo edge (alpha falloff should look continuous)
  4. Open DevTools device toolbar -> iPhone SE (375x667) -> reload — confirm same smoothness on mobile ambient drift
  5. Compare next/tests/visual/__snapshots__/blob-halo-feather.spec.ts/halo-feather-desktop-zoom2x.png and halo-feather-mobile.png against your subjective read
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the visible artifact (e.g., "still see a ring at ~70% radius")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer -> Canvas2D | Same-origin, no untrusted input; halo color values come from CSS custom properties read once at engine start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | T (tampering) | drawHalo gradient math | accept | No external input touches the gradient; stop offsets are hardcoded constants |
| T-96-02 | D (denial of service) | Larger halo radius (~580px) might increase fill cost | mitigate | Radius cap is bounded: `360 + 100*heat` * `1 + min(0.6, velocity/1500)` = 576px max; still 1 fillRect call per frame; verify no FPS regression in checkpoint task. If Lighthouse INP regresses >10ms vs Phase 95 baseline, revert radius to 300 and rely on stop curve only. |
| T-96-03 | T (rendering integrity break) | Browser quirks: Safari/Firefox radial-gradient interpolation differs from Chromium | mitigate | Test in Safari Tech Preview manually after autonomous edits; visual baseline run in Chromium only is acceptable for v9.0.1, document Safari/FF parity in 96-SUMMARY.md |
| T-96-04 | I (information disclosure) | Canvas pixel readback via `__blobDebug` | accept | Debug API is dev-only (NODE_ENV guard in debug.ts); no PII in canvas |
| T-96-05 | D (rAF leak from new test) | New Playwright spec might leak engine across navigations if it doesn't follow Phase 93 patterns | mitigate | Spec uses `reducedMotion: 'reduce'` which short-circuits the rAF loop entirely (mode='static' -> rAF not scheduled, see index.ts:217-219). Page.close in afterEach is implicit via Playwright. |
</threat_model>

<verification>
- `cd next &amp;&amp; pnpm tsc --noEmit` passes
- `cd next &amp;&amp; pnpm exec playwright test visual/blob-halo-feather` produces 2 passing snapshots
- `cd next &amp;&amp; pnpm exec playwright test visual/baseline` still passes (Phase 93 baselines NOT regressed — baseline.spec.ts hides the blob so renderer changes cannot affect it)
- User checkpoint: confirms "no visible glow border" subjectively at desktop zoom + mobile
</verification>

<success_criteria>
- BR-01 success-criterion line 1 satisfied: halo edge feathered, no visible gradient stop boundary at desktop 1280x800 zoomed and mobile 375x667
- User confirms via checkpoint
- Phase 95 visual baselines unchanged (baseline.spec.ts masks the blob, so this plan cannot regress them)
- TypeScript build clean; no other engine functions modified
</success_criteria>

<output>
After completion, create `.planning/phases/96-blob-unification/96-01-SUMMARY.md` with:
- Diff: drawHalo before/after (paste both gradient chains)
- Screenshots: link to halo-feather-desktop-zoom2x.png and halo-feather-mobile.png
- Subjective UAT outcome from user checkpoint
- Cross-browser note: Chromium baseline only; Safari/Firefox manual smoke
</output>
