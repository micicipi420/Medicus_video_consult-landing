---
phase: 17-design-tokens-gradient-buttons-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
autonomous: true
requirements:
  - TOKEN-02
  - BTN-04
  - BTN-05
  - BTN-06
  - LAYOUT-01
  - LAYOUT-02

must_haves:
  truths:
    - "All CTA buttons display a green-to-teal gradient (#1AC67E to #0D9DB5) instead of solid green"
    - "CTA buttons have border-radius 16px, not pill-shape 100px"
    - "Hovering a CTA button shows a visible opacity transition"
    - "Hero section background is pure white (#ffffff), not cream"
    - "Page content container max-width is 1200px"
  artifacts:
    - path: "css/styles.css"
      provides: "All design token, button, and layout changes"
      contains: "--gradient-cta"
  key_links:
    - from: "css/styles.css :root"
      to: "css/styles.css .button--primary"
      via: "var(--gradient-cta) token"
      pattern: "background:\\s*var\\(--gradient-cta\\)"
    - from: "css/styles.css :root"
      to: "css/styles.css .button--primary:hover"
      via: "var(--color-cta-hover-kz) token"
      pattern: "--color-cta-hover-kz"
---

<objective>
Apply gradient CTA buttons, updated border-radius, white hero background, and verify 1200px container to align the site with medicusunion.kz design.

Purpose: Bring the visual design in line with the KZ brand -- gradient buttons, rounded (not pill) corners, clean white hero.
Output: Updated css/styles.css with all Phase 17 requirements fulfilled.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-design-tokens-gradient-buttons-layout/17-CONTEXT.md
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add design tokens and update button styles (TOKEN-02, BTN-04, BTN-05, BTN-06)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css (full file -- understand current :root tokens at lines 53-112, .button at lines 259-276, .button--primary at lines 280-287, @keyframes pulse-glow at lines 1502-1509)
    - .planning/phases/17-design-tokens-gradient-buttons-layout/17-CONTEXT.md
  </read_first>
  <action>
Make the following exact changes to css/styles.css:

**1. Add gradient tokens in :root (after line 70, after --color-text-on-cta):**

```css
/* Colors - CTA Gradient (per TOKEN-02, matching medicusunion.kz) */
--gradient-cta: linear-gradient(0.25turn, #1AC67E 0%, #0D9DB5 100%);
--color-cta-hover-kz: #00c08e;
```

**2. Change .button border-radius (line 272) from `100px` to `16px` (per BTN-05):**

```css
border-radius: 16px;
```

**3. Update .button transition property (lines 274-275) to include opacity (per BTN-06):**

```css
transition: background-color var(--transition-normal),
            transform var(--transition-fast),
            opacity var(--transition-normal);
```

**4. Change .button--primary (lines 280-283) to use gradient background (per BTN-04):**

```css
.button--primary {
  background: var(--gradient-cta);
  color: var(--color-text-on-cta);
}
```

Note: change `background-color` to `background` so the gradient shorthand works.

**5. Update .button--primary:hover (lines 285-287) for opacity-based hover (per BTN-06):**

```css
.button--primary:hover {
  opacity: 0.85;
  background: var(--gradient-cta);
}
```

The opacity shift provides a visible hover feedback without needing to override the gradient. Keep `background` declaration to prevent any inherited background-color from overriding.

**6. Update @keyframes pulse-glow (lines 1502-1509) to use new gradient start color #1AC67E instead of old #35B678:**

```css
@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(26, 198, 126, 0.4);
  }
  50% {
    box-shadow: 0 0 20px 8px rgba(26, 198, 126, 0.25);
  }
}
```

The rgba values `26, 198, 126` are the decimal equivalent of #1AC67E.
  </action>
  <verify>
    <automated>grep -n "gradient-cta" css/styles.css && grep -n "border-radius: 16px" css/styles.css && grep -n "opacity: 0.85" css/styles.css && grep -n "rgba(26, 198, 126" css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - css/styles.css contains `--gradient-cta: linear-gradient(0.25turn, #1AC67E 0%, #0D9DB5 100%)`
    - css/styles.css contains `--color-cta-hover-kz: #00c08e`
    - .button rule contains `border-radius: 16px` (NOT `100px`)
    - .button rule transition includes `opacity`
    - .button--primary contains `background: var(--gradient-cta)` (NOT `background-color: var(--color-cta)`)
    - .button--primary:hover contains `opacity: 0.85`
    - @keyframes pulse-glow contains `rgba(26, 198, 126` (NOT `rgba(53, 182, 120`)
  </acceptance_criteria>
  <done>All CTA buttons use gradient background, have 16px border-radius, show opacity transition on hover, and pulse-glow keyframes use updated color.</done>
</task>

<task type="auto">
  <name>Task 2: White hero background and verify container width (LAYOUT-01, LAYOUT-02)</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css (specifically .hero at line ~420-423 and :root --container-max at line 100 and .container at line 225-230)
  </read_first>
  <action>
Make the following exact changes to css/styles.css:

**1. Change .hero background (line 422) from cream to white (per LAYOUT-02):**

```css
.hero {
  padding-block: var(--space-8) var(--space-6);
  background: #ffffff;
}
```

Change `background: #fffbf4` to `background: #ffffff`.

**2. Verify LAYOUT-01 (container 1200px) -- no change needed:**

Confirm that line 100 has `--container-max: 1200px` and line 227 has `max-width: var(--container-max)`. These are already correct. Do NOT modify them. Just verify they exist.
  </action>
  <verify>
    <automated>grep -n "background: #ffffff" css/styles.css | grep -i hero && grep -n "container-max: 1200px" css/styles.css && grep -n "max-width: var(--container-max)" css/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - .hero rule contains `background: #ffffff` (NOT `#fffbf4`)
    - :root contains `--container-max: 1200px`
    - .container contains `max-width: var(--container-max)`
    - No other hero background declarations override this value
  </acceptance_criteria>
  <done>Hero background is pure white. Container max-width confirmed at 1200px.</done>
</task>

</tasks>

<verification>
Open index.html in browser and visually confirm:
1. CTA buttons show green-to-teal gradient (left green, right teal)
2. Button corners are rounded but NOT pill-shaped (16px radius)
3. Hovering CTA buttons produces a visible opacity dim effect
4. Hero section has a clean white background (no cream/warm tint)
5. Page content is 1200px wide on a wide viewport

Automated checks:
```bash
# All requirements met in single file
grep "gradient-cta" css/styles.css
grep "border-radius: 16px" css/styles.css
grep "opacity: 0.85" css/styles.css
grep "background: #ffffff" css/styles.css
grep "container-max: 1200px" css/styles.css
# Old values should NOT exist
! grep "border-radius: 100px" css/styles.css
! grep "background-color: var(--color-cta)" css/styles.css
! grep "background: #fffbf4" css/styles.css
! grep "rgba(53, 182, 120" css/styles.css
```
</verification>

<success_criteria>
- All 6 requirements (TOKEN-02, BTN-04, BTN-05, BTN-06, LAYOUT-01, LAYOUT-02) are fulfilled
- css/styles.css is the only file modified
- No CSS syntax errors introduced
- Old solid-green button style completely replaced by gradient
- Old cream hero background completely replaced by white
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-tokens-gradient-buttons-layout/17-01-SUMMARY.md`
</output>
