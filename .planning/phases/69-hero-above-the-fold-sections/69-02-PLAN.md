---
phase: 69-hero-above-the-fold-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/components/sections/StatsBar.tsx
  - next/src/components/sections/ServicesGrid.tsx
  - next/public/service-consultation.webp
  - next/public/service-treatment.webp
  - next/public/service-checkup.webp
  - next/src/app/page.tsx
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "StatsSection displays 4 glass cards (43 клиники, 11 стран, 500+ врачей, 15+ лет) with per-card accent colors"
    - "ServicesSection displays 3 service cards with photos, titles, descriptions, feature lists, and CTA links"
    - "Stats cards use glass styling: bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-glass-border shadow-glass"
    - "Service cards use glass styling: bg-white/60 backdrop-blur-2xl rounded-[3rem] with image at top, content below"
    - "page.tsx imports and renders HeroHub, StatsBar, ServicesGrid without ScrollReveal wrappers"
  artifacts:
    - path: "next/src/components/sections/StatsBar.tsx"
      provides: "Stats section with 4 glass metric cards"
      exports: ["StatsBar"]
    - path: "next/src/components/sections/ServicesGrid.tsx"
      provides: "Services section with 3 glass service cards"
      exports: ["ServicesGrid"]
    - path: "next/src/app/page.tsx"
      provides: "Index page with updated section imports and rendering"
    - path: "next/public/service-consultation.webp"
      provides: "Online consultation card image"
    - path: "next/public/service-treatment.webp"
      provides: "Treatment abroad card image"
    - path: "next/public/service-checkup.webp"
      provides: "Checkup card image"
  key_links:
    - from: "next/src/components/sections/ServicesGrid.tsx"
      to: "/consultations"
      via: "Next.js Link href"
      pattern: 'href="/consultations"'
    - from: "next/src/components/sections/ServicesGrid.tsx"
      to: "/treatment-abroad"
      via: "Next.js Link href"
      pattern: 'href="/treatment-abroad"'
    - from: "next/src/components/sections/ServicesGrid.tsx"
      to: "/checkup"
      via: "Next.js Link href"
      pattern: 'href="/checkup"'
    - from: "next/src/app/page.tsx"
      to: "next/src/components/sections/StatsBar.tsx"
      via: "import"
      pattern: "import.*StatsBar"
---

<objective>
Rewrite StatsBar.tsx and ServicesGrid.tsx as server components matching the new design from feat/new-design:index.html (commit d450232). Download 3 service card images. Update page.tsx to render the new sections without ScrollReveal animation wrappers.

Purpose: SEC-01 (4 metric cards with glass styling) and SEC-02 (3 service cards with photos). These sections immediately below the hero reinforce credibility and guide users to services.
Output: Rewritten StatsBar.tsx and ServicesGrid.tsx, 3 service images, updated page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-hero-above-the-fold-sections/69-CONTEXT.md
@.planning/phases/68-design-tokens-layout-chrome/68-01-SUMMARY.md

@next/src/app/globals.css
@next/src/app/page.tsx
@next/src/components/sections/StatsBar.tsx
@next/src/components/sections/ServicesGrid.tsx

<interfaces>
<!-- Design tokens available in globals.css (Phase 68) -->
Colors: mu-blue (#38C6F4), mu-accent-blue (#4F84E8), mu-accent-teal (#78C3BF), mu-accent-orange (#FFA25C), mu-green-600 (#35B678), mu-green-700 (#2D9E68), mu-green-50 (#E4FAEF), mu-text-900 (#1B212C), mu-text-700 (#4A4E5C), mu-accent-teal-bg (#EBFAF9)
Shadows: shadow-glass-sm, shadow-glass, shadow-glass-lg, shadow-glass-inner-strong
Borders: border-glass-border (rgba(255,255,255,0.6)), border-glass-border-strong (rgba(255,255,255,0.8))

<!-- page.tsx currently imports: HeroHub, StatsBar, ServicesGrid, GuideGrid, AdvantagesGrid, ContactSection, FinalCTA, ScrollReveal -->
<!-- page.tsx wraps StatsBar and ServicesGrid in ScrollReveal -- animations deferred, remove wrappers -->

<!-- StatsBar currently uses dark bg-[#1A365D] -- must change to glass cards -->
<!-- ServicesGrid currently uses card-prod squircle-lg classes -- must change to glass cards with photos -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite StatsBar.tsx + download service images + rewrite ServicesGrid.tsx</name>
  <files>next/src/components/sections/StatsBar.tsx, next/src/components/sections/ServicesGrid.tsx, next/public/service-consultation.webp, next/public/service-treatment.webp, next/public/service-checkup.webp</files>
  <read_first>
    - next/src/components/sections/StatsBar.tsx (current implementation)
    - next/src/components/sections/ServicesGrid.tsx (current implementation)
    - next/src/app/globals.css (design tokens)
  </read_first>
  <action>
**Part A: Rewrite StatsBar.tsx**

Replace the entire file content. NO 'use client' directive -- this is a server component. Remove old dark-bg styling.

Exact structure from source HTML:

```tsx
const STATS = [
  { number: '43', label: 'клиники', color: 'text-mu-accent-blue' },
  { number: '11', label: 'стран', color: 'text-mu-accent-teal' },
  { number: '500+', label: 'врачей', color: 'text-mu-accent-orange' },
  { number: '15+', label: 'лет опыта', color: 'text-mu-green-600' },
] as const;

export function StatsBar() {
  return (
    <section className="relative py-12 z-20" aria-label="Ключевые цифры">
      <div className="container mx-auto px-4 lg:px-6">
        <div className="grid grid-cols-2 lg:grid-cols-4 gap-6">
          {STATS.map((stat) => (
            <div
              key={stat.label}
              className="relative group flex flex-col items-center justify-center p-8 bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-glass-border shadow-glass hover:shadow-glass-lg hover:bg-white/70 hover:border-glass-border-strong transition-all duration-500 overflow-hidden"
            >
              <div className={`text-5xl md:text-6xl font-extrabold mb-3 drop-shadow-sm ${stat.color} relative z-10`}>
                {stat.number}
              </div>
              <div className="text-mu-text-700 font-bold text-lg text-center uppercase tracking-wider relative z-10">
                {stat.label}
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

Key requirements per CONTEXT.md:
- Glass cards: bg-white/60 backdrop-blur-2xl rounded-[2.5rem] border border-glass-border shadow-glass
- Hover: hover:shadow-glass-lg hover:bg-white/70 hover:border-glass-border-strong
- Per-card accent colors: mu-accent-blue, mu-accent-teal, mu-accent-orange, mu-green-600
- Static numbers (NO count-up animation -- deferred)
- Uppercase tracking-wider labels
- text-5xl md:text-6xl font-extrabold for numbers

**Part B: Download 3 service card images**

Download and convert to WebP:

1. Online consultation image:
   - Source: https://images.unsplash.com/photo-1612944095914-33fd0a85fcfc?w=1080&q=80
   - Save as: next/public/service-consultation.webp

2. Treatment abroad (modern hospital):
   - Source: https://images.unsplash.com/photo-1720180244462-648c13ee01e5?w=1080&q=80
   - Save as: next/public/service-treatment.webp

3. Checkup (doctor with tablet):
   - Source: https://images.unsplash.com/photo-1758691462774-f01ed567f2c4?w=1080&q=80
   - Save as: next/public/service-checkup.webp

Use curl to download, then convert to WebP with sips or sharp. Target: under 200KB each.

**Part C: Rewrite ServicesGrid.tsx**

Replace entirely. NO 'use client'. Use next/image for photos and next/link for navigation.

Import at top:
```tsx
import Image from 'next/image';
import Link from 'next/link';
import { Video, Globe, ClipboardCheck, ArrowRight } from 'lucide-react';
```

Section wrapper with title:
```tsx
<section className="py-16 relative z-10" id="services">
  <div className="container mx-auto px-4 lg:px-6">
    {/* Section Title */}
    <div className="text-center mb-16 max-w-3xl mx-auto">
      <div className="inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-glass-border px-5 py-2.5 rounded-full shadow-sm shadow-glass-inner mb-6">
        <span className="text-sm font-bold text-mu-accent-blue uppercase tracking-wider">Наши Услуги</span>
      </div>
      <h2 className="text-5xl md:text-6xl font-extrabold mb-6">
        <span className="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent">Выберите, что вам нужно</span>
      </h2>
    </div>
```

Services grid: className="grid md:grid-cols-2 lg:grid-cols-3 gap-8"

Each card structure (3 cards total):

Card outer: `className="relative group h-full flex flex-col"`
Card inner: `className="bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border hover:border-glass-border-strong hover:shadow-glass-lg transition-all duration-500 hover:-translate-y-2 h-full flex flex-col overflow-hidden"`

Image section (inside card):
```tsx
<div className="relative h-56 w-full overflow-hidden p-3">
  <div className="relative w-full h-full rounded-[2rem] overflow-hidden border border-white/40 shadow-inner">
    <Image src={card.image} alt={card.imageAlt} fill className="object-cover transition-transform duration-700 group-hover:scale-105" sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw" />
    <div className="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent" />
    {/* Floating icon */}
    <div className={`absolute top-4 right-4 w-12 h-12 ${card.iconBg} backdrop-blur-xl rounded-2xl flex items-center justify-center ${card.iconColor} shadow-glass-sm border border-glass-border transition-transform duration-500 group-hover:scale-110 group-hover:rotate-3`}>
      {card.icon}
    </div>
  </div>
</div>
```

Content section (inside card):
```tsx
<div className="p-8 pt-4 flex-grow flex flex-col">
  {/* Price badge */}
  <div className="inline-flex items-center gap-2 bg-white/50 backdrop-blur-md border border-glass-border px-4 py-1.5 rounded-full shadow-sm w-fit mb-5">
    <span className={`text-sm font-bold ${card.badgeColor}`}>{card.badge}</span>
  </div>
  <h3 className="text-2xl font-bold text-mu-text-900 mb-4">{card.title}</h3>
  <p className="text-mu-text-700 font-medium leading-relaxed mb-6">{card.description}</p>
  {/* Feature list with checkmark icons */}
  <ul className="space-y-4 mb-8 flex-grow">
    {card.features.map((feature) => (
      <li key={feature} className="flex items-start gap-3">
        <div className="w-6 h-6 bg-white/60 backdrop-blur-md border border-glass-border rounded-full flex items-center justify-center flex-shrink-0 mt-0.5 shadow-glass-inner-strong">
          <svg className="w-3.5 h-3.5 text-mu-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={3} d="M5 13l4 4L19 7" /></svg>
        </div>
        <span className="text-mu-text-900 font-medium">{feature}</span>
      </li>
    ))}
  </ul>
  {/* CTA button */}
  <Link href={card.href} className="w-full bg-white/50 backdrop-blur-xl border border-glass-border text-mu-text-900 py-4 rounded-2xl font-bold shadow-glass-sm hover:bg-white/70 hover:shadow-glass transition-all flex items-center justify-center gap-2 group/btn mt-auto">
    {card.ctaText}
    <ArrowRight className="w-5 h-5 group-hover/btn:translate-x-1 transition-transform" />
  </Link>
</div>
```

**3 card data objects:**

Card 1 -- Online Consultation:
- image: "/service-consultation.webp"
- imageAlt: "Онлайн-консультация с врачом"
- icon: `<Video className="w-6 h-6" />`
- iconBg: "bg-mu-blue/10"
- iconColor: "text-mu-accent-blue"
- badge: "от 450 \u20AC" (euro symbol)
- badgeColor: "text-mu-accent-blue"
- title: "Мнение зарубежного врача\u00A0\u2014 без\u00A0перелёта"
- description: "Загрузите медицинские документы\u00A0\u2014 мы\u00A0переведём их и\u00A0организуем видеоконсультацию с\u00A0профильным специалистом из\u00A0Германии, Израиля, Швейцарии или другой страны. Врач изучит ваш случай до\u00A0встречи. После\u00A0\u2014 письменное заключение в\u00A0личном кабинете."
- features: ["Перевод документов и консультации", "Результат за 5 дней", "Письменное заключение"]
- ctaText: "Записаться на\u00A0консультацию"
- href: "/consultations"

Card 2 -- Treatment Abroad:
- image: "/service-treatment.webp"
- imageAlt: "Современная клиника"
- icon: `<Globe className="w-6 h-6" />`
- iconBg: "bg-mu-accent-teal-bg"
- iconColor: "text-mu-accent-teal"
- badge: "план лечения бесплатно"
- badgeColor: "text-mu-accent-teal"
- title: "Организуем лечение за\u00A0границей\u00A0\u2014 под\u00A0ключ"
- description: "Подберём клинику и\u00A0врача под ваш диагноз из\u00A0сети в\u00A011\u00A0странах. Организуем онлайн-консультацию до\u00A0вылета, возьмём на\u00A0себя визу, логистику, переводчика и\u00A0сопровождение. После возвращения\u00A0\u2014 наблюдение и\u00A0связь с\u00A0врачом."
- features: ["11 стран, 43 клиники-партнёра", "Полная организация", "Координация до выздоровления"]
- ctaText: "Получить план лечения"
- href: "/treatment-abroad"

Card 3 -- Checkup:
- image: "/service-checkup.webp"
- imageAlt: "Чек-ап обследование"
- icon: `<ClipboardCheck className="w-6 h-6" />`
- iconBg: "bg-mu-green-50"
- iconColor: "text-mu-green-600"
- badge: "от $350"
- badgeColor: "text-mu-green-700"
- title: "Проверьте здоровье в\u00A0клинике мирового уровня"
- description: "Комплексное обследование за\u00A01\u20132\u00A0дня в\u00A0Samsung Medical Center, Severance Hospital или клиниках Стамбула. Полная организация: виза, трансфер, переводчик, сопровождающий. Результаты\u00A0\u2014 в\u00A0приложении с\u00A0переводом на\u00A0русский."
- features: ["Южная Корея и Турция", "Программы от базовой до премиум", "Корпоративные чек-апы"]
- ctaText: "Подобрать программу"
- href: "/checkup"
  </action>
  <verify>
    <automated>test -f next/public/service-consultation.webp && test -f next/public/service-treatment.webp && test -f next/public/service-checkup.webp && echo "PASS: Service images exist" && cd next && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StatsBar.tsx does NOT contain 'use client'
    - StatsBar.tsx does NOT contain bg-[#1A365D] (old dark bg)
    - StatsBar.tsx contains "bg-white/60 backdrop-blur-2xl rounded-[2.5rem]" glass card pattern
    - StatsBar.tsx contains text-mu-accent-blue, text-mu-accent-teal, text-mu-accent-orange, text-mu-green-600 per-card colors
    - StatsBar.tsx contains "uppercase tracking-wider" on labels
    - StatsBar.tsx contains all 4 stats: "43", "11", "500+", "15+"
    - ServicesGrid.tsx does NOT contain 'use client'
    - ServicesGrid.tsx does NOT contain card-prod or squircle-lg (old classes)
    - ServicesGrid.tsx contains "bg-white/60 backdrop-blur-2xl rounded-[3rem]" glass card pattern
    - ServicesGrid.tsx contains id="services" on section
    - ServicesGrid.tsx contains 3 service cards linking to /consultations, /treatment-abroad, /checkup
    - ServicesGrid.tsx uses next/image Image component for card photos
    - ServicesGrid.tsx contains gradient section title "Выберите, что вам нужно"
    - ServicesGrid.tsx contains feature checkmark lists for each card
    - 3 service image files exist in next/public/
    - Build succeeds
  </acceptance_criteria>
  <done>StatsBar and ServicesGrid rewritten with glass styling matching new design, 3 service images downloaded</done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx to render new sections without animation wrappers</name>
  <files>next/src/app/page.tsx</files>
  <read_first>
    - next/src/app/page.tsx (current implementation with ScrollReveal wrappers)
  </read_first>
  <action>
Update page.tsx to remove ScrollReveal wrappers from the three rewritten sections (HeroHub, StatsBar, ServicesGrid). Keep ScrollReveal on sections that have NOT been rewritten yet (GuideGrid, AdvantagesGrid, ContactSection, FinalCTA) -- those will be updated in Phases 70-71.

The updated page.tsx should be:

```tsx
import type { Metadata } from 'next';
import { HeroHub } from '@/components/sections/HeroHub';
import { StatsBar } from '@/components/sections/StatsBar';
import { ServicesGrid } from '@/components/sections/ServicesGrid';
import { GuideGrid } from '@/components/sections/GuideGrid';
import { AdvantagesGrid } from '@/components/sections/AdvantagesGrid';
import { ContactSection } from '@/components/sections/ContactSection';
import { FinalCTA } from '@/components/sections/FinalCTA';
import { ScrollReveal } from '@/components/motion/ScrollReveal';

export const metadata: Metadata = {
  title: {
    absolute: 'MedicusUnion KZ \u2014 Медицина мирового уровня для Казахстана',
  },
  description:
    'Онлайн-консультации с европейскими врачами, лечение за рубежом, чек-ап в Samsung Medical Center и клиниках Стамбула. Организация под ключ.',
  alternates: {
    canonical: '/',
  },
  openGraph: {
    title: 'MedicusUnion KZ \u2014 Медицина мирового уровня для Казахстана',
    description:
      'Онлайн-консультации с врачами Европы, лечение за рубежом, чек-ап в ведущих клиниках мира. 43 клиники, 11 стран, организация под ключ.',
    url: '/',
  },
};

export default function Home() {
  return (
    <>
      <HeroHub />
      <StatsBar />
      <ServicesGrid />
      <ScrollReveal delay={0.05}>
        <GuideGrid />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <AdvantagesGrid />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <ContactSection />
      </ScrollReveal>
      <ScrollReveal delay={0.05}>
        <FinalCTA />
      </ScrollReveal>
    </>
  );
}
```

Key changes:
1. HeroHub rendered without any wrapper (was already direct)
2. StatsBar rendered directly -- removed ScrollReveal wrapper
3. ServicesGrid rendered directly -- removed ScrollReveal wrapper with delay={0.05}
4. GuideGrid, AdvantagesGrid, ContactSection, FinalCTA KEEP their ScrollReveal wrappers (not yet redesigned)
5. ALL existing metadata preserved exactly as-is
  </action>
  <verify>
    <automated>cd next && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx renders HeroHub without any wrapper element
    - page.tsx renders StatsBar without ScrollReveal wrapper
    - page.tsx renders ServicesGrid without ScrollReveal wrapper
    - page.tsx still renders GuideGrid, AdvantagesGrid, ContactSection, FinalCTA inside ScrollReveal wrappers
    - page.tsx still imports ScrollReveal (needed for remaining sections)
    - page.tsx metadata is unchanged
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>page.tsx updated to render new sections directly while preserving ScrollReveal on unrewritten sections</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service images | Downloaded from external Unsplash URLs, served locally |
| Navigation links | Internal links to /consultations, /treatment-abroad, /checkup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-03 | T (Tampering) | Service images from Unsplash | accept | Decorative stock photos downloaded once, no user data |
| T-69-04 | I (Info Disclosure) | Service card links | accept | Links navigate to public service pages, no sensitive data |
</threat_model>

<verification>
1. `cd next && npx next build` -- build succeeds
2. StatsBar.tsx uses glass card styling with per-card accent colors
3. ServicesGrid.tsx uses glass card styling with photos and feature lists
4. page.tsx renders all sections correctly
5. 3 service images exist in next/public/
6. Visual check at 1440px: stats grid 4 cols, services grid 3 cols
7. Visual check at 375px: stats grid 2 cols, services stack vertically
</verification>

<success_criteria>
- 4 stats cards render with glass styling and accent colors (43 blue, 11 teal, 500+ orange, 15+ green)
- 3 service cards render with photos, price badges, descriptions, feature lists, and CTA links
- Service cards link to /consultations, /treatment-abroad, /checkup
- Section title "Выберите, что вам нужно" renders with gradient text
- page.tsx renders all sections: Hero, Stats, Services, then remaining sections with ScrollReveal
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/69-hero-above-the-fold-sections/69-02-SUMMARY.md`
</output>
