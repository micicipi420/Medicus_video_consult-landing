---
phase: 19-v1-3-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
  - .planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "css/styles.css contains no --color-cta-hover-kz declaration"
    - ".pricing__card has no box-shadow property"
    - "18-01-SUMMARY.md frontmatter lists requirements_completed: [CARD-04, CARD-05, CARD-06, NAV-01]"
  artifacts:
    - path: "css/styles.css"
      provides: "Cleaned-up design tokens and flat pricing card"
      contains: "no orphaned --color-cta-hover-kz token, no .pricing__card box-shadow"
    - path: ".planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md"
      provides: "Accurate requirements_completed frontmatter"
      contains: "requirements_completed: [CARD-04, CARD-05, CARD-06, NAV-01]"
  key_links:
    - from: "css/styles.css :root"
      to: "button hover state"
      via: "opacity: 0.85 on .button--primary:hover (not the removed token)"
      pattern: "opacity.*0\\.85"
---

<objective>
Remove three pieces of tech debt left after v1.3 milestone: one orphaned CSS token, one missed box-shadow on the pricing card, and a missing `requirements_completed` field in the Phase 18 SUMMARY.

Purpose: Close out v1.3 cleanly — consistent flat design everywhere, accurate planning docs.
Output: css/styles.css with token and shadow removed; 18-01-SUMMARY.md with correct frontmatter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned --color-cta-hover-kz token</name>
  <files>css/styles.css</files>
  <read_first>css/styles.css lines 72-80 (the :root CTA gradient block)</read_first>
  <action>
    Delete line 76 from css/styles.css exactly:
      `  --color-cta-hover-kz: #00c08e;`

    This token was declared in Phase 17 but never used by any CSS selector. The hover effect on `.button--primary:hover` uses `opacity: 0.85`, not this token. No other selector references `--color-cta-hover-kz`.

    After deletion, lines 74-75 should read:
      /* Colors - CTA Gradient (per TOKEN-02, matching medicusunion.kz) */
      --gradient-cta: linear-gradient(0.25turn, #1AC67E 0%, #0D9DB5 100%);

    Leave the blank line below (now line 76) intact to preserve block spacing.
  </action>
  <verify>
    <automated>grep -n "color-cta-hover-kz" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css | wc -l | grep -q "^0$" && echo "PASS: token removed" || echo "FAIL: token still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "color-cta-hover-kz" css/styles.css` returns no output
    - `grep "gradient-cta" css/styles.css` still returns the --gradient-cta declaration (token not accidentally removed)
  </acceptance_criteria>
  <done>--color-cta-hover-kz is not present anywhere in css/styles.css</done>
</task>

<task type="auto">
  <name>Task 2: Remove box-shadow from .pricing__card</name>
  <files>css/styles.css</files>
  <read_first>css/styles.css lines 844-856 (the .pricing__card block)</read_first>
  <action>
    Delete line 854 from css/styles.css exactly:
      `  box-shadow: 0 8px 32px rgba(56, 198, 244, 0.12), 0 2px 8px rgba(0, 0, 0, 0.06);`

    This extends the CARD-05 flat design intent (no shadows) to the pricing card, which was missed in Phase 18.

    After deletion, .pricing__card should contain these properties (no box-shadow):
      background-color, border-radius, padding, max-width, margin-top, margin-left,
      margin-right, border, border-left, position

    There is no `.pricing__card:hover` rule with a shadow — no other lines need removing.
  </action>
  <verify>
    <automated>grep -n "pricing__card" /Users/mikhail/Projects/Medicus_video_consult-landing/css/styles.css | grep "box-shadow" | wc -l | grep -q "^0$" && echo "PASS: shadow removed" || echo "FAIL: shadow still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A20 "\.pricing__card {" css/styles.css` contains no `box-shadow` line
    - `.pricing__card` block still has `border-radius: var(--radius-lg)` (not accidentally broken)
  </acceptance_criteria>
  <done>.pricing__card has no box-shadow in any state; flat design is now consistent across all cards</done>
</task>

<task type="auto">
  <name>Task 3: Fix Phase 18 SUMMARY frontmatter — add requirements_completed</name>
  <files>.planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md</files>
  <read_first>.planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md (full file — it is short)</read_first>
  <action>
    In the YAML frontmatter of 18-01-SUMMARY.md, add the following field after the `metrics:` block (before the closing `---`):

      requirements_completed: [CARD-04, CARD-05, CARD-06, NAV-01]

    The four requirements were actually completed in Phase 18 (verified in the SUMMARY body, Tasks table). This is a documentation-only fix — no CSS changes.

    The frontmatter currently ends at line 26 (`---`). Insert the new field at line 25 (before the closing `---`), so the block reads:

      metrics:
        duration: "8 minutes"
        completed_date: "2026-03-23"
        tasks_completed: 3
        files_modified: 1
      requirements_completed: [CARD-04, CARD-05, CARD-06, NAV-01]
      ---
  </action>
  <verify>
    <automated>grep -q "requirements_completed: \[CARD-04, CARD-05, CARD-06, NAV-01\]" /Users/mikhail/Projects/Medicus_video_consult-landing/.planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "requirements_completed" .planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md` returns the line with all four IDs
    - All four IDs present: CARD-04, CARD-05, CARD-06, NAV-01
    - SUMMARY body content is unchanged (no accidental edits below the frontmatter)
  </acceptance_criteria>
  <done>18-01-SUMMARY.md frontmatter contains requirements_completed: [CARD-04, CARD-05, CARD-06, NAV-01]</done>
</task>

</tasks>

<verification>
Run all three acceptance checks in sequence:

```bash
# Task 1: orphaned token gone
grep "color-cta-hover-kz" css/styles.css | wc -l
# Expected: 0

# Task 2: pricing card shadow gone
grep -A20 "\.pricing__card {" css/styles.css | grep "box-shadow"
# Expected: no output

# Task 3: SUMMARY doc fixed
grep "requirements_completed" .planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md
# Expected: requirements_completed: [CARD-04, CARD-05, CARD-06, NAV-01]
```
</verification>

<success_criteria>
Phase 19 complete when ALL three are true:
1. `grep "color-cta-hover-kz" css/styles.css` returns nothing
2. `grep -A20 "\.pricing__card {" css/styles.css` contains no `box-shadow`
3. `grep "requirements_completed" .planning/phases/18-cards-badges-navigation/18-01-SUMMARY.md` returns `[CARD-04, CARD-05, CARD-06, NAV-01]`
</success_criteria>

<output>
After completion, create `.planning/phases/19-v1-3-cleanup/19-01-SUMMARY.md`
</output>
