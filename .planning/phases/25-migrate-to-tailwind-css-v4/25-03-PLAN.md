---
phase: 25-migrate-to-tailwind-css-v4
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - index.html
autonomous: true
requirements:
  - TW-INDEX-BOTTOM
must_haves:
  truths:
    - "index.html bottom-half elements (WhyUs, Contact form, CTA, Footer) carry Tailwind utility classes matching the Redesign TSX source"
    - "All existing BEM class names used by JS (faq__question, contact-form, form__submit, form__success, form__error, is-invalid, is-open) are PRESERVED"
    - "Tailwind build compiles without errors after class additions"
  artifacts:
    - path: "index.html"
      provides: "Bottom-half sections with Tailwind utility classes"
      contains: "backdrop-blur-3xl rounded-[3rem]"
  key_links:
    - from: "index.html"
      to: "js/main.js"
      via: "Form and FAQ selectors preserved"
      pattern: "class=\".*contact-form.*\""
---

<objective>
Migrate the bottom half of index.html to Tailwind CSS v4 utility classes. For each HTML section (WhyUs, Contact form, FAQ, CTA, Footer, sticky bar), read the corresponding TSX component from Redesign/ and copy the Tailwind className strings into HTML element class attributes. Preserve all existing BEM classes used by JS selectors.

Purpose: Complete the Tailwind migration for the main page's lower sections.
Output: index.html bottom-half sections carry Tailwind utility classes from Redesign TSX source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-migrate-to-tailwind-css-v4/25-CONTEXT.md
@.planning/phases/25-migrate-to-tailwind-css-v4/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate WhyUs section and Contact form section to Tailwind classes</name>
  <files>index.html</files>
  <read_first>
    - Redesign/src/app/components/WhyUsSection.tsx (advantages + collage — 150 lines)
    - Redesign/src/app/components/ContactSection.tsx (form + coordinator card — 237 lines)
    - index.html (lines 500-750 approx — WhyUs through Contact form end)
    - js/main.js (lines 225-400 — form validation and submission selectors)
  </read_first>
  <action>
CRITICAL RULE: Every existing BEM class used by JS must be KEPT. Add Tailwind classes ALONGSIDE them.

**JS-targeted classes to preserve (from main.js):**
- `.contact-form` — form submission handler
- `.form__submit` — submit button
- `.form__success` — success message
- `.form__error` — error message
- `.form__field-error` — field-level errors
- `.is-invalid` — field validation state
- `input[name="name"]`, `input[type="tel"]`, `select[required]`, `textarea` — form field selectors (attribute-based, safe)
- `input[name="website"]` — honeypot
- `.faq__question` — FAQ accordion toggle
- `.is-open` — FAQ open state
- `.animate-fade-up`, `.animate-fade-left`, `.animate-fade-right` — animation classes

**Section 9: WhyUs Section** (WhyUsSection.tsx lines 43-150)
- Section: `class="why-us py-16 relative z-10"` + keep `id="why-us"`
- Container: `class="container mx-auto px-4 lg:px-6"`
- Grid: `class="grid lg:grid-cols-2 gap-16 items-center"`

Left column (advantages):
- Title wrapper: `class="mb-12"` (keep animate classes)
- Section badge: `class="inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-glass-border px-5 py-2.5 rounded-full shadow-sm shadow-glass-inner mb-6"`
- Badge span: `class="text-sm font-bold text-mu-green-600 uppercase tracking-wider"`
- H2: `class="text-5xl md:text-6xl font-extrabold mb-6 text-mu-text-900 drop-shadow-sm"`
- H2 gradient span (for "MedicusUnion"): `class="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent"`
- Description p: `class="text-mu-text-700 font-medium text-xl"`
- Advantages list wrapper: `class="space-y-12"`

Each AdvantageCard (WhyUsSection.tsx lines 19-40):
- Card wrapper: `class="relative flex gap-5 group"` (keep `animate-fade-left` or `animate-fade-up`)
- Icon container outer: `class="flex-shrink-0"`
- Icon box: `class="w-16 h-16 backdrop-blur-xl rounded-[1.5rem] flex items-center justify-center shadow-glass-sm border border-glass-border transition-transform duration-500 group-hover:scale-110 group-hover:rotate-3"` + per-card bg/color:
  - Globe (43 clinics): `bg-mu-accent-teal-bg text-mu-accent-teal`
  - Smartphone (app): `bg-mu-blue/10 text-mu-accent-blue`
  - Award (15+ years): `bg-mu-accent-orange-bg text-mu-accent-orange`
  - Shield (reliability): `bg-mu-green-50 text-mu-green-600`
- Content div: `class="pt-2"`
- H3: `class="text-xl font-extrabold text-mu-text-900 mb-2 leading-snug drop-shadow-sm group-hover:text-mu-blue transition-colors"`
- Description p: `class="text-mu-text-700 font-medium leading-relaxed text-sm md:text-base"`

Right column (image collage, WhyUsSection.tsx lines 108-146):
- Collage wrapper: `class="relative h-[600px] hidden md:block"` (keep `animate-fade-right`)
- Grid: `class="grid grid-cols-2 gap-6 h-full"`
- Left column: `class="space-y-6 pt-12"`
- Image containers: `class="h-64 rounded-[3rem] overflow-hidden shadow-glass-lg border-[6px] border-white/50 backdrop-blur-2xl bg-white/20"` (first image) and `class="h-80 rounded-[3rem] overflow-hidden shadow-glass-lg border-[6px] border-white/50 backdrop-blur-2xl bg-white/20"` (second)
- Images: `class="w-full h-full object-cover hover:scale-105 transition-transform duration-700"`
- Right column: `class="space-y-6"`
- Third image: `class="h-80 rounded-[3rem] overflow-hidden shadow-glass-lg border-[6px] border-white/50 backdrop-blur-2xl bg-white/20"`
- Bottom card (100% confidentiality): `class="h-64 rounded-[3rem] overflow-hidden border border-white/60 bg-white/40 backdrop-blur-2xl p-8 flex flex-col justify-center shadow-glass-inner"`
- Shield icon in card: `class="w-12 h-12 mb-4 text-mu-blue drop-shadow-sm"`
- "100%" text: `class="text-4xl font-extrabold mb-2 text-mu-text-900"`
- Subtitle: `class="text-mu-text-700 font-bold leading-tight"`

**Section 10: Contact Section** (ContactSection.tsx lines 43-237)
- Section: `class="contact py-16 relative z-10"` + keep `id="contact"`
- Container: `class="container mx-auto px-4 lg:px-6 relative z-10"`
- Grid: `class="grid lg:grid-cols-2 gap-12 items-start"`

Left column (info + coordinator):
- Wrapper: `class="flex flex-col h-full"` (keep animate class)
- H2: `class="text-5xl md:text-6xl font-extrabold mb-6"`
- H2 gradient span: `class="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent"`
- Description p: `class="text-mu-text-700 font-medium text-lg mb-8 leading-relaxed max-w-lg"`
- Coordinator card: `class="mt-auto bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-6 border border-white/60 flex flex-col sm:flex-row gap-6 items-center shadow-glass"`
- Coordinator image wrapper: `class="w-32 h-32 rounded-full overflow-hidden flex-shrink-0 border-4 border-white/60 bg-white/50 backdrop-blur-sm shadow-glass-sm"`
- Coordinator img: `class="w-full h-full object-cover"`
- Name h4: `class="text-2xl font-extrabold text-mu-text-900 mb-1"`
- Role p: `class="text-mu-blue font-bold text-sm mb-4"`
- Contact links wrapper: `class="space-y-3"`
- Phone/email links: `class="flex items-center gap-3 text-mu-text-900 hover:text-mu-blue transition-colors text-sm font-semibold"`
- Icon circle: `class="w-8 h-8 rounded-full bg-white/60 backdrop-blur-md flex items-center justify-center border border-white/60 shadow-glass-inner-strong"`
- Trust signals wrapper: `class="mt-8 flex flex-wrap gap-4"`
- Trust pills: `class="inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-white/60 text-mu-text-900 px-5 py-2.5 rounded-full text-sm font-bold shadow-sm shadow-glass-inner"`

Right column (form):
- Form wrapper: `class="bg-white/60 backdrop-blur-3xl rounded-[3rem] p-8 border border-white/60 relative overflow-hidden shadow-glass-lg"`
- Success overlay (form__success): keep `form__success` class, add `class="form__success absolute inset-0 bg-white/80 backdrop-blur-3xl z-20 flex flex-col items-center justify-center p-8"` (hidden by default via JS)
- Form tag: keep `class="contact-form"`, add `class="contact-form space-y-6"`
- Each field wrapper div: `class=""`  (just spacing from form's space-y-6)
- Labels: `class="block text-sm font-bold text-mu-text-900 mb-2"`
- Text input: `class="w-full px-5 py-4 rounded-2xl border border-white/40 bg-white/50 backdrop-blur-md focus:bg-white/70 focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-all placeholder:text-mu-text-500 font-medium text-mu-text-900 shadow-glass-inner"`
- Select: `class="w-full px-5 py-4 rounded-2xl border border-white/40 bg-white/50 backdrop-blur-md focus:bg-white/70 focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-all text-mu-text-900 font-medium shadow-glass-inner appearance-none"`
- Textarea: `class="w-full px-5 py-4 rounded-2xl border border-white/40 bg-white/50 backdrop-blur-md focus:bg-white/70 focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-all resize-none placeholder:text-mu-text-500 font-medium text-mu-text-900 shadow-glass-inner"`
- Submit button: `class="form__submit w-full bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white py-4 rounded-2xl font-bold shadow-lg shadow-mu-blue/30 hover:shadow-xl hover:shadow-mu-blue/40 transition-all flex items-center justify-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed text-lg mt-8"`
- Privacy text: `class="text-sm text-mu-text-700 font-medium text-center mt-4"`
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -q 'contact-form space-y-6' index.html && grep -q 'backdrop-blur-3xl rounded-\[3rem\]' index.html && grep -q 'space-y-12' index.html && echo "WHYUS+CONTACT OK" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'contact-form' index.html` confirms form JS class preserved
    - `grep 'form__submit' index.html` confirms submit button JS class preserved
    - `grep 'faq__question' index.html` confirms FAQ JS class preserved (if FAQ section is in this range)
    - `grep 'space-y-12' index.html` confirms WhyUs advantages layout from TSX
    - `grep 'backdrop-blur-3xl rounded-\[3rem\]' index.html` confirms glass form wrapper from TSX
    - `grep 'grid lg:grid-cols-2 gap-16' index.html` confirms WhyUs grid from TSX
    - Tailwind build succeeds
  </acceptance_criteria>
  <done>WhyUs and Contact sections in index.html carry Tailwind utility classes from WhyUsSection.tsx and ContactSection.tsx, with all form and animation JS classes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate FAQ, CTA, Footer, and sticky bar sections to Tailwind classes</name>
  <files>index.html</files>
  <read_first>
    - Redesign/src/app/components/CTASection.tsx (CTA — 80 lines)
    - Redesign/src/app/components/Footer.tsx (footer — 106 lines)
    - index.html (lines 750-875 — FAQ through end of file)
    - js/main.js (lines 15-60 — FAQ accordion selectors)
  </read_first>
  <action>
Continue editing index.html bottom sections. Preserve all JS-targeted classes.

**JS-targeted classes to preserve:**
- `.faq__question` — accordion toggle
- `.is-open` — accordion state
- `.sticky-bar` — sticky bottom bar (if present)

**Section 11: FAQ Section** (No direct TSX equivalent — FAQ is custom to the HTML site, not in Redesign. Apply glass card styling consistent with other sections.)
- Section wrapper: keep existing BEM classes, add `class="faq py-16 relative z-10"` 
- Container: `class="container mx-auto px-4 lg:px-6"`
- Title: style consistent with other sections: `class="text-4xl md:text-5xl font-extrabold text-mu-text-900 mb-4 text-center"`  
- FAQ items wrapper: keep structure, add `class="max-w-3xl mx-auto space-y-4"`
- Each FAQ item: `class="faq__item bg-white/60 backdrop-blur-2xl rounded-2xl border border-glass-border shadow-glass-sm overflow-hidden"`
- Question button: `class="faq__question w-full text-left px-6 py-5 font-bold text-mu-text-900 flex items-center justify-between transition-colors hover:bg-white/80"`
- Answer div: `class="faq__answer px-6 pb-5 text-mu-text-700 font-medium leading-relaxed"` (controlled by .is-open + max-height CSS from JS)
- Keep the existing `.is-open` CSS logic in the `<style>` block (max-height transition).

**Section 12: CTA Section** (CTASection.tsx lines 17-80)
- Section: `class="cta py-16 relative overflow-hidden z-10"`
- Container: `class="container mx-auto px-4 lg:px-6 relative z-10"`
- CTA card: `class="bg-white/60 backdrop-blur-3xl rounded-[3.5rem] overflow-hidden relative shadow-glass-lg border border-glass-border-strong"`
- Grid: `class="grid lg:grid-cols-2"`
- Left content: `class="p-12 lg:p-20 flex flex-col justify-center relative z-10"`
- Background blob: `class="absolute top-0 left-0 w-96 h-96 bg-mu-blue/30 rounded-full blur-[100px] -z-10 mix-blend-multiply"` (add this div if not present)
- H2: `class="text-4xl md:text-5xl lg:text-6xl font-extrabold text-mu-text-900 mb-6 leading-tight drop-shadow-sm"`
- Description p: `class="text-xl text-mu-text-700 font-medium mb-10 leading-relaxed max-w-lg"`
- Buttons wrapper: `class="flex flex-col sm:flex-row items-center gap-4"`
- Primary CTA: `class="w-full sm:w-auto bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-8 py-4 rounded-3xl font-bold shadow-lg shadow-mu-blue/30 shadow-glass-inner hover:shadow-xl hover:shadow-mu-blue/40 transition-all flex items-center justify-center gap-2 group text-lg"`
- Phone CTA: `class="w-full sm:w-auto bg-white/60 backdrop-blur-xl text-mu-text-900 px-8 py-4 rounded-3xl font-bold border border-glass-border hover:bg-white/80 transition-all flex items-center justify-center gap-2 shadow-glass-sm shadow-glass-inner-strong text-lg"`
- Right image container: `class="relative h-[400px] lg:h-auto overflow-hidden hidden md:block"`
- Image: `class="w-full h-full object-cover object-center"`
- Image fade overlay: `class="absolute inset-0 bg-gradient-to-r from-white/60 to-transparent w-1/3"`

**Section 13: Footer** (Footer.tsx lines 5-106)
- Footer tag: `class="footer relative overflow-hidden z-10 py-16"`
- Container: `class="container mx-auto px-4 lg:px-6 relative z-10"`
- Footer card: `class="bg-white/60 backdrop-blur-3xl rounded-[3rem] p-12 border border-white/60 shadow-glass-lg"`
- Grid: `class="grid md:grid-cols-2 lg:grid-cols-4 gap-12 mb-12"`
- Company column:
  - H3 (brand): `class="text-3xl font-extrabold bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent mb-4 drop-shadow-sm"`
  - Description p: `class="text-mu-text-700 font-medium leading-relaxed"`
- Services column:
  - H4: `class="font-extrabold text-lg text-mu-text-900 mb-4"`
  - UL: `class="space-y-3"`
  - Links: `class="text-mu-text-700 hover:text-mu-blue transition-colors font-medium"`
- Navigation column: same pattern as Services
- Contacts column:
  - UL: `class="space-y-4"`
  - Link wrapper: `class="flex items-center gap-3 text-mu-text-900 hover:text-mu-blue transition-colors font-semibold"`
  - Icon box: `class="bg-white/60 backdrop-blur-md p-2.5 rounded-xl border border-white/60 shadow-glass-inner-strong"`
- Bottom bar: `class="border-t border-mu-text-300/30 pt-8 mt-8"`
- Bottom flex: `class="flex flex-col md:flex-row justify-between items-center gap-4"`
- Copyright: `class="text-mu-text-700 font-medium text-sm"`
- ISO/Hub: `class="text-mu-text-700 font-medium text-sm flex items-center gap-2"`

**Section 14: Sticky Bar** (if present in current HTML)
Style with consistent glass treatment. Keep any JS-targeted classes.

**Section 15: Wrapper div around main content**
From Layout.tsx line 39, the content wrapper has `class="relative z-10 flex flex-col gap-8 md:gap-16 pb-8"`. Add this to the wrapping element around the main content (likely `<main>` or a wrapper div).

Also add to the `<style>` block in `<head>`:
```css
/* FAQ accordion animation */
.faq__answer { max-height: 0; overflow: hidden; transition: max-height 0.3s ease; }
.faq__item.is-open .faq__answer { max-height: 500px; }
```
(If this is already handled by existing CSS, just keep it.)
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && ./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css 2>&1 && grep -q 'backdrop-blur-3xl rounded-\[3\.5rem\]' index.html && grep -q 'faq__question' index.html && grep -q 'lg:grid-cols-4 gap-12' index.html && echo "BOTTOM OK" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'faq__question' index.html` confirms FAQ JS class preserved
    - `grep 'backdrop-blur-3xl rounded-\[3\.5rem\]' index.html` confirms CTA glass card from TSX
    - `grep 'lg:grid-cols-4 gap-12' index.html` confirms footer grid from TSX
    - `grep 'from-mu-blue via-mu-accent-blue to-mu-green-600' index.html` confirms gradient in footer brand
    - Tailwind build succeeds
  </acceptance_criteria>
  <done>FAQ, CTA, Footer, and sticky bar in index.html carry Tailwind utility classes from CTASection.tsx and Footer.tsx. All JS-targeted FAQ and sticky bar classes preserved.</done>
</task>

</tasks>

<verification>
1. `./tailwindcss -i src/styles/tailwind.css -o css/tw-output.css` succeeds
2. All bottom sections (WhyUs, Contact, FAQ, CTA, Footer) have Tailwind classes
3. JS-targeted BEM classes preserved: faq__question, contact-form, form__submit, form__success, is-open, is-invalid
4. Glass card patterns (backdrop-blur, rounded-[Xrem], shadow-glass) are consistent across sections
</verification>

<success_criteria>
Bottom half of index.html (WhyUs through Footer) carries Tailwind utility classes copied from the corresponding Redesign TSX components. All JS-targeted classes preserved. Tailwind build compiles without error.
</success_criteria>

<output>
After completion, create `.planning/phases/25-migrate-to-tailwind-css-v4/25-03-SUMMARY.md`
</output>
