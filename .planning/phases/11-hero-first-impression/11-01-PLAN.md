---
phase: 11-hero-first-impression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - PROOF-01

must_haves:
  truths:
    - "Hero section displays a detailed duotone SVG illustration of a doctor at a laptop instead of the abstract stethoscope"
    - "CTA buttons in hero are large and easy to tap (min-height 56px, font-size 18px)"
    - "Hero background gradient is more saturated and visually separated from the rest of the page"
    - "A social proof bar with 3 key numbers (7 stран, 50+ врачей, 15+ специализаций) appears between hero and Знакомо? section"
    - "Hero illustration is hidden on mobile (<768px), CTA buttons stack full-width"
    - "Social proof numbers stack vertically and centered on mobile"
  artifacts:
    - path: "index.html"
      provides: "Updated hero SVG illustration and social proof HTML block"
      contains: "social-proof"
    - path: "css/styles.css"
      provides: "Hero CTA enlargement, enhanced gradient, social proof styling"
      contains: ".social-proof"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "BEM classes .hero__*, .social-proof__*"
      pattern: "social-proof"
---

<objective>
Upgrade the hero first impression: replace abstract stethoscope SVG with a detailed doctor-at-laptop duotone illustration, enlarge CTA buttons for 45+ audience, enhance the hero background gradient, and add a social proof numbers bar between hero and the "Знакомо?" section.

Purpose: The hero is the first thing visitors see. A professional medical illustration, prominent CTAs, and trust-building numbers create an immediate impression of credibility and drive form submissions.
Output: Updated index.html with new SVG illustration and social proof block; updated styles.css with enlarged CTAs, enhanced gradient, and social proof styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hero-first-impression/11-CONTEXT.md
@index.html
@css/styles.css

<interfaces>
<!-- Key existing patterns the executor needs -->

From css/styles.css (design tokens):
```css
--color-primary: #38C6F4;
--color-primary-dark: #0E7490;
--color-secondary: #35B678;
--color-dark: #18212C;
--color-light: #F8FAFB;
--color-white: #FFFFFF;
--space-2: 1rem;     /* 16px */
--space-3: 1.5rem;   /* 24px */
--space-4: 2rem;     /* 32px */
--space-5: 2.5rem;   /* 40px */
--space-6: 3rem;     /* 48px */
--font-size-base: 1.125rem;  /* 18px */
--font-size-lg: 1.25rem;     /* 20px */
--radius-md: 0.5rem;
```

From css/styles.css (current button base):
```css
.button {
  min-height: 48px;
  padding: var(--space-2) var(--space-4);  /* 16px 32px */
  font-size: var(--font-size-base);        /* 18px */
}
```

From css/styles.css (current hero):
```css
.hero {
  background: linear-gradient(135deg, rgba(56, 198, 244, 0.08) 0%, rgba(53, 182, 120, 0.06) 100%), var(--color-light);
}
/* dot-grid via .hero::before with opacity: 0.03 */
/* .hero__illustration { display: none } on mobile, flex 0 0 35% at 768px+ */
```

From index.html (hero structure, lines 47-85):
```html
<section class="section hero" id="hero">
  <div class="container hero__container">
    <div class="hero__content">...</div>
    <div class="hero__illustration" aria-hidden="true">
      <svg><!-- stethoscope SVG --></svg>
    </div>
  </div>
</section>
<!-- section-divider between hero and problem (lines 87-91) -->
<section class="section problem" id="problem">
```

SVG duotone palette used throughout the site:
- Stroke/fill primary: #38C6F4
- Fill light: rgba(56,198,244,0.08)
- Fill accent: rgba(56,198,244,0.15)
- Secondary accent: rgba(53,182,120,0.12) and rgba(53,182,120,0.3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero SVG, enhance gradient, enlarge CTAs</name>
  <files>index.html, css/styles.css</files>

  <read_first>
    - index.html (lines 47-91: hero section through section-divider)
    - css/styles.css (lines 252-269: .button base, lines 273-280: .button--primary, lines 388-481: .hero block, lines 443-452: .button--outline)
  </read_first>

  <action>
**A. Replace hero SVG illustration (HERO-01)** in index.html lines 58-82:

Replace the entire `<svg>` inside `.hero__illustration` with a new duotone SVG illustration depicting a doctor at a laptop/video call. The SVG must:
- Use viewBox="0 0 400 400"
- Use the existing duotone palette: stroke `#38C6F4`, light fill `rgba(56,198,244,0.08)`, accent fill `rgba(56,198,244,0.15)`, green accents `rgba(53,182,120,0.12)` and `rgba(53,182,120,0.3)`
- Depict: a person (doctor) sitting at a desk with a laptop, screen showing a video call interface. Include a small medical cross or stethoscope detail to indicate medical context. Include decorative dots similar to the current ones.
- Use stroke-width="2" or "3" consistent with existing icons
- Keep stroke-linecap="round" and stroke-linejoin="round" for a friendly feel

**B. Enhance hero background gradient (HERO-03)** in css/styles.css:

Change `.hero` background (line 390) from:
```css
background: linear-gradient(135deg, rgba(56, 198, 244, 0.08) 0%, rgba(53, 182, 120, 0.06) 100%), var(--color-light);
```
to:
```css
background: linear-gradient(135deg, #e0f4fb 0%, #f0fdf9 50%, var(--color-white) 100%);
```

This makes the gradient more saturated (from nearly transparent 0.08 opacity to solid `#e0f4fb` light cyan) while fading to white at the bottom, creating a clear visual distinction from the white sections below.

Also increase the dot-grid opacity in `.hero::before` (line 401) from `opacity: 0.03` to `opacity: 0.05` for slightly more visible texture.

**C. Enlarge hero CTA buttons (HERO-02)** in css/styles.css:

Add a new rule block after `.hero__actions` (after line 440):
```css
/* HERO-02: Enlarged hero CTAs for 45+ audience */
.hero__actions .button {
  min-height: 56px;
  font-size: var(--font-size-lg);  /* 20px — one step up from base 18px */
  padding: var(--space-2) var(--space-4);  /* 16px 32px */
}
```

Add mobile-specific full-width stacking. Inside the existing mobile-first scope (before the 768px media query), add:
```css
.hero__actions {
  flex-direction: column;
}
.hero__actions .button {
  width: 100%;
}
```

At the 768px breakpoint (inside the existing `@media (min-width: 768px)` block near line 455), add:
```css
.hero__actions {
  flex-direction: row;
}
.hero__actions .button {
  width: auto;
}
```
  </action>

  <verify>
    <automated>grep -c "min-height: 56px" css/styles.css && grep -c "#e0f4fb" css/styles.css && grep -c "opacity: 0.05" css/styles.css && grep -c "laptop\|monitor\|screen\|desk" index.html</automated>
  </verify>

  <acceptance_criteria>
    - css/styles.css contains `.hero__actions .button` rule with `min-height: 56px`
    - css/styles.css contains `.hero__actions .button` rule with `font-size: var(--font-size-lg)`
    - css/styles.css `.hero` background contains `#e0f4fb`
    - css/styles.css `.hero::before` has `opacity: 0.05`
    - index.html `.hero__illustration svg` no longer contains the stethoscope paths (no "Stethoscope body" comment)
    - index.html `.hero__illustration svg` contains paths/elements depicting a doctor-at-laptop scene
    - css/styles.css contains mobile rule `.hero__actions .button { width: 100% }`
    - css/styles.css contains 768px+ rule `.hero__actions .button { width: auto }`
  </acceptance_criteria>

  <done>Hero displays a detailed duotone doctor-at-laptop SVG illustration. Background gradient is visibly more saturated (#e0f4fb to white). CTA buttons are 56px tall with 20px font. Buttons stack full-width on mobile, inline on 768px+.</done>
</task>

<task type="auto">
  <name>Task 2: Add social proof numbers bar</name>
  <files>index.html, css/styles.css</files>

  <read_first>
    - index.html (lines 85-100: area between hero closing tag and problem section opening)
    - css/styles.css (lines 1384-1415: section-divider styles for context on existing dividers)
    - css/styles.css (lines 53-117: design tokens for spacing/color values)
  </read_first>

  <action>
**A. Add social proof HTML (PROOF-01)** in index.html:

Between the closing `</section>` of hero (line 85) and the existing `.section-divider--light-to-white` div (line 87), insert a new social proof section:

```html
    <!-- Social Proof Numbers Bar (PROOF-01) -->
    <section class="social-proof" aria-label="Ключевые цифры">
      <div class="container social-proof__container">
        <div class="social-proof__item">
          <span class="social-proof__number">7</span>
          <span class="social-proof__label">стран</span>
        </div>
        <div class="social-proof__item">
          <span class="social-proof__number">50+</span>
          <span class="social-proof__label">врачей</span>
        </div>
        <div class="social-proof__item">
          <span class="social-proof__number">15+</span>
          <span class="social-proof__label">специализаций</span>
        </div>
      </div>
    </section>
```

Update the section-divider that follows: change its class from `section-divider--light-to-white` to `section-divider--dark-to-white` because the social proof bar has a dark background, and the divider now transitions from dark to white (problem section).

**B. Add social proof CSS** at the end of the hero section styles in css/styles.css (before the `/* -- Problem -- */` comment, around line 483):

```css
/* -- Social Proof Bar (PROOF-01) -- */

.social-proof {
  background-color: var(--color-primary-dark);  /* #0E7490 */
  padding-block: var(--space-4);  /* 32px */
}

.social-proof__container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-3);  /* 24px */
  text-align: center;
}

.social-proof__item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.25rem;  /* 4px */
}

.social-proof__number {
  font-family: var(--font-heading);
  font-size: 2.5rem;  /* 40px */
  font-weight: 700;
  line-height: 1;
  color: var(--color-white);
}

.social-proof__label {
  font-family: var(--font-body);
  font-size: var(--font-size-base);  /* 18px */
  font-weight: 400;
  color: rgba(255, 255, 255, 0.85);
}

@media (min-width: 768px) {
  .social-proof__container {
    flex-direction: row;
    justify-content: center;
    gap: var(--space-8);  /* 64px */
  }
}
```

**C. Add the new divider variant** in css/styles.css alongside the existing divider variants (after line 1407):

```css
.section-divider--dark-to-white {
  background-color: var(--color-primary-dark);  /* #0E7490 */
}
.section-divider--dark-to-white svg {
  fill: var(--color-white);
}
```
  </action>

  <verify>
    <automated>grep -c "social-proof" index.html && grep -c "social-proof__number" css/styles.css && grep -c "section-divider--dark-to-white" index.html && grep -c "section-divider--dark-to-white" css/styles.css</automated>
  </verify>

  <acceptance_criteria>
    - index.html contains `<section class="social-proof"` between hero section and section-divider
    - index.html social-proof section contains exactly 3 `.social-proof__item` elements
    - index.html social-proof numbers are "7", "50+", "15+" with labels "стран", "врачей", "специализаций"
    - index.html section-divider after social proof has class `section-divider--dark-to-white` (not `light-to-white`)
    - css/styles.css contains `.social-proof` with `background-color: var(--color-primary-dark)`
    - css/styles.css contains `.social-proof__number` with `font-size: 2.5rem`
    - css/styles.css contains `.social-proof__container` mobile: `flex-direction: column`, 768px+: `flex-direction: row`
    - css/styles.css contains `.section-divider--dark-to-white` with background-color and svg fill rules
  </acceptance_criteria>

  <done>A dark accent bar (#0E7490) with "7 стран", "50+ врачей", "15+ специализаций" appears between hero and "Знакомо?" section. Numbers stack vertically on mobile, display as a horizontal row on 768px+. Wave divider transitions from dark bar to white problem section.</done>
</task>

</tasks>

<verification>
1. Open index.html in a browser -- hero shows a doctor-at-laptop SVG illustration (not stethoscope)
2. Hero background is visibly cyan-tinted (#e0f4fb gradient), distinct from white sections below
3. CTA buttons are larger than before (56px height, 20px font)
4. Below hero, a dark teal bar shows 3 numbers in white text
5. On mobile viewport (<768px): illustration hidden, buttons full-width stacked, numbers stacked vertically
6. On desktop (768px+): 2-column hero with illustration, inline buttons, horizontal numbers row
7. Wave divider between social proof and problem section renders correctly (dark to white transition)
</verification>

<success_criteria>
- Hero illustration is a detailed duotone SVG of a doctor at a laptop (HERO-01)
- Hero CTA buttons have min-height: 56px and font-size: 20px (HERO-02)
- Hero background uses saturated #e0f4fb gradient (HERO-03)
- Social proof bar with 3 numbers visible between hero and "Знакомо?" (PROOF-01)
- Mobile and desktop responsive behavior correct
- No visual regression in surrounding sections
</success_criteria>

<output>
After completion, create `.planning/phases/11-hero-first-impression/11-01-SUMMARY.md`
</output>
