---
phase: 46-service-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [checkup.html, css/styles.css]
autonomous: true
requirements: [MIGRATE-03]
must_haves:
  truths:
    - "checkup.html uses max-w-[1200px] grid wrappers on all 12 sections"
    - "All card surfaces use liquid-card-wrap > liquid-card squircle-* shadow-wrap pattern"
    - "Stats bar uses .stats-glass squircle-xl grouped glass backdrop (DIFF-02)"
    - "whitespace-nowrap span on 'za 1-2 dnya' preserved in hero h1"
    - "All 380 nbsp entities preserved exactly"
    - "Form inputs use squircle-md (no nested glass), submit uses liquid-btn-primary squircle-md"
    - "FAQ items use liquid-regular squircle-md overflow-hidden"
    - "No rounded-[, rounded-2xl, rounded-3xl remain in <main> content"
  artifacts:
    - path: "checkup.html"
      provides: "v4.0 migrated checkup page"
      contains: "liquid-card"
    - path: "css/styles.css"
      provides: "Rebuilt Tailwind CSS with new utility usage"
  key_links:
    - from: "checkup.html"
      to: "css/styles.css"
      via: "Tailwind classes (liquid-*, squircle-*, grid-cols-12)"
      pattern: "liquid-card|squircle-xl|grid-cols-12"
    - from: "checkup.html stats section"
      to: "liquid-glass.css .stats-glass"
      via: "stats-glass class"
      pattern: "stats-glass"
---

<objective>
Migrate checkup.html main content (12 sections, 636 lines) from v3.x glass styling to v4.0 Liquid Design System classes. This is the most complex service page with 14 program cards, stats bar requiring grouped glass (DIFF-02), B2B section, highlighted/featured program cards with custom shadows, and the protected whitespace-nowrap span.

Purpose: Prove v4.0 migration pattern on the highest-complexity service page, including the DIFF-02 stats-glass requirement.
Output: checkup.html fully migrated, css/styles.css rebuilt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-service-pages/46-RESEARCH.md
@.planning/phases/45-simple-pages/45-02-SUMMARY.md

<interfaces>
<!-- Available CSS classes from liquid-glass.css and squircles.css -->

From src/styles/liquid-glass.css:
- .liquid-regular — base glass material (backdrop-filter blur + saturate + brightness, inset rim shadows)
- .liquid-card — glass card material (same as regular + padding: 1.5rem)
- .liquid-card-wrap — outer shadow wrapper (box-shadow: var(--liquid-shadow-outer), border-radius: 24px)
- .liquid-btn-primary — gradient CTA button (green-to-teal, specular edges, hover/active states)
- .liquid-btn-secondary — glass secondary button (backdrop-filter, hover/active states)
- .stats-glass — grouped stats backdrop (--liquid-blur-lg 40px, larger surface blur)
- .shimmer-sweep — hero CTA shimmer (not used on this page)

From src/styles/squircles.css:
- .squircle-md — 16px radius mask
- .squircle-lg — 24px radius mask
- .squircle-xl — 40px radius mask
- .squircle-full — 9999px radius (pill/circle)

Shadow-wrap pattern:
  <div class="liquid-card-wrap">           <!-- outer: shadow -->
    <div class="liquid-card squircle-xl">  <!-- inner: glass + mask -->
    </div>
  </div>
</interfaces>

<!-- Migration Map (canonical from Phase 45) -->
| Element Type | Old Pattern | New Pattern |
|-------------|------------|-------------|
| Section wrapper | container mx-auto px-4 lg:px-6 | max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8 |
| Grid layout | grid lg:grid-cols-2 gap-12 | grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-8 lg:gap-12 |
| Hero badge | bg-white/40 backdrop-blur-xl border border-white/60 rounded-full shadow-sm shadow-glass-inner | liquid-regular squircle-full |
| Large card | bg-white/60 backdrop-blur-2xl rounded-[2.5rem] ... shadow-glass | liquid-card-wrap > liquid-card squircle-xl |
| Medium card | bg-white/60 backdrop-blur-2xl rounded-[2rem] ... shadow-glass | liquid-card-wrap > liquid-card squircle-lg |
| Icon box | bg-white/50 backdrop-blur-xl rounded-2xl ... border border-white/60 | liquid-regular squircle-md |
| Form container | bg-white/70 backdrop-blur-3xl rounded-[3rem] ... shadow-glass-lg | liquid-card-wrap > liquid-card squircle-xl overflow-hidden |
| Form input | rounded-2xl (keep bg/blur) | squircle-md (keep bg/blur) |
| Submit button | bg-gradient-to-r ... rounded-2xl shadow-lg ... | liquid-btn-primary squircle-md |
| Primary CTA link | bg-gradient-to-r ... rounded-3xl font-semibold shadow-lg ... | liquid-btn-primary squircle-md |
| Secondary CTA link | bg-white/50 backdrop-blur-xl ... rounded-3xl ... | liquid-btn-secondary squircle-md |
| FAQ item | bg-white/60 backdrop-blur-2xl rounded-2xl ... shadow-glass-sm | liquid-regular squircle-md overflow-hidden |
| CTA card (full-width) | bg-white/60 backdrop-blur-3xl rounded-[3.5rem] ... shadow-glass-lg | liquid-card-wrap > liquid-card squircle-xl |
| Avatar/flag circle | rounded-full | squircle-full |
| Trust badge | bg-white/40 ... rounded-full ... | liquid-regular squircle-full |
| Image frame | rounded-[3rem] overflow-hidden ... border-[8px] border-white/40 | squircle-xl overflow-hidden in liquid-card-wrap |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate checkup.html 12 sections to v4.0 design system</name>
  <files>checkup.html, css/styles.css</files>
  <read_first>
    - checkup.html (lines 134-770, the entire <main> block)
    - src/styles/liquid-glass.css (full file -- available classes)
    - src/styles/squircles.css (full file -- available classes)
    - contacts.html (lines 134-270, migrated reference from Phase 45)
    - .planning/phases/46-service-pages/46-RESEARCH.md (migration map + section inventory)
    - .planning/phases/45-simple-pages/45-02-SUMMARY.md (proven migration pattern)
  </read_first>
  <action>
Migrate all 12 sections inside checkup.html `<main>` (lines 134-770) from v3.x glass classes to v4.0 Liquid Design System classes. Do NOT touch anything outside `<main>` (head, header, footer, sticky-bar, mobile-menu are chrome -- already upgraded in Phase 44).

**CRITICAL PROTECTED LEGACY:**
- MUST preserve all 380 `&nbsp;` entities exactly (run `grep -o '&nbsp;' checkup.html | wc -l` before AND after -- must equal 380)
- MUST preserve the `whitespace-nowrap` span: `<span class="whitespace-nowrap">` at approximately line 157 in hero h1
- MUST preserve all `role="alert"` (4 instances), `aria-live` (4 instances), `visually-hidden` (2 honeypot instances)
- MUST NOT modify `<head>`, `<style>` block, or any BUILD marker sections

**Section-by-section migration (12 sections):**

**Section 1: Hero (#hero-checkup)**
- Replace inner `container mx-auto px-4 lg:px-6` with `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8`
- Replace grid: `grid lg:grid-cols-2 gap-12` -> `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-8 lg:gap-12`
- Left content column: add `md:col-span-8 lg:col-span-6`
- Right image column: add `md:col-span-8 lg:col-span-6`
- Hero badge (glass-badge div): replace `bg-white/40 backdrop-blur-xl border border-white/60 ... rounded-full shadow-sm shadow-glass-inner` with `liquid-regular squircle-full`
- Primary CTA buttons: replace `bg-gradient-to-r from-mu-cta-from to-mu-cta-to text-white ... rounded-3xl font-semibold shadow-lg shadow-mu-blue/30 hover:shadow-xl hover:shadow-mu-blue/40 transition-all` with `liquid-btn-primary squircle-md` (keep px, py, text size, flex, gap, tracking)
- Secondary CTA: replace `bg-white/50 backdrop-blur-xl text-mu-text-900 ... rounded-3xl font-semibold shadow-glass ... border border-white/60` with `liquid-btn-secondary squircle-md` (keep px, py, text, flex, gap, tracking)
- Hero image frame: replace `rounded-[3rem] overflow-hidden shadow-glass-lg border-[8px] border-white/40 bg-white/20` with `squircle-xl overflow-hidden` wrapped in `liquid-card-wrap`
- DO NOT touch the `whitespace-nowrap` span or h1 structure

**Section 2: Stats bar (aria-label="Klyuchevye tsifry")**
- Replace `container mx-auto ...` wrapper with `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8`
- DIFF-02 grouped glass: Wrap the 4 stat cards in a single `liquid-card-wrap` > `stats-glass squircle-xl` container
- Inside the stats-glass container, keep the grid: `grid grid-cols-2 lg:grid-cols-4 gap-6 p-6`
- Remove individual card glass styling (`bg-white/60 backdrop-blur-2xl rounded-[2.5rem] ... shadow-glass border border-white/60`) -- stat cards become transparent cells within the grouped surface
- Keep stat card padding, text, counter attributes, and structural classes

**Section 3: Why Checkup (#why-checkup) -- 3-col cards**
- Replace `container mx-auto ...` with `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8`
- Replace card grid: use `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Each of the 3 cards: `md:col-span-4 lg:col-span-4`
- Card surface: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Icon boxes: replace `bg-white/50 backdrop-blur-xl rounded-2xl ... border border-white/60` with `liquid-regular squircle-md`

**Section 4: Why Abroad (#why-abroad) -- 2x2 cards**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Each of 4 cards: `md:col-span-4 lg:col-span-6`
- Card surface: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Icon boxes: `liquid-regular squircle-md`

**Section 5: Why Us (#why-us) -- 5 text-only cards**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Cards: `md:col-span-4 lg:col-span-4` (wraps at 3+2 on lg, 2+2+1 on md)
- Card surface: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Icon boxes: `liquid-regular squircle-md`

**Section 6: Programs Korea (#programs-korea) -- 14 program cards**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Regular program cards: `md:col-span-4 lg:col-span-4`, `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- 2 HIGHLIGHTED program cards (with `border-mu-blue/40` and custom shadow): `liquid-card-wrap shadow-[0_16px_48px_color-mix(in_oklch,var(--color-mu-blue)_15%,transparent)]` > `liquid-card squircle-xl p-8` -- custom shadow on wrapper, NOT the default
- Program badge pills (rounded-full with gradient): replace `rounded-full` with `squircle-full` (keep gradient bg on highlighted badges, keep non-gradient styling on regular badges)
- CTA buttons in cards: replace gradient rounded-3xl with `liquid-btn-primary squircle-md`

**Section 7: Programs Turkey (#programs-turkey) -- 4 program cards + info block**
- Same container and grid replacement
- Program cards: same pattern as Section 6 regular cards
- Info card (rounded-[2.5rem]): `liquid-card-wrap` > `liquid-card squircle-xl p-8`

**Section 8: How It Works (#how-it-works) -- 5 step cards**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Step cards: `md:col-span-4 lg:col-span-4` (wraps 3+2 at lg), `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Timing badges (rounded-full): `squircle-full` (keep bg colors)

**Section 9: B2B (#b2b) -- 2 cards + info card + CTA**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Cards: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Info/full-width card: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- CTA button: `liquid-btn-primary squircle-md`

**Section 10: FAQ (#faq-checkup) -- 7 accordion items**
- Same container replacement
- FAQ items: replace `bg-white/60 backdrop-blur-2xl rounded-2xl border border-white/60 shadow-glass-sm` with `liquid-regular squircle-md` -- keep `overflow-hidden` for accordion animation
- Do NOT use shadow-wrap on FAQ items (shadow-glass-sm is inset only)

**Section 11: Form (#form-checkup) -- 2-col form**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-8 lg:gap-12`
- Left info column: `md:col-span-4 lg:col-span-5`
- Right form column: `md:col-span-4 lg:col-span-7`
- Form container: `liquid-card-wrap` > `liquid-card squircle-xl p-8 relative overflow-hidden`
- Form inputs: ONLY replace `rounded-2xl` with `squircle-md` -- keep existing `bg-white/50 backdrop-blur-md` etc. NO liquid-regular/liquid-card on inputs (no nested glass)
- Submit button: replace gradient+rounded classes with `liquid-btn-primary squircle-md` -- keep `w-full py-4 font-bold flex items-center justify-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed text-lg mt-8`
- MUST preserve honeypot inputs (visually-hidden), role="alert", aria-live="polite" on all error containers

**Section 12: Final CTA (#final-cta-checkup) -- cross-sell card**
- Same container replacement
- CTA card: `liquid-card-wrap` > `liquid-card squircle-xl p-12`
- CTA buttons: `liquid-btn-primary squircle-md` and `liquid-btn-secondary squircle-md`

**After all edits, rebuild CSS:**
```bash
make build
```

**Post-migration verification (run ALL these commands):**
```bash
grep -o '&amp;nbsp;' checkup.html | wc -l              # MUST be 380
grep -c 'whitespace-nowrap' checkup.html          # MUST be 1
grep -c 'visually-hidden' checkup.html            # MUST be 2
grep -c 'role="alert"' checkup.html               # MUST be 4
grep -c 'aria-live' checkup.html                  # MUST be 4
sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-\['    # MUST be 0
sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-2xl'   # MUST be 0
sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-3xl'   # MUST be 0
sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'container mx-auto'  # MUST be 0
grep -c 'squircle-' checkup.html                  # Should be high (60+)
grep -c 'liquid-card' checkup.html                # Should be high (30+)
grep -c 'liquid-btn-primary' checkup.html         # Should be >= 3
grep -c 'max-w-\[1200px\]' checkup.html          # Should be >= 12
grep -c 'grid-cols-12' checkup.html               # Should be >= 1
grep -c 'stats-glass' checkup.html                # MUST be >= 1 (DIFF-02)
```
  </action>
  <acceptance_criteria>
    - `grep -o '&amp;nbsp;' checkup.html | wc -l` equals 380
    - `grep -c 'whitespace-nowrap' checkup.html` equals 1
    - `grep -c 'visually-hidden' checkup.html` equals 2
    - `grep -c 'role="alert"' checkup.html` equals 4
    - `grep -c 'aria-live' checkup.html` equals 4
    - `sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-\['` equals 0
    - `sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-2xl'` equals 0
    - `sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-3xl'` equals 0
    - `sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'container mx-auto'` equals 0
    - `grep -c 'squircle-' checkup.html` >= 60
    - `grep -c 'liquid-card' checkup.html` >= 30
    - `grep -c 'liquid-btn-primary' checkup.html` >= 3
    - `grep -c 'max-w-\[1200px\]' checkup.html` >= 12
    - `grep -c 'stats-glass' checkup.html` >= 1
    - `make build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "--- Protected Legacy ---" && echo -n "nbsp: " && grep -o '&amp;nbsp;' checkup.html | wc -l && echo -n "whitespace-nowrap: " && grep -c 'whitespace-nowrap' checkup.html && echo -n "visually-hidden: " && grep -c 'visually-hidden' checkup.html && echo -n "role-alert: " && grep -c 'role="alert"' checkup.html && echo -n "aria-live: " && grep -c 'aria-live' checkup.html && echo "--- Old classes removed ---" && echo -n "rounded-[ in main: " && sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-\[' && echo -n "rounded-2xl in main: " && sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-2xl' && echo -n "rounded-3xl in main: " && sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'rounded-3xl' && echo -n "container mx-auto in main: " && sed -n '/<main/,/<\/main>/p' checkup.html | grep -c 'container mx-auto' && echo "--- New classes present ---" && echo -n "squircle-: " && grep -c 'squircle-' checkup.html && echo -n "liquid-card: " && grep -c 'liquid-card' checkup.html && echo -n "liquid-btn-primary: " && grep -c 'liquid-btn-primary' checkup.html && echo -n "max-w-[1200px]: " && grep -c 'max-w-\[1200px\]' checkup.html && echo -n "stats-glass: " && grep -c 'stats-glass' checkup.html</automated>
  </verify>
  <done>checkup.html fully migrated to v4.0 with grid wrappers, liquid glass surfaces, squircle shapes, stats-glass grouped backdrop (DIFF-02), protected legacy preserved (380 nbsp, whitespace-nowrap, ARIA, honeypots), zero old rounded-[/2xl/3xl in main, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form submission | User form data sent to Directus API (unchanged -- this phase is CSS-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | checkup.html form | accept | Form validation JS and honeypot are Protected Legacy -- untouched by CSS migration |
| T-46-02 | Information Disclosure | checkup.html head metadata | accept | Head section excluded from migration scope -- append-only policy |
</threat_model>

<verification>
1. `make build` exits 0
2. Protected legacy: 380 nbsp, 1 whitespace-nowrap, 2 visually-hidden, 4 role="alert", 4 aria-live
3. Zero old rounded classes in main: rounded-[, rounded-2xl, rounded-3xl all 0
4. Zero container mx-auto in main
5. New classes present: squircle- >= 60, liquid-card >= 30, liquid-btn-primary >= 3, max-w-[1200px] >= 12, stats-glass >= 1
</verification>

<success_criteria>
checkup.html renders with v4.0 Liquid Design System visual language -- grid wrappers, glass card surfaces, squircle shapes, grouped stats backdrop -- with all protected legacy items verified by grep counts matching baselines.
</success_criteria>

<output>
After completion, create `.planning/phases/46-service-pages/46-01-SUMMARY.md`
</output>
