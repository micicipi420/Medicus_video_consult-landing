---
phase: 46-service-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [online-consultations.html, css/styles.css]
autonomous: true
requirements: [MIGRATE-04]
must_haves:
  truths:
    - "online-consultations.html uses max-w-[1200px] grid wrappers on all 12 sections"
    - "All card surfaces use liquid-card-wrap > liquid-card squircle-* shadow-wrap pattern"
    - "Doctor country cards use liquid-card-wrap > liquid-card squircle-lg with squircle-full flag images"
    - "Pricing card uses liquid-card-wrap > liquid-card squircle-xl with gradient CTA intact"
    - "Specialization badges use liquid-regular squircle-full"
    - "All 203 nbsp entities preserved exactly"
    - "Form inputs use squircle-md (no nested glass), submit uses liquid-btn-primary squircle-md"
    - "No rounded-[, rounded-2xl, rounded-3xl remain in <main> content"
  artifacts:
    - path: "online-consultations.html"
      provides: "v4.0 migrated online consultations page"
      contains: "liquid-card"
    - path: "css/styles.css"
      provides: "Rebuilt Tailwind CSS with new utility usage"
  key_links:
    - from: "online-consultations.html"
      to: "css/styles.css"
      via: "Tailwind classes (liquid-*, squircle-*, grid-cols-12)"
      pattern: "liquid-card|squircle-xl|grid-cols-12"
---

<objective>
Migrate online-consultations.html main content (12 sections, 661 lines) from v3.x glass styling to v4.0 Liquid Design System classes. This page has unique elements: 7 doctor country cards with flag images, 14 specialization badges, a pricing card with gradient CTA badge, and a trigger checklist card.

Purpose: Prove v4.0 migration on the consultations page with its distinctive card varieties.
Output: online-consultations.html fully migrated, css/styles.css rebuilt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-service-pages/46-RESEARCH.md
@.planning/phases/45-simple-pages/45-02-SUMMARY.md

<interfaces>
<!-- Available CSS classes from liquid-glass.css and squircles.css -->

From src/styles/liquid-glass.css:
- .liquid-regular — base glass material (backdrop-filter blur + saturate + brightness, inset rim shadows)
- .liquid-card — glass card material (same as regular + padding: 1.5rem)
- .liquid-card-wrap — outer shadow wrapper (box-shadow: var(--liquid-shadow-outer), border-radius: 24px)
- .liquid-btn-primary — gradient CTA button (green-to-teal, specular edges, hover/active states)
- .liquid-btn-secondary — glass secondary button (backdrop-filter, hover/active states)

From src/styles/squircles.css:
- .squircle-md — 16px radius mask
- .squircle-lg — 24px radius mask
- .squircle-xl — 40px radius mask
- .squircle-full — 9999px radius (pill/circle)

Shadow-wrap pattern:
  <div class="liquid-card-wrap">           <!-- outer: shadow -->
    <div class="liquid-card squircle-xl">  <!-- inner: glass + mask -->
    </div>
  </div>
</interfaces>

<!-- Migration Map (canonical from Phase 45) -->
| Element Type | Old Pattern | New Pattern |
|-------------|------------|-------------|
| Section wrapper | container mx-auto px-4 lg:px-6 | max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8 |
| Grid layout | grid lg:grid-cols-2 gap-12 | grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-8 lg:gap-12 |
| Hero badge | bg-white/40 backdrop-blur-xl border border-white/60 rounded-full shadow-sm shadow-glass-inner | liquid-regular squircle-full |
| Large card | bg-white/60 backdrop-blur-2xl rounded-[2.5rem] ... shadow-glass | liquid-card-wrap > liquid-card squircle-xl |
| Medium card | bg-white/60 backdrop-blur-2xl rounded-[2rem] ... shadow-glass | liquid-card-wrap > liquid-card squircle-lg |
| Icon box | bg-white/50 backdrop-blur-xl rounded-2xl ... border border-white/60 | liquid-regular squircle-md |
| Form container | bg-white/70 backdrop-blur-3xl rounded-[3rem] ... shadow-glass-lg | liquid-card-wrap > liquid-card squircle-xl overflow-hidden |
| Form input | rounded-2xl (keep bg/blur) | squircle-md (keep bg/blur) |
| Submit button | bg-gradient-to-r ... rounded-2xl shadow-lg ... | liquid-btn-primary squircle-md |
| Primary CTA link | bg-gradient-to-r ... rounded-3xl font-semibold shadow-lg ... | liquid-btn-primary squircle-md |
| Secondary CTA link | bg-white/50 backdrop-blur-xl ... rounded-3xl ... | liquid-btn-secondary squircle-md |
| FAQ item | bg-white/60 backdrop-blur-2xl rounded-2xl ... shadow-glass-sm | liquid-regular squircle-md overflow-hidden |
| CTA card (full-width) | bg-white/60 backdrop-blur-3xl rounded-[3.5rem] ... shadow-glass-lg | liquid-card-wrap > liquid-card squircle-xl |
| Avatar/flag circle | rounded-full | squircle-full |
| Trust badge (pill) | bg-white/40 ... rounded-full ... | liquid-regular squircle-full |
| Image frame | rounded-[3rem] overflow-hidden ... | squircle-xl overflow-hidden in liquid-card-wrap |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate online-consultations.html 12 sections to v4.0 design system</name>
  <files>online-consultations.html, css/styles.css</files>
  <read_first>
    - online-consultations.html (lines 137-798, the entire <main> block)
    - src/styles/liquid-glass.css (full file -- available classes)
    - src/styles/squircles.css (full file -- available classes)
    - contacts.html (lines 134-270, migrated reference from Phase 45)
    - .planning/phases/46-service-pages/46-RESEARCH.md (migration map + section inventory)
    - .planning/phases/45-simple-pages/45-02-SUMMARY.md (proven migration pattern)
  </read_first>
  <action>
Migrate all 12 sections inside online-consultations.html `<main>` (lines 137-798) from v3.x glass classes to v4.0 Liquid Design System classes. Do NOT touch anything outside `<main>` (head, header, footer, sticky-bar, mobile-menu are chrome -- already upgraded in Phase 44).

**CRITICAL PROTECTED LEGACY:**
- MUST preserve all 203 `&nbsp;` entities exactly (run `grep -o '&amp;nbsp;' online-consultations.html | wc -l` before AND after -- must equal 203)
- MUST preserve all `role="alert"` (4 instances), `aria-live` (4 instances), `visually-hidden` (2 honeypot instances)
- MUST NOT modify `<head>`, `<style>` block, or any BUILD marker sections

**Section-by-section migration (12 sections):**

**Section 1: Hero (#hero)**
- Replace inner `container mx-auto px-4 lg:px-6` with `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8`
- Replace grid: `grid lg:grid-cols-2 gap-12` -> `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-8 lg:gap-12`
- Left content column: add `md:col-span-8 lg:col-span-6`
- Right image column: add `md:col-span-8 lg:col-span-6`
- Hero badge: replace `bg-white/40 backdrop-blur-xl border border-white/60 ... rounded-full shadow-sm shadow-glass-inner` with `liquid-regular squircle-full`
- Primary CTA: replace gradient+rounded-3xl+shadow with `liquid-btn-primary squircle-md` (keep px, py, text, flex, gap, tracking)
- Secondary CTA: replace glass+rounded-3xl with `liquid-btn-secondary squircle-md` (keep px, py, text, flex, gap, tracking)
- Hero image frame: replace `rounded-[3rem] overflow-hidden shadow-glass-lg border-[8px] border-white/40 bg-white/20` with `squircle-xl overflow-hidden` wrapped in `liquid-card-wrap`
- Trust items below buttons: replace glass pill styling with `liquid-regular squircle-full`

**Section 2: Features (#features) -- 3x2 feature cards**
- Section already uses `container mx-auto px-4 lg:px-6` on section tag -- replace with `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8`
- Replace card grid to use 12-col: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Each of 6 cards: `md:col-span-4 lg:col-span-4`
- Card surface: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Icon boxes: replace `bg-white/50 backdrop-blur-xl rounded-2xl ... border border-white/60` with `liquid-regular squircle-md`

**Section 3: Problem (#problem) -- single content card**
- Same container replacement
- Card: `liquid-card-wrap` > `liquid-card squircle-xl p-8`

**Section 4: Benefits (#benefits) -- 2x2 benefit cards**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Each of 4 cards: `md:col-span-4 lg:col-span-6`
- Card surface: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Icon boxes: `liquid-regular squircle-md`

**Section 5: Process (#process) -- 3 step cards**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Each of 3 cards: `md:col-span-4 lg:col-span-4`
- Card surface: `liquid-card-wrap` > `liquid-card squircle-xl p-8`

**Section 6: Doctors (#doctors) -- country cards + specialization badges**
- Same container replacement
- Country cards grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- 7 country cards: `md:col-span-4 lg:col-span-3`, `liquid-card-wrap` > `liquid-card squircle-lg p-4` (medium cards)
- Flag images: replace `rounded-full` with `squircle-full`
- Specialization container: replace `rounded-[3rem]` with `liquid-card-wrap` > `liquid-card squircle-xl`
- 14 specialization badges: replace `rounded-full` glass styling with `liquid-regular squircle-full`
- Info card: `liquid-card-wrap` > `liquid-card squircle-xl p-8`

**Section 7: Why MedicusUnion (#why-medicusunion) -- 5 cards**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-6`
- Each card: `md:col-span-4 lg:col-span-4` (wraps 3+2 at lg)
- Card surface: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Icon boxes: `liquid-regular squircle-md`

**Section 8: Triggers (#triggers) -- single card + checklist**
- Same container replacement
- Card: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Check circles (rounded-full): `squircle-full`

**Section 9: Pricing (#pricing) -- pricing card**
- Same container replacement
- Pricing card: `liquid-card-wrap` > `liquid-card squircle-xl p-8`
- Badge (rounded-full): `squircle-full` -- keep gradient bg if it has one
- CTA button: replace `rounded-2xl` gradient with `liquid-btn-primary squircle-md`

**Section 10: Consultation Form (#consultation-form) -- 2-col form + trust badges**
- Same container replacement
- Grid: `grid grid-cols-1 md:grid-cols-8 lg:grid-cols-12 gap-8 lg:gap-12`
- Left info column: `md:col-span-4 lg:col-span-5`
- Right form column: `md:col-span-4 lg:col-span-7`
- Form container: `liquid-card-wrap` > `liquid-card squircle-xl p-8 relative overflow-hidden`
- Form inputs: ONLY replace `rounded-2xl` with `squircle-md` -- keep existing bg/blur. NO liquid-* on inputs
- Submit button: `liquid-btn-primary squircle-md` -- keep w-full py-4 font-bold flex items-center justify-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed text-lg mt-8
- Trust badges (rounded-full): `liquid-regular squircle-full`
- MUST preserve honeypot inputs, role="alert", aria-live="polite" on error containers

**Section 11: FAQ (#faq) -- 6 accordion items**
- Same container replacement
- FAQ items: replace `bg-white/60 backdrop-blur-2xl rounded-2xl border border-white/60 shadow-glass-sm` with `liquid-regular squircle-md` -- keep `overflow-hidden`

**Section 12: Final CTA (#final-cta) -- cross-sell card**
- Same container replacement
- CTA card: `liquid-card-wrap` > `liquid-card squircle-xl p-12`
- CTA button: `liquid-btn-primary squircle-md`

**After all edits, rebuild CSS:**
```bash
make build
```

**Post-migration verification (run ALL these commands):**
```bash
grep -o '&amp;nbsp;' online-consultations.html | wc -l      # MUST be 203
grep -c 'visually-hidden' online-consultations.html    # MUST be 2
grep -c 'role="alert"' online-consultations.html       # MUST be 4
grep -c 'aria-live' online-consultations.html          # MUST be 4
sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-\['    # MUST be 0
sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-2xl'   # MUST be 0
sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-3xl'   # MUST be 0
sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'container mx-auto'  # MUST be 0
grep -c 'squircle-' online-consultations.html          # Should be high (60+)
grep -c 'liquid-card' online-consultations.html        # Should be high (30+)
grep -c 'liquid-btn-primary' online-consultations.html # Should be >= 2
grep -c 'max-w-\[1200px\]' online-consultations.html  # Should be >= 12
grep -c 'grid-cols-12' online-consultations.html       # Should be >= 1
```
  </action>
  <acceptance_criteria>
    - `grep -o '&amp;nbsp;' online-consultations.html | wc -l` equals 203
    - `grep -c 'visually-hidden' online-consultations.html` equals 2
    - `grep -c 'role="alert"' online-consultations.html` equals 4
    - `grep -c 'aria-live' online-consultations.html` equals 4
    - `sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-\['` equals 0
    - `sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-2xl'` equals 0
    - `sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-3xl'` equals 0
    - `sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'container mx-auto'` equals 0
    - `grep -c 'squircle-' online-consultations.html` >= 60
    - `grep -c 'liquid-card' online-consultations.html` >= 30
    - `grep -c 'liquid-btn-primary' online-consultations.html` >= 2
    - `grep -c 'max-w-\[1200px\]' online-consultations.html` >= 12
    - `make build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && make build && echo "--- Protected Legacy ---" && echo -n "nbsp: " && grep -o '&amp;nbsp;' online-consultations.html | wc -l && echo -n "visually-hidden: " && grep -c 'visually-hidden' online-consultations.html && echo -n "role-alert: " && grep -c 'role="alert"' online-consultations.html && echo -n "aria-live: " && grep -c 'aria-live' online-consultations.html && echo "--- Old classes removed ---" && echo -n "rounded-[ in main: " && sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-\[' && echo -n "rounded-2xl in main: " && sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-2xl' && echo -n "rounded-3xl in main: " && sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'rounded-3xl' && echo -n "container mx-auto in main: " && sed -n '/<main/,/<\/main>/p' online-consultations.html | grep -c 'container mx-auto' && echo "--- New classes present ---" && echo -n "squircle-: " && grep -c 'squircle-' online-consultations.html && echo -n "liquid-card: " && grep -c 'liquid-card' online-consultations.html && echo -n "liquid-btn-primary: " && grep -c 'liquid-btn-primary' online-consultations.html && echo -n "max-w-[1200px]: " && grep -c 'max-w-\[1200px\]' online-consultations.html</automated>
  </verify>
  <done>online-consultations.html fully migrated to v4.0 with grid wrappers, liquid glass surfaces, squircle shapes on all cards/badges/inputs, protected legacy preserved (203 nbsp, ARIA, honeypots), zero old rounded-[/2xl/3xl in main, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form submission | User form data sent to Directus API (unchanged -- CSS-only migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03 | Tampering | online-consultations.html form | accept | Form validation JS and honeypot are Protected Legacy -- untouched by CSS migration |
| T-46-04 | Information Disclosure | online-consultations.html head metadata | accept | Head section excluded from migration scope -- append-only policy |
</threat_model>

<verification>
1. `make build` exits 0
2. Protected legacy: 203 nbsp, 2 visually-hidden, 4 role="alert", 4 aria-live
3. Zero old rounded classes in main: rounded-[, rounded-2xl, rounded-3xl all 0
4. Zero container mx-auto in main
5. New classes present: squircle- >= 60, liquid-card >= 30, liquid-btn-primary >= 2, max-w-[1200px] >= 12
</verification>

<success_criteria>
online-consultations.html renders with v4.0 Liquid Design System visual language -- grid wrappers, glass card surfaces, squircle shapes on all elements including doctor country cards, specialization badges, and pricing card -- with all protected legacy items verified by grep counts.
</success_criteria>

<output>
After completion, create `.planning/phases/46-service-pages/46-02-SUMMARY.md`
</output>
