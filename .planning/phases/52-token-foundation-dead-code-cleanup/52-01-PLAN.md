---
phase: 52-token-foundation-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/theme.css
  - src/styles/index.css
autonomous: true
requirements:
  - CLEN-01
  - CLEN-03

must_haves:
  truths:
    - "No shadcn/React legacy tokens (popover, chart, sidebar families) exist in any CSS file"
    - "src/styles/index.css does not exist on disk"
    - "Unused green ramp tokens (--mu-green-200, --mu-green-400, --mu-green-900) are gone from theme.css"
    - "make build exits 0 after all removals"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Clean token foundation without dead shadcn/React or green ramp tokens"
      contains: "--mu-green-50"
  key_links:
    - from: "src/styles/theme.css"
      to: "css/styles.css"
      via: "Tailwind build pipeline"
      pattern: "make build"
---

<objective>
Remove all unused CSS tokens from theme.css and delete the dead src/styles/index.css file.

Purpose: Eliminate ~80 lines of shadcn/React legacy tokens (popover, chart-1..5, sidebar families) that were carried over from a React/Redesign project and are never consumed. Also remove 3 unused green ramp tokens and a dead reference file. This cleans the token foundation for subsequent v5.0 phases.

Output: A leaner theme.css with only actively-used tokens, and one fewer dead file in src/styles/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shadcn/React legacy tokens from theme.css (CLEN-01)</name>
  <files>src/styles/theme.css</files>
  <read_first>src/styles/theme.css</read_first>
  <action>
Remove all shadcn/React legacy token declarations from src/styles/theme.css. These tokens are defined but never consumed by any component CSS or HTML in the project.

**In the `:root` block (lines 64-96 area), remove these lines:**
- `--popover: oklch(1 0 0);`
- `--popover-foreground: oklch(0.145 0 0);`
- `--chart-1: oklch(0.646 0.222 41.116);`
- `--chart-2: oklch(0.6 0.118 184.704);`
- `--chart-3: oklch(0.398 0.07 227.392);`
- `--chart-4: oklch(0.828 0.189 84.429);`
- `--chart-5: oklch(0.769 0.188 70.08);`
- `--sidebar: oklch(0.985 0 0);`
- `--sidebar-foreground: oklch(0.145 0 0);`
- `--sidebar-primary: #030213;`
- `--sidebar-primary-foreground: oklch(0.985 0 0);`
- `--sidebar-accent: oklch(0.97 0 0);`
- `--sidebar-accent-foreground: oklch(0.205 0 0);`
- `--sidebar-border: oklch(0.922 0 0);`
- `--sidebar-ring: oklch(0.708 0 0);`

**In the `.dark` block (lines 149-178 area), remove these lines:**
- `--popover: oklch(0.145 0 0);`
- `--popover-foreground: oklch(0.985 0 0);`
- `--chart-1: oklch(0.488 0.243 264.376);`
- `--chart-2: oklch(0.696 0.17 162.48);`
- `--chart-3: oklch(0.769 0.188 70.08);`
- `--chart-4: oklch(0.627 0.265 303.9);`
- `--chart-5: oklch(0.645 0.246 16.439);`
- `--sidebar: oklch(0.205 0 0);`
- `--sidebar-foreground: oklch(0.985 0 0);`
- `--sidebar-primary: oklch(0.488 0.243 264.376);`
- `--sidebar-primary-foreground: oklch(0.985 0 0);`
- `--sidebar-accent: oklch(0.269 0 0);`
- `--sidebar-accent-foreground: oklch(0.985 0 0);`
- `--sidebar-border: oklch(0.269 0 0);`
- `--sidebar-ring: oklch(0.439 0 0);`

**In the `@theme inline` block (lines 243-276 area), remove these lines:**
- `--color-popover: var(--popover);`
- `--color-popover-foreground: var(--popover-foreground);`
- `--color-chart-1: var(--chart-1);`
- `--color-chart-2: var(--chart-2);`
- `--color-chart-3: var(--chart-3);`
- `--color-chart-4: var(--chart-4);`
- `--color-chart-5: var(--chart-5);`
- `--color-sidebar: var(--sidebar);`
- `--color-sidebar-foreground: var(--sidebar-foreground);`
- `--color-sidebar-primary: var(--sidebar-primary);`
- `--color-sidebar-primary-foreground: var(--sidebar-primary-foreground);`
- `--color-sidebar-accent: var(--sidebar-accent);`
- `--color-sidebar-accent-foreground: var(--sidebar-accent-foreground);`
- `--color-sidebar-border: var(--sidebar-border);`
- `--color-sidebar-ring: var(--sidebar-ring);`

Do NOT remove: --background, --foreground, --card, --card-foreground, --primary, --secondary, --muted, --accent, --destructive, --border, --input, --ring, --radius -- these may be consumed by Tailwind's base layer or component styles.

After removal, verify no blank line clusters remain (collapse consecutive empty lines to single blank lines).
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && grep -c "popover\|chart-[1-5]\|sidebar" src/styles/theme.css | grep -q "^0$" && echo "PASS: zero legacy tokens" || echo "FAIL: legacy tokens remain"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "popover" src/styles/theme.css` returns 0
- `grep -c "chart-" src/styles/theme.css` returns 0
- `grep -c "sidebar" src/styles/theme.css` returns 0
- `grep -c "--color-popover" src/styles/theme.css` returns 0
- `grep -c "--color-chart" src/styles/theme.css` returns 0
- `grep -c "--color-sidebar" src/styles/theme.css` returns 0
- theme.css still contains `--background:` (not removed)
- theme.css still contains `--primary:` (not removed)
- theme.css still contains `--mu-green-50:` (not part of CLEN-01)
  </acceptance_criteria>
  <done>All shadcn/React legacy tokens (popover, chart-1..5, sidebar families) removed from :root, .dark, and @theme inline blocks in theme.css. ~45 lines eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead file and remove unused green ramp tokens (CLEN-03)</name>
  <files>src/styles/index.css, src/styles/theme.css</files>
  <read_first>src/styles/index.css, src/styles/theme.css</read_first>
  <action>
**Step 1: Delete src/styles/index.css**

Run `rm src/styles/index.css` (or use git rm). This file is a 12-line comment-only reference file from the React/Redesign era. It is not imported by any build step, not referenced in the Makefile, and not loaded by any HTML page.

**Step 2: Remove unused green ramp tokens from theme.css**

In the `:root` block, remove these 3 lines:
- `--mu-green-200: #B8F2D6;`
- `--mu-green-400: #79E9B3;`
- `--mu-green-900: #1A6B42;`

In the `@theme inline` block, remove these 3 lines:
- `--color-mu-green-200: var(--mu-green-200);`
- `--color-mu-green-400: var(--mu-green-400);`
- `--color-mu-green-900: var(--mu-green-900);`

These 6 tokens are defined but never consumed: no HTML file references them via class or inline style, no CSS file uses `var(--mu-green-200)` etc. outside of their own definition lines.

Do NOT remove: --mu-green-50, --mu-green-100, --mu-green-300, --mu-green-500, --mu-green-600, --mu-green-700 -- these ARE used (e.g., bg-mu-green-50 in HTML, --mu-green-600 in form validation CSS).

**Step 3: Run make build and verify**

Run `make build` and confirm exit 0. The Tailwind compilation will produce a smaller css/styles.css since the removed tokens no longer generate utility classes.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && test ! -f src/styles/index.css && grep -c "mu-green-200\|mu-green-400\|mu-green-900" src/styles/theme.css | grep -q "^0$" && make build 2>&1 | tail -1 | grep -q "done" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `test -f src/styles/index.css` returns exit code 1 (file does not exist)
- `grep -c "mu-green-200" src/styles/theme.css` returns 0
- `grep -c "mu-green-400" src/styles/theme.css` returns 0
- `grep -c "mu-green-900" src/styles/theme.css` returns 0
- `grep "mu-green-50" src/styles/theme.css` still finds the token (not removed)
- `grep "mu-green-300" src/styles/theme.css` still finds the token (not removed)
- `grep "mu-green-600" src/styles/theme.css` still finds the token (not removed)
- `make build` exits 0 with "[build] done" output
  </acceptance_criteria>
  <done>src/styles/index.css deleted. 3 unused green ramp tokens (--mu-green-200, -400, -900) removed from both :root and @theme inline. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this plan only removes dead CSS tokens and a dead file. No user input, no API calls, no data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Denial of Service | CSS build | accept | Token removal reduces attack surface; build verified via `make build` |
</threat_model>

<verification>
1. `grep -E "popover|chart-[1-5]|sidebar" src/styles/theme.css` returns no matches
2. `grep -E "mu-green-200|mu-green-400|mu-green-900" src/styles/theme.css` returns no matches
3. `test ! -f src/styles/index.css` exits 0
4. `make build` exits 0
5. `grep "mu-green-50" src/styles/theme.css` still finds token (safety check: not over-deleted)
</verification>

<success_criteria>
- theme.css contains zero shadcn/React legacy tokens
- theme.css contains zero unused green ramp tokens
- src/styles/index.css is deleted
- make build exits 0
- All actively-used tokens (--mu-green-50 through --mu-green-700, --background, --primary, etc.) remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/52-token-foundation-dead-code-cleanup/52-01-SUMMARY.md`
</output>
