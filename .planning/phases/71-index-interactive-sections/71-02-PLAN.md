---
phase: 71-index-interactive-sections
plan: 02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - next/src/components/sections/ContactSection.tsx
  - next/src/components/sections/ContactForm.tsx
  - next/src/components/sections/FinalCTA.tsx
  - next/src/app/page.tsx
  - next/public/cta-doctor.webp
autonomous: true
requirements:
  - SEC-10
  - SEC-11

must_haves:
  truths:
    - "ContactSection displays a 2-column layout with info/coordinator on left and glass form card on right"
    - "ContactForm preserves the existing submitContactForm server action, validation, honeypot, and all form state management"
    - "FinalCTA renders as a glass card with 2-column layout: left content with CTAs, right doctor image"
    - "page.tsx imports and renders ReviewsSection and FAQSection between PlatformSection and ContactSection"
  artifacts:
    - path: "next/src/components/sections/ContactSection.tsx"
      provides: "Contact section with coordinator card and trust badges"
      exports: ["ContactSection"]
    - path: "next/src/components/sections/ContactForm.tsx"
      provides: "Contact form with Directus integration (preserved)"
      exports: ["ContactForm"]
    - path: "next/src/components/sections/FinalCTA.tsx"
      provides: "Final CTA section with glass card and doctor image"
      exports: ["FinalCTA"]
    - path: "next/src/app/page.tsx"
      provides: "Index page with all sections in correct order"
  key_links:
    - from: "next/src/components/sections/ContactForm.tsx"
      to: "next/src/lib/db/actions.ts"
      via: "import { submitContactForm } from '@/lib/db/actions'"
      pattern: "submitContactForm"
    - from: "next/src/app/page.tsx"
      to: "next/src/components/sections/ReviewsSection.tsx"
      via: "import { ReviewsSection }"
      pattern: "ReviewsSection"
    - from: "next/src/app/page.tsx"
      to: "next/src/components/sections/FAQSection.tsx"
      via: "import { FAQSection }"
      pattern: "FAQSection"
---

<objective>
Rewrite ContactSection, ContactForm, and FinalCTA to match the new design from d450232, wire ReviewsSection and FAQSection into page.tsx.

Purpose: Complete the conversion funnel with the new glass design language while preserving the working Directus form submission.
Output: All interactive sections styled per new design, page.tsx updated with full section order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-index-interactive-sections/71-01-SUMMARY.md

Source of truth: `git show d450232:index.html` -- sections #contact and #cta.

<interfaces>
<!-- CRITICAL: ContactForm.tsx Directus integration that MUST be preserved -->
From next/src/lib/db/actions.ts:
```tsx
export async function submitContactForm(formData: {
  name: string;
  phone: string;
  specialization: string;
  description: string;
  honeypot: string;
  loadTime: number;
}): Promise<{ success: boolean; errors?: Record<string, string> }>;
```

From next/src/components/sections/ContactForm.tsx (PRESERVE all logic):
```tsx
// 'use client' component
// State: formState ('idle'|'submitting'|'success'), errors, formError, loadTimeRef
// formatPhone function
// validate function
// handleSubmit with honeypot check, timing check, validation, submitContactForm call
// Success state rendering
// Form with: name, phone, interest (select), description, honeypot, submit button
```

From next/src/lib/navigation.ts:
```tsx
export const PHONE_NUMBER = '+77015322478';
export const PHONE_DISPLAY = '+7\u00A0701\u00A0532\u00A024\u00A078';
export const EMAIL = 'kz@medicusunion.com';
```

<!-- Glass tokens available: -->
<!-- shadow-glass-sm, shadow-glass, shadow-glass-lg, shadow-glass-inner, shadow-glass-inner-strong -->
<!-- border-glass-border, border-glass-border-strong -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ContactSection + ContactForm with new glass design</name>
  <files>next/src/components/sections/ContactSection.tsx, next/src/components/sections/ContactForm.tsx</files>
  <read_first>
    - next/src/components/sections/ContactSection.tsx (current implementation)
    - next/src/components/sections/ContactForm.tsx (current implementation -- PRESERVE all logic)
    - next/src/lib/db/actions.ts (server action interface)
    - `git show d450232:index.html` section #contact (exact classes and structure)
  </read_first>
  <action>
**ContactSection.tsx** -- Rewrite as server component. Port exact HTML from d450232 `#contact`.

Section wrapper: `<section className="py-16 relative z-10" id="contact">`
Container: `<div className="container mx-auto px-4 lg:px-6 relative z-10">`
Grid: `<div className="grid lg:grid-cols-2 gap-12 items-start">`

**Left column** (flex flex-col h-full):
1. Gradient heading:
   ```
   <h2 className="text-5xl md:text-6xl font-extrabold mb-6">
     <span className="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent">
       Обсудите ваш случай{'\u00A0'}{'\u2014'} бесплатно и{'\u00A0'}без{'\u00A0'}обязательств
     </span>
   </h2>
   ```

2. Description paragraph: `text-mu-text-700 font-medium text-lg mb-8 leading-relaxed max-w-lg`
   Text: "Оставьте заявку{'\u00A0'}{'\u2014'} мы{'\u00A0'}свяжемся в{'\u00A0'}течение 24{'\u00A0'}часов..."

3. Coordinator card (mt-auto): `bg-white/60 backdrop-blur-2xl rounded-[2.5rem] p-6 border border-white/60 flex flex-col sm:flex-row gap-6 items-center shadow-glass`
   - Photo placeholder: `w-32 h-32 rounded-full overflow-hidden flex-shrink-0 border-4 border-white/60 bg-white/50 backdrop-blur-sm shadow-glass-sm`
     Use Next.js Image component with the coordinator Unsplash URL from d450232: `https://images.unsplash.com/photo-1559839734-2b71ea197ec2?...&w=1080`
     alt="Медицинский координатор", width={256}, height={256}, className="w-full h-full object-cover"
   - Info: Name "Айгерим", title "Старший медицинский координатор" in `text-mu-blue font-bold text-sm mb-4`
   - Phone link: `<a href="tel:+77015322478">` with phone icon in glass circle `w-8 h-8 rounded-full bg-white/60 backdrop-blur-md flex items-center justify-center border border-white/60 shadow-glass-inner-strong`
   - Email link: `<a href="mailto:kz@medicusunion.com">` with envelope icon in same glass circle style

4. Trust badges (mt-8, flex flex-wrap gap-4): Two glass pills:
   `inline-flex items-center gap-2 bg-white/40 backdrop-blur-xl border border-white/60 text-mu-text-900 px-5 py-2.5 rounded-full text-sm font-bold shadow-sm shadow-glass-inner`
   Items: "На связи 24/7" and "ISO 27001" -- each with green checkmark SVG (stroke="var(--mu-green-600)")

**Right column**:
Glass form card wrapper: `bg-white/60 backdrop-blur-3xl rounded-[3rem] p-8 border border-white/60 relative overflow-hidden shadow-glass-lg`
Inside: `<ContactForm />`

Import ContactForm and PHONE_NUMBER, PHONE_DISPLAY, EMAIL from navigation.ts.

**ContactForm.tsx** -- PRESERVE ALL EXISTING LOGIC. Only change the CSS classes.

CRITICAL: Keep these EXACTLY as they are:
- 'use client' directive
- import { submitContactForm } from '@/lib/db/actions'
- formatPhone function
- All useState hooks (formState, errors, formError, loadTimeRef, name, phone, interest, description, website)
- validate() function
- handleSubmit() function (honeypot, timing, validation, submitContactForm call, error handling)
- Success state JSX

ONLY change the Tailwind classes to match d450232 glass form inputs:

Input base class (replace current inputBase):
`w-full px-5 py-4 rounded-2xl border border-white/40 bg-white/50 backdrop-blur-md focus:bg-white/70 focus:border-mu-blue focus:ring-4 focus:ring-mu-blue/20 outline-none transition-all placeholder:text-mu-text-500 font-medium text-mu-text-900 shadow-glass-inner`

Error input: same but with `border-red-400` instead of `border-white/40`

Label class: `block text-sm font-bold text-mu-text-900 mb-2`

Submit button:
`w-full bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white py-4 rounded-2xl font-bold shadow-[0_16px_32px_color-mix(in_oklch,var(--color-mu-blue)_30%,transparent)] shadow-glass-inner hover:shadow-[0_20px_40px_color-mix(in_oklch,var(--color-mu-blue)_40%,transparent)] transition-all flex items-center justify-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed text-lg mt-8`

Add send icon SVG after button text: `<svg><path d="m22 2-7 20-4-9-9-4Z"/><path d="M22 2 11 13"/></svg>`

Form spacing: `space-y-6` (increase from current gap-5)
Bottom text: `text-sm text-mu-text-700 font-medium text-center mt-4`

Success state: update to glass styling:
- Glass circle: `w-24 h-24 bg-white/80 border border-white/60 rounded-full flex items-center justify-center mb-6 shadow-glass-sm`
- Title: `text-3xl font-extrabold`
- Keep absolute positioning within parent: add to success div `absolute inset-0 bg-white/80 backdrop-blur-3xl z-20 flex flex-col items-center justify-center p-8`

DO NOT change: state variables, event handlers, form submission flow, validation logic, honeypot field, or the submitContactForm import.
  </action>
  <acceptance_criteria>
    - ContactSection uses lg:grid-cols-2 layout with coordinator card on left
    - Coordinator card has glass styling (bg-white/60 backdrop-blur-2xl rounded-[2.5rem])
    - Trust badges render as glass pills
    - ContactForm STILL calls submitContactForm from @/lib/db/actions
    - ContactForm STILL has formatPhone, validate, handleSubmit, honeypot, timing check
    - Form inputs use glass classes: bg-white/50 backdrop-blur-md shadow-glass-inner
    - Submit button has gradient and send icon
    - Success state renders with glass circle
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ContactSection and ContactForm rewritten with d450232 glass design. All form submission logic preserved. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite FinalCTA with glass card and wire page.tsx</name>
  <files>next/src/components/sections/FinalCTA.tsx, next/src/app/page.tsx, next/public/cta-doctor.webp</files>
  <read_first>
    - next/src/components/sections/FinalCTA.tsx (current implementation)
    - next/src/app/page.tsx (current section order and imports)
    - `git show d450232:index.html` section #cta (exact classes and structure)
  </read_first>
  <action>
**Step 1: Download CTA doctor image**

Download the Unsplash image used in d450232 #cta right column and save as WebP:
```bash
curl -L "https://images.unsplash.com/photo-1758691463393-a2aa9900af8a?crop=entropy&cs=tinysrgb&fit=max&fm=webp&w=1080&q=80" -o next/public/cta-doctor.webp
```

**Step 2: Rewrite FinalCTA.tsx**

Import: `import Image from 'next/image'` and `import { PHONE_NUMBER, EMAIL } from '@/lib/navigation'`

Section wrapper: `<section className="py-16 relative overflow-hidden z-10" id="cta">`
Container: `<div className="container mx-auto px-4 lg:px-6 relative z-10">`

Outer glass card:
`bg-white/60 backdrop-blur-3xl rounded-[3.5rem] overflow-hidden relative shadow-glass-lg border border-glass-border-strong`

Grid inside: `grid lg:grid-cols-2`

**Left column** (p-12 lg:p-20 flex flex-col justify-center relative z-10):
- Decorative blur blob: `<div className="absolute top-0 left-0 w-96 h-96 bg-mu-blue/30 rounded-full blur-[100px] -z-10 mix-blend-multiply" aria-hidden="true" />`
- Heading: `text-4xl md:text-5xl lg:text-6xl font-extrabold text-mu-text-900 mb-6 leading-tight drop-shadow-sm`
  Text: "Не{'\u00A0'}откладывайте решение о{'\u00A0'}здоровье"
- Paragraph: `text-xl text-mu-text-700 font-medium mb-10 leading-relaxed max-w-lg`
  Text: "Мнение зарубежного врача, комплексное обследование или лечение в{'\u00A0'}мировой клинике{'\u00A0'}{'\u2014'} начните с{'\u00A0'}бесплатной консультации."
- CTA buttons row: `flex flex-col sm:flex-row items-center gap-4`
  1. Primary: `w-full sm:w-auto bg-gradient-to-r from-mu-blue to-mu-accent-blue text-white px-8 py-4 rounded-3xl font-bold shadow-lg shadow-mu-blue/30 shadow-glass-inner hover:shadow-xl hover:shadow-mu-blue/40 transition-all flex items-center justify-center gap-2 group text-lg`
     Text: "Обсудить мой случай" + arrow SVG with `group-hover:translate-x-1 transition-transform`
     href="#contact"
  2. Secondary: `w-full sm:w-auto bg-white/60 backdrop-blur-xl text-mu-text-900 px-8 py-4 rounded-3xl font-bold border border-glass-border hover:bg-white/80 transition-all flex items-center justify-center gap-2 shadow-glass-sm shadow-glass-inner-strong text-lg`
     Text: phone icon SVG (stroke="var(--mu-blue)") + "Позвонить"
     href={`tel:${PHONE_NUMBER}`}

- Contact links paragraph (mt-8): `text-mu-text-700 font-medium`
  "Или напишите нам: " + email link + WhatsApp link + Telegram link
  Links: `text-mu-blue hover:underline`

**Right column** (image, hidden on mobile):
`relative h-[400px] lg:h-auto overflow-hidden hidden md:block`
- Next.js Image: src="/cta-doctor.webp", alt="Врач MedicusUnion", fill, className="object-cover object-center", sizes="(max-width: 1024px) 50vw, 540px"
- Gradient overlay: `absolute inset-0 bg-gradient-to-r from-white/60 to-transparent w-1/3`

**Step 3: Update page.tsx**

Add two new imports:
```tsx
import { ReviewsSection } from '@/components/sections/ReviewsSection';
import { FAQSection } from '@/components/sections/FAQSection';
```

Insert into the JSX between PlatformSection and ContactSection, wrapped in ScrollReveal:
```tsx
<ScrollReveal delay={0.05}>
  <ReviewsSection />
</ScrollReveal>
<ScrollReveal delay={0.05}>
  <FAQSection />
</ScrollReveal>
```

Final section order in page.tsx:
1. HeroHub
2. StatsBar
3. ServicesGrid
4. ProblemSection (ScrollReveal)
5. ProcessSection (ScrollReveal)
6. WhyUsSection (ScrollReveal)
7. ClinicsSection (ScrollReveal)
8. PlatformSection (ScrollReveal)
9. ReviewsSection (ScrollReveal) -- NEW
10. FAQSection (ScrollReveal) -- NEW
11. ContactSection (ScrollReveal)
12. FinalCTA (ScrollReveal)
  </action>
  <acceptance_criteria>
    - FinalCTA renders as glass card with lg:grid-cols-2 layout
    - Left column has heading, paragraph, two CTA buttons, contact links
    - Right column shows doctor image (hidden on mobile, visible md+)
    - cta-doctor.webp downloaded to next/public/
    - page.tsx imports ReviewsSection and FAQSection
    - Section order is correct: ...Platform, Reviews, FAQ, Contact, FinalCTA
    - All ScrollReveal wrappers present
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>FinalCTA rewritten with glass card design and doctor image. page.tsx updated with all 12 sections in correct order. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server action | ContactForm submits user data through submitContactForm server action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02 | S (Spoofing) | ContactForm | mitigate | Existing honeypot + timing check preserved from prior implementation |
| T-71-03 | T (Tampering) | ContactForm | mitigate | Server-side Zod validation in submitContactForm (preserved, not modified) |
| T-71-04 | D (DoS) | ContactForm | accept | Single form submission, rate limiting deferred to infrastructure |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors
- ContactForm still imports and calls submitContactForm
- page.tsx renders all 12 sections in correct order
- FinalCTA has doctor image visible on desktop
</verification>

<success_criteria>
- ContactSection + ContactForm restyled with glass design from d450232
- ContactForm Directus submission logic fully preserved (submitContactForm, validate, honeypot, timing)
- FinalCTA renders as 2-column glass card with image and CTAs
- page.tsx has ReviewsSection and FAQSection in correct position
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/71-index-interactive-sections/71-02-SUMMARY.md`
</output>
