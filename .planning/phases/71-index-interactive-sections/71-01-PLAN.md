---
phase: 71-index-interactive-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/components/sections/ReviewsSection.tsx
  - next/src/components/sections/FAQSection.tsx
autonomous: true
requirements:
  - SEC-08
  - SEC-09

must_haves:
  truths:
    - "ReviewsSection displays 4 patient testimonial cards in a 2-column grid with glass styling"
    - "FAQSection displays 7 FAQ items as an accordion with expand/collapse on click"
    - "FAQ allows only one item open at a time, with smooth max-height animation"
    - "FAQSection is a 'use client' component for accordion interactivity"
  artifacts:
    - path: "next/src/components/sections/ReviewsSection.tsx"
      provides: "Patient testimonial cards with glass styling"
      exports: ["ReviewsSection"]
    - path: "next/src/components/sections/FAQSection.tsx"
      provides: "FAQ accordion with 7 questions"
      exports: ["FAQSection"]
  key_links:
    - from: "next/src/components/sections/FAQSection.tsx"
      to: "React useState"
      via: "'use client' directive + useState for open item tracking"
      pattern: "useState.*openIndex"
---

<objective>
Create ReviewsSection and FAQSection as new components matching the feat/new-design source HTML at commit d450232.

Purpose: These two social-proof and objection-handling sections complete the conversion funnel above the contact form.
Output: Two new section components ready for page.tsx import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@next/src/components/sections/ProblemSection.tsx (established glass card pattern to follow)
@next/src/app/globals.css (glass token definitions: shadow-glass, glass-border, etc.)

Source of truth: `git show d450232:index.html` -- sections #reviews and #faq.

<interfaces>
<!-- Tailwind v4 glass tokens available in globals.css @theme inline: -->
<!-- shadow-glass-sm, shadow-glass, shadow-glass-lg, shadow-glass-inner, shadow-glass-inner-strong -->
<!-- border-glass-border (rgba(255,255,255,0.6)), border-glass-border-strong (rgba(255,255,255,0.8)) -->
<!-- All mu-* colors mapped in @theme inline -->

From next/src/components/sections/ProblemSection.tsx (glass card pattern):
```tsx
// Glass card outer: bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border
// Hover: hover:border-glass-border-strong hover:shadow-glass-lg transition-all duration-500
// Icon container: w-14 h-14 {bg} backdrop-blur-xl rounded-2xl flex items-center justify-center shadow-glass-sm border border-glass-border
// Section wrapper: <section className="py-16 relative z-10" id="...">
// Container: <div className="container mx-auto px-4 lg:px-6 relative z-10">
// Gradient heading: bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReviewsSection with 4 glass review cards</name>
  <files>next/src/components/sections/ReviewsSection.tsx</files>
  <read_first>
    - `git show d450232:index.html` lines around `id="reviews"` (section #reviews, 4 review cards)
    - next/src/components/sections/ProblemSection.tsx (glass card pattern reference)
  </read_first>
  <action>
Create `next/src/components/sections/ReviewsSection.tsx` as a server component (no 'use client' needed -- pure static content).

Port the exact HTML structure from d450232 `#reviews` section. Key implementation details:

**Section wrapper:**
```
<section className="py-16 relative z-10" id="reviews">
  <div className="container mx-auto px-4 lg:px-6">
```

**Heading (gradient text):**
```
<h2 className="text-4xl md:text-5xl font-extrabold mb-6">
  <span className="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent">
    Пациенты, которые прошли этот путь
  </span>
</h2>
```
Center-aligned with `text-center mb-12 max-w-3xl mx-auto` wrapper.

**Review cards** -- 4 cards in `grid md:grid-cols-2 gap-8 max-w-5xl mx-auto`:

Each card: `bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border p-8 hover:shadow-glass-lg hover:border-glass-border-strong transition-all duration-300`

Each card has:
1. Avatar circle (initials, gradient background): `w-12 h-12 bg-gradient-to-br from-{color1} to-{color2} rounded-full flex items-center justify-center text-white font-bold text-lg`
2. Name + context: `font-bold text-mu-text-900` and `text-mu-text-700 text-sm font-medium`
3. Quote text: `text-mu-text-700 font-medium leading-relaxed`

**Review data (from d450232):**
1. Ренат, Казахстан -- "Лечение в Австрии, Rudolfinerhaus" -- gradient: from-mu-blue to-mu-accent-blue, initial: "Р"
   Quote: use \u00AB \u00BB for quotes, \u00A0 for nbsp bindings per project rules (bind subject+verb pairs)
2. Жанна, Россия -- "Лечение за рубежом" -- gradient: from-mu-accent-teal to-mu-green-600, initial: "Ж"
3. Андрей, Украина -- "Регулярное обращение" -- gradient: from-mu-accent-orange to-mu-accent-red, initial: "А"
4. Арина, Россия -- "Обследование в Вене" -- gradient: from-mu-green-500 to-mu-green-600, initial: "А"

Use a typed const array for review data and .map() for rendering, same pattern as ProblemSection.

Apply nbsp (\u00A0) bindings between subject+verb pairs per project conventions.
  </action>
  <acceptance_criteria>
    - ReviewsSection exported as named export
    - 4 review cards render in 2-column grid on md+, single column on mobile
    - Glass card classes match d450232 exactly: bg-white/60 backdrop-blur-2xl rounded-[3rem] shadow-glass border border-glass-border
    - Each card has colored initial avatar with correct gradient per source
    - All review text matches d450232 content with proper nbsp bindings
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ReviewsSection.tsx compiles without errors. Exports ReviewsSection. Contains 4 review cards with glass styling matching d450232.</done>
</task>

<task type="auto">
  <name>Task 2: Create FAQSection as client component with accordion</name>
  <files>next/src/components/sections/FAQSection.tsx</files>
  <read_first>
    - `git show d450232:index.html` lines around `id="faq"` (section #faq, 7 FAQ items)
    - `git show d450232:js/main.js` (initAccordion function for behavior reference)
  </read_first>
  <action>
Create `next/src/components/sections/FAQSection.tsx` as a CLIENT component ('use client' directive at top).

Port the exact HTML structure from d450232 `#faq` section. Implement accordion behavior matching d450232 main.js `initAccordion`.

**Section wrapper:**
```
<section className="py-16 relative z-10" id="faq">
  <div className="container mx-auto px-4 lg:px-6">
```

**Heading:**
```
<h2 className="text-4xl md:text-5xl font-extrabold text-mu-text-900 mb-8 text-center">
  <span className="bg-gradient-to-r from-mu-blue via-mu-accent-blue to-mu-green-600 bg-clip-text text-transparent">
    Частые вопросы
  </span>
</h2>
```

**FAQ list** -- `max-w-3xl mx-auto space-y-4`:

**Each FAQ item card:**
```
bg-white/60 backdrop-blur-2xl rounded-2xl border border-glass-border shadow-glass-sm overflow-hidden
```

**Question button (always visible):**
```
w-full text-left px-6 py-5 font-bold text-mu-text-900 flex items-center justify-between transition-colors hover:bg-white/80
```
With a chevron SVG icon that rotates when open: `<svg> <path d="m6 9 6 6 6-6"/> </svg>` -- add `transition-transform duration-300` and `rotate-180` when item is open.

**Answer div:**
Use CSS max-height transition for smooth expand/collapse:
- Closed: `max-h-0 overflow-hidden transition-[max-height] duration-300 ease-in-out`
- Open: `max-h-[500px]`
Inner content: `px-6 pb-5 text-mu-text-700 font-medium leading-relaxed`

**Accordion behavior (useState):**
- `const [openIndex, setOpenIndex] = useState<number | null>(null)`
- Click toggles: if same index, close (null). If different, open new (close previous).
- Only one item open at a time (matches d450232 main.js behavior).
- Set `aria-expanded="true"/"false"` on button.

**7 FAQ items (from d450232):**
1. "Что именно делает MedicusUnion?" -- answer about international platform
2. "С какими заболеваниями можно обратиться?" -- answer about specializations
3. "Сколько это стоит?" -- answer about pricing
4. "А если я не говорю на иностранном языке?" -- answer about translation
5. "Чем вы отличаетесь от агентств из Instagram?" -- answer about legal entity
6. "Могу ли я обратиться в клинику напрямую?" -- answer about benefits
7. "Что будет, если чек-ап или консультация выявят проблему?" -- answer about treatment continuity

Store FAQ data as a typed const array `FAQ_ITEMS: { question: string; answer: React.ReactNode }[]` and .map() for rendering.

Apply \u00A0 bindings in answers per project conventions (subject+verb pairs). Use \u00AB\u00BB for Russian quotes, \u2014 for em-dash.
  </action>
  <acceptance_criteria>
    - FAQSection has 'use client' directive at top
    - FAQSection exported as named export
    - 7 FAQ items render with glass card styling from d450232
    - Clicking a question expands its answer and collapses any other open answer
    - Smooth CSS transition on expand/collapse via max-height
    - Chevron icon rotates 180deg when item is open
    - aria-expanded attribute toggles correctly
    - All 7 Q&A texts match d450232 content with proper nbsp bindings
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing/next && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>FAQSection.tsx compiles without errors. Is a 'use client' component. Exports FAQSection. Contains 7 accordion items with glass styling and smooth expand/collapse behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI components with no data input or API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | I (Information Disclosure) | ReviewsSection | accept | Public testimonial data, no PII beyond first names already on public site |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors for both new components
- ReviewsSection renders 4 cards with glass styling
- FAQSection accordion opens/closes with one item at a time
</verification>

<success_criteria>
- Two new section components exist and compile
- Glass styling matches d450232 source exactly
- FAQ accordion is interactive (client component) with smooth animation
- Both components are ready for page.tsx import in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/71-index-interactive-sections/71-01-SUMMARY.md`
</output>
