---
phase: 30-seo-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - online-consultations.html
  - treatment-abroad.html
  - checkup.html
  - contacts.html
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "Each of the 5 existing pages has a unique <title> sourced from copywriting documents"
    - "Each of the 5 existing pages has a unique <meta name=description> with page-specific text"
    - "Each page has og:title, og:description, og:url, og:type, og:image with page-specific values"
    - "Each page has a <link rel=canonical> pointing to its absolute URL on medicusunion.kz"
    - "index.html contains a JSON-LD script with Schema.org MedicalBusiness structured data"
  artifacts:
    - path: "index.html"
      provides: "Main page with complete SEO meta + JSON-LD Schema.org"
      contains: "application/ld+json"
    - path: "online-consultations.html"
      provides: "Consultations page with complete OG tags including og:image"
      contains: "og:image"
    - path: "treatment-abroad.html"
      provides: "Treatment page with complete OG tags including og:image"
      contains: "og:image"
    - path: "checkup.html"
      provides: "Checkup page with complete OG tags including og:image"
      contains: "og:image"
    - path: "contacts.html"
      provides: "Contacts page with fixed canonical + OG tags including og:image"
      contains: "og:image"
  key_links:
    - from: "index.html"
      to: "Schema.org"
      via: "JSON-LD script in head"
      pattern: "application/ld\\+json"
    - from: "all pages"
      to: "og:image"
      via: "meta property tag"
      pattern: 'og:image.*medicusunion'
---

<objective>
Add missing og:image meta tags to all 5 pages, fix contacts.html canonical URL consistency, and add Schema.org JSON-LD structured data to the main page.

Purpose: Complete SEO optimization so every page is fully discoverable by search engines and renders rich previews when shared on social media.
Output: All 5 HTML files updated with complete, unique SEO meta; index.html additionally contains MedicalBusiness JSON-LD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Current state analysis (no need to re-read files, use this):

**index.html** (lines 3-14): Has title, description, og:type, og:locale, og:title, og:description, og:url, og:site_name, canonical. MISSING: og:image. MISSING: JSON-LD Schema.org.

**online-consultations.html** (lines 3-17): Has title, description, og:type, og:locale, og:title, og:description, og:url, og:site_name, twitter:card, twitter:title, twitter:description, canonical. MISSING: og:image.

**treatment-abroad.html** (lines 3-19): Has title, description, og:type, og:locale, og:title, og:description, og:url, og:site_name, canonical. MISSING: og:image.

**checkup.html** (lines 3-14): Has title, description, og:type, og:locale, og:title, og:description, og:url, og:site_name, canonical. MISSING: og:image.

**contacts.html** (lines 3-14): Has title, description, og:type, og:locale, og:title, og:description, og:url, og:site_name, canonical. ISSUES: og:url uses "contacts.html" (inconsistent with other pages that use clean URLs). canonical uses "contacts.html" (same issue). MISSING: og:image.

SEO meta values from copywriting documents (already applied to title/description in prior phases):
- index.html title: "MedicusUnion -- онлайн-консультации, лечение и чек-апы за рубежом | Казахстан" (ALREADY SET)
- index.html description: "Австрийская медицинская платформа..." (ALREADY SET)
- treatment-abroad.html title: "Лечение за границей из Казахстана -- организация под ключ | MedicusUnion" (ALREADY SET)
- treatment-abroad.html description: "Организуем лечение за рубежом..." (ALREADY SET)
- checkup.html title: "Чек-ап за рубежом -- Samsung Medical Center, Severance Hospital, клиники Стамбула | MedicusUnion" (ALREADY SET)
- checkup.html description: "Комплексное обследование за 1--2 дня..." (ALREADY SET)
- online-consultations.html: title/description already set (from Phase 26, not from a .md copywriting doc but matches page content)
- contacts.html: title/description already derived and set
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add og:image to all 5 pages and fix contacts.html URLs</name>
  <files>index.html, online-consultations.html, treatment-abroad.html, checkup.html, contacts.html</files>
  <action>
For each of the 5 HTML files, add the missing og:image meta tag in the head section, immediately after the existing og:site_name tag.

Use a generic OG image URL that represents the MedicusUnion brand. Since no page-specific OG images exist yet, use the same branded image for all pages:

```html
<meta property="og:image" content="https://medicusunion.kz/images/og-cover.jpg">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:image:alt" content="MedicusUnion -- международная медицинская платформа">
```

NOTE: The actual og-cover.jpg image file creation is a Phase 31 (Performance) concern. The meta tags should reference the planned URL now so the markup is correct.

**contacts.html specific fixes (SEO-04):**
- Change `<meta property="og:url" content="https://medicusunion.kz/contacts.html">` to `<meta property="og:url" content="https://medicusunion.kz/contacts">`
- Change `<link rel="canonical" href="https://medicusunion.kz/contacts.html">` to `<link rel="canonical" href="https://medicusunion.kz/contacts">`

This makes contacts.html consistent with how all other pages use clean URLs without the .html extension.

**Verify existing title/description uniqueness (SEO-01, SEO-02):**
After edits, confirm each page has a unique title and description. No changes needed to titles/descriptions -- they are already unique and sourced from copywriting docs.
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && echo "=== og:image check ===" && for f in index.html online-consultations.html treatment-abroad.html checkup.html contacts.html; do echo -n "$f: "; grep -c 'og:image' "$f"; done && echo "=== contacts canonical check ===" && grep 'rel="canonical"' contacts.html | grep -v '.html"' && echo "=== contacts og:url check ===" && grep 'og:url' contacts.html | grep -v '.html"' && echo "=== unique titles check ===" && for f in index.html online-consultations.html treatment-abroad.html checkup.html contacts.html; do echo -n "$f: "; grep '<title>' "$f"; done</automated>
  </verify>
  <done>
- All 5 pages contain og:image meta tag pointing to https://medicusunion.kz/images/og-cover.jpg
- All 5 pages have og:image:width (1200) and og:image:height (630) tags
- contacts.html canonical URL is https://medicusunion.kz/contacts (no .html)
- contacts.html og:url is https://medicusunion.kz/contacts (no .html)
- All 5 pages have unique titles (no duplicates)
- All 5 pages have unique descriptions (no duplicates)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Schema.org JSON-LD structured data to index.html</name>
  <files>index.html</files>
  <action>
Add a JSON-LD script block at the end of the `<head>` section of index.html (before `</head>`), after the `<style>` block.

The JSON-LD must contain Schema.org structured data combining Organization and MedicalBusiness types. Use the following exact structure:

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "MedicalBusiness",
  "name": "MedicusUnion",
  "alternateName": "MedicusUnion KZ",
  "url": "https://medicusunion.kz",
  "logo": "https://medicusunion.kz/images/logo.png",
  "description": "Австрийская медицинская платформа с офисом в Казахстане. Онлайн-консультации с европейскими врачами, организация лечения за рубежом, чек-апы в ведущих клиниках мира.",
  "telephone": "+77015322478",
  "email": "kz@medicusunion.com",
  "address": [
    {
      "@type": "PostalAddress",
      "addressCountry": "KZ",
      "addressLocality": "Алматы",
      "addressRegion": "Алматы"
    },
    {
      "@type": "PostalAddress",
      "streetAddress": "Bruno-Marek-Allee 20/50",
      "postalCode": "1020",
      "addressLocality": "Wien",
      "addressCountry": "AT"
    }
  ],
  "parentOrganization": {
    "@type": "Organization",
    "name": "MedicusUnion GmbH",
    "url": "https://medicusunion.com",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "Bruno-Marek-Allee 20/50",
      "postalCode": "1020",
      "addressLocality": "Wien",
      "addressCountry": "AT"
    }
  },
  "sameAs": [
    "https://wa.me/77015322478"
  ],
  "areaServed": {
    "@type": "Country",
    "name": "Kazakhstan"
  },
  "medicalSpecialty": [
    "Oncology",
    "Cardiology",
    "Orthopedics",
    "Neurology",
    "Pediatrics"
  ],
  "hasOfferCatalog": {
    "@type": "OfferCatalog",
    "name": "Медицинские услуги",
    "itemListElement": [
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "MedicalProcedure",
          "name": "Онлайн-консультация с европейским врачом",
          "url": "https://medicusunion.kz/online-consultations"
        }
      },
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "MedicalProcedure",
          "name": "Организация лечения за рубежом",
          "url": "https://medicusunion.kz/treatment-abroad"
        }
      },
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "MedicalProcedure",
          "name": "Чек-ап за рубежом",
          "url": "https://medicusunion.kz/checkup"
        }
      }
    ]
  },
  "iso6523Code": "ISO 27001",
  "knowsAbout": ["Medical Tourism", "Telemedicine", "Health Checkups", "International Healthcare"]
}
</script>
```

Place this script AFTER the closing `</style>` tag and BEFORE `</head>`.

Data sources:
- Company name, addresses, phone, email: from footer content in MedicusUnion_Main_Page_Copy.md
- Medical specialties: from the FAQ and services listed across all pages
- Service URLs: consistent with canonical URLs already set on each page
  </action>
  <verify>
    <automated>cd /Users/mikhail/Projects/Medicus_video_consult-landing && echo "=== JSON-LD exists ===" && grep -c 'application/ld+json' index.html && echo "=== MedicalBusiness type ===" && grep -o '"@type": "MedicalBusiness"' index.html && echo "=== Has name ===" && grep -o '"name": "MedicusUnion"' index.html && echo "=== Has telephone ===" && grep -o '"telephone":' index.html && echo "=== Has address ===" && grep -c '"PostalAddress"' index.html && echo "=== Has services ===" && grep -c '"MedicalProcedure"' index.html</automated>
  </verify>
  <done>
- index.html contains a script tag with type="application/ld+json"
- JSON-LD @type is "MedicalBusiness"
- JSON-LD contains name "MedicusUnion", telephone, email, two addresses (KZ + AT)
- JSON-LD contains parentOrganization with "MedicusUnion GmbH"
- JSON-LD contains 3 MedicalProcedure offers linking to the 3 service pages
- JSON-LD contains medicalSpecialty array with at least 5 specialties
- JSON-LD is valid JSON (no syntax errors)
  </done>
</task>

</tasks>

<verification>
Run these checks to verify the entire plan is complete:

1. Every page has og:image:
```bash
for f in index.html online-consultations.html treatment-abroad.html checkup.html contacts.html; do
  count=$(grep -c 'og:image' "$f")
  echo "$f: $count og:image tags (expect 3+)"
done
```

2. All titles are unique:
```bash
for f in index.html online-consultations.html treatment-abroad.html checkup.html contacts.html; do
  grep -oP '(?<=<title>).*(?=</title>)' "$f"
done | sort | uniq -d | wc -l
# Expect: 0 (no duplicates)
```

3. All descriptions are unique:
```bash
for f in index.html online-consultations.html treatment-abroad.html checkup.html contacts.html; do
  grep -oP 'name="description" content="[^"]*"' "$f"
done | sort | uniq -d | wc -l
# Expect: 0 (no duplicates)
```

4. All canonicals point to medicusunion.kz:
```bash
grep -h 'rel="canonical"' index.html online-consultations.html treatment-abroad.html checkup.html contacts.html
# All should start with https://medicusunion.kz
```

5. JSON-LD is present and valid:
```bash
grep 'application/ld+json' index.html | wc -l
# Expect: 1
```
</verification>

<success_criteria>
- SEO-01: All 5 pages have unique title tags (already true, verified)
- SEO-02: All 5 pages have unique meta descriptions (already true, verified)
- SEO-03: All 5 pages have og:title, og:description, og:url, og:type, og:image with page-specific values
- SEO-04: All 5 pages have canonical URLs pointing to absolute medicusunion.kz URLs (contacts.html fixed)
- SEO-05: index.html contains JSON-LD with MedicalBusiness Schema.org structured data

NOTE: 404.html (the 6th page) does not exist yet -- it is created in Phase 29. When Phase 29 completes, 404.html should include standard meta tags (title: "Страница не найдена -- MedicusUnion", description, og tags, canonical). This is a Phase 29 responsibility documented in 404-02 requirement, not Phase 30.
</success_criteria>

<output>
After completion, create `.planning/phases/30-seo-optimization/30-01-SUMMARY.md`
</output>
