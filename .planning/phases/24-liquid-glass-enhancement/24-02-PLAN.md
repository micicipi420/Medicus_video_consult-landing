---
phase: 24-liquid-glass-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - css/styles.css
  - index.html
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-16
  - D-17

must_haves:
  truths:
    - "Sticky bar at bottom of viewport shows frosted glass effect on BOTH mobile and desktop"
    - "Lead form card on right column has frosted glass surface matching pricing card style"
    - "Social proof stat items show glass panels over dark teal background"
    - "Glass form card and social proof panels animate in with subtle scale on scroll reveal"
  artifacts:
    - path: "css/styles.css"
      provides: "Sticky bar glass, social proof glass panels, glass enter animation"
      contains: ".social-proof__item--glass"
    - path: "index.html"
      provides: "card--glass class on lead-form__wrapper, glass modifier on social proof items"
      contains: "lead-form__wrapper card--glass"
  key_links:
    - from: "index.html .lead-form__wrapper.card--glass"
      to: "css/styles.css .card--glass"
      via: "HTML class addition wires to existing glass modifier"
      pattern: "lead-form__wrapper card--glass"
    - from: "css/styles.css .sticky-bar glass rules"
      to: "var(--glass-blur), var(--glass-bg)"
      via: "token cascade from Plan 01 updated tokens"
      pattern: "sticky-bar.*backdrop-filter"
---

<objective>
Add glass treatment to three new elements and wire enter animations.

Apply liquid glass to sticky bar (both mobile and desktop per D-17), lead-form wrapper (via card--glass class), and social proof stat items (new glass modifier). Add glass-specific enter animation (scale 0.98 + opacity) for form and social proof elements. Wire HTML classes.

Purpose: Extends glass from 2 elements (Phase 22) to 5 elements, completing the iOS 26 liquid glass treatment across the full page.
Output: Updated css/styles.css with new glass rules and index.html with glass class wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-liquid-glass-enhancement/24-CONTEXT.md
@.planning/phases/24-liquid-glass-enhancement/24-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, glass tokens in :root will be: -->
--glass-bg:     rgba(255, 255, 255, 0.60);
--glass-border: rgba(255, 255, 255, 0.9);
--glass-blur:   blur(20px);
--glass-tint:   rgba(26, 198, 126, 0.05);

<!-- .card--glass modifier (updated in Plan 01): -->
.card--glass {
  background: linear-gradient(var(--glass-tint), var(--glass-tint)), var(--glass-bg);
  -webkit-backdrop-filter: var(--glass-blur);
  backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  border-top: 1px solid rgba(255, 255, 255, 0.75);
}

<!-- Current .sticky-bar CSS (lines 1553-1565): -->
.sticky-bar {
  position: fixed;
  bottom: 0; left: 0; right: 0;
  z-index: 100;
  background-color: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border-top: 1px solid rgba(24, 33, 44, 0.1);
  padding-block: var(--space-1);
  transition: transform var(--transition-normal);
}

<!-- Current .social-proof__item (lines 722-727): -->
.social-proof__item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.25rem;
}

<!-- Current .lead-form__wrapper (line 1169-1171): -->
.lead-form__wrapper { max-width: 540px; }

<!-- Current index.html lead-form wrapper (line 593): -->
<div class="lead-form__wrapper">

<!-- Current index.html social proof items (lines 160-170): -->
<div class="social-proof__item">
  <span class="social-proof__number">7</span>
  <span class="social-proof__label">...</span>
</div>

<!-- Existing animation system (lines 1655-1663): -->
.animate-on-scroll {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.4s ease-out, transform 0.4s ease-out;
}
.animate-on-scroll.is-visible {
  opacity: 1;
  transform: translateY(0);
}

<!-- CRITICAL: @media (min-width: 1024px) sticky-bar hidden (lines 1601-1605) — MUST BE REMOVED per D-17: -->
@media (min-width: 1024px) { .sticky-bar { display: none; } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glass to sticky bar, social proof items, and lead-form wrapper CSS</name>
  <read_first>
    - css/styles.css (full file — focus on lines 1553-1605 for .sticky-bar, lines 709-750 for .social-proof, lines 1169-1171 for .lead-form__wrapper, lines 411-432 for .card--glass and dark mode, lines 1655-1663 for .animate-on-scroll, lines 1716-1729 for reduced-motion)
  </read_first>
  <files>css/styles.css</files>
  <action>
**1. Update `.sticky-bar` to use glass tokens per D-06, D-17 (replace lines 1553-1565):**

Replace the entire `.sticky-bar` rule with:
```css
.sticky-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: linear-gradient(var(--glass-tint), var(--glass-tint)), var(--glass-bg);
  -webkit-backdrop-filter: var(--glass-blur) saturate(180%);
  backdrop-filter: var(--glass-blur) saturate(180%);
  border-top: 1px solid rgba(255, 255, 255, 0.75);
  padding-block: var(--space-1);
  transition: transform var(--transition-normal);
}
```
Key changes: uses `var(--glass-blur)` instead of hardcoded `blur(8px)`, adds glass tint, specular highlight on `border-top`, `saturate(180%)`.

**2. Add dark mode sticky bar override** immediately after the `.sticky-bar.is-hidden` rule (after line 1569):
```css
[data-theme="dark"] .sticky-bar {
  background: linear-gradient(var(--glass-tint), var(--glass-tint)), var(--glass-bg);
  border-top: 1px solid rgba(255, 255, 255, 0.15);
}
```
The dark mode glass tokens (--glass-bg, --glass-blur) cascade automatically. Only override specular highlight opacity and explicitly set background to use dark tint.

**3. Add `@supports not` fallback for `.sticky-bar`** after the dark mode override:
```css
@supports not (backdrop-filter: blur(1px)) {
  .sticky-bar {
    background: rgba(255, 255, 255, 0.95);
    border-top: 1px solid rgba(24, 33, 44, 0.1);
  }
}
```

**4. Add glass treatment to `.social-proof__item` per D-08.**

Add a new BEM modifier class `.social-proof__item--glass` immediately after the existing `.social-proof__item` rule (after line 727):
```css
.social-proof__item--glass {
  background: rgba(255, 255, 255, 0.10);
  -webkit-backdrop-filter: blur(16px) saturate(140%);
  backdrop-filter: blur(16px) saturate(140%);
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-top: 1px solid rgba(255, 255, 255, 0.35);
  border-radius: var(--radius-lg);
  padding: var(--space-3) var(--space-4);
}
```
Note: Social proof sits on dark teal (#0E7490) background, so glass uses lighter semi-transparent white (not the standard --glass-bg which assumes light background). blur(16px) slightly less than header for subtlety. Specular highlight at 0.35 (between light 0.75 and dark 0.15 — appropriate for teal bg).

**5. Add dark mode social proof glass** after the modifier:
```css
[data-theme="dark"] .social-proof__item--glass {
  background: rgba(15, 25, 35, 0.40);
  border: 1px solid rgba(255, 255, 255, 0.10);
  border-top: 1px solid rgba(255, 255, 255, 0.15);
}
```

**6. Add `@supports not` fallback for social proof glass:**
```css
@supports not (backdrop-filter: blur(1px)) {
  .social-proof__item--glass {
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
}
```

**7. Add glass enter animation per D-05.**

In the animations section (after the `.animate-on-scroll.is-visible` rule, around line 1664), add:
```css
/* Glass surface enter animation — scale(0.98) + opacity for liquid feel (Phase 24 D-05) */
.animate-on-scroll.animate-on-scroll--glass {
  transform: scale(0.98);
  opacity: 0;
  transition: opacity 200ms ease-out, transform 200ms ease-out;
}

.animate-on-scroll.animate-on-scroll--glass.is-visible {
  opacity: 1;
  transform: scale(1);
}
```
This is a BEM modifier on the existing `.animate-on-scroll` class. When both classes present, the glass-specific animation (scale instead of translateY, 200ms instead of 400ms) takes priority due to higher specificity from the doubled class.

**8. Add glass animation to `prefers-reduced-motion` block (inside the @media rule around line 1718):**

Add inside the existing `@media (prefers-reduced-motion: reduce)` block:
```css
.animate-on-scroll.animate-on-scroll--glass {
  opacity: 1;
  transform: none;
  transition: none;
}
```

**9. Style `.lead-form__wrapper` when it has `.card--glass`:**

The `.card--glass` modifier from Plan 01 handles glass visuals. But `.lead-form__wrapper` needs a small adjustment for padding and border-radius when used as a glass card. Add after the `.lead-form__wrapper` rule (line 1171):
```css
.lead-form__wrapper.card--glass {
  padding: var(--space-5);
  border-radius: var(--radius-lg);
}
```

**10. Remove desktop `display: none` on sticky bar per D-17 (user confirmed "everywhere"):**

Find the `@media (min-width: 1024px)` rule that hides the sticky bar (around lines 1601-1605):
```css
@media (min-width: 1024px) {
  .sticky-bar {
    display: none;
  }
}
```
**DELETE this entire rule block.** D-17 explicitly states sticky bar glass applies on both desktop and mobile. The user confirmed "everywhere" (everywhere). The sticky bar must be visible with glass treatment on all viewport sizes.
  </action>
  <verify>
    <automated>grep -n "sticky-bar" css/styles.css | grep -i "glass\|blur\|tint\|backdrop" | head -5 && grep -n "social-proof__item--glass" css/styles.css | head -5 && grep -n "animate-on-scroll--glass" css/styles.css | head -5 && grep -n "lead-form__wrapper.card--glass" css/styles.css | head -3 && grep -A2 "min-width: 1024px" css/styles.css | grep -c "display: none.*sticky"</automated>
  </verify>
  <acceptance_criteria>
    - `.sticky-bar` rule contains `backdrop-filter: var(--glass-blur) saturate(180%);`
    - `.sticky-bar` rule contains `background: linear-gradient(var(--glass-tint), var(--glass-tint)), var(--glass-bg);`
    - `.sticky-bar` rule contains `border-top: 1px solid rgba(255, 255, 255, 0.75);`
    - `[data-theme="dark"] .sticky-bar` rule exists with `border-top: 1px solid rgba(255, 255, 255, 0.15);`
    - `@supports not` fallback exists for `.sticky-bar`
    - `.social-proof__item--glass` rule exists with `backdrop-filter: blur(16px) saturate(140%);`
    - `.social-proof__item--glass` has `border-top: 1px solid rgba(255, 255, 255, 0.35);`
    - `.social-proof__item--glass` has `border-radius: var(--radius-lg);`
    - `[data-theme="dark"] .social-proof__item--glass` rule exists
    - `@supports not` fallback exists for `.social-proof__item--glass`
    - `.animate-on-scroll.animate-on-scroll--glass` rule exists with `transform: scale(0.98);` and `transition: opacity 200ms ease-out, transform 200ms ease-out;`
    - `.animate-on-scroll.animate-on-scroll--glass.is-visible` rule exists with `transform: scale(1);`
    - `@media (prefers-reduced-motion: reduce)` block contains `.animate-on-scroll.animate-on-scroll--glass` override
    - `.lead-form__wrapper.card--glass` rule exists with `padding: var(--space-5);` and `border-radius: var(--radius-lg);`
    - No hardcoded `blur(8px)` remaining in `.sticky-bar` (replaced by token)
    - The `@media (min-width: 1024px) { .sticky-bar { display: none; } }` rule is REMOVED — sticky bar visible on desktop per D-17
  </acceptance_criteria>
  <done>Sticky bar uses glass tokens and is visible on all viewports (D-17), social proof glass modifier defined, glass enter animation defined, lead-form wrapper glass padding set, all dark mode and fallback variants present, reduced-motion guard covers glass animation</done>
</task>

<task type="auto">
  <name>Task 2: Wire glass classes in HTML</name>
  <read_first>
    - index.html (focus on line 593 for lead-form__wrapper, lines 158-170 for social-proof items, line 771 for sticky-bar)
  </read_first>
  <files>index.html</files>
  <action>
**1. Add `card--glass` class to `.lead-form__wrapper` per D-07 (line 593):**

Change:
```html
<div class="lead-form__wrapper">
```
To:
```html
<div class="lead-form__wrapper card--glass animate-on-scroll animate-on-scroll--glass">
```
The `card--glass` class activates the glass modifier. `animate-on-scroll animate-on-scroll--glass` adds the glass-specific enter animation (scale 0.98 -> 1, 200ms).

**2. Add glass modifier + animation to all three `.social-proof__item` elements per D-08 (lines 160, 164, 168):**

Change each:
```html
<div class="social-proof__item">
```
To:
```html
<div class="social-proof__item social-proof__item--glass animate-on-scroll animate-on-scroll--glass">
```

There are exactly 3 instances on lines 160, 164, 168. All three must be updated.

**3. No HTML change needed for `.sticky-bar` (D-06).** The sticky bar glass is handled entirely in CSS on the existing `.sticky-bar` element. It already exists at line 771 and the CSS update from Task 1 applies glass directly to the base `.sticky-bar` class.
  </action>
  <verify>
    <automated>grep -n "lead-form__wrapper card--glass" index.html && grep -c "social-proof__item--glass" index.html && grep -c "animate-on-scroll--glass" index.html</automated>
  </verify>
  <acceptance_criteria>
    - Line containing `lead-form__wrapper` also contains `card--glass animate-on-scroll animate-on-scroll--glass`
    - `grep -c "social-proof__item--glass" index.html` returns exactly `3`
    - `grep -c "animate-on-scroll--glass" index.html` returns exactly `4` (1 form + 3 social proof)
    - No other HTML elements modified
    - `.sticky-bar` line in HTML unchanged (no class additions)
  </acceptance_criteria>
  <done>Lead-form wrapper has card--glass class and glass animation, all 3 social proof items have glass modifier and animation, sticky bar glass applied via CSS only</done>
</task>

</tasks>

<verification>
1. `grep -c "social-proof__item--glass" index.html` returns 3
2. `grep "lead-form__wrapper card--glass" index.html` matches 1 line
3. `grep -c "animate-on-scroll--glass" index.html` returns 4
4. `grep "backdrop-filter: none" css/styles.css` returns 0 matches (confirmed in Plan 01)
5. `grep "blur(8px)" css/styles.css` returns 0 matches (old sticky bar blur removed)
6. `grep -c "display: none" css/styles.css` — verify sticky bar display:none rule is gone
7. Open in browser — scroll to social proof section, see glass stat panels over teal
8. Open in browser — scroll to lead form, see frosted glass card on right column
9. Open in browser — check sticky bar visible on BOTH mobile and desktop viewports with frosted glass
</verification>

<success_criteria>
- Sticky bar uses glass tokens (blur 20px, green tint, specular highlight)
- Sticky bar is visible on both mobile and desktop viewports (D-17 display:none removed)
- Lead-form wrapper is a glass card via card--glass class
- Social proof stat items have glass panels over dark teal background
- Glass elements animate in with scale(0.98) + opacity, 200ms
- All new glass elements have @supports not fallbacks
- All new glass elements have dark mode variants
- Reduced-motion guard covers glass animation
- Total glass elements on page: 5 (header, pricing card, sticky bar, form wrapper, social proof items)
</success_criteria>

<output>
After completion, create `.planning/phases/24-liquid-glass-enhancement/24-02-SUMMARY.md`
</output>
