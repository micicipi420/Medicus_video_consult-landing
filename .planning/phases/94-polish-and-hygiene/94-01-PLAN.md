---
phase: 94-polish-and-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/src/components/sections/consultations/ConsultationDoctors.tsx
  - next/src/components/sections/treatment/TreatmentClinics.tsx
  - next/tests/visual/console-errors.spec.ts
autonomous: true
requirements: [POL-01]
must_haves:
  truths:
    - "Visiting /consultations produces 0 `<rect> attribute rx` console errors"
    - "Visiting /treatment-abroad produces 0 `<rect> attribute rx` console errors"
    - "Country flag stripes still render visibly (rounding may be uniform instead of asymmetric — acceptable)"
    - "No other console errors are introduced"
  artifacts:
    - path: "next/src/components/sections/consultations/ConsultationDoctors.tsx"
      provides: "Doctor cards with country flag SVGs (rx values fixed)"
      contains: "rx=\"3\""
    - path: "next/src/components/sections/treatment/TreatmentClinics.tsx"
      provides: "Country clinic cards with flag SVGs (rx values fixed)"
      contains: "rx=\"3\""
    - path: "next/tests/visual/console-errors.spec.ts"
      provides: "Playwright spec asserting 0 SVG rx errors on /consultations and /treatment-abroad"
      exports: ["test descriptions for both routes"]
  key_links:
    - from: "next/tests/visual/console-errors.spec.ts"
      to: "next dev server"
      via: "page.on('console') listener filtering for 'attribute rx'"
      pattern: "attribute rx"
---

<objective>
Replace 7 invalid 4-value SVG `rx` attributes (e.g. `rx="0 0 3 3"`, `rx="3 0 0 3"`, `rx="3 3 0 0"`) on country-flag stripes with a valid single-length value (`rx="3"`). Wire a Playwright spec that asserts 0 `<rect> attribute rx` console errors on `/consultations` and `/treatment-abroad`. Closes POL-01.

Purpose: Eliminates pre-existing console noise that has shipped since 2026-04-12 (commit 6b6a3dde) — surfaced during Phase 93 UAT (F1). Establishes a clean console baseline ahead of Phase 95 audits, and silences the bug for Phase 95's axe-core / Lighthouse runs.

Output: 7 attribute edits across 2 TSX files, plus one new Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/flag-svg-rx-invalid.md
@.planning/phases/93-per-page-propagation-sub-routes/93-UAT.md
@next/src/components/sections/consultations/ConsultationDoctors.tsx
@next/src/components/sections/treatment/TreatmentClinics.tsx
@next/playwright.config.ts
@next/tests/visual/baseline.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace invalid rx attributes with single-length value rx="3"</name>
  <files>next/src/components/sections/consultations/ConsultationDoctors.tsx, next/src/components/sections/treatment/TreatmentClinics.tsx</files>
  <read_first>.planning/todos/pending/flag-svg-rx-invalid.md</read_first>
  <action>Default fix is option (1) from `flag-svg-rx-invalid.md` — replace every 4-value rx attribute with `rx="3"`. The 4-value form was an attempt to round only specific corners (CSS `border-radius` shorthand) but SVG `<rect>` `rx` accepts a single length only; browsers reject the value and render with no rounding. Single-value `rx="3"` produces uniform 3px rounding on all four corners, which is acceptable per todo guidance ("Recommend (1) for cost; (2) only if visual review insists on asymmetric rounding") and per user's autonomous-decisions delegation in MEMORY.md.

Make exactly 7 edits:

In `next/src/components/sections/consultations/ConsultationDoctors.tsx`:
- Line 50: `rx="0 0 3 3"` → `rx="3"` (Германия flag bottom yellow stripe)
- Line 104: `rx="0 0 3 3"` → `rx="3"` (ОАЭ flag bottom black stripe)
- Line 105: `rx="3 0 0 3"` → `rx="3"` (ОАЭ flag left red bar)

In `next/src/components/sections/treatment/TreatmentClinics.tsx`:
- Line 37: `rx="0 0 3 3"` → `rx="3"` (Германия flag bottom yellow stripe)
- Line 90: `rx="3 3 0 0"` → `rx="3"` (Индия flag top orange stripe)
- Line 91: `rx="0 0 3 3"` → `rx="3"` (Индия flag bottom green stripe)
- Line 112: `rx="3 0 0 3"` → `rx="3"` (ОАЭ flag left red bar)

Do NOT change the `rx="3"` values that already exist on outer flag rectangles (those are valid). Do NOT add path-based corner drawing (option 2 in the todo) — single-value `rx="3"` is the cheaper, autonomous-delegated fix.

Use Edit tool per occurrence. Do not modify any other attributes (`width`, `height`, `x`, `y`, `fill`).</action>
  <verify>
    <automated>cd next &amp;&amp; grep -nE 'rx="[0-9 ]+ [0-9]+' src/components/sections/consultations/ConsultationDoctors.tsx src/components/sections/treatment/TreatmentClinics.tsx ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'rx="[^"]+ ' next/src/components/sections/consultations/ConsultationDoctors.tsx` returns 0 lines (no whitespace inside any `rx` attribute value)
    - `grep -E 'rx="[^"]+ ' next/src/components/sections/treatment/TreatmentClinics.tsx` returns 0 lines
    - `grep -c 'rx="0 0 3 3"' next/src/components/sections/consultations/ConsultationDoctors.tsx next/src/components/sections/treatment/TreatmentClinics.tsx` returns 0 across both files
    - `grep -c 'rx="3 0 0 3"' next/src/components/sections/consultations/ConsultationDoctors.tsx next/src/components/sections/treatment/TreatmentClinics.tsx` returns 0 across both files
    - `grep -c 'rx="3 3 0 0"' next/src/components/sections/treatment/TreatmentClinics.tsx` returns 0
    - `pnpm --dir next lint` exits 0 (or whichever lint command the next package defines)
  </acceptance_criteria>
  <done>All 7 invalid rx attributes replaced with `rx="3"`; build/lint clean; visual diff at most one extra rounded outer corner per flag (acceptable per todo recommendation).</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright console-error spec for /consultations and /treatment-abroad</name>
  <files>next/tests/visual/console-errors.spec.ts</files>
  <read_first>next/tests/visual/baseline.spec.ts, next/playwright.config.ts</read_first>
  <action>Create a new Playwright test file `next/tests/visual/console-errors.spec.ts` that:

1. Imports `test, expect` from `@playwright/test`.
2. Defines `const routes = ['/consultations', '/treatment-abroad'] as const;`.
3. For each route, registers a test that:
   - Collects every console message via `page.on('console', ...)` BEFORE `page.goto`.
   - Navigates to the route, waits for `networkidle` (consistent with baseline.spec.ts).
   - Filters collected messages to entries where `msg.type() === 'error'` AND `msg.text()` includes `'attribute rx'`.
   - Asserts the filtered list length is exactly 0.
4. Also asserts that all collected `error`-type messages (regardless of content) is exactly 0 on both routes — POL-01 success criterion 1 says "0 `<rect> attribute rx` console errors" but a clean console baseline is the spirit of the polish phase. If a non-rx error pre-exists, this assertion will surface it for Phase 95 audit work; failure here is informative, not regressive.
5. Uses the same project structure as `baseline.spec.ts` — runs on the `desktop` Playwright project (config defines `desktop` 1280×800 and `mobile-375`); no need to run on mobile-375 since console errors are viewport-agnostic.

Do NOT add visual screenshot assertions in this spec (that's the baseline.spec.ts territory). Do NOT inject `addStyleTag` to hide the blob (irrelevant to console errors). Do NOT add `webServer` or PORT overrides — `playwright.config.ts` already handles this via `PLAYWRIGHT_PORT` env var.

After implementation, the spec must run via `cd next && pnpm exec playwright test tests/visual/console-errors.spec.ts --project=desktop` against a running dev server (port 3000 default; PLAYWRIGHT_PORT=3100 if 3000 is occupied per Phase 93 P07 precedent). It must PASS with the Task 1 fix landed, and FAIL if the rx attributes are reverted.

Do not commit screenshot artifacts — this spec produces none.</action>
  <verify>
    <automated>cd next &amp;&amp; pnpm exec tsc --noEmit tests/visual/console-errors.spec.ts &amp;&amp; pnpm exec playwright test tests/visual/console-errors.spec.ts --project=desktop --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - File `next/tests/visual/console-errors.spec.ts` exists
    - File contains the strings `'/consultations'` and `'/treatment-abroad'`
    - File contains `page.on('console'`
    - File contains `attribute rx`
    - `pnpm exec playwright test tests/visual/console-errors.spec.ts` exits 0 when run against a dev server with the Task 1 fix landed
  </acceptance_criteria>
  <done>Playwright spec asserts 0 console errors matching `attribute rx` on both routes; passes with Task 1 fix applied.</done>
</task>

</tasks>

<verification>
- All 4-value `rx` attribute strings eliminated from the two affected TSX files (negative-grep across `next/src/components/sections/`).
- New Playwright spec exists, type-checks, and passes when executed against a live dev server.
- `pnpm --dir next build` and `pnpm --dir next lint` exit 0.
- Manual sanity check: open `/consultations` in DevTools → Console → reload → 0 `<rect> attribute rx` errors. Repeat for `/treatment-abroad`. (Optional; the Playwright spec replaces this for CI, but a one-time human glance is cheap.)
</verification>

<success_criteria>
- POL-01 success criterion 1 (REQUIREMENTS.md) satisfied: 0 `<rect> attribute rx` errors on both routes, Playwright-verified.
- 0 unsanctioned 4-value `rx` attribute strings remain in `next/src/`.
- No regression in flag rendering (uniform 3px rounding on outer flag rect remains; previously-invalid stripes now have the same 3px rounding instead of no rounding — minor visual change, acceptable).
</success_criteria>

<output>
After completion, create `.planning/phases/94-polish-and-hygiene/94-01-SUMMARY.md` per the standard summary template, recording:
- The 7 attribute edits (line-by-line)
- The new spec file path
- Playwright run output (pass/fail counts)
- Commit hash that lands the change
</output>
