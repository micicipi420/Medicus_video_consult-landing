---
phase: 94-polish-and-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements: [POL-04]
must_haves:
  truths:
    - "Untracked screenshot PNGs at repo root no longer appear in `git status`"
    - "Future audit screenshot patterns are covered by .gitignore so they don't pollute the working tree"
    - ".gitignore additions are explicit and named — no broad `*.png` ban that would block legitimate image assets"
  artifacts:
    - path: ".gitignore"
      provides: "Augmented ignore patterns for audit-screenshot prefixes"
      contains: "90-*.png"
  key_links:
    - from: ".gitignore"
      to: "git status"
      via: "ignore-pattern matching"
      pattern: "90-*.png|91-*.png|93-uat-*.png|v8-*.png|9?-*.png"
---

<objective>
Stop tracking audit-screenshot PNGs that have accumulated at the repo root over Phases 90-93 (`90-*.png`, `91-*.png`, `93-uat-*.png`, `v8-*.png`). Add named-prefix patterns to `.gitignore` so they don't show up in `git status` and don't slip into commits. Closes POL-04.

Purpose: Clean working tree for future GSD plan/execute cycles. Prevents screenshot blobs from being committed accidentally. Locks in a convention for future-phase audit screenshots (Phase 95 audits will produce more).

Output: Augmented `.gitignore` with named-prefix patterns; clean `git status` working tree (excluding legitimately new files like `.mcp.json`, `design/`, planning docs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add named-prefix screenshot patterns to .gitignore</name>
  <files>.gitignore</files>
  <read_first>.gitignore</read_first>
  <action>The existing `.gitignore` already covers `audit-*.png`, `current-site-*.png`, `github-pages-target.png`, and `screenshots/`. Augment with new patterns capturing the Phase 90+ audit screenshot prefixes. Recommended approach: keep the patterns explicit by named prefix rather than using a broad `*.png` rule, because legitimate assets in `next/public/` are also PNGs.

Append the following block to `.gitignore` BELOW the existing "Audit / scratch artifacts at repo root" section (currently around lines 28-32):

```
# v9.0 / v9.0.1 audit & UAT screenshots at repo root (per phase prefix)
90-*.png
91-*.png
92-*.png
93-uat-*.png
93-*.png
94-*.png
95-*.png
v8-*.png
v9-*.png
# Sweep glob for any 9{N}-*.png at root, future-proofs Phase 96+ work
9?-*.png
```

Do NOT add `*.png` (would mask `next/public/*.png` and any future image assets in tracked directories — `.gitignore` is project-rooted, so a top-level `*.png` covers ALL .png files anywhere; that's wrong).

Do NOT touch any existing pattern (the `audit-*.png` block must remain intact — it's used by older Phase 78/89 audits).

Do NOT relocate the actual screenshot files into `.planning/audit-screenshots/`. The cleanup-todo gave both options; gitignore is cheaper, leaves files in place for the user to triage manually, and POL-04 success criterion accepts either option.

After editing, run `git status --porcelain | grep -E '^\?\? (90|91|92|93|94|95|v8|v9)-' || echo OK` — should print `OK` (no untracked PNGs matching the new patterns).

The user explicitly delegated technical decisions (per MEMORY.md autonomous-decisions feedback). Default to gitignore-only; do NOT prompt the user.</action>
  <verify>
    <automated>grep -c '90-\*.png' .gitignore | awk '{exit !($1 &gt;= 1)}' &amp;&amp; git status --porcelain | grep -E '^\?\? (9[0-9]|v[89])-.*\.png$' ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the literal lines `90-*.png`, `91-*.png`, `93-uat-*.png`, `v8-*.png`
    - `.gitignore` contains the line `9?-*.png` (sweep pattern for future phases)
    - `git status --porcelain` does NOT list any `?? 90-*.png`, `?? 91-*.png`, `?? 93-uat-*.png`, or `?? v8-*.png` lines
    - `.gitignore` still contains the original `audit-*.png` line (not regressed)
    - `.gitignore` does NOT contain a bare `*.png` line (would mask legitimate PNG assets)
  </acceptance_criteria>
  <done>.gitignore augmented; working tree no longer reports the audit screenshots as untracked; existing patterns preserved.</done>
</task>

</tasks>

<verification>
- `git check-ignore -v 90-medicusunion-com.png 93-uat-checkup-desktop.png v8-index-desktop.png` reports the new patterns matching each.
- `git status --porcelain | grep -E 'png$'` returns only legitimately tracked / staged image files (none of the audit prefixes).
- No regression in existing `.gitignore` blocks (all original lines still present).
</verification>

<success_criteria>
- POL-04 success criterion 4 satisfied (REQUIREMENTS.md): untracked screenshots no longer in `git status` working tree (or relocated; chose gitignore option per autonomous delegation).
- Future audit screenshots prefixed by phase (`94-*`, `95-*`, etc.) are pre-emptively covered by the `9?-*.png` sweep glob.
</success_criteria>

<output>
After completion, create `.planning/phases/94-polish-and-hygiene/94-03-SUMMARY.md` recording:
- The exact `.gitignore` patch (diff hunk)
- `git check-ignore -v` output proving the new patterns match
- `git status --porcelain` before/after counts for the affected prefixes
- Commit hash
</output>
